# Capture Profitable Trade Percentage in Summary Net

The objective is to enhance the `_summary_net.json` file by adding `buyPercent` and `sellPercent` fields for each product/strategy/direction combination. This will provide an ongoing percentage of trades that are profitable.

## Proposed Changes

### Summary Net Generator

#### [MODIFY] [summary_net_generator.py](file:///c:/Users/edebe/eds/TradeApps/breakout/summary_net_generator.py)

- Update `cumulatives` initialization to include `buy_prof_cnt` and `sell_prof_cnt`.
- Update the trade processing loop to increment these profitable counters if `net_return > 0`.
- Update the `data_store` entry to calculate and include `buyPercent` and `sellPercent`.
- Add comments with timestamp and version tag.

### Constants

#### [MODIFY] [constants.py](file:///c:/Users/edebe/eds/TradeApps/breakout/constants.py)

- Update `VERSION` to `V20260115_0415`.

## Verification Plan

### Automated Tests
- Monitor the `summary_gen_debug.log` for any errors after the update.
- Manually inspect the generated `_summary_net.json` to ensure the new fields are present and correctly calculated.

### Manual Verification
- Check the calculated percentages against a few manual samples from the trade JSON files.
