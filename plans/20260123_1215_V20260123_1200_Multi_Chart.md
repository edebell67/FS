# Plan - V20260123_1200 - Multi-Chart P&L Grid Implementation

## 1. Understanding of Requirements
- The user wants a new screen ("Screen 2") that displays multiple smaller chart cards in a grid layout.
- This screen is a copy of the P&L Graph ("Screen 1") but optimized for side-by-side comparison of different strategies or products.
- Objective is to have a "Multi-Overlay" view where each overlay gets its own dedicated chart card.

## 2. Plan of Approach
1.  **Create `multi_chart.html`**:
    - Re-use the premium sidebar, header, global config, and playback simulation bar from `pnl_graph.html`.
    - Replace the single large `graph-wrapper` with a dynamic `chart-grid` (`main` element).
    - Add custom CSS for `.chart-card` and `.chart-grid` to ensure a responsive flex/grid layout.
2.  **Create `multi_chart.js`**:
    - Adapt the state management from `pnl_graph.js`.
    - Refactor `chart` state into an object `charts = {}` to track multiple Chart.js instances.
    - Implement `updateCharts()` loop that ensures a card/canvas exists for every `activeKey`.
    - Sync X-axis (min/max) and playback state across all grid charts.
3.  **Update Sidebar**:
    - Add "Multi Chart" link to `sidebar.html` with icon `fa-th-large`.
4.  **BUMP VERSION** to `V20260123_1200`.

## 3. List of Changes
- `fs/multi_chart.html`: 
    - [x] Initial structure with chart grid layout. (2026-01-23 12:00)
- `fs/multi_chart.js`:
    - [x] Implementation of multi-chart logic, playback sync, and auto-ranking. (2026-01-23 12:05)
- `fs/sidebar.html`:
    - [x] Added navigation link for Multi Chart. (2026-01-23 12:10)
    - [x] Updated footer version. (2026-01-23 12:12)
- `fs/constants.py`:
    - [x] Update version to `V20260123_1200`. (2026-01-23 12:11)

## 4. Verification Results
- [x] HTML/JS files created and cross-linked.
- [x] Sidebar integration confirmed.
- [x] Multi-chart syncing logic verified via code review.
