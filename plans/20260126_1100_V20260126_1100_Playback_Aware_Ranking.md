# Plan: Playback-Aware Smart Ranking

Date: 2026-01-26
Version: V20260126_1100

## 1. Objective
Enable "Smart Rank" (Auto Top) to be context-aware of the current playback time. Currently, it uses the end-of-day trade totals for filtering/sorting even if the user is simulating the start of the day.

## 2. Plan of Approach
1.  **Modify `multi_chart_v3.js`**:
    *   Inside `applyRankSelection()`, identify the `trade_count` at the current `evalMs` (playback/slider position).
    *   Apply the `minTrades` filter against this "count-so-far" instead of the session final.
    *   Sort candidates by this "count-so-far" when the user selects "Trade Count" as the basis.
2.  **Maintenance**:
    *   Update version string in `constants.py` and UI files.

## 3. List of Changes
- [x] **`c:\Users\edebe\eds\TradeApps\breakout\fs\multi_chart_v3.js`**:
    *   Update `applyRankSelection()` to correctly calculate `tradesAtEval`.
- [x] **`c:\Users\edebe\eds\TradeApps\breakout\fs\constants.py`**:
    *   Update `VERSION` to `V20260126_1100`.
- [x] **`c:\Users\edebe\eds\TradeApps\breakout\fs\multi_chart_v3.html`**:
    *   Update script version.

## 4. Verification
- Select a date and drag the slider to 8:00 AM.
- Set "Min Trades" to 1.
- Click "Auto Top".
- Verify that only models that had traded BY 8:00 AM are added.
- Verify that sorting correctly reflects the rank at that point in time.
