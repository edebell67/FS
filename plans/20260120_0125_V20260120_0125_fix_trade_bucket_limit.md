# Plan: Fix Trade Bucket Creation Limit and API Response

**Date**: 2026-01-20 01:25
**Version**: V20260120_0125
**Description**: Fix the missing `max_strategies` in the Trade Bucket API response which prevents users from creating buckets with more than 5 strategies.

## Proposed Changes

### 1. Backend Fix (`trade_viewer_api.py`)
- [ ] Update `get_trade_buckets` to include `max_strategies` in its JSON response.
- [ ] Ensure it calls `get_trade_bucket_limit()` to fetch the value from the current config.

### 2. Versioning & Cache Busting
- [ ] Update `VERSION` in `constants.py` to `V20260120_0125`.
- [ ] Update version strings in `pnl_graph.html` for CSS/JS to force browser refresh.

## Verification Plan
1. [ ] Check the API response for `GET /api/trade_buckets` and verify `max_strategies` is present.
2. [ ] Add 6-10 overlays to the P&L Graph.
3. [ ] Click "Create Bucket" and verify it no longer blocks at the 5-strategy limit (assuming config allows more).
4. [ ] Verify the bucket is successfully created and persisted to the DB.
