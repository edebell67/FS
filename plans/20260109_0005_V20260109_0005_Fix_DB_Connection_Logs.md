# Plan - Fix Database Connection Logs (V20260109_0005)

The goal is to ensure that when the API server connects to a database, the log messages accurately reflect the database name (e.g., "Tradedb_sim2" vs "Tradedb") and are visible at the INFO level.

## 1. Understanding of Requirements
- User reports that in simulation mode, the log message still says "Tradedb" instead of "Tradedb_sim2".
- Discovered that the current log message in `api_server_sql\main.py` is at the `DEBUG` level and doesn't explicitly state the database name in the "success" message.
- The user also reports no trade files are created in simulation mode, which I identified is due to empty auto-activations (empty frequency view in `tradedb_sim2`).

## 2. Plan of Approach
1.  **Modify `api_server_sql\main.py`**:
    *   Ensure `connect_to_db` logs the database name at `INFO` level upon successful connection.
    *   Update the "Connecting to..." message to `INFO` level.
2.  **Modify `api_server_sql\main copy.py`**:
    *   Apply similar changes for consistency, in case it's being used.
3.  **Update `constants.py`**:
    *   Update VERSION to `V20260109_0005`.
4.  **Verify Logs**:
    *   Restart (or simulate restart) of the API server and check output.

## 3. List of Changes
- [x] `api_server_sql\main.py`: Update `connect_to_db` logging.
- [x] `api_server_sql\main copy.py`: Update `connect_to_db` logging.
- [x] `api_server_sql_copy\main.py`: Update `connect_to_db` logging.
- [x] `TradeApps\breakout\constants.py`: Update version number.

## 4. Verification
- Confirm that the log message now shows the correct database name passed via the `db` parameter.
