# Plan: Trade Drilldown Enhancements (V3)

Date: 2026-01-26
Version: V20260126_1215

## 1. Objective
Enhance the Trade Drilldown modal in Multi-Chart V3 to:
1.  Display the metric type ('A' or 'N') for each trade.
2.  Enable client-side column sorting for the trade list.

## 2. Plan of Approach
1.  **Modify `multi_chart_v3.html`**:
    *   Add a "Type" column header to the drilldown table.
    *   Add `onclick` events to all headers for sorting.
    *   Add sort icons (Up/Down) for visual feedback.
    *   Add sticky headers and hover states for better usability.
2.  **Modify `multi_chart_v3.js`**:
    *   Add a global state `currentDrilldownTrades` to track the data being viewed.
    *   Update `showTradeDrilldown()` to populate the `type` field and call a central `renderDrilldownTable()` function.
    *   Implement `sortDrilldownTable(columnIdx)` to handle sorting logic (Time, Model, Type, Signal, Result, Total).
    *   Ensure numeric columns (Result, Total) sort numerically.
3.  **Versioning**: Update to `V20260126_1215`.

## 3. List of Changes
- [x] **`c:\Users\edebe\eds\TradeApps\breakout\fs\multi_chart_v3.html`**:
    *   Revise modal table headers.
- [x] **`c:\Users\edebe\eds\TradeApps\breakout\fs\multi_chart_v3.js`**:
    *   Update drilldown rendering and add sorting logic.
- [x] **`c:\Users\edebe\eds\TradeApps\breakout\fs\constants.py`**:
    *   Update version to `V20260126_1215`.

## 4. Verification
- Open Drilldown (double-click card).
- Verify 'A' (Alt) or 'N' (Net) appears in the Type column.
- Click headers (e.g., "Result") and verify rows sort correctly.
