# Plan: Multi-Chart V2 (SQL Backend Restoration)

Date: 2026-01-24
Version: V20260124_2100

## 1. Objective
Create a new version of the Multi-Chart Grid that uses a SQL-backed API endpoint instead of local JSON files.

## 2. Plan of Approach
1.  **New HTML File**: Create `multi_chart_v2.html` by cloning and adapting `multi_chart.html`.
2.  **New JS File**: Create `multi_chart_v2.js` by cloning and adapting `multi_chart.js`.
3.  **Data Source**: Update data fetching to use `http://127.0.0.1:8001/api/vwCombined_trades_closed_output_top200?db=tradedb`.
4.  **Structure Adaptation**:
    *   The API returns `{ "metadata": [...], "data": [...] }`.
    *   The `data` array contains flat objects with `model` (as strategy), `product`, `created` (as timestamp), `net_return_sum`, and `alt_net_return_sum`.
    *   `processedSeries` will be populated by grouping this flat list.
    *   `net_return_sum` will map to the "Net" series.
    *   `alt_net_return_sum` will map to the "Alt" series.
5.  **UI Updates**:
    *   Maintain "Buy Net" / "Sell Net" options as requested.
    *   Ensure they map to `buy_net_return_sum` and `sell_net_return_sum`.
6.  **SQL View Modification**:
    *   Updated `vwCombined_trades_closed_output_top200` to include `buy_net_return_sum`, `sell_net_return_sum`, and `signal`.
7.  **Navigation**: Update `sidebar.html` to include a link to the new page.

## 3. List of Changes
- [x] Create `c:\Users\edebe\eds\TradeApps\breakout\fs\multi_chart_v2.html`
- [x] Create `c:\Users\edebe\eds\TradeApps\breakout\fs\multi_chart_v2.js`
- [x] Update `c:\Users\edebe\eds\TradeApps\breakout\fs\sidebar.html`
- [x] Verify data binding and display.
- [x] Fix dropdown visibility (white background issue).

## 4. Verification
- Open `multi_chart_v2.html` in browser via proxy or direct access (if applicable).
- Confirm data loads from SQL API.
- Confirm charts render correctly with cumulative P&L.
- Verify playback and ranking functionality still work with the new data structure.
