# Plan: Sync Live Mode and Legend Style to Multi-Chart V2 [V20260126_1230]

This plan outlines the integration of the "Live Mode" toggle and refined legend styling from Multi-Chart V3 into Multi-Chart V2 for consistency.

## 1. Requirements
*   Implement "Live Mode" toggle in Multi-Chart V2 (UI + JS logic).
*   Sync live status with the SQL API (port 8001) on startup and toggle.
*   Update legend labels in V2 to match the V3 aesthetic (e.g., `(A)` instead of `[A]`).
*   Ensure pulse animations and live button styling are consistent.

## 2. Plan of Approach

### A. Versioning
*   [ ] Update `constants.py` to `V20260126_1230`.

### B. UI Enhancements (Multi-Chart V2 HTML)
*   [ ] Add pulse animation CSS and `.live-btn` styling to `multi_chart_v2.html`.
*   [ ] Update script reference version.

### C. Logic Implementation (Multi-Chart V2 JS)
*   [ ] Add `loadGridLiveStatus()`: Fetches `/api/grid_live` from port 8001 and maps to `activeOverlays`.
*   [ ] Add `toggleLiveMode(groupName)`: Posts toggle status to port 8001.
*   [ ] Update `createGroupCard()`:
    *   Find if group is live.
    *   Add the `<button class="live-btn ...">` element to the header.
*   [ ] Update `updateCharts()` legend:
    *   Change labels to use `(A)`, `(B)`, `(S)`, `(N)` with the V3-style bold colored prefixes.
*   [ ] Call `loadGridLiveStatus()` inside `initEventHandlers` or similar initialization point.

### D. Verification
*   [ ] Check V2 for the Live button on cards.
*   [ ] Verify toggling updating port 8001 (via console or network log).
*   [ ] Verify legend styling matches V3.

## 3. Checklist
*   [ ] `constants.py` updated.
*   [ ] `multi_chart_v2.html` CSS and version updated.
*   [ ] `multi_chart_v2.js` logic added and updated.
*   [ ] Legend styles synchronized.
*   [ ] Verified all changes.
