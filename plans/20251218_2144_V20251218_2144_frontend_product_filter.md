# 20251218_2144_V20251218_2144_Frontend_Product_Filter

## 1. Understanding of Requirements
The goal is to add a product selection filter to the `trade_viewer.html` dashboard to support the multi-product backend (GBP, EUR, etc.). This ensures separate summary cards for different products using the same strategy.

## 2. Plan of Approach

### Frontend (trade_viewer.html)
1. **Product Selection UI**: Add a multi-select dropdown for "Product" in the controls section.
2. **Dynamic Population**: Implement `populateProductFilter(trades)` to detect available products from the loaded trade data and populate the dropdown.
3. **Filtering Logic**: Update `loadTrades` to apply client-side filtering based on selected products.
4. **Grouping Logic**: Update `groupTrades` to use a key that includes the product: `${trade.app_name}_${trade.product}_${trade.strategy}`. This ensures separate cards for different products using the same strategy.
5. **UI Display**: Update `renderSummary` to show the product name on each card.

### API (trade_viewer_api.py)
* [x] Verified: `product` is consistently returned in the trade objects.

## 3. List of Changes

### Project Setup
- [x] Create `c:\Users\edebe\eds\TradeApps\breakout\constants.py` with `VERSION = "V20251219_0947"`. <!-- id: setup_0 -->
- [x] Update `common.py` `_create_tradeable_json` filename format for TWS orders. <!-- id: setup_1 -->
- [x] Implement dynamic `quantity` calculation based on `trade_qty_percent` in `config.json`. <!-- id: setup_2 -->
- [x] Fix `DEFAULT_TRADE_PRODUCTS` bug to correctly load products from `config.json`. <!-- id: setup_3 -->
- [x] Refactor `_is_profitability_guard_passed` to use disk-based JSON logs instead of in-memory `trade_log`. <!-- id: setup_4 -->
- [x] Implement `State Persistence` (configurable) to resume open trades on script restart. <!-- id: setup_5 -->
- [x] Add `Configuration Editor` pop-up and button to the trade viewer. <!-- id: setup_6 -->
- [x] Add `guidePrice` to TWS order JSON (Ask for BUYS, Bid for SELLS). <!-- id: setup_7 -->

### `trade_viewer.html`
- [x] Add `<div class="control-group">` with product dropdown. <!-- id: 0 -->
- [x] Implement `populateProductFilter()`. <!-- id: 1 -->
- [x] Modify `loadTrades()` to filter by product. <!-- id: 2 -->
- [x] Modify `groupTrades()` to include product in grouping key. <!-- id: 3 -->
- [x] Modify `renderSummary()` to display product name. <!-- id: 4 -->

## 4. Verification Plan
- [ ] Load data with multiple products (e.g. GBP, EUR).
- [ ] Verify product dropdown is populated.
- [ ] Verify filtering by single/multiple products works.
- [ ] Verify separate cards for same strategy but different products.
