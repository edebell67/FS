## Plan for Updating `05_str_topPerformerAgeEntryPrice_live.py` for New Price Endpoint

### 1. Understanding of Requirements:
The `get_live_prices()` function in `05_str_topPerformerAgeEntryPrice_live.py` needs to be updated to correctly parse the new JSON format from the `http://127.0.0.1:8000/api/vw_000_fx_quotes` endpoint. The new format uses `code` instead of `product` for currency identification and requires `mid_price` to be calculated from `bid` and `ask`.

### 2. Plan of Approach (Checklist):

*   **`algo_viewer/strategy_library/05_str_topPerformerAgeEntryPrice_live.py`**:
    *   [x] Modify `get_live_prices()` function:
        *   [x] Change `price_data.get('product')` to `price_data.get('code')` when filtering for `PRODUCT_TO_TRADE`.
        *   [x] Ensure `bid` and `ask` values are converted to `float`.
        *   [x] Calculate `mid_price` as `(bid + ask) / 2`.
        *   [x] Update error handling to catch `ValueError` for float conversion.

### 3. Version Update:
*   [x] Update `VERSION` in `05_str_topPerformerAgeEntryPrice_live.py` to `1.0.1`.
