### Quant-Trading Omni Dashboard Prototype Enhancement Plan

**Version: 1.0.0**
**Date: 2025-08-01**

---

### **Phase 1: Core UI/UX Modernization & Data Refinement**

#### **1.1 Global Styling & Theming**
*   **Objective**: Implement a modern, visually appealing dark/light theme and typography.
    *   [x] **Task 1.1.1**: Update `src/index.css` to define a more sophisticated color palette for both light and dark themes using CSS variables.
    *   [x] **Task 1.1.2**: Select and implement a modern, clean font family (e.g., via Google Fonts) in `src/index.css`.
    *   [x] **Task 1.1.3**: Refine global Tailwind CSS configurations in `tailwind.config.js` for spacing, typography, and color usage.
    *   [x] **Task 1.1.4**: Ensure `App.css` is cleaned up, removing any conflicting or redundant styles.

#### **1.2 `App.tsx` Layout & Hero Section**
*   **Objective**: Redesign the main dashboard layout for a modern aesthetic and improved user experience.
    *   [x] **Task 1.2.1**: Modify `App.tsx` to implement a visually engaging hero section for top-level metrics.
        *   [x] **Task 1.2.1.1**: Change the current `div` structure for top-level metrics to a card-based layout.
        *   [x] **Task 1.2.1.2**: Apply subtle animations or gradients to the metric cards for visual appeal.
    *   [x] **Task 1.2.2**: Improve the navigation/view switcher.
        *   [x] **Task 1.2.2.1**: Redesign the view switcher (`Performance`, `Risk`, etc.) from a basic `div` and `button` structure to a more modern tab or segmented control design using Tailwind CSS.
    *   [x] **Task 1.2.3**: Integrate header buttons more seamlessly.
        *   [x] **Task 1.2.3.1**: Replace plain text "Trade" and "Notifications" buttons with appropriate icons (e.g., from a free icon library like Heroicons, if available via CDN or simple import).
        *   [x] **Task 1.2.3.2**: Adjust button styling in the header for a cleaner, more integrated look.

#### **1.3 Data Layer Refinement**
*   **Objective**: Prepare mock data for richer and more accurate visualizations.
    *   [x] **Task 1.3.1**: Create a new mock data file `src/data/correlation-data.json` with realistic correlation values between strategies.
        *   [x] **Task 1.3.1.1**: Define a schema for `correlation-data.json` that includes strategy IDs and their pairwise correlation coefficients.
        *   [x] **Task 1.3.1.2**: Populate `correlation-data.json` with sample data.
    *   [x] **Task 1.3.2**: Update `src/data/portfolios.json` to include historical P&L data suitable for sparklines.
        *   [x] **Task 1.3.2.1**: Add a `historical_pnl` array (e.g., `[100, 105, 102, 108, 110]`) to each portfolio object in `portfolios.json`.
    *   [x] **Task 1.3.3**: Review and potentially expand other mock data files (`strategies.json`, `trades.json`) for future enhancements, ensuring consistency.

---

### **Phase 2: Component Enhancements & Interactivity**

#### **2.1 `PerformanceTiles.tsx` Enhancement**
*   **Objective**: Redesign portfolio tiles and add sparklines.
    *   [x] **Task 2.1.1**: Redesign the individual portfolio tiles in `PerformanceTiles.tsx` with a cleaner, more modern aesthetic (e.g., rounded corners, subtle shadows, improved typography).
    *   [x] **Task 2.1.2**: Implement sparklines for historical P&L trends.
        *   [x] **Task 2.1.2.1**: Import `LineChart`, `Line`, and `ResponsiveContainer` from Recharts into `PerformanceTiles.tsx`.
        *   [x] **Task 2.1.2.2**: For each portfolio tile, render a `ResponsiveContainer` with a `LineChart` and a `Line` component, using the `historical_pnl` data.
        *   [x] **Task 2.1.2.3**: Style the sparklines to be compact and visually appealing within the tile.
    *   [x] **Task 2.1.3**: Add subtle hover effects to portfolio tiles (e.g., slight scale-up, border highlight).

#### **2.2 `CorrelationMatrix.tsx` Visualization**
*   **Objective**: Replace the basic grid with a Recharts heatmap.
    *   [x] **Task 2.2.1**: Modify `CorrelationMatrix.tsx` to import `Heatmap` (or similar component) from Recharts.
    *   [x] **Task 2.2.2**: Load the new `correlation-data.json` instead of `strategies.json` for correlation values.
    *   [x] **Task 2.2.3**: Render a Recharts heatmap component, mapping the correlation data to the heatmap's `x`, `y`, and `value` props.
    *   [x] **Task 2.2.4**: Implement tooltips on hover for the heatmap cells to display the exact correlation value.
    *   [x] **Task 2.2.5**: Style the heatmap for clarity and visual appeal, including color gradients for correlation strength.

#### **2.3 `Watchlist.tsx` Improvement**
*   **Objective**: Enhance the visual presentation and functionality of the watchlist.
    *   [x] **Task 2.3.1**: Redesign the visual presentation of watchlist items (e.g., use cards or distinct list items with more padding and clear separation).
    *   [x] **Task 2.3.2**: For watched portfolios, display key metrics like current P&L and YTD return alongside the name.
    *   [x] **Task 2.3.3**: For watched strategies, display a relevant key metric (e.g., number of active trades, win rate).
    *   [x] **Task 2.3.4**: Implement a simple drag-and-drop reordering functionality for watchlist items (using a lightweight library or custom logic if feasible).

#### **2.4 `NotificationPanel.tsx` Functionality & Design**
*   **Objective**: Improve the notification panel's interactivity and visual design.
    *   [x] **Task 2.4.1**: Implement a slide-in/slide-out animation for the `NotificationPanel` using Tailwind CSS transitions and `useState` to control its visibility.
    *   [x] **Task 2.4.2**: Add a "Clear All" button to dismiss all notifications.
    *   [x] **Task 2.4.3**: Add a "Dismiss" button for individual notifications.
    *   [x] **Task 2.4.4**: Introduce different notification types (e.g., `success`, `warning`, `error`) with distinct background colors and icons.

#### **2.5 `TradeExecutionModal.tsx` Design**
*   **Objective**: Create a more realistic and user-friendly trade execution form.
    *   [x] **Task 2.5.1**: Design a basic form within `TradeExecutionModal` with input fields for:
        *   Instrument (text input or dropdown for common symbols).
        *   Quantity (number input).
        *   Order Type (dropdown: Market, Limit, Stop).
        *   Side (radio buttons: Buy, Sell).
    *   [x] **Task 2.5.2**: Add a "Submit Trade" button.
    *   [x] **Task 2.5.3**: Implement basic client-side validation (e.g., quantity must be positive).
    *   [x] **Task 2.5.4**: Add a confirmation step before "executing" the trade (e.g., a simple `alert` or a small confirmation modal).

---

### **Phase 3: Advanced Features & Polish**

#### **3.1 Multi-Perspective Switcher Content**
*   **Objective**: Provide placeholder content for other dashboard views.
    *   [x] **Task 3.1.1**: In `App.tsx`, add simple placeholder `div` elements for `Risk`, `Liquidity`, and `Compliance` views, indicating "Content coming soon" or similar.

#### **3.2 `SkeletonLoader.tsx` Refinement**
*   **Objective**: Improve the visual fidelity of skeleton loaders.
    *   [x] **Task 3.2.1**: Modify `SkeletonLoader.tsx` to accept props for `width` and `height` to allow more flexible sizing.
    *   [x] **Task 3.2.2**: In `App.tsx`, use the refined `SkeletonLoader` components to more closely mimic the actual content layout for each section (e.g., a row of smaller skeletons for metric cards, a larger one for the main content area).

#### **3.3 Responsiveness**
*   **Objective**: Ensure the dashboard is fully responsive across devices.
    *   [x] **Task 3.3.1**: Review all components and `App.tsx` for responsiveness using Tailwind's responsive prefixes (e.g., `sm:`, `md:`, `lg:`).
    *   [x] **Task 3.3.2**: Adjust grid layouts, font sizes, and padding to ensure optimal display on mobile, tablet, and desktop viewports.