# Plan: Transparent Modals without Blur [V20260126_1330]

This plan updates the modal styling to be transparent while removing all background blur effects to ensure the underlying page remains legible.

## 1. Requirements
*   **Remove Blur**: Specifically remove `backdrop-filter: blur(...)` from `.modal` and `.modal-content`.
*   **Transparency**: Maintain a transparent look so the background is visible.
*   **Consistency**: Apply to Standard, V2, and V3 dashboards.

## 2. Plan of Approach

### A. Style Updates (`multi_chart.html`, `v2.html`, `v3.html`)
*   **`.modal`**: 
    *   Change `background-color` to `rgba(0, 0, 0, 0.2)` (Lighter shadow).
    *   Delete `backdrop-filter: blur(8px)`.
*   **`.modal-content`**:
    *   Change `background` to `rgba(15, 23, 42, 0.85)` (Dark but transparent slate to keep text readable).
    *   Delete `backdrop-filter: blur(20px)`.
    *   Keep the thin `border` to define the container.

### B. Versioning
*   Update version to `V20260126_1330`.

## 3. Checklist
*   [ ] `multi_chart.html`: Blur removed, transparency adjusted.
*   [ ] `multi_chart_v2.html`: Blur removed, transparency adjusted.
*   [ ] `multi_chart_v3.html`: Blur removed, transparency adjusted.
*   [ ] `constants.py`: Version updated to `V20260126_1330`.

## 4. Verification
*   Open any dashboard.
*   Click '+' to add a chart.
*   Confirm you can see the charts/text underneath the modal without any blurring.
