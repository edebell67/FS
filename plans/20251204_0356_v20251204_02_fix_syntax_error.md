# Fix Syntax Error in Trade Engine (2025-12-04)

## 1. Understanding of Requirements
The user reported a `SyntaxError: expected 'except' or 'finally' block` in `algo_viewer/tradepanel/trade_engine.py`.
This is caused by incorrect indentation of an `except` block in `_forex_price_update_loop` and missing `except` blocks in `_fetch_rt_group_metrics` and `_periodic_trade_lifecycle_snapshot_loop`.

## 2. Plan of Approach
1.  **Correct Indentation**: Fix the indentation of the `except` block in `_forex_price_update_loop` to match its corresponding `try` block.
2.  **Add Missing Except Blocks**: Add missing `except Exception as e:` blocks in `_fetch_rt_group_metrics` and `_periodic_trade_lifecycle_snapshot_loop` to properly catch exceptions and define the variable `e` used in error logging.
3.  **Verify Indentation**: Ensure `wait` calls are correctly indented.

## 3. List of Changes
*   **`algo_viewer/tradepanel/trade_engine.py`**:
    *   [ ] Fix indentation of `except` block in `_forex_price_update_loop` (around line 9894).
    *   [ ] Add `except Exception as e:` in `_fetch_rt_group_metrics` (before line 10121).
    *   [ ] Add `except Exception as e:` in `_periodic_trade_lifecycle_snapshot_loop` (before line 10273).
*   **`algo_viewer/tradepanel/Constants.py`**:
    *   [ ] Update version to `v20251204_02`.
