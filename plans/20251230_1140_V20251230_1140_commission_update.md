# Plan for Commission Update (V20251230_1140)

## 1. Understanding of Requirements
Modify the commission calculation in `common.py`:
*   Trades for products ending in `_C` (case-insensitive) should be charged a commission of $10.
*   All other trades should be charged a commission of $5.

## 2. Plan of Approach
1.  **Update Global Constant**: Modify the default value of `COMMISSION_USD` from 10.0 to 5.0 in the configuration loading section.
2.  **Update Calculation Logic**: Modify the `calculate_pnl` method in `BaseBreakoutStrategy` to use 10.0 for `_C` trades instead of the current 20.0, and continue using the (now updated) `COMMISSION_USD` for others.
3.  **Version Update**: Increment the version number in `constants.py`.
4.  **Verification**: Confirm that the code correctly distinguishes between `_C` products and others.

## 3. List of Changes
*   **`TradeApps/breakout/common.py`**:
    *   [ ] Change `COMMISSION_USD` default from 10.0 to 5.0 (Line 115).
    *   [ ] Update `calculate_pnl` to set `commission_usd_for_trade = 10.0` for `_C` products (Line 621).
*   **`TradeApps/breakout/constants.py`**:
    *   [ ] Update `VERSION` to `V20251230_1140`.

## 4. Confirmation Required
Please confirm the plan.
