# Handover Plan: UI Restoration & Data Consistency
Date: 2026-01-21 16:55
Version: V20260121_1700

## 1. Identified Issues
- [ ] **Parm Column 'undefined'**: Mapping mismatch between `params` (API) and `parm` (UI).
- [ ] **Drill-down Broken**: `showDrillDown` filter logic fails because it uses old JSON keys instead of PG keys (`app_name` vs `script_name`).
- [ ] **Auto Buy/Sell Disconnected**: Buttons do not reflect or update the true activation state.
- [ ] **P&L Inconsistency**: Graph and Table show different values for the same date/product.

## 2. Block 1: UI Restoration (Mapping & Drill-down)
- Update `strategy_performance.html` -> `update_stats()` to map:
  - `parm` = `d.params`
  - `parm_raw` = `d.params`
- Update `showDrillDown()` filter:
  - Match `t.script_name` instead of `t.app_name`.
  - Match `t.strategy_key` instead of `t.strategy`.

## 3. Block 2: Activation Integration
- Ensure `/api/toggle_activation` in `trade_viewer_api.py` is writing to the PostgreSQL `activations` table.
- Update `isAutoActive()` in JS to reflect DB state.

## 4. Block 3: P&L Graph Sync
- Modify backend for `pnl_graph.html` to pull from `vw_trades_all` when date is CURRENT_DATE.
