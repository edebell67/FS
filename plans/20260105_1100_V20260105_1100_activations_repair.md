# Plan: Repair activations.json and Enforce Integrity (V20260105_1100)

## 1. Objective
Repair the corrupted `activations.json` file so that the Activation Explorer can load it correctly.

## 2. Root Cause Analysis
- `activations.json` is syntactically invalid (corrupted by overlapping writes or a failed edit).
- The API server catches the JSON error and returns an empty list, resulting in the "No activations found" message.

## 3. Plan of Approach
1.  **Reconstruct File**: Rewrite `activations.json` using the fragments discovered in the corrupted version to restore a valid JSON structure.
2.  **Verify UI**: Check if the Activation Explorer displays the cards after the repair.
3.  **Harden API**: (Optional) Improve error reporting in `trade_viewer_api.py` so the user knows IF the file is corrupted vs just empty.

## 4. List of Changes
- [x] **`activations.json`**: Rewrite with valid JSON structure.
- [x] **`trade_viewer_api.py`**: Add logging for JSON decode errors in `_load_activations`.

## 5. Verification
- [x] Activation Explorer shows the restored strategies.
