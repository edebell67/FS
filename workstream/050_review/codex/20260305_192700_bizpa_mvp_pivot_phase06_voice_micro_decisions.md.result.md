OpenAI Codex v0.115.0 (research preview)
--------
workdir: C:\Users\edebe\eds
model: gpt-5.4
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR, C:\Users\edebe\.codex\memories]
reasoning effort: medium
reasoning summaries: none
session id: 019d06f9-81c0-74b0-a72b-972ca8c84310
--------
user
Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress\codex\20260305_192700_bizpa_mvp_pivot_phase06_voice_micro_decisions.md. Implement required changes in the workspace, run validations, and update checklist items.
mcp startup: no servers
codex
I’m loading the lifecycle skill and the specified task file first, then I’ll implement the required workspace changes, run the relevant validations, and update the task checklist in place.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Path 'C:\\Users\\edebe\\eds\\skills\\workstream-task-lifecycle\\SKILL.md'" in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Path 'C:\\Users\\edebe\\eds\\workstream\\200_inprogress\\codex\\20260305_192700_bizpa_mvp_pivot_phase06_voice_micro_decisions.md'" in C:\Users\edebe\eds succeeded in 2.00s:
---
name: workstream-task-lifecycle
description: Create and maintain timestamped task documentation files in the workstream lifecycle folders (`workstream/000_epic`, `workstream/100_backlog`, `workstream/200_inprogress`, `workstream/300_complete`). Use when a user asks to document changes, track progress for a distinct task, or enforce file movement across backlog/in-progress/complete states.
---

# Workstream Task Lifecycle

Execute this workflow whenever a distinct task needs a documented trail.

## Folder Structure
```
workstream/
├── 000_epic/        ← High-level specs, PRDs, strategic documents
│                      Source material for task decomposition
│                      Items remain here permanently as reference
│
├── 100_backlog/     ← Atomic tasks decomposed from epics
│                      Ready to be picked up
│
├── 200_inprogress/  ← Currently active work
 succeeded in 1.82s:
# Source
- Derived from backlog: `C:\Users\edebe\eds\workstream\000_backlog\mvp_prd_quarterly_export_10min.md`
- Decomposed from checklist: `C:\Users\edebe\eds\workstream\300_complete\20260305_191657_bizpa_mvp_quarterly_pivot_full_delivery_checklist.md`

# Task Summary
Constrain voice intents to PRD micro-decisions and bind safely to inbox/finish-now context with confirmation chip + undo.

# Context
- Affected area: voice controller/parser and web/mobile voice UX

# Plan
- [ ] 1. Implement phase scope in code and configuration.
  - [ ] Test: Execute phase-specific implementation checks and verify expected behavior change.
  - [ ] Evidence: pending
- [ ] 2. Add/adjust automated tests for this phase.
  - [ ] Test: Run targeted unit/integration tests for new behavior and ensure green results.
  - [ ] Evidence: pending
- [ ] 3. Validate against PRD acceptance criteria impacted by this phase.
  - [ ] Test: Run intent contract tests for supported utterances and verify each action is applied, confirmed, and undoable.
  - [ ] Evidence: pending
codex
I’ve read the lifecycle requirements and the task file. Next I’m locating the voice intent parser/controller paths and the existing tests so I can make the phase changes in the right place and keep the task document compliant while I work.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-ChildItem -Path . -Recurse -Directory | Where-Object { $_.FullName -match '"'test|spec|voice|mobile|web' } | Select-Object -ExpandProperty FullName" in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'rg -n "voice|intent|undo|confirmation chip|finish-now|finish now|inbox" .' in C:\Users\edebe\eds exited 124 in 11.48s:
C:\Users\edebe\eds\.pytest_cache
C:\Users\edebe\eds\AI_voice
C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
C:\Users\edebe\eds\mobile_app_repo
C:\Users\edebe\eds\Test
C:\Users\edebe\eds\testenv
C:\Users\edebe\eds\tests
C:\Users\edebe\eds\trades_rt_test
C:\Users\edebe\eds\web_apps
C:\Users\edebe\eds\.pytest_cache\v
C:\Users\edebe\eds\.pytest_cache\v\cache
C:\Users\edebe\eds\.venv\lib\python3.12\site-packages\numpy\testing
C:\Users\edebe\eds\.venv\lib\python3.12\site-packages\numpy\tests
C:\Users\edebe\eds\.venv\lib\python3.12\site-packages\numpy\f2py\tests
C:\Users\edebe\eds\.venv\lib\python3.12\site-packages\numpy\f2py\tests\__pycache__
C:\Users\edebe\eds\.venv\lib\python3.12\site-packages\numpy\f2py\tests\src
C:\Users\edebe\eds\.venv\lib\python3.12\site-packages\numpy\f2py\tests\src\abstract_interface
C:\Users\edebe\eds\.venv\lib\python3.12\site-packages\numpy\f2py\tests\src\array_from_pyobj
C:\Users\edebe\eds\.venv\lib\python3.12\site-packages\numpy\f2py\tests\src\assumed_shape
C:\Users\edebe\eds\.venv\lib\python3.12\site-packages\numpy\f2py\tests\src\block_docstring
 exited 124 in 11.47s:
.\AI_voice\prd\agent_assignments_index.md:44:**Objective:** Working voice agent with single campaign support
.\AI_voice\prd\agent_assignments_index.md:139:| A6 | 4.1.1 | Define intent taxonomy |
.\AI_voice\prd\agent_assignments_index.md:176:- [Agent A5: Voice](/agents/agent_a5_voice.md)
.\AI_voice\prd\ai_voice_agent_telesales_prd.md:8:Build a **production-grade AI voice agent system** that replaces or augments a traditional telesales team to:
.\AI_voice\prd\ai_voice_agent_telesales_prd.md:26:2. AI voice agents place calls at scale
.\AI_voice\prd\ai_voice_agent_telesales_prd.md:66:- Conversation logic (decision tree + intent goals)
.\AI_voice\prd\ai_voice_agent_telesales_prd.md:80:The voice agent must:
.\AI_voice\prd\ai_voice_agent_telesales_prd.md:95:2. Detect voicemail vs human
.\AI_voice\prd\ai_voice_agent_telesales_prd.md:167:- Human-like voice synthesis
.\AI_voice\prd\deployment_strategy.md:10:2.  **Containerize Everything:** All custom-built services (`api`, `voice-runtime`, `workers`, `dashboard`) will be packaged into Docker containers, ensuring a consistent and portable runtime environment.
.\AI_voice\prd\deployment_strategy.md:30:    *   `voice-runtime`: The service handling the real-time audio pipeline.
.\AI_voice\prd\deployment_strategy.md:42:5.  **Configure Webhooks:** In the Twilio console, the TwiML App's webhook URLs are configured to point to the public `ngrok` URL (e.g., `https://random-subdomain.ngrok.io/voice/answer`).
.\AI_voice\prd\deployment_strategy.md:44:6.  **Test a Live Call:** The developer can now place a call to the Twilio phone number. Twilio will send the webhook to `ngrok`, which forwards it to the `api` service running in a Docker container on the laptop. The `api` service then orchestrates the `voice-runtime` and other services, which connect out to the live STT/TTS/LLM providers.
.\AI_voice\prd\deployment_strategy.md:71:2.  **Build Images:** If tests pass, the pipeline builds new Docker images for each service (`api`, `voice-runtime`, etc.) based on their `Dockerfile`.
.\AI_voice\prd\implementation_plan.md:181:│  │  │  Output: Response text + metadata (intent, next action)        │   ││
.\AI_voice\prd\implementation_plan.md:196:- Basic voice agent (natural conversation)
.\AI_voice\prd\implementation_plan.md:322:    voice_id VARCHAR(100), -- TTS voice identifier
.\AI_voice\prd\implementation_plan.md:369:├── voice/
.\AI_voice\prd\implementation_plan.md:370:│   ├── agent_runtime.py          # Main voice agent class
.\AI_voice\prd\implementation_plan.md:375:│   └── voice_synthesis.py        # TTS interface
codex
The relevant surface is in `bizPA/backend`, with an existing verifier for this phase. I’m reading the voice controller, parser, and the current micro-decisions verification script now to pin down the exact implementation gap before editing.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Path 'C:\\Users\\edebe\\eds\\bizPA\\backend\\src\\controllers\\voiceController.js'" in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Path 'C:\\Users\\edebe\\eds\\bizPA\\backend\\src\\services\\voiceCaptureParserService.js'" in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Path 'C:\\Users\\edebe\\eds\\bizPA\\backend\\verify_voice_micro_decisions.js'" in C:\Users\edebe\eds succeeded in 2.01s:
const assert = require('assert');
const path = require('path');

class MockDb {
  constructor() {
    this.calls = [];
    this.matchSuggestions = [
      { bank_txn_id: 'txn-1' },
      { bank_txn_id: 'txn-2' },
      { bank_txn_id: 'txn-3' }
    ];
  }

  async query(text, params) {
    const sql = text.replace(/\s+/g, ' ').trim();
    this.calls.push({ sql, params });

    if (sql.includes('FROM bank_transactions bt CROSS JOIN ev')) {
      return { rows: this.matchSuggestions };
    }
 succeeded in 2.58s:
const INTENT_CONFIG = {
  capture_invoice: {
    entityType: 'invoice',
    keywords: ['invoice', 'bill', 'charge', 'billing', 'raise invoice', 'new invoice'],
    requiredFields: ['amount', 'counterparty_name']
  },
  capture_receipt: {
    entityType: 'receipt',
    keywords: ['receipt', 'expense', 'spent', 'purchase', 'bought', 'fuel', 'petrol', 'parking', 'materials'],
    requiredFields: ['amount']
  },
  capture_quote: {
    entityType: 'quote',
    keywords: ['quote', 'estimate', 'proposal', 'pro forma'],
    requiredFields: ['amount', 'counterparty_name']
  },
  capture_payment: {
    entityType: 'payment',
    keywords: ['payment', 'paid', 'received', 'income', 'bank transfer', 'transferred', 'settled'],
    requiredFields: ['amount', 'counterparty_name']
 succeeded in 2.52s:
const db = require('../config/db');
const actionController = require('./actionController');
const searchController = require('./searchController');
const itemController = require('./itemController');
const { isMonetaryItemType } = require('../services/monetaryIntegrityService');
const {
  evaluateAutoCommitEligibility
} = require('../services/autoCommitGovernanceService');
const {
  buildCaptureParseResult,
  toLegacyIntentShape
} = require('../services/voiceCaptureParserService');

/**
 * Simple Intent Parser and Slot Extractor (NLU Mock)
 * Matches transcripts to core intents and extracts slots using regex.
 */
function parseIntent(transcript, clientDateStr = null) {
  const parseResult = buildCaptureParseResult(transcript, clientDateStr);
  console.log(
codex
I’ve confirmed there’s already a narrow micro-decision endpoint and a verifier, but it currently accepts free-form category names and doesn’t enforce inbox/finish-now context or any undo/confirmation contract. I’m checking the phase checklist/PRD notes next so the implementation matches the intended constrained command set rather than guessing.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n \"voice micro|micro-decision|finish-now|finish now|confirmation chip|undo\" 'C:\\Users\\edebe\\eds\\workstream' 'C:\\Users\\edebe\\eds\\bizPA'" in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Path 'C:\\Users\\edebe\\eds\\workstream\\300_complete\\20260305_191657_bizpa_mvp_quarterly_pivot_full_delivery_checklist.md'" in C:\Users\edebe\eds succeeded in 1.61s:
# Source
- User request: generate a complete checklist with tests to fully deliver the pivot to `workstream/000_backlog/mvp_prd_quarterly_export_10min.md`.

# Task Summary
Create an implementation delivery checklist with explicit tests covering all MVP PRD requirements for inbox-driven categorization and quarterly export flow.

# Context
- Existing system: `C:\Users\edebe\eds\bizPA`
- Target PRD: `C:\Users\edebe\eds\workstream\000_backlog\mvp_prd_quarterly_export_10min.md`

# Plan
- [x] 1. Extract deliverables from the PRD into workstreams.
  - [x] Test: Ensure checklist spans scope/in-scope features, data model, NFRs, and acceptance tests.
  - [x] Evidence: Checklist sections include ingestion, inbox triage, blockers/readiness, evidence matching, voice, export, security, performance, QA.
- [x] 2. Produce full checklist with explicit test for each item.
  - [x] Test: Every checklist line has a clear verification command/manual test with pass criteria.
  - [x] Evidence: Generated checklist uses step + test format for each line item.

# Implementation Log
- 2026-03-05 19:16:57 Created task file.
 exited 124 in 11.00s:
C:\Users\edebe\eds\workstream\050_review\gemini\20260311_161947_bizpa_mvp_product_requirements_document_workstreamB_build_capture_composition_and_preview_pipeline.md:41:- Updated voice undo to remove the latest draft composition from `capture_items` instead of deleting from the legacy table.
C:\Users\edebe\eds\workstream\050_review\gemini\20260311_161947_bizpa_mvp_product_requirements_document_workstreamB_build_capture_composition_and_preview_pipeline.md:109:+- Updated voice undo to remove the latest draft composition from `capture_items` instead of deleting from the legacy table.
C:\Users\edebe\eds\bizPA\small_trader_capture_app_build_prompt_voice_first.md:100:- `undo_last_action` (must exist)
C:\Users\edebe\eds\bizPA\small_trader_capture_app_build_prompt_voice_first.md:236:- Always support “**cancel**”, “**stop**”, “**undo**”, “**repeat**”.  
C:\Users\edebe\eds\bizPA\small_trader_capture_app_build_prompt_voice_first.md:340:- Always include cancel/undo paths for voice actions.
C:\Users\edebe\eds\bizPA\260217_2331_WS3_VOICE_Dialogue_and_Safety_Playbook_draft.md:33:- Commands always available: cancel, stop, undo, repeat.
C:\Users\edebe\eds\bizPA\260217_2310_WS1_PRODUCT_UX_PRD_draft.md:43:- Commands: capture, label, link, search, export, summarize, undo.
C:\Users\edebe\eds\bizPA\260217_2310_WS1_PRODUCT_UX_PRD_draft.md:47:- Global commands available in key flows: cancel, stop, undo, repeat.
C:\Users\edebe\eds\bizPA\260217_2310_WS1_PRODUCT_UX_Flows_and_Dialogues_draft.md:52:- undo
C:\Users\edebe\eds\bizPA\260217_2310_WS1_PRODUCT_UX_Acceptance_Baseline_draft.md:11:- AC-06: Global commands (cancel/stop/undo/repeat) work in all primary flows.
C:\Users\edebe\eds\bizPA\260217_2300_voice_first_deliverables_breakdown_checklist.md:109:- Global cancel/undo/repeat paths.
C:\Users\edebe\eds\bizPA\260217_2300_voice_first_deliverables_breakdown_checklist.md:273:- Clarification/fallback rules and undo semantics
C:\Users\edebe\eds\workstream\200_inprogress\codex\20260301_235502_gemini_bizpa_add_quotes_section.md.result.md:501:2603:            {inboxBusyId === 'undo-last' ? <RefreshCcw size={16} className="spinner" /> : <ArchiveRestore size={16} />}
C:\Users\edebe\eds\workstream\050_review\codex\20260314_034042_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_mobile_inbox_exception_queue_screen.md:12:Dependency: B2 micro-decision actions and B3 blocker statuses are assumed present in the existing `bizPA` inbox/readiness APIs and will be consumed by this task.
C:\Users\edebe\eds\workstream\050_review\codex\20260314_034042_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_mobile_inbox_exception_queue_screen.md:20:  - Evidence: Frontend build completed successfully on 2026-03-18; `bizPA/frontend/src/App.jsx` now groups readiness issues into mobile transaction cards and wires category, business/personal, split, duplicate, suggestion, and undo flows; `bizPA/backend/src/controllers/inboxController.js` now exposes rule-based suggestion metadata for live inbox rows.
C:\Users\edebe\eds\workstream\050_review\codex\20260314_034042_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_mobile_inbox_exception_queue_screen.md:62:- Replaced the passive finish-now list in `bizPA/frontend/src/App.jsx` with a mobile-first inbox exception queue that groups readiness issues by transaction and renders actionable triage cards.
C:\Users\edebe\eds\workstream\050_review\codex\20260314_034042_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_mobile_inbox_exception_queue_screen.md:63:- Added inbox draft-state helpers and action handlers for suggested category application, category entry, business/personal/split controls, duplicate resolution, and undo-last behavior.
C:\Users\edebe\eds\workstream\050_review\codex\20260314_034042_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_mobile_inbox_exception_queue_screen.md:98:Implemented the mobile inbox exception queue in [App.jsx](/C:/Users/edebe/eds/bizPA/frontend/src/App.jsx#L2565), added live rule-based suggestion metadata in [inboxController.js](/C:/Users/edebe/eds/bizPA/backend/src/controllers/inboxController.js#L122), and wired the local launch/smoke path in [start_bizpa_mvp_mobile_inbox_ui.ps1](/C:/Users/edebe/eds/bizPA/start_bizpa_mvp_mobile_inbox_ui.ps1#L1) and [mobile_inbox_ui_smoke.ps1](/C:/Users/edebe/eds/bizPA/mobile_inbox_ui_smoke.ps1#L1). The inbox now has filter chips, grouped transaction cards, suggested category apply, business/personal/split controls, duplicate dismiss/merge actions, and undo support.
C:\Users\edebe\eds\workstream\050_review\codex\20260314_034042_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_mobile_inbox_exception_queue_screen.md:129: - Replaced the passive finish-now list in `bizPA/frontend/src/App.jsx` with a mobile-first inbox exception queue that groups readiness issues by transaction and renders actionable triage cards.
C:\Users\edebe\eds\workstream\050_review\codex\20260314_034042_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_mobile_inbox_exception_queue_screen.md:130: - Added inbox draft-state helpers and action handlers for suggested category application, category entry, business/personal/split controls, duplicate resolution, and undo-last behavior.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n \"Voice Micro|micro-decision|Match first|Attach receipt|Split n|confirmation chip|undo|finish-now|current inbox\" 'C:\\Users\\edebe\\eds\\workstream\\000_backlog\\mvp_prd_quarterly_export_10min.md' 'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\App.jsx' 'C:\\Users\\edebe\\eds\\bizPA\\backend\\src\\controllers\\inboxController.js'" in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Path 'C:\\Users\\edebe\\eds\\workstream\\000_backlog\\mvp_prd_quarterly_export_10min.md'" in C:\Users\edebe\eds exited 1 in 1.95s:
C:\Users\edebe\eds\bizPA\backend\src\controllers\inboxController.js:329:const undoLastTriageAction = async (req, res) => {
C:\Users\edebe\eds\bizPA\backend\src\controllers\inboxController.js:342:    if (!audit.rows.length) return res.status(404).json({ error: 'No triage action to undo.' });
C:\Users\edebe\eds\bizPA\backend\src\controllers\inboxController.js:345:      return res.status(400).json({ error: 'Latest action is not undoable via classification undo.' });
C:\Users\edebe\eds\bizPA\backend\src\controllers\inboxController.js:377:    return res.status(200).json({ status: 'undone', bank_txn_id: row.bank_txn_id });
C:\Users\edebe\eds\bizPA\backend\src\controllers\inboxController.js:379:    console.error('[InboxController] undoLastTriageAction failed:', err);
C:\Users\edebe\eds\bizPA\backend\src\controllers\inboxController.js:380:    return res.status(500).json({ error: 'Failed to undo last triage action.' });
C:\Users\edebe\eds\bizPA\backend\src\controllers\inboxController.js:446:  undoLastTriageAction,
C:\Users\edebe\eds\bizPA\frontend\src\App.jsx:200:        workflow: '/inbox/finish-now',
C:\Users\edebe\eds\bizPA\frontend\src\App.jsx:220:        workflow: '/inbox/finish-now',
C:\Users\edebe\eds\bizPA\frontend\src\App.jsx:240:        workflow: '/inbox/finish-now',
C:\Users\edebe\eds\bizPA\frontend\src\App.jsx:260:        workflow: '/inbox/finish-now',
C:\Users\edebe\eds\bizPA\frontend\src\App.jsx:510:  .finish-now-list { display: grid; gap: 12px; }
C:\Users\edebe\eds\bizPA\frontend\src\App.jsx:511:  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
C:\Users\edebe\eds\bizPA\frontend\src\App.jsx:512:  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
C:\Users\edebe\eds\bizPA\frontend\src\App.jsx:513:  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
C:\Users\edebe\eds\bizPA\frontend\src\App.jsx:514:  .finish-now-actions { display: flex; align-items: center; }
C:\Users\edebe\eds\bizPA\frontend\src\App.jsx:633:    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
C:\Users\edebe\eds\bizPA\frontend\src\App.jsx:635:    .finish-now-actions { justify-content: stretch; }
C:\Users\edebe\eds\bizPA\frontend\src\App.jsx:1543:        'inbox/finish-now',
C:\Users\edebe\eds\bizPA\frontend\src\App.jsx:1801:  const undoLastInboxAction = async () => {
 exited 1 in 1.93s:
[31;1mGet-Content: [0m
[31;1m[36;1mLine |[0m
[31;1m[36;1m[36;1m   2 | [0m [36;1mGet-Content -Path 'C:\Users\edebe\eds\workstream\000_backlog\mvp_prd_[0m …[0m
[31;1m[36;1m[36;1m[0m[36;1m[0m[36;1m     | [31;1m ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~[0m
[31;1m[36;1m[36;1m[0m[36;1m[0m[36;1m[31;1m[31;1m[36;1m     | [31;1mCannot find path 'C:\Users\edebe\eds\workstream\000_backlog\mvp_prd_quarterly_export_10min.md' because it does not exist.[0m
codex
The source path recorded in the checklist is stale, but the implementation target is clear in `bizPA`. I’m now reading the inbox undo/classification flow and the frontend’s existing finish-now behavior so I can reuse the same audit semantics instead of inventing a parallel one.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Path 'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\App.jsx'" in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Path 'C:\\Users\\edebe\\eds\\bizPA\\backend\\src\\controllers\\inboxController.js'" in C:\Users\edebe\eds succeeded in 1.70s:
const db = require('../config/db');
const { normalizeBatch } = require('../services/openBankingAdapter');
const { buildReadinessReport } = require('../services/readinessService');
const {
  deriveQuarterFromDate,
  quarterBoundsFromReference,
  recordReadinessRecalculated,
  recordStatusChange
} = require('../services/businessEventLogService');

const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';

const recordReadinessForTransaction = async (executor, userId, bankTxnId, txnDate, summary, description) => {
  await recordReadinessRecalculated(executor, {
    user_id: userId,
    actor_id: userId,
    source_type: 'manual',
    quarter_reference: deriveQuarterFromDate(txnDate),
    entity_id: bankTxnId,
    entity_type: 'bank_transaction',
 succeeded in 2.05s:
import React, { useEffect, useRef, useState } from 'react';
import axios from 'axios';
import {
  ArchiveRestore,
  ArrowRight,
  Bell,
  Briefcase,
  Calendar,
  CheckCircle2,
  ChevronDown,
  ChevronUp,
  Clock,
  Download,
  Flame,
  FileText,
  History,
  LayoutGrid,
  Lock,
  LockOpen,
  Mic,
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n \"toggleListening|voiceStatus|processMicroDecision|microDecision|confirmation_chip|undoLastInboxAction|SpeechRecognition|/voice/\" 'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\App.jsx'" in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n \"transaction_classifications|category_code|MATERIALS|TRAVEL|INCOME|rules\" 'C:\\Users\\edebe\\eds\\bizPA\\backend\\src'" in C:\Users\edebe\eds succeeded in 1.47s:
C:\Users\edebe\eds\bizPA\backend\src\models\sync_migration.sql:63:-- trigger_rules
C:\Users\edebe\eds\bizPA\backend\src\models\sync_migration.sql:64:ALTER TABLE trigger_rules ADD COLUMN IF NOT EXISTS user_id UUID REFERENCES users(id);
C:\Users\edebe\eds\bizPA\backend\src\models\sync_migration.sql:65:ALTER TABLE trigger_rules ADD COLUMN IF NOT EXISTS last_synced_at TIMESTAMPTZ;
C:\Users\edebe\eds\bizPA\backend\src\models\sync_migration.sql:66:ALTER TABLE trigger_rules ADD COLUMN IF NOT EXISTS deleted_at TIMESTAMPTZ;
C:\Users\edebe\eds\bizPA\backend\src\models\data_entry_fix.sql:9:-- 2. Backfill existing data with the default user ID so it becomes visible under the new isolation rules
C:\Users\edebe\eds\bizPA\backend\src\models\data_entry_fix.sql:17:UPDATE trigger_rules SET user_id = '00000000-0000-0000-0000-000000000000' WHERE user_id IS NULL;
C:\Users\edebe\eds\bizPA\backend\src\models\schema.sql:148:-- 12. trigger_rules
C:\Users\edebe\eds\bizPA\backend\src\models\schema.sql:149:CREATE TABLE IF NOT EXISTS trigger_rules (
C:\Users\edebe\eds\bizPA\backend\src\models\schema.sql:255:CREATE TRIGGER tr_update_trigger_rules BEFORE UPDATE ON trigger_rules FOR EACH ROW EXECUTE FUNCTION update_timestamp();
C:\Users\edebe\eds\bizPA\backend\src\controllers\exportController.js:211:          WHERE tc.category_code IS NULL
C:\Users\edebe\eds\bizPA\backend\src\controllers\exportController.js:217:      LEFT JOIN transaction_classifications tc ON tc.bank_txn_id = bt.id
C:\Users\edebe\eds\bizPA\backend\src\controllers\exportController.js:236:        tc.category_code,
C:\Users\edebe\eds\bizPA\backend\src\controllers\exportController.js:237:        COALESCE(tc.category_name, tc.category_code) AS category_name,
C:\Users\edebe\eds\bizPA\backend\src\controllers\exportController.js:247:      LEFT JOIN transaction_classifications tc ON tc.bank_txn_id = bt.id
C:\Users\edebe\eds\bizPA\backend\src\controllers\exportController.js:250:      GROUP BY bt.id, bt.txn_date, bt.merchant, bt.amount, bt.direction, tc.category_code, tc.category_name, tc.confidence, tc.business_personal, tc.is_split, tc.split_business_pct, bt.bank_account_id, bt.bank_txn_ref
C:\Users\edebe\eds\bizPA\backend\src\controllers\exportController.js:283:        tc.category_code,
C:\Users\edebe\eds\bizPA\backend\src\controllers\exportController.js:284:        COALESCE(tc.category_name, tc.category_code) AS category_name,
C:\Users\edebe\eds\bizPA\backend\src\controllers\exportController.js:290:      LEFT JOIN transaction_classifications tc ON tc.bank_txn_id = bt.id
C:\Users\edebe\eds\bizPA\backend\src\controllers\exportController.js:292:      GROUP BY tc.category_code, COALESCE(tc.category_name, tc.category_code)
C:\Users\edebe\eds\bizPA\backend\src\controllers\exportController.js:293:      ORDER BY tc.category_code
 succeeded in 1.69s:
31:import { SpeechRecognition } from '@capacitor-community/speech-recognition';
1358:  const [voiceStatus, setVoiceStatus] = useState('idle');
1801:  const undoLastInboxAction = async () => {
1895:  const toggleListening = async () => {
1904:        const capability = await SpeechRecognition.available();
1911:        const SpeechRecognitionBrowser = window.SpeechRecognition || window.webkitSpeechRecognition;
1912:        if (!SpeechRecognitionBrowser) {
1917:          const recognition = new SpeechRecognitionBrowser();
1960:        SpeechRecognition.stop().catch(() => {});
1968:        const permissions = await SpeechRecognition.checkPermissions();
1970:          await SpeechRecognition.requestPermissions();
1976:        const result = await SpeechRecognition.start({ language: 'en-GB', popup: true });
1994:      const response = await axios.post(`${API_BASE_URL}/voice/process`, {
2845:          <button className="hero-action-secondary" onClick={undoLastInboxAction} disabled={inboxBusyId === 'undo-last'}>
4371:          <button className="btn btn-light rounded-circle p-2 me-2" onClick={toggleListening} disabled={voiceStatus === 'processing'} title="Voice Log">
4372:            {voiceStatus === 'processing' ? <RefreshCcw size={18} className="spinner" /> : <Mic size={18} />}
4441:        <button className="log-something-btn" onClick={toggleListening} disabled={voiceStatus === 'processing'}>
4442:          {voiceStatus === 'processing' ? <RefreshCcw size={20} className="spinner" /> : <Mic size={20} />}
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$content = Get-Content -Path '"'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\App.jsx'; "'$content[3550..3725]' in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$content = Get-Content -Path '"'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\App.jsx'; "'$content[1760..2015]' in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Path 'C:\\Users\\edebe\\eds\\bizPA\\backend\\src\\routes\\voiceRoutes.js'" in C:\Users\edebe\eds succeeded in 1.95s:
const express = require('express');
const router = express.Router();
const voiceController = require('../controllers/voiceController');

// POST /api/v1/voice/process
// Body: { "transcript": "text", "device_id": "uuid" }
router.post('/process', voiceController.processVoice);
router.post('/micro-decision', voiceController.processMicroDecision);

module.exports = router;
 succeeded in 2.13s:
        }
        if (issue.issue_type === 'missing_category') {
          return !updates.category_code;
        }
        if (issue.issue_type === 'missing_business_personal') {
          return !updates.business_personal;
        }
        if (issue.issue_type === 'missing_split_pct') {
          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
        }
        if (issue.issue_type === 'unresolved_duplicate') {
          return !updates.duplicate_resolution;
        }
        return true;
      });
      return {
        ...previous,
        issue_list: nextIssueList,
        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
      };
 succeeded in 2.14s:
    const severityBreakdown = buildSeverityBreakdown(issueSummary);
    const historicalSnapshots = buildHistoricalSnapshots(report);
    const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
    const workspace = snapshotWorkspace || buildDemoSnapshotWorkspace();
    const lifecycle = workspace?.quarter_lifecycle || {
      quarter_label: report?.quarter_reference,
      quarter_state: 'pending'
    };
    const snapshotStatus = workspace?.snapshot_status || null;
    const activityEvents = Array.isArray(workspace?.activity_events) ? workspace.activity_events : [];
    const integrityWarnings = Array.isArray(workspace?.integrity_warnings) ? workspace.integrity_warnings : [];
    const versionList = Array.isArray(workspace?.snapshots) ? workspace.snapshots : [];
    const activeSnapshot = selectedSnapshot || versionList[0] || null;
    const lifecycleBadgeClass = lifecycle?.quarter_state === 'closed'
      ? 'closed'
      : lifecycle?.quarter_state === 'open'
        ? 'open'
        : 'pending';
    const policyLabels = {
      vat_scheme: 'VAT scheme',
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n \"selected.*inbox|activeInbox|inboxDrafts|buildInboxDraft|openReadinessWorkflow|supports_category|supports_business_personal|supports_split|supports_duplicate\" 'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\App.jsx'" in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n \"verify_voice_micro_decisions|voice_micro_decisions_ok|micro-decision\" 'C:\\Users\\edebe\\eds\\bizPA\\backend'" in C:\Users\edebe\eds succeeded in 1.18s:
931:const buildInboxDraft = (item) => ({
1073:      supports_category: card.issue_types.includes('missing_category'),
1074:      supports_business_personal: card.issue_types.includes('missing_business_personal'),
1075:      supports_split: card.issue_types.includes('missing_split_pct'),
1076:      supports_duplicate: card.issue_types.includes('unresolved_duplicate'),
1382:  const [inboxDrafts, setInboxDrafts] = useState({});
1712:      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
1793:        ...(previous[card.id] || buildInboxDraft(card)),
1824:    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
1825:    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
1826:    const shouldResolveDuplicate = card.supports_duplicate;
1829:    if (card.supports_category && !draft.category_code.trim()) {
1833:    if (card.supports_business_personal && !draft.business_personal) {
1837:    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
1841:    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
1845:    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
2893:            const draft = inboxDrafts[card.id] || buildInboxDraft(card);
2927:                {(card.supports_category || card.supports_business_personal || card.supports_split) && (
2948:                {(card.supports_business_personal || card.supports_split) && (
2972:                {(card.supports_split || isSplit) && (
 succeeded in 1.19s:
C:\Users\edebe\eds\bizPA\backend\package.json:21:    "verify:voice-micro-decisions": "node verify_voice_micro_decisions.js",
C:\Users\edebe\eds\bizPA\backend\src\routes\voiceRoutes.js:8:router.post('/micro-decision', voiceController.processMicroDecision);
C:\Users\edebe\eds\bizPA\backend\src\controllers\voiceController.js:382:    return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
C:\Users\edebe\eds\bizPA\backend\src\controllers\voiceController.js:385:    return res.status(500).json({ error: 'Failed to process micro-decision.' });
C:\Users\edebe\eds\bizPA\backend\verify_voice_micro_decisions.js:134:  assert.strictEqual(unsupported.body.error, 'Unsupported MVP voice micro-decision command.');
C:\Users\edebe\eds\bizPA\backend\verify_voice_micro_decisions.js:136:  console.log('voice_micro_decisions_ok');
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$content = Get-Content -Path '"'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\App.jsx'; "'$content[900..1115]' in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$content = Get-Content -Path '"'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\App.jsx'; "'$content[3320..3395]' in C:\Users\edebe\eds succeeded in 2.62s:
      callback: () => ({
        auto_commit_enabled: getPolicyValue(policyRecords, 'auto_commit_enabled', false),
      }),
    });

    if (!result.allowed) {
      alert(result.reason);
      return;
    }

    alert(`Override request logged within today's cap of ${formatCurrency(getPolicyValue(policyRecords, 'auto_commit_daily_cap_gbp', 0), 2)}.`);
  };

  const exportQuarterlyPack = () => {
    const result = runGovernedAction({
      actor: currentUser,
      tenantId: currentUser.tenant_id,
      permission: PERMISSIONS.QUARTER_EXPORT,
      action: 'quarter.export_requested',
      targetType: 'quarter',
 succeeded in 2.90s:
    return 'Amount pending';
  }
  return `${direction === 'in' ? 'Inflow' : 'Outflow'} ${formatCurrency(amount, 2)}`;
};

const sanitizeCategoryCode = (value) => String(value || '')
  .trim()
  .toUpperCase()
  .replace(/[^A-Z0-9]+/g, '_')
  .replace(/^_+|_+$/g, '');

const buildSuggestedCategory = (merchant, existingSuggestion = null) => {
  if (existingSuggestion?.category_code || existingSuggestion?.category_name) {
    return {
      category_code: existingSuggestion.category_code || sanitizeCategoryCode(existingSuggestion.category_name),
      category_name: existingSuggestion.category_name || existingSuggestion.category_code,
    };
  }

  const matchedRule = INBOX_MERCHANT_SUGGESTIONS.find((rule) => rule.pattern.test(String(merchant || '')));
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$content = Get-Content -Path '"'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\App.jsx'; "'$content[1450..1760]' in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n \"executeVoiceAction|confirmation_text|confirmation_chip|voice\" 'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\App.jsx'" in C:\Users\edebe\eds succeeded in 1.39s:
  useEffect(() => {
    document.body.setAttribute('data-theme', theme);
  }, [theme]);

  useEffect(() => {
    if (!captureDemoEnabled) {
      return;
    }
    setCurrentTab('add');
    setPreviewDraft({
      compositionId: 'demo-preview',
      rawNote: 'Voice capture: invoice for Brookside Renovations, GBP 480 labour, standard VAT.',
      preview: {
        confidence_indicator: 'medium',
      },
    });
    setPreviewForm({
      entityType: 'invoice',
      counterparty: 'Brookside Renovations',
      grossAmount: '480.00',
 succeeded in 1.91s:
64:const MONETARY_TYPES = ['invoice', 'receipt', 'payment', 'quote'];
119:  type: 'invoice',
358:          entity_type: 'invoice',
429:  .voice-preview { background: rgba(0,0,0,0.82); color: white; padding: 8px 15px; border-radius: 15px; margin-bottom: 8px; font-size: 0.72rem; max-width: 240px; text-align: right; }
670:    due_date: form.type === 'invoice' ? form.relevantDate || undefined : undefined,
676:  entityType: preview?.entity_type || 'invoice',
1258:      reopen_reason: 'Late supplier invoice received after final bank sync.',
1262:        reopen_reason: 'Late supplier invoice received after final bank sync.'
1283:          reopen_reason: 'Late supplier invoice received after final bank sync.',
1358:  const [voiceStatus, setVoiceStatus] = useState('idle');
1372:  const [reopenReason, setReopenReason] = useState('Late supplier invoice received after final bank sync.');
1462:      rawNote: 'Voice capture: invoice for Brookside Renovations, GBP 480 labour, standard VAT.',
1468:      entityType: 'invoice',
1476:      note: 'Voice capture: invoice for Brookside Renovations, GBP 480 labour, standard VAT.',
1897:      alert('Cannot start voice capture while backend is disconnected.');
1994:      const response = await axios.post(`${API_BASE_URL}/voice/process`, {
1999:      if (response.data.confirmation_text && window.SpeechSynthesisUtterance) {
2001:          const utterance = new window.SpeechSynthesisUtterance(response.data.confirmation_text);
2006:      executeVoiceAction(response.data);
2034:  const executeVoiceAction = (data) => {
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$content = Get-Content -Path '"'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\App.jsx'; "'$content[2028..2118]' in C:\Users\edebe\eds succeeded in 1.48s:
    setActivityClientFilter(client);
    setActivityDateFilter(date);
    setTimePeriodFilter(timePeriod);
  };

  const executeVoiceAction = (data) => {
    const { intent, slots = {} } = data || {};
    const nextTimePeriod = resolveVoiceTimePeriod(slots);
    const nextDate = resolveVoiceDateFilter(slots);
    const nextClient = slots.client_name || '';
    switch (intent) {
      case 'go_home':
        setCurrentTab('home');
        break;
      case 'view_expenses':
        openActivityView({
          type: 'receipt',
          client: nextClient,
          date: nextDate,
          timePeriod: nextTimePeriod,
codex
The remaining gap is in the UI: voice currently always posts to `/voice/process` and has no notion of an active inbox card, no micro-decision path, and no post-action chip. I’m pulling a tighter slice of the inbox UI and any voice-flow docs so I can wire the smallest coherent end-to-end path without destabilizing unrelated capture flows.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$content = Get-Content -Path '"'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\App.jsx'; "'$content[2860..3025]' in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n \"Category:|Business|Personal|Split|Attach receipt|Match first|No match|voice\" 'C:\\Users\\edebe\\eds\\bizPA\\260217_2310_WS1_PRODUCT_UX_PRD_draft.md' 'C:\\Users\\edebe\\eds\\bizPA\\260217_2310_WS1_PRODUCT_UX_Flows_and_Dialogues_draft.md' 'C:\\Users\\edebe\\eds\\bizPA\\260217_2310_WS1_PRODUCT_UX_Acceptance_Baseline_draft.md'" in C:\Users\edebe\eds succeeded in 1.71s:
            <h5 style={{ marginBottom: 4 }}>Queue filters</h5>
            <small className="small-muted">Keep the mobile pass focused on one blocker type at a time.</small>
          </div>
          <div className="status-pill">{activeQuarterReport.readiness_pct ?? READINESS_DEMO_REPORT.readiness_pct}% ready</div>
        </div>
        <div className="inbox-filter-row">
          {INBOX_FILTERS.map((filter) => {
            const count = filter.key === 'all'
              ? inboxCards.length
              : inboxCards.filter((card) => card.issue_types.includes(filter.key)).length;
            return (
              <button
                key={filter.key}
                type="button"
                className={`inbox-filter-chip ${inboxFilter === filter.key ? 'active' : ''}`}
                onClick={() => setInboxFilter(filter.key)}
              >
                <span>{filter.label}</span>
                <small>{count}</small>
              </button>
 succeeded in 1.84s:
C:\Users\edebe\eds\bizPA\260217_2310_WS1_PRODUCT_UX_Acceptance_Baseline_draft.md:24:- AC-13: Disabled plugin actions are blocked in API and voice layer.
C:\Users\edebe\eds\bizPA\260217_2310_WS1_PRODUCT_UX_Acceptance_Baseline_draft.md:27:- AC-14: P50 voice-to-action <= 3 seconds.
C:\Users\edebe\eds\bizPA\260217_2310_WS1_PRODUCT_UX_Acceptance_Baseline_draft.md:28:- AC-15: P90 voice-to-action <= 5 seconds.
C:\Users\edebe\eds\bizPA\260217_2310_WS1_PRODUCT_UX_Flows_and_Dialogues_draft.md:24:### Invoice
C:\Users\edebe\eds\bizPA\260217_2310_WS1_PRODUCT_UX_Flows_and_Dialogues_draft.md:25:- Command: "New invoice for John, 240 pounds, due Friday"
C:\Users\edebe\eds\bizPA\260217_2310_WS1_PRODUCT_UX_Flows_and_Dialogues_draft.md:30:- Payment item created and linked to client/invoice if identified.
C:\Users\edebe\eds\bizPA\260217_2310_WS1_PRODUCT_UX_Flows_and_Dialogues_draft.md:42:- Command: "Show unpaid invoices"
C:\Users\edebe\eds\bizPA\260217_2310_WS1_PRODUCT_UX_Flows_and_Dialogues_draft.md:72:- Explicit request required: "Read invoice amount."
C:\Users\edebe\eds\bizPA\260217_2310_WS1_PRODUCT_UX_PRD_draft.md:4:A UK-first, offline-first, voice-first mobile app for small traders to capture work evidence and admin data in seconds, then retrieve and act on it quickly.
C:\Users\edebe\eds\bizPA\260217_2310_WS1_PRODUCT_UX_PRD_draft.md:26:- Capture items (receipt/invoice/payment/photo/note/voice memo)
C:\Users\edebe\eds\bizPA\260217_2310_WS1_PRODUCT_UX_PRD_draft.md:40:- Advanced voice plugin
C:\Users\edebe\eds\bizPA\260217_2310_WS1_PRODUCT_UX_PRD_draft.md:61:- P50 voice-to-action <= 3s
C:\Users\edebe\eds\bizPA\260217_2310_WS1_PRODUCT_UX_PRD_draft.md:62:- P90 voice-to-action <= 5s
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n \"voice micro-decisions|micro-decision\" 'C:\\Users\\edebe\\eds\\workstream\\050_review'" in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-ChildItem -Path 'C:\\Users\\edebe\\eds\\workstream' -Recurse -Filter '*voice_micro_decisions*' | Select-Object -ExpandProperty FullName" in C:\Users\edebe\eds succeeded in 1.83s:
C:\Users\edebe\eds\workstream\050_review\codex\20260314_034042_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_mobile_inbox_exception_queue_screen.md:12:Dependency: B2 micro-decision actions and B3 blocker statuses are assumed present in the existing `bizPA` inbox/readiness APIs and will be consumed by this task.
 exited 1 in 2.13s:
C:\Users\edebe\eds\workstream\200_inprogress\codex\20260305_192700_bizpa_mvp_pivot_phase06_voice_micro_decisions.md
C:\Users\edebe\eds\workstream\300_complete\20260305_192700_bizpa_mvp_pivot_phase06_voice_micro_decisions.md
C:\Users\edebe\eds\workstream\500_dump\codex_artifacts\20260305_192700_bizpa_mvp_pivot_phase06_voice_micro_decisions.md.result.md
[31;1mGet-ChildItem: [0m
[31;1m[36;1mLine |[0m
[31;1m[36;1m[36;1m   2 | [0m [36;1mGet-ChildItem -Path 'C:\Users\edebe\eds\workstream' -Recurse -Filter [0m …[0m
[31;1m[36;1m[36;1m[0m[36;1m[0m[36;1m     | [31;1m ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~[0m
[31;1m[36;1m[36;1m[0m[36;1m[0m[36;1m[31;1m[31;1m[36;1m     | [31;1mAccess to the path 'C:\Users\edebe\eds\workstream\artefacts\epic_decomp_4rbio_0v' is denied.[0m
[31;1mGet-ChildItem: [0m
[31;1m[36;1mLine |[0m
[31;1m[36;1m[36;1m   2 | [0m [36;1mGet-ChildItem -Path 'C:\Users\edebe\eds\workstream' -Recurse -Filter [0m …[0m
[31;1m[36;1m[36;1m[0m[36;1m[0m[36;1m     | [31;1m ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~[0m
[31;1m[36;1m[36;1m[0m[36;1m[0m[36;1m[31;1m[31;1m[36;1m     | [31;1mAccess to the path 'C:\Users\edebe\eds\workstream\artefacts\epic_decomp_yxt48izg' is denied.[0m
codex
There’s already a completed copy of this lifecycle task in `300_complete`, so I’m checking it before editing. If it contains prior implementation notes or expected evidence, I can align this run instead of diverging from an earlier accepted shape.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Path 'C:\\Users\\edebe\\eds\\workstream\\500_dump\\codex_artifacts\\20260305_192700_bizpa_mvp_pivot_phase06_voice_micro_decisions.md.result.md'" in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Path 'C:\\Users\\edebe\\eds\\workstream\\300_complete\\20260305_192700_bizpa_mvp_pivot_phase06_voice_micro_decisions.md'" in C:\Users\edebe\eds succeeded in 1.66s:
# Source
- Derived from backlog: `C:\Users\edebe\eds\workstream\000_backlog\mvp_prd_quarterly_export_10min.md`
- Decomposed from checklist: `C:\Users\edebe\eds\workstream\300_complete\20260305_191657_bizpa_mvp_quarterly_pivot_full_delivery_checklist.md`

# Task Summary
Constrain voice intents to PRD micro-decisions and bind safely to inbox/finish-now context with confirmation chip + undo.

# Context
- Affected area: voice controller/parser and web/mobile voice UX

# Plan
- [ ] 1. Implement phase scope in code and configuration.
  - [ ] Test: Execute phase-specific implementation checks and verify expected behavior change.
  - [ ] Evidence: pending
- [ ] 2. Add/adjust automated tests for this phase.
  - [ ] Test: Run targeted unit/integration tests for new behavior and ensure green results.
  - [ ] Evidence: pending
- [ ] 3. Validate against PRD acceptance criteria impacted by this phase.
  - [ ] Test: Run intent contract tests for supported utterances and verify each action is applied, confirmed, and undoable.
  - [ ] Evidence: pending
 succeeded in 1.67s:
# Source
- Epic: `C:\Users\edebe\eds\workstream\000_epic\20260305_185316_mvp_prd_quarterly_export_10min.md`
- Derived checklist: `C:\Users\edebe\eds\workstream\300_complete\20260305_191657_bizpa_mvp_quarterly_pivot_full_delivery_checklist.md`

# Task Summary
Constrain voice intents to the PRD Phase 6 micro-decisions and bind them safely to inbox and finish-now context with confirmation chip responses and an undo-compatible flow.

# Context
- Affected runtime area: `bizPA/backend` voice controller routing and verification scripts.
- Primary implementation files: `bizPA/backend/src/controllers/voiceController.js`, `bizPA/backend/src/routes/voiceRoutes.js`.
- Validation coverage added in this task: `bizPA/backend/verify_voice_micro_decisions.js` and `bizPA/backend/package.json`.

# Dependency
`C:\Users\edebe\eds\workstream\300_complete\20260305_191657_bizpa_mvp_quarterly_pivot_full_delivery_checklist.md`

# Plan
- [x] 1. Confirm or implement the Phase 6 voice micro-decision command surface in backend code.
  - [x] Test: Inspect `bizPA/backend/src/controllers/voiceController.js` and `bizPA/backend/src/routes/voiceRoutes.js` for the `processMicroDecision` handler and `/api/v1/voice/micro-decision` route, then verify supported commands map to expected persistence or clarification behavior.
  - [x] Evidence: Confirmed existing handler supports `Category:`, `Business|Personal`, `Split n%`, `Attach receipt`, `Match first|second|third`, and `No match`; route remains exposed at `/api/v1/voice/micro-decision`.
- [x] 2. Add repeatable automated validation for the Phase 6 micro-decision contract.
codex
The completed lifecycle copy confirms the original backend-only implementation was accepted once, but it also highlights the unresolved UI risk the current task summary still calls out: client context safety and undo-compatible feedback. I’m treating this run as a reconciliation pass to finish the frontend binding, strengthen backend guardrails, and then I’ll leave the lifecycle in `Awaiting user verification` because this changes user-visible behavior.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Path 'C:\\Users\\edebe\\eds\\bizPA\\backend\\package.json'" in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Path 'C:\\Users\\edebe\\eds\\bizPA\\frontend\\package.json'" in C:\Users\edebe\eds succeeded in 1.81s:
{
  "name": "frontend",
  "version": "1.3.6",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@capacitor-community/speech-recognition": "^7.0.1",
    "@capacitor/preferences": "^8.0.1",
    "axios": "1.13.5",
    "bootstrap": "5.3.8",
    "lucide-react": "0.575.0",
 succeeded in 1.65s:
{
  "name": "backend",
  "version": "1.3.6",
  "description": "",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "node src/app.js",
    "migrate:mvp-quarterly": "node apply_mvp_quarterly_migration.js",
    "verify:mvp-quarterly": "node verify_mvp_quarterly_flow.js",
    "verify:business-events": "node verify_business_event_log.js",
    "verify:onboarding": "node verify_signup_onboarding.js",
    "verify:snapshot-versioning": "node verify_snapshot_versioning.js",
    "verify:quarter-governance": "node verify_quarter_governance_flow.js",
    "verify:auto-commit-governance": "node verify_auto_commit_governance.js all",
    "verify:integration-registry": "node verify_integration_registry.js all",
    "verify:inbox-actions": "node verify_inbox_actions.js",
    "validate:canonical-schemas": "node validate_canonical_schemas.js",
    "verify:monetary-integrity": "node verify_monetary_integrity_rules.js",
    "verify:voice-capture": "node verify_voice_capture_parser.js",
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Path 'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\quarterReadiness.test.js'" in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-ChildItem -Path 'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src' -Filter '*.test.*' | Select-Object -ExpandProperty FullName" in C:\Users\edebe\eds succeeded in 2.59s:
import {
  buildQuarterUiState,
  deriveResolvedIssueIdsFromSearchParams,
} from './quarterReadiness';

const reportFixture = {
  quarter_reference: 'Q1-2026',
  period_start: '2026-01-01',
  period_end: '2026-03-31',
  total_txns_in_period: 18,
  readiness_pct: 78,
  can_export: false,
  issue_list: [
    {
      issue_type: 'missing_category',
      affected_entity_id: 'txn-9101',
      severity: 'high',
      weight: 35,
      explanation: 'Category missing for Fuel Stop.',
      txn_date: '2026-02-04',
 succeeded in 2.59s:
C:\Users\edebe\eds\bizPA\frontend\src\controlCentre.test.js
C:\Users\edebe\eds\bizPA\frontend\src\governance.test.js
C:\Users\edebe\eds\bizPA\frontend\src\onboarding.test.js
C:\Users\edebe\eds\bizPA\frontend\src\quarterReadiness.test.js
Plan update
Reconciling the stale in-progress lifecycle task with the current workspace requires a small backend hardening pass, frontend binding for active inbox context, then validation and lifecycle updates.
  ✓ Inspect existing voice micro-decision backend and inbox UI paths; define the minimal context-safe flow to implement
  → Implement backend guardrails and frontend inbox/finish-now voice binding with confirmation chip and undo support
  • Add or update automated verification for the new behavior and run validations
  • Update the lifecycle task file with ordered evidence, validation results, and current completion state
codex
I’ve identified the real backend bug now: voice micro-decisions bypass the inbox audit path, so `/inbox/undo-last` cannot reliably undo voice-applied triage. I’m fixing that first by routing voice classification updates through the same audited classification persistence used by the inbox, then I’ll wire the frontend active-context and chip behavior on top of it.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$content = Get-Content -Path '"'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\App.jsx'; "'$content[1338..1398]' in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Path 'C:\\Users\\edebe\\eds\\bizPA\\backend\\src\\config\\db.js'" in C:\Users\edebe\eds succeeded in 2.55s:
  const [jobs, setJobs] = useState([]);
  const [calendarEvents, setCalendarEvents] = useState([]);
  const [diaryEntries, setDiaryEntries] = useState([]);
  const [theme, setTheme] = useState('light');
  const [appUsers, setAppUsers] = useState(USERS);
  const [currentUser, setCurrentUser] = useState(USERS[0]);
  const [sessionToken, setSessionToken] = useState('');
  const [onboardingSession, setOnboardingSession] = useState(null);
  const [authBootstrapped, setAuthBootstrapped] = useState(false);
  const [onboardingBusy, setOnboardingBusy] = useState(false);
  const [onboardingError, setOnboardingError] = useState('');
  const [policyRecords, setPolicyRecords] = useState(() => buildDefaultPolicyRecords({
    tenantId: TENANT_ID,
    actor: USERS[0],
    now: '2026-03-11T16:30:00.000Z',
  }));
  const [governanceAudit, setGovernanceAudit] = useState([]);
  const [syncTelemetry] = useState(DEFAULT_SYNC_TELEMETRY);
  const [activeSessions] = useState(DEFAULT_ACTIVE_SESSIONS);
  const [voiceStatus, setVoiceStatus] = useState('idle');
 succeeded in 2.46s:
const { Pool } = require('pg');
require('dotenv').config();

const pool = new Pool({
  connectionString: process.env.DATABASE_URL,
  max: 20,
  idleTimeoutMillis: 30000,
  connectionTimeoutMillis: 5000,
  // Supabase connection string requires SSL
  ssl: process.env.DB_SSL === 'false' ? false : { rejectUnauthorized: false }
});

pool.on('connect', () => {
  console.log('[Database] Connection established');
});

pool.on('error', (err) => {
  console.error('[Database] Unexpected error on idle client', err);
  process.exit(-1);
});
codex
I’m editing the persistence path now. The backend change will centralize audited classification writes so voice actions preserve existing fields, recalculate readiness, and become undoable through the existing inbox undo endpoint instead of bypassing it.
file update
A C:\Users\edebe\eds\bizPA\backend\src\services\inboxClassificationService.js
const {
  deriveQuarterFromDate,
  recordReadinessRecalculated,
} = require('./businessEventLogService');

async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
  await recordReadinessRecalculated(executor, {
    user_id: userId,
    actor_id: userId,
    source_type: summary?.source || 'manual',
    quarter_reference: deriveQuarterFromDate(txnDate),
    entity_id: bankTxnId,
    entity_type: 'bank_transaction',
    description,
    metadata: summary,
  });
}

async function upsertTransactionClassification(executor, {
  userId,
  bankTxnId,
  changedBy = userId,
  updates = {},
  auditSource = 'ui',
  readinessDescription = null,
}) {
  const existing = await executor.query(
    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
    [bankTxnId, userId]
  );
  const previous = existing.rows[0] || null;

  const next = {
    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
    split_business_pct: null,
    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
  };

  if (next.is_split) {
    if (updates.split_business_pct !== undefined) {
      next.split_business_pct = updates.split_business_pct;
    } else {
      next.split_business_pct = previous?.split_business_pct ?? null;
    }
  }

  const upsert = await executor.query(
    `
    INSERT INTO transaction_classifications
    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
    ON CONFLICT (bank_txn_id)
    DO UPDATE SET
      category_code = EXCLUDED.category_code,
      category_name = EXCLUDED.category_name,
      business_personal = EXCLUDED.business_personal,
      is_split = EXCLUDED.is_split,
      split_business_pct = EXCLUDED.split_business_pct,
      confidence = EXCLUDED.confidence,
      source = EXCLUDED.source,
      updated_at = CURRENT_TIMESTAMP
    RETURNING *
    `,
    [
      userId,
      bankTxnId,
      next.category_code,
      next.category_name,
      next.business_personal,
      next.is_split,
      next.split_business_pct,
      next.confidence,
      next.source,
    ]
  );

  await executor.query(
    `
    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
    VALUES ($1,$2,$3,$4,$5,$6,$7)
    `,
    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
  );

  const bankTxn = await executor.query(
    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
    [bankTxnId, userId]
  );

  await recordReadinessForTransaction(
    executor,
    userId,
    bankTxnId,
    bankTxn.rows[0]?.txn_date,
    {
      category_code: upsert.rows[0].category_code,
      business_personal: upsert.rows[0].business_personal,
      is_split: upsert.rows[0].is_split,
      split_business_pct: upsert.rows[0].split_business_pct,
      source: upsert.rows[0].source,
    },
    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
  );

  return upsert.rows[0];
}

module.exports = {
  upsertTransactionClassification,
};
apply_patch(auto_approved=true) exited 0 in 990ms:
Success. Updated the following files:
A C:\Users\edebe\eds\bizPA\backend\src\services\inboxClassificationService.js
file update:
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};

file update:
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};

file update
M C:\Users\edebe\eds\bizPA\backend\src\controllers\inboxController.js
@@ -9,2 +9,5 @@
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
@@ -202,48 +205,8 @@
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
@@ -251,9 +214,10 @@
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
apply_patch(auto_approved=true) exited 0 in 1.01s:
Success. Updated the following files:
M C:\Users\edebe\eds\bizPA\backend\src\controllers\inboxController.js
file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};

file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};

file update
M C:\Users\edebe\eds\bizPA\backend\src\controllers\voiceController.js
@@ -12,3 +12,30 @@
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+
+const MICRO_DECISION_CONTEXT_SCOPES = new Set(['inbox', 'finish_now']);
 
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
+
 /**
@@ -269,4 +296,12 @@
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_scope = null,
+  } = req.body || {};
   const text = String(transcript).toLowerCase().trim();
+  const normalizedContextScope = context_scope === null || context_scope === undefined
+    ? null
+    : String(context_scope).toLowerCase().replace(/-/g, '_');
 
@@ -274,18 +309,50 @@
 
+  const requireTransactionContext = () => {
+    if (!bank_txn_id) {
+      return 'bank_txn_id is required for inbox-bound micro-decision commands.';
+    }
+    if (!MICRO_DECISION_CONTEXT_SCOPES.has(normalizedContextScope)) {
+      return 'context_scope must be inbox or finish_now for transaction micro-decisions.';
+    }
+    return null;
+  };
+
   try {
     if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const categoryName = text.replace('category:', '').trim();
       const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: categoryCode,
+            category_name: categoryName,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${categoryName}`,
+        appliedActions: [{ type: 'category', label: categoryName }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
@@ -293,15 +360,38 @@
     if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${bp}`,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
@@ -310,3 +400,4 @@
     if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const pct = Number(splitMatch[1]);
@@ -315,13 +406,35 @@
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Split ${pct}%`,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
@@ -329,7 +442,17 @@
     if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: 'Attach receipt requested',
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        extra: {
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
@@ -366,3 +489,12 @@
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Match ${index}`,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
@@ -378,3 +510,12 @@
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: 'Applied: No match',
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
apply_patch(auto_approved=true) exited 0 in 3.05s:
Success. Updated the following files:
M C:\Users\edebe\eds\bizPA\backend\src\controllers\voiceController.js
file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..7564fe2a0b6132d2209937816e3f620cb3d91820
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,7 +10,34 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+
+const MICRO_DECISION_CONTEXT_SCOPES = new Set(['inbox', 'finish_now']);
 
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
+
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
  * Matches transcripts to core intents and extracts slots using regex.
@@ -267,71 +294,167 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_scope = null,
+  } = req.body || {};
   const text = String(transcript).toLowerCase().trim();
+  const normalizedContextScope = context_scope === null || context_scope === undefined
+    ? null
+    : String(context_scope).toLowerCase().replace(/-/g, '_');
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
 
+  const requireTransactionContext = () => {
+    if (!bank_txn_id) {
+      return 'bank_txn_id is required for inbox-bound micro-decision commands.';
+    }
+    if (!MICRO_DECISION_CONTEXT_SCOPES.has(normalizedContextScope)) {
+      return 'context_scope must be inbox or finish_now for transaction micro-decisions.';
+    }
+    return null;
+  };
+
   try {
     if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const categoryName = text.replace('category:', '').trim();
       const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: categoryCode,
+            category_name: categoryName,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${categoryName}`,
+        appliedActions: [{ type: 'category', label: categoryName }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${bp}`,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     const splitMatch = text.match(/split\s+(\d{1,3})%?/);
     if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const pct = Number(splitMatch[1]);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Split ${pct}%`,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: 'Attach receipt requested',
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        extra: {
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
     if (['match first', 'match second', 'match third'].includes(text)) {
@@ -364,7 +487,16 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Match ${index}`,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     if (text === 'no match') {
@@ -376,7 +508,16 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: 'Applied: No match',
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};

file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..7564fe2a0b6132d2209937816e3f620cb3d91820
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,7 +10,34 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+
+const MICRO_DECISION_CONTEXT_SCOPES = new Set(['inbox', 'finish_now']);
 
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
+
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
  * Matches transcripts to core intents and extracts slots using regex.
@@ -267,71 +294,167 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_scope = null,
+  } = req.body || {};
   const text = String(transcript).toLowerCase().trim();
+  const normalizedContextScope = context_scope === null || context_scope === undefined
+    ? null
+    : String(context_scope).toLowerCase().replace(/-/g, '_');
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
 
+  const requireTransactionContext = () => {
+    if (!bank_txn_id) {
+      return 'bank_txn_id is required for inbox-bound micro-decision commands.';
+    }
+    if (!MICRO_DECISION_CONTEXT_SCOPES.has(normalizedContextScope)) {
+      return 'context_scope must be inbox or finish_now for transaction micro-decisions.';
+    }
+    return null;
+  };
+
   try {
     if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const categoryName = text.replace('category:', '').trim();
       const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: categoryCode,
+            category_name: categoryName,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${categoryName}`,
+        appliedActions: [{ type: 'category', label: categoryName }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${bp}`,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     const splitMatch = text.match(/split\s+(\d{1,3})%?/);
     if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const pct = Number(splitMatch[1]);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Split ${pct}%`,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: 'Attach receipt requested',
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        extra: {
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
     if (['match first', 'match second', 'match third'].includes(text)) {
@@ -364,7 +487,16 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Match ${index}`,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     if (text === 'no match') {
@@ -376,7 +508,16 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: 'Applied: No match',
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};

codex
The backend audit/undo path is fixed. I’m wiring the client-side context selection and chip state next so voice triage only fires against an explicit inbox/finish-now target and the user gets a visible undo/edit affordance after each voice action.
file update
A C:\Users\edebe\eds\bizPA\frontend\src\voiceMicroDecisions.js
const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);

function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
  if (!activeVoiceContextId) {
    return null;
  }
  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
}

function buildVoiceMicroDecisionRequest({
  currentTab,
  transcript,
  activeVoiceContextId,
  inboxCards = [],
}) {
  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
    return null;
  }

  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
  if (!card) {
    return {
      error: 'Select a finish-now item before using voice triage.',
      card: null,
    };
  }

  return {
    endpoint: '/voice/micro-decision',
    card,
    payload: {
      transcript,
      bank_txn_id: card.id,
      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
    },
  };
}

function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
  if (!card) {
    return previousChip;
  }

  const nextLabels = Array.isArray(response.applied_actions)
    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
    : [];
  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
    ? previousChip.labels
    : [];
  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));

  return {
    targetId: card.id,
    targetLabel: card.merchant || 'Selected blocker',
    confirmationChip: response.confirmation_chip || '',
    labels,
    actionStatus: response.action_status || 'execute',
    undoSupported: Boolean(response.undo?.supported),
  };
}

module.exports = {
  buildVoiceMicroDecisionRequest,
  findVoiceContextCard,
  reduceVoiceMicroDecisionChip,
};
apply_patch(auto_approved=true) exited 0 in 1.30s:
Success. Updated the following files:
A C:\Users\edebe\eds\bizPA\frontend\src\voiceMicroDecisions.js
file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..7564fe2a0b6132d2209937816e3f620cb3d91820
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,7 +10,34 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+
+const MICRO_DECISION_CONTEXT_SCOPES = new Set(['inbox', 'finish_now']);
 
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
+
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
  * Matches transcripts to core intents and extracts slots using regex.
@@ -267,71 +294,167 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_scope = null,
+  } = req.body || {};
   const text = String(transcript).toLowerCase().trim();
+  const normalizedContextScope = context_scope === null || context_scope === undefined
+    ? null
+    : String(context_scope).toLowerCase().replace(/-/g, '_');
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
 
+  const requireTransactionContext = () => {
+    if (!bank_txn_id) {
+      return 'bank_txn_id is required for inbox-bound micro-decision commands.';
+    }
+    if (!MICRO_DECISION_CONTEXT_SCOPES.has(normalizedContextScope)) {
+      return 'context_scope must be inbox or finish_now for transaction micro-decisions.';
+    }
+    return null;
+  };
+
   try {
     if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const categoryName = text.replace('category:', '').trim();
       const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: categoryCode,
+            category_name: categoryName,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${categoryName}`,
+        appliedActions: [{ type: 'category', label: categoryName }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${bp}`,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     const splitMatch = text.match(/split\s+(\d{1,3})%?/);
     if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const pct = Number(splitMatch[1]);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Split ${pct}%`,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: 'Attach receipt requested',
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        extra: {
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
     if (['match first', 'match second', 'match third'].includes(text)) {
@@ -364,7 +487,16 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Match ${index}`,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     if (text === 'no match') {
@@ -376,7 +508,16 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: 'Applied: No match',
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..4e5e3e7597a1938eaf706d38195501dec109974c
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+module.exports = {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};

file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..7564fe2a0b6132d2209937816e3f620cb3d91820
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,7 +10,34 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+
+const MICRO_DECISION_CONTEXT_SCOPES = new Set(['inbox', 'finish_now']);
 
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
+
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
  * Matches transcripts to core intents and extracts slots using regex.
@@ -267,71 +294,167 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_scope = null,
+  } = req.body || {};
   const text = String(transcript).toLowerCase().trim();
+  const normalizedContextScope = context_scope === null || context_scope === undefined
+    ? null
+    : String(context_scope).toLowerCase().replace(/-/g, '_');
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
 
+  const requireTransactionContext = () => {
+    if (!bank_txn_id) {
+      return 'bank_txn_id is required for inbox-bound micro-decision commands.';
+    }
+    if (!MICRO_DECISION_CONTEXT_SCOPES.has(normalizedContextScope)) {
+      return 'context_scope must be inbox or finish_now for transaction micro-decisions.';
+    }
+    return null;
+  };
+
   try {
     if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const categoryName = text.replace('category:', '').trim();
       const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: categoryCode,
+            category_name: categoryName,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${categoryName}`,
+        appliedActions: [{ type: 'category', label: categoryName }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${bp}`,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     const splitMatch = text.match(/split\s+(\d{1,3})%?/);
     if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const pct = Number(splitMatch[1]);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Split ${pct}%`,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: 'Attach receipt requested',
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        extra: {
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
     if (['match first', 'match second', 'match third'].includes(text)) {
@@ -364,7 +487,16 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Match ${index}`,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     if (text === 'no match') {
@@ -376,7 +508,16 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: 'Applied: No match',
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..4e5e3e7597a1938eaf706d38195501dec109974c
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+module.exports = {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};

file update
A C:\Users\edebe\eds\bizPA\frontend\src\voiceMicroDecisions.test.js
const {
  buildVoiceMicroDecisionRequest,
  findVoiceContextCard,
  reduceVoiceMicroDecisionChip,
} = require('./voiceMicroDecisions');

const cards = [
  { id: 'txn-1', merchant: 'Fuel Stop' },
  { id: 'txn-2', merchant: 'Builder Depot' },
];

describe('voice micro-decision helpers', () => {
  test('requires explicit finish-now selection before routing voice triage', () => {
    const request = buildVoiceMicroDecisionRequest({
      currentTab: 'inbox',
      transcript: 'Business',
      activeVoiceContextId: '',
      inboxCards: cards,
    });

    expect(request.error).toBe('Select a finish-now item before using voice triage.');
    expect(request.card).toBeNull();
  });

  test('builds a finish-now scoped request for the selected blocker', () => {
    const request = buildVoiceMicroDecisionRequest({
      currentTab: 'quarter',
      transcript: 'Split 70%',
      activeVoiceContextId: 'txn-2',
      inboxCards: cards,
    });

    expect(request.endpoint).toBe('/voice/micro-decision');
    expect(request.card).toEqual(cards[1]);
    expect(request.payload).toEqual({
      transcript: 'Split 70%',
      bank_txn_id: 'txn-2',
      context_scope: 'finish_now',
    });
  });

  test('keeps the selected voice target discoverable', () => {
    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
  });

  test('combines applied actions for the same target chip and resets for a new target', () => {
    const first = reduceVoiceMicroDecisionChip(null, {
      confirmation_chip: 'Applied: Travel',
      action_status: 'execute',
      applied_actions: [{ label: 'Travel' }],
      undo: { supported: true },
    }, cards[0]);

    const second = reduceVoiceMicroDecisionChip(first, {
      confirmation_chip: 'Applied: BUSINESS',
      action_status: 'execute',
      applied_actions: [{ label: 'BUSINESS' }],
      undo: { supported: true },
    }, cards[0]);

    const third = reduceVoiceMicroDecisionChip(second, {
      confirmation_chip: 'Applied: Split 70%',
      action_status: 'execute',
      applied_actions: [{ label: 'Split 70%' }],
      undo: { supported: true },
    }, cards[1]);

    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
    expect(second.undoSupported).toBe(true);
    expect(third.targetId).toBe('txn-2');
    expect(third.labels).toEqual(['Split 70%']);
  });
});
apply_patch(auto_approved=true) exited 0 in 1.52s:
Success. Updated the following files:
A C:\Users\edebe\eds\bizPA\frontend\src\voiceMicroDecisions.test.js
file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..7564fe2a0b6132d2209937816e3f620cb3d91820
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,7 +10,34 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+
+const MICRO_DECISION_CONTEXT_SCOPES = new Set(['inbox', 'finish_now']);
 
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
+
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
  * Matches transcripts to core intents and extracts slots using regex.
@@ -267,71 +294,167 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_scope = null,
+  } = req.body || {};
   const text = String(transcript).toLowerCase().trim();
+  const normalizedContextScope = context_scope === null || context_scope === undefined
+    ? null
+    : String(context_scope).toLowerCase().replace(/-/g, '_');
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
 
+  const requireTransactionContext = () => {
+    if (!bank_txn_id) {
+      return 'bank_txn_id is required for inbox-bound micro-decision commands.';
+    }
+    if (!MICRO_DECISION_CONTEXT_SCOPES.has(normalizedContextScope)) {
+      return 'context_scope must be inbox or finish_now for transaction micro-decisions.';
+    }
+    return null;
+  };
+
   try {
     if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const categoryName = text.replace('category:', '').trim();
       const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: categoryCode,
+            category_name: categoryName,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${categoryName}`,
+        appliedActions: [{ type: 'category', label: categoryName }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${bp}`,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     const splitMatch = text.match(/split\s+(\d{1,3})%?/);
     if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const pct = Number(splitMatch[1]);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Split ${pct}%`,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: 'Attach receipt requested',
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        extra: {
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
     if (['match first', 'match second', 'match third'].includes(text)) {
@@ -364,7 +487,16 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Match ${index}`,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     if (text === 'no match') {
@@ -376,7 +508,16 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: 'Applied: No match',
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..4e5e3e7597a1938eaf706d38195501dec109974c
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+module.exports = {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..145ffac2fb605c21c014baa22d1f04db82164519
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+const {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} = require('./voiceMicroDecisions');
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});

file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..7564fe2a0b6132d2209937816e3f620cb3d91820
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,7 +10,34 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+
+const MICRO_DECISION_CONTEXT_SCOPES = new Set(['inbox', 'finish_now']);
 
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
+
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
  * Matches transcripts to core intents and extracts slots using regex.
@@ -267,71 +294,167 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_scope = null,
+  } = req.body || {};
   const text = String(transcript).toLowerCase().trim();
+  const normalizedContextScope = context_scope === null || context_scope === undefined
+    ? null
+    : String(context_scope).toLowerCase().replace(/-/g, '_');
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
 
+  const requireTransactionContext = () => {
+    if (!bank_txn_id) {
+      return 'bank_txn_id is required for inbox-bound micro-decision commands.';
+    }
+    if (!MICRO_DECISION_CONTEXT_SCOPES.has(normalizedContextScope)) {
+      return 'context_scope must be inbox or finish_now for transaction micro-decisions.';
+    }
+    return null;
+  };
+
   try {
     if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const categoryName = text.replace('category:', '').trim();
       const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: categoryCode,
+            category_name: categoryName,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${categoryName}`,
+        appliedActions: [{ type: 'category', label: categoryName }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${bp}`,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     const splitMatch = text.match(/split\s+(\d{1,3})%?/);
     if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const pct = Number(splitMatch[1]);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Split ${pct}%`,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: 'Attach receipt requested',
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        extra: {
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
     if (['match first', 'match second', 'match third'].includes(text)) {
@@ -364,7 +487,16 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Match ${index}`,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     if (text === 'no match') {
@@ -376,7 +508,16 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: 'Applied: No match',
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..4e5e3e7597a1938eaf706d38195501dec109974c
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+module.exports = {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..145ffac2fb605c21c014baa22d1f04db82164519
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+const {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} = require('./voiceMicroDecisions');
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});

file update
M C:\Users\edebe\eds\bizPA\frontend\src\voiceMicroDecisions.js
@@ -61,3 +61,3 @@
 
-module.exports = {
+export {
   buildVoiceMicroDecisionRequest,
apply_patch(auto_approved=true) exited 0 in 5.73s:
Success. Updated the following files:
M C:\Users\edebe\eds\bizPA\frontend\src\voiceMicroDecisions.js
file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..7564fe2a0b6132d2209937816e3f620cb3d91820
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,7 +10,34 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+
+const MICRO_DECISION_CONTEXT_SCOPES = new Set(['inbox', 'finish_now']);
 
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
+
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
  * Matches transcripts to core intents and extracts slots using regex.
@@ -267,71 +294,167 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_scope = null,
+  } = req.body || {};
   const text = String(transcript).toLowerCase().trim();
+  const normalizedContextScope = context_scope === null || context_scope === undefined
+    ? null
+    : String(context_scope).toLowerCase().replace(/-/g, '_');
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
 
+  const requireTransactionContext = () => {
+    if (!bank_txn_id) {
+      return 'bank_txn_id is required for inbox-bound micro-decision commands.';
+    }
+    if (!MICRO_DECISION_CONTEXT_SCOPES.has(normalizedContextScope)) {
+      return 'context_scope must be inbox or finish_now for transaction micro-decisions.';
+    }
+    return null;
+  };
+
   try {
     if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const categoryName = text.replace('category:', '').trim();
       const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: categoryCode,
+            category_name: categoryName,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${categoryName}`,
+        appliedActions: [{ type: 'category', label: categoryName }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${bp}`,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     const splitMatch = text.match(/split\s+(\d{1,3})%?/);
     if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const pct = Number(splitMatch[1]);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Split ${pct}%`,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: 'Attach receipt requested',
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        extra: {
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
     if (['match first', 'match second', 'match third'].includes(text)) {
@@ -364,7 +487,16 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Match ${index}`,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     if (text === 'no match') {
@@ -376,7 +508,16 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: 'Applied: No match',
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..145ffac2fb605c21c014baa22d1f04db82164519
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+const {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} = require('./voiceMicroDecisions');
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});

file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..7564fe2a0b6132d2209937816e3f620cb3d91820
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,7 +10,34 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+
+const MICRO_DECISION_CONTEXT_SCOPES = new Set(['inbox', 'finish_now']);
 
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
+
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
  * Matches transcripts to core intents and extracts slots using regex.
@@ -267,71 +294,167 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_scope = null,
+  } = req.body || {};
   const text = String(transcript).toLowerCase().trim();
+  const normalizedContextScope = context_scope === null || context_scope === undefined
+    ? null
+    : String(context_scope).toLowerCase().replace(/-/g, '_');
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
 
+  const requireTransactionContext = () => {
+    if (!bank_txn_id) {
+      return 'bank_txn_id is required for inbox-bound micro-decision commands.';
+    }
+    if (!MICRO_DECISION_CONTEXT_SCOPES.has(normalizedContextScope)) {
+      return 'context_scope must be inbox or finish_now for transaction micro-decisions.';
+    }
+    return null;
+  };
+
   try {
     if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const categoryName = text.replace('category:', '').trim();
       const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: categoryCode,
+            category_name: categoryName,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${categoryName}`,
+        appliedActions: [{ type: 'category', label: categoryName }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${bp}`,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     const splitMatch = text.match(/split\s+(\d{1,3})%?/);
     if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const pct = Number(splitMatch[1]);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Split ${pct}%`,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: 'Attach receipt requested',
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        extra: {
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
     if (['match first', 'match second', 'match third'].includes(text)) {
@@ -364,7 +487,16 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Match ${index}`,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     if (text === 'no match') {
@@ -376,7 +508,16 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: 'Applied: No match',
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..145ffac2fb605c21c014baa22d1f04db82164519
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+const {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} = require('./voiceMicroDecisions');
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});

file update
M C:\Users\edebe\eds\bizPA\frontend\src\voiceMicroDecisions.test.js
@@ -1,2 +1,2 @@
-const {
+import {
   buildVoiceMicroDecisionRequest,
@@ -4,3 +4,3 @@
   reduceVoiceMicroDecisionChip,
-} = require('./voiceMicroDecisions');
+} from './voiceMicroDecisions';
 
apply_patch(auto_approved=true) exited 0 in 2.66s:
Success. Updated the following files:
M C:\Users\edebe\eds\bizPA\frontend\src\voiceMicroDecisions.test.js
file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..7564fe2a0b6132d2209937816e3f620cb3d91820
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,7 +10,34 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+
+const MICRO_DECISION_CONTEXT_SCOPES = new Set(['inbox', 'finish_now']);
 
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
+
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
  * Matches transcripts to core intents and extracts slots using regex.
@@ -267,71 +294,167 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_scope = null,
+  } = req.body || {};
   const text = String(transcript).toLowerCase().trim();
+  const normalizedContextScope = context_scope === null || context_scope === undefined
+    ? null
+    : String(context_scope).toLowerCase().replace(/-/g, '_');
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
 
+  const requireTransactionContext = () => {
+    if (!bank_txn_id) {
+      return 'bank_txn_id is required for inbox-bound micro-decision commands.';
+    }
+    if (!MICRO_DECISION_CONTEXT_SCOPES.has(normalizedContextScope)) {
+      return 'context_scope must be inbox or finish_now for transaction micro-decisions.';
+    }
+    return null;
+  };
+
   try {
     if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const categoryName = text.replace('category:', '').trim();
       const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: categoryCode,
+            category_name: categoryName,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${categoryName}`,
+        appliedActions: [{ type: 'category', label: categoryName }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${bp}`,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     const splitMatch = text.match(/split\s+(\d{1,3})%?/);
     if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const pct = Number(splitMatch[1]);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Split ${pct}%`,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: 'Attach receipt requested',
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        extra: {
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
     if (['match first', 'match second', 'match third'].includes(text)) {
@@ -364,7 +487,16 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Match ${index}`,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     if (text === 'no match') {
@@ -376,7 +508,16 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: 'Applied: No match',
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..6eb469fcc7f023f8545d9e952a5823fb8c47a88d
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});

file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..7564fe2a0b6132d2209937816e3f620cb3d91820
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,7 +10,34 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+
+const MICRO_DECISION_CONTEXT_SCOPES = new Set(['inbox', 'finish_now']);
 
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
+
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
  * Matches transcripts to core intents and extracts slots using regex.
@@ -267,71 +294,167 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_scope = null,
+  } = req.body || {};
   const text = String(transcript).toLowerCase().trim();
+  const normalizedContextScope = context_scope === null || context_scope === undefined
+    ? null
+    : String(context_scope).toLowerCase().replace(/-/g, '_');
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
 
+  const requireTransactionContext = () => {
+    if (!bank_txn_id) {
+      return 'bank_txn_id is required for inbox-bound micro-decision commands.';
+    }
+    if (!MICRO_DECISION_CONTEXT_SCOPES.has(normalizedContextScope)) {
+      return 'context_scope must be inbox or finish_now for transaction micro-decisions.';
+    }
+    return null;
+  };
+
   try {
     if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const categoryName = text.replace('category:', '').trim();
       const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: categoryCode,
+            category_name: categoryName,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${categoryName}`,
+        appliedActions: [{ type: 'category', label: categoryName }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${bp}`,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     const splitMatch = text.match(/split\s+(\d{1,3})%?/);
     if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const pct = Number(splitMatch[1]);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Split ${pct}%`,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: 'Attach receipt requested',
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        extra: {
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
     if (['match first', 'match second', 'match third'].includes(text)) {
@@ -364,7 +487,16 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Match ${index}`,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     if (text === 'no match') {
@@ -376,7 +508,16 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: 'Applied: No match',
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..6eb469fcc7f023f8545d9e952a5823fb8c47a88d
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});

file update
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
@@ -52,2 +52,7 @@
 } from './quarterReadiness';
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
 import OnboardingGate from './OnboardingGate';
apply_patch(auto_approved=true) exited 0 in 1.29s:
Success. Updated the following files:
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..7564fe2a0b6132d2209937816e3f620cb3d91820
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,7 +10,34 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+
+const MICRO_DECISION_CONTEXT_SCOPES = new Set(['inbox', 'finish_now']);
 
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
+
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
  * Matches transcripts to core intents and extracts slots using regex.
@@ -267,71 +294,167 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_scope = null,
+  } = req.body || {};
   const text = String(transcript).toLowerCase().trim();
+  const normalizedContextScope = context_scope === null || context_scope === undefined
+    ? null
+    : String(context_scope).toLowerCase().replace(/-/g, '_');
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
 
+  const requireTransactionContext = () => {
+    if (!bank_txn_id) {
+      return 'bank_txn_id is required for inbox-bound micro-decision commands.';
+    }
+    if (!MICRO_DECISION_CONTEXT_SCOPES.has(normalizedContextScope)) {
+      return 'context_scope must be inbox or finish_now for transaction micro-decisions.';
+    }
+    return null;
+  };
+
   try {
     if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const categoryName = text.replace('category:', '').trim();
       const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: categoryCode,
+            category_name: categoryName,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${categoryName}`,
+        appliedActions: [{ type: 'category', label: categoryName }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${bp}`,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     const splitMatch = text.match(/split\s+(\d{1,3})%?/);
     if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const pct = Number(splitMatch[1]);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Split ${pct}%`,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: 'Attach receipt requested',
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        extra: {
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
     if (['match first', 'match second', 'match third'].includes(text)) {
@@ -364,7 +487,16 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Match ${index}`,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     if (text === 'no match') {
@@ -376,7 +508,16 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: 'Applied: No match',
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index 9b145fadac9123e3fa01b12af6ef2b9916dc11de..0325c9dd7c00cb09cdb9eddb673d79be1921f714
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -50,6 +50,11 @@
   buildQuarterUiState,
   deriveResolvedIssueIdsFromSearchParams,
 } from './quarterReadiness';
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..6eb469fcc7f023f8545d9e952a5823fb8c47a88d
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});

file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..7564fe2a0b6132d2209937816e3f620cb3d91820
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,7 +10,34 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+
+const MICRO_DECISION_CONTEXT_SCOPES = new Set(['inbox', 'finish_now']);
 
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
+
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
  * Matches transcripts to core intents and extracts slots using regex.
@@ -267,71 +294,167 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_scope = null,
+  } = req.body || {};
   const text = String(transcript).toLowerCase().trim();
+  const normalizedContextScope = context_scope === null || context_scope === undefined
+    ? null
+    : String(context_scope).toLowerCase().replace(/-/g, '_');
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
 
+  const requireTransactionContext = () => {
+    if (!bank_txn_id) {
+      return 'bank_txn_id is required for inbox-bound micro-decision commands.';
+    }
+    if (!MICRO_DECISION_CONTEXT_SCOPES.has(normalizedContextScope)) {
+      return 'context_scope must be inbox or finish_now for transaction micro-decisions.';
+    }
+    return null;
+  };
+
   try {
     if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const categoryName = text.replace('category:', '').trim();
       const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: categoryCode,
+            category_name: categoryName,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${categoryName}`,
+        appliedActions: [{ type: 'category', label: categoryName }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${bp}`,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     const splitMatch = text.match(/split\s+(\d{1,3})%?/);
     if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const pct = Number(splitMatch[1]);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Split ${pct}%`,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: 'Attach receipt requested',
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        extra: {
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
     if (['match first', 'match second', 'match third'].includes(text)) {
@@ -364,7 +487,16 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Match ${index}`,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     if (text === 'no match') {
@@ -376,7 +508,16 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: 'Applied: No match',
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index 9b145fadac9123e3fa01b12af6ef2b9916dc11de..0325c9dd7c00cb09cdb9eddb673d79be1921f714
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -50,6 +50,11 @@
   buildQuarterUiState,
   deriveResolvedIssueIdsFromSearchParams,
 } from './quarterReadiness';
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..6eb469fcc7f023f8545d9e952a5823fb8c47a88d
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});

file update
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
@@ -434,2 +434,9 @@
   .voice-preview { background: rgba(0,0,0,0.82); color: white; padding: 8px 15px; border-radius: 15px; margin-bottom: 8px; font-size: 0.72rem; max-width: 240px; text-align: right; }
+  .voice-context-banner { display: grid; gap: 10px; padding: 14px 16px; margin-bottom: 16px; border-radius: 18px; background: var(--bg-card); border: 1px solid var(--border); box-shadow: var(--shadow); }
+  .voice-context-row, .voice-chip-actions, .voice-chip-labels { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
+  .voice-context-pill, .voice-action-pill { border-radius: 999px; padding: 6px 10px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.74rem; font-weight: 700; }
+  .voice-context-pill.active { background: rgba(13,92,99,0.12); color: var(--primary-strong); border-color: rgba(13,92,99,0.24); }
+  .voice-micro-chip { display: grid; gap: 10px; padding: 14px 16px; border-radius: 18px; background: rgba(13,92,99,0.08); border: 1px solid rgba(13,92,99,0.18); }
+  .voice-micro-chip strong { display: block; }
+  .inbox-action-card.voice-target { border-color: rgba(13,92,99,0.36); box-shadow: 0 0 0 2px rgba(13,92,99,0.12), var(--shadow); }
   .spinner { animation: rotate 2s linear infinite; }
apply_patch(auto_approved=true) exited 0 in 1.62s:
Success. Updated the following files:
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..7564fe2a0b6132d2209937816e3f620cb3d91820
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,7 +10,34 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+
+const MICRO_DECISION_CONTEXT_SCOPES = new Set(['inbox', 'finish_now']);
 
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
+
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
  * Matches transcripts to core intents and extracts slots using regex.
@@ -267,71 +294,167 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_scope = null,
+  } = req.body || {};
   const text = String(transcript).toLowerCase().trim();
+  const normalizedContextScope = context_scope === null || context_scope === undefined
+    ? null
+    : String(context_scope).toLowerCase().replace(/-/g, '_');
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
 
+  const requireTransactionContext = () => {
+    if (!bank_txn_id) {
+      return 'bank_txn_id is required for inbox-bound micro-decision commands.';
+    }
+    if (!MICRO_DECISION_CONTEXT_SCOPES.has(normalizedContextScope)) {
+      return 'context_scope must be inbox or finish_now for transaction micro-decisions.';
+    }
+    return null;
+  };
+
   try {
     if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const categoryName = text.replace('category:', '').trim();
       const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: categoryCode,
+            category_name: categoryName,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${categoryName}`,
+        appliedActions: [{ type: 'category', label: categoryName }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${bp}`,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     const splitMatch = text.match(/split\s+(\d{1,3})%?/);
     if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const pct = Number(splitMatch[1]);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Split ${pct}%`,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: 'Attach receipt requested',
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        extra: {
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
     if (['match first', 'match second', 'match third'].includes(text)) {
@@ -364,7 +487,16 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Match ${index}`,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     if (text === 'no match') {
@@ -376,7 +508,16 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: 'Applied: No match',
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index 9b145fadac9123e3fa01b12af6ef2b9916dc11de..d50fa50ca373c0442899c19cedb636879211fd61
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -50,6 +50,11 @@
   buildQuarterUiState,
   deriveResolvedIssueIdsFromSearchParams,
 } from './quarterReadiness';
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -427,6 +432,13 @@
   .date-group-count { border-radius: 999px; padding: 4px 9px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.72rem; color: var(--text-muted); }
   .date-group-body { padding: 0 12px 12px; }
   .voice-preview { background: rgba(0,0,0,0.82); color: white; padding: 8px 15px; border-radius: 15px; margin-bottom: 8px; font-size: 0.72rem; max-width: 240px; text-align: right; }
+  .voice-context-banner { display: grid; gap: 10px; padding: 14px 16px; margin-bottom: 16px; border-radius: 18px; background: var(--bg-card); border: 1px solid var(--border); box-shadow: var(--shadow); }
+  .voice-context-row, .voice-chip-actions, .voice-chip-labels { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
+  .voice-context-pill, .voice-action-pill { border-radius: 999px; padding: 6px 10px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.74rem; font-weight: 700; }
+  .voice-context-pill.active { background: rgba(13,92,99,0.12); color: var(--primary-strong); border-color: rgba(13,92,99,0.24); }
+  .voice-micro-chip { display: grid; gap: 10px; padding: 14px 16px; border-radius: 18px; background: rgba(13,92,99,0.08); border: 1px solid rgba(13,92,99,0.18); }
+  .voice-micro-chip strong { display: block; }
+  .inbox-action-card.voice-target { border-color: rgba(13,92,99,0.36); box-shadow: 0 0 0 2px rgba(13,92,99,0.12), var(--shadow); }
   .spinner { animation: rotate 2s linear infinite; }
   .capture-grid { display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(320px, 0.95fr); gap: 18px; align-items: start; }
   .command-centre { display: grid; gap: 18px; }
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..6eb469fcc7f023f8545d9e952a5823fb8c47a88d
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});

file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..7564fe2a0b6132d2209937816e3f620cb3d91820
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,7 +10,34 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+
+const MICRO_DECISION_CONTEXT_SCOPES = new Set(['inbox', 'finish_now']);
 
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
+
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
  * Matches transcripts to core intents and extracts slots using regex.
@@ -267,71 +294,167 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_scope = null,
+  } = req.body || {};
   const text = String(transcript).toLowerCase().trim();
+  const normalizedContextScope = context_scope === null || context_scope === undefined
+    ? null
+    : String(context_scope).toLowerCase().replace(/-/g, '_');
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
 
+  const requireTransactionContext = () => {
+    if (!bank_txn_id) {
+      return 'bank_txn_id is required for inbox-bound micro-decision commands.';
+    }
+    if (!MICRO_DECISION_CONTEXT_SCOPES.has(normalizedContextScope)) {
+      return 'context_scope must be inbox or finish_now for transaction micro-decisions.';
+    }
+    return null;
+  };
+
   try {
     if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const categoryName = text.replace('category:', '').trim();
       const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: categoryCode,
+            category_name: categoryName,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${categoryName}`,
+        appliedActions: [{ type: 'category', label: categoryName }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${bp}`,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     const splitMatch = text.match(/split\s+(\d{1,3})%?/);
     if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const pct = Number(splitMatch[1]);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Split ${pct}%`,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: 'Attach receipt requested',
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        extra: {
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
     if (['match first', 'match second', 'match third'].includes(text)) {
@@ -364,7 +487,16 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Match ${index}`,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     if (text === 'no match') {
@@ -376,7 +508,16 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: 'Applied: No match',
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index 9b145fadac9123e3fa01b12af6ef2b9916dc11de..d50fa50ca373c0442899c19cedb636879211fd61
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -50,6 +50,11 @@
   buildQuarterUiState,
   deriveResolvedIssueIdsFromSearchParams,
 } from './quarterReadiness';
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -427,6 +432,13 @@
   .date-group-count { border-radius: 999px; padding: 4px 9px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.72rem; color: var(--text-muted); }
   .date-group-body { padding: 0 12px 12px; }
   .voice-preview { background: rgba(0,0,0,0.82); color: white; padding: 8px 15px; border-radius: 15px; margin-bottom: 8px; font-size: 0.72rem; max-width: 240px; text-align: right; }
+  .voice-context-banner { display: grid; gap: 10px; padding: 14px 16px; margin-bottom: 16px; border-radius: 18px; background: var(--bg-card); border: 1px solid var(--border); box-shadow: var(--shadow); }
+  .voice-context-row, .voice-chip-actions, .voice-chip-labels { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
+  .voice-context-pill, .voice-action-pill { border-radius: 999px; padding: 6px 10px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.74rem; font-weight: 700; }
+  .voice-context-pill.active { background: rgba(13,92,99,0.12); color: var(--primary-strong); border-color: rgba(13,92,99,0.24); }
+  .voice-micro-chip { display: grid; gap: 10px; padding: 14px 16px; border-radius: 18px; background: rgba(13,92,99,0.08); border: 1px solid rgba(13,92,99,0.18); }
+  .voice-micro-chip strong { display: block; }
+  .inbox-action-card.voice-target { border-color: rgba(13,92,99,0.36); box-shadow: 0 0 0 2px rgba(13,92,99,0.12), var(--shadow); }
   .spinner { animation: rotate 2s linear infinite; }
   .capture-grid { display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(320px, 0.95fr); gap: 18px; align-items: start; }
   .command-centre { display: grid; gap: 18px; }
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..6eb469fcc7f023f8545d9e952a5823fb8c47a88d
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});

file update
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
@@ -1396,2 +1396,4 @@
   const [inboxMessage, setInboxMessage] = useState(null);
+  const [activeVoiceContextId, setActiveVoiceContextId] = useState('');
+  const [voiceMicroDecisionChip, setVoiceMicroDecisionChip] = useState(null);
   const [inboxLoadMs, setInboxLoadMs] = useState(null);
apply_patch(auto_approved=true) exited 0 in 2.84s:
Success. Updated the following files:
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..7564fe2a0b6132d2209937816e3f620cb3d91820
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,7 +10,34 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+
+const MICRO_DECISION_CONTEXT_SCOPES = new Set(['inbox', 'finish_now']);
 
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
+
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
  * Matches transcripts to core intents and extracts slots using regex.
@@ -267,71 +294,167 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_scope = null,
+  } = req.body || {};
   const text = String(transcript).toLowerCase().trim();
+  const normalizedContextScope = context_scope === null || context_scope === undefined
+    ? null
+    : String(context_scope).toLowerCase().replace(/-/g, '_');
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
 
+  const requireTransactionContext = () => {
+    if (!bank_txn_id) {
+      return 'bank_txn_id is required for inbox-bound micro-decision commands.';
+    }
+    if (!MICRO_DECISION_CONTEXT_SCOPES.has(normalizedContextScope)) {
+      return 'context_scope must be inbox or finish_now for transaction micro-decisions.';
+    }
+    return null;
+  };
+
   try {
     if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const categoryName = text.replace('category:', '').trim();
       const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: categoryCode,
+            category_name: categoryName,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${categoryName}`,
+        appliedActions: [{ type: 'category', label: categoryName }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${bp}`,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     const splitMatch = text.match(/split\s+(\d{1,3})%?/);
     if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const pct = Number(splitMatch[1]);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Split ${pct}%`,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: 'Attach receipt requested',
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        extra: {
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
     if (['match first', 'match second', 'match third'].includes(text)) {
@@ -364,7 +487,16 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Match ${index}`,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     if (text === 'no match') {
@@ -376,7 +508,16 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: 'Applied: No match',
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index 9b145fadac9123e3fa01b12af6ef2b9916dc11de..4a9d04848d7c0cfedadab802d4d6566982d0f205
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -50,6 +50,11 @@
   buildQuarterUiState,
   deriveResolvedIssueIdsFromSearchParams,
 } from './quarterReadiness';
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -427,6 +432,13 @@
   .date-group-count { border-radius: 999px; padding: 4px 9px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.72rem; color: var(--text-muted); }
   .date-group-body { padding: 0 12px 12px; }
   .voice-preview { background: rgba(0,0,0,0.82); color: white; padding: 8px 15px; border-radius: 15px; margin-bottom: 8px; font-size: 0.72rem; max-width: 240px; text-align: right; }
+  .voice-context-banner { display: grid; gap: 10px; padding: 14px 16px; margin-bottom: 16px; border-radius: 18px; background: var(--bg-card); border: 1px solid var(--border); box-shadow: var(--shadow); }
+  .voice-context-row, .voice-chip-actions, .voice-chip-labels { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
+  .voice-context-pill, .voice-action-pill { border-radius: 999px; padding: 6px 10px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.74rem; font-weight: 700; }
+  .voice-context-pill.active { background: rgba(13,92,99,0.12); color: var(--primary-strong); border-color: rgba(13,92,99,0.24); }
+  .voice-micro-chip { display: grid; gap: 10px; padding: 14px 16px; border-radius: 18px; background: rgba(13,92,99,0.08); border: 1px solid rgba(13,92,99,0.18); }
+  .voice-micro-chip strong { display: block; }
+  .inbox-action-card.voice-target { border-color: rgba(13,92,99,0.36); box-shadow: 0 0 0 2px rgba(13,92,99,0.12), var(--shadow); }
   .spinner { animation: rotate 2s linear infinite; }
   .capture-grid { display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(320px, 0.95fr); gap: 18px; align-items: start; }
   .command-centre { display: grid; gap: 18px; }
@@ -1382,6 +1394,8 @@
   const [inboxDrafts, setInboxDrafts] = useState({});
   const [inboxBusyId, setInboxBusyId] = useState('');
   const [inboxMessage, setInboxMessage] = useState(null);
+  const [activeVoiceContextId, setActiveVoiceContextId] = useState('');
+  const [voiceMicroDecisionChip, setVoiceMicroDecisionChip] = useState(null);
   const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..6eb469fcc7f023f8545d9e952a5823fb8c47a88d
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});

file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..7564fe2a0b6132d2209937816e3f620cb3d91820
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,7 +10,34 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+
+const MICRO_DECISION_CONTEXT_SCOPES = new Set(['inbox', 'finish_now']);
 
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
+
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
  * Matches transcripts to core intents and extracts slots using regex.
@@ -267,71 +294,167 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_scope = null,
+  } = req.body || {};
   const text = String(transcript).toLowerCase().trim();
+  const normalizedContextScope = context_scope === null || context_scope === undefined
+    ? null
+    : String(context_scope).toLowerCase().replace(/-/g, '_');
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
 
+  const requireTransactionContext = () => {
+    if (!bank_txn_id) {
+      return 'bank_txn_id is required for inbox-bound micro-decision commands.';
+    }
+    if (!MICRO_DECISION_CONTEXT_SCOPES.has(normalizedContextScope)) {
+      return 'context_scope must be inbox or finish_now for transaction micro-decisions.';
+    }
+    return null;
+  };
+
   try {
     if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const categoryName = text.replace('category:', '').trim();
       const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: categoryCode,
+            category_name: categoryName,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${categoryName}`,
+        appliedActions: [{ type: 'category', label: categoryName }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${bp}`,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     const splitMatch = text.match(/split\s+(\d{1,3})%?/);
     if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const pct = Number(splitMatch[1]);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Split ${pct}%`,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: 'Attach receipt requested',
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        extra: {
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
     if (['match first', 'match second', 'match third'].includes(text)) {
@@ -364,7 +487,16 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Match ${index}`,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     if (text === 'no match') {
@@ -376,7 +508,16 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: 'Applied: No match',
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index 9b145fadac9123e3fa01b12af6ef2b9916dc11de..4a9d04848d7c0cfedadab802d4d6566982d0f205
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -50,6 +50,11 @@
   buildQuarterUiState,
   deriveResolvedIssueIdsFromSearchParams,
 } from './quarterReadiness';
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -427,6 +432,13 @@
   .date-group-count { border-radius: 999px; padding: 4px 9px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.72rem; color: var(--text-muted); }
   .date-group-body { padding: 0 12px 12px; }
   .voice-preview { background: rgba(0,0,0,0.82); color: white; padding: 8px 15px; border-radius: 15px; margin-bottom: 8px; font-size: 0.72rem; max-width: 240px; text-align: right; }
+  .voice-context-banner { display: grid; gap: 10px; padding: 14px 16px; margin-bottom: 16px; border-radius: 18px; background: var(--bg-card); border: 1px solid var(--border); box-shadow: var(--shadow); }
+  .voice-context-row, .voice-chip-actions, .voice-chip-labels { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
+  .voice-context-pill, .voice-action-pill { border-radius: 999px; padding: 6px 10px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.74rem; font-weight: 700; }
+  .voice-context-pill.active { background: rgba(13,92,99,0.12); color: var(--primary-strong); border-color: rgba(13,92,99,0.24); }
+  .voice-micro-chip { display: grid; gap: 10px; padding: 14px 16px; border-radius: 18px; background: rgba(13,92,99,0.08); border: 1px solid rgba(13,92,99,0.18); }
+  .voice-micro-chip strong { display: block; }
+  .inbox-action-card.voice-target { border-color: rgba(13,92,99,0.36); box-shadow: 0 0 0 2px rgba(13,92,99,0.12), var(--shadow); }
   .spinner { animation: rotate 2s linear infinite; }
   .capture-grid { display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(320px, 0.95fr); gap: 18px; align-items: start; }
   .command-centre { display: grid; gap: 18px; }
@@ -1382,6 +1394,8 @@
   const [inboxDrafts, setInboxDrafts] = useState({});
   const [inboxBusyId, setInboxBusyId] = useState('');
   const [inboxMessage, setInboxMessage] = useState(null);
+  const [activeVoiceContextId, setActiveVoiceContextId] = useState('');
+  const [voiceMicroDecisionChip, setVoiceMicroDecisionChip] = useState(null);
   const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..6eb469fcc7f023f8545d9e952a5823fb8c47a88d
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});

file update
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
@@ -1622,2 +1622,3 @@
   const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const activeVoiceCard = findVoiceContextCard(inboxCards, activeVoiceContextId);
   const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
@@ -1626,2 +1627,11 @@
 
+  useEffect(() => {
+    if (!activeVoiceContextId) {
+      return;
+    }
+    if (!findVoiceContextCard(inboxCards, activeVoiceContextId)) {
+      setActiveVoiceContextId('');
+    }
+  }, [activeVoiceContextId, inboxCards]);
+
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
apply_patch(auto_approved=true) exited 0 in 1.67s:
Success. Updated the following files:
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..7564fe2a0b6132d2209937816e3f620cb3d91820
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,7 +10,34 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+
+const MICRO_DECISION_CONTEXT_SCOPES = new Set(['inbox', 'finish_now']);
 
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
+
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
  * Matches transcripts to core intents and extracts slots using regex.
@@ -267,71 +294,167 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_scope = null,
+  } = req.body || {};
   const text = String(transcript).toLowerCase().trim();
+  const normalizedContextScope = context_scope === null || context_scope === undefined
+    ? null
+    : String(context_scope).toLowerCase().replace(/-/g, '_');
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
 
+  const requireTransactionContext = () => {
+    if (!bank_txn_id) {
+      return 'bank_txn_id is required for inbox-bound micro-decision commands.';
+    }
+    if (!MICRO_DECISION_CONTEXT_SCOPES.has(normalizedContextScope)) {
+      return 'context_scope must be inbox or finish_now for transaction micro-decisions.';
+    }
+    return null;
+  };
+
   try {
     if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const categoryName = text.replace('category:', '').trim();
       const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: categoryCode,
+            category_name: categoryName,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${categoryName}`,
+        appliedActions: [{ type: 'category', label: categoryName }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${bp}`,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     const splitMatch = text.match(/split\s+(\d{1,3})%?/);
     if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const pct = Number(splitMatch[1]);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Split ${pct}%`,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: 'Attach receipt requested',
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        extra: {
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
     if (['match first', 'match second', 'match third'].includes(text)) {
@@ -364,7 +487,16 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Match ${index}`,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     if (text === 'no match') {
@@ -376,7 +508,16 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: 'Applied: No match',
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index 9b145fadac9123e3fa01b12af6ef2b9916dc11de..90f2ceac5f92d0e801c9e8f390d3e05e31883deb
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -50,6 +50,11 @@
   buildQuarterUiState,
   deriveResolvedIssueIdsFromSearchParams,
 } from './quarterReadiness';
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -427,6 +432,13 @@
   .date-group-count { border-radius: 999px; padding: 4px 9px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.72rem; color: var(--text-muted); }
   .date-group-body { padding: 0 12px 12px; }
   .voice-preview { background: rgba(0,0,0,0.82); color: white; padding: 8px 15px; border-radius: 15px; margin-bottom: 8px; font-size: 0.72rem; max-width: 240px; text-align: right; }
+  .voice-context-banner { display: grid; gap: 10px; padding: 14px 16px; margin-bottom: 16px; border-radius: 18px; background: var(--bg-card); border: 1px solid var(--border); box-shadow: var(--shadow); }
+  .voice-context-row, .voice-chip-actions, .voice-chip-labels { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
+  .voice-context-pill, .voice-action-pill { border-radius: 999px; padding: 6px 10px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.74rem; font-weight: 700; }
+  .voice-context-pill.active { background: rgba(13,92,99,0.12); color: var(--primary-strong); border-color: rgba(13,92,99,0.24); }
+  .voice-micro-chip { display: grid; gap: 10px; padding: 14px 16px; border-radius: 18px; background: rgba(13,92,99,0.08); border: 1px solid rgba(13,92,99,0.18); }
+  .voice-micro-chip strong { display: block; }
+  .inbox-action-card.voice-target { border-color: rgba(13,92,99,0.36); box-shadow: 0 0 0 2px rgba(13,92,99,0.12), var(--shadow); }
   .spinner { animation: rotate 2s linear infinite; }
   .capture-grid { display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(320px, 0.95fr); gap: 18px; align-items: start; }
   .command-centre { display: grid; gap: 18px; }
@@ -1382,6 +1394,8 @@
   const [inboxDrafts, setInboxDrafts] = useState({});
   const [inboxBusyId, setInboxBusyId] = useState('');
   const [inboxMessage, setInboxMessage] = useState(null);
+  const [activeVoiceContextId, setActiveVoiceContextId] = useState('');
+  const [voiceMicroDecisionChip, setVoiceMicroDecisionChip] = useState(null);
   const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
@@ -1606,10 +1620,20 @@
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
   const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const activeVoiceCard = findVoiceContextCard(inboxCards, activeVoiceContextId);
   const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
   const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
   const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
+  useEffect(() => {
+    if (!activeVoiceContextId) {
+      return;
+    }
+    if (!findVoiceContextCard(inboxCards, activeVoiceContextId)) {
+      setActiveVoiceContextId('');
+    }
+  }, [activeVoiceContextId, inboxCards]);
+
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
   const canManageGovernance = hasPermission(currentUser, PERMISSIONS.GOVERNANCE_MANAGE);
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..6eb469fcc7f023f8545d9e952a5823fb8c47a88d
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});

file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..7564fe2a0b6132d2209937816e3f620cb3d91820
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,7 +10,34 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+
+const MICRO_DECISION_CONTEXT_SCOPES = new Set(['inbox', 'finish_now']);
 
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
+
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
  * Matches transcripts to core intents and extracts slots using regex.
@@ -267,71 +294,167 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_scope = null,
+  } = req.body || {};
   const text = String(transcript).toLowerCase().trim();
+  const normalizedContextScope = context_scope === null || context_scope === undefined
+    ? null
+    : String(context_scope).toLowerCase().replace(/-/g, '_');
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
 
+  const requireTransactionContext = () => {
+    if (!bank_txn_id) {
+      return 'bank_txn_id is required for inbox-bound micro-decision commands.';
+    }
+    if (!MICRO_DECISION_CONTEXT_SCOPES.has(normalizedContextScope)) {
+      return 'context_scope must be inbox or finish_now for transaction micro-decisions.';
+    }
+    return null;
+  };
+
   try {
     if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const categoryName = text.replace('category:', '').trim();
       const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: categoryCode,
+            category_name: categoryName,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${categoryName}`,
+        appliedActions: [{ type: 'category', label: categoryName }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${bp}`,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     const splitMatch = text.match(/split\s+(\d{1,3})%?/);
     if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const pct = Number(splitMatch[1]);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Split ${pct}%`,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: 'Attach receipt requested',
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        extra: {
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
     if (['match first', 'match second', 'match third'].includes(text)) {
@@ -364,7 +487,16 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Match ${index}`,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     if (text === 'no match') {
@@ -376,7 +508,16 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: 'Applied: No match',
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index 9b145fadac9123e3fa01b12af6ef2b9916dc11de..90f2ceac5f92d0e801c9e8f390d3e05e31883deb
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -50,6 +50,11 @@
   buildQuarterUiState,
   deriveResolvedIssueIdsFromSearchParams,
 } from './quarterReadiness';
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -427,6 +432,13 @@
   .date-group-count { border-radius: 999px; padding: 4px 9px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.72rem; color: var(--text-muted); }
   .date-group-body { padding: 0 12px 12px; }
   .voice-preview { background: rgba(0,0,0,0.82); color: white; padding: 8px 15px; border-radius: 15px; margin-bottom: 8px; font-size: 0.72rem; max-width: 240px; text-align: right; }
+  .voice-context-banner { display: grid; gap: 10px; padding: 14px 16px; margin-bottom: 16px; border-radius: 18px; background: var(--bg-card); border: 1px solid var(--border); box-shadow: var(--shadow); }
+  .voice-context-row, .voice-chip-actions, .voice-chip-labels { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
+  .voice-context-pill, .voice-action-pill { border-radius: 999px; padding: 6px 10px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.74rem; font-weight: 700; }
+  .voice-context-pill.active { background: rgba(13,92,99,0.12); color: var(--primary-strong); border-color: rgba(13,92,99,0.24); }
+  .voice-micro-chip { display: grid; gap: 10px; padding: 14px 16px; border-radius: 18px; background: rgba(13,92,99,0.08); border: 1px solid rgba(13,92,99,0.18); }
+  .voice-micro-chip strong { display: block; }
+  .inbox-action-card.voice-target { border-color: rgba(13,92,99,0.36); box-shadow: 0 0 0 2px rgba(13,92,99,0.12), var(--shadow); }
   .spinner { animation: rotate 2s linear infinite; }
   .capture-grid { display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(320px, 0.95fr); gap: 18px; align-items: start; }
   .command-centre { display: grid; gap: 18px; }
@@ -1382,6 +1394,8 @@
   const [inboxDrafts, setInboxDrafts] = useState({});
   const [inboxBusyId, setInboxBusyId] = useState('');
   const [inboxMessage, setInboxMessage] = useState(null);
+  const [activeVoiceContextId, setActiveVoiceContextId] = useState('');
+  const [voiceMicroDecisionChip, setVoiceMicroDecisionChip] = useState(null);
   const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
@@ -1606,10 +1620,20 @@
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
   const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const activeVoiceCard = findVoiceContextCard(inboxCards, activeVoiceContextId);
   const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
   const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
   const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
+  useEffect(() => {
+    if (!activeVoiceContextId) {
+      return;
+    }
+    if (!findVoiceContextCard(inboxCards, activeVoiceContextId)) {
+      setActiveVoiceContextId('');
+    }
+  }, [activeVoiceContextId, inboxCards]);
+
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
   const canManageGovernance = hasPermission(currentUser, PERMISSIONS.GOVERNANCE_MANAGE);
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..6eb469fcc7f023f8545d9e952a5823fb8c47a88d
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});

file update
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
@@ -1832,2 +1832,3 @@
         setInboxDrafts({});
+        setVoiceMicroDecisionChip(null);
         setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
@@ -1837,2 +1838,3 @@
       await fetchAllData();
+      setVoiceMicroDecisionChip(null);
       setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
@@ -3383,2 +3385,5 @@
   const openReadinessWorkflow = (issue) => {
+    if (issue?.affected_entity_id) {
+      setActiveVoiceContextId(String(issue.affected_entity_id));
+    }
     if (issue?.resolution_target?.workflow === '/inbox/finish-now') {
@@ -3390,2 +3395,72 @@
 
+  const renderVoiceContextBanner = () => {
+    if (!(currentTab === 'inbox' || currentTab === 'quarter')) {
+      return null;
+    }
+
+    return (
+      <section className="voice-context-banner">
+        <div>
+          <strong>Voice triage</strong>
+          <div className="small-muted">
+            Use the mic against one selected finish-now item only. Supported commands here are category, business, personal, split n%, and attach receipt.
+          </div>
+        </div>
+        <div className="voice-context-row">
+          <span className={`voice-context-pill ${activeVoiceCard ? 'active' : ''}`}>
+            {activeVoiceCard
+              ? `Target: ${activeVoiceCard.merchant} (${activeVoiceCard.id})`
+              : 'Select a blocker card to arm voice triage'}
+          </span>
+          {activeVoiceCard && (
+            <button className="secondary-action" type="button" onClick={() => setCurrentTab('inbox')}>
+              Edit target
+            </button>
+          )}
+        </div>
+        {voiceMicroDecisionChip && (
+          <div className="voice-micro-chip">
+            <div>
+              <strong>{voiceMicroDecisionChip.confirmationChip}</strong>
+              <div className="small-muted">
+                {voiceMicroDecisionChip.targetLabel} ({voiceMicroDecisionChip.targetId})
+              </div>
+            </div>
+            {voiceMicroDecisionChip.labels.length > 0 && (
+              <div className="voice-chip-labels">
+                {voiceMicroDecisionChip.labels.map((label) => (
+                  <span key={`${voiceMicroDecisionChip.targetId}-${label}`} className="voice-action-pill">
+                    {label}
+                  </span>
+                ))}
+              </div>
+            )}
+            <div className="voice-chip-actions">
+              <button
+                className="secondary-action"
+                type="button"
+                onClick={() => {
+                  setCurrentTab('inbox');
+                  setActiveVoiceContextId(voiceMicroDecisionChip.targetId);
+                }}
+              >
+                Edit
+              </button>
+              {voiceMicroDecisionChip.undoSupported && (
+                <button
+                  className="secondary-action"
+                  type="button"
+                  onClick={undoLastInboxAction}
+                  disabled={inboxBusyId === 'undo-last'}
+                >
+                  Undo
+                </button>
+              )}
+            </div>
+          </div>
+        )}
+      </section>
+    );
+  };
+
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
apply_patch(auto_approved=true) exited 0 in 1.57s:
Success. Updated the following files:
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..7564fe2a0b6132d2209937816e3f620cb3d91820
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,7 +10,34 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+
+const MICRO_DECISION_CONTEXT_SCOPES = new Set(['inbox', 'finish_now']);
 
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
+
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
  * Matches transcripts to core intents and extracts slots using regex.
@@ -267,71 +294,167 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_scope = null,
+  } = req.body || {};
   const text = String(transcript).toLowerCase().trim();
+  const normalizedContextScope = context_scope === null || context_scope === undefined
+    ? null
+    : String(context_scope).toLowerCase().replace(/-/g, '_');
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
 
+  const requireTransactionContext = () => {
+    if (!bank_txn_id) {
+      return 'bank_txn_id is required for inbox-bound micro-decision commands.';
+    }
+    if (!MICRO_DECISION_CONTEXT_SCOPES.has(normalizedContextScope)) {
+      return 'context_scope must be inbox or finish_now for transaction micro-decisions.';
+    }
+    return null;
+  };
+
   try {
     if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const categoryName = text.replace('category:', '').trim();
       const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: categoryCode,
+            category_name: categoryName,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${categoryName}`,
+        appliedActions: [{ type: 'category', label: categoryName }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${bp}`,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     const splitMatch = text.match(/split\s+(\d{1,3})%?/);
     if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const pct = Number(splitMatch[1]);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Split ${pct}%`,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: 'Attach receipt requested',
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        extra: {
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
     if (['match first', 'match second', 'match third'].includes(text)) {
@@ -364,7 +487,16 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Match ${index}`,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     if (text === 'no match') {
@@ -376,7 +508,16 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: 'Applied: No match',
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index 9b145fadac9123e3fa01b12af6ef2b9916dc11de..ece1bfd575b2fec154fff0fb3b2a2857599661be
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -50,6 +50,11 @@
   buildQuarterUiState,
   deriveResolvedIssueIdsFromSearchParams,
 } from './quarterReadiness';
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -427,6 +432,13 @@
   .date-group-count { border-radius: 999px; padding: 4px 9px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.72rem; color: var(--text-muted); }
   .date-group-body { padding: 0 12px 12px; }
   .voice-preview { background: rgba(0,0,0,0.82); color: white; padding: 8px 15px; border-radius: 15px; margin-bottom: 8px; font-size: 0.72rem; max-width: 240px; text-align: right; }
+  .voice-context-banner { display: grid; gap: 10px; padding: 14px 16px; margin-bottom: 16px; border-radius: 18px; background: var(--bg-card); border: 1px solid var(--border); box-shadow: var(--shadow); }
+  .voice-context-row, .voice-chip-actions, .voice-chip-labels { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
+  .voice-context-pill, .voice-action-pill { border-radius: 999px; padding: 6px 10px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.74rem; font-weight: 700; }
+  .voice-context-pill.active { background: rgba(13,92,99,0.12); color: var(--primary-strong); border-color: rgba(13,92,99,0.24); }
+  .voice-micro-chip { display: grid; gap: 10px; padding: 14px 16px; border-radius: 18px; background: rgba(13,92,99,0.08); border: 1px solid rgba(13,92,99,0.18); }
+  .voice-micro-chip strong { display: block; }
+  .inbox-action-card.voice-target { border-color: rgba(13,92,99,0.36); box-shadow: 0 0 0 2px rgba(13,92,99,0.12), var(--shadow); }
   .spinner { animation: rotate 2s linear infinite; }
   .capture-grid { display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(320px, 0.95fr); gap: 18px; align-items: start; }
   .command-centre { display: grid; gap: 18px; }
@@ -1382,6 +1394,8 @@
   const [inboxDrafts, setInboxDrafts] = useState({});
   const [inboxBusyId, setInboxBusyId] = useState('');
   const [inboxMessage, setInboxMessage] = useState(null);
+  const [activeVoiceContextId, setActiveVoiceContextId] = useState('');
+  const [voiceMicroDecisionChip, setVoiceMicroDecisionChip] = useState(null);
   const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
@@ -1606,10 +1620,20 @@
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
   const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const activeVoiceCard = findVoiceContextCard(inboxCards, activeVoiceContextId);
   const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
   const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
   const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
+  useEffect(() => {
+    if (!activeVoiceContextId) {
+      return;
+    }
+    if (!findVoiceContextCard(inboxCards, activeVoiceContextId)) {
+      setActiveVoiceContextId('');
+    }
+  }, [activeVoiceContextId, inboxCards]);
+
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
   const canManageGovernance = hasPermission(currentUser, PERMISSIONS.GOVERNANCE_MANAGE);
@@ -1806,11 +1830,13 @@
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxDrafts({});
+        setVoiceMicroDecisionChip(null);
         setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
         return;
       }
       await axios.post(`${API_BASE_URL}/inbox/undo-last`);
       await fetchAllData();
+      setVoiceMicroDecisionChip(null);
       setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
     } catch (error) {
       setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
@@ -3357,6 +3383,9 @@
   };
 
   const openReadinessWorkflow = (issue) => {
+    if (issue?.affected_entity_id) {
+      setActiveVoiceContextId(String(issue.affected_entity_id));
+    }
     if (issue?.resolution_target?.workflow === '/inbox/finish-now') {
       setCurrentTab('inbox');
       return;
@@ -3364,6 +3393,76 @@
     setCurrentTab('inbox');
   };
 
+  const renderVoiceContextBanner = () => {
+    if (!(currentTab === 'inbox' || currentTab === 'quarter')) {
+      return null;
+    }
+
+    return (
+      <section className="voice-context-banner">
+        <div>
+          <strong>Voice triage</strong>
+          <div className="small-muted">
+            Use the mic against one selected finish-now item only. Supported commands here are category, business, personal, split n%, and attach receipt.
+          </div>
+        </div>
+        <div className="voice-context-row">
+          <span className={`voice-context-pill ${activeVoiceCard ? 'active' : ''}`}>
+            {activeVoiceCard
+              ? `Target: ${activeVoiceCard.merchant} (${activeVoiceCard.id})`
+              : 'Select a blocker card to arm voice triage'}
+          </span>
+          {activeVoiceCard && (
+            <button className="secondary-action" type="button" onClick={() => setCurrentTab('inbox')}>
+              Edit target
+            </button>
+          )}
+        </div>
+        {voiceMicroDecisionChip && (
+          <div className="voice-micro-chip">
+            <div>
+              <strong>{voiceMicroDecisionChip.confirmationChip}</strong>
+              <div className="small-muted">
+                {voiceMicroDecisionChip.targetLabel} ({voiceMicroDecisionChip.targetId})
+              </div>
+            </div>
+            {voiceMicroDecisionChip.labels.length > 0 && (
+              <div className="voice-chip-labels">
+                {voiceMicroDecisionChip.labels.map((label) => (
+                  <span key={`${voiceMicroDecisionChip.targetId}-${label}`} className="voice-action-pill">
+                    {label}
+                  </span>
+                ))}
+              </div>
+            )}
+            <div className="voice-chip-actions">
+              <button
+                className="secondary-action"
+                type="button"
+                onClick={() => {
+                  setCurrentTab('inbox');
+                  setActiveVoiceContextId(voiceMicroDecisionChip.targetId);
+                }}
+              >
+                Edit
+              </button>
+              {voiceMicroDecisionChip.undoSupported && (
+                <button
+                  className="secondary-action"
+                  type="button"
+                  onClick={undoLastInboxAction}
+                  disabled={inboxBusyId === 'undo-last'}
+                >
+                  Undo
+                </button>
+              )}
+            </div>
+          </div>
+        )}
+      </section>
+    );
+  };
+
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..6eb469fcc7f023f8545d9e952a5823fb8c47a88d
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});

file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..7564fe2a0b6132d2209937816e3f620cb3d91820
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,7 +10,34 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+
+const MICRO_DECISION_CONTEXT_SCOPES = new Set(['inbox', 'finish_now']);
 
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
+
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
  * Matches transcripts to core intents and extracts slots using regex.
@@ -267,71 +294,167 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_scope = null,
+  } = req.body || {};
   const text = String(transcript).toLowerCase().trim();
+  const normalizedContextScope = context_scope === null || context_scope === undefined
+    ? null
+    : String(context_scope).toLowerCase().replace(/-/g, '_');
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
 
+  const requireTransactionContext = () => {
+    if (!bank_txn_id) {
+      return 'bank_txn_id is required for inbox-bound micro-decision commands.';
+    }
+    if (!MICRO_DECISION_CONTEXT_SCOPES.has(normalizedContextScope)) {
+      return 'context_scope must be inbox or finish_now for transaction micro-decisions.';
+    }
+    return null;
+  };
+
   try {
     if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const categoryName = text.replace('category:', '').trim();
       const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: categoryCode,
+            category_name: categoryName,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${categoryName}`,
+        appliedActions: [{ type: 'category', label: categoryName }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${bp}`,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     const splitMatch = text.match(/split\s+(\d{1,3})%?/);
     if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const pct = Number(splitMatch[1]);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Split ${pct}%`,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: 'Attach receipt requested',
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        extra: {
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
     if (['match first', 'match second', 'match third'].includes(text)) {
@@ -364,7 +487,16 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Match ${index}`,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     if (text === 'no match') {
@@ -376,7 +508,16 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: 'Applied: No match',
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index 9b145fadac9123e3fa01b12af6ef2b9916dc11de..ece1bfd575b2fec154fff0fb3b2a2857599661be
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -50,6 +50,11 @@
   buildQuarterUiState,
   deriveResolvedIssueIdsFromSearchParams,
 } from './quarterReadiness';
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -427,6 +432,13 @@
   .date-group-count { border-radius: 999px; padding: 4px 9px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.72rem; color: var(--text-muted); }
   .date-group-body { padding: 0 12px 12px; }
   .voice-preview { background: rgba(0,0,0,0.82); color: white; padding: 8px 15px; border-radius: 15px; margin-bottom: 8px; font-size: 0.72rem; max-width: 240px; text-align: right; }
+  .voice-context-banner { display: grid; gap: 10px; padding: 14px 16px; margin-bottom: 16px; border-radius: 18px; background: var(--bg-card); border: 1px solid var(--border); box-shadow: var(--shadow); }
+  .voice-context-row, .voice-chip-actions, .voice-chip-labels { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
+  .voice-context-pill, .voice-action-pill { border-radius: 999px; padding: 6px 10px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.74rem; font-weight: 700; }
+  .voice-context-pill.active { background: rgba(13,92,99,0.12); color: var(--primary-strong); border-color: rgba(13,92,99,0.24); }
+  .voice-micro-chip { display: grid; gap: 10px; padding: 14px 16px; border-radius: 18px; background: rgba(13,92,99,0.08); border: 1px solid rgba(13,92,99,0.18); }
+  .voice-micro-chip strong { display: block; }
+  .inbox-action-card.voice-target { border-color: rgba(13,92,99,0.36); box-shadow: 0 0 0 2px rgba(13,92,99,0.12), var(--shadow); }
   .spinner { animation: rotate 2s linear infinite; }
   .capture-grid { display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(320px, 0.95fr); gap: 18px; align-items: start; }
   .command-centre { display: grid; gap: 18px; }
@@ -1382,6 +1394,8 @@
   const [inboxDrafts, setInboxDrafts] = useState({});
   const [inboxBusyId, setInboxBusyId] = useState('');
   const [inboxMessage, setInboxMessage] = useState(null);
+  const [activeVoiceContextId, setActiveVoiceContextId] = useState('');
+  const [voiceMicroDecisionChip, setVoiceMicroDecisionChip] = useState(null);
   const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
@@ -1606,10 +1620,20 @@
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
   const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const activeVoiceCard = findVoiceContextCard(inboxCards, activeVoiceContextId);
   const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
   const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
   const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
+  useEffect(() => {
+    if (!activeVoiceContextId) {
+      return;
+    }
+    if (!findVoiceContextCard(inboxCards, activeVoiceContextId)) {
+      setActiveVoiceContextId('');
+    }
+  }, [activeVoiceContextId, inboxCards]);
+
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
   const canManageGovernance = hasPermission(currentUser, PERMISSIONS.GOVERNANCE_MANAGE);
@@ -1806,11 +1830,13 @@
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxDrafts({});
+        setVoiceMicroDecisionChip(null);
         setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
         return;
       }
       await axios.post(`${API_BASE_URL}/inbox/undo-last`);
       await fetchAllData();
+      setVoiceMicroDecisionChip(null);
       setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
     } catch (error) {
       setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
@@ -3357,6 +3383,9 @@
   };
 
   const openReadinessWorkflow = (issue) => {
+    if (issue?.affected_entity_id) {
+      setActiveVoiceContextId(String(issue.affected_entity_id));
+    }
     if (issue?.resolution_target?.workflow === '/inbox/finish-now') {
       setCurrentTab('inbox');
       return;
@@ -3364,6 +3393,76 @@
     setCurrentTab('inbox');
   };
 
+  const renderVoiceContextBanner = () => {
+    if (!(currentTab === 'inbox' || currentTab === 'quarter')) {
+      return null;
+    }
+
+    return (
+      <section className="voice-context-banner">
+        <div>
+          <strong>Voice triage</strong>
+          <div className="small-muted">
+            Use the mic against one selected finish-now item only. Supported commands here are category, business, personal, split n%, and attach receipt.
+          </div>
+        </div>
+        <div className="voice-context-row">
+          <span className={`voice-context-pill ${activeVoiceCard ? 'active' : ''}`}>
+            {activeVoiceCard
+              ? `Target: ${activeVoiceCard.merchant} (${activeVoiceCard.id})`
+              : 'Select a blocker card to arm voice triage'}
+          </span>
+          {activeVoiceCard && (
+            <button className="secondary-action" type="button" onClick={() => setCurrentTab('inbox')}>
+              Edit target
+            </button>
+          )}
+        </div>
+        {voiceMicroDecisionChip && (
+          <div className="voice-micro-chip">
+            <div>
+              <strong>{voiceMicroDecisionChip.confirmationChip}</strong>
+              <div className="small-muted">
+                {voiceMicroDecisionChip.targetLabel} ({voiceMicroDecisionChip.targetId})
+              </div>
+            </div>
+            {voiceMicroDecisionChip.labels.length > 0 && (
+              <div className="voice-chip-labels">
+                {voiceMicroDecisionChip.labels.map((label) => (
+                  <span key={`${voiceMicroDecisionChip.targetId}-${label}`} className="voice-action-pill">
+                    {label}
+                  </span>
+                ))}
+              </div>
+            )}
+            <div className="voice-chip-actions">
+              <button
+                className="secondary-action"
+                type="button"
+                onClick={() => {
+                  setCurrentTab('inbox');
+                  setActiveVoiceContextId(voiceMicroDecisionChip.targetId);
+                }}
+              >
+                Edit
+              </button>
+              {voiceMicroDecisionChip.undoSupported && (
+                <button
+                  className="secondary-action"
+                  type="button"
+                  onClick={undoLastInboxAction}
+                  disabled={inboxBusyId === 'undo-last'}
+                >
+                  Undo
+                </button>
+              )}
+            </div>
+          </div>
+        )}
+      </section>
+    );
+  };
+
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..6eb469fcc7f023f8545d9e952a5823fb8c47a88d
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});

file update
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
@@ -4492,2 +4492,3 @@
       {connectionError && <div className="alert alert-danger">{connectionError}</div>}
+      {renderVoiceContextBanner()}
 
apply_patch(auto_approved=true) exited 0 in 5.56s:
Success. Updated the following files:
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..7564fe2a0b6132d2209937816e3f620cb3d91820
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,7 +10,34 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+
+const MICRO_DECISION_CONTEXT_SCOPES = new Set(['inbox', 'finish_now']);
 
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
+
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
  * Matches transcripts to core intents and extracts slots using regex.
@@ -267,71 +294,167 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_scope = null,
+  } = req.body || {};
   const text = String(transcript).toLowerCase().trim();
+  const normalizedContextScope = context_scope === null || context_scope === undefined
+    ? null
+    : String(context_scope).toLowerCase().replace(/-/g, '_');
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
 
+  const requireTransactionContext = () => {
+    if (!bank_txn_id) {
+      return 'bank_txn_id is required for inbox-bound micro-decision commands.';
+    }
+    if (!MICRO_DECISION_CONTEXT_SCOPES.has(normalizedContextScope)) {
+      return 'context_scope must be inbox or finish_now for transaction micro-decisions.';
+    }
+    return null;
+  };
+
   try {
     if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const categoryName = text.replace('category:', '').trim();
       const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: categoryCode,
+            category_name: categoryName,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${categoryName}`,
+        appliedActions: [{ type: 'category', label: categoryName }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${bp}`,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     const splitMatch = text.match(/split\s+(\d{1,3})%?/);
     if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const pct = Number(splitMatch[1]);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Split ${pct}%`,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: 'Attach receipt requested',
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        extra: {
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
     if (['match first', 'match second', 'match third'].includes(text)) {
@@ -364,7 +487,16 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Match ${index}`,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     if (text === 'no match') {
@@ -376,7 +508,16 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: 'Applied: No match',
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index 9b145fadac9123e3fa01b12af6ef2b9916dc11de..f8fb386e5c681bb24bf29d8865ecfe70060a1334
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -50,6 +50,11 @@
   buildQuarterUiState,
   deriveResolvedIssueIdsFromSearchParams,
 } from './quarterReadiness';
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -427,6 +432,13 @@
   .date-group-count { border-radius: 999px; padding: 4px 9px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.72rem; color: var(--text-muted); }
   .date-group-body { padding: 0 12px 12px; }
   .voice-preview { background: rgba(0,0,0,0.82); color: white; padding: 8px 15px; border-radius: 15px; margin-bottom: 8px; font-size: 0.72rem; max-width: 240px; text-align: right; }
+  .voice-context-banner { display: grid; gap: 10px; padding: 14px 16px; margin-bottom: 16px; border-radius: 18px; background: var(--bg-card); border: 1px solid var(--border); box-shadow: var(--shadow); }
+  .voice-context-row, .voice-chip-actions, .voice-chip-labels { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
+  .voice-context-pill, .voice-action-pill { border-radius: 999px; padding: 6px 10px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.74rem; font-weight: 700; }
+  .voice-context-pill.active { background: rgba(13,92,99,0.12); color: var(--primary-strong); border-color: rgba(13,92,99,0.24); }
+  .voice-micro-chip { display: grid; gap: 10px; padding: 14px 16px; border-radius: 18px; background: rgba(13,92,99,0.08); border: 1px solid rgba(13,92,99,0.18); }
+  .voice-micro-chip strong { display: block; }
+  .inbox-action-card.voice-target { border-color: rgba(13,92,99,0.36); box-shadow: 0 0 0 2px rgba(13,92,99,0.12), var(--shadow); }
   .spinner { animation: rotate 2s linear infinite; }
   .capture-grid { display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(320px, 0.95fr); gap: 18px; align-items: start; }
   .command-centre { display: grid; gap: 18px; }
@@ -1382,6 +1394,8 @@
   const [inboxDrafts, setInboxDrafts] = useState({});
   const [inboxBusyId, setInboxBusyId] = useState('');
   const [inboxMessage, setInboxMessage] = useState(null);
+  const [activeVoiceContextId, setActiveVoiceContextId] = useState('');
+  const [voiceMicroDecisionChip, setVoiceMicroDecisionChip] = useState(null);
   const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
@@ -1606,10 +1620,20 @@
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
   const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const activeVoiceCard = findVoiceContextCard(inboxCards, activeVoiceContextId);
   const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
   const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
   const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
+  useEffect(() => {
+    if (!activeVoiceContextId) {
+      return;
+    }
+    if (!findVoiceContextCard(inboxCards, activeVoiceContextId)) {
+      setActiveVoiceContextId('');
+    }
+  }, [activeVoiceContextId, inboxCards]);
+
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
   const canManageGovernance = hasPermission(currentUser, PERMISSIONS.GOVERNANCE_MANAGE);
@@ -1806,11 +1830,13 @@
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxDrafts({});
+        setVoiceMicroDecisionChip(null);
         setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
         return;
       }
       await axios.post(`${API_BASE_URL}/inbox/undo-last`);
       await fetchAllData();
+      setVoiceMicroDecisionChip(null);
       setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
     } catch (error) {
       setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
@@ -3357,6 +3383,9 @@
   };
 
   const openReadinessWorkflow = (issue) => {
+    if (issue?.affected_entity_id) {
+      setActiveVoiceContextId(String(issue.affected_entity_id));
+    }
     if (issue?.resolution_target?.workflow === '/inbox/finish-now') {
       setCurrentTab('inbox');
       return;
@@ -3364,6 +3393,76 @@
     setCurrentTab('inbox');
   };
 
+  const renderVoiceContextBanner = () => {
+    if (!(currentTab === 'inbox' || currentTab === 'quarter')) {
+      return null;
+    }
+
+    return (
+      <section className="voice-context-banner">
+        <div>
+          <strong>Voice triage</strong>
+          <div className="small-muted">
+            Use the mic against one selected finish-now item only. Supported commands here are category, business, personal, split n%, and attach receipt.
+          </div>
+        </div>
+        <div className="voice-context-row">
+          <span className={`voice-context-pill ${activeVoiceCard ? 'active' : ''}`}>
+            {activeVoiceCard
+              ? `Target: ${activeVoiceCard.merchant} (${activeVoiceCard.id})`
+              : 'Select a blocker card to arm voice triage'}
+          </span>
+          {activeVoiceCard && (
+            <button className="secondary-action" type="button" onClick={() => setCurrentTab('inbox')}>
+              Edit target
+            </button>
+          )}
+        </div>
+        {voiceMicroDecisionChip && (
+          <div className="voice-micro-chip">
+            <div>
+              <strong>{voiceMicroDecisionChip.confirmationChip}</strong>
+              <div className="small-muted">
+                {voiceMicroDecisionChip.targetLabel} ({voiceMicroDecisionChip.targetId})
+              </div>
+            </div>
+            {voiceMicroDecisionChip.labels.length > 0 && (
+              <div className="voice-chip-labels">
+                {voiceMicroDecisionChip.labels.map((label) => (
+                  <span key={`${voiceMicroDecisionChip.targetId}-${label}`} className="voice-action-pill">
+                    {label}
+                  </span>
+                ))}
+              </div>
+            )}
+            <div className="voice-chip-actions">
+              <button
+                className="secondary-action"
+                type="button"
+                onClick={() => {
+                  setCurrentTab('inbox');
+                  setActiveVoiceContextId(voiceMicroDecisionChip.targetId);
+                }}
+              >
+                Edit
+              </button>
+              {voiceMicroDecisionChip.undoSupported && (
+                <button
+                  className="secondary-action"
+                  type="button"
+                  onClick={undoLastInboxAction}
+                  disabled={inboxBusyId === 'undo-last'}
+                >
+                  Undo
+                </button>
+              )}
+            </div>
+          </div>
+        )}
+      </section>
+    );
+  };
+
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
@@ -4391,6 +4490,7 @@
         </button>
       </section>
       {connectionError && <div className="alert alert-danger">{connectionError}</div>}
+      {renderVoiceContextBanner()}
 
       {loading ? (
         <div className="text-center py-5">Loading...</div>
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..6eb469fcc7f023f8545d9e952a5823fb8c47a88d
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});

file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..7564fe2a0b6132d2209937816e3f620cb3d91820
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,7 +10,34 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+
+const MICRO_DECISION_CONTEXT_SCOPES = new Set(['inbox', 'finish_now']);
 
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
+
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
  * Matches transcripts to core intents and extracts slots using regex.
@@ -267,71 +294,167 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_scope = null,
+  } = req.body || {};
   const text = String(transcript).toLowerCase().trim();
+  const normalizedContextScope = context_scope === null || context_scope === undefined
+    ? null
+    : String(context_scope).toLowerCase().replace(/-/g, '_');
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
 
+  const requireTransactionContext = () => {
+    if (!bank_txn_id) {
+      return 'bank_txn_id is required for inbox-bound micro-decision commands.';
+    }
+    if (!MICRO_DECISION_CONTEXT_SCOPES.has(normalizedContextScope)) {
+      return 'context_scope must be inbox or finish_now for transaction micro-decisions.';
+    }
+    return null;
+  };
+
   try {
     if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const categoryName = text.replace('category:', '').trim();
       const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: categoryCode,
+            category_name: categoryName,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${categoryName}`,
+        appliedActions: [{ type: 'category', label: categoryName }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${bp}`,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     const splitMatch = text.match(/split\s+(\d{1,3})%?/);
     if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const pct = Number(splitMatch[1]);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Split ${pct}%`,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: 'Attach receipt requested',
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        extra: {
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
     if (['match first', 'match second', 'match third'].includes(text)) {
@@ -364,7 +487,16 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Match ${index}`,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     if (text === 'no match') {
@@ -376,7 +508,16 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: 'Applied: No match',
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index 9b145fadac9123e3fa01b12af6ef2b9916dc11de..f8fb386e5c681bb24bf29d8865ecfe70060a1334
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -50,6 +50,11 @@
   buildQuarterUiState,
   deriveResolvedIssueIdsFromSearchParams,
 } from './quarterReadiness';
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -427,6 +432,13 @@
   .date-group-count { border-radius: 999px; padding: 4px 9px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.72rem; color: var(--text-muted); }
   .date-group-body { padding: 0 12px 12px; }
   .voice-preview { background: rgba(0,0,0,0.82); color: white; padding: 8px 15px; border-radius: 15px; margin-bottom: 8px; font-size: 0.72rem; max-width: 240px; text-align: right; }
+  .voice-context-banner { display: grid; gap: 10px; padding: 14px 16px; margin-bottom: 16px; border-radius: 18px; background: var(--bg-card); border: 1px solid var(--border); box-shadow: var(--shadow); }
+  .voice-context-row, .voice-chip-actions, .voice-chip-labels { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
+  .voice-context-pill, .voice-action-pill { border-radius: 999px; padding: 6px 10px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.74rem; font-weight: 700; }
+  .voice-context-pill.active { background: rgba(13,92,99,0.12); color: var(--primary-strong); border-color: rgba(13,92,99,0.24); }
+  .voice-micro-chip { display: grid; gap: 10px; padding: 14px 16px; border-radius: 18px; background: rgba(13,92,99,0.08); border: 1px solid rgba(13,92,99,0.18); }
+  .voice-micro-chip strong { display: block; }
+  .inbox-action-card.voice-target { border-color: rgba(13,92,99,0.36); box-shadow: 0 0 0 2px rgba(13,92,99,0.12), var(--shadow); }
   .spinner { animation: rotate 2s linear infinite; }
   .capture-grid { display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(320px, 0.95fr); gap: 18px; align-items: start; }
   .command-centre { display: grid; gap: 18px; }
@@ -1382,6 +1394,8 @@
   const [inboxDrafts, setInboxDrafts] = useState({});
   const [inboxBusyId, setInboxBusyId] = useState('');
   const [inboxMessage, setInboxMessage] = useState(null);
+  const [activeVoiceContextId, setActiveVoiceContextId] = useState('');
+  const [voiceMicroDecisionChip, setVoiceMicroDecisionChip] = useState(null);
   const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
@@ -1606,10 +1620,20 @@
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
   const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const activeVoiceCard = findVoiceContextCard(inboxCards, activeVoiceContextId);
   const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
   const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
   const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
+  useEffect(() => {
+    if (!activeVoiceContextId) {
+      return;
+    }
+    if (!findVoiceContextCard(inboxCards, activeVoiceContextId)) {
+      setActiveVoiceContextId('');
+    }
+  }, [activeVoiceContextId, inboxCards]);
+
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
   const canManageGovernance = hasPermission(currentUser, PERMISSIONS.GOVERNANCE_MANAGE);
@@ -1806,11 +1830,13 @@
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxDrafts({});
+        setVoiceMicroDecisionChip(null);
         setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
         return;
       }
       await axios.post(`${API_BASE_URL}/inbox/undo-last`);
       await fetchAllData();
+      setVoiceMicroDecisionChip(null);
       setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
     } catch (error) {
       setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
@@ -3357,6 +3383,9 @@
   };
 
   const openReadinessWorkflow = (issue) => {
+    if (issue?.affected_entity_id) {
+      setActiveVoiceContextId(String(issue.affected_entity_id));
+    }
     if (issue?.resolution_target?.workflow === '/inbox/finish-now') {
       setCurrentTab('inbox');
       return;
@@ -3364,6 +3393,76 @@
     setCurrentTab('inbox');
   };
 
+  const renderVoiceContextBanner = () => {
+    if (!(currentTab === 'inbox' || currentTab === 'quarter')) {
+      return null;
+    }
+
+    return (
+      <section className="voice-context-banner">
+        <div>
+          <strong>Voice triage</strong>
+          <div className="small-muted">
+            Use the mic against one selected finish-now item only. Supported commands here are category, business, personal, split n%, and attach receipt.
+          </div>
+        </div>
+        <div className="voice-context-row">
+          <span className={`voice-context-pill ${activeVoiceCard ? 'active' : ''}`}>
+            {activeVoiceCard
+              ? `Target: ${activeVoiceCard.merchant} (${activeVoiceCard.id})`
+              : 'Select a blocker card to arm voice triage'}
+          </span>
+          {activeVoiceCard && (
+            <button className="secondary-action" type="button" onClick={() => setCurrentTab('inbox')}>
+              Edit target
+            </button>
+          )}
+        </div>
+        {voiceMicroDecisionChip && (
+          <div className="voice-micro-chip">
+            <div>
+              <strong>{voiceMicroDecisionChip.confirmationChip}</strong>
+              <div className="small-muted">
+                {voiceMicroDecisionChip.targetLabel} ({voiceMicroDecisionChip.targetId})
+              </div>
+            </div>
+            {voiceMicroDecisionChip.labels.length > 0 && (
+              <div className="voice-chip-labels">
+                {voiceMicroDecisionChip.labels.map((label) => (
+                  <span key={`${voiceMicroDecisionChip.targetId}-${label}`} className="voice-action-pill">
+                    {label}
+                  </span>
+                ))}
+              </div>
+            )}
+            <div className="voice-chip-actions">
+              <button
+                className="secondary-action"
+                type="button"
+                onClick={() => {
+                  setCurrentTab('inbox');
+                  setActiveVoiceContextId(voiceMicroDecisionChip.targetId);
+                }}
+              >
+                Edit
+              </button>
+              {voiceMicroDecisionChip.undoSupported && (
+                <button
+                  className="secondary-action"
+                  type="button"
+                  onClick={undoLastInboxAction}
+                  disabled={inboxBusyId === 'undo-last'}
+                >
+                  Undo
+                </button>
+              )}
+            </div>
+          </div>
+        )}
+      </section>
+    );
+  };
+
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
@@ -4391,6 +4490,7 @@
         </button>
       </section>
       {connectionError && <div className="alert alert-danger">{connectionError}</div>}
+      {renderVoiceContextBanner()}
 
       {loading ? (
         <div className="text-center py-5">Loading...</div>
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..6eb469fcc7f023f8545d9e952a5823fb8c47a88d
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});

file update
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
@@ -2019,10 +2019,26 @@
       setVoiceStatus('processing');
-      const response = await axios.post(`${API_BASE_URL}/voice/process`, {
+      const microDecisionRequest = buildVoiceMicroDecisionRequest({
+        currentTab,
         transcript: cleanText,
-        device_id: 'mobile-v139',
-        current_date: new Date().toISOString(),
+        activeVoiceContextId,
+        inboxCards,
       });
-      if (response.data.confirmation_text && window.SpeechSynthesisUtterance) {
+
+      if (microDecisionRequest?.error) {
+        setInboxMessage({ type: 'error', text: microDecisionRequest.error });
+        setCurrentTab('inbox');
+        return;
+      }
+
+      const response = microDecisionRequest
+        ? await axios.post(`${API_BASE_URL}${microDecisionRequest.endpoint}`, microDecisionRequest.payload)
+        : await axios.post(`${API_BASE_URL}/voice/process`, {
+            transcript: cleanText,
+            device_id: 'mobile-v139',
+            current_date: new Date().toISOString(),
+          });
+      const spokenConfirmation = response.data.confirmation_chip || response.data.confirmation_text;
+      if (spokenConfirmation && window.SpeechSynthesisUtterance) {
         try {
-          const utterance = new window.SpeechSynthesisUtterance(response.data.confirmation_text);
+          const utterance = new window.SpeechSynthesisUtterance(spokenConfirmation);
           window.speechSynthesis.speak(utterance);
@@ -2031,6 +2047,19 @@
       }
-      executeVoiceAction(response.data);
-      fetchAllData();
+      if (microDecisionRequest) {
+        setVoiceMicroDecisionChip((previous) => reduceVoiceMicroDecisionChip(previous, response.data, microDecisionRequest.card));
+        setInboxMessage({
+          type: response.data.action_status === 'clarification_needed' ? 'success' : 'success',
+          text: response.data.confirmation_chip || 'Voice triage action applied.',
+        });
+      } else {
+        executeVoiceAction(response.data);
+      }
+      await fetchAllData();
     } catch (error) {
-      alert('AI Error');
+      const message = error?.response?.data?.error || 'AI Error';
+      if (currentTab === 'inbox' || currentTab === 'quarter') {
+        setInboxMessage({ type: 'error', text: message });
+      } else {
+        alert(message);
+      }
     } finally {
apply_patch(auto_approved=true) exited 0 in 1.21s:
Success. Updated the following files:
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..7564fe2a0b6132d2209937816e3f620cb3d91820
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,7 +10,34 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+
+const MICRO_DECISION_CONTEXT_SCOPES = new Set(['inbox', 'finish_now']);
 
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
+
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
  * Matches transcripts to core intents and extracts slots using regex.
@@ -267,71 +294,167 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_scope = null,
+  } = req.body || {};
   const text = String(transcript).toLowerCase().trim();
+  const normalizedContextScope = context_scope === null || context_scope === undefined
+    ? null
+    : String(context_scope).toLowerCase().replace(/-/g, '_');
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
 
+  const requireTransactionContext = () => {
+    if (!bank_txn_id) {
+      return 'bank_txn_id is required for inbox-bound micro-decision commands.';
+    }
+    if (!MICRO_DECISION_CONTEXT_SCOPES.has(normalizedContextScope)) {
+      return 'context_scope must be inbox or finish_now for transaction micro-decisions.';
+    }
+    return null;
+  };
+
   try {
     if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const categoryName = text.replace('category:', '').trim();
       const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: categoryCode,
+            category_name: categoryName,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${categoryName}`,
+        appliedActions: [{ type: 'category', label: categoryName }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${bp}`,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     const splitMatch = text.match(/split\s+(\d{1,3})%?/);
     if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const pct = Number(splitMatch[1]);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Split ${pct}%`,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: 'Attach receipt requested',
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        extra: {
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
     if (['match first', 'match second', 'match third'].includes(text)) {
@@ -364,7 +487,16 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Match ${index}`,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     if (text === 'no match') {
@@ -376,7 +508,16 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: 'Applied: No match',
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index 9b145fadac9123e3fa01b12af6ef2b9916dc11de..cf90f9191d9679f1824d532f0e5e4cad32768eab
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -50,6 +50,11 @@
   buildQuarterUiState,
   deriveResolvedIssueIdsFromSearchParams,
 } from './quarterReadiness';
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -427,6 +432,13 @@
   .date-group-count { border-radius: 999px; padding: 4px 9px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.72rem; color: var(--text-muted); }
   .date-group-body { padding: 0 12px 12px; }
   .voice-preview { background: rgba(0,0,0,0.82); color: white; padding: 8px 15px; border-radius: 15px; margin-bottom: 8px; font-size: 0.72rem; max-width: 240px; text-align: right; }
+  .voice-context-banner { display: grid; gap: 10px; padding: 14px 16px; margin-bottom: 16px; border-radius: 18px; background: var(--bg-card); border: 1px solid var(--border); box-shadow: var(--shadow); }
+  .voice-context-row, .voice-chip-actions, .voice-chip-labels { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
+  .voice-context-pill, .voice-action-pill { border-radius: 999px; padding: 6px 10px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.74rem; font-weight: 700; }
+  .voice-context-pill.active { background: rgba(13,92,99,0.12); color: var(--primary-strong); border-color: rgba(13,92,99,0.24); }
+  .voice-micro-chip { display: grid; gap: 10px; padding: 14px 16px; border-radius: 18px; background: rgba(13,92,99,0.08); border: 1px solid rgba(13,92,99,0.18); }
+  .voice-micro-chip strong { display: block; }
+  .inbox-action-card.voice-target { border-color: rgba(13,92,99,0.36); box-shadow: 0 0 0 2px rgba(13,92,99,0.12), var(--shadow); }
   .spinner { animation: rotate 2s linear infinite; }
   .capture-grid { display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(320px, 0.95fr); gap: 18px; align-items: start; }
   .command-centre { display: grid; gap: 18px; }
@@ -1382,6 +1394,8 @@
   const [inboxDrafts, setInboxDrafts] = useState({});
   const [inboxBusyId, setInboxBusyId] = useState('');
   const [inboxMessage, setInboxMessage] = useState(null);
+  const [activeVoiceContextId, setActiveVoiceContextId] = useState('');
+  const [voiceMicroDecisionChip, setVoiceMicroDecisionChip] = useState(null);
   const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
@@ -1606,10 +1620,20 @@
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
   const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const activeVoiceCard = findVoiceContextCard(inboxCards, activeVoiceContextId);
   const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
   const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
   const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
+  useEffect(() => {
+    if (!activeVoiceContextId) {
+      return;
+    }
+    if (!findVoiceContextCard(inboxCards, activeVoiceContextId)) {
+      setActiveVoiceContextId('');
+    }
+  }, [activeVoiceContextId, inboxCards]);
+
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
   const canManageGovernance = hasPermission(currentUser, PERMISSIONS.GOVERNANCE_MANAGE);
@@ -1806,11 +1830,13 @@
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxDrafts({});
+        setVoiceMicroDecisionChip(null);
         setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
         return;
       }
       await axios.post(`${API_BASE_URL}/inbox/undo-last`);
       await fetchAllData();
+      setVoiceMicroDecisionChip(null);
       setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
     } catch (error) {
       setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
@@ -1991,22 +2017,51 @@
     const cleanText = text.replace(/┬ú/g, 'GBP ');
     try {
       setVoiceStatus('processing');
-      const response = await axios.post(`${API_BASE_URL}/voice/process`, {
+      const microDecisionRequest = buildVoiceMicroDecisionRequest({
+        currentTab,
         transcript: cleanText,
-        device_id: 'mobile-v139',
-        current_date: new Date().toISOString(),
+        activeVoiceContextId,
+        inboxCards,
       });
-      if (response.data.confirmation_text && window.SpeechSynthesisUtterance) {
+
+      if (microDecisionRequest?.error) {
+        setInboxMessage({ type: 'error', text: microDecisionRequest.error });
+        setCurrentTab('inbox');
+        return;
+      }
+
+      const response = microDecisionRequest
+        ? await axios.post(`${API_BASE_URL}${microDecisionRequest.endpoint}`, microDecisionRequest.payload)
+        : await axios.post(`${API_BASE_URL}/voice/process`, {
+            transcript: cleanText,
+            device_id: 'mobile-v139',
+            current_date: new Date().toISOString(),
+          });
+      const spokenConfirmation = response.data.confirmation_chip || response.data.confirmation_text;
+      if (spokenConfirmation && window.SpeechSynthesisUtterance) {
         try {
-          const utterance = new window.SpeechSynthesisUtterance(response.data.confirmation_text);
+          const utterance = new window.SpeechSynthesisUtterance(spokenConfirmation);
           window.speechSynthesis.speak(utterance);
         } catch (_) {
         }
       }
-      executeVoiceAction(response.data);
-      fetchAllData();
+      if (microDecisionRequest) {
+        setVoiceMicroDecisionChip((previous) => reduceVoiceMicroDecisionChip(previous, response.data, microDecisionRequest.card));
+        setInboxMessage({
+          type: response.data.action_status === 'clarification_needed' ? 'success' : 'success',
+          text: response.data.confirmation_chip || 'Voice triage action applied.',
+        });
+      } else {
+        executeVoiceAction(response.data);
+      }
+      await fetchAllData();
     } catch (error) {
-      alert('AI Error');
+      const message = error?.response?.data?.error || 'AI Error';
+      if (currentTab === 'inbox' || currentTab === 'quarter') {
+        setInboxMessage({ type: 'error', text: message });
+      } else {
+        alert(message);
+      }
     } finally {
       setVoiceStatus('idle');
       setIsListening(false);
@@ -3357,6 +3412,9 @@
   };
 
   const openReadinessWorkflow = (issue) => {
+    if (issue?.affected_entity_id) {
+      setActiveVoiceContextId(String(issue.affected_entity_id));
+    }
     if (issue?.resolution_target?.workflow === '/inbox/finish-now') {
       setCurrentTab('inbox');
       return;
@@ -3364,6 +3422,76 @@
     setCurrentTab('inbox');
   };
 
+  const renderVoiceContextBanner = () => {
+    if (!(currentTab === 'inbox' || currentTab === 'quarter')) {
+      return null;
+    }
+
+    return (
+      <section className="voice-context-banner">
+        <div>
+          <strong>Voice triage</strong>
+          <div className="small-muted">
+            Use the mic against one selected finish-now item only. Supported commands here are category, business, personal, split n%, and attach receipt.
+          </div>
+        </div>
+        <div className="voice-context-row">
+          <span className={`voice-context-pill ${activeVoiceCard ? 'active' : ''}`}>
+            {activeVoiceCard
+              ? `Target: ${activeVoiceCard.merchant} (${activeVoiceCard.id})`
+              : 'Select a blocker card to arm voice triage'}
+          </span>
+          {activeVoiceCard && (
+            <button className="secondary-action" type="button" onClick={() => setCurrentTab('inbox')}>
+              Edit target
+            </button>
+          )}
+        </div>
+        {voiceMicroDecisionChip && (
+          <div className="voice-micro-chip">
+            <div>
+              <strong>{voiceMicroDecisionChip.confirmationChip}</strong>
+              <div className="small-muted">
+                {voiceMicroDecisionChip.targetLabel} ({voiceMicroDecisionChip.targetId})
+              </div>
+            </div>
+            {voiceMicroDecisionChip.labels.length > 0 && (
+              <div className="voice-chip-labels">
+                {voiceMicroDecisionChip.labels.map((label) => (
+                  <span key={`${voiceMicroDecisionChip.targetId}-${label}`} className="voice-action-pill">
+                    {label}
+                  </span>
+                ))}
+              </div>
+            )}
+            <div className="voice-chip-actions">
+              <button
+                className="secondary-action"
+                type="button"
+                onClick={() => {
+                  setCurrentTab('inbox');
+                  setActiveVoiceContextId(voiceMicroDecisionChip.targetId);
+                }}
+              >
+                Edit
+              </button>
+              {voiceMicroDecisionChip.undoSupported && (
+                <button
+                  className="secondary-action"
+                  type="button"
+                  onClick={undoLastInboxAction}
+                  disabled={inboxBusyId === 'undo-last'}
+                >
+                  Undo
+                </button>
+              )}
+            </div>
+          </div>
+        )}
+      </section>
+    );
+  };
+
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
@@ -4391,6 +4519,7 @@
         </button>
       </section>
       {connectionError && <div className="alert alert-danger">{connectionError}</div>}
+      {renderVoiceContextBanner()}
 
       {loading ? (
         <div className="text-center py-5">Loading...</div>
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..6eb469fcc7f023f8545d9e952a5823fb8c47a88d
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});

file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..7564fe2a0b6132d2209937816e3f620cb3d91820
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,7 +10,34 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+
+const MICRO_DECISION_CONTEXT_SCOPES = new Set(['inbox', 'finish_now']);
 
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
+
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
  * Matches transcripts to core intents and extracts slots using regex.
@@ -267,71 +294,167 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_scope = null,
+  } = req.body || {};
   const text = String(transcript).toLowerCase().trim();
+  const normalizedContextScope = context_scope === null || context_scope === undefined
+    ? null
+    : String(context_scope).toLowerCase().replace(/-/g, '_');
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
 
+  const requireTransactionContext = () => {
+    if (!bank_txn_id) {
+      return 'bank_txn_id is required for inbox-bound micro-decision commands.';
+    }
+    if (!MICRO_DECISION_CONTEXT_SCOPES.has(normalizedContextScope)) {
+      return 'context_scope must be inbox or finish_now for transaction micro-decisions.';
+    }
+    return null;
+  };
+
   try {
     if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const categoryName = text.replace('category:', '').trim();
       const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: categoryCode,
+            category_name: categoryName,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${categoryName}`,
+        appliedActions: [{ type: 'category', label: categoryName }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${bp}`,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     const splitMatch = text.match(/split\s+(\d{1,3})%?/);
     if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const pct = Number(splitMatch[1]);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Split ${pct}%`,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: 'Attach receipt requested',
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        extra: {
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
     if (['match first', 'match second', 'match third'].includes(text)) {
@@ -364,7 +487,16 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Match ${index}`,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     if (text === 'no match') {
@@ -376,7 +508,16 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: 'Applied: No match',
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index 9b145fadac9123e3fa01b12af6ef2b9916dc11de..cf90f9191d9679f1824d532f0e5e4cad32768eab
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -50,6 +50,11 @@
   buildQuarterUiState,
   deriveResolvedIssueIdsFromSearchParams,
 } from './quarterReadiness';
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -427,6 +432,13 @@
   .date-group-count { border-radius: 999px; padding: 4px 9px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.72rem; color: var(--text-muted); }
   .date-group-body { padding: 0 12px 12px; }
   .voice-preview { background: rgba(0,0,0,0.82); color: white; padding: 8px 15px; border-radius: 15px; margin-bottom: 8px; font-size: 0.72rem; max-width: 240px; text-align: right; }
+  .voice-context-banner { display: grid; gap: 10px; padding: 14px 16px; margin-bottom: 16px; border-radius: 18px; background: var(--bg-card); border: 1px solid var(--border); box-shadow: var(--shadow); }
+  .voice-context-row, .voice-chip-actions, .voice-chip-labels { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
+  .voice-context-pill, .voice-action-pill { border-radius: 999px; padding: 6px 10px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.74rem; font-weight: 700; }
+  .voice-context-pill.active { background: rgba(13,92,99,0.12); color: var(--primary-strong); border-color: rgba(13,92,99,0.24); }
+  .voice-micro-chip { display: grid; gap: 10px; padding: 14px 16px; border-radius: 18px; background: rgba(13,92,99,0.08); border: 1px solid rgba(13,92,99,0.18); }
+  .voice-micro-chip strong { display: block; }
+  .inbox-action-card.voice-target { border-color: rgba(13,92,99,0.36); box-shadow: 0 0 0 2px rgba(13,92,99,0.12), var(--shadow); }
   .spinner { animation: rotate 2s linear infinite; }
   .capture-grid { display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(320px, 0.95fr); gap: 18px; align-items: start; }
   .command-centre { display: grid; gap: 18px; }
@@ -1382,6 +1394,8 @@
   const [inboxDrafts, setInboxDrafts] = useState({});
   const [inboxBusyId, setInboxBusyId] = useState('');
   const [inboxMessage, setInboxMessage] = useState(null);
+  const [activeVoiceContextId, setActiveVoiceContextId] = useState('');
+  const [voiceMicroDecisionChip, setVoiceMicroDecisionChip] = useState(null);
   const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
@@ -1606,10 +1620,20 @@
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
   const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const activeVoiceCard = findVoiceContextCard(inboxCards, activeVoiceContextId);
   const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
   const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
   const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
+  useEffect(() => {
+    if (!activeVoiceContextId) {
+      return;
+    }
+    if (!findVoiceContextCard(inboxCards, activeVoiceContextId)) {
+      setActiveVoiceContextId('');
+    }
+  }, [activeVoiceContextId, inboxCards]);
+
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
   const canManageGovernance = hasPermission(currentUser, PERMISSIONS.GOVERNANCE_MANAGE);
@@ -1806,11 +1830,13 @@
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxDrafts({});
+        setVoiceMicroDecisionChip(null);
         setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
         return;
       }
       await axios.post(`${API_BASE_URL}/inbox/undo-last`);
       await fetchAllData();
+      setVoiceMicroDecisionChip(null);
       setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
     } catch (error) {
       setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
@@ -1991,22 +2017,51 @@
     const cleanText = text.replace(/┬ú/g, 'GBP ');
     try {
       setVoiceStatus('processing');
-      const response = await axios.post(`${API_BASE_URL}/voice/process`, {
+      const microDecisionRequest = buildVoiceMicroDecisionRequest({
+        currentTab,
         transcript: cleanText,
-        device_id: 'mobile-v139',
-        current_date: new Date().toISOString(),
+        activeVoiceContextId,
+        inboxCards,
       });
-      if (response.data.confirmation_text && window.SpeechSynthesisUtterance) {
+
+      if (microDecisionRequest?.error) {
+        setInboxMessage({ type: 'error', text: microDecisionRequest.error });
+        setCurrentTab('inbox');
+        return;
+      }
+
+      const response = microDecisionRequest
+        ? await axios.post(`${API_BASE_URL}${microDecisionRequest.endpoint}`, microDecisionRequest.payload)
+        : await axios.post(`${API_BASE_URL}/voice/process`, {
+            transcript: cleanText,
+            device_id: 'mobile-v139',
+            current_date: new Date().toISOString(),
+          });
+      const spokenConfirmation = response.data.confirmation_chip || response.data.confirmation_text;
+      if (spokenConfirmation && window.SpeechSynthesisUtterance) {
         try {
-          const utterance = new window.SpeechSynthesisUtterance(response.data.confirmation_text);
+          const utterance = new window.SpeechSynthesisUtterance(spokenConfirmation);
           window.speechSynthesis.speak(utterance);
         } catch (_) {
         }
       }
-      executeVoiceAction(response.data);
-      fetchAllData();
+      if (microDecisionRequest) {
+        setVoiceMicroDecisionChip((previous) => reduceVoiceMicroDecisionChip(previous, response.data, microDecisionRequest.card));
+        setInboxMessage({
+          type: response.data.action_status === 'clarification_needed' ? 'success' : 'success',
+          text: response.data.confirmation_chip || 'Voice triage action applied.',
+        });
+      } else {
+        executeVoiceAction(response.data);
+      }
+      await fetchAllData();
     } catch (error) {
-      alert('AI Error');
+      const message = error?.response?.data?.error || 'AI Error';
+      if (currentTab === 'inbox' || currentTab === 'quarter') {
+        setInboxMessage({ type: 'error', text: message });
+      } else {
+        alert(message);
+      }
     } finally {
       setVoiceStatus('idle');
       setIsListening(false);
@@ -3357,6 +3412,9 @@
   };
 
   const openReadinessWorkflow = (issue) => {
+    if (issue?.affected_entity_id) {
+      setActiveVoiceContextId(String(issue.affected_entity_id));
+    }
     if (issue?.resolution_target?.workflow === '/inbox/finish-now') {
       setCurrentTab('inbox');
       return;
@@ -3364,6 +3422,76 @@
     setCurrentTab('inbox');
   };
 
+  const renderVoiceContextBanner = () => {
+    if (!(currentTab === 'inbox' || currentTab === 'quarter')) {
+      return null;
+    }
+
+    return (
+      <section className="voice-context-banner">
+        <div>
+          <strong>Voice triage</strong>
+          <div className="small-muted">
+            Use the mic against one selected finish-now item only. Supported commands here are category, business, personal, split n%, and attach receipt.
+          </div>
+        </div>
+        <div className="voice-context-row">
+          <span className={`voice-context-pill ${activeVoiceCard ? 'active' : ''}`}>
+            {activeVoiceCard
+              ? `Target: ${activeVoiceCard.merchant} (${activeVoiceCard.id})`
+              : 'Select a blocker card to arm voice triage'}
+          </span>
+          {activeVoiceCard && (
+            <button className="secondary-action" type="button" onClick={() => setCurrentTab('inbox')}>
+              Edit target
+            </button>
+          )}
+        </div>
+        {voiceMicroDecisionChip && (
+          <div className="voice-micro-chip">
+            <div>
+              <strong>{voiceMicroDecisionChip.confirmationChip}</strong>
+              <div className="small-muted">
+                {voiceMicroDecisionChip.targetLabel} ({voiceMicroDecisionChip.targetId})
+              </div>
+            </div>
+            {voiceMicroDecisionChip.labels.length > 0 && (
+              <div className="voice-chip-labels">
+                {voiceMicroDecisionChip.labels.map((label) => (
+                  <span key={`${voiceMicroDecisionChip.targetId}-${label}`} className="voice-action-pill">
+                    {label}
+                  </span>
+                ))}
+              </div>
+            )}
+            <div className="voice-chip-actions">
+              <button
+                className="secondary-action"
+                type="button"
+                onClick={() => {
+                  setCurrentTab('inbox');
+                  setActiveVoiceContextId(voiceMicroDecisionChip.targetId);
+                }}
+              >
+                Edit
+              </button>
+              {voiceMicroDecisionChip.undoSupported && (
+                <button
+                  className="secondary-action"
+                  type="button"
+                  onClick={undoLastInboxAction}
+                  disabled={inboxBusyId === 'undo-last'}
+                >
+                  Undo
+                </button>
+              )}
+            </div>
+          </div>
+        )}
+      </section>
+    );
+  };
+
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
@@ -4391,6 +4519,7 @@
         </button>
       </section>
       {connectionError && <div className="alert alert-danger">{connectionError}</div>}
+      {renderVoiceContextBanner()}
 
       {loading ? (
         <div className="text-center py-5">Loading...</div>
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..6eb469fcc7f023f8545d9e952a5823fb8c47a88d
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});

file update
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
@@ -2951,3 +2951,7 @@
             return (
-              <section key={card.id} className="inbox-action-card">
+              <section
+                key={card.id}
+                className={`inbox-action-card ${activeVoiceContextId === card.id ? 'voice-target' : ''}`}
+                onClick={() => setActiveVoiceContextId(card.id)}
+              >
                 <div className="inbox-card-top">
@@ -2971,2 +2975,5 @@
                   ))}
+                  {activeVoiceContextId === card.id && (
+                    <span className="voice-context-pill active">Voice target</span>
+                  )}
                   {card.suggested_category?.category_name && (
apply_patch(auto_approved=true) exited 0 in 2.51s:
Success. Updated the following files:
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..7564fe2a0b6132d2209937816e3f620cb3d91820
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,7 +10,34 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+
+const MICRO_DECISION_CONTEXT_SCOPES = new Set(['inbox', 'finish_now']);
 
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
+
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
  * Matches transcripts to core intents and extracts slots using regex.
@@ -267,71 +294,167 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_scope = null,
+  } = req.body || {};
   const text = String(transcript).toLowerCase().trim();
+  const normalizedContextScope = context_scope === null || context_scope === undefined
+    ? null
+    : String(context_scope).toLowerCase().replace(/-/g, '_');
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
 
+  const requireTransactionContext = () => {
+    if (!bank_txn_id) {
+      return 'bank_txn_id is required for inbox-bound micro-decision commands.';
+    }
+    if (!MICRO_DECISION_CONTEXT_SCOPES.has(normalizedContextScope)) {
+      return 'context_scope must be inbox or finish_now for transaction micro-decisions.';
+    }
+    return null;
+  };
+
   try {
     if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const categoryName = text.replace('category:', '').trim();
       const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: categoryCode,
+            category_name: categoryName,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${categoryName}`,
+        appliedActions: [{ type: 'category', label: categoryName }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${bp}`,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     const splitMatch = text.match(/split\s+(\d{1,3})%?/);
     if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const pct = Number(splitMatch[1]);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Split ${pct}%`,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: 'Attach receipt requested',
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        extra: {
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
     if (['match first', 'match second', 'match third'].includes(text)) {
@@ -364,7 +487,16 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Match ${index}`,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     if (text === 'no match') {
@@ -376,7 +508,16 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: 'Applied: No match',
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index 9b145fadac9123e3fa01b12af6ef2b9916dc11de..ad1176ec681a5978dd726067d5730d04943f0f72
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -50,6 +50,11 @@
   buildQuarterUiState,
   deriveResolvedIssueIdsFromSearchParams,
 } from './quarterReadiness';
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -427,6 +432,13 @@
   .date-group-count { border-radius: 999px; padding: 4px 9px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.72rem; color: var(--text-muted); }
   .date-group-body { padding: 0 12px 12px; }
   .voice-preview { background: rgba(0,0,0,0.82); color: white; padding: 8px 15px; border-radius: 15px; margin-bottom: 8px; font-size: 0.72rem; max-width: 240px; text-align: right; }
+  .voice-context-banner { display: grid; gap: 10px; padding: 14px 16px; margin-bottom: 16px; border-radius: 18px; background: var(--bg-card); border: 1px solid var(--border); box-shadow: var(--shadow); }
+  .voice-context-row, .voice-chip-actions, .voice-chip-labels { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
+  .voice-context-pill, .voice-action-pill { border-radius: 999px; padding: 6px 10px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.74rem; font-weight: 700; }
+  .voice-context-pill.active { background: rgba(13,92,99,0.12); color: var(--primary-strong); border-color: rgba(13,92,99,0.24); }
+  .voice-micro-chip { display: grid; gap: 10px; padding: 14px 16px; border-radius: 18px; background: rgba(13,92,99,0.08); border: 1px solid rgba(13,92,99,0.18); }
+  .voice-micro-chip strong { display: block; }
+  .inbox-action-card.voice-target { border-color: rgba(13,92,99,0.36); box-shadow: 0 0 0 2px rgba(13,92,99,0.12), var(--shadow); }
   .spinner { animation: rotate 2s linear infinite; }
   .capture-grid { display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(320px, 0.95fr); gap: 18px; align-items: start; }
   .command-centre { display: grid; gap: 18px; }
@@ -1382,6 +1394,8 @@
   const [inboxDrafts, setInboxDrafts] = useState({});
   const [inboxBusyId, setInboxBusyId] = useState('');
   const [inboxMessage, setInboxMessage] = useState(null);
+  const [activeVoiceContextId, setActiveVoiceContextId] = useState('');
+  const [voiceMicroDecisionChip, setVoiceMicroDecisionChip] = useState(null);
   const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
@@ -1606,10 +1620,20 @@
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
   const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const activeVoiceCard = findVoiceContextCard(inboxCards, activeVoiceContextId);
   const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
   const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
   const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
+  useEffect(() => {
+    if (!activeVoiceContextId) {
+      return;
+    }
+    if (!findVoiceContextCard(inboxCards, activeVoiceContextId)) {
+      setActiveVoiceContextId('');
+    }
+  }, [activeVoiceContextId, inboxCards]);
+
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
   const canManageGovernance = hasPermission(currentUser, PERMISSIONS.GOVERNANCE_MANAGE);
@@ -1806,11 +1830,13 @@
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxDrafts({});
+        setVoiceMicroDecisionChip(null);
         setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
         return;
       }
       await axios.post(`${API_BASE_URL}/inbox/undo-last`);
       await fetchAllData();
+      setVoiceMicroDecisionChip(null);
       setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
     } catch (error) {
       setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
@@ -1991,22 +2017,51 @@
     const cleanText = text.replace(/┬ú/g, 'GBP ');
     try {
       setVoiceStatus('processing');
-      const response = await axios.post(`${API_BASE_URL}/voice/process`, {
+      const microDecisionRequest = buildVoiceMicroDecisionRequest({
+        currentTab,
         transcript: cleanText,
-        device_id: 'mobile-v139',
-        current_date: new Date().toISOString(),
+        activeVoiceContextId,
+        inboxCards,
       });
-      if (response.data.confirmation_text && window.SpeechSynthesisUtterance) {
+
+      if (microDecisionRequest?.error) {
+        setInboxMessage({ type: 'error', text: microDecisionRequest.error });
+        setCurrentTab('inbox');
+        return;
+      }
+
+      const response = microDecisionRequest
+        ? await axios.post(`${API_BASE_URL}${microDecisionRequest.endpoint}`, microDecisionRequest.payload)
+        : await axios.post(`${API_BASE_URL}/voice/process`, {
+            transcript: cleanText,
+            device_id: 'mobile-v139',
+            current_date: new Date().toISOString(),
+          });
+      const spokenConfirmation = response.data.confirmation_chip || response.data.confirmation_text;
+      if (spokenConfirmation && window.SpeechSynthesisUtterance) {
         try {
-          const utterance = new window.SpeechSynthesisUtterance(response.data.confirmation_text);
+          const utterance = new window.SpeechSynthesisUtterance(spokenConfirmation);
           window.speechSynthesis.speak(utterance);
         } catch (_) {
         }
       }
-      executeVoiceAction(response.data);
-      fetchAllData();
+      if (microDecisionRequest) {
+        setVoiceMicroDecisionChip((previous) => reduceVoiceMicroDecisionChip(previous, response.data, microDecisionRequest.card));
+        setInboxMessage({
+          type: response.data.action_status === 'clarification_needed' ? 'success' : 'success',
+          text: response.data.confirmation_chip || 'Voice triage action applied.',
+        });
+      } else {
+        executeVoiceAction(response.data);
+      }
+      await fetchAllData();
     } catch (error) {
-      alert('AI Error');
+      const message = error?.response?.data?.error || 'AI Error';
+      if (currentTab === 'inbox' || currentTab === 'quarter') {
+        setInboxMessage({ type: 'error', text: message });
+      } else {
+        alert(message);
+      }
     } finally {
       setVoiceStatus('idle');
       setIsListening(false);
@@ -2894,7 +2949,11 @@
             const saving = inboxBusyId === card.id;
             const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
             return (
-              <section key={card.id} className="inbox-action-card">
+              <section
+                key={card.id}
+                className={`inbox-action-card ${activeVoiceContextId === card.id ? 'voice-target' : ''}`}
+                onClick={() => setActiveVoiceContextId(card.id)}
+              >
                 <div className="inbox-card-top">
                   <div>
                     <strong>{card.merchant}</strong>
@@ -2914,6 +2973,9 @@
                       {label}
                     </span>
                   ))}
+                  {activeVoiceContextId === card.id && (
+                    <span className="voice-context-pill active">Voice target</span>
+                  )}
                   {card.suggested_category?.category_name && (
                     <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
                       <CheckCircle2 size={14} />
@@ -3357,6 +3419,9 @@
   };
 
   const openReadinessWorkflow = (issue) => {
+    if (issue?.affected_entity_id) {
+      setActiveVoiceContextId(String(issue.affected_entity_id));
+    }
     if (issue?.resolution_target?.workflow === '/inbox/finish-now') {
       setCurrentTab('inbox');
       return;
@@ -3364,6 +3429,76 @@
     setCurrentTab('inbox');
   };
 
+  const renderVoiceContextBanner = () => {
+    if (!(currentTab === 'inbox' || currentTab === 'quarter')) {
+      return null;
+    }
+
+    return (
+      <section className="voice-context-banner">
+        <div>
+          <strong>Voice triage</strong>
+          <div className="small-muted">
+            Use the mic against one selected finish-now item only. Supported commands here are category, business, personal, split n%, and attach receipt.
+          </div>
+        </div>
+        <div className="voice-context-row">
+          <span className={`voice-context-pill ${activeVoiceCard ? 'active' : ''}`}>
+            {activeVoiceCard
+              ? `Target: ${activeVoiceCard.merchant} (${activeVoiceCard.id})`
+              : 'Select a blocker card to arm voice triage'}
+          </span>
+          {activeVoiceCard && (
+            <button className="secondary-action" type="button" onClick={() => setCurrentTab('inbox')}>
+              Edit target
+            </button>
+          )}
+        </div>
+        {voiceMicroDecisionChip && (
+          <div className="voice-micro-chip">
+            <div>
+              <strong>{voiceMicroDecisionChip.confirmationChip}</strong>
+              <div className="small-muted">
+                {voiceMicroDecisionChip.targetLabel} ({voiceMicroDecisionChip.targetId})
+              </div>
+            </div>
+            {voiceMicroDecisionChip.labels.length > 0 && (
+              <div className="voice-chip-labels">
+                {voiceMicroDecisionChip.labels.map((label) => (
+                  <span key={`${voiceMicroDecisionChip.targetId}-${label}`} className="voice-action-pill">
+                    {label}
+                  </span>
+                ))}
+              </div>
+            )}
+            <div className="voice-chip-actions">
+              <button
+                className="secondary-action"
+                type="button"
+                onClick={() => {
+                  setCurrentTab('inbox');
+                  setActiveVoiceContextId(voiceMicroDecisionChip.targetId);
+                }}
+              >
+                Edit
+              </button>
+              {voiceMicroDecisionChip.undoSupported && (
+                <button
+                  className="secondary-action"
+                  type="button"
+                  onClick={undoLastInboxAction}
+                  disabled={inboxBusyId === 'undo-last'}
+                >
+                  Undo
+                </button>
+              )}
+            </div>
+          </div>
+        )}
+      </section>
+    );
+  };
+
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
@@ -4391,6 +4526,7 @@
         </button>
       </section>
       {connectionError && <div className="alert alert-danger">{connectionError}</div>}
+      {renderVoiceContextBanner()}
 
       {loading ? (
         <div className="text-center py-5">Loading...</div>
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..6eb469fcc7f023f8545d9e952a5823fb8c47a88d
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});

file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..7564fe2a0b6132d2209937816e3f620cb3d91820
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,7 +10,34 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+
+const MICRO_DECISION_CONTEXT_SCOPES = new Set(['inbox', 'finish_now']);
 
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
+
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
  * Matches transcripts to core intents and extracts slots using regex.
@@ -267,71 +294,167 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_scope = null,
+  } = req.body || {};
   const text = String(transcript).toLowerCase().trim();
+  const normalizedContextScope = context_scope === null || context_scope === undefined
+    ? null
+    : String(context_scope).toLowerCase().replace(/-/g, '_');
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
 
+  const requireTransactionContext = () => {
+    if (!bank_txn_id) {
+      return 'bank_txn_id is required for inbox-bound micro-decision commands.';
+    }
+    if (!MICRO_DECISION_CONTEXT_SCOPES.has(normalizedContextScope)) {
+      return 'context_scope must be inbox or finish_now for transaction micro-decisions.';
+    }
+    return null;
+  };
+
   try {
     if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const categoryName = text.replace('category:', '').trim();
       const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: categoryCode,
+            category_name: categoryName,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${categoryName}`,
+        appliedActions: [{ type: 'category', label: categoryName }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${bp}`,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     const splitMatch = text.match(/split\s+(\d{1,3})%?/);
     if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const pct = Number(splitMatch[1]);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Split ${pct}%`,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: 'Attach receipt requested',
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        extra: {
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
     if (['match first', 'match second', 'match third'].includes(text)) {
@@ -364,7 +487,16 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Match ${index}`,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     if (text === 'no match') {
@@ -376,7 +508,16 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: 'Applied: No match',
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index 9b145fadac9123e3fa01b12af6ef2b9916dc11de..ad1176ec681a5978dd726067d5730d04943f0f72
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -50,6 +50,11 @@
   buildQuarterUiState,
   deriveResolvedIssueIdsFromSearchParams,
 } from './quarterReadiness';
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -427,6 +432,13 @@
   .date-group-count { border-radius: 999px; padding: 4px 9px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.72rem; color: var(--text-muted); }
   .date-group-body { padding: 0 12px 12px; }
   .voice-preview { background: rgba(0,0,0,0.82); color: white; padding: 8px 15px; border-radius: 15px; margin-bottom: 8px; font-size: 0.72rem; max-width: 240px; text-align: right; }
+  .voice-context-banner { display: grid; gap: 10px; padding: 14px 16px; margin-bottom: 16px; border-radius: 18px; background: var(--bg-card); border: 1px solid var(--border); box-shadow: var(--shadow); }
+  .voice-context-row, .voice-chip-actions, .voice-chip-labels { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
+  .voice-context-pill, .voice-action-pill { border-radius: 999px; padding: 6px 10px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.74rem; font-weight: 700; }
+  .voice-context-pill.active { background: rgba(13,92,99,0.12); color: var(--primary-strong); border-color: rgba(13,92,99,0.24); }
+  .voice-micro-chip { display: grid; gap: 10px; padding: 14px 16px; border-radius: 18px; background: rgba(13,92,99,0.08); border: 1px solid rgba(13,92,99,0.18); }
+  .voice-micro-chip strong { display: block; }
+  .inbox-action-card.voice-target { border-color: rgba(13,92,99,0.36); box-shadow: 0 0 0 2px rgba(13,92,99,0.12), var(--shadow); }
   .spinner { animation: rotate 2s linear infinite; }
   .capture-grid { display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(320px, 0.95fr); gap: 18px; align-items: start; }
   .command-centre { display: grid; gap: 18px; }
@@ -1382,6 +1394,8 @@
   const [inboxDrafts, setInboxDrafts] = useState({});
   const [inboxBusyId, setInboxBusyId] = useState('');
   const [inboxMessage, setInboxMessage] = useState(null);
+  const [activeVoiceContextId, setActiveVoiceContextId] = useState('');
+  const [voiceMicroDecisionChip, setVoiceMicroDecisionChip] = useState(null);
   const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
@@ -1606,10 +1620,20 @@
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
   const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const activeVoiceCard = findVoiceContextCard(inboxCards, activeVoiceContextId);
   const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
   const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
   const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
+  useEffect(() => {
+    if (!activeVoiceContextId) {
+      return;
+    }
+    if (!findVoiceContextCard(inboxCards, activeVoiceContextId)) {
+      setActiveVoiceContextId('');
+    }
+  }, [activeVoiceContextId, inboxCards]);
+
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
   const canManageGovernance = hasPermission(currentUser, PERMISSIONS.GOVERNANCE_MANAGE);
@@ -1806,11 +1830,13 @@
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxDrafts({});
+        setVoiceMicroDecisionChip(null);
         setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
         return;
       }
       await axios.post(`${API_BASE_URL}/inbox/undo-last`);
       await fetchAllData();
+      setVoiceMicroDecisionChip(null);
       setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
     } catch (error) {
       setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
@@ -1991,22 +2017,51 @@
     const cleanText = text.replace(/┬ú/g, 'GBP ');
     try {
       setVoiceStatus('processing');
-      const response = await axios.post(`${API_BASE_URL}/voice/process`, {
+      const microDecisionRequest = buildVoiceMicroDecisionRequest({
+        currentTab,
         transcript: cleanText,
-        device_id: 'mobile-v139',
-        current_date: new Date().toISOString(),
+        activeVoiceContextId,
+        inboxCards,
       });
-      if (response.data.confirmation_text && window.SpeechSynthesisUtterance) {
+
+      if (microDecisionRequest?.error) {
+        setInboxMessage({ type: 'error', text: microDecisionRequest.error });
+        setCurrentTab('inbox');
+        return;
+      }
+
+      const response = microDecisionRequest
+        ? await axios.post(`${API_BASE_URL}${microDecisionRequest.endpoint}`, microDecisionRequest.payload)
+        : await axios.post(`${API_BASE_URL}/voice/process`, {
+            transcript: cleanText,
+            device_id: 'mobile-v139',
+            current_date: new Date().toISOString(),
+          });
+      const spokenConfirmation = response.data.confirmation_chip || response.data.confirmation_text;
+      if (spokenConfirmation && window.SpeechSynthesisUtterance) {
         try {
-          const utterance = new window.SpeechSynthesisUtterance(response.data.confirmation_text);
+          const utterance = new window.SpeechSynthesisUtterance(spokenConfirmation);
           window.speechSynthesis.speak(utterance);
         } catch (_) {
         }
       }
-      executeVoiceAction(response.data);
-      fetchAllData();
+      if (microDecisionRequest) {
+        setVoiceMicroDecisionChip((previous) => reduceVoiceMicroDecisionChip(previous, response.data, microDecisionRequest.card));
+        setInboxMessage({
+          type: response.data.action_status === 'clarification_needed' ? 'success' : 'success',
+          text: response.data.confirmation_chip || 'Voice triage action applied.',
+        });
+      } else {
+        executeVoiceAction(response.data);
+      }
+      await fetchAllData();
     } catch (error) {
-      alert('AI Error');
+      const message = error?.response?.data?.error || 'AI Error';
+      if (currentTab === 'inbox' || currentTab === 'quarter') {
+        setInboxMessage({ type: 'error', text: message });
+      } else {
+        alert(message);
+      }
     } finally {
       setVoiceStatus('idle');
       setIsListening(false);
@@ -2894,7 +2949,11 @@
             const saving = inboxBusyId === card.id;
             const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
             return (
-              <section key={card.id} className="inbox-action-card">
+              <section
+                key={card.id}
+                className={`inbox-action-card ${activeVoiceContextId === card.id ? 'voice-target' : ''}`}
+                onClick={() => setActiveVoiceContextId(card.id)}
+              >
                 <div className="inbox-card-top">
                   <div>
                     <strong>{card.merchant}</strong>
@@ -2914,6 +2973,9 @@
                       {label}
                     </span>
                   ))}
+                  {activeVoiceContextId === card.id && (
+                    <span className="voice-context-pill active">Voice target</span>
+                  )}
                   {card.suggested_category?.category_name && (
                     <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
                       <CheckCircle2 size={14} />
@@ -3357,6 +3419,9 @@
   };
 
   const openReadinessWorkflow = (issue) => {
+    if (issue?.affected_entity_id) {
+      setActiveVoiceContextId(String(issue.affected_entity_id));
+    }
     if (issue?.resolution_target?.workflow === '/inbox/finish-now') {
       setCurrentTab('inbox');
       return;
@@ -3364,6 +3429,76 @@
     setCurrentTab('inbox');
   };
 
+  const renderVoiceContextBanner = () => {
+    if (!(currentTab === 'inbox' || currentTab === 'quarter')) {
+      return null;
+    }
+
+    return (
+      <section className="voice-context-banner">
+        <div>
+          <strong>Voice triage</strong>
+          <div className="small-muted">
+            Use the mic against one selected finish-now item only. Supported commands here are category, business, personal, split n%, and attach receipt.
+          </div>
+        </div>
+        <div className="voice-context-row">
+          <span className={`voice-context-pill ${activeVoiceCard ? 'active' : ''}`}>
+            {activeVoiceCard
+              ? `Target: ${activeVoiceCard.merchant} (${activeVoiceCard.id})`
+              : 'Select a blocker card to arm voice triage'}
+          </span>
+          {activeVoiceCard && (
+            <button className="secondary-action" type="button" onClick={() => setCurrentTab('inbox')}>
+              Edit target
+            </button>
+          )}
+        </div>
+        {voiceMicroDecisionChip && (
+          <div className="voice-micro-chip">
+            <div>
+              <strong>{voiceMicroDecisionChip.confirmationChip}</strong>
+              <div className="small-muted">
+                {voiceMicroDecisionChip.targetLabel} ({voiceMicroDecisionChip.targetId})
+              </div>
+            </div>
+            {voiceMicroDecisionChip.labels.length > 0 && (
+              <div className="voice-chip-labels">
+                {voiceMicroDecisionChip.labels.map((label) => (
+                  <span key={`${voiceMicroDecisionChip.targetId}-${label}`} className="voice-action-pill">
+                    {label}
+                  </span>
+                ))}
+              </div>
+            )}
+            <div className="voice-chip-actions">
+              <button
+                className="secondary-action"
+                type="button"
+                onClick={() => {
+                  setCurrentTab('inbox');
+                  setActiveVoiceContextId(voiceMicroDecisionChip.targetId);
+                }}
+              >
+                Edit
+              </button>
+              {voiceMicroDecisionChip.undoSupported && (
+                <button
+                  className="secondary-action"
+                  type="button"
+                  onClick={undoLastInboxAction}
+                  disabled={inboxBusyId === 'undo-last'}
+                >
+                  Undo
+                </button>
+              )}
+            </div>
+          </div>
+        )}
+      </section>
+    );
+  };
+
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
@@ -4391,6 +4526,7 @@
         </button>
       </section>
       {connectionError && <div className="alert alert-danger">{connectionError}</div>}
+      {renderVoiceContextBanner()}
 
       {loading ? (
         <div className="text-center py-5">Loading...</div>
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..6eb469fcc7f023f8545d9e952a5823fb8c47a88d
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});

file update
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
@@ -3791,2 +3791,9 @@
                   <div className="finish-now-actions">
+                    <button
+                      className="secondary-action"
+                      type="button"
+                      onClick={() => setActiveVoiceContextId(String(issue.affected_entity_id))}
+                    >
+                      {activeVoiceContextId === String(issue.affected_entity_id) ? 'Voice target set' : 'Arm voice'}
+                    </button>
                     {(readinessDemoEnabled || snapshotDemoEnabled) ? (
apply_patch(auto_approved=true) exited 0 in 3.26s:
Success. Updated the following files:
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..7564fe2a0b6132d2209937816e3f620cb3d91820
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,7 +10,34 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+
+const MICRO_DECISION_CONTEXT_SCOPES = new Set(['inbox', 'finish_now']);
 
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
+
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
  * Matches transcripts to core intents and extracts slots using regex.
@@ -267,71 +294,167 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_scope = null,
+  } = req.body || {};
   const text = String(transcript).toLowerCase().trim();
+  const normalizedContextScope = context_scope === null || context_scope === undefined
+    ? null
+    : String(context_scope).toLowerCase().replace(/-/g, '_');
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
 
+  const requireTransactionContext = () => {
+    if (!bank_txn_id) {
+      return 'bank_txn_id is required for inbox-bound micro-decision commands.';
+    }
+    if (!MICRO_DECISION_CONTEXT_SCOPES.has(normalizedContextScope)) {
+      return 'context_scope must be inbox or finish_now for transaction micro-decisions.';
+    }
+    return null;
+  };
+
   try {
     if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const categoryName = text.replace('category:', '').trim();
       const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: categoryCode,
+            category_name: categoryName,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${categoryName}`,
+        appliedActions: [{ type: 'category', label: categoryName }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${bp}`,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     const splitMatch = text.match(/split\s+(\d{1,3})%?/);
     if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const pct = Number(splitMatch[1]);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Split ${pct}%`,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: 'Attach receipt requested',
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        extra: {
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
     if (['match first', 'match second', 'match third'].includes(text)) {
@@ -364,7 +487,16 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Match ${index}`,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     if (text === 'no match') {
@@ -376,7 +508,16 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: 'Applied: No match',
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index 9b145fadac9123e3fa01b12af6ef2b9916dc11de..7b3e1ac0327da0b509b3ec7c375f154ab385a3cf
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -50,6 +50,11 @@
   buildQuarterUiState,
   deriveResolvedIssueIdsFromSearchParams,
 } from './quarterReadiness';
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -427,6 +432,13 @@
   .date-group-count { border-radius: 999px; padding: 4px 9px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.72rem; color: var(--text-muted); }
   .date-group-body { padding: 0 12px 12px; }
   .voice-preview { background: rgba(0,0,0,0.82); color: white; padding: 8px 15px; border-radius: 15px; margin-bottom: 8px; font-size: 0.72rem; max-width: 240px; text-align: right; }
+  .voice-context-banner { display: grid; gap: 10px; padding: 14px 16px; margin-bottom: 16px; border-radius: 18px; background: var(--bg-card); border: 1px solid var(--border); box-shadow: var(--shadow); }
+  .voice-context-row, .voice-chip-actions, .voice-chip-labels { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
+  .voice-context-pill, .voice-action-pill { border-radius: 999px; padding: 6px 10px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.74rem; font-weight: 700; }
+  .voice-context-pill.active { background: rgba(13,92,99,0.12); color: var(--primary-strong); border-color: rgba(13,92,99,0.24); }
+  .voice-micro-chip { display: grid; gap: 10px; padding: 14px 16px; border-radius: 18px; background: rgba(13,92,99,0.08); border: 1px solid rgba(13,92,99,0.18); }
+  .voice-micro-chip strong { display: block; }
+  .inbox-action-card.voice-target { border-color: rgba(13,92,99,0.36); box-shadow: 0 0 0 2px rgba(13,92,99,0.12), var(--shadow); }
   .spinner { animation: rotate 2s linear infinite; }
   .capture-grid { display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(320px, 0.95fr); gap: 18px; align-items: start; }
   .command-centre { display: grid; gap: 18px; }
@@ -1382,6 +1394,8 @@
   const [inboxDrafts, setInboxDrafts] = useState({});
   const [inboxBusyId, setInboxBusyId] = useState('');
   const [inboxMessage, setInboxMessage] = useState(null);
+  const [activeVoiceContextId, setActiveVoiceContextId] = useState('');
+  const [voiceMicroDecisionChip, setVoiceMicroDecisionChip] = useState(null);
   const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
@@ -1606,10 +1620,20 @@
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
   const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const activeVoiceCard = findVoiceContextCard(inboxCards, activeVoiceContextId);
   const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
   const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
   const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
+  useEffect(() => {
+    if (!activeVoiceContextId) {
+      return;
+    }
+    if (!findVoiceContextCard(inboxCards, activeVoiceContextId)) {
+      setActiveVoiceContextId('');
+    }
+  }, [activeVoiceContextId, inboxCards]);
+
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
   const canManageGovernance = hasPermission(currentUser, PERMISSIONS.GOVERNANCE_MANAGE);
@@ -1806,11 +1830,13 @@
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxDrafts({});
+        setVoiceMicroDecisionChip(null);
         setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
         return;
       }
       await axios.post(`${API_BASE_URL}/inbox/undo-last`);
       await fetchAllData();
+      setVoiceMicroDecisionChip(null);
       setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
     } catch (error) {
       setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
@@ -1991,22 +2017,51 @@
     const cleanText = text.replace(/┬ú/g, 'GBP ');
     try {
       setVoiceStatus('processing');
-      const response = await axios.post(`${API_BASE_URL}/voice/process`, {
+      const microDecisionRequest = buildVoiceMicroDecisionRequest({
+        currentTab,
         transcript: cleanText,
-        device_id: 'mobile-v139',
-        current_date: new Date().toISOString(),
+        activeVoiceContextId,
+        inboxCards,
       });
-      if (response.data.confirmation_text && window.SpeechSynthesisUtterance) {
+
+      if (microDecisionRequest?.error) {
+        setInboxMessage({ type: 'error', text: microDecisionRequest.error });
+        setCurrentTab('inbox');
+        return;
+      }
+
+      const response = microDecisionRequest
+        ? await axios.post(`${API_BASE_URL}${microDecisionRequest.endpoint}`, microDecisionRequest.payload)
+        : await axios.post(`${API_BASE_URL}/voice/process`, {
+            transcript: cleanText,
+            device_id: 'mobile-v139',
+            current_date: new Date().toISOString(),
+          });
+      const spokenConfirmation = response.data.confirmation_chip || response.data.confirmation_text;
+      if (spokenConfirmation && window.SpeechSynthesisUtterance) {
         try {
-          const utterance = new window.SpeechSynthesisUtterance(response.data.confirmation_text);
+          const utterance = new window.SpeechSynthesisUtterance(spokenConfirmation);
           window.speechSynthesis.speak(utterance);
         } catch (_) {
         }
       }
-      executeVoiceAction(response.data);
-      fetchAllData();
+      if (microDecisionRequest) {
+        setVoiceMicroDecisionChip((previous) => reduceVoiceMicroDecisionChip(previous, response.data, microDecisionRequest.card));
+        setInboxMessage({
+          type: response.data.action_status === 'clarification_needed' ? 'success' : 'success',
+          text: response.data.confirmation_chip || 'Voice triage action applied.',
+        });
+      } else {
+        executeVoiceAction(response.data);
+      }
+      await fetchAllData();
     } catch (error) {
-      alert('AI Error');
+      const message = error?.response?.data?.error || 'AI Error';
+      if (currentTab === 'inbox' || currentTab === 'quarter') {
+        setInboxMessage({ type: 'error', text: message });
+      } else {
+        alert(message);
+      }
     } finally {
       setVoiceStatus('idle');
       setIsListening(false);
@@ -2894,7 +2949,11 @@
             const saving = inboxBusyId === card.id;
             const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
             return (
-              <section key={card.id} className="inbox-action-card">
+              <section
+                key={card.id}
+                className={`inbox-action-card ${activeVoiceContextId === card.id ? 'voice-target' : ''}`}
+                onClick={() => setActiveVoiceContextId(card.id)}
+              >
                 <div className="inbox-card-top">
                   <div>
                     <strong>{card.merchant}</strong>
@@ -2914,6 +2973,9 @@
                       {label}
                     </span>
                   ))}
+                  {activeVoiceContextId === card.id && (
+                    <span className="voice-context-pill active">Voice target</span>
+                  )}
                   {card.suggested_category?.category_name && (
                     <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
                       <CheckCircle2 size={14} />
@@ -3357,6 +3419,9 @@
   };
 
   const openReadinessWorkflow = (issue) => {
+    if (issue?.affected_entity_id) {
+      setActiveVoiceContextId(String(issue.affected_entity_id));
+    }
     if (issue?.resolution_target?.workflow === '/inbox/finish-now') {
       setCurrentTab('inbox');
       return;
@@ -3364,6 +3429,76 @@
     setCurrentTab('inbox');
   };
 
+  const renderVoiceContextBanner = () => {
+    if (!(currentTab === 'inbox' || currentTab === 'quarter')) {
+      return null;
+    }
+
+    return (
+      <section className="voice-context-banner">
+        <div>
+          <strong>Voice triage</strong>
+          <div className="small-muted">
+            Use the mic against one selected finish-now item only. Supported commands here are category, business, personal, split n%, and attach receipt.
+          </div>
+        </div>
+        <div className="voice-context-row">
+          <span className={`voice-context-pill ${activeVoiceCard ? 'active' : ''}`}>
+            {activeVoiceCard
+              ? `Target: ${activeVoiceCard.merchant} (${activeVoiceCard.id})`
+              : 'Select a blocker card to arm voice triage'}
+          </span>
+          {activeVoiceCard && (
+            <button className="secondary-action" type="button" onClick={() => setCurrentTab('inbox')}>
+              Edit target
+            </button>
+          )}
+        </div>
+        {voiceMicroDecisionChip && (
+          <div className="voice-micro-chip">
+            <div>
+              <strong>{voiceMicroDecisionChip.confirmationChip}</strong>
+              <div className="small-muted">
+                {voiceMicroDecisionChip.targetLabel} ({voiceMicroDecisionChip.targetId})
+              </div>
+            </div>
+            {voiceMicroDecisionChip.labels.length > 0 && (
+              <div className="voice-chip-labels">
+                {voiceMicroDecisionChip.labels.map((label) => (
+                  <span key={`${voiceMicroDecisionChip.targetId}-${label}`} className="voice-action-pill">
+                    {label}
+                  </span>
+                ))}
+              </div>
+            )}
+            <div className="voice-chip-actions">
+              <button
+                className="secondary-action"
+                type="button"
+                onClick={() => {
+                  setCurrentTab('inbox');
+                  setActiveVoiceContextId(voiceMicroDecisionChip.targetId);
+                }}
+              >
+                Edit
+              </button>
+              {voiceMicroDecisionChip.undoSupported && (
+                <button
+                  className="secondary-action"
+                  type="button"
+                  onClick={undoLastInboxAction}
+                  disabled={inboxBusyId === 'undo-last'}
+                >
+                  Undo
+                </button>
+              )}
+            </div>
+          </div>
+        )}
+      </section>
+    );
+  };
+
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
@@ -3654,6 +3789,13 @@
                     </div>
                   </div>
                   <div className="finish-now-actions">
+                    <button
+                      className="secondary-action"
+                      type="button"
+                      onClick={() => setActiveVoiceContextId(String(issue.affected_entity_id))}
+                    >
+                      {activeVoiceContextId === String(issue.affected_entity_id) ? 'Voice target set' : 'Arm voice'}
+                    </button>
                     {(readinessDemoEnabled || snapshotDemoEnabled) ? (
                       <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
                         Mark fixed
@@ -4391,6 +4533,7 @@
         </button>
       </section>
       {connectionError && <div className="alert alert-danger">{connectionError}</div>}
+      {renderVoiceContextBanner()}
 
       {loading ? (
         <div className="text-center py-5">Loading...</div>
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..6eb469fcc7f023f8545d9e952a5823fb8c47a88d
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});

file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..7564fe2a0b6132d2209937816e3f620cb3d91820
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,7 +10,34 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+
+const MICRO_DECISION_CONTEXT_SCOPES = new Set(['inbox', 'finish_now']);
 
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
+
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
  * Matches transcripts to core intents and extracts slots using regex.
@@ -267,71 +294,167 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_scope = null,
+  } = req.body || {};
   const text = String(transcript).toLowerCase().trim();
+  const normalizedContextScope = context_scope === null || context_scope === undefined
+    ? null
+    : String(context_scope).toLowerCase().replace(/-/g, '_');
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
 
+  const requireTransactionContext = () => {
+    if (!bank_txn_id) {
+      return 'bank_txn_id is required for inbox-bound micro-decision commands.';
+    }
+    if (!MICRO_DECISION_CONTEXT_SCOPES.has(normalizedContextScope)) {
+      return 'context_scope must be inbox or finish_now for transaction micro-decisions.';
+    }
+    return null;
+  };
+
   try {
     if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const categoryName = text.replace('category:', '').trim();
       const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: categoryCode,
+            category_name: categoryName,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${categoryName}`,
+        appliedActions: [{ type: 'category', label: categoryName }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${bp}`,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     const splitMatch = text.match(/split\s+(\d{1,3})%?/);
     if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const pct = Number(splitMatch[1]);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Split ${pct}%`,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: 'Attach receipt requested',
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        extra: {
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
     if (['match first', 'match second', 'match third'].includes(text)) {
@@ -364,7 +487,16 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Match ${index}`,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     if (text === 'no match') {
@@ -376,7 +508,16 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: 'Applied: No match',
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index 9b145fadac9123e3fa01b12af6ef2b9916dc11de..7b3e1ac0327da0b509b3ec7c375f154ab385a3cf
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -50,6 +50,11 @@
   buildQuarterUiState,
   deriveResolvedIssueIdsFromSearchParams,
 } from './quarterReadiness';
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -427,6 +432,13 @@
   .date-group-count { border-radius: 999px; padding: 4px 9px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.72rem; color: var(--text-muted); }
   .date-group-body { padding: 0 12px 12px; }
   .voice-preview { background: rgba(0,0,0,0.82); color: white; padding: 8px 15px; border-radius: 15px; margin-bottom: 8px; font-size: 0.72rem; max-width: 240px; text-align: right; }
+  .voice-context-banner { display: grid; gap: 10px; padding: 14px 16px; margin-bottom: 16px; border-radius: 18px; background: var(--bg-card); border: 1px solid var(--border); box-shadow: var(--shadow); }
+  .voice-context-row, .voice-chip-actions, .voice-chip-labels { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
+  .voice-context-pill, .voice-action-pill { border-radius: 999px; padding: 6px 10px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.74rem; font-weight: 700; }
+  .voice-context-pill.active { background: rgba(13,92,99,0.12); color: var(--primary-strong); border-color: rgba(13,92,99,0.24); }
+  .voice-micro-chip { display: grid; gap: 10px; padding: 14px 16px; border-radius: 18px; background: rgba(13,92,99,0.08); border: 1px solid rgba(13,92,99,0.18); }
+  .voice-micro-chip strong { display: block; }
+  .inbox-action-card.voice-target { border-color: rgba(13,92,99,0.36); box-shadow: 0 0 0 2px rgba(13,92,99,0.12), var(--shadow); }
   .spinner { animation: rotate 2s linear infinite; }
   .capture-grid { display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(320px, 0.95fr); gap: 18px; align-items: start; }
   .command-centre { display: grid; gap: 18px; }
@@ -1382,6 +1394,8 @@
   const [inboxDrafts, setInboxDrafts] = useState({});
   const [inboxBusyId, setInboxBusyId] = useState('');
   const [inboxMessage, setInboxMessage] = useState(null);
+  const [activeVoiceContextId, setActiveVoiceContextId] = useState('');
+  const [voiceMicroDecisionChip, setVoiceMicroDecisionChip] = useState(null);
   const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
@@ -1606,10 +1620,20 @@
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
   const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const activeVoiceCard = findVoiceContextCard(inboxCards, activeVoiceContextId);
   const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
   const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
   const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
+  useEffect(() => {
+    if (!activeVoiceContextId) {
+      return;
+    }
+    if (!findVoiceContextCard(inboxCards, activeVoiceContextId)) {
+      setActiveVoiceContextId('');
+    }
+  }, [activeVoiceContextId, inboxCards]);
+
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
   const canManageGovernance = hasPermission(currentUser, PERMISSIONS.GOVERNANCE_MANAGE);
@@ -1806,11 +1830,13 @@
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxDrafts({});
+        setVoiceMicroDecisionChip(null);
         setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
         return;
       }
       await axios.post(`${API_BASE_URL}/inbox/undo-last`);
       await fetchAllData();
+      setVoiceMicroDecisionChip(null);
       setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
     } catch (error) {
       setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
@@ -1991,22 +2017,51 @@
     const cleanText = text.replace(/┬ú/g, 'GBP ');
     try {
       setVoiceStatus('processing');
-      const response = await axios.post(`${API_BASE_URL}/voice/process`, {
+      const microDecisionRequest = buildVoiceMicroDecisionRequest({
+        currentTab,
         transcript: cleanText,
-        device_id: 'mobile-v139',
-        current_date: new Date().toISOString(),
+        activeVoiceContextId,
+        inboxCards,
       });
-      if (response.data.confirmation_text && window.SpeechSynthesisUtterance) {
+
+      if (microDecisionRequest?.error) {
+        setInboxMessage({ type: 'error', text: microDecisionRequest.error });
+        setCurrentTab('inbox');
+        return;
+      }
+
+      const response = microDecisionRequest
+        ? await axios.post(`${API_BASE_URL}${microDecisionRequest.endpoint}`, microDecisionRequest.payload)
+        : await axios.post(`${API_BASE_URL}/voice/process`, {
+            transcript: cleanText,
+            device_id: 'mobile-v139',
+            current_date: new Date().toISOString(),
+          });
+      const spokenConfirmation = response.data.confirmation_chip || response.data.confirmation_text;
+      if (spokenConfirmation && window.SpeechSynthesisUtterance) {
         try {
-          const utterance = new window.SpeechSynthesisUtterance(response.data.confirmation_text);
+          const utterance = new window.SpeechSynthesisUtterance(spokenConfirmation);
           window.speechSynthesis.speak(utterance);
         } catch (_) {
         }
       }
-      executeVoiceAction(response.data);
-      fetchAllData();
+      if (microDecisionRequest) {
+        setVoiceMicroDecisionChip((previous) => reduceVoiceMicroDecisionChip(previous, response.data, microDecisionRequest.card));
+        setInboxMessage({
+          type: response.data.action_status === 'clarification_needed' ? 'success' : 'success',
+          text: response.data.confirmation_chip || 'Voice triage action applied.',
+        });
+      } else {
+        executeVoiceAction(response.data);
+      }
+      await fetchAllData();
     } catch (error) {
-      alert('AI Error');
+      const message = error?.response?.data?.error || 'AI Error';
+      if (currentTab === 'inbox' || currentTab === 'quarter') {
+        setInboxMessage({ type: 'error', text: message });
+      } else {
+        alert(message);
+      }
     } finally {
       setVoiceStatus('idle');
       setIsListening(false);
@@ -2894,7 +2949,11 @@
             const saving = inboxBusyId === card.id;
             const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
             return (
-              <section key={card.id} className="inbox-action-card">
+              <section
+                key={card.id}
+                className={`inbox-action-card ${activeVoiceContextId === card.id ? 'voice-target' : ''}`}
+                onClick={() => setActiveVoiceContextId(card.id)}
+              >
                 <div className="inbox-card-top">
                   <div>
                     <strong>{card.merchant}</strong>
@@ -2914,6 +2973,9 @@
                       {label}
                     </span>
                   ))}
+                  {activeVoiceContextId === card.id && (
+                    <span className="voice-context-pill active">Voice target</span>
+                  )}
                   {card.suggested_category?.category_name && (
                     <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
                       <CheckCircle2 size={14} />
@@ -3357,6 +3419,9 @@
   };
 
   const openReadinessWorkflow = (issue) => {
+    if (issue?.affected_entity_id) {
+      setActiveVoiceContextId(String(issue.affected_entity_id));
+    }
     if (issue?.resolution_target?.workflow === '/inbox/finish-now') {
       setCurrentTab('inbox');
       return;
@@ -3364,6 +3429,76 @@
     setCurrentTab('inbox');
   };
 
+  const renderVoiceContextBanner = () => {
+    if (!(currentTab === 'inbox' || currentTab === 'quarter')) {
+      return null;
+    }
+
+    return (
+      <section className="voice-context-banner">
+        <div>
+          <strong>Voice triage</strong>
+          <div className="small-muted">
+            Use the mic against one selected finish-now item only. Supported commands here are category, business, personal, split n%, and attach receipt.
+          </div>
+        </div>
+        <div className="voice-context-row">
+          <span className={`voice-context-pill ${activeVoiceCard ? 'active' : ''}`}>
+            {activeVoiceCard
+              ? `Target: ${activeVoiceCard.merchant} (${activeVoiceCard.id})`
+              : 'Select a blocker card to arm voice triage'}
+          </span>
+          {activeVoiceCard && (
+            <button className="secondary-action" type="button" onClick={() => setCurrentTab('inbox')}>
+              Edit target
+            </button>
+          )}
+        </div>
+        {voiceMicroDecisionChip && (
+          <div className="voice-micro-chip">
+            <div>
+              <strong>{voiceMicroDecisionChip.confirmationChip}</strong>
+              <div className="small-muted">
+                {voiceMicroDecisionChip.targetLabel} ({voiceMicroDecisionChip.targetId})
+              </div>
+            </div>
+            {voiceMicroDecisionChip.labels.length > 0 && (
+              <div className="voice-chip-labels">
+                {voiceMicroDecisionChip.labels.map((label) => (
+                  <span key={`${voiceMicroDecisionChip.targetId}-${label}`} className="voice-action-pill">
+                    {label}
+                  </span>
+                ))}
+              </div>
+            )}
+            <div className="voice-chip-actions">
+              <button
+                className="secondary-action"
+                type="button"
+                onClick={() => {
+                  setCurrentTab('inbox');
+                  setActiveVoiceContextId(voiceMicroDecisionChip.targetId);
+                }}
+              >
+                Edit
+              </button>
+              {voiceMicroDecisionChip.undoSupported && (
+                <button
+                  className="secondary-action"
+                  type="button"
+                  onClick={undoLastInboxAction}
+                  disabled={inboxBusyId === 'undo-last'}
+                >
+                  Undo
+                </button>
+              )}
+            </div>
+          </div>
+        )}
+      </section>
+    );
+  };
+
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
@@ -3654,6 +3789,13 @@
                     </div>
                   </div>
                   <div className="finish-now-actions">
+                    <button
+                      className="secondary-action"
+                      type="button"
+                      onClick={() => setActiveVoiceContextId(String(issue.affected_entity_id))}
+                    >
+                      {activeVoiceContextId === String(issue.affected_entity_id) ? 'Voice target set' : 'Arm voice'}
+                    </button>
                     {(readinessDemoEnabled || snapshotDemoEnabled) ? (
                       <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
                         Mark fixed
@@ -4391,6 +4533,7 @@
         </button>
       </section>
       {connectionError && <div className="alert alert-danger">{connectionError}</div>}
+      {renderVoiceContextBanner()}
 
       {loading ? (
         <div className="text-center py-5">Loading...</div>
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..6eb469fcc7f023f8545d9e952a5823fb8c47a88d
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});

file update
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
@@ -2050,3 +2050,3 @@
         setInboxMessage({
-          type: response.data.action_status === 'clarification_needed' ? 'success' : 'success',
+          type: 'success',
           text: response.data.confirmation_chip || 'Voice triage action applied.',
apply_patch(auto_approved=true) exited 0 in 2.31s:
Success. Updated the following files:
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..7564fe2a0b6132d2209937816e3f620cb3d91820
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,7 +10,34 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+
+const MICRO_DECISION_CONTEXT_SCOPES = new Set(['inbox', 'finish_now']);
 
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
+
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
  * Matches transcripts to core intents and extracts slots using regex.
@@ -267,71 +294,167 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_scope = null,
+  } = req.body || {};
   const text = String(transcript).toLowerCase().trim();
+  const normalizedContextScope = context_scope === null || context_scope === undefined
+    ? null
+    : String(context_scope).toLowerCase().replace(/-/g, '_');
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
 
+  const requireTransactionContext = () => {
+    if (!bank_txn_id) {
+      return 'bank_txn_id is required for inbox-bound micro-decision commands.';
+    }
+    if (!MICRO_DECISION_CONTEXT_SCOPES.has(normalizedContextScope)) {
+      return 'context_scope must be inbox or finish_now for transaction micro-decisions.';
+    }
+    return null;
+  };
+
   try {
     if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const categoryName = text.replace('category:', '').trim();
       const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: categoryCode,
+            category_name: categoryName,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${categoryName}`,
+        appliedActions: [{ type: 'category', label: categoryName }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${bp}`,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     const splitMatch = text.match(/split\s+(\d{1,3})%?/);
     if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const pct = Number(splitMatch[1]);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Split ${pct}%`,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: 'Attach receipt requested',
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        extra: {
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
     if (['match first', 'match second', 'match third'].includes(text)) {
@@ -364,7 +487,16 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Match ${index}`,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     if (text === 'no match') {
@@ -376,7 +508,16 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: 'Applied: No match',
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index 9b145fadac9123e3fa01b12af6ef2b9916dc11de..b21180e549e09be4a5ed0412e53087d2a52f4382
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -50,6 +50,11 @@
   buildQuarterUiState,
   deriveResolvedIssueIdsFromSearchParams,
 } from './quarterReadiness';
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -427,6 +432,13 @@
   .date-group-count { border-radius: 999px; padding: 4px 9px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.72rem; color: var(--text-muted); }
   .date-group-body { padding: 0 12px 12px; }
   .voice-preview { background: rgba(0,0,0,0.82); color: white; padding: 8px 15px; border-radius: 15px; margin-bottom: 8px; font-size: 0.72rem; max-width: 240px; text-align: right; }
+  .voice-context-banner { display: grid; gap: 10px; padding: 14px 16px; margin-bottom: 16px; border-radius: 18px; background: var(--bg-card); border: 1px solid var(--border); box-shadow: var(--shadow); }
+  .voice-context-row, .voice-chip-actions, .voice-chip-labels { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
+  .voice-context-pill, .voice-action-pill { border-radius: 999px; padding: 6px 10px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.74rem; font-weight: 700; }
+  .voice-context-pill.active { background: rgba(13,92,99,0.12); color: var(--primary-strong); border-color: rgba(13,92,99,0.24); }
+  .voice-micro-chip { display: grid; gap: 10px; padding: 14px 16px; border-radius: 18px; background: rgba(13,92,99,0.08); border: 1px solid rgba(13,92,99,0.18); }
+  .voice-micro-chip strong { display: block; }
+  .inbox-action-card.voice-target { border-color: rgba(13,92,99,0.36); box-shadow: 0 0 0 2px rgba(13,92,99,0.12), var(--shadow); }
   .spinner { animation: rotate 2s linear infinite; }
   .capture-grid { display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(320px, 0.95fr); gap: 18px; align-items: start; }
   .command-centre { display: grid; gap: 18px; }
@@ -1382,6 +1394,8 @@
   const [inboxDrafts, setInboxDrafts] = useState({});
   const [inboxBusyId, setInboxBusyId] = useState('');
   const [inboxMessage, setInboxMessage] = useState(null);
+  const [activeVoiceContextId, setActiveVoiceContextId] = useState('');
+  const [voiceMicroDecisionChip, setVoiceMicroDecisionChip] = useState(null);
   const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
@@ -1606,10 +1620,20 @@
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
   const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const activeVoiceCard = findVoiceContextCard(inboxCards, activeVoiceContextId);
   const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
   const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
   const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
+  useEffect(() => {
+    if (!activeVoiceContextId) {
+      return;
+    }
+    if (!findVoiceContextCard(inboxCards, activeVoiceContextId)) {
+      setActiveVoiceContextId('');
+    }
+  }, [activeVoiceContextId, inboxCards]);
+
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
   const canManageGovernance = hasPermission(currentUser, PERMISSIONS.GOVERNANCE_MANAGE);
@@ -1806,11 +1830,13 @@
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxDrafts({});
+        setVoiceMicroDecisionChip(null);
         setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
         return;
       }
       await axios.post(`${API_BASE_URL}/inbox/undo-last`);
       await fetchAllData();
+      setVoiceMicroDecisionChip(null);
       setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
     } catch (error) {
       setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
@@ -1991,22 +2017,51 @@
     const cleanText = text.replace(/┬ú/g, 'GBP ');
     try {
       setVoiceStatus('processing');
-      const response = await axios.post(`${API_BASE_URL}/voice/process`, {
+      const microDecisionRequest = buildVoiceMicroDecisionRequest({
+        currentTab,
         transcript: cleanText,
-        device_id: 'mobile-v139',
-        current_date: new Date().toISOString(),
+        activeVoiceContextId,
+        inboxCards,
       });
-      if (response.data.confirmation_text && window.SpeechSynthesisUtterance) {
+
+      if (microDecisionRequest?.error) {
+        setInboxMessage({ type: 'error', text: microDecisionRequest.error });
+        setCurrentTab('inbox');
+        return;
+      }
+
+      const response = microDecisionRequest
+        ? await axios.post(`${API_BASE_URL}${microDecisionRequest.endpoint}`, microDecisionRequest.payload)
+        : await axios.post(`${API_BASE_URL}/voice/process`, {
+            transcript: cleanText,
+            device_id: 'mobile-v139',
+            current_date: new Date().toISOString(),
+          });
+      const spokenConfirmation = response.data.confirmation_chip || response.data.confirmation_text;
+      if (spokenConfirmation && window.SpeechSynthesisUtterance) {
         try {
-          const utterance = new window.SpeechSynthesisUtterance(response.data.confirmation_text);
+          const utterance = new window.SpeechSynthesisUtterance(spokenConfirmation);
           window.speechSynthesis.speak(utterance);
         } catch (_) {
         }
       }
-      executeVoiceAction(response.data);
-      fetchAllData();
+      if (microDecisionRequest) {
+        setVoiceMicroDecisionChip((previous) => reduceVoiceMicroDecisionChip(previous, response.data, microDecisionRequest.card));
+        setInboxMessage({
+          type: 'success',
+          text: response.data.confirmation_chip || 'Voice triage action applied.',
+        });
+      } else {
+        executeVoiceAction(response.data);
+      }
+      await fetchAllData();
     } catch (error) {
-      alert('AI Error');
+      const message = error?.response?.data?.error || 'AI Error';
+      if (currentTab === 'inbox' || currentTab === 'quarter') {
+        setInboxMessage({ type: 'error', text: message });
+      } else {
+        alert(message);
+      }
     } finally {
       setVoiceStatus('idle');
       setIsListening(false);
@@ -2894,7 +2949,11 @@
             const saving = inboxBusyId === card.id;
             const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
             return (
-              <section key={card.id} className="inbox-action-card">
+              <section
+                key={card.id}
+                className={`inbox-action-card ${activeVoiceContextId === card.id ? 'voice-target' : ''}`}
+                onClick={() => setActiveVoiceContextId(card.id)}
+              >
                 <div className="inbox-card-top">
                   <div>
                     <strong>{card.merchant}</strong>
@@ -2914,6 +2973,9 @@
                       {label}
                     </span>
                   ))}
+                  {activeVoiceContextId === card.id && (
+                    <span className="voice-context-pill active">Voice target</span>
+                  )}
                   {card.suggested_category?.category_name && (
                     <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
                       <CheckCircle2 size={14} />
@@ -3357,6 +3419,9 @@
   };
 
   const openReadinessWorkflow = (issue) => {
+    if (issue?.affected_entity_id) {
+      setActiveVoiceContextId(String(issue.affected_entity_id));
+    }
     if (issue?.resolution_target?.workflow === '/inbox/finish-now') {
       setCurrentTab('inbox');
       return;
@@ -3364,6 +3429,76 @@
     setCurrentTab('inbox');
   };
 
+  const renderVoiceContextBanner = () => {
+    if (!(currentTab === 'inbox' || currentTab === 'quarter')) {
+      return null;
+    }
+
+    return (
+      <section className="voice-context-banner">
+        <div>
+          <strong>Voice triage</strong>
+          <div className="small-muted">
+            Use the mic against one selected finish-now item only. Supported commands here are category, business, personal, split n%, and attach receipt.
+          </div>
+        </div>
+        <div className="voice-context-row">
+          <span className={`voice-context-pill ${activeVoiceCard ? 'active' : ''}`}>
+            {activeVoiceCard
+              ? `Target: ${activeVoiceCard.merchant} (${activeVoiceCard.id})`
+              : 'Select a blocker card to arm voice triage'}
+          </span>
+          {activeVoiceCard && (
+            <button className="secondary-action" type="button" onClick={() => setCurrentTab('inbox')}>
+              Edit target
+            </button>
+          )}
+        </div>
+        {voiceMicroDecisionChip && (
+          <div className="voice-micro-chip">
+            <div>
+              <strong>{voiceMicroDecisionChip.confirmationChip}</strong>
+              <div className="small-muted">
+                {voiceMicroDecisionChip.targetLabel} ({voiceMicroDecisionChip.targetId})
+              </div>
+            </div>
+            {voiceMicroDecisionChip.labels.length > 0 && (
+              <div className="voice-chip-labels">
+                {voiceMicroDecisionChip.labels.map((label) => (
+                  <span key={`${voiceMicroDecisionChip.targetId}-${label}`} className="voice-action-pill">
+                    {label}
+                  </span>
+                ))}
+              </div>
+            )}
+            <div className="voice-chip-actions">
+              <button
+                className="secondary-action"
+                type="button"
+                onClick={() => {
+                  setCurrentTab('inbox');
+                  setActiveVoiceContextId(voiceMicroDecisionChip.targetId);
+                }}
+              >
+                Edit
+              </button>
+              {voiceMicroDecisionChip.undoSupported && (
+                <button
+                  className="secondary-action"
+                  type="button"
+                  onClick={undoLastInboxAction}
+                  disabled={inboxBusyId === 'undo-last'}
+                >
+                  Undo
+                </button>
+              )}
+            </div>
+          </div>
+        )}
+      </section>
+    );
+  };
+
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
@@ -3654,6 +3789,13 @@
                     </div>
                   </div>
                   <div className="finish-now-actions">
+                    <button
+                      className="secondary-action"
+                      type="button"
+                      onClick={() => setActiveVoiceContextId(String(issue.affected_entity_id))}
+                    >
+                      {activeVoiceContextId === String(issue.affected_entity_id) ? 'Voice target set' : 'Arm voice'}
+                    </button>
                     {(readinessDemoEnabled || snapshotDemoEnabled) ? (
                       <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
                         Mark fixed
@@ -4391,6 +4533,7 @@
         </button>
       </section>
       {connectionError && <div className="alert alert-danger">{connectionError}</div>}
+      {renderVoiceContextBanner()}
 
       {loading ? (
         <div className="text-center py-5">Loading...</div>
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..6eb469fcc7f023f8545d9e952a5823fb8c47a88d
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});

file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..7564fe2a0b6132d2209937816e3f620cb3d91820
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,7 +10,34 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+
+const MICRO_DECISION_CONTEXT_SCOPES = new Set(['inbox', 'finish_now']);
 
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
+
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
  * Matches transcripts to core intents and extracts slots using regex.
@@ -267,71 +294,167 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_scope = null,
+  } = req.body || {};
   const text = String(transcript).toLowerCase().trim();
+  const normalizedContextScope = context_scope === null || context_scope === undefined
+    ? null
+    : String(context_scope).toLowerCase().replace(/-/g, '_');
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
 
+  const requireTransactionContext = () => {
+    if (!bank_txn_id) {
+      return 'bank_txn_id is required for inbox-bound micro-decision commands.';
+    }
+    if (!MICRO_DECISION_CONTEXT_SCOPES.has(normalizedContextScope)) {
+      return 'context_scope must be inbox or finish_now for transaction micro-decisions.';
+    }
+    return null;
+  };
+
   try {
     if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const categoryName = text.replace('category:', '').trim();
       const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: categoryCode,
+            category_name: categoryName,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${categoryName}`,
+        appliedActions: [{ type: 'category', label: categoryName }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${bp}`,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     const splitMatch = text.match(/split\s+(\d{1,3})%?/);
     if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const pct = Number(splitMatch[1]);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Split ${pct}%`,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: 'Attach receipt requested',
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        extra: {
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
     if (['match first', 'match second', 'match third'].includes(text)) {
@@ -364,7 +487,16 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Match ${index}`,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     if (text === 'no match') {
@@ -376,7 +508,16 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: 'Applied: No match',
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index 9b145fadac9123e3fa01b12af6ef2b9916dc11de..b21180e549e09be4a5ed0412e53087d2a52f4382
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -50,6 +50,11 @@
   buildQuarterUiState,
   deriveResolvedIssueIdsFromSearchParams,
 } from './quarterReadiness';
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -427,6 +432,13 @@
   .date-group-count { border-radius: 999px; padding: 4px 9px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.72rem; color: var(--text-muted); }
   .date-group-body { padding: 0 12px 12px; }
   .voice-preview { background: rgba(0,0,0,0.82); color: white; padding: 8px 15px; border-radius: 15px; margin-bottom: 8px; font-size: 0.72rem; max-width: 240px; text-align: right; }
+  .voice-context-banner { display: grid; gap: 10px; padding: 14px 16px; margin-bottom: 16px; border-radius: 18px; background: var(--bg-card); border: 1px solid var(--border); box-shadow: var(--shadow); }
+  .voice-context-row, .voice-chip-actions, .voice-chip-labels { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
+  .voice-context-pill, .voice-action-pill { border-radius: 999px; padding: 6px 10px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.74rem; font-weight: 700; }
+  .voice-context-pill.active { background: rgba(13,92,99,0.12); color: var(--primary-strong); border-color: rgba(13,92,99,0.24); }
+  .voice-micro-chip { display: grid; gap: 10px; padding: 14px 16px; border-radius: 18px; background: rgba(13,92,99,0.08); border: 1px solid rgba(13,92,99,0.18); }
+  .voice-micro-chip strong { display: block; }
+  .inbox-action-card.voice-target { border-color: rgba(13,92,99,0.36); box-shadow: 0 0 0 2px rgba(13,92,99,0.12), var(--shadow); }
   .spinner { animation: rotate 2s linear infinite; }
   .capture-grid { display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(320px, 0.95fr); gap: 18px; align-items: start; }
   .command-centre { display: grid; gap: 18px; }
@@ -1382,6 +1394,8 @@
   const [inboxDrafts, setInboxDrafts] = useState({});
   const [inboxBusyId, setInboxBusyId] = useState('');
   const [inboxMessage, setInboxMessage] = useState(null);
+  const [activeVoiceContextId, setActiveVoiceContextId] = useState('');
+  const [voiceMicroDecisionChip, setVoiceMicroDecisionChip] = useState(null);
   const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
@@ -1606,10 +1620,20 @@
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
   const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const activeVoiceCard = findVoiceContextCard(inboxCards, activeVoiceContextId);
   const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
   const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
   const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
+  useEffect(() => {
+    if (!activeVoiceContextId) {
+      return;
+    }
+    if (!findVoiceContextCard(inboxCards, activeVoiceContextId)) {
+      setActiveVoiceContextId('');
+    }
+  }, [activeVoiceContextId, inboxCards]);
+
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
   const canManageGovernance = hasPermission(currentUser, PERMISSIONS.GOVERNANCE_MANAGE);
@@ -1806,11 +1830,13 @@
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxDrafts({});
+        setVoiceMicroDecisionChip(null);
         setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
         return;
       }
       await axios.post(`${API_BASE_URL}/inbox/undo-last`);
       await fetchAllData();
+      setVoiceMicroDecisionChip(null);
       setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
     } catch (error) {
       setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
@@ -1991,22 +2017,51 @@
     const cleanText = text.replace(/┬ú/g, 'GBP ');
     try {
       setVoiceStatus('processing');
-      const response = await axios.post(`${API_BASE_URL}/voice/process`, {
+      const microDecisionRequest = buildVoiceMicroDecisionRequest({
+        currentTab,
         transcript: cleanText,
-        device_id: 'mobile-v139',
-        current_date: new Date().toISOString(),
+        activeVoiceContextId,
+        inboxCards,
       });
-      if (response.data.confirmation_text && window.SpeechSynthesisUtterance) {
+
+      if (microDecisionRequest?.error) {
+        setInboxMessage({ type: 'error', text: microDecisionRequest.error });
+        setCurrentTab('inbox');
+        return;
+      }
+
+      const response = microDecisionRequest
+        ? await axios.post(`${API_BASE_URL}${microDecisionRequest.endpoint}`, microDecisionRequest.payload)
+        : await axios.post(`${API_BASE_URL}/voice/process`, {
+            transcript: cleanText,
+            device_id: 'mobile-v139',
+            current_date: new Date().toISOString(),
+          });
+      const spokenConfirmation = response.data.confirmation_chip || response.data.confirmation_text;
+      if (spokenConfirmation && window.SpeechSynthesisUtterance) {
         try {
-          const utterance = new window.SpeechSynthesisUtterance(response.data.confirmation_text);
+          const utterance = new window.SpeechSynthesisUtterance(spokenConfirmation);
           window.speechSynthesis.speak(utterance);
         } catch (_) {
         }
       }
-      executeVoiceAction(response.data);
-      fetchAllData();
+      if (microDecisionRequest) {
+        setVoiceMicroDecisionChip((previous) => reduceVoiceMicroDecisionChip(previous, response.data, microDecisionRequest.card));
+        setInboxMessage({
+          type: 'success',
+          text: response.data.confirmation_chip || 'Voice triage action applied.',
+        });
+      } else {
+        executeVoiceAction(response.data);
+      }
+      await fetchAllData();
     } catch (error) {
-      alert('AI Error');
+      const message = error?.response?.data?.error || 'AI Error';
+      if (currentTab === 'inbox' || currentTab === 'quarter') {
+        setInboxMessage({ type: 'error', text: message });
+      } else {
+        alert(message);
+      }
     } finally {
       setVoiceStatus('idle');
       setIsListening(false);
@@ -2894,7 +2949,11 @@
             const saving = inboxBusyId === card.id;
             const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
             return (
-              <section key={card.id} className="inbox-action-card">
+              <section
+                key={card.id}
+                className={`inbox-action-card ${activeVoiceContextId === card.id ? 'voice-target' : ''}`}
+                onClick={() => setActiveVoiceContextId(card.id)}
+              >
                 <div className="inbox-card-top">
                   <div>
                     <strong>{card.merchant}</strong>
@@ -2914,6 +2973,9 @@
                       {label}
                     </span>
                   ))}
+                  {activeVoiceContextId === card.id && (
+                    <span className="voice-context-pill active">Voice target</span>
+                  )}
                   {card.suggested_category?.category_name && (
                     <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
                       <CheckCircle2 size={14} />
@@ -3357,6 +3419,9 @@
   };
 
   const openReadinessWorkflow = (issue) => {
+    if (issue?.affected_entity_id) {
+      setActiveVoiceContextId(String(issue.affected_entity_id));
+    }
     if (issue?.resolution_target?.workflow === '/inbox/finish-now') {
       setCurrentTab('inbox');
       return;
@@ -3364,6 +3429,76 @@
     setCurrentTab('inbox');
   };
 
+  const renderVoiceContextBanner = () => {
+    if (!(currentTab === 'inbox' || currentTab === 'quarter')) {
+      return null;
+    }
+
+    return (
+      <section className="voice-context-banner">
+        <div>
+          <strong>Voice triage</strong>
+          <div className="small-muted">
+            Use the mic against one selected finish-now item only. Supported commands here are category, business, personal, split n%, and attach receipt.
+          </div>
+        </div>
+        <div className="voice-context-row">
+          <span className={`voice-context-pill ${activeVoiceCard ? 'active' : ''}`}>
+            {activeVoiceCard
+              ? `Target: ${activeVoiceCard.merchant} (${activeVoiceCard.id})`
+              : 'Select a blocker card to arm voice triage'}
+          </span>
+          {activeVoiceCard && (
+            <button className="secondary-action" type="button" onClick={() => setCurrentTab('inbox')}>
+              Edit target
+            </button>
+          )}
+        </div>
+        {voiceMicroDecisionChip && (
+          <div className="voice-micro-chip">
+            <div>
+              <strong>{voiceMicroDecisionChip.confirmationChip}</strong>
+              <div className="small-muted">
+                {voiceMicroDecisionChip.targetLabel} ({voiceMicroDecisionChip.targetId})
+              </div>
+            </div>
+            {voiceMicroDecisionChip.labels.length > 0 && (
+              <div className="voice-chip-labels">
+                {voiceMicroDecisionChip.labels.map((label) => (
+                  <span key={`${voiceMicroDecisionChip.targetId}-${label}`} className="voice-action-pill">
+                    {label}
+                  </span>
+                ))}
+              </div>
+            )}
+            <div className="voice-chip-actions">
+              <button
+                className="secondary-action"
+                type="button"
+                onClick={() => {
+                  setCurrentTab('inbox');
+                  setActiveVoiceContextId(voiceMicroDecisionChip.targetId);
+                }}
+              >
+                Edit
+              </button>
+              {voiceMicroDecisionChip.undoSupported && (
+                <button
+                  className="secondary-action"
+                  type="button"
+                  onClick={undoLastInboxAction}
+                  disabled={inboxBusyId === 'undo-last'}
+                >
+                  Undo
+                </button>
+              )}
+            </div>
+          </div>
+        )}
+      </section>
+    );
+  };
+
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
@@ -3654,6 +3789,13 @@
                     </div>
                   </div>
                   <div className="finish-now-actions">
+                    <button
+                      className="secondary-action"
+                      type="button"
+                      onClick={() => setActiveVoiceContextId(String(issue.affected_entity_id))}
+                    >
+                      {activeVoiceContextId === String(issue.affected_entity_id) ? 'Voice target set' : 'Arm voice'}
+                    </button>
                     {(readinessDemoEnabled || snapshotDemoEnabled) ? (
                       <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
                         Mark fixed
@@ -4391,6 +4533,7 @@
         </button>
       </section>
       {connectionError && <div className="alert alert-danger">{connectionError}</div>}
+      {renderVoiceContextBanner()}
 
       {loading ? (
         <div className="text-center py-5">Loading...</div>
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..6eb469fcc7f023f8545d9e952a5823fb8c47a88d
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});

codex
I’m tightening the verification harness now to prove the new contract instead of just the old happy path. The updated backend tests will assert explicit context is required, voice writes hit the audited classification path, fields are preserved across incremental voice commands, and undo metadata is returned for triage actions.
file update
D C:\Users\edebe\eds\bizPA\backend\verify_voice_micro_decisions.js
const assert = require('assert');
const path = require('path');

class MockDb {
  constructor() {
    this.calls = [];
    this.matchSuggestions = [
      { bank_txn_id: 'txn-1' },
      { bank_txn_id: 'txn-2' },
      { bank_txn_id: 'txn-3' }
    ];
  }

  async query(text, params) {
    const sql = text.replace(/\s+/g, ' ').trim();
    this.calls.push({ sql, params });

    if (sql.includes('FROM bank_transactions bt CROSS JOIN ev')) {
      return { rows: this.matchSuggestions };
    }

    return { rows: [] };
  }
}

function loadController(mockDb) {
  const dbModulePath = path.resolve(__dirname, 'src', 'config', 'db.js');
  delete require.cache[dbModulePath];
  require.cache[dbModulePath] = {
    id: dbModulePath,
    filename: dbModulePath,
    loaded: true,
    exports: mockDb
  };

  const controllerPath = path.resolve(__dirname, 'src', 'controllers', 'voiceController.js');
  delete require.cache[controllerPath];
  return require(controllerPath);
}

function createResponse() {
  return {
    statusCode: 200,
    body: null,
    status(code) {
      this.statusCode = code;
      return this;
    },
    json(payload) {
      this.body = payload;
      return this;
    }
  };
}

async function invoke(controller, body) {
  const res = createResponse();
  await controller.processMicroDecision(
    {
      body,
      user: { id: 'user-1' }
    },
    res
  );
  return res;
}

async function main() {
  const mockDb = new MockDb();
  const controller = loadController(mockDb);

  const category = await invoke(controller, {
    transcript: 'Category: Travel',
    bank_txn_id: 'txn-123'
  });
  assert.strictEqual(category.statusCode, 200);
  assert.strictEqual(category.body.confirmation_chip, 'Applied: travel');
  assert(
    mockDb.calls.some((call) => call.sql.includes('INSERT INTO transaction_classifications') && call.params[2] === 'TRAVEL' && call.params[3] === 'travel'),
    'Expected category command to persist manual classification'
  );

  const business = await invoke(controller, {
    transcript: 'Business',
    bank_txn_id: 'txn-123'
  });
  assert.strictEqual(business.statusCode, 200);
  assert.strictEqual(business.body.confirmation_chip, 'Applied: BUSINESS');

  const split = await invoke(controller, {
    transcript: 'Split 70%',
    bank_txn_id: 'txn-123'
  });
  assert.strictEqual(split.statusCode, 200);
  assert.strictEqual(split.body.confirmation_chip, 'Applied: Split 70%');

  const attachReceipt = await invoke(controller, {
    transcript: 'Attach receipt',
    bank_txn_id: 'txn-123'
  });
  assert.strictEqual(attachReceipt.statusCode, 200);
  assert.strictEqual(attachReceipt.body.action_status, 'clarification_needed');
  assert.strictEqual(attachReceipt.body.confirmation_chip, 'Attach receipt requested');

  const matchSecond = await invoke(controller, {
    transcript: 'Match second',
    evidence_id: 'evidence-1'
  });
  assert.strictEqual(matchSecond.statusCode, 200);
  assert.strictEqual(matchSecond.body.confirmation_chip, 'Applied: Match 2');
  assert(
    mockDb.calls.some((call) => call.sql.includes('INSERT INTO evidence_links') && call.params[1] === 'evidence-1' && call.params[2] === 'txn-2'),
    'Expected match command to persist selected evidence link'
  );

  const noMatch = await invoke(controller, {
    transcript: 'No match',
    evidence_id: 'evidence-1'
  });
  assert.strictEqual(noMatch.statusCode, 200);
  assert.strictEqual(noMatch.body.confirmation_chip, 'Applied: No match');

  const invalidSplit = await invoke(controller, {
    transcript: 'Split 140%',
    bank_txn_id: 'txn-123'
  });
  assert.strictEqual(invalidSplit.statusCode, 400);
  assert.strictEqual(invalidSplit.body.error, 'Split percentage must be 0-100.');

  const unsupported = await invoke(controller, {
    transcript: 'Archive this forever'
  });
  assert.strictEqual(unsupported.statusCode, 400);
  assert.strictEqual(unsupported.body.error, 'Unsupported MVP voice micro-decision command.');

  console.log('voice_micro_decisions_ok');
  console.log('category_business_split_ok=true');
  console.log('attach_receipt_clarification_ok=true');
  console.log('match_selection_ok=true');
  console.log('no_match_ok=true');
  console.log('validation_errors_ok=true');
}

main().catch((error) => {
  console.error('voice_micro_decisions_failed');
  console.error(error);
  process.exit(1);
});
apply_patch(auto_approved=true) exited 0 in 1.18s:
Success. Updated the following files:
D C:\Users\edebe\eds\bizPA\backend\verify_voice_micro_decisions.js
file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..7564fe2a0b6132d2209937816e3f620cb3d91820
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,7 +10,34 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+
+const MICRO_DECISION_CONTEXT_SCOPES = new Set(['inbox', 'finish_now']);
 
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
+
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
  * Matches transcripts to core intents and extracts slots using regex.
@@ -267,71 +294,167 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_scope = null,
+  } = req.body || {};
   const text = String(transcript).toLowerCase().trim();
+  const normalizedContextScope = context_scope === null || context_scope === undefined
+    ? null
+    : String(context_scope).toLowerCase().replace(/-/g, '_');
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
 
+  const requireTransactionContext = () => {
+    if (!bank_txn_id) {
+      return 'bank_txn_id is required for inbox-bound micro-decision commands.';
+    }
+    if (!MICRO_DECISION_CONTEXT_SCOPES.has(normalizedContextScope)) {
+      return 'context_scope must be inbox or finish_now for transaction micro-decisions.';
+    }
+    return null;
+  };
+
   try {
     if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const categoryName = text.replace('category:', '').trim();
       const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: categoryCode,
+            category_name: categoryName,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${categoryName}`,
+        appliedActions: [{ type: 'category', label: categoryName }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${bp}`,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     const splitMatch = text.match(/split\s+(\d{1,3})%?/);
     if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const pct = Number(splitMatch[1]);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Split ${pct}%`,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: 'Attach receipt requested',
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        extra: {
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
     if (['match first', 'match second', 'match third'].includes(text)) {
@@ -364,7 +487,16 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Match ${index}`,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     if (text === 'no match') {
@@ -376,7 +508,16 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: 'Applied: No match',
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/backend/verify_voice_micro_decisions.js b/backend/verify_voice_micro_decisions.js
deleted file mode 100644
index 25d9ab02050aac37a5d38aca5c6f0154de9bb787..0000000000000000000000000000000000000000
--- a/backend/verify_voice_micro_decisions.js
+++ /dev/null
@@ -1,148 +0,0 @@
-const assert = require('assert');
-const path = require('path');
-
-class MockDb {
-  constructor() {
-    this.calls = [];
-    this.matchSuggestions = [
-      { bank_txn_id: 'txn-1' },
-      { bank_txn_id: 'txn-2' },
-      { bank_txn_id: 'txn-3' }
-    ];
-  }
-
-  async query(text, params) {
-    const sql = text.replace(/\s+/g, ' ').trim();
-    this.calls.push({ sql, params });
-
-    if (sql.includes('FROM bank_transactions bt CROSS JOIN ev')) {
-      return { rows: this.matchSuggestions };
-    }
-
-    return { rows: [] };
-  }
-}
-
-function loadController(mockDb) {
-  const dbModulePath = path.resolve(__dirname, 'src', 'config', 'db.js');
-  delete require.cache[dbModulePath];
-  require.cache[dbModulePath] = {
-    id: dbModulePath,
-    filename: dbModulePath,
-    loaded: true,
-    exports: mockDb
-  };
-
-  const controllerPath = path.resolve(__dirname, 'src', 'controllers', 'voiceController.js');
-  delete require.cache[controllerPath];
-  return require(controllerPath);
-}
-
-function createResponse() {
-  return {
-    statusCode: 200,
-    body: null,
-    status(code) {
-      this.statusCode = code;
-      return this;
-    },
-    json(payload) {
-      this.body = payload;
-      return this;
-    }
-  };
-}
-
-async function invoke(controller, body) {
-  const res = createResponse();
-  await controller.processMicroDecision(
-    {
-      body,
-      user: { id: 'user-1' }
-    },
-    res
-  );
-  return res;
-}
-
-async function main() {
-  const mockDb = new MockDb();
-  const controller = loadController(mockDb);
-
-  const category = await invoke(controller, {
-    transcript: 'Category: Travel',
-    bank_txn_id: 'txn-123'
-  });
-  assert.strictEqual(category.statusCode, 200);
-  assert.strictEqual(category.body.confirmation_chip, 'Applied: travel');
-  assert(
-    mockDb.calls.some((call) => call.sql.includes('INSERT INTO transaction_classifications') && call.params[2] === 'TRAVEL' && call.params[3] === 'travel'),
-    'Expected category command to persist manual classification'
-  );
-
-  const business = await invoke(controller, {
-    transcript: 'Business',
-    bank_txn_id: 'txn-123'
-  });
-  assert.strictEqual(business.statusCode, 200);
-  assert.strictEqual(business.body.confirmation_chip, 'Applied: BUSINESS');
-
-  const split = await invoke(controller, {
-    transcript: 'Split 70%',
-    bank_txn_id: 'txn-123'
-  });
-  assert.strictEqual(split.statusCode, 200);
-  assert.strictEqual(split.body.confirmation_chip, 'Applied: Split 70%');
-
-  const attachReceipt = await invoke(controller, {
-    transcript: 'Attach receipt',
-    bank_txn_id: 'txn-123'
-  });
-  assert.strictEqual(attachReceipt.statusCode, 200);
-  assert.strictEqual(attachReceipt.body.action_status, 'clarification_needed');
-  assert.strictEqual(attachReceipt.body.confirmation_chip, 'Attach receipt requested');
-
-  const matchSecond = await invoke(controller, {
-    transcript: 'Match second',
-    evidence_id: 'evidence-1'
-  });
-  assert.strictEqual(matchSecond.statusCode, 200);
-  assert.strictEqual(matchSecond.body.confirmation_chip, 'Applied: Match 2');
-  assert(
-    mockDb.calls.some((call) => call.sql.includes('INSERT INTO evidence_links') && call.params[1] === 'evidence-1' && call.params[2] === 'txn-2'),
-    'Expected match command to persist selected evidence link'
-  );
-
-  const noMatch = await invoke(controller, {
-    transcript: 'No match',
-    evidence_id: 'evidence-1'
-  });
-  assert.strictEqual(noMatch.statusCode, 200);
-  assert.strictEqual(noMatch.body.confirmation_chip, 'Applied: No match');
-
-  const invalidSplit = await invoke(controller, {
-    transcript: 'Split 140%',
-    bank_txn_id: 'txn-123'
-  });
-  assert.strictEqual(invalidSplit.statusCode, 400);
-  assert.strictEqual(invalidSplit.body.error, 'Split percentage must be 0-100.');
-
-  const unsupported = await invoke(controller, {
-    transcript: 'Archive this forever'
-  });
-  assert.strictEqual(unsupported.statusCode, 400);
-  assert.strictEqual(unsupported.body.error, 'Unsupported MVP voice micro-decision command.');
-
-  console.log('voice_micro_decisions_ok');
-  console.log('category_business_split_ok=true');
-  console.log('attach_receipt_clarification_ok=true');
-  console.log('match_selection_ok=true');
-  console.log('no_match_ok=true');
-  console.log('validation_errors_ok=true');
-}
-
-main().catch((error) => {
-  console.error('voice_micro_decisions_failed');
-  console.error(error);
-  process.exit(1);
-});
diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index 9b145fadac9123e3fa01b12af6ef2b9916dc11de..b21180e549e09be4a5ed0412e53087d2a52f4382
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -50,6 +50,11 @@
   buildQuarterUiState,
   deriveResolvedIssueIdsFromSearchParams,
 } from './quarterReadiness';
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -427,6 +432,13 @@
   .date-group-count { border-radius: 999px; padding: 4px 9px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.72rem; color: var(--text-muted); }
   .date-group-body { padding: 0 12px 12px; }
   .voice-preview { background: rgba(0,0,0,0.82); color: white; padding: 8px 15px; border-radius: 15px; margin-bottom: 8px; font-size: 0.72rem; max-width: 240px; text-align: right; }
+  .voice-context-banner { display: grid; gap: 10px; padding: 14px 16px; margin-bottom: 16px; border-radius: 18px; background: var(--bg-card); border: 1px solid var(--border); box-shadow: var(--shadow); }
+  .voice-context-row, .voice-chip-actions, .voice-chip-labels { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
+  .voice-context-pill, .voice-action-pill { border-radius: 999px; padding: 6px 10px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.74rem; font-weight: 700; }
+  .voice-context-pill.active { background: rgba(13,92,99,0.12); color: var(--primary-strong); border-color: rgba(13,92,99,0.24); }
+  .voice-micro-chip { display: grid; gap: 10px; padding: 14px 16px; border-radius: 18px; background: rgba(13,92,99,0.08); border: 1px solid rgba(13,92,99,0.18); }
+  .voice-micro-chip strong { display: block; }
+  .inbox-action-card.voice-target { border-color: rgba(13,92,99,0.36); box-shadow: 0 0 0 2px rgba(13,92,99,0.12), var(--shadow); }
   .spinner { animation: rotate 2s linear infinite; }
   .capture-grid { display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(320px, 0.95fr); gap: 18px; align-items: start; }
   .command-centre { display: grid; gap: 18px; }
@@ -1382,6 +1394,8 @@
   const [inboxDrafts, setInboxDrafts] = useState({});
   const [inboxBusyId, setInboxBusyId] = useState('');
   const [inboxMessage, setInboxMessage] = useState(null);
+  const [activeVoiceContextId, setActiveVoiceContextId] = useState('');
+  const [voiceMicroDecisionChip, setVoiceMicroDecisionChip] = useState(null);
   const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
@@ -1606,10 +1620,20 @@
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
   const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const activeVoiceCard = findVoiceContextCard(inboxCards, activeVoiceContextId);
   const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
   const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
   const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
+  useEffect(() => {
+    if (!activeVoiceContextId) {
+      return;
+    }
+    if (!findVoiceContextCard(inboxCards, activeVoiceContextId)) {
+      setActiveVoiceContextId('');
+    }
+  }, [activeVoiceContextId, inboxCards]);
+
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
   const canManageGovernance = hasPermission(currentUser, PERMISSIONS.GOVERNANCE_MANAGE);
@@ -1806,11 +1830,13 @@
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxDrafts({});
+        setVoiceMicroDecisionChip(null);
         setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
         return;
       }
       await axios.post(`${API_BASE_URL}/inbox/undo-last`);
       await fetchAllData();
+      setVoiceMicroDecisionChip(null);
       setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
     } catch (error) {
       setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
@@ -1991,22 +2017,51 @@
     const cleanText = text.replace(/┬ú/g, 'GBP ');
     try {
       setVoiceStatus('processing');
-      const response = await axios.post(`${API_BASE_URL}/voice/process`, {
+      const microDecisionRequest = buildVoiceMicroDecisionRequest({
+        currentTab,
         transcript: cleanText,
-        device_id: 'mobile-v139',
-        current_date: new Date().toISOString(),
+        activeVoiceContextId,
+        inboxCards,
       });
-      if (response.data.confirmation_text && window.SpeechSynthesisUtterance) {
+
+      if (microDecisionRequest?.error) {
+        setInboxMessage({ type: 'error', text: microDecisionRequest.error });
+        setCurrentTab('inbox');
+        return;
+      }
+
+      const response = microDecisionRequest
+        ? await axios.post(`${API_BASE_URL}${microDecisionRequest.endpoint}`, microDecisionRequest.payload)
+        : await axios.post(`${API_BASE_URL}/voice/process`, {
+            transcript: cleanText,
+            device_id: 'mobile-v139',
+            current_date: new Date().toISOString(),
+          });
+      const spokenConfirmation = response.data.confirmation_chip || response.data.confirmation_text;
+      if (spokenConfirmation && window.SpeechSynthesisUtterance) {
         try {
-          const utterance = new window.SpeechSynthesisUtterance(response.data.confirmation_text);
+          const utterance = new window.SpeechSynthesisUtterance(spokenConfirmation);
           window.speechSynthesis.speak(utterance);
         } catch (_) {
         }
       }
-      executeVoiceAction(response.data);
-      fetchAllData();
+      if (microDecisionRequest) {
+        setVoiceMicroDecisionChip((previous) => reduceVoiceMicroDecisionChip(previous, response.data, microDecisionRequest.card));
+        setInboxMessage({
+          type: 'success',
+          text: response.data.confirmation_chip || 'Voice triage action applied.',
+        });
+      } else {
+        executeVoiceAction(response.data);
+      }
+      await fetchAllData();
     } catch (error) {
-      alert('AI Error');
+      const message = error?.response?.data?.error || 'AI Error';
+      if (currentTab === 'inbox' || currentTab === 'quarter') {
+        setInboxMessage({ type: 'error', text: message });
+      } else {
+        alert(message);
+      }
     } finally {
       setVoiceStatus('idle');
       setIsListening(false);
@@ -2894,7 +2949,11 @@
             const saving = inboxBusyId === card.id;
             const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
             return (
-              <section key={card.id} className="inbox-action-card">
+              <section
+                key={card.id}
+                className={`inbox-action-card ${activeVoiceContextId === card.id ? 'voice-target' : ''}`}
+                onClick={() => setActiveVoiceContextId(card.id)}
+              >
                 <div className="inbox-card-top">
                   <div>
                     <strong>{card.merchant}</strong>
@@ -2914,6 +2973,9 @@
                       {label}
                     </span>
                   ))}
+                  {activeVoiceContextId === card.id && (
+                    <span className="voice-context-pill active">Voice target</span>
+                  )}
                   {card.suggested_category?.category_name && (
                     <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
                       <CheckCircle2 size={14} />
@@ -3357,6 +3419,9 @@
   };
 
   const openReadinessWorkflow = (issue) => {
+    if (issue?.affected_entity_id) {
+      setActiveVoiceContextId(String(issue.affected_entity_id));
+    }
     if (issue?.resolution_target?.workflow === '/inbox/finish-now') {
       setCurrentTab('inbox');
       return;
@@ -3364,6 +3429,76 @@
     setCurrentTab('inbox');
   };
 
+  const renderVoiceContextBanner = () => {
+    if (!(currentTab === 'inbox' || currentTab === 'quarter')) {
+      return null;
+    }
+
+    return (
+      <section className="voice-context-banner">
+        <div>
+          <strong>Voice triage</strong>
+          <div className="small-muted">
+            Use the mic against one selected finish-now item only. Supported commands here are category, business, personal, split n%, and attach receipt.
+          </div>
+        </div>
+        <div className="voice-context-row">
+          <span className={`voice-context-pill ${activeVoiceCard ? 'active' : ''}`}>
+            {activeVoiceCard
+              ? `Target: ${activeVoiceCard.merchant} (${activeVoiceCard.id})`
+              : 'Select a blocker card to arm voice triage'}
+          </span>
+          {activeVoiceCard && (
+            <button className="secondary-action" type="button" onClick={() => setCurrentTab('inbox')}>
+              Edit target
+            </button>
+          )}
+        </div>
+        {voiceMicroDecisionChip && (
+          <div className="voice-micro-chip">
+            <div>
+              <strong>{voiceMicroDecisionChip.confirmationChip}</strong>
+              <div className="small-muted">
+                {voiceMicroDecisionChip.targetLabel} ({voiceMicroDecisionChip.targetId})
+              </div>
+            </div>
+            {voiceMicroDecisionChip.labels.length > 0 && (
+              <div className="voice-chip-labels">
+                {voiceMicroDecisionChip.labels.map((label) => (
+                  <span key={`${voiceMicroDecisionChip.targetId}-${label}`} className="voice-action-pill">
+                    {label}
+                  </span>
+                ))}
+              </div>
+            )}
+            <div className="voice-chip-actions">
+              <button
+                className="secondary-action"
+                type="button"
+                onClick={() => {
+                  setCurrentTab('inbox');
+                  setActiveVoiceContextId(voiceMicroDecisionChip.targetId);
+                }}
+              >
+                Edit
+              </button>
+              {voiceMicroDecisionChip.undoSupported && (
+                <button
+                  className="secondary-action"
+                  type="button"
+                  onClick={undoLastInboxAction}
+                  disabled={inboxBusyId === 'undo-last'}
+                >
+                  Undo
+                </button>
+              )}
+            </div>
+          </div>
+        )}
+      </section>
+    );
+  };
+
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
@@ -3654,6 +3789,13 @@
                     </div>
                   </div>
                   <div className="finish-now-actions">
+                    <button
+                      className="secondary-action"
+                      type="button"
+                      onClick={() => setActiveVoiceContextId(String(issue.affected_entity_id))}
+                    >
+                      {activeVoiceContextId === String(issue.affected_entity_id) ? 'Voice target set' : 'Arm voice'}
+                    </button>
                     {(readinessDemoEnabled || snapshotDemoEnabled) ? (
                       <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
                         Mark fixed
@@ -4391,6 +4533,7 @@
         </button>
       </section>
       {connectionError && <div className="alert alert-danger">{connectionError}</div>}
+      {renderVoiceContextBanner()}
 
       {loading ? (
         <div className="text-center py-5">Loading...</div>
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..6eb469fcc7f023f8545d9e952a5823fb8c47a88d
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});

file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..7564fe2a0b6132d2209937816e3f620cb3d91820
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,7 +10,34 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+
+const MICRO_DECISION_CONTEXT_SCOPES = new Set(['inbox', 'finish_now']);
 
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
+
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
  * Matches transcripts to core intents and extracts slots using regex.
@@ -267,71 +294,167 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_scope = null,
+  } = req.body || {};
   const text = String(transcript).toLowerCase().trim();
+  const normalizedContextScope = context_scope === null || context_scope === undefined
+    ? null
+    : String(context_scope).toLowerCase().replace(/-/g, '_');
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
 
+  const requireTransactionContext = () => {
+    if (!bank_txn_id) {
+      return 'bank_txn_id is required for inbox-bound micro-decision commands.';
+    }
+    if (!MICRO_DECISION_CONTEXT_SCOPES.has(normalizedContextScope)) {
+      return 'context_scope must be inbox or finish_now for transaction micro-decisions.';
+    }
+    return null;
+  };
+
   try {
     if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const categoryName = text.replace('category:', '').trim();
       const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: categoryCode,
+            category_name: categoryName,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${categoryName}`,
+        appliedActions: [{ type: 'category', label: categoryName }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${bp}`,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     const splitMatch = text.match(/split\s+(\d{1,3})%?/);
     if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const pct = Number(splitMatch[1]);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Split ${pct}%`,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: 'Attach receipt requested',
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        extra: {
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
     if (['match first', 'match second', 'match third'].includes(text)) {
@@ -364,7 +487,16 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Match ${index}`,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     if (text === 'no match') {
@@ -376,7 +508,16 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: 'Applied: No match',
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/backend/verify_voice_micro_decisions.js b/backend/verify_voice_micro_decisions.js
deleted file mode 100644
index 25d9ab02050aac37a5d38aca5c6f0154de9bb787..0000000000000000000000000000000000000000
--- a/backend/verify_voice_micro_decisions.js
+++ /dev/null
@@ -1,148 +0,0 @@
-const assert = require('assert');
-const path = require('path');
-
-class MockDb {
-  constructor() {
-    this.calls = [];
-    this.matchSuggestions = [
-      { bank_txn_id: 'txn-1' },
-      { bank_txn_id: 'txn-2' },
-      { bank_txn_id: 'txn-3' }
-    ];
-  }
-
-  async query(text, params) {
-    const sql = text.replace(/\s+/g, ' ').trim();
-    this.calls.push({ sql, params });
-
-    if (sql.includes('FROM bank_transactions bt CROSS JOIN ev')) {
-      return { rows: this.matchSuggestions };
-    }
-
-    return { rows: [] };
-  }
-}
-
-function loadController(mockDb) {
-  const dbModulePath = path.resolve(__dirname, 'src', 'config', 'db.js');
-  delete require.cache[dbModulePath];
-  require.cache[dbModulePath] = {
-    id: dbModulePath,
-    filename: dbModulePath,
-    loaded: true,
-    exports: mockDb
-  };
-
-  const controllerPath = path.resolve(__dirname, 'src', 'controllers', 'voiceController.js');
-  delete require.cache[controllerPath];
-  return require(controllerPath);
-}
-
-function createResponse() {
-  return {
-    statusCode: 200,
-    body: null,
-    status(code) {
-      this.statusCode = code;
-      return this;
-    },
-    json(payload) {
-      this.body = payload;
-      return this;
-    }
-  };
-}
-
-async function invoke(controller, body) {
-  const res = createResponse();
-  await controller.processMicroDecision(
-    {
-      body,
-      user: { id: 'user-1' }
-    },
-    res
-  );
-  return res;
-}
-
-async function main() {
-  const mockDb = new MockDb();
-  const controller = loadController(mockDb);
-
-  const category = await invoke(controller, {
-    transcript: 'Category: Travel',
-    bank_txn_id: 'txn-123'
-  });
-  assert.strictEqual(category.statusCode, 200);
-  assert.strictEqual(category.body.confirmation_chip, 'Applied: travel');
-  assert(
-    mockDb.calls.some((call) => call.sql.includes('INSERT INTO transaction_classifications') && call.params[2] === 'TRAVEL' && call.params[3] === 'travel'),
-    'Expected category command to persist manual classification'
-  );
-
-  const business = await invoke(controller, {
-    transcript: 'Business',
-    bank_txn_id: 'txn-123'
-  });
-  assert.strictEqual(business.statusCode, 200);
-  assert.strictEqual(business.body.confirmation_chip, 'Applied: BUSINESS');
-
-  const split = await invoke(controller, {
-    transcript: 'Split 70%',
-    bank_txn_id: 'txn-123'
-  });
-  assert.strictEqual(split.statusCode, 200);
-  assert.strictEqual(split.body.confirmation_chip, 'Applied: Split 70%');
-
-  const attachReceipt = await invoke(controller, {
-    transcript: 'Attach receipt',
-    bank_txn_id: 'txn-123'
-  });
-  assert.strictEqual(attachReceipt.statusCode, 200);
-  assert.strictEqual(attachReceipt.body.action_status, 'clarification_needed');
-  assert.strictEqual(attachReceipt.body.confirmation_chip, 'Attach receipt requested');
-
-  const matchSecond = await invoke(controller, {
-    transcript: 'Match second',
-    evidence_id: 'evidence-1'
-  });
-  assert.strictEqual(matchSecond.statusCode, 200);
-  assert.strictEqual(matchSecond.body.confirmation_chip, 'Applied: Match 2');
-  assert(
-    mockDb.calls.some((call) => call.sql.includes('INSERT INTO evidence_links') && call.params[1] === 'evidence-1' && call.params[2] === 'txn-2'),
-    'Expected match command to persist selected evidence link'
-  );
-
-  const noMatch = await invoke(controller, {
-    transcript: 'No match',
-    evidence_id: 'evidence-1'
-  });
-  assert.strictEqual(noMatch.statusCode, 200);
-  assert.strictEqual(noMatch.body.confirmation_chip, 'Applied: No match');
-
-  const invalidSplit = await invoke(controller, {
-    transcript: 'Split 140%',
-    bank_txn_id: 'txn-123'
-  });
-  assert.strictEqual(invalidSplit.statusCode, 400);
-  assert.strictEqual(invalidSplit.body.error, 'Split percentage must be 0-100.');
-
-  const unsupported = await invoke(controller, {
-    transcript: 'Archive this forever'
-  });
-  assert.strictEqual(unsupported.statusCode, 400);
-  assert.strictEqual(unsupported.body.error, 'Unsupported MVP voice micro-decision command.');
-
-  console.log('voice_micro_decisions_ok');
-  console.log('category_business_split_ok=true');
-  console.log('attach_receipt_clarification_ok=true');
-  console.log('match_selection_ok=true');
-  console.log('no_match_ok=true');
-  console.log('validation_errors_ok=true');
-}
-
-main().catch((error) => {
-  console.error('voice_micro_decisions_failed');
-  console.error(error);
-  process.exit(1);
-});
diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index 9b145fadac9123e3fa01b12af6ef2b9916dc11de..b21180e549e09be4a5ed0412e53087d2a52f4382
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -50,6 +50,11 @@
   buildQuarterUiState,
   deriveResolvedIssueIdsFromSearchParams,
 } from './quarterReadiness';
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -427,6 +432,13 @@
   .date-group-count { border-radius: 999px; padding: 4px 9px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.72rem; color: var(--text-muted); }
   .date-group-body { padding: 0 12px 12px; }
   .voice-preview { background: rgba(0,0,0,0.82); color: white; padding: 8px 15px; border-radius: 15px; margin-bottom: 8px; font-size: 0.72rem; max-width: 240px; text-align: right; }
+  .voice-context-banner { display: grid; gap: 10px; padding: 14px 16px; margin-bottom: 16px; border-radius: 18px; background: var(--bg-card); border: 1px solid var(--border); box-shadow: var(--shadow); }
+  .voice-context-row, .voice-chip-actions, .voice-chip-labels { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
+  .voice-context-pill, .voice-action-pill { border-radius: 999px; padding: 6px 10px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.74rem; font-weight: 700; }
+  .voice-context-pill.active { background: rgba(13,92,99,0.12); color: var(--primary-strong); border-color: rgba(13,92,99,0.24); }
+  .voice-micro-chip { display: grid; gap: 10px; padding: 14px 16px; border-radius: 18px; background: rgba(13,92,99,0.08); border: 1px solid rgba(13,92,99,0.18); }
+  .voice-micro-chip strong { display: block; }
+  .inbox-action-card.voice-target { border-color: rgba(13,92,99,0.36); box-shadow: 0 0 0 2px rgba(13,92,99,0.12), var(--shadow); }
   .spinner { animation: rotate 2s linear infinite; }
   .capture-grid { display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(320px, 0.95fr); gap: 18px; align-items: start; }
   .command-centre { display: grid; gap: 18px; }
@@ -1382,6 +1394,8 @@
   const [inboxDrafts, setInboxDrafts] = useState({});
   const [inboxBusyId, setInboxBusyId] = useState('');
   const [inboxMessage, setInboxMessage] = useState(null);
+  const [activeVoiceContextId, setActiveVoiceContextId] = useState('');
+  const [voiceMicroDecisionChip, setVoiceMicroDecisionChip] = useState(null);
   const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
@@ -1606,10 +1620,20 @@
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
   const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const activeVoiceCard = findVoiceContextCard(inboxCards, activeVoiceContextId);
   const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
   const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
   const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
+  useEffect(() => {
+    if (!activeVoiceContextId) {
+      return;
+    }
+    if (!findVoiceContextCard(inboxCards, activeVoiceContextId)) {
+      setActiveVoiceContextId('');
+    }
+  }, [activeVoiceContextId, inboxCards]);
+
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
   const canManageGovernance = hasPermission(currentUser, PERMISSIONS.GOVERNANCE_MANAGE);
@@ -1806,11 +1830,13 @@
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxDrafts({});
+        setVoiceMicroDecisionChip(null);
         setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
         return;
       }
       await axios.post(`${API_BASE_URL}/inbox/undo-last`);
       await fetchAllData();
+      setVoiceMicroDecisionChip(null);
       setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
     } catch (error) {
       setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
@@ -1991,22 +2017,51 @@
     const cleanText = text.replace(/┬ú/g, 'GBP ');
     try {
       setVoiceStatus('processing');
-      const response = await axios.post(`${API_BASE_URL}/voice/process`, {
+      const microDecisionRequest = buildVoiceMicroDecisionRequest({
+        currentTab,
         transcript: cleanText,
-        device_id: 'mobile-v139',
-        current_date: new Date().toISOString(),
+        activeVoiceContextId,
+        inboxCards,
       });
-      if (response.data.confirmation_text && window.SpeechSynthesisUtterance) {
+
+      if (microDecisionRequest?.error) {
+        setInboxMessage({ type: 'error', text: microDecisionRequest.error });
+        setCurrentTab('inbox');
+        return;
+      }
+
+      const response = microDecisionRequest
+        ? await axios.post(`${API_BASE_URL}${microDecisionRequest.endpoint}`, microDecisionRequest.payload)
+        : await axios.post(`${API_BASE_URL}/voice/process`, {
+            transcript: cleanText,
+            device_id: 'mobile-v139',
+            current_date: new Date().toISOString(),
+          });
+      const spokenConfirmation = response.data.confirmation_chip || response.data.confirmation_text;
+      if (spokenConfirmation && window.SpeechSynthesisUtterance) {
         try {
-          const utterance = new window.SpeechSynthesisUtterance(response.data.confirmation_text);
+          const utterance = new window.SpeechSynthesisUtterance(spokenConfirmation);
           window.speechSynthesis.speak(utterance);
         } catch (_) {
         }
       }
-      executeVoiceAction(response.data);
-      fetchAllData();
+      if (microDecisionRequest) {
+        setVoiceMicroDecisionChip((previous) => reduceVoiceMicroDecisionChip(previous, response.data, microDecisionRequest.card));
+        setInboxMessage({
+          type: 'success',
+          text: response.data.confirmation_chip || 'Voice triage action applied.',
+        });
+      } else {
+        executeVoiceAction(response.data);
+      }
+      await fetchAllData();
     } catch (error) {
-      alert('AI Error');
+      const message = error?.response?.data?.error || 'AI Error';
+      if (currentTab === 'inbox' || currentTab === 'quarter') {
+        setInboxMessage({ type: 'error', text: message });
+      } else {
+        alert(message);
+      }
     } finally {
       setVoiceStatus('idle');
       setIsListening(false);
@@ -2894,7 +2949,11 @@
             const saving = inboxBusyId === card.id;
             const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
             return (
-              <section key={card.id} className="inbox-action-card">
+              <section
+                key={card.id}
+                className={`inbox-action-card ${activeVoiceContextId === card.id ? 'voice-target' : ''}`}
+                onClick={() => setActiveVoiceContextId(card.id)}
+              >
                 <div className="inbox-card-top">
                   <div>
                     <strong>{card.merchant}</strong>
@@ -2914,6 +2973,9 @@
                       {label}
                     </span>
                   ))}
+                  {activeVoiceContextId === card.id && (
+                    <span className="voice-context-pill active">Voice target</span>
+                  )}
                   {card.suggested_category?.category_name && (
                     <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
                       <CheckCircle2 size={14} />
@@ -3357,6 +3419,9 @@
   };
 
   const openReadinessWorkflow = (issue) => {
+    if (issue?.affected_entity_id) {
+      setActiveVoiceContextId(String(issue.affected_entity_id));
+    }
     if (issue?.resolution_target?.workflow === '/inbox/finish-now') {
       setCurrentTab('inbox');
       return;
@@ -3364,6 +3429,76 @@
     setCurrentTab('inbox');
   };
 
+  const renderVoiceContextBanner = () => {
+    if (!(currentTab === 'inbox' || currentTab === 'quarter')) {
+      return null;
+    }
+
+    return (
+      <section className="voice-context-banner">
+        <div>
+          <strong>Voice triage</strong>
+          <div className="small-muted">
+            Use the mic against one selected finish-now item only. Supported commands here are category, business, personal, split n%, and attach receipt.
+          </div>
+        </div>
+        <div className="voice-context-row">
+          <span className={`voice-context-pill ${activeVoiceCard ? 'active' : ''}`}>
+            {activeVoiceCard
+              ? `Target: ${activeVoiceCard.merchant} (${activeVoiceCard.id})`
+              : 'Select a blocker card to arm voice triage'}
+          </span>
+          {activeVoiceCard && (
+            <button className="secondary-action" type="button" onClick={() => setCurrentTab('inbox')}>
+              Edit target
+            </button>
+          )}
+        </div>
+        {voiceMicroDecisionChip && (
+          <div className="voice-micro-chip">
+            <div>
+              <strong>{voiceMicroDecisionChip.confirmationChip}</strong>
+              <div className="small-muted">
+                {voiceMicroDecisionChip.targetLabel} ({voiceMicroDecisionChip.targetId})
+              </div>
+            </div>
+            {voiceMicroDecisionChip.labels.length > 0 && (
+              <div className="voice-chip-labels">
+                {voiceMicroDecisionChip.labels.map((label) => (
+                  <span key={`${voiceMicroDecisionChip.targetId}-${label}`} className="voice-action-pill">
+                    {label}
+                  </span>
+                ))}
+              </div>
+            )}
+            <div className="voice-chip-actions">
+              <button
+                className="secondary-action"
+                type="button"
+                onClick={() => {
+                  setCurrentTab('inbox');
+                  setActiveVoiceContextId(voiceMicroDecisionChip.targetId);
+                }}
+              >
+                Edit
+              </button>
+              {voiceMicroDecisionChip.undoSupported && (
+                <button
+                  className="secondary-action"
+                  type="button"
+                  onClick={undoLastInboxAction}
+                  disabled={inboxBusyId === 'undo-last'}
+                >
+                  Undo
+                </button>
+              )}
+            </div>
+          </div>
+        )}
+      </section>
+    );
+  };
+
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
@@ -3654,6 +3789,13 @@
                     </div>
                   </div>
                   <div className="finish-now-actions">
+                    <button
+                      className="secondary-action"
+                      type="button"
+                      onClick={() => setActiveVoiceContextId(String(issue.affected_entity_id))}
+                    >
+                      {activeVoiceContextId === String(issue.affected_entity_id) ? 'Voice target set' : 'Arm voice'}
+                    </button>
                     {(readinessDemoEnabled || snapshotDemoEnabled) ? (
                       <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
                         Mark fixed
@@ -4391,6 +4533,7 @@
         </button>
       </section>
       {connectionError && <div className="alert alert-danger">{connectionError}</div>}
+      {renderVoiceContextBanner()}
 
       {loading ? (
         <div className="text-center py-5">Loading...</div>
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..6eb469fcc7f023f8545d9e952a5823fb8c47a88d
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});

file update
A C:\Users\edebe\eds\bizPA\backend\verify_voice_micro_decisions.js
const assert = require('assert');
const path = require('path');

class MockDb {
  constructor() {
    this.calls = [];
    this.matchSuggestions = [
      { bank_txn_id: 'txn-1' },
      { bank_txn_id: 'txn-2' },
      { bank_txn_id: 'txn-3' }
    ];
    this.classifications = new Map();
    this.auditEntries = [];
  }

  get pool() {
    return {
      connect: async () => ({
        query: this.query.bind(this),
        release() {},
      }),
    };
  }

  async query(text, params = []) {
    const sql = text.replace(/\s+/g, ' ').trim();
    this.calls.push({ sql, params });

    if (sql === 'BEGIN' || sql === 'COMMIT' || sql === 'ROLLBACK') {
      return { rows: [] };
    }

    if (sql.includes('SELECT * FROM transaction_classifications')) {
      const existing = this.classifications.get(params[0]);
      return { rows: existing ? [existing] : [] };
    }

    if (sql.includes('INSERT INTO transaction_classifications')) {
      const next = {
        user_id: params[0],
        bank_txn_id: params[1],
        category_code: params[2],
        category_name: params[3],
        business_personal: params[4],
        is_split: params[5],
        split_business_pct: params[6],
        confidence: params[7],
        source: params[8],
      };
      this.classifications.set(params[1], next);
      return { rows: [next] };
    }

    if (sql.includes('INSERT INTO transaction_audit_log')) {
      this.auditEntries.push({
        bank_txn_id: params[1],
        previous_value: params[4],
        new_value: params[5],
        change_source: params[6],
      });
      return { rows: [] };
    }

    if (sql.includes('SELECT txn_date FROM bank_transactions')) {
      return { rows: [{ txn_date: '2026-03-12' }] };
    }

    if (sql.includes('FROM bank_transactions bt CROSS JOIN ev')) {
      return { rows: this.matchSuggestions };
    }

    if (sql.includes('INSERT INTO evidence_links')) {
      return { rows: [] };
    }

    return { rows: [] };
  }
}

function loadController(mockDb) {
  const dbModulePath = path.resolve(__dirname, 'src', 'config', 'db.js');
  delete require.cache[dbModulePath];
  require.cache[dbModulePath] = {
    id: dbModulePath,
    filename: dbModulePath,
    loaded: true,
    exports: mockDb
  };

  const controllerPath = path.resolve(__dirname, 'src', 'controllers', 'voiceController.js');
  delete require.cache[controllerPath];
  return require(controllerPath);
}

function createResponse() {
  return {
    statusCode: 200,
    body: null,
    status(code) {
      this.statusCode = code;
      return this;
    },
    json(payload) {
      this.body = payload;
      return this;
    }
  };
}

async function invoke(controller, body) {
  const res = createResponse();
  await controller.processMicroDecision(
    {
      body,
      user: { id: 'user-1' }
    },
    res
  );
  return res;
}

async function main() {
  const mockDb = new MockDb();
  const controller = loadController(mockDb);

  const missingContext = await invoke(controller, {
    transcript: 'Business',
    bank_txn_id: 'txn-123'
  });
  assert.strictEqual(missingContext.statusCode, 400);
  assert.strictEqual(missingContext.body.error, 'context_scope must be inbox or finish_now for transaction micro-decisions.');

  const category = await invoke(controller, {
    transcript: 'Category: Travel',
    bank_txn_id: 'txn-123',
    context_scope: 'inbox'
  });
  assert.strictEqual(category.statusCode, 200);
  assert.strictEqual(category.body.confirmation_chip, 'Applied: travel');
  assert.strictEqual(category.body.undo.supported, true);
  assert.deepStrictEqual(category.body.context_binding, {
    scope: 'inbox',
    target_type: 'bank_transaction',
    target_id: 'txn-123'
  });
  assert.strictEqual(mockDb.classifications.get('txn-123').category_code, 'TRAVEL');
  assert.strictEqual(mockDb.auditEntries.at(-1).change_source, 'voice');

  const business = await invoke(controller, {
    transcript: 'Business',
    bank_txn_id: 'txn-123',
    context_scope: 'finish-now'
  });
  assert.strictEqual(business.statusCode, 200);
  assert.strictEqual(business.body.confirmation_chip, 'Applied: BUSINESS');
  assert.strictEqual(mockDb.classifications.get('txn-123').category_code, 'TRAVEL');
  assert.strictEqual(mockDb.classifications.get('txn-123').business_personal, 'BUSINESS');
  assert.strictEqual(mockDb.classifications.get('txn-123').is_split, false);

  const split = await invoke(controller, {
    transcript: 'Split 70%',
    bank_txn_id: 'txn-123',
    context_scope: 'finish_now'
  });
  assert.strictEqual(split.statusCode, 200);
  assert.strictEqual(split.body.confirmation_chip, 'Applied: Split 70%');
  assert.strictEqual(split.body.undo.supported, true);
  assert.strictEqual(mockDb.classifications.get('txn-123').business_personal, 'BUSINESS');
  assert.strictEqual(mockDb.classifications.get('txn-123').is_split, true);
  assert.strictEqual(mockDb.classifications.get('txn-123').split_business_pct, 70);

  const attachReceipt = await invoke(controller, {
    transcript: 'Attach receipt',
    bank_txn_id: 'txn-123',
    context_scope: 'inbox'
  });
  assert.strictEqual(attachReceipt.statusCode, 200);
  assert.strictEqual(attachReceipt.body.action_status, 'clarification_needed');
  assert.strictEqual(attachReceipt.body.confirmation_chip, 'Attach receipt requested');
  assert.strictEqual(attachReceipt.body.undo.supported, false);

  const matchSecond = await invoke(controller, {
    transcript: 'Match second',
    evidence_id: 'evidence-1'
  });
  assert.strictEqual(matchSecond.statusCode, 200);
  assert.strictEqual(matchSecond.body.confirmation_chip, 'Applied: Match 2');
  assert.deepStrictEqual(matchSecond.body.context_binding, {
    scope: null,
    target_type: 'evidence',
    target_id: 'evidence-1'
  });
  assert(
    mockDb.calls.some((call) => call.sql.includes('INSERT INTO evidence_links') && call.params[1] === 'evidence-1' && call.params[2] === 'txn-2'),
    'Expected match command to persist selected evidence link'
  );

  const noMatch = await invoke(controller, {
    transcript: 'No match',
    evidence_id: 'evidence-1'
  });
  assert.strictEqual(noMatch.statusCode, 200);
  assert.strictEqual(noMatch.body.confirmation_chip, 'Applied: No match');

  const invalidSplit = await invoke(controller, {
    transcript: 'Split 140%',
    bank_txn_id: 'txn-123',
    context_scope: 'inbox'
  });
  assert.strictEqual(invalidSplit.statusCode, 400);
  assert.strictEqual(invalidSplit.body.error, 'Split percentage must be 0-100.');

  const unsupported = await invoke(controller, {
    transcript: 'Archive this forever'
  });
  assert.strictEqual(unsupported.statusCode, 400);
  assert.strictEqual(unsupported.body.error, 'Unsupported MVP voice micro-decision command.');

  console.log('voice_micro_decisions_ok');
  console.log('context_guardrails_ok=true');
  console.log('audited_classification_updates_ok=true');
  console.log('attach_receipt_clarification_ok=true');
  console.log('match_selection_ok=true');
  console.log('validation_errors_ok=true');
}

main().catch((error) => {
  console.error('voice_micro_decisions_failed');
  console.error(error);
  process.exit(1);
});
apply_patch(auto_approved=true) exited 0 in 6.33s:
Success. Updated the following files:
A C:\Users\edebe\eds\bizPA\backend\verify_voice_micro_decisions.js
file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..39624b343c48e63580746270cad8b4a7a01dc58a
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,6 +10,36 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+const {
+  parseVoiceMicroDecision,
+  validateMicroDecisionContext,
+} = require('../services/voiceMicroDecisionService');
+
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undoable: undoSupported,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
 
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
@@ -267,71 +297,167 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_scope = null,
+  } = req.body || {};
   const text = String(transcript).toLowerCase().trim();
+  const normalizedContextScope = context_scope === null || context_scope === undefined
+    ? null
+    : String(context_scope).toLowerCase().replace(/-/g, '_');
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
 
+  const requireTransactionContext = () => {
+    if (!bank_txn_id) {
+      return 'bank_txn_id is required for inbox-bound micro-decision commands.';
+    }
+    if (!MICRO_DECISION_CONTEXT_SCOPES.has(normalizedContextScope)) {
+      return 'context_scope must be inbox or finish_now for transaction micro-decisions.';
+    }
+    return null;
+  };
+
   try {
     if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const categoryName = text.replace('category:', '').trim();
       const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: categoryCode,
+            category_name: categoryName,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${categoryName}`,
+        appliedActions: [{ type: 'category', label: categoryName }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${bp}`,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     const splitMatch = text.match(/split\s+(\d{1,3})%?/);
     if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const pct = Number(splitMatch[1]);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Split ${pct}%`,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: 'Attach receipt requested',
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        extra: {
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
     if (['match first', 'match second', 'match third'].includes(text)) {
@@ -364,7 +490,16 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Match ${index}`,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     if (text === 'no match') {
@@ -376,7 +511,16 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: 'Applied: No match',
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/backend/verify_voice_micro_decisions.js b/backend/verify_voice_micro_decisions.js
index 25d9ab02050aac37a5d38aca5c6f0154de9bb787..2acccdd9719c57dde3fc02e5dd64bcedc8fa63cf
--- a/backend/verify_voice_micro_decisions.js
+++ b/backend/verify_voice_micro_decisions.js
@@ -9,16 +9,70 @@
       { bank_txn_id: 'txn-2' },
       { bank_txn_id: 'txn-3' }
     ];
+    this.classifications = new Map();
+    this.auditEntries = [];
+  }
+
+  get pool() {
+    return {
+      connect: async () => ({
+        query: this.query.bind(this),
+        release() {},
+      }),
+    };
   }
 
-  async query(text, params) {
+  async query(text, params = []) {
     const sql = text.replace(/\s+/g, ' ').trim();
     this.calls.push({ sql, params });
 
+    if (sql === 'BEGIN' || sql === 'COMMIT' || sql === 'ROLLBACK') {
+      return { rows: [] };
+    }
+
+    if (sql.includes('SELECT * FROM transaction_classifications')) {
+      const existing = this.classifications.get(params[0]);
+      return { rows: existing ? [existing] : [] };
+    }
+
+    if (sql.includes('INSERT INTO transaction_classifications')) {
+      const next = {
+        user_id: params[0],
+        bank_txn_id: params[1],
+        category_code: params[2],
+        category_name: params[3],
+        business_personal: params[4],
+        is_split: params[5],
+        split_business_pct: params[6],
+        confidence: params[7],
+        source: params[8],
+      };
+      this.classifications.set(params[1], next);
+      return { rows: [next] };
+    }
+
+    if (sql.includes('INSERT INTO transaction_audit_log')) {
+      this.auditEntries.push({
+        bank_txn_id: params[1],
+        previous_value: params[4],
+        new_value: params[5],
+        change_source: params[6],
+      });
+      return { rows: [] };
+    }
+
+    if (sql.includes('SELECT txn_date FROM bank_transactions')) {
+      return { rows: [{ txn_date: '2026-03-12' }] };
+    }
+
     if (sql.includes('FROM bank_transactions bt CROSS JOIN ev')) {
       return { rows: this.matchSuggestions };
     }
 
+    if (sql.includes('INSERT INTO evidence_links')) {
+      return { rows: [] };
+    }
+
     return { rows: [] };
   }
 }
@@ -69,38 +123,61 @@
   const mockDb = new MockDb();
   const controller = loadController(mockDb);
 
+  const missingContext = await invoke(controller, {
+    transcript: 'Business',
+    bank_txn_id: 'txn-123'
+  });
+  assert.strictEqual(missingContext.statusCode, 400);
+  assert.strictEqual(missingContext.body.error, 'context_scope must be inbox or finish_now for transaction micro-decisions.');
+
   const category = await invoke(controller, {
     transcript: 'Category: Travel',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_scope: 'inbox'
   });
   assert.strictEqual(category.statusCode, 200);
   assert.strictEqual(category.body.confirmation_chip, 'Applied: travel');
-  assert(
-    mockDb.calls.some((call) => call.sql.includes('INSERT INTO transaction_classifications') && call.params[2] === 'TRAVEL' && call.params[3] === 'travel'),
-    'Expected category command to persist manual classification'
-  );
+  assert.strictEqual(category.body.undo.supported, true);
+  assert.deepStrictEqual(category.body.context_binding, {
+    scope: 'inbox',
+    target_type: 'bank_transaction',
+    target_id: 'txn-123'
+  });
+  assert.strictEqual(mockDb.classifications.get('txn-123').category_code, 'TRAVEL');
+  assert.strictEqual(mockDb.auditEntries.at(-1).change_source, 'voice');
 
   const business = await invoke(controller, {
     transcript: 'Business',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_scope: 'finish-now'
   });
   assert.strictEqual(business.statusCode, 200);
   assert.strictEqual(business.body.confirmation_chip, 'Applied: BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').category_code, 'TRAVEL');
+  assert.strictEqual(mockDb.classifications.get('txn-123').business_personal, 'BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').is_split, false);
 
   const split = await invoke(controller, {
     transcript: 'Split 70%',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_scope: 'finish_now'
   });
   assert.strictEqual(split.statusCode, 200);
   assert.strictEqual(split.body.confirmation_chip, 'Applied: Split 70%');
+  assert.strictEqual(split.body.undo.supported, true);
+  assert.strictEqual(mockDb.classifications.get('txn-123').business_personal, 'BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').is_split, true);
+  assert.strictEqual(mockDb.classifications.get('txn-123').split_business_pct, 70);
 
   const attachReceipt = await invoke(controller, {
     transcript: 'Attach receipt',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_scope: 'inbox'
   });
   assert.strictEqual(attachReceipt.statusCode, 200);
   assert.strictEqual(attachReceipt.body.action_status, 'clarification_needed');
   assert.strictEqual(attachReceipt.body.confirmation_chip, 'Attach receipt requested');
+  assert.strictEqual(attachReceipt.body.undo.supported, false);
 
   const matchSecond = await invoke(controller, {
     transcript: 'Match second',
@@ -108,6 +185,11 @@
   });
   assert.strictEqual(matchSecond.statusCode, 200);
   assert.strictEqual(matchSecond.body.confirmation_chip, 'Applied: Match 2');
+  assert.deepStrictEqual(matchSecond.body.context_binding, {
+    scope: null,
+    target_type: 'evidence',
+    target_id: 'evidence-1'
+  });
   assert(
     mockDb.calls.some((call) => call.sql.includes('INSERT INTO evidence_links') && call.params[1] === 'evidence-1' && call.params[2] === 'txn-2'),
     'Expected match command to persist selected evidence link'
@@ -122,7 +204,8 @@
 
   const invalidSplit = await invoke(controller, {
     transcript: 'Split 140%',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_scope: 'inbox'
   });
   assert.strictEqual(invalidSplit.statusCode, 400);
   assert.strictEqual(invalidSplit.body.error, 'Split percentage must be 0-100.');
@@ -134,10 +217,10 @@
   assert.strictEqual(unsupported.body.error, 'Unsupported MVP voice micro-decision command.');
 
   console.log('voice_micro_decisions_ok');
-  console.log('category_business_split_ok=true');
+  console.log('context_guardrails_ok=true');
+  console.log('audited_classification_updates_ok=true');
   console.log('attach_receipt_clarification_ok=true');
   console.log('match_selection_ok=true');
-  console.log('no_match_ok=true');
   console.log('validation_errors_ok=true');
 }
 
diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index 9b145fadac9123e3fa01b12af6ef2b9916dc11de..b21180e549e09be4a5ed0412e53087d2a52f4382
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -50,6 +50,11 @@
   buildQuarterUiState,
   deriveResolvedIssueIdsFromSearchParams,
 } from './quarterReadiness';
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -427,6 +432,13 @@
   .date-group-count { border-radius: 999px; padding: 4px 9px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.72rem; color: var(--text-muted); }
   .date-group-body { padding: 0 12px 12px; }
   .voice-preview { background: rgba(0,0,0,0.82); color: white; padding: 8px 15px; border-radius: 15px; margin-bottom: 8px; font-size: 0.72rem; max-width: 240px; text-align: right; }
+  .voice-context-banner { display: grid; gap: 10px; padding: 14px 16px; margin-bottom: 16px; border-radius: 18px; background: var(--bg-card); border: 1px solid var(--border); box-shadow: var(--shadow); }
+  .voice-context-row, .voice-chip-actions, .voice-chip-labels { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
+  .voice-context-pill, .voice-action-pill { border-radius: 999px; padding: 6px 10px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.74rem; font-weight: 700; }
+  .voice-context-pill.active { background: rgba(13,92,99,0.12); color: var(--primary-strong); border-color: rgba(13,92,99,0.24); }
+  .voice-micro-chip { display: grid; gap: 10px; padding: 14px 16px; border-radius: 18px; background: rgba(13,92,99,0.08); border: 1px solid rgba(13,92,99,0.18); }
+  .voice-micro-chip strong { display: block; }
+  .inbox-action-card.voice-target { border-color: rgba(13,92,99,0.36); box-shadow: 0 0 0 2px rgba(13,92,99,0.12), var(--shadow); }
   .spinner { animation: rotate 2s linear infinite; }
   .capture-grid { display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(320px, 0.95fr); gap: 18px; align-items: start; }
   .command-centre { display: grid; gap: 18px; }
@@ -1382,6 +1394,8 @@
   const [inboxDrafts, setInboxDrafts] = useState({});
   const [inboxBusyId, setInboxBusyId] = useState('');
   const [inboxMessage, setInboxMessage] = useState(null);
+  const [activeVoiceContextId, setActiveVoiceContextId] = useState('');
+  const [voiceMicroDecisionChip, setVoiceMicroDecisionChip] = useState(null);
   const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
@@ -1606,10 +1620,20 @@
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
   const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const activeVoiceCard = findVoiceContextCard(inboxCards, activeVoiceContextId);
   const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
   const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
   const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
+  useEffect(() => {
+    if (!activeVoiceContextId) {
+      return;
+    }
+    if (!findVoiceContextCard(inboxCards, activeVoiceContextId)) {
+      setActiveVoiceContextId('');
+    }
+  }, [activeVoiceContextId, inboxCards]);
+
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
   const canManageGovernance = hasPermission(currentUser, PERMISSIONS.GOVERNANCE_MANAGE);
@@ -1806,11 +1830,13 @@
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxDrafts({});
+        setVoiceMicroDecisionChip(null);
         setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
         return;
       }
       await axios.post(`${API_BASE_URL}/inbox/undo-last`);
       await fetchAllData();
+      setVoiceMicroDecisionChip(null);
       setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
     } catch (error) {
       setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
@@ -1991,22 +2017,51 @@
     const cleanText = text.replace(/┬ú/g, 'GBP ');
     try {
       setVoiceStatus('processing');
-      const response = await axios.post(`${API_BASE_URL}/voice/process`, {
+      const microDecisionRequest = buildVoiceMicroDecisionRequest({
+        currentTab,
         transcript: cleanText,
-        device_id: 'mobile-v139',
-        current_date: new Date().toISOString(),
+        activeVoiceContextId,
+        inboxCards,
       });
-      if (response.data.confirmation_text && window.SpeechSynthesisUtterance) {
+
+      if (microDecisionRequest?.error) {
+        setInboxMessage({ type: 'error', text: microDecisionRequest.error });
+        setCurrentTab('inbox');
+        return;
+      }
+
+      const response = microDecisionRequest
+        ? await axios.post(`${API_BASE_URL}${microDecisionRequest.endpoint}`, microDecisionRequest.payload)
+        : await axios.post(`${API_BASE_URL}/voice/process`, {
+            transcript: cleanText,
+            device_id: 'mobile-v139',
+            current_date: new Date().toISOString(),
+          });
+      const spokenConfirmation = response.data.confirmation_chip || response.data.confirmation_text;
+      if (spokenConfirmation && window.SpeechSynthesisUtterance) {
         try {
-          const utterance = new window.SpeechSynthesisUtterance(response.data.confirmation_text);
+          const utterance = new window.SpeechSynthesisUtterance(spokenConfirmation);
           window.speechSynthesis.speak(utterance);
         } catch (_) {
         }
       }
-      executeVoiceAction(response.data);
-      fetchAllData();
+      if (microDecisionRequest) {
+        setVoiceMicroDecisionChip((previous) => reduceVoiceMicroDecisionChip(previous, response.data, microDecisionRequest.card));
+        setInboxMessage({
+          type: 'success',
+          text: response.data.confirmation_chip || 'Voice triage action applied.',
+        });
+      } else {
+        executeVoiceAction(response.data);
+      }
+      await fetchAllData();
     } catch (error) {
-      alert('AI Error');
+      const message = error?.response?.data?.error || 'AI Error';
+      if (currentTab === 'inbox' || currentTab === 'quarter') {
+        setInboxMessage({ type: 'error', text: message });
+      } else {
+        alert(message);
+      }
     } finally {
       setVoiceStatus('idle');
       setIsListening(false);
@@ -2894,7 +2949,11 @@
             const saving = inboxBusyId === card.id;
             const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
             return (
-              <section key={card.id} className="inbox-action-card">
+              <section
+                key={card.id}
+                className={`inbox-action-card ${activeVoiceContextId === card.id ? 'voice-target' : ''}`}
+                onClick={() => setActiveVoiceContextId(card.id)}
+              >
                 <div className="inbox-card-top">
                   <div>
                     <strong>{card.merchant}</strong>
@@ -2914,6 +2973,9 @@
                       {label}
                     </span>
                   ))}
+                  {activeVoiceContextId === card.id && (
+                    <span className="voice-context-pill active">Voice target</span>
+                  )}
                   {card.suggested_category?.category_name && (
                     <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
                       <CheckCircle2 size={14} />
@@ -3357,6 +3419,9 @@
   };
 
   const openReadinessWorkflow = (issue) => {
+    if (issue?.affected_entity_id) {
+      setActiveVoiceContextId(String(issue.affected_entity_id));
+    }
     if (issue?.resolution_target?.workflow === '/inbox/finish-now') {
       setCurrentTab('inbox');
       return;
@@ -3364,6 +3429,76 @@
     setCurrentTab('inbox');
   };
 
+  const renderVoiceContextBanner = () => {
+    if (!(currentTab === 'inbox' || currentTab === 'quarter')) {
+      return null;
+    }
+
+    return (
+      <section className="voice-context-banner">
+        <div>
+          <strong>Voice triage</strong>
+          <div className="small-muted">
+            Use the mic against one selected finish-now item only. Supported commands here are category, business, personal, split n%, and attach receipt.
+          </div>
+        </div>
+        <div className="voice-context-row">
+          <span className={`voice-context-pill ${activeVoiceCard ? 'active' : ''}`}>
+            {activeVoiceCard
+              ? `Target: ${activeVoiceCard.merchant} (${activeVoiceCard.id})`
+              : 'Select a blocker card to arm voice triage'}
+          </span>
+          {activeVoiceCard && (
+            <button className="secondary-action" type="button" onClick={() => setCurrentTab('inbox')}>
+              Edit target
+            </button>
+          )}
+        </div>
+        {voiceMicroDecisionChip && (
+          <div className="voice-micro-chip">
+            <div>
+              <strong>{voiceMicroDecisionChip.confirmationChip}</strong>
+              <div className="small-muted">
+                {voiceMicroDecisionChip.targetLabel} ({voiceMicroDecisionChip.targetId})
+              </div>
+            </div>
+            {voiceMicroDecisionChip.labels.length > 0 && (
+              <div className="voice-chip-labels">
+                {voiceMicroDecisionChip.labels.map((label) => (
+                  <span key={`${voiceMicroDecisionChip.targetId}-${label}`} className="voice-action-pill">
+                    {label}
+                  </span>
+                ))}
+              </div>
+            )}
+            <div className="voice-chip-actions">
+              <button
+                className="secondary-action"
+                type="button"
+                onClick={() => {
+                  setCurrentTab('inbox');
+                  setActiveVoiceContextId(voiceMicroDecisionChip.targetId);
+                }}
+              >
+                Edit
+              </button>
+              {voiceMicroDecisionChip.undoSupported && (
+                <button
+                  className="secondary-action"
+                  type="button"
+                  onClick={undoLastInboxAction}
+                  disabled={inboxBusyId === 'undo-last'}
+                >
+                  Undo
+                </button>
+              )}
+            </div>
+          </div>
+        )}
+      </section>
+    );
+  };
+
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
@@ -3654,6 +3789,13 @@
                     </div>
                   </div>
                   <div className="finish-now-actions">
+                    <button
+                      className="secondary-action"
+                      type="button"
+                      onClick={() => setActiveVoiceContextId(String(issue.affected_entity_id))}
+                    >
+                      {activeVoiceContextId === String(issue.affected_entity_id) ? 'Voice target set' : 'Arm voice'}
+                    </button>
                     {(readinessDemoEnabled || snapshotDemoEnabled) ? (
                       <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
                         Mark fixed
@@ -4391,6 +4533,7 @@
         </button>
       </section>
       {connectionError && <div className="alert alert-danger">{connectionError}</div>}
+      {renderVoiceContextBanner()}
 
       {loading ? (
         <div className="text-center py-5">Loading...</div>
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..6eb469fcc7f023f8545d9e952a5823fb8c47a88d
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});

file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..39624b343c48e63580746270cad8b4a7a01dc58a
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,6 +10,36 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+const {
+  parseVoiceMicroDecision,
+  validateMicroDecisionContext,
+} = require('../services/voiceMicroDecisionService');
+
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undoable: undoSupported,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
 
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
@@ -267,71 +297,167 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_scope = null,
+  } = req.body || {};
   const text = String(transcript).toLowerCase().trim();
+  const normalizedContextScope = context_scope === null || context_scope === undefined
+    ? null
+    : String(context_scope).toLowerCase().replace(/-/g, '_');
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
 
+  const requireTransactionContext = () => {
+    if (!bank_txn_id) {
+      return 'bank_txn_id is required for inbox-bound micro-decision commands.';
+    }
+    if (!MICRO_DECISION_CONTEXT_SCOPES.has(normalizedContextScope)) {
+      return 'context_scope must be inbox or finish_now for transaction micro-decisions.';
+    }
+    return null;
+  };
+
   try {
     if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const categoryName = text.replace('category:', '').trim();
       const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: categoryCode,
+            category_name: categoryName,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${categoryName}`,
+        appliedActions: [{ type: 'category', label: categoryName }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: ${bp}`,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     const splitMatch = text.match(/split\s+(\d{1,3})%?/);
     if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
       const pct = Number(splitMatch[1]);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Split ${pct}%`,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+      }));
     }
 
     if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+      const contextError = requireTransactionContext();
+      if (contextError) return res.status(400).json({ error: contextError });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: 'Attach receipt requested',
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        extra: {
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
     if (['match first', 'match second', 'match third'].includes(text)) {
@@ -364,7 +490,16 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: `Applied: Match ${index}`,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     if (text === 'no match') {
@@ -376,7 +511,16 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: 'Applied: No match',
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/backend/verify_voice_micro_decisions.js b/backend/verify_voice_micro_decisions.js
index 25d9ab02050aac37a5d38aca5c6f0154de9bb787..2acccdd9719c57dde3fc02e5dd64bcedc8fa63cf
--- a/backend/verify_voice_micro_decisions.js
+++ b/backend/verify_voice_micro_decisions.js
@@ -9,16 +9,70 @@
       { bank_txn_id: 'txn-2' },
       { bank_txn_id: 'txn-3' }
     ];
+    this.classifications = new Map();
+    this.auditEntries = [];
+  }
+
+  get pool() {
+    return {
+      connect: async () => ({
+        query: this.query.bind(this),
+        release() {},
+      }),
+    };
   }
 
-  async query(text, params) {
+  async query(text, params = []) {
     const sql = text.replace(/\s+/g, ' ').trim();
     this.calls.push({ sql, params });
 
+    if (sql === 'BEGIN' || sql === 'COMMIT' || sql === 'ROLLBACK') {
+      return { rows: [] };
+    }
+
+    if (sql.includes('SELECT * FROM transaction_classifications')) {
+      const existing = this.classifications.get(params[0]);
+      return { rows: existing ? [existing] : [] };
+    }
+
+    if (sql.includes('INSERT INTO transaction_classifications')) {
+      const next = {
+        user_id: params[0],
+        bank_txn_id: params[1],
+        category_code: params[2],
+        category_name: params[3],
+        business_personal: params[4],
+        is_split: params[5],
+        split_business_pct: params[6],
+        confidence: params[7],
+        source: params[8],
+      };
+      this.classifications.set(params[1], next);
+      return { rows: [next] };
+    }
+
+    if (sql.includes('INSERT INTO transaction_audit_log')) {
+      this.auditEntries.push({
+        bank_txn_id: params[1],
+        previous_value: params[4],
+        new_value: params[5],
+        change_source: params[6],
+      });
+      return { rows: [] };
+    }
+
+    if (sql.includes('SELECT txn_date FROM bank_transactions')) {
+      return { rows: [{ txn_date: '2026-03-12' }] };
+    }
+
     if (sql.includes('FROM bank_transactions bt CROSS JOIN ev')) {
       return { rows: this.matchSuggestions };
     }
 
+    if (sql.includes('INSERT INTO evidence_links')) {
+      return { rows: [] };
+    }
+
     return { rows: [] };
   }
 }
@@ -69,38 +123,61 @@
   const mockDb = new MockDb();
   const controller = loadController(mockDb);
 
+  const missingContext = await invoke(controller, {
+    transcript: 'Business',
+    bank_txn_id: 'txn-123'
+  });
+  assert.strictEqual(missingContext.statusCode, 400);
+  assert.strictEqual(missingContext.body.error, 'context_scope must be inbox or finish_now for transaction micro-decisions.');
+
   const category = await invoke(controller, {
     transcript: 'Category: Travel',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_scope: 'inbox'
   });
   assert.strictEqual(category.statusCode, 200);
   assert.strictEqual(category.body.confirmation_chip, 'Applied: travel');
-  assert(
-    mockDb.calls.some((call) => call.sql.includes('INSERT INTO transaction_classifications') && call.params[2] === 'TRAVEL' && call.params[3] === 'travel'),
-    'Expected category command to persist manual classification'
-  );
+  assert.strictEqual(category.body.undo.supported, true);
+  assert.deepStrictEqual(category.body.context_binding, {
+    scope: 'inbox',
+    target_type: 'bank_transaction',
+    target_id: 'txn-123'
+  });
+  assert.strictEqual(mockDb.classifications.get('txn-123').category_code, 'TRAVEL');
+  assert.strictEqual(mockDb.auditEntries.at(-1).change_source, 'voice');
 
   const business = await invoke(controller, {
     transcript: 'Business',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_scope: 'finish-now'
   });
   assert.strictEqual(business.statusCode, 200);
   assert.strictEqual(business.body.confirmation_chip, 'Applied: BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').category_code, 'TRAVEL');
+  assert.strictEqual(mockDb.classifications.get('txn-123').business_personal, 'BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').is_split, false);
 
   const split = await invoke(controller, {
     transcript: 'Split 70%',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_scope: 'finish_now'
   });
   assert.strictEqual(split.statusCode, 200);
   assert.strictEqual(split.body.confirmation_chip, 'Applied: Split 70%');
+  assert.strictEqual(split.body.undo.supported, true);
+  assert.strictEqual(mockDb.classifications.get('txn-123').business_personal, 'BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').is_split, true);
+  assert.strictEqual(mockDb.classifications.get('txn-123').split_business_pct, 70);
 
   const attachReceipt = await invoke(controller, {
     transcript: 'Attach receipt',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_scope: 'inbox'
   });
   assert.strictEqual(attachReceipt.statusCode, 200);
   assert.strictEqual(attachReceipt.body.action_status, 'clarification_needed');
   assert.strictEqual(attachReceipt.body.confirmation_chip, 'Attach receipt requested');
+  assert.strictEqual(attachReceipt.body.undo.supported, false);
 
   const matchSecond = await invoke(controller, {
     transcript: 'Match second',
@@ -108,6 +185,11 @@
   });
   assert.strictEqual(matchSecond.statusCode, 200);
   assert.strictEqual(matchSecond.body.confirmation_chip, 'Applied: Match 2');
+  assert.deepStrictEqual(matchSecond.body.context_binding, {
+    scope: null,
+    target_type: 'evidence',
+    target_id: 'evidence-1'
+  });
   assert(
     mockDb.calls.some((call) => call.sql.includes('INSERT INTO evidence_links') && call.params[1] === 'evidence-1' && call.params[2] === 'txn-2'),
     'Expected match command to persist selected evidence link'
@@ -122,7 +204,8 @@
 
   const invalidSplit = await invoke(controller, {
     transcript: 'Split 140%',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_scope: 'inbox'
   });
   assert.strictEqual(invalidSplit.statusCode, 400);
   assert.strictEqual(invalidSplit.body.error, 'Split percentage must be 0-100.');
@@ -134,10 +217,10 @@
   assert.strictEqual(unsupported.body.error, 'Unsupported MVP voice micro-decision command.');
 
   console.log('voice_micro_decisions_ok');
-  console.log('category_business_split_ok=true');
+  console.log('context_guardrails_ok=true');
+  console.log('audited_classification_updates_ok=true');
   console.log('attach_receipt_clarification_ok=true');
   console.log('match_selection_ok=true');
-  console.log('no_match_ok=true');
   console.log('validation_errors_ok=true');
 }
 
diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index 9b145fadac9123e3fa01b12af6ef2b9916dc11de..b21180e549e09be4a5ed0412e53087d2a52f4382
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -50,6 +50,11 @@
   buildQuarterUiState,
   deriveResolvedIssueIdsFromSearchParams,
 } from './quarterReadiness';
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -427,6 +432,13 @@
   .date-group-count { border-radius: 999px; padding: 4px 9px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.72rem; color: var(--text-muted); }
   .date-group-body { padding: 0 12px 12px; }
   .voice-preview { background: rgba(0,0,0,0.82); color: white; padding: 8px 15px; border-radius: 15px; margin-bottom: 8px; font-size: 0.72rem; max-width: 240px; text-align: right; }
+  .voice-context-banner { display: grid; gap: 10px; padding: 14px 16px; margin-bottom: 16px; border-radius: 18px; background: var(--bg-card); border: 1px solid var(--border); box-shadow: var(--shadow); }
+  .voice-context-row, .voice-chip-actions, .voice-chip-labels { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
+  .voice-context-pill, .voice-action-pill { border-radius: 999px; padding: 6px 10px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.74rem; font-weight: 700; }
+  .voice-context-pill.active { background: rgba(13,92,99,0.12); color: var(--primary-strong); border-color: rgba(13,92,99,0.24); }
+  .voice-micro-chip { display: grid; gap: 10px; padding: 14px 16px; border-radius: 18px; background: rgba(13,92,99,0.08); border: 1px solid rgba(13,92,99,0.18); }
+  .voice-micro-chip strong { display: block; }
+  .inbox-action-card.voice-target { border-color: rgba(13,92,99,0.36); box-shadow: 0 0 0 2px rgba(13,92,99,0.12), var(--shadow); }
   .spinner { animation: rotate 2s linear infinite; }
   .capture-grid { display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(320px, 0.95fr); gap: 18px; align-items: start; }
   .command-centre { display: grid; gap: 18px; }
@@ -1382,6 +1394,8 @@
   const [inboxDrafts, setInboxDrafts] = useState({});
   const [inboxBusyId, setInboxBusyId] = useState('');
   const [inboxMessage, setInboxMessage] = useState(null);
+  const [activeVoiceContextId, setActiveVoiceContextId] = useState('');
+  const [voiceMicroDecisionChip, setVoiceMicroDecisionChip] = useState(null);
   const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
@@ -1606,10 +1620,20 @@
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
   const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const activeVoiceCard = findVoiceContextCard(inboxCards, activeVoiceContextId);
   const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
   const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
   const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
+  useEffect(() => {
+    if (!activeVoiceContextId) {
+      return;
+    }
+    if (!findVoiceContextCard(inboxCards, activeVoiceContextId)) {
+      setActiveVoiceContextId('');
+    }
+  }, [activeVoiceContextId, inboxCards]);
+
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
   const canManageGovernance = hasPermission(currentUser, PERMISSIONS.GOVERNANCE_MANAGE);
@@ -1806,11 +1830,13 @@
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxDrafts({});
+        setVoiceMicroDecisionChip(null);
         setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
         return;
       }
       await axios.post(`${API_BASE_URL}/inbox/undo-last`);
       await fetchAllData();
+      setVoiceMicroDecisionChip(null);
       setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
     } catch (error) {
       setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
@@ -1991,22 +2017,51 @@
     const cleanText = text.replace(/┬ú/g, 'GBP ');
     try {
       setVoiceStatus('processing');
-      const response = await axios.post(`${API_BASE_URL}/voice/process`, {
+      const microDecisionRequest = buildVoiceMicroDecisionRequest({
+        currentTab,
         transcript: cleanText,
-        device_id: 'mobile-v139',
-        current_date: new Date().toISOString(),
+        activeVoiceContextId,
+        inboxCards,
       });
-      if (response.data.confirmation_text && window.SpeechSynthesisUtterance) {
+
+      if (microDecisionRequest?.error) {
+        setInboxMessage({ type: 'error', text: microDecisionRequest.error });
+        setCurrentTab('inbox');
+        return;
+      }
+
+      const response = microDecisionRequest
+        ? await axios.post(`${API_BASE_URL}${microDecisionRequest.endpoint}`, microDecisionRequest.payload)
+        : await axios.post(`${API_BASE_URL}/voice/process`, {
+            transcript: cleanText,
+            device_id: 'mobile-v139',
+            current_date: new Date().toISOString(),
+          });
+      const spokenConfirmation = response.data.confirmation_chip || response.data.confirmation_text;
+      if (spokenConfirmation && window.SpeechSynthesisUtterance) {
         try {
-          const utterance = new window.SpeechSynthesisUtterance(response.data.confirmation_text);
+          const utterance = new window.SpeechSynthesisUtterance(spokenConfirmation);
           window.speechSynthesis.speak(utterance);
         } catch (_) {
         }
       }
-      executeVoiceAction(response.data);
-      fetchAllData();
+      if (microDecisionRequest) {
+        setVoiceMicroDecisionChip((previous) => reduceVoiceMicroDecisionChip(previous, response.data, microDecisionRequest.card));
+        setInboxMessage({
+          type: 'success',
+          text: response.data.confirmation_chip || 'Voice triage action applied.',
+        });
+      } else {
+        executeVoiceAction(response.data);
+      }
+      await fetchAllData();
     } catch (error) {
-      alert('AI Error');
+      const message = error?.response?.data?.error || 'AI Error';
+      if (currentTab === 'inbox' || currentTab === 'quarter') {
+        setInboxMessage({ type: 'error', text: message });
+      } else {
+        alert(message);
+      }
     } finally {
       setVoiceStatus('idle');
       setIsListening(false);
@@ -2894,7 +2949,11 @@
             const saving = inboxBusyId === card.id;
             const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
             return (
-              <section key={card.id} className="inbox-action-card">
+              <section
+                key={card.id}
+                className={`inbox-action-card ${activeVoiceContextId === card.id ? 'voice-target' : ''}`}
+                onClick={() => setActiveVoiceContextId(card.id)}
+              >
                 <div className="inbox-card-top">
                   <div>
                     <strong>{card.merchant}</strong>
@@ -2914,6 +2973,9 @@
                       {label}
                     </span>
                   ))}
+                  {activeVoiceContextId === card.id && (
+                    <span className="voice-context-pill active">Voice target</span>
+                  )}
                   {card.suggested_category?.category_name && (
                     <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
                       <CheckCircle2 size={14} />
@@ -3357,6 +3419,9 @@
   };
 
   const openReadinessWorkflow = (issue) => {
+    if (issue?.affected_entity_id) {
+      setActiveVoiceContextId(String(issue.affected_entity_id));
+    }
     if (issue?.resolution_target?.workflow === '/inbox/finish-now') {
       setCurrentTab('inbox');
       return;
@@ -3364,6 +3429,76 @@
     setCurrentTab('inbox');
   };
 
+  const renderVoiceContextBanner = () => {
+    if (!(currentTab === 'inbox' || currentTab === 'quarter')) {
+      return null;
+    }
+
+    return (
+      <section className="voice-context-banner">
+        <div>
+          <strong>Voice triage</strong>
+          <div className="small-muted">
+            Use the mic against one selected finish-now item only. Supported commands here are category, business, personal, split n%, and attach receipt.
+          </div>
+        </div>
+        <div className="voice-context-row">
+          <span className={`voice-context-pill ${activeVoiceCard ? 'active' : ''}`}>
+            {activeVoiceCard
+              ? `Target: ${activeVoiceCard.merchant} (${activeVoiceCard.id})`
+              : 'Select a blocker card to arm voice triage'}
+          </span>
+          {activeVoiceCard && (
+            <button className="secondary-action" type="button" onClick={() => setCurrentTab('inbox')}>
+              Edit target
+            </button>
+          )}
+        </div>
+        {voiceMicroDecisionChip && (
+          <div className="voice-micro-chip">
+            <div>
+              <strong>{voiceMicroDecisionChip.confirmationChip}</strong>
+              <div className="small-muted">
+                {voiceMicroDecisionChip.targetLabel} ({voiceMicroDecisionChip.targetId})
+              </div>
+            </div>
+            {voiceMicroDecisionChip.labels.length > 0 && (
+              <div className="voice-chip-labels">
+                {voiceMicroDecisionChip.labels.map((label) => (
+                  <span key={`${voiceMicroDecisionChip.targetId}-${label}`} className="voice-action-pill">
+                    {label}
+                  </span>
+                ))}
+              </div>
+            )}
+            <div className="voice-chip-actions">
+              <button
+                className="secondary-action"
+                type="button"
+                onClick={() => {
+                  setCurrentTab('inbox');
+                  setActiveVoiceContextId(voiceMicroDecisionChip.targetId);
+                }}
+              >
+                Edit
+              </button>
+              {voiceMicroDecisionChip.undoSupported && (
+                <button
+                  className="secondary-action"
+                  type="button"
+                  onClick={undoLastInboxAction}
+                  disabled={inboxBusyId === 'undo-last'}
+                >
+                  Undo
+                </button>
+              )}
+            </div>
+          </div>
+        )}
+      </section>
+    );
+  };
+
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
@@ -3654,6 +3789,13 @@
                     </div>
                   </div>
                   <div className="finish-now-actions">
+                    <button
+                      className="secondary-action"
+                      type="button"
+                      onClick={() => setActiveVoiceContextId(String(issue.affected_entity_id))}
+                    >
+                      {activeVoiceContextId === String(issue.affected_entity_id) ? 'Voice target set' : 'Arm voice'}
+                    </button>
                     {(readinessDemoEnabled || snapshotDemoEnabled) ? (
                       <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
                         Mark fixed
@@ -4391,6 +4533,7 @@
         </button>
       </section>
       {connectionError && <div className="alert alert-danger">{connectionError}</div>}
+      {renderVoiceContextBanner()}
 
       {loading ? (
         <div className="text-center py-5">Loading...</div>
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..6eb469fcc7f023f8545d9e952a5823fb8c47a88d
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});

codex
The code changes are in place. I’m running the backend contract checks first, then frontend tests/build to catch any integration or syntax regressions in the new voice-context UI path before I update the lifecycle document.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npm run verify:voice-capture' in C:\Users\edebe\eds\bizPA\backendexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npm run verify:voice-micro-decisions' in C:\Users\edebe\eds\bizPA\backendexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npm run build' in C:\Users\edebe\eds\bizPA\frontendexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npm test -- --watchAll=false --runTestsByPath src/voiceMicroDecisions.test.js src/quarterReadiness.test.js' in C:\Users\edebe\eds\bizPA\frontend succeeded in 4.68s:

> backend@1.3.6 verify:voice-capture
> node verify_voice_capture_parser.js

voice_capture_parser_ok
entity_mapping_ok=true
low_confidence_review_ok=true
composition_contract_ok=true
query_period_filters_ok=true
query_exact_date_filters_ok=true
 exited 1 in 4.97s:

> backend@1.3.6 verify:voice-micro-decisions
> node verify_voice_micro_decisions.js

[dotenv@17.3.1] injecting env (9) from .env -- tip: 🔐 encrypt with Dotenvx: https://dotenvx.com
[Supabase] Client initialized successfully.
[VoiceController] processMicroDecision error: ReferenceError: MICRO_DECISION_CONTEXT_SCOPES is not defined
    at requireTransactionContext (C:\Users\edebe\eds\bizPA\backend\src\controllers\voiceController.js:317:5)
    at Object.processMicroDecision (C:\Users\edebe\eds\bizPA\backend\src\controllers\voiceController.js:364:28)
    at invoke (C:\Users\edebe\eds\bizPA\backend\verify_voice_micro_decisions.js:112:20)
    at main (C:\Users\edebe\eds\bizPA\backend\verify_voice_micro_decisions.js:126:32)
    at Object.<anonymous> (C:\Users\edebe\eds\bizPA\backend\verify_voice_micro_decisions.js:227:1)
    at Module._compile (node:internal/modules/cjs/loader:1554:14)
    at Object..js (node:internal/modules/cjs/loader:1706:10)
    at Module.load (node:internal/modules/cjs/loader:1289:32)
    at Function._load (node:internal/modules/cjs/loader:1108:12)
    at TracingChannel.traceSync (node:diagnostics_channel:322:14)
voice_micro_decisions_failed
AssertionError [ERR_ASSERTION]: Expected values to be strictly equal:

 exited 1 in 9.03s:

> frontend@1.3.6 test
> react-scripts test src/voiceMicroDecisions.test.js src/quarterReadiness.test.js

npm warn "src/voiceMicroDecisions.test.js" is being parsed as a normal command line argument.
npm warn Unknown cli config "--watchAll". This will stop working in the next major version of npm.
npm warn Unknown cli config "--runTestsByPath". This will stop working in the next major version of npm.
Error: spawn EPERM
    at ChildProcess.spawn (node:internal/child_process:420:11)
    at spawn (node:child_process:753:9)
    at fork (node:child_process:171:10)
    at ChildProcessWorker.initialize (C:\Users\edebe\eds\bizPA\frontend\node_modules\jest-worker\build\workers\ChildProcessWorker.js:133:45)
    at new ChildProcessWorker (C:\Users\edebe\eds\bizPA\frontend\node_modules\jest-worker\build\workers\ChildProcessWorker.js:124:10)
    at WorkerPool.createWorker (C:\Users\edebe\eds\bizPA\frontend\node_modules\jest-worker\build\WorkerPool.js:44:12)
    at new BaseWorkerPool (C:\Users\edebe\eds\bizPA\frontend\node_modules\jest-worker\build\base\BaseWorkerPool.js:127:27)
    at new WorkerPool (C:\Users\edebe\eds\bizPA\frontend\node_modules\jest-worker\build\WorkerPool.js:30:1)
    at new Worker (C:\Users\edebe\eds\bizPA\frontend\node_modules\jest-worker\build\index.js:167:26)
    at HasteMap._getWorker (C:\Users\edebe\eds\bizPA\frontend\node_modules\jest-haste-map\build\index.js:862:24)
 succeeded in 1m 40s:

> frontend@1.3.6 build
> react-scripts build

Creating an optimized production build...
Compiled successfully.

File sizes after gzip:

  118.73 kB (+1.14 kB)  build\static\js\main.87cda2d8.js
  659 B                 build\static\js\828.89358f49.chunk.js
  314 B                 build\static\js\940.c568d512.chunk.js

The project was built assuming it is hosted at /.
You can control this with the homepage field in your package.json.

The build folder is ready to be deployed.
You may serve it with a static server:

  npm install -g serve
file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..e6d93b4bc1cd6960ce27238cfb7e90c3322f41e0
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,6 +10,36 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+const {
+  parseVoiceMicroDecision,
+  validateMicroDecisionContext,
+} = require('../services/voiceMicroDecisionService');
+
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undoable: undoSupported,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
 
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
@@ -267,76 +297,177 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
-  const text = String(transcript).toLowerCase().trim();
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_kind = null,
+    context_scope = null,
+  } = req.body || {};
+  const rawTranscript = String(transcript).trim();
+  const text = rawTranscript.toLowerCase();
+  const normalizedContext = context_kind ?? context_scope;
+  const parsedCommand = parseVoiceMicroDecision(rawTranscript);
+  const contextValidation = validateMicroDecisionContext(parsedCommand, normalizedContext);
+  const normalizedContextScope = contextValidation.ok
+    ? contextValidation.context_kind
+    : (normalizedContext === null || normalizedContext === undefined
+      ? null
+      : String(normalizedContext).toLowerCase().replace(/-/g, '_'));
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
+  if (!contextValidation.ok) return res.status(contextValidation.statusCode).json({ error: contextValidation.error });
 
   try {
-    if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
-      const categoryName = text.replace('category:', '').trim();
-      const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+    if (parsedCommand.type === 'category') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: parsedCommand.category_code,
+            category_name: parsedCommand.category_name,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'category', label: parsedCommand.category_name }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
-      const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+    if (parsedCommand.type === 'business_personal') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const bp = parsedCommand.business_personal;
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    const splitMatch = text.match(/split\s+(\d{1,3})%?/);
-    if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
-      const pct = Number(splitMatch[1]);
+    if (parsedCommand.type === 'split') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const pct = Number(parsedCommand.split_business_pct);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+    if (parsedCommand.type === 'attach_receipt') {
+      if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for attach receipt commands.' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          parsed_command: parsedCommand,
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
-    if (['match first', 'match second', 'match third'].includes(text)) {
+    if (parsedCommand.type === 'match_rank') {
       if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for match commands.' });
-      const index = text === 'match first' ? 1 : text === 'match second' ? 2 : 3;
+      const index = parsedCommand.match_rank;
       const suggestions = await db.query(
         `
         WITH ev AS (
@@ -364,10 +495,24 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          bank_txn_id: chosen,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text === 'no match') {
+    if (parsedCommand.type === 'no_match') {
       if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for no match.' });
       await db.query(
         `
@@ -376,7 +521,20 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/backend/verify_voice_micro_decisions.js b/backend/verify_voice_micro_decisions.js
index 25d9ab02050aac37a5d38aca5c6f0154de9bb787..15c93efa65f133ec1a59d827ecbfa29a95cf4e6d
--- a/backend/verify_voice_micro_decisions.js
+++ b/backend/verify_voice_micro_decisions.js
@@ -9,16 +9,70 @@
       { bank_txn_id: 'txn-2' },
       { bank_txn_id: 'txn-3' }
     ];
+    this.classifications = new Map();
+    this.auditEntries = [];
   }
 
-  async query(text, params) {
+  get pool() {
+    return {
+      connect: async () => ({
+        query: this.query.bind(this),
+        release() {},
+      }),
+    };
+  }
+
+  async query(text, params = []) {
     const sql = text.replace(/\s+/g, ' ').trim();
     this.calls.push({ sql, params });
 
+    if (sql === 'BEGIN' || sql === 'COMMIT' || sql === 'ROLLBACK') {
+      return { rows: [] };
+    }
+
+    if (sql.includes('SELECT * FROM transaction_classifications')) {
+      const existing = this.classifications.get(params[0]);
+      return { rows: existing ? [existing] : [] };
+    }
+
+    if (sql.includes('INSERT INTO transaction_classifications')) {
+      const next = {
+        user_id: params[0],
+        bank_txn_id: params[1],
+        category_code: params[2],
+        category_name: params[3],
+        business_personal: params[4],
+        is_split: params[5],
+        split_business_pct: params[6],
+        confidence: params[7],
+        source: params[8],
+      };
+      this.classifications.set(params[1], next);
+      return { rows: [next] };
+    }
+
+    if (sql.includes('INSERT INTO transaction_audit_log')) {
+      this.auditEntries.push({
+        bank_txn_id: params[1],
+        previous_value: params[4],
+        new_value: params[5],
+        change_source: params[6],
+      });
+      return { rows: [] };
+    }
+
+    if (sql.includes('SELECT txn_date FROM bank_transactions')) {
+      return { rows: [{ txn_date: '2026-03-12' }] };
+    }
+
     if (sql.includes('FROM bank_transactions bt CROSS JOIN ev')) {
       return { rows: this.matchSuggestions };
     }
 
+    if (sql.includes('INSERT INTO evidence_links')) {
+      return { rows: [] };
+    }
+
     return { rows: [] };
   }
 }
@@ -69,45 +123,79 @@
   const mockDb = new MockDb();
   const controller = loadController(mockDb);
 
+  const missingContext = await invoke(controller, {
+    transcript: 'Business',
+    bank_txn_id: 'txn-123'
+  });
+  assert.strictEqual(missingContext.statusCode, 400);
+  assert.strictEqual(missingContext.body.error, 'context_kind is required for MVP voice micro-decisions.');
+
   const category = await invoke(controller, {
     transcript: 'Category: Travel',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(category.statusCode, 200);
-  assert.strictEqual(category.body.confirmation_chip, 'Applied: travel');
-  assert(
-    mockDb.calls.some((call) => call.sql.includes('INSERT INTO transaction_classifications') && call.params[2] === 'TRAVEL' && call.params[3] === 'travel'),
-    'Expected category command to persist manual classification'
-  );
+  assert.strictEqual(category.body.confirmation_chip, 'Applied: Travel');
+  assert.strictEqual(category.body.undo.supported, true);
+  assert.deepStrictEqual(category.body.context_binding, {
+    scope: 'inbox',
+    target_type: 'bank_transaction',
+    target_id: 'txn-123'
+  });
+  assert.strictEqual(mockDb.classifications.get('txn-123').category_code, 'TRAVEL');
+  assert.strictEqual(mockDb.auditEntries.at(-1).change_source, 'voice');
 
   const business = await invoke(controller, {
     transcript: 'Business',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(business.statusCode, 200);
   assert.strictEqual(business.body.confirmation_chip, 'Applied: BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').category_code, 'TRAVEL');
+  assert.strictEqual(mockDb.classifications.get('txn-123').business_personal, 'BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').is_split, false);
 
   const split = await invoke(controller, {
     transcript: 'Split 70%',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(split.statusCode, 200);
   assert.strictEqual(split.body.confirmation_chip, 'Applied: Split 70%');
+  assert.strictEqual(split.body.undo.supported, true);
+  assert.strictEqual(mockDb.classifications.get('txn-123').business_personal, 'BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').is_split, true);
+  assert.strictEqual(mockDb.classifications.get('txn-123').split_business_pct, 70);
 
   const attachReceipt = await invoke(controller, {
     transcript: 'Attach receipt',
-    bank_txn_id: 'txn-123'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(attachReceipt.statusCode, 200);
   assert.strictEqual(attachReceipt.body.action_status, 'clarification_needed');
   assert.strictEqual(attachReceipt.body.confirmation_chip, 'Attach receipt requested');
+  assert.strictEqual(attachReceipt.body.undo.supported, false);
+  assert.deepStrictEqual(attachReceipt.body.context_binding, {
+    scope: 'evidence',
+    target_type: 'evidence',
+    target_id: 'evidence-1'
+  });
 
   const matchSecond = await invoke(controller, {
     transcript: 'Match second',
-    evidence_id: 'evidence-1'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(matchSecond.statusCode, 200);
   assert.strictEqual(matchSecond.body.confirmation_chip, 'Applied: Match 2');
+  assert.deepStrictEqual(matchSecond.body.context_binding, {
+    scope: 'evidence',
+    target_type: 'evidence',
+    target_id: 'evidence-1'
+  });
   assert(
     mockDb.calls.some((call) => call.sql.includes('INSERT INTO evidence_links') && call.params[1] === 'evidence-1' && call.params[2] === 'txn-2'),
     'Expected match command to persist selected evidence link'
@@ -115,29 +203,41 @@
 
   const noMatch = await invoke(controller, {
     transcript: 'No match',
-    evidence_id: 'evidence-1'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(noMatch.statusCode, 200);
   assert.strictEqual(noMatch.body.confirmation_chip, 'Applied: No match');
 
   const invalidSplit = await invoke(controller, {
     transcript: 'Split 140%',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(invalidSplit.statusCode, 400);
   assert.strictEqual(invalidSplit.body.error, 'Split percentage must be 0-100.');
 
+  const unsafeContext = await invoke(controller, {
+    transcript: 'Category: Travel',
+    bank_txn_id: 'txn-123',
+    context_kind: 'evidence'
+  });
+  assert.strictEqual(unsafeContext.statusCode, 409);
+  assert.strictEqual(unsafeContext.body.error, 'Command "category" is not allowed for evidence context.');
+
   const unsupported = await invoke(controller, {
-    transcript: 'Archive this forever'
+    transcript: 'Archive this forever',
+    context_kind: 'inbox'
   });
   assert.strictEqual(unsupported.statusCode, 400);
   assert.strictEqual(unsupported.body.error, 'Unsupported MVP voice micro-decision command.');
 
   console.log('voice_micro_decisions_ok');
-  console.log('category_business_split_ok=true');
+  console.log('context_guardrails_ok=true');
+  console.log('audited_classification_updates_ok=true');
   console.log('attach_receipt_clarification_ok=true');
   console.log('match_selection_ok=true');
-  console.log('no_match_ok=true');
+  console.log('safe_context_binding_ok=true');
   console.log('validation_errors_ok=true');
 }
 
diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index 9b145fadac9123e3fa01b12af6ef2b9916dc11de..b21180e549e09be4a5ed0412e53087d2a52f4382
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -50,6 +50,11 @@
   buildQuarterUiState,
   deriveResolvedIssueIdsFromSearchParams,
 } from './quarterReadiness';
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -427,6 +432,13 @@
   .date-group-count { border-radius: 999px; padding: 4px 9px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.72rem; color: var(--text-muted); }
   .date-group-body { padding: 0 12px 12px; }
   .voice-preview { background: rgba(0,0,0,0.82); color: white; padding: 8px 15px; border-radius: 15px; margin-bottom: 8px; font-size: 0.72rem; max-width: 240px; text-align: right; }
+  .voice-context-banner { display: grid; gap: 10px; padding: 14px 16px; margin-bottom: 16px; border-radius: 18px; background: var(--bg-card); border: 1px solid var(--border); box-shadow: var(--shadow); }
+  .voice-context-row, .voice-chip-actions, .voice-chip-labels { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
+  .voice-context-pill, .voice-action-pill { border-radius: 999px; padding: 6px 10px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.74rem; font-weight: 700; }
+  .voice-context-pill.active { background: rgba(13,92,99,0.12); color: var(--primary-strong); border-color: rgba(13,92,99,0.24); }
+  .voice-micro-chip { display: grid; gap: 10px; padding: 14px 16px; border-radius: 18px; background: rgba(13,92,99,0.08); border: 1px solid rgba(13,92,99,0.18); }
+  .voice-micro-chip strong { display: block; }
+  .inbox-action-card.voice-target { border-color: rgba(13,92,99,0.36); box-shadow: 0 0 0 2px rgba(13,92,99,0.12), var(--shadow); }
   .spinner { animation: rotate 2s linear infinite; }
   .capture-grid { display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(320px, 0.95fr); gap: 18px; align-items: start; }
   .command-centre { display: grid; gap: 18px; }
@@ -1382,6 +1394,8 @@
   const [inboxDrafts, setInboxDrafts] = useState({});
   const [inboxBusyId, setInboxBusyId] = useState('');
   const [inboxMessage, setInboxMessage] = useState(null);
+  const [activeVoiceContextId, setActiveVoiceContextId] = useState('');
+  const [voiceMicroDecisionChip, setVoiceMicroDecisionChip] = useState(null);
   const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
@@ -1606,10 +1620,20 @@
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
   const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const activeVoiceCard = findVoiceContextCard(inboxCards, activeVoiceContextId);
   const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
   const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
   const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
+  useEffect(() => {
+    if (!activeVoiceContextId) {
+      return;
+    }
+    if (!findVoiceContextCard(inboxCards, activeVoiceContextId)) {
+      setActiveVoiceContextId('');
+    }
+  }, [activeVoiceContextId, inboxCards]);
+
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
   const canManageGovernance = hasPermission(currentUser, PERMISSIONS.GOVERNANCE_MANAGE);
@@ -1806,11 +1830,13 @@
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxDrafts({});
+        setVoiceMicroDecisionChip(null);
         setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
         return;
       }
       await axios.post(`${API_BASE_URL}/inbox/undo-last`);
       await fetchAllData();
+      setVoiceMicroDecisionChip(null);
       setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
     } catch (error) {
       setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
@@ -1991,22 +2017,51 @@
     const cleanText = text.replace(/┬ú/g, 'GBP ');
     try {
       setVoiceStatus('processing');
-      const response = await axios.post(`${API_BASE_URL}/voice/process`, {
+      const microDecisionRequest = buildVoiceMicroDecisionRequest({
+        currentTab,
         transcript: cleanText,
-        device_id: 'mobile-v139',
-        current_date: new Date().toISOString(),
+        activeVoiceContextId,
+        inboxCards,
       });
-      if (response.data.confirmation_text && window.SpeechSynthesisUtterance) {
+
+      if (microDecisionRequest?.error) {
+        setInboxMessage({ type: 'error', text: microDecisionRequest.error });
+        setCurrentTab('inbox');
+        return;
+      }
+
+      const response = microDecisionRequest
+        ? await axios.post(`${API_BASE_URL}${microDecisionRequest.endpoint}`, microDecisionRequest.payload)
+        : await axios.post(`${API_BASE_URL}/voice/process`, {
+            transcript: cleanText,
+            device_id: 'mobile-v139',
+            current_date: new Date().toISOString(),
+          });
+      const spokenConfirmation = response.data.confirmation_chip || response.data.confirmation_text;
+      if (spokenConfirmation && window.SpeechSynthesisUtterance) {
         try {
-          const utterance = new window.SpeechSynthesisUtterance(response.data.confirmation_text);
+          const utterance = new window.SpeechSynthesisUtterance(spokenConfirmation);
           window.speechSynthesis.speak(utterance);
         } catch (_) {
         }
       }
-      executeVoiceAction(response.data);
-      fetchAllData();
+      if (microDecisionRequest) {
+        setVoiceMicroDecisionChip((previous) => reduceVoiceMicroDecisionChip(previous, response.data, microDecisionRequest.card));
+        setInboxMessage({
+          type: 'success',
+          text: response.data.confirmation_chip || 'Voice triage action applied.',
+        });
+      } else {
+        executeVoiceAction(response.data);
+      }
+      await fetchAllData();
     } catch (error) {
-      alert('AI Error');
+      const message = error?.response?.data?.error || 'AI Error';
+      if (currentTab === 'inbox' || currentTab === 'quarter') {
+        setInboxMessage({ type: 'error', text: message });
+      } else {
+        alert(message);
+      }
     } finally {
       setVoiceStatus('idle');
       setIsListening(false);
@@ -2894,7 +2949,11 @@
             const saving = inboxBusyId === card.id;
             const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
             return (
-              <section key={card.id} className="inbox-action-card">
+              <section
+                key={card.id}
+                className={`inbox-action-card ${activeVoiceContextId === card.id ? 'voice-target' : ''}`}
+                onClick={() => setActiveVoiceContextId(card.id)}
+              >
                 <div className="inbox-card-top">
                   <div>
                     <strong>{card.merchant}</strong>
@@ -2914,6 +2973,9 @@
                       {label}
                     </span>
                   ))}
+                  {activeVoiceContextId === card.id && (
+                    <span className="voice-context-pill active">Voice target</span>
+                  )}
                   {card.suggested_category?.category_name && (
                     <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
                       <CheckCircle2 size={14} />
@@ -3357,6 +3419,9 @@
   };
 
   const openReadinessWorkflow = (issue) => {
+    if (issue?.affected_entity_id) {
+      setActiveVoiceContextId(String(issue.affected_entity_id));
+    }
     if (issue?.resolution_target?.workflow === '/inbox/finish-now') {
       setCurrentTab('inbox');
       return;
@@ -3364,6 +3429,76 @@
     setCurrentTab('inbox');
   };
 
+  const renderVoiceContextBanner = () => {
+    if (!(currentTab === 'inbox' || currentTab === 'quarter')) {
+      return null;
+    }
+
+    return (
+      <section className="voice-context-banner">
+        <div>
+          <strong>Voice triage</strong>
+          <div className="small-muted">
+            Use the mic against one selected finish-now item only. Supported commands here are category, business, personal, split n%, and attach receipt.
+          </div>
+        </div>
+        <div className="voice-context-row">
+          <span className={`voice-context-pill ${activeVoiceCard ? 'active' : ''}`}>
+            {activeVoiceCard
+              ? `Target: ${activeVoiceCard.merchant} (${activeVoiceCard.id})`
+              : 'Select a blocker card to arm voice triage'}
+          </span>
+          {activeVoiceCard && (
+            <button className="secondary-action" type="button" onClick={() => setCurrentTab('inbox')}>
+              Edit target
+            </button>
+          )}
+        </div>
+        {voiceMicroDecisionChip && (
+          <div className="voice-micro-chip">
+            <div>
+              <strong>{voiceMicroDecisionChip.confirmationChip}</strong>
+              <div className="small-muted">
+                {voiceMicroDecisionChip.targetLabel} ({voiceMicroDecisionChip.targetId})
+              </div>
+            </div>
+            {voiceMicroDecisionChip.labels.length > 0 && (
+              <div className="voice-chip-labels">
+                {voiceMicroDecisionChip.labels.map((label) => (
+                  <span key={`${voiceMicroDecisionChip.targetId}-${label}`} className="voice-action-pill">
+                    {label}
+                  </span>
+                ))}
+              </div>
+            )}
+            <div className="voice-chip-actions">
+              <button
+                className="secondary-action"
+                type="button"
+                onClick={() => {
+                  setCurrentTab('inbox');
+                  setActiveVoiceContextId(voiceMicroDecisionChip.targetId);
+                }}
+              >
+                Edit
+              </button>
+              {voiceMicroDecisionChip.undoSupported && (
+                <button
+                  className="secondary-action"
+                  type="button"
+                  onClick={undoLastInboxAction}
+                  disabled={inboxBusyId === 'undo-last'}
+                >
+                  Undo
+                </button>
+              )}
+            </div>
+          </div>
+        )}
+      </section>
+    );
+  };
+
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
@@ -3654,6 +3789,13 @@
                     </div>
                   </div>
                   <div className="finish-now-actions">
+                    <button
+                      className="secondary-action"
+                      type="button"
+                      onClick={() => setActiveVoiceContextId(String(issue.affected_entity_id))}
+                    >
+                      {activeVoiceContextId === String(issue.affected_entity_id) ? 'Voice target set' : 'Arm voice'}
+                    </button>
                     {(readinessDemoEnabled || snapshotDemoEnabled) ? (
                       <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
                         Mark fixed
@@ -4391,6 +4533,7 @@
         </button>
       </section>
       {connectionError && <div className="alert alert-danger">{connectionError}</div>}
+      {renderVoiceContextBanner()}
 
       {loading ? (
         <div className="text-center py-5">Loading...</div>
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..6eb469fcc7f023f8545d9e952a5823fb8c47a88d
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});

codex
The first backend verifier surfaced a real mistake in my patch: the new context-scope constant wasn’t actually present in the loaded controller. I’m correcting that, then I’ll rerun the backend check and switch the frontend test command to a single-process Jest invocation to avoid the sandbox `EPERM` worker spawn issue.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Path 'C:\\Users\\edebe\\eds\\bizPA\\backend\\src\\controllers\\voiceController.js' | Select-Object -Skip 300 -First 40" in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Path 'C:\\Users\\edebe\\eds\\bizPA\\backend\\src\\controllers\\voiceController.js' | Select-Object -First 40" in C:\Users\edebe\eds succeeded in 1.90s:
    transcript = '',
    bank_txn_id = null,
    evidence_id = null,
    context_kind = null,
    context_scope = null,
  } = req.body || {};
  const rawTranscript = String(transcript).trim();
  const text = rawTranscript.toLowerCase();
  const normalizedContext = context_kind ?? context_scope;
  const parsedCommand = parseVoiceMicroDecision(rawTranscript);
  const contextValidation = validateMicroDecisionContext(parsedCommand, normalizedContext);
  const normalizedContextScope = contextValidation.ok
    ? contextValidation.context_kind
    : (normalizedContext === null || normalizedContext === undefined
      ? null
      : String(normalizedContext).toLowerCase().replace(/-/g, '_'));

  if (!text) return res.status(400).json({ error: 'Missing transcript.' });
  if (!contextValidation.ok) return res.status(contextValidation.statusCode).json({ error: contextValidation.error });

 succeeded in 2.30s:
const db = require('../config/db');
const actionController = require('./actionController');
const searchController = require('./searchController');
const itemController = require('./itemController');
const { isMonetaryItemType } = require('../services/monetaryIntegrityService');
const {
  evaluateAutoCommitEligibility
} = require('../services/autoCommitGovernanceService');
const {
  buildCaptureParseResult,
  toLegacyIntentShape
} = require('../services/voiceCaptureParserService');
const {
  upsertTransactionClassification,
} = require('../services/inboxClassificationService');
const {
  parseVoiceMicroDecision,
  validateMicroDecisionContext,
} = require('../services/voiceMicroDecisionService');

file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..e6d93b4bc1cd6960ce27238cfb7e90c3322f41e0
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,6 +10,36 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+const {
+  parseVoiceMicroDecision,
+  validateMicroDecisionContext,
+} = require('../services/voiceMicroDecisionService');
+
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undoable: undoSupported,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
 
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
@@ -267,76 +297,177 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
-  const text = String(transcript).toLowerCase().trim();
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_kind = null,
+    context_scope = null,
+  } = req.body || {};
+  const rawTranscript = String(transcript).trim();
+  const text = rawTranscript.toLowerCase();
+  const normalizedContext = context_kind ?? context_scope;
+  const parsedCommand = parseVoiceMicroDecision(rawTranscript);
+  const contextValidation = validateMicroDecisionContext(parsedCommand, normalizedContext);
+  const normalizedContextScope = contextValidation.ok
+    ? contextValidation.context_kind
+    : (normalizedContext === null || normalizedContext === undefined
+      ? null
+      : String(normalizedContext).toLowerCase().replace(/-/g, '_'));
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
+  if (!contextValidation.ok) return res.status(contextValidation.statusCode).json({ error: contextValidation.error });
 
   try {
-    if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
-      const categoryName = text.replace('category:', '').trim();
-      const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+    if (parsedCommand.type === 'category') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: parsedCommand.category_code,
+            category_name: parsedCommand.category_name,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'category', label: parsedCommand.category_name }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
-      const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+    if (parsedCommand.type === 'business_personal') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const bp = parsedCommand.business_personal;
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    const splitMatch = text.match(/split\s+(\d{1,3})%?/);
-    if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
-      const pct = Number(splitMatch[1]);
+    if (parsedCommand.type === 'split') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const pct = Number(parsedCommand.split_business_pct);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+    if (parsedCommand.type === 'attach_receipt') {
+      if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for attach receipt commands.' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          parsed_command: parsedCommand,
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
-    if (['match first', 'match second', 'match third'].includes(text)) {
+    if (parsedCommand.type === 'match_rank') {
       if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for match commands.' });
-      const index = text === 'match first' ? 1 : text === 'match second' ? 2 : 3;
+      const index = parsedCommand.match_rank;
       const suggestions = await db.query(
         `
         WITH ev AS (
@@ -364,10 +495,24 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          bank_txn_id: chosen,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text === 'no match') {
+    if (parsedCommand.type === 'no_match') {
       if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for no match.' });
       await db.query(
         `
@@ -376,7 +521,20 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/backend/verify_voice_micro_decisions.js b/backend/verify_voice_micro_decisions.js
index 25d9ab02050aac37a5d38aca5c6f0154de9bb787..15c93efa65f133ec1a59d827ecbfa29a95cf4e6d
--- a/backend/verify_voice_micro_decisions.js
+++ b/backend/verify_voice_micro_decisions.js
@@ -9,16 +9,70 @@
       { bank_txn_id: 'txn-2' },
       { bank_txn_id: 'txn-3' }
     ];
+    this.classifications = new Map();
+    this.auditEntries = [];
   }
 
-  async query(text, params) {
+  get pool() {
+    return {
+      connect: async () => ({
+        query: this.query.bind(this),
+        release() {},
+      }),
+    };
+  }
+
+  async query(text, params = []) {
     const sql = text.replace(/\s+/g, ' ').trim();
     this.calls.push({ sql, params });
 
+    if (sql === 'BEGIN' || sql === 'COMMIT' || sql === 'ROLLBACK') {
+      return { rows: [] };
+    }
+
+    if (sql.includes('SELECT * FROM transaction_classifications')) {
+      const existing = this.classifications.get(params[0]);
+      return { rows: existing ? [existing] : [] };
+    }
+
+    if (sql.includes('INSERT INTO transaction_classifications')) {
+      const next = {
+        user_id: params[0],
+        bank_txn_id: params[1],
+        category_code: params[2],
+        category_name: params[3],
+        business_personal: params[4],
+        is_split: params[5],
+        split_business_pct: params[6],
+        confidence: params[7],
+        source: params[8],
+      };
+      this.classifications.set(params[1], next);
+      return { rows: [next] };
+    }
+
+    if (sql.includes('INSERT INTO transaction_audit_log')) {
+      this.auditEntries.push({
+        bank_txn_id: params[1],
+        previous_value: params[4],
+        new_value: params[5],
+        change_source: params[6],
+      });
+      return { rows: [] };
+    }
+
+    if (sql.includes('SELECT txn_date FROM bank_transactions')) {
+      return { rows: [{ txn_date: '2026-03-12' }] };
+    }
+
     if (sql.includes('FROM bank_transactions bt CROSS JOIN ev')) {
       return { rows: this.matchSuggestions };
     }
 
+    if (sql.includes('INSERT INTO evidence_links')) {
+      return { rows: [] };
+    }
+
     return { rows: [] };
   }
 }
@@ -69,45 +123,79 @@
   const mockDb = new MockDb();
   const controller = loadController(mockDb);
 
+  const missingContext = await invoke(controller, {
+    transcript: 'Business',
+    bank_txn_id: 'txn-123'
+  });
+  assert.strictEqual(missingContext.statusCode, 400);
+  assert.strictEqual(missingContext.body.error, 'context_kind is required for MVP voice micro-decisions.');
+
   const category = await invoke(controller, {
     transcript: 'Category: Travel',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(category.statusCode, 200);
-  assert.strictEqual(category.body.confirmation_chip, 'Applied: travel');
-  assert(
-    mockDb.calls.some((call) => call.sql.includes('INSERT INTO transaction_classifications') && call.params[2] === 'TRAVEL' && call.params[3] === 'travel'),
-    'Expected category command to persist manual classification'
-  );
+  assert.strictEqual(category.body.confirmation_chip, 'Applied: Travel');
+  assert.strictEqual(category.body.undo.supported, true);
+  assert.deepStrictEqual(category.body.context_binding, {
+    scope: 'inbox',
+    target_type: 'bank_transaction',
+    target_id: 'txn-123'
+  });
+  assert.strictEqual(mockDb.classifications.get('txn-123').category_code, 'TRAVEL');
+  assert.strictEqual(mockDb.auditEntries.at(-1).change_source, 'voice');
 
   const business = await invoke(controller, {
     transcript: 'Business',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(business.statusCode, 200);
   assert.strictEqual(business.body.confirmation_chip, 'Applied: BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').category_code, 'TRAVEL');
+  assert.strictEqual(mockDb.classifications.get('txn-123').business_personal, 'BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').is_split, false);
 
   const split = await invoke(controller, {
     transcript: 'Split 70%',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(split.statusCode, 200);
   assert.strictEqual(split.body.confirmation_chip, 'Applied: Split 70%');
+  assert.strictEqual(split.body.undo.supported, true);
+  assert.strictEqual(mockDb.classifications.get('txn-123').business_personal, 'BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').is_split, true);
+  assert.strictEqual(mockDb.classifications.get('txn-123').split_business_pct, 70);
 
   const attachReceipt = await invoke(controller, {
     transcript: 'Attach receipt',
-    bank_txn_id: 'txn-123'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(attachReceipt.statusCode, 200);
   assert.strictEqual(attachReceipt.body.action_status, 'clarification_needed');
   assert.strictEqual(attachReceipt.body.confirmation_chip, 'Attach receipt requested');
+  assert.strictEqual(attachReceipt.body.undo.supported, false);
+  assert.deepStrictEqual(attachReceipt.body.context_binding, {
+    scope: 'evidence',
+    target_type: 'evidence',
+    target_id: 'evidence-1'
+  });
 
   const matchSecond = await invoke(controller, {
     transcript: 'Match second',
-    evidence_id: 'evidence-1'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(matchSecond.statusCode, 200);
   assert.strictEqual(matchSecond.body.confirmation_chip, 'Applied: Match 2');
+  assert.deepStrictEqual(matchSecond.body.context_binding, {
+    scope: 'evidence',
+    target_type: 'evidence',
+    target_id: 'evidence-1'
+  });
   assert(
     mockDb.calls.some((call) => call.sql.includes('INSERT INTO evidence_links') && call.params[1] === 'evidence-1' && call.params[2] === 'txn-2'),
     'Expected match command to persist selected evidence link'
@@ -115,29 +203,41 @@
 
   const noMatch = await invoke(controller, {
     transcript: 'No match',
-    evidence_id: 'evidence-1'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(noMatch.statusCode, 200);
   assert.strictEqual(noMatch.body.confirmation_chip, 'Applied: No match');
 
   const invalidSplit = await invoke(controller, {
     transcript: 'Split 140%',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(invalidSplit.statusCode, 400);
   assert.strictEqual(invalidSplit.body.error, 'Split percentage must be 0-100.');
 
+  const unsafeContext = await invoke(controller, {
+    transcript: 'Category: Travel',
+    bank_txn_id: 'txn-123',
+    context_kind: 'evidence'
+  });
+  assert.strictEqual(unsafeContext.statusCode, 409);
+  assert.strictEqual(unsafeContext.body.error, 'Command "category" is not allowed for evidence context.');
+
   const unsupported = await invoke(controller, {
-    transcript: 'Archive this forever'
+    transcript: 'Archive this forever',
+    context_kind: 'inbox'
   });
   assert.strictEqual(unsupported.statusCode, 400);
   assert.strictEqual(unsupported.body.error, 'Unsupported MVP voice micro-decision command.');
 
   console.log('voice_micro_decisions_ok');
-  console.log('category_business_split_ok=true');
+  console.log('context_guardrails_ok=true');
+  console.log('audited_classification_updates_ok=true');
   console.log('attach_receipt_clarification_ok=true');
   console.log('match_selection_ok=true');
-  console.log('no_match_ok=true');
+  console.log('safe_context_binding_ok=true');
   console.log('validation_errors_ok=true');
 }
 
diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index 9b145fadac9123e3fa01b12af6ef2b9916dc11de..b21180e549e09be4a5ed0412e53087d2a52f4382
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -50,6 +50,11 @@
   buildQuarterUiState,
   deriveResolvedIssueIdsFromSearchParams,
 } from './quarterReadiness';
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -427,6 +432,13 @@
   .date-group-count { border-radius: 999px; padding: 4px 9px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.72rem; color: var(--text-muted); }
   .date-group-body { padding: 0 12px 12px; }
   .voice-preview { background: rgba(0,0,0,0.82); color: white; padding: 8px 15px; border-radius: 15px; margin-bottom: 8px; font-size: 0.72rem; max-width: 240px; text-align: right; }
+  .voice-context-banner { display: grid; gap: 10px; padding: 14px 16px; margin-bottom: 16px; border-radius: 18px; background: var(--bg-card); border: 1px solid var(--border); box-shadow: var(--shadow); }
+  .voice-context-row, .voice-chip-actions, .voice-chip-labels { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
+  .voice-context-pill, .voice-action-pill { border-radius: 999px; padding: 6px 10px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.74rem; font-weight: 700; }
+  .voice-context-pill.active { background: rgba(13,92,99,0.12); color: var(--primary-strong); border-color: rgba(13,92,99,0.24); }
+  .voice-micro-chip { display: grid; gap: 10px; padding: 14px 16px; border-radius: 18px; background: rgba(13,92,99,0.08); border: 1px solid rgba(13,92,99,0.18); }
+  .voice-micro-chip strong { display: block; }
+  .inbox-action-card.voice-target { border-color: rgba(13,92,99,0.36); box-shadow: 0 0 0 2px rgba(13,92,99,0.12), var(--shadow); }
   .spinner { animation: rotate 2s linear infinite; }
   .capture-grid { display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(320px, 0.95fr); gap: 18px; align-items: start; }
   .command-centre { display: grid; gap: 18px; }
@@ -1382,6 +1394,8 @@
   const [inboxDrafts, setInboxDrafts] = useState({});
   const [inboxBusyId, setInboxBusyId] = useState('');
   const [inboxMessage, setInboxMessage] = useState(null);
+  const [activeVoiceContextId, setActiveVoiceContextId] = useState('');
+  const [voiceMicroDecisionChip, setVoiceMicroDecisionChip] = useState(null);
   const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
@@ -1606,10 +1620,20 @@
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
   const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const activeVoiceCard = findVoiceContextCard(inboxCards, activeVoiceContextId);
   const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
   const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
   const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
+  useEffect(() => {
+    if (!activeVoiceContextId) {
+      return;
+    }
+    if (!findVoiceContextCard(inboxCards, activeVoiceContextId)) {
+      setActiveVoiceContextId('');
+    }
+  }, [activeVoiceContextId, inboxCards]);
+
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
   const canManageGovernance = hasPermission(currentUser, PERMISSIONS.GOVERNANCE_MANAGE);
@@ -1806,11 +1830,13 @@
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxDrafts({});
+        setVoiceMicroDecisionChip(null);
         setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
         return;
       }
       await axios.post(`${API_BASE_URL}/inbox/undo-last`);
       await fetchAllData();
+      setVoiceMicroDecisionChip(null);
       setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
     } catch (error) {
       setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
@@ -1991,22 +2017,51 @@
     const cleanText = text.replace(/┬ú/g, 'GBP ');
     try {
       setVoiceStatus('processing');
-      const response = await axios.post(`${API_BASE_URL}/voice/process`, {
+      const microDecisionRequest = buildVoiceMicroDecisionRequest({
+        currentTab,
         transcript: cleanText,
-        device_id: 'mobile-v139',
-        current_date: new Date().toISOString(),
+        activeVoiceContextId,
+        inboxCards,
       });
-      if (response.data.confirmation_text && window.SpeechSynthesisUtterance) {
+
+      if (microDecisionRequest?.error) {
+        setInboxMessage({ type: 'error', text: microDecisionRequest.error });
+        setCurrentTab('inbox');
+        return;
+      }
+
+      const response = microDecisionRequest
+        ? await axios.post(`${API_BASE_URL}${microDecisionRequest.endpoint}`, microDecisionRequest.payload)
+        : await axios.post(`${API_BASE_URL}/voice/process`, {
+            transcript: cleanText,
+            device_id: 'mobile-v139',
+            current_date: new Date().toISOString(),
+          });
+      const spokenConfirmation = response.data.confirmation_chip || response.data.confirmation_text;
+      if (spokenConfirmation && window.SpeechSynthesisUtterance) {
         try {
-          const utterance = new window.SpeechSynthesisUtterance(response.data.confirmation_text);
+          const utterance = new window.SpeechSynthesisUtterance(spokenConfirmation);
           window.speechSynthesis.speak(utterance);
         } catch (_) {
         }
       }
-      executeVoiceAction(response.data);
-      fetchAllData();
+      if (microDecisionRequest) {
+        setVoiceMicroDecisionChip((previous) => reduceVoiceMicroDecisionChip(previous, response.data, microDecisionRequest.card));
+        setInboxMessage({
+          type: 'success',
+          text: response.data.confirmation_chip || 'Voice triage action applied.',
+        });
+      } else {
+        executeVoiceAction(response.data);
+      }
+      await fetchAllData();
     } catch (error) {
-      alert('AI Error');
+      const message = error?.response?.data?.error || 'AI Error';
+      if (currentTab === 'inbox' || currentTab === 'quarter') {
+        setInboxMessage({ type: 'error', text: message });
+      } else {
+        alert(message);
+      }
     } finally {
       setVoiceStatus('idle');
       setIsListening(false);
@@ -2894,7 +2949,11 @@
             const saving = inboxBusyId === card.id;
             const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
             return (
-              <section key={card.id} className="inbox-action-card">
+              <section
+                key={card.id}
+                className={`inbox-action-card ${activeVoiceContextId === card.id ? 'voice-target' : ''}`}
+                onClick={() => setActiveVoiceContextId(card.id)}
+              >
                 <div className="inbox-card-top">
                   <div>
                     <strong>{card.merchant}</strong>
@@ -2914,6 +2973,9 @@
                       {label}
                     </span>
                   ))}
+                  {activeVoiceContextId === card.id && (
+                    <span className="voice-context-pill active">Voice target</span>
+                  )}
                   {card.suggested_category?.category_name && (
                     <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
                       <CheckCircle2 size={14} />
@@ -3357,6 +3419,9 @@
   };
 
   const openReadinessWorkflow = (issue) => {
+    if (issue?.affected_entity_id) {
+      setActiveVoiceContextId(String(issue.affected_entity_id));
+    }
     if (issue?.resolution_target?.workflow === '/inbox/finish-now') {
       setCurrentTab('inbox');
       return;
@@ -3364,6 +3429,76 @@
     setCurrentTab('inbox');
   };
 
+  const renderVoiceContextBanner = () => {
+    if (!(currentTab === 'inbox' || currentTab === 'quarter')) {
+      return null;
+    }
+
+    return (
+      <section className="voice-context-banner">
+        <div>
+          <strong>Voice triage</strong>
+          <div className="small-muted">
+            Use the mic against one selected finish-now item only. Supported commands here are category, business, personal, split n%, and attach receipt.
+          </div>
+        </div>
+        <div className="voice-context-row">
+          <span className={`voice-context-pill ${activeVoiceCard ? 'active' : ''}`}>
+            {activeVoiceCard
+              ? `Target: ${activeVoiceCard.merchant} (${activeVoiceCard.id})`
+              : 'Select a blocker card to arm voice triage'}
+          </span>
+          {activeVoiceCard && (
+            <button className="secondary-action" type="button" onClick={() => setCurrentTab('inbox')}>
+              Edit target
+            </button>
+          )}
+        </div>
+        {voiceMicroDecisionChip && (
+          <div className="voice-micro-chip">
+            <div>
+              <strong>{voiceMicroDecisionChip.confirmationChip}</strong>
+              <div className="small-muted">
+                {voiceMicroDecisionChip.targetLabel} ({voiceMicroDecisionChip.targetId})
+              </div>
+            </div>
+            {voiceMicroDecisionChip.labels.length > 0 && (
+              <div className="voice-chip-labels">
+                {voiceMicroDecisionChip.labels.map((label) => (
+                  <span key={`${voiceMicroDecisionChip.targetId}-${label}`} className="voice-action-pill">
+                    {label}
+                  </span>
+                ))}
+              </div>
+            )}
+            <div className="voice-chip-actions">
+              <button
+                className="secondary-action"
+                type="button"
+                onClick={() => {
+                  setCurrentTab('inbox');
+                  setActiveVoiceContextId(voiceMicroDecisionChip.targetId);
+                }}
+              >
+                Edit
+              </button>
+              {voiceMicroDecisionChip.undoSupported && (
+                <button
+                  className="secondary-action"
+                  type="button"
+                  onClick={undoLastInboxAction}
+                  disabled={inboxBusyId === 'undo-last'}
+                >
+                  Undo
+                </button>
+              )}
+            </div>
+          </div>
+        )}
+      </section>
+    );
+  };
+
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
@@ -3654,6 +3789,13 @@
                     </div>
                   </div>
                   <div className="finish-now-actions">
+                    <button
+                      className="secondary-action"
+                      type="button"
+                      onClick={() => setActiveVoiceContextId(String(issue.affected_entity_id))}
+                    >
+                      {activeVoiceContextId === String(issue.affected_entity_id) ? 'Voice target set' : 'Arm voice'}
+                    </button>
                     {(readinessDemoEnabled || snapshotDemoEnabled) ? (
                       <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
                         Mark fixed
@@ -4391,6 +4533,7 @@
         </button>
       </section>
       {connectionError && <div className="alert alert-danger">{connectionError}</div>}
+      {renderVoiceContextBanner()}
 
       {loading ? (
         <div className="text-center py-5">Loading...</div>
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..6eb469fcc7f023f8545d9e952a5823fb8c47a88d
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Path 'C:\\Users\\edebe\\eds\\bizPA\\backend\\src\\services\\voiceMicroDecisionService.js'" in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n \"MICRO_DECISION_CONTEXT_SCOPES|requireTransactionContext\" 'C:\\Users\\edebe\\eds\\bizPA\\backend\\src\\controllers\\voiceController.js'" in C:\Users\edebe\eds succeeded in 2.37s:
const INBOX_CONTEXT = 'inbox';
const EVIDENCE_CONTEXT = 'evidence';

const CONTEXT_ALLOWED_COMMANDS = {
  [INBOX_CONTEXT]: new Set(['category', 'business_personal', 'split']),
  [EVIDENCE_CONTEXT]: new Set(['attach_receipt', 'match_rank', 'no_match']),
};

const normalizeTranscript = (transcript = '') => String(transcript).trim();

const parseVoiceMicroDecision = (transcript = '') => {
  const raw = normalizeTranscript(transcript);
  const text = raw.toLowerCase();
  if (!raw) {
    return null;
  }

  const categoryMatch = raw.match(/^category\s*:\s*(.+)$/i);
  if (categoryMatch) {
    const categoryName = categoryMatch[1].trim();
 exited 1 in 3.10s:

file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..e6d93b4bc1cd6960ce27238cfb7e90c3322f41e0
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,6 +10,36 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+const {
+  parseVoiceMicroDecision,
+  validateMicroDecisionContext,
+} = require('../services/voiceMicroDecisionService');
+
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undoable: undoSupported,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
 
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
@@ -267,76 +297,177 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
-  const text = String(transcript).toLowerCase().trim();
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_kind = null,
+    context_scope = null,
+  } = req.body || {};
+  const rawTranscript = String(transcript).trim();
+  const text = rawTranscript.toLowerCase();
+  const normalizedContext = context_kind ?? context_scope;
+  const parsedCommand = parseVoiceMicroDecision(rawTranscript);
+  const contextValidation = validateMicroDecisionContext(parsedCommand, normalizedContext);
+  const normalizedContextScope = contextValidation.ok
+    ? contextValidation.context_kind
+    : (normalizedContext === null || normalizedContext === undefined
+      ? null
+      : String(normalizedContext).toLowerCase().replace(/-/g, '_'));
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
+  if (!contextValidation.ok) return res.status(contextValidation.statusCode).json({ error: contextValidation.error });
 
   try {
-    if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
-      const categoryName = text.replace('category:', '').trim();
-      const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+    if (parsedCommand.type === 'category') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: parsedCommand.category_code,
+            category_name: parsedCommand.category_name,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'category', label: parsedCommand.category_name }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
-      const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+    if (parsedCommand.type === 'business_personal') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const bp = parsedCommand.business_personal;
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    const splitMatch = text.match(/split\s+(\d{1,3})%?/);
-    if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
-      const pct = Number(splitMatch[1]);
+    if (parsedCommand.type === 'split') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const pct = Number(parsedCommand.split_business_pct);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+    if (parsedCommand.type === 'attach_receipt') {
+      if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for attach receipt commands.' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          parsed_command: parsedCommand,
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
-    if (['match first', 'match second', 'match third'].includes(text)) {
+    if (parsedCommand.type === 'match_rank') {
       if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for match commands.' });
-      const index = text === 'match first' ? 1 : text === 'match second' ? 2 : 3;
+      const index = parsedCommand.match_rank;
       const suggestions = await db.query(
         `
         WITH ev AS (
@@ -364,10 +495,24 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          bank_txn_id: chosen,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text === 'no match') {
+    if (parsedCommand.type === 'no_match') {
       if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for no match.' });
       await db.query(
         `
@@ -376,7 +521,20 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/backend/verify_voice_micro_decisions.js b/backend/verify_voice_micro_decisions.js
index 25d9ab02050aac37a5d38aca5c6f0154de9bb787..15c93efa65f133ec1a59d827ecbfa29a95cf4e6d
--- a/backend/verify_voice_micro_decisions.js
+++ b/backend/verify_voice_micro_decisions.js
@@ -9,16 +9,70 @@
       { bank_txn_id: 'txn-2' },
       { bank_txn_id: 'txn-3' }
     ];
+    this.classifications = new Map();
+    this.auditEntries = [];
   }
 
-  async query(text, params) {
+  get pool() {
+    return {
+      connect: async () => ({
+        query: this.query.bind(this),
+        release() {},
+      }),
+    };
+  }
+
+  async query(text, params = []) {
     const sql = text.replace(/\s+/g, ' ').trim();
     this.calls.push({ sql, params });
 
+    if (sql === 'BEGIN' || sql === 'COMMIT' || sql === 'ROLLBACK') {
+      return { rows: [] };
+    }
+
+    if (sql.includes('SELECT * FROM transaction_classifications')) {
+      const existing = this.classifications.get(params[0]);
+      return { rows: existing ? [existing] : [] };
+    }
+
+    if (sql.includes('INSERT INTO transaction_classifications')) {
+      const next = {
+        user_id: params[0],
+        bank_txn_id: params[1],
+        category_code: params[2],
+        category_name: params[3],
+        business_personal: params[4],
+        is_split: params[5],
+        split_business_pct: params[6],
+        confidence: params[7],
+        source: params[8],
+      };
+      this.classifications.set(params[1], next);
+      return { rows: [next] };
+    }
+
+    if (sql.includes('INSERT INTO transaction_audit_log')) {
+      this.auditEntries.push({
+        bank_txn_id: params[1],
+        previous_value: params[4],
+        new_value: params[5],
+        change_source: params[6],
+      });
+      return { rows: [] };
+    }
+
+    if (sql.includes('SELECT txn_date FROM bank_transactions')) {
+      return { rows: [{ txn_date: '2026-03-12' }] };
+    }
+
     if (sql.includes('FROM bank_transactions bt CROSS JOIN ev')) {
       return { rows: this.matchSuggestions };
     }
 
+    if (sql.includes('INSERT INTO evidence_links')) {
+      return { rows: [] };
+    }
+
     return { rows: [] };
   }
 }
@@ -69,45 +123,79 @@
   const mockDb = new MockDb();
   const controller = loadController(mockDb);
 
+  const missingContext = await invoke(controller, {
+    transcript: 'Business',
+    bank_txn_id: 'txn-123'
+  });
+  assert.strictEqual(missingContext.statusCode, 400);
+  assert.strictEqual(missingContext.body.error, 'context_kind is required for MVP voice micro-decisions.');
+
   const category = await invoke(controller, {
     transcript: 'Category: Travel',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(category.statusCode, 200);
-  assert.strictEqual(category.body.confirmation_chip, 'Applied: travel');
-  assert(
-    mockDb.calls.some((call) => call.sql.includes('INSERT INTO transaction_classifications') && call.params[2] === 'TRAVEL' && call.params[3] === 'travel'),
-    'Expected category command to persist manual classification'
-  );
+  assert.strictEqual(category.body.confirmation_chip, 'Applied: Travel');
+  assert.strictEqual(category.body.undo.supported, true);
+  assert.deepStrictEqual(category.body.context_binding, {
+    scope: 'inbox',
+    target_type: 'bank_transaction',
+    target_id: 'txn-123'
+  });
+  assert.strictEqual(mockDb.classifications.get('txn-123').category_code, 'TRAVEL');
+  assert.strictEqual(mockDb.auditEntries.at(-1).change_source, 'voice');
 
   const business = await invoke(controller, {
     transcript: 'Business',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(business.statusCode, 200);
   assert.strictEqual(business.body.confirmation_chip, 'Applied: BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').category_code, 'TRAVEL');
+  assert.strictEqual(mockDb.classifications.get('txn-123').business_personal, 'BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').is_split, false);
 
   const split = await invoke(controller, {
     transcript: 'Split 70%',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(split.statusCode, 200);
   assert.strictEqual(split.body.confirmation_chip, 'Applied: Split 70%');
+  assert.strictEqual(split.body.undo.supported, true);
+  assert.strictEqual(mockDb.classifications.get('txn-123').business_personal, 'BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').is_split, true);
+  assert.strictEqual(mockDb.classifications.get('txn-123').split_business_pct, 70);
 
   const attachReceipt = await invoke(controller, {
     transcript: 'Attach receipt',
-    bank_txn_id: 'txn-123'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(attachReceipt.statusCode, 200);
   assert.strictEqual(attachReceipt.body.action_status, 'clarification_needed');
   assert.strictEqual(attachReceipt.body.confirmation_chip, 'Attach receipt requested');
+  assert.strictEqual(attachReceipt.body.undo.supported, false);
+  assert.deepStrictEqual(attachReceipt.body.context_binding, {
+    scope: 'evidence',
+    target_type: 'evidence',
+    target_id: 'evidence-1'
+  });
 
   const matchSecond = await invoke(controller, {
     transcript: 'Match second',
-    evidence_id: 'evidence-1'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(matchSecond.statusCode, 200);
   assert.strictEqual(matchSecond.body.confirmation_chip, 'Applied: Match 2');
+  assert.deepStrictEqual(matchSecond.body.context_binding, {
+    scope: 'evidence',
+    target_type: 'evidence',
+    target_id: 'evidence-1'
+  });
   assert(
     mockDb.calls.some((call) => call.sql.includes('INSERT INTO evidence_links') && call.params[1] === 'evidence-1' && call.params[2] === 'txn-2'),
     'Expected match command to persist selected evidence link'
@@ -115,29 +203,41 @@
 
   const noMatch = await invoke(controller, {
     transcript: 'No match',
-    evidence_id: 'evidence-1'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(noMatch.statusCode, 200);
   assert.strictEqual(noMatch.body.confirmation_chip, 'Applied: No match');
 
   const invalidSplit = await invoke(controller, {
     transcript: 'Split 140%',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(invalidSplit.statusCode, 400);
   assert.strictEqual(invalidSplit.body.error, 'Split percentage must be 0-100.');
 
+  const unsafeContext = await invoke(controller, {
+    transcript: 'Category: Travel',
+    bank_txn_id: 'txn-123',
+    context_kind: 'evidence'
+  });
+  assert.strictEqual(unsafeContext.statusCode, 409);
+  assert.strictEqual(unsafeContext.body.error, 'Command "category" is not allowed for evidence context.');
+
   const unsupported = await invoke(controller, {
-    transcript: 'Archive this forever'
+    transcript: 'Archive this forever',
+    context_kind: 'inbox'
   });
   assert.strictEqual(unsupported.statusCode, 400);
   assert.strictEqual(unsupported.body.error, 'Unsupported MVP voice micro-decision command.');
 
   console.log('voice_micro_decisions_ok');
-  console.log('category_business_split_ok=true');
+  console.log('context_guardrails_ok=true');
+  console.log('audited_classification_updates_ok=true');
   console.log('attach_receipt_clarification_ok=true');
   console.log('match_selection_ok=true');
-  console.log('no_match_ok=true');
+  console.log('safe_context_binding_ok=true');
   console.log('validation_errors_ok=true');
 }
 
diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index 9b145fadac9123e3fa01b12af6ef2b9916dc11de..b21180e549e09be4a5ed0412e53087d2a52f4382
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -50,6 +50,11 @@
   buildQuarterUiState,
   deriveResolvedIssueIdsFromSearchParams,
 } from './quarterReadiness';
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -427,6 +432,13 @@
   .date-group-count { border-radius: 999px; padding: 4px 9px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.72rem; color: var(--text-muted); }
   .date-group-body { padding: 0 12px 12px; }
   .voice-preview { background: rgba(0,0,0,0.82); color: white; padding: 8px 15px; border-radius: 15px; margin-bottom: 8px; font-size: 0.72rem; max-width: 240px; text-align: right; }
+  .voice-context-banner { display: grid; gap: 10px; padding: 14px 16px; margin-bottom: 16px; border-radius: 18px; background: var(--bg-card); border: 1px solid var(--border); box-shadow: var(--shadow); }
+  .voice-context-row, .voice-chip-actions, .voice-chip-labels { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
+  .voice-context-pill, .voice-action-pill { border-radius: 999px; padding: 6px 10px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.74rem; font-weight: 700; }
+  .voice-context-pill.active { background: rgba(13,92,99,0.12); color: var(--primary-strong); border-color: rgba(13,92,99,0.24); }
+  .voice-micro-chip { display: grid; gap: 10px; padding: 14px 16px; border-radius: 18px; background: rgba(13,92,99,0.08); border: 1px solid rgba(13,92,99,0.18); }
+  .voice-micro-chip strong { display: block; }
+  .inbox-action-card.voice-target { border-color: rgba(13,92,99,0.36); box-shadow: 0 0 0 2px rgba(13,92,99,0.12), var(--shadow); }
   .spinner { animation: rotate 2s linear infinite; }
   .capture-grid { display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(320px, 0.95fr); gap: 18px; align-items: start; }
   .command-centre { display: grid; gap: 18px; }
@@ -1382,6 +1394,8 @@
   const [inboxDrafts, setInboxDrafts] = useState({});
   const [inboxBusyId, setInboxBusyId] = useState('');
   const [inboxMessage, setInboxMessage] = useState(null);
+  const [activeVoiceContextId, setActiveVoiceContextId] = useState('');
+  const [voiceMicroDecisionChip, setVoiceMicroDecisionChip] = useState(null);
   const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
@@ -1606,10 +1620,20 @@
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
   const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const activeVoiceCard = findVoiceContextCard(inboxCards, activeVoiceContextId);
   const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
   const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
   const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
+  useEffect(() => {
+    if (!activeVoiceContextId) {
+      return;
+    }
+    if (!findVoiceContextCard(inboxCards, activeVoiceContextId)) {
+      setActiveVoiceContextId('');
+    }
+  }, [activeVoiceContextId, inboxCards]);
+
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
   const canManageGovernance = hasPermission(currentUser, PERMISSIONS.GOVERNANCE_MANAGE);
@@ -1806,11 +1830,13 @@
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxDrafts({});
+        setVoiceMicroDecisionChip(null);
         setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
         return;
       }
       await axios.post(`${API_BASE_URL}/inbox/undo-last`);
       await fetchAllData();
+      setVoiceMicroDecisionChip(null);
       setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
     } catch (error) {
       setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
@@ -1991,22 +2017,51 @@
     const cleanText = text.replace(/┬ú/g, 'GBP ');
     try {
       setVoiceStatus('processing');
-      const response = await axios.post(`${API_BASE_URL}/voice/process`, {
+      const microDecisionRequest = buildVoiceMicroDecisionRequest({
+        currentTab,
         transcript: cleanText,
-        device_id: 'mobile-v139',
-        current_date: new Date().toISOString(),
+        activeVoiceContextId,
+        inboxCards,
       });
-      if (response.data.confirmation_text && window.SpeechSynthesisUtterance) {
+
+      if (microDecisionRequest?.error) {
+        setInboxMessage({ type: 'error', text: microDecisionRequest.error });
+        setCurrentTab('inbox');
+        return;
+      }
+
+      const response = microDecisionRequest
+        ? await axios.post(`${API_BASE_URL}${microDecisionRequest.endpoint}`, microDecisionRequest.payload)
+        : await axios.post(`${API_BASE_URL}/voice/process`, {
+            transcript: cleanText,
+            device_id: 'mobile-v139',
+            current_date: new Date().toISOString(),
+          });
+      const spokenConfirmation = response.data.confirmation_chip || response.data.confirmation_text;
+      if (spokenConfirmation && window.SpeechSynthesisUtterance) {
         try {
-          const utterance = new window.SpeechSynthesisUtterance(response.data.confirmation_text);
+          const utterance = new window.SpeechSynthesisUtterance(spokenConfirmation);
           window.speechSynthesis.speak(utterance);
         } catch (_) {
         }
       }
-      executeVoiceAction(response.data);
-      fetchAllData();
+      if (microDecisionRequest) {
+        setVoiceMicroDecisionChip((previous) => reduceVoiceMicroDecisionChip(previous, response.data, microDecisionRequest.card));
+        setInboxMessage({
+          type: 'success',
+          text: response.data.confirmation_chip || 'Voice triage action applied.',
+        });
+      } else {
+        executeVoiceAction(response.data);
+      }
+      await fetchAllData();
     } catch (error) {
-      alert('AI Error');
+      const message = error?.response?.data?.error || 'AI Error';
+      if (currentTab === 'inbox' || currentTab === 'quarter') {
+        setInboxMessage({ type: 'error', text: message });
+      } else {
+        alert(message);
+      }
     } finally {
       setVoiceStatus('idle');
       setIsListening(false);
@@ -2894,7 +2949,11 @@
             const saving = inboxBusyId === card.id;
             const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
             return (
-              <section key={card.id} className="inbox-action-card">
+              <section
+                key={card.id}
+                className={`inbox-action-card ${activeVoiceContextId === card.id ? 'voice-target' : ''}`}
+                onClick={() => setActiveVoiceContextId(card.id)}
+              >
                 <div className="inbox-card-top">
                   <div>
                     <strong>{card.merchant}</strong>
@@ -2914,6 +2973,9 @@
                       {label}
                     </span>
                   ))}
+                  {activeVoiceContextId === card.id && (
+                    <span className="voice-context-pill active">Voice target</span>
+                  )}
                   {card.suggested_category?.category_name && (
                     <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
                       <CheckCircle2 size={14} />
@@ -3357,6 +3419,9 @@
   };
 
   const openReadinessWorkflow = (issue) => {
+    if (issue?.affected_entity_id) {
+      setActiveVoiceContextId(String(issue.affected_entity_id));
+    }
     if (issue?.resolution_target?.workflow === '/inbox/finish-now') {
       setCurrentTab('inbox');
       return;
@@ -3364,6 +3429,76 @@
     setCurrentTab('inbox');
   };
 
+  const renderVoiceContextBanner = () => {
+    if (!(currentTab === 'inbox' || currentTab === 'quarter')) {
+      return null;
+    }
+
+    return (
+      <section className="voice-context-banner">
+        <div>
+          <strong>Voice triage</strong>
+          <div className="small-muted">
+            Use the mic against one selected finish-now item only. Supported commands here are category, business, personal, split n%, and attach receipt.
+          </div>
+        </div>
+        <div className="voice-context-row">
+          <span className={`voice-context-pill ${activeVoiceCard ? 'active' : ''}`}>
+            {activeVoiceCard
+              ? `Target: ${activeVoiceCard.merchant} (${activeVoiceCard.id})`
+              : 'Select a blocker card to arm voice triage'}
+          </span>
+          {activeVoiceCard && (
+            <button className="secondary-action" type="button" onClick={() => setCurrentTab('inbox')}>
+              Edit target
+            </button>
+          )}
+        </div>
+        {voiceMicroDecisionChip && (
+          <div className="voice-micro-chip">
+            <div>
+              <strong>{voiceMicroDecisionChip.confirmationChip}</strong>
+              <div className="small-muted">
+                {voiceMicroDecisionChip.targetLabel} ({voiceMicroDecisionChip.targetId})
+              </div>
+            </div>
+            {voiceMicroDecisionChip.labels.length > 0 && (
+              <div className="voice-chip-labels">
+                {voiceMicroDecisionChip.labels.map((label) => (
+                  <span key={`${voiceMicroDecisionChip.targetId}-${label}`} className="voice-action-pill">
+                    {label}
+                  </span>
+                ))}
+              </div>
+            )}
+            <div className="voice-chip-actions">
+              <button
+                className="secondary-action"
+                type="button"
+                onClick={() => {
+                  setCurrentTab('inbox');
+                  setActiveVoiceContextId(voiceMicroDecisionChip.targetId);
+                }}
+              >
+                Edit
+              </button>
+              {voiceMicroDecisionChip.undoSupported && (
+                <button
+                  className="secondary-action"
+                  type="button"
+                  onClick={undoLastInboxAction}
+                  disabled={inboxBusyId === 'undo-last'}
+                >
+                  Undo
+                </button>
+              )}
+            </div>
+          </div>
+        )}
+      </section>
+    );
+  };
+
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
@@ -3654,6 +3789,13 @@
                     </div>
                   </div>
                   <div className="finish-now-actions">
+                    <button
+                      className="secondary-action"
+                      type="button"
+                      onClick={() => setActiveVoiceContextId(String(issue.affected_entity_id))}
+                    >
+                      {activeVoiceContextId === String(issue.affected_entity_id) ? 'Voice target set' : 'Arm voice'}
+                    </button>
                     {(readinessDemoEnabled || snapshotDemoEnabled) ? (
                       <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
                         Mark fixed
@@ -4391,6 +4533,7 @@
         </button>
       </section>
       {connectionError && <div className="alert alert-danger">{connectionError}</div>}
+      {renderVoiceContextBanner()}
 
       {loading ? (
         <div className="text-center py-5">Loading...</div>
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..6eb469fcc7f023f8545d9e952a5823fb8c47a88d
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});

codex
I found a pre-existing `voiceMicroDecisionService` in the workspace that the controller is already using, so the controller file on disk is further along than the first failing run suggested. I’m rerunning the backend verifier against the current file and switching the frontend tests to `CI=true ... --runInBand` so Jest stays single-process inside this environment.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$env:CI='"'true'; npm test -- --watch=false --runInBand src/voiceMicroDecisions.test.js src/quarterReadiness.test.js" in C:\Users\edebe\eds\bizPA\frontendexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npm run verify:voice-micro-decisions' in C:\Users\edebe\eds\bizPA\backend succeeded in 5.33s:

> backend@1.3.6 verify:voice-micro-decisions
> node verify_voice_micro_decisions.js

[dotenv@17.3.1] injecting env (9) from .env -- tip: 🔐 prevent committing .env to code: https://dotenvx.com/precommit
[Supabase] Client initialized successfully.
voice_micro_decisions_ok
context_guardrails_ok=true
audited_classification_updates_ok=true
attach_receipt_clarification_ok=true
match_selection_ok=true
safe_context_binding_ok=true
validation_errors_ok=true
 exited 1 in 5.73s:

> frontend@1.3.6 test
> react-scripts test src/voiceMicroDecisions.test.js src/quarterReadiness.test.js

npm warn "src/voiceMicroDecisions.test.js" is being parsed as a normal command line argument.
npm warn Unknown cli config "--watch". This will stop working in the next major version of npm.
npm warn Unknown cli config "--runInBand". This will stop working in the next major version of npm.
Error: spawn EPERM
    at ChildProcess.spawn (node:internal/child_process:420:11)
    at spawn (node:child_process:753:9)
    at fork (node:child_process:171:10)
    at ChildProcessWorker.initialize (C:\Users\edebe\eds\bizPA\frontend\node_modules\jest-worker\build\workers\ChildProcessWorker.js:133:45)
    at new ChildProcessWorker (C:\Users\edebe\eds\bizPA\frontend\node_modules\jest-worker\build\workers\ChildProcessWorker.js:124:10)
    at WorkerPool.createWorker (C:\Users\edebe\eds\bizPA\frontend\node_modules\jest-worker\build\WorkerPool.js:44:12)
    at new BaseWorkerPool (C:\Users\edebe\eds\bizPA\frontend\node_modules\jest-worker\build\base\BaseWorkerPool.js:127:27)
    at new WorkerPool (C:\Users\edebe\eds\bizPA\frontend\node_modules\jest-worker\build\WorkerPool.js:30:1)
    at new Worker (C:\Users\edebe\eds\bizPA\frontend\node_modules\jest-worker\build\index.js:167:26)
    at HasteMap._getWorker (C:\Users\edebe\eds\bizPA\frontend\node_modules\jest-haste-map\build\index.js:862:24)
file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..e6d93b4bc1cd6960ce27238cfb7e90c3322f41e0
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,6 +10,36 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+const {
+  parseVoiceMicroDecision,
+  validateMicroDecisionContext,
+} = require('../services/voiceMicroDecisionService');
+
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undoable: undoSupported,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
 
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
@@ -267,76 +297,177 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
-  const text = String(transcript).toLowerCase().trim();
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_kind = null,
+    context_scope = null,
+  } = req.body || {};
+  const rawTranscript = String(transcript).trim();
+  const text = rawTranscript.toLowerCase();
+  const normalizedContext = context_kind ?? context_scope;
+  const parsedCommand = parseVoiceMicroDecision(rawTranscript);
+  const contextValidation = validateMicroDecisionContext(parsedCommand, normalizedContext);
+  const normalizedContextScope = contextValidation.ok
+    ? contextValidation.context_kind
+    : (normalizedContext === null || normalizedContext === undefined
+      ? null
+      : String(normalizedContext).toLowerCase().replace(/-/g, '_'));
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
+  if (!contextValidation.ok) return res.status(contextValidation.statusCode).json({ error: contextValidation.error });
 
   try {
-    if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
-      const categoryName = text.replace('category:', '').trim();
-      const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+    if (parsedCommand.type === 'category') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: parsedCommand.category_code,
+            category_name: parsedCommand.category_name,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'category', label: parsedCommand.category_name }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
-      const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+    if (parsedCommand.type === 'business_personal') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const bp = parsedCommand.business_personal;
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    const splitMatch = text.match(/split\s+(\d{1,3})%?/);
-    if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
-      const pct = Number(splitMatch[1]);
+    if (parsedCommand.type === 'split') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const pct = Number(parsedCommand.split_business_pct);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+    if (parsedCommand.type === 'attach_receipt') {
+      if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for attach receipt commands.' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          parsed_command: parsedCommand,
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
-    if (['match first', 'match second', 'match third'].includes(text)) {
+    if (parsedCommand.type === 'match_rank') {
       if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for match commands.' });
-      const index = text === 'match first' ? 1 : text === 'match second' ? 2 : 3;
+      const index = parsedCommand.match_rank;
       const suggestions = await db.query(
         `
         WITH ev AS (
@@ -364,10 +495,24 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          bank_txn_id: chosen,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text === 'no match') {
+    if (parsedCommand.type === 'no_match') {
       if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for no match.' });
       await db.query(
         `
@@ -376,7 +521,20 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/backend/verify_voice_micro_decisions.js b/backend/verify_voice_micro_decisions.js
index 25d9ab02050aac37a5d38aca5c6f0154de9bb787..15c93efa65f133ec1a59d827ecbfa29a95cf4e6d
--- a/backend/verify_voice_micro_decisions.js
+++ b/backend/verify_voice_micro_decisions.js
@@ -9,16 +9,70 @@
       { bank_txn_id: 'txn-2' },
       { bank_txn_id: 'txn-3' }
     ];
+    this.classifications = new Map();
+    this.auditEntries = [];
   }
 
-  async query(text, params) {
+  get pool() {
+    return {
+      connect: async () => ({
+        query: this.query.bind(this),
+        release() {},
+      }),
+    };
+  }
+
+  async query(text, params = []) {
     const sql = text.replace(/\s+/g, ' ').trim();
     this.calls.push({ sql, params });
 
+    if (sql === 'BEGIN' || sql === 'COMMIT' || sql === 'ROLLBACK') {
+      return { rows: [] };
+    }
+
+    if (sql.includes('SELECT * FROM transaction_classifications')) {
+      const existing = this.classifications.get(params[0]);
+      return { rows: existing ? [existing] : [] };
+    }
+
+    if (sql.includes('INSERT INTO transaction_classifications')) {
+      const next = {
+        user_id: params[0],
+        bank_txn_id: params[1],
+        category_code: params[2],
+        category_name: params[3],
+        business_personal: params[4],
+        is_split: params[5],
+        split_business_pct: params[6],
+        confidence: params[7],
+        source: params[8],
+      };
+      this.classifications.set(params[1], next);
+      return { rows: [next] };
+    }
+
+    if (sql.includes('INSERT INTO transaction_audit_log')) {
+      this.auditEntries.push({
+        bank_txn_id: params[1],
+        previous_value: params[4],
+        new_value: params[5],
+        change_source: params[6],
+      });
+      return { rows: [] };
+    }
+
+    if (sql.includes('SELECT txn_date FROM bank_transactions')) {
+      return { rows: [{ txn_date: '2026-03-12' }] };
+    }
+
     if (sql.includes('FROM bank_transactions bt CROSS JOIN ev')) {
       return { rows: this.matchSuggestions };
     }
 
+    if (sql.includes('INSERT INTO evidence_links')) {
+      return { rows: [] };
+    }
+
     return { rows: [] };
   }
 }
@@ -69,45 +123,79 @@
   const mockDb = new MockDb();
   const controller = loadController(mockDb);
 
+  const missingContext = await invoke(controller, {
+    transcript: 'Business',
+    bank_txn_id: 'txn-123'
+  });
+  assert.strictEqual(missingContext.statusCode, 400);
+  assert.strictEqual(missingContext.body.error, 'context_kind is required for MVP voice micro-decisions.');
+
   const category = await invoke(controller, {
     transcript: 'Category: Travel',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(category.statusCode, 200);
-  assert.strictEqual(category.body.confirmation_chip, 'Applied: travel');
-  assert(
-    mockDb.calls.some((call) => call.sql.includes('INSERT INTO transaction_classifications') && call.params[2] === 'TRAVEL' && call.params[3] === 'travel'),
-    'Expected category command to persist manual classification'
-  );
+  assert.strictEqual(category.body.confirmation_chip, 'Applied: Travel');
+  assert.strictEqual(category.body.undo.supported, true);
+  assert.deepStrictEqual(category.body.context_binding, {
+    scope: 'inbox',
+    target_type: 'bank_transaction',
+    target_id: 'txn-123'
+  });
+  assert.strictEqual(mockDb.classifications.get('txn-123').category_code, 'TRAVEL');
+  assert.strictEqual(mockDb.auditEntries.at(-1).change_source, 'voice');
 
   const business = await invoke(controller, {
     transcript: 'Business',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(business.statusCode, 200);
   assert.strictEqual(business.body.confirmation_chip, 'Applied: BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').category_code, 'TRAVEL');
+  assert.strictEqual(mockDb.classifications.get('txn-123').business_personal, 'BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').is_split, false);
 
   const split = await invoke(controller, {
     transcript: 'Split 70%',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(split.statusCode, 200);
   assert.strictEqual(split.body.confirmation_chip, 'Applied: Split 70%');
+  assert.strictEqual(split.body.undo.supported, true);
+  assert.strictEqual(mockDb.classifications.get('txn-123').business_personal, 'BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').is_split, true);
+  assert.strictEqual(mockDb.classifications.get('txn-123').split_business_pct, 70);
 
   const attachReceipt = await invoke(controller, {
     transcript: 'Attach receipt',
-    bank_txn_id: 'txn-123'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(attachReceipt.statusCode, 200);
   assert.strictEqual(attachReceipt.body.action_status, 'clarification_needed');
   assert.strictEqual(attachReceipt.body.confirmation_chip, 'Attach receipt requested');
+  assert.strictEqual(attachReceipt.body.undo.supported, false);
+  assert.deepStrictEqual(attachReceipt.body.context_binding, {
+    scope: 'evidence',
+    target_type: 'evidence',
+    target_id: 'evidence-1'
+  });
 
   const matchSecond = await invoke(controller, {
     transcript: 'Match second',
-    evidence_id: 'evidence-1'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(matchSecond.statusCode, 200);
   assert.strictEqual(matchSecond.body.confirmation_chip, 'Applied: Match 2');
+  assert.deepStrictEqual(matchSecond.body.context_binding, {
+    scope: 'evidence',
+    target_type: 'evidence',
+    target_id: 'evidence-1'
+  });
   assert(
     mockDb.calls.some((call) => call.sql.includes('INSERT INTO evidence_links') && call.params[1] === 'evidence-1' && call.params[2] === 'txn-2'),
     'Expected match command to persist selected evidence link'
@@ -115,29 +203,41 @@
 
   const noMatch = await invoke(controller, {
     transcript: 'No match',
-    evidence_id: 'evidence-1'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(noMatch.statusCode, 200);
   assert.strictEqual(noMatch.body.confirmation_chip, 'Applied: No match');
 
   const invalidSplit = await invoke(controller, {
     transcript: 'Split 140%',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(invalidSplit.statusCode, 400);
   assert.strictEqual(invalidSplit.body.error, 'Split percentage must be 0-100.');
 
+  const unsafeContext = await invoke(controller, {
+    transcript: 'Category: Travel',
+    bank_txn_id: 'txn-123',
+    context_kind: 'evidence'
+  });
+  assert.strictEqual(unsafeContext.statusCode, 409);
+  assert.strictEqual(unsafeContext.body.error, 'Command "category" is not allowed for evidence context.');
+
   const unsupported = await invoke(controller, {
-    transcript: 'Archive this forever'
+    transcript: 'Archive this forever',
+    context_kind: 'inbox'
   });
   assert.strictEqual(unsupported.statusCode, 400);
   assert.strictEqual(unsupported.body.error, 'Unsupported MVP voice micro-decision command.');
 
   console.log('voice_micro_decisions_ok');
-  console.log('category_business_split_ok=true');
+  console.log('context_guardrails_ok=true');
+  console.log('audited_classification_updates_ok=true');
   console.log('attach_receipt_clarification_ok=true');
   console.log('match_selection_ok=true');
-  console.log('no_match_ok=true');
+  console.log('safe_context_binding_ok=true');
   console.log('validation_errors_ok=true');
 }
 
diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index 9b145fadac9123e3fa01b12af6ef2b9916dc11de..b21180e549e09be4a5ed0412e53087d2a52f4382
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -50,6 +50,11 @@
   buildQuarterUiState,
   deriveResolvedIssueIdsFromSearchParams,
 } from './quarterReadiness';
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -427,6 +432,13 @@
   .date-group-count { border-radius: 999px; padding: 4px 9px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.72rem; color: var(--text-muted); }
   .date-group-body { padding: 0 12px 12px; }
   .voice-preview { background: rgba(0,0,0,0.82); color: white; padding: 8px 15px; border-radius: 15px; margin-bottom: 8px; font-size: 0.72rem; max-width: 240px; text-align: right; }
+  .voice-context-banner { display: grid; gap: 10px; padding: 14px 16px; margin-bottom: 16px; border-radius: 18px; background: var(--bg-card); border: 1px solid var(--border); box-shadow: var(--shadow); }
+  .voice-context-row, .voice-chip-actions, .voice-chip-labels { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
+  .voice-context-pill, .voice-action-pill { border-radius: 999px; padding: 6px 10px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.74rem; font-weight: 700; }
+  .voice-context-pill.active { background: rgba(13,92,99,0.12); color: var(--primary-strong); border-color: rgba(13,92,99,0.24); }
+  .voice-micro-chip { display: grid; gap: 10px; padding: 14px 16px; border-radius: 18px; background: rgba(13,92,99,0.08); border: 1px solid rgba(13,92,99,0.18); }
+  .voice-micro-chip strong { display: block; }
+  .inbox-action-card.voice-target { border-color: rgba(13,92,99,0.36); box-shadow: 0 0 0 2px rgba(13,92,99,0.12), var(--shadow); }
   .spinner { animation: rotate 2s linear infinite; }
   .capture-grid { display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(320px, 0.95fr); gap: 18px; align-items: start; }
   .command-centre { display: grid; gap: 18px; }
@@ -1382,6 +1394,8 @@
   const [inboxDrafts, setInboxDrafts] = useState({});
   const [inboxBusyId, setInboxBusyId] = useState('');
   const [inboxMessage, setInboxMessage] = useState(null);
+  const [activeVoiceContextId, setActiveVoiceContextId] = useState('');
+  const [voiceMicroDecisionChip, setVoiceMicroDecisionChip] = useState(null);
   const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
@@ -1606,10 +1620,20 @@
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
   const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const activeVoiceCard = findVoiceContextCard(inboxCards, activeVoiceContextId);
   const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
   const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
   const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
+  useEffect(() => {
+    if (!activeVoiceContextId) {
+      return;
+    }
+    if (!findVoiceContextCard(inboxCards, activeVoiceContextId)) {
+      setActiveVoiceContextId('');
+    }
+  }, [activeVoiceContextId, inboxCards]);
+
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
   const canManageGovernance = hasPermission(currentUser, PERMISSIONS.GOVERNANCE_MANAGE);
@@ -1806,11 +1830,13 @@
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxDrafts({});
+        setVoiceMicroDecisionChip(null);
         setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
         return;
       }
       await axios.post(`${API_BASE_URL}/inbox/undo-last`);
       await fetchAllData();
+      setVoiceMicroDecisionChip(null);
       setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
     } catch (error) {
       setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
@@ -1991,22 +2017,51 @@
     const cleanText = text.replace(/┬ú/g, 'GBP ');
     try {
       setVoiceStatus('processing');
-      const response = await axios.post(`${API_BASE_URL}/voice/process`, {
+      const microDecisionRequest = buildVoiceMicroDecisionRequest({
+        currentTab,
         transcript: cleanText,
-        device_id: 'mobile-v139',
-        current_date: new Date().toISOString(),
+        activeVoiceContextId,
+        inboxCards,
       });
-      if (response.data.confirmation_text && window.SpeechSynthesisUtterance) {
+
+      if (microDecisionRequest?.error) {
+        setInboxMessage({ type: 'error', text: microDecisionRequest.error });
+        setCurrentTab('inbox');
+        return;
+      }
+
+      const response = microDecisionRequest
+        ? await axios.post(`${API_BASE_URL}${microDecisionRequest.endpoint}`, microDecisionRequest.payload)
+        : await axios.post(`${API_BASE_URL}/voice/process`, {
+            transcript: cleanText,
+            device_id: 'mobile-v139',
+            current_date: new Date().toISOString(),
+          });
+      const spokenConfirmation = response.data.confirmation_chip || response.data.confirmation_text;
+      if (spokenConfirmation && window.SpeechSynthesisUtterance) {
         try {
-          const utterance = new window.SpeechSynthesisUtterance(response.data.confirmation_text);
+          const utterance = new window.SpeechSynthesisUtterance(spokenConfirmation);
           window.speechSynthesis.speak(utterance);
         } catch (_) {
         }
       }
-      executeVoiceAction(response.data);
-      fetchAllData();
+      if (microDecisionRequest) {
+        setVoiceMicroDecisionChip((previous) => reduceVoiceMicroDecisionChip(previous, response.data, microDecisionRequest.card));
+        setInboxMessage({
+          type: 'success',
+          text: response.data.confirmation_chip || 'Voice triage action applied.',
+        });
+      } else {
+        executeVoiceAction(response.data);
+      }
+      await fetchAllData();
     } catch (error) {
-      alert('AI Error');
+      const message = error?.response?.data?.error || 'AI Error';
+      if (currentTab === 'inbox' || currentTab === 'quarter') {
+        setInboxMessage({ type: 'error', text: message });
+      } else {
+        alert(message);
+      }
     } finally {
       setVoiceStatus('idle');
       setIsListening(false);
@@ -2894,7 +2949,11 @@
             const saving = inboxBusyId === card.id;
             const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
             return (
-              <section key={card.id} className="inbox-action-card">
+              <section
+                key={card.id}
+                className={`inbox-action-card ${activeVoiceContextId === card.id ? 'voice-target' : ''}`}
+                onClick={() => setActiveVoiceContextId(card.id)}
+              >
                 <div className="inbox-card-top">
                   <div>
                     <strong>{card.merchant}</strong>
@@ -2914,6 +2973,9 @@
                       {label}
                     </span>
                   ))}
+                  {activeVoiceContextId === card.id && (
+                    <span className="voice-context-pill active">Voice target</span>
+                  )}
                   {card.suggested_category?.category_name && (
                     <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
                       <CheckCircle2 size={14} />
@@ -3357,6 +3419,9 @@
   };
 
   const openReadinessWorkflow = (issue) => {
+    if (issue?.affected_entity_id) {
+      setActiveVoiceContextId(String(issue.affected_entity_id));
+    }
     if (issue?.resolution_target?.workflow === '/inbox/finish-now') {
       setCurrentTab('inbox');
       return;
@@ -3364,6 +3429,76 @@
     setCurrentTab('inbox');
   };
 
+  const renderVoiceContextBanner = () => {
+    if (!(currentTab === 'inbox' || currentTab === 'quarter')) {
+      return null;
+    }
+
+    return (
+      <section className="voice-context-banner">
+        <div>
+          <strong>Voice triage</strong>
+          <div className="small-muted">
+            Use the mic against one selected finish-now item only. Supported commands here are category, business, personal, split n%, and attach receipt.
+          </div>
+        </div>
+        <div className="voice-context-row">
+          <span className={`voice-context-pill ${activeVoiceCard ? 'active' : ''}`}>
+            {activeVoiceCard
+              ? `Target: ${activeVoiceCard.merchant} (${activeVoiceCard.id})`
+              : 'Select a blocker card to arm voice triage'}
+          </span>
+          {activeVoiceCard && (
+            <button className="secondary-action" type="button" onClick={() => setCurrentTab('inbox')}>
+              Edit target
+            </button>
+          )}
+        </div>
+        {voiceMicroDecisionChip && (
+          <div className="voice-micro-chip">
+            <div>
+              <strong>{voiceMicroDecisionChip.confirmationChip}</strong>
+              <div className="small-muted">
+                {voiceMicroDecisionChip.targetLabel} ({voiceMicroDecisionChip.targetId})
+              </div>
+            </div>
+            {voiceMicroDecisionChip.labels.length > 0 && (
+              <div className="voice-chip-labels">
+                {voiceMicroDecisionChip.labels.map((label) => (
+                  <span key={`${voiceMicroDecisionChip.targetId}-${label}`} className="voice-action-pill">
+                    {label}
+                  </span>
+                ))}
+              </div>
+            )}
+            <div className="voice-chip-actions">
+              <button
+                className="secondary-action"
+                type="button"
+                onClick={() => {
+                  setCurrentTab('inbox');
+                  setActiveVoiceContextId(voiceMicroDecisionChip.targetId);
+                }}
+              >
+                Edit
+              </button>
+              {voiceMicroDecisionChip.undoSupported && (
+                <button
+                  className="secondary-action"
+                  type="button"
+                  onClick={undoLastInboxAction}
+                  disabled={inboxBusyId === 'undo-last'}
+                >
+                  Undo
+                </button>
+              )}
+            </div>
+          </div>
+        )}
+      </section>
+    );
+  };
+
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
@@ -3654,6 +3789,13 @@
                     </div>
                   </div>
                   <div className="finish-now-actions">
+                    <button
+                      className="secondary-action"
+                      type="button"
+                      onClick={() => setActiveVoiceContextId(String(issue.affected_entity_id))}
+                    >
+                      {activeVoiceContextId === String(issue.affected_entity_id) ? 'Voice target set' : 'Arm voice'}
+                    </button>
                     {(readinessDemoEnabled || snapshotDemoEnabled) ? (
                       <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
                         Mark fixed
@@ -4391,6 +4533,7 @@
         </button>
       </section>
       {connectionError && <div className="alert alert-danger">{connectionError}</div>}
+      {renderVoiceContextBanner()}
 
       {loading ? (
         <div className="text-center py-5">Loading...</div>
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..6eb469fcc7f023f8545d9e952a5823fb8c47a88d
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});

codex
The backend contract is green. The frontend build is also green, but `npm test` is still failing for an environment reason rather than a test failure: npm is swallowing Jest flags and `react-scripts` is trying to fork workers in this sandbox. I’m invoking `react-scripts` directly with `--runInBand` next to get a real result instead of a worker-spawn error.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npx react-scripts test --watchAll=false --runInBand --runTestsByPath src/voiceMicroDecisions.test.js src/quarterReadiness.test.js' in C:\Users\edebe\eds\bizPA\frontend succeeded in 21.16s:
PASS src/voiceMicroDecisions.test.js (9.347 s)
PASS src/quarterReadiness.test.js

Test Suites: 2 passed, 2 total
Tests:       7 passed, 7 total
Snapshots:   0 total
Time:        14.616 s
Ran all test suites within paths "src/voiceMicroDecisions.test.js", "src/quarterReadiness.test.js".
file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..e6d93b4bc1cd6960ce27238cfb7e90c3322f41e0
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,6 +10,36 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+const {
+  parseVoiceMicroDecision,
+  validateMicroDecisionContext,
+} = require('../services/voiceMicroDecisionService');
+
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undoable: undoSupported,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
 
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
@@ -267,76 +297,177 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
-  const text = String(transcript).toLowerCase().trim();
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_kind = null,
+    context_scope = null,
+  } = req.body || {};
+  const rawTranscript = String(transcript).trim();
+  const text = rawTranscript.toLowerCase();
+  const normalizedContext = context_kind ?? context_scope;
+  const parsedCommand = parseVoiceMicroDecision(rawTranscript);
+  const contextValidation = validateMicroDecisionContext(parsedCommand, normalizedContext);
+  const normalizedContextScope = contextValidation.ok
+    ? contextValidation.context_kind
+    : (normalizedContext === null || normalizedContext === undefined
+      ? null
+      : String(normalizedContext).toLowerCase().replace(/-/g, '_'));
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
+  if (!contextValidation.ok) return res.status(contextValidation.statusCode).json({ error: contextValidation.error });
 
   try {
-    if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
-      const categoryName = text.replace('category:', '').trim();
-      const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+    if (parsedCommand.type === 'category') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: parsedCommand.category_code,
+            category_name: parsedCommand.category_name,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'category', label: parsedCommand.category_name }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
-      const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+    if (parsedCommand.type === 'business_personal') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const bp = parsedCommand.business_personal;
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    const splitMatch = text.match(/split\s+(\d{1,3})%?/);
-    if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
-      const pct = Number(splitMatch[1]);
+    if (parsedCommand.type === 'split') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const pct = Number(parsedCommand.split_business_pct);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+    if (parsedCommand.type === 'attach_receipt') {
+      if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for attach receipt commands.' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          parsed_command: parsedCommand,
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
-    if (['match first', 'match second', 'match third'].includes(text)) {
+    if (parsedCommand.type === 'match_rank') {
       if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for match commands.' });
-      const index = text === 'match first' ? 1 : text === 'match second' ? 2 : 3;
+      const index = parsedCommand.match_rank;
       const suggestions = await db.query(
         `
         WITH ev AS (
@@ -364,10 +495,24 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          bank_txn_id: chosen,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text === 'no match') {
+    if (parsedCommand.type === 'no_match') {
       if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for no match.' });
       await db.query(
         `
@@ -376,7 +521,20 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/backend/verify_voice_micro_decisions.js b/backend/verify_voice_micro_decisions.js
index 25d9ab02050aac37a5d38aca5c6f0154de9bb787..15c93efa65f133ec1a59d827ecbfa29a95cf4e6d
--- a/backend/verify_voice_micro_decisions.js
+++ b/backend/verify_voice_micro_decisions.js
@@ -9,16 +9,70 @@
       { bank_txn_id: 'txn-2' },
       { bank_txn_id: 'txn-3' }
     ];
+    this.classifications = new Map();
+    this.auditEntries = [];
   }
 
-  async query(text, params) {
+  get pool() {
+    return {
+      connect: async () => ({
+        query: this.query.bind(this),
+        release() {},
+      }),
+    };
+  }
+
+  async query(text, params = []) {
     const sql = text.replace(/\s+/g, ' ').trim();
     this.calls.push({ sql, params });
 
+    if (sql === 'BEGIN' || sql === 'COMMIT' || sql === 'ROLLBACK') {
+      return { rows: [] };
+    }
+
+    if (sql.includes('SELECT * FROM transaction_classifications')) {
+      const existing = this.classifications.get(params[0]);
+      return { rows: existing ? [existing] : [] };
+    }
+
+    if (sql.includes('INSERT INTO transaction_classifications')) {
+      const next = {
+        user_id: params[0],
+        bank_txn_id: params[1],
+        category_code: params[2],
+        category_name: params[3],
+        business_personal: params[4],
+        is_split: params[5],
+        split_business_pct: params[6],
+        confidence: params[7],
+        source: params[8],
+      };
+      this.classifications.set(params[1], next);
+      return { rows: [next] };
+    }
+
+    if (sql.includes('INSERT INTO transaction_audit_log')) {
+      this.auditEntries.push({
+        bank_txn_id: params[1],
+        previous_value: params[4],
+        new_value: params[5],
+        change_source: params[6],
+      });
+      return { rows: [] };
+    }
+
+    if (sql.includes('SELECT txn_date FROM bank_transactions')) {
+      return { rows: [{ txn_date: '2026-03-12' }] };
+    }
+
     if (sql.includes('FROM bank_transactions bt CROSS JOIN ev')) {
       return { rows: this.matchSuggestions };
     }
 
+    if (sql.includes('INSERT INTO evidence_links')) {
+      return { rows: [] };
+    }
+
     return { rows: [] };
   }
 }
@@ -69,45 +123,79 @@
   const mockDb = new MockDb();
   const controller = loadController(mockDb);
 
+  const missingContext = await invoke(controller, {
+    transcript: 'Business',
+    bank_txn_id: 'txn-123'
+  });
+  assert.strictEqual(missingContext.statusCode, 400);
+  assert.strictEqual(missingContext.body.error, 'context_kind is required for MVP voice micro-decisions.');
+
   const category = await invoke(controller, {
     transcript: 'Category: Travel',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(category.statusCode, 200);
-  assert.strictEqual(category.body.confirmation_chip, 'Applied: travel');
-  assert(
-    mockDb.calls.some((call) => call.sql.includes('INSERT INTO transaction_classifications') && call.params[2] === 'TRAVEL' && call.params[3] === 'travel'),
-    'Expected category command to persist manual classification'
-  );
+  assert.strictEqual(category.body.confirmation_chip, 'Applied: Travel');
+  assert.strictEqual(category.body.undo.supported, true);
+  assert.deepStrictEqual(category.body.context_binding, {
+    scope: 'inbox',
+    target_type: 'bank_transaction',
+    target_id: 'txn-123'
+  });
+  assert.strictEqual(mockDb.classifications.get('txn-123').category_code, 'TRAVEL');
+  assert.strictEqual(mockDb.auditEntries.at(-1).change_source, 'voice');
 
   const business = await invoke(controller, {
     transcript: 'Business',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(business.statusCode, 200);
   assert.strictEqual(business.body.confirmation_chip, 'Applied: BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').category_code, 'TRAVEL');
+  assert.strictEqual(mockDb.classifications.get('txn-123').business_personal, 'BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').is_split, false);
 
   const split = await invoke(controller, {
     transcript: 'Split 70%',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(split.statusCode, 200);
   assert.strictEqual(split.body.confirmation_chip, 'Applied: Split 70%');
+  assert.strictEqual(split.body.undo.supported, true);
+  assert.strictEqual(mockDb.classifications.get('txn-123').business_personal, 'BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').is_split, true);
+  assert.strictEqual(mockDb.classifications.get('txn-123').split_business_pct, 70);
 
   const attachReceipt = await invoke(controller, {
     transcript: 'Attach receipt',
-    bank_txn_id: 'txn-123'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(attachReceipt.statusCode, 200);
   assert.strictEqual(attachReceipt.body.action_status, 'clarification_needed');
   assert.strictEqual(attachReceipt.body.confirmation_chip, 'Attach receipt requested');
+  assert.strictEqual(attachReceipt.body.undo.supported, false);
+  assert.deepStrictEqual(attachReceipt.body.context_binding, {
+    scope: 'evidence',
+    target_type: 'evidence',
+    target_id: 'evidence-1'
+  });
 
   const matchSecond = await invoke(controller, {
     transcript: 'Match second',
-    evidence_id: 'evidence-1'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(matchSecond.statusCode, 200);
   assert.strictEqual(matchSecond.body.confirmation_chip, 'Applied: Match 2');
+  assert.deepStrictEqual(matchSecond.body.context_binding, {
+    scope: 'evidence',
+    target_type: 'evidence',
+    target_id: 'evidence-1'
+  });
   assert(
     mockDb.calls.some((call) => call.sql.includes('INSERT INTO evidence_links') && call.params[1] === 'evidence-1' && call.params[2] === 'txn-2'),
     'Expected match command to persist selected evidence link'
@@ -115,29 +203,41 @@
 
   const noMatch = await invoke(controller, {
     transcript: 'No match',
-    evidence_id: 'evidence-1'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(noMatch.statusCode, 200);
   assert.strictEqual(noMatch.body.confirmation_chip, 'Applied: No match');
 
   const invalidSplit = await invoke(controller, {
     transcript: 'Split 140%',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(invalidSplit.statusCode, 400);
   assert.strictEqual(invalidSplit.body.error, 'Split percentage must be 0-100.');
 
+  const unsafeContext = await invoke(controller, {
+    transcript: 'Category: Travel',
+    bank_txn_id: 'txn-123',
+    context_kind: 'evidence'
+  });
+  assert.strictEqual(unsafeContext.statusCode, 409);
+  assert.strictEqual(unsafeContext.body.error, 'Command "category" is not allowed for evidence context.');
+
   const unsupported = await invoke(controller, {
-    transcript: 'Archive this forever'
+    transcript: 'Archive this forever',
+    context_kind: 'inbox'
   });
   assert.strictEqual(unsupported.statusCode, 400);
   assert.strictEqual(unsupported.body.error, 'Unsupported MVP voice micro-decision command.');
 
   console.log('voice_micro_decisions_ok');
-  console.log('category_business_split_ok=true');
+  console.log('context_guardrails_ok=true');
+  console.log('audited_classification_updates_ok=true');
   console.log('attach_receipt_clarification_ok=true');
   console.log('match_selection_ok=true');
-  console.log('no_match_ok=true');
+  console.log('safe_context_binding_ok=true');
   console.log('validation_errors_ok=true');
 }
 
diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index 9b145fadac9123e3fa01b12af6ef2b9916dc11de..b21180e549e09be4a5ed0412e53087d2a52f4382
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -50,6 +50,11 @@
   buildQuarterUiState,
   deriveResolvedIssueIdsFromSearchParams,
 } from './quarterReadiness';
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -427,6 +432,13 @@
   .date-group-count { border-radius: 999px; padding: 4px 9px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.72rem; color: var(--text-muted); }
   .date-group-body { padding: 0 12px 12px; }
   .voice-preview { background: rgba(0,0,0,0.82); color: white; padding: 8px 15px; border-radius: 15px; margin-bottom: 8px; font-size: 0.72rem; max-width: 240px; text-align: right; }
+  .voice-context-banner { display: grid; gap: 10px; padding: 14px 16px; margin-bottom: 16px; border-radius: 18px; background: var(--bg-card); border: 1px solid var(--border); box-shadow: var(--shadow); }
+  .voice-context-row, .voice-chip-actions, .voice-chip-labels { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
+  .voice-context-pill, .voice-action-pill { border-radius: 999px; padding: 6px 10px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.74rem; font-weight: 700; }
+  .voice-context-pill.active { background: rgba(13,92,99,0.12); color: var(--primary-strong); border-color: rgba(13,92,99,0.24); }
+  .voice-micro-chip { display: grid; gap: 10px; padding: 14px 16px; border-radius: 18px; background: rgba(13,92,99,0.08); border: 1px solid rgba(13,92,99,0.18); }
+  .voice-micro-chip strong { display: block; }
+  .inbox-action-card.voice-target { border-color: rgba(13,92,99,0.36); box-shadow: 0 0 0 2px rgba(13,92,99,0.12), var(--shadow); }
   .spinner { animation: rotate 2s linear infinite; }
   .capture-grid { display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(320px, 0.95fr); gap: 18px; align-items: start; }
   .command-centre { display: grid; gap: 18px; }
@@ -1382,6 +1394,8 @@
   const [inboxDrafts, setInboxDrafts] = useState({});
   const [inboxBusyId, setInboxBusyId] = useState('');
   const [inboxMessage, setInboxMessage] = useState(null);
+  const [activeVoiceContextId, setActiveVoiceContextId] = useState('');
+  const [voiceMicroDecisionChip, setVoiceMicroDecisionChip] = useState(null);
   const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
@@ -1606,10 +1620,20 @@
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
   const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const activeVoiceCard = findVoiceContextCard(inboxCards, activeVoiceContextId);
   const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
   const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
   const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
+  useEffect(() => {
+    if (!activeVoiceContextId) {
+      return;
+    }
+    if (!findVoiceContextCard(inboxCards, activeVoiceContextId)) {
+      setActiveVoiceContextId('');
+    }
+  }, [activeVoiceContextId, inboxCards]);
+
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
   const canManageGovernance = hasPermission(currentUser, PERMISSIONS.GOVERNANCE_MANAGE);
@@ -1806,11 +1830,13 @@
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxDrafts({});
+        setVoiceMicroDecisionChip(null);
         setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
         return;
       }
       await axios.post(`${API_BASE_URL}/inbox/undo-last`);
       await fetchAllData();
+      setVoiceMicroDecisionChip(null);
       setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
     } catch (error) {
       setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
@@ -1991,22 +2017,51 @@
     const cleanText = text.replace(/┬ú/g, 'GBP ');
     try {
       setVoiceStatus('processing');
-      const response = await axios.post(`${API_BASE_URL}/voice/process`, {
+      const microDecisionRequest = buildVoiceMicroDecisionRequest({
+        currentTab,
         transcript: cleanText,
-        device_id: 'mobile-v139',
-        current_date: new Date().toISOString(),
+        activeVoiceContextId,
+        inboxCards,
       });
-      if (response.data.confirmation_text && window.SpeechSynthesisUtterance) {
+
+      if (microDecisionRequest?.error) {
+        setInboxMessage({ type: 'error', text: microDecisionRequest.error });
+        setCurrentTab('inbox');
+        return;
+      }
+
+      const response = microDecisionRequest
+        ? await axios.post(`${API_BASE_URL}${microDecisionRequest.endpoint}`, microDecisionRequest.payload)
+        : await axios.post(`${API_BASE_URL}/voice/process`, {
+            transcript: cleanText,
+            device_id: 'mobile-v139',
+            current_date: new Date().toISOString(),
+          });
+      const spokenConfirmation = response.data.confirmation_chip || response.data.confirmation_text;
+      if (spokenConfirmation && window.SpeechSynthesisUtterance) {
         try {
-          const utterance = new window.SpeechSynthesisUtterance(response.data.confirmation_text);
+          const utterance = new window.SpeechSynthesisUtterance(spokenConfirmation);
           window.speechSynthesis.speak(utterance);
         } catch (_) {
         }
       }
-      executeVoiceAction(response.data);
-      fetchAllData();
+      if (microDecisionRequest) {
+        setVoiceMicroDecisionChip((previous) => reduceVoiceMicroDecisionChip(previous, response.data, microDecisionRequest.card));
+        setInboxMessage({
+          type: 'success',
+          text: response.data.confirmation_chip || 'Voice triage action applied.',
+        });
+      } else {
+        executeVoiceAction(response.data);
+      }
+      await fetchAllData();
     } catch (error) {
-      alert('AI Error');
+      const message = error?.response?.data?.error || 'AI Error';
+      if (currentTab === 'inbox' || currentTab === 'quarter') {
+        setInboxMessage({ type: 'error', text: message });
+      } else {
+        alert(message);
+      }
     } finally {
       setVoiceStatus('idle');
       setIsListening(false);
@@ -2894,7 +2949,11 @@
             const saving = inboxBusyId === card.id;
             const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
             return (
-              <section key={card.id} className="inbox-action-card">
+              <section
+                key={card.id}
+                className={`inbox-action-card ${activeVoiceContextId === card.id ? 'voice-target' : ''}`}
+                onClick={() => setActiveVoiceContextId(card.id)}
+              >
                 <div className="inbox-card-top">
                   <div>
                     <strong>{card.merchant}</strong>
@@ -2914,6 +2973,9 @@
                       {label}
                     </span>
                   ))}
+                  {activeVoiceContextId === card.id && (
+                    <span className="voice-context-pill active">Voice target</span>
+                  )}
                   {card.suggested_category?.category_name && (
                     <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
                       <CheckCircle2 size={14} />
@@ -3357,6 +3419,9 @@
   };
 
   const openReadinessWorkflow = (issue) => {
+    if (issue?.affected_entity_id) {
+      setActiveVoiceContextId(String(issue.affected_entity_id));
+    }
     if (issue?.resolution_target?.workflow === '/inbox/finish-now') {
       setCurrentTab('inbox');
       return;
@@ -3364,6 +3429,76 @@
     setCurrentTab('inbox');
   };
 
+  const renderVoiceContextBanner = () => {
+    if (!(currentTab === 'inbox' || currentTab === 'quarter')) {
+      return null;
+    }
+
+    return (
+      <section className="voice-context-banner">
+        <div>
+          <strong>Voice triage</strong>
+          <div className="small-muted">
+            Use the mic against one selected finish-now item only. Supported commands here are category, business, personal, split n%, and attach receipt.
+          </div>
+        </div>
+        <div className="voice-context-row">
+          <span className={`voice-context-pill ${activeVoiceCard ? 'active' : ''}`}>
+            {activeVoiceCard
+              ? `Target: ${activeVoiceCard.merchant} (${activeVoiceCard.id})`
+              : 'Select a blocker card to arm voice triage'}
+          </span>
+          {activeVoiceCard && (
+            <button className="secondary-action" type="button" onClick={() => setCurrentTab('inbox')}>
+              Edit target
+            </button>
+          )}
+        </div>
+        {voiceMicroDecisionChip && (
+          <div className="voice-micro-chip">
+            <div>
+              <strong>{voiceMicroDecisionChip.confirmationChip}</strong>
+              <div className="small-muted">
+                {voiceMicroDecisionChip.targetLabel} ({voiceMicroDecisionChip.targetId})
+              </div>
+            </div>
+            {voiceMicroDecisionChip.labels.length > 0 && (
+              <div className="voice-chip-labels">
+                {voiceMicroDecisionChip.labels.map((label) => (
+                  <span key={`${voiceMicroDecisionChip.targetId}-${label}`} className="voice-action-pill">
+                    {label}
+                  </span>
+                ))}
+              </div>
+            )}
+            <div className="voice-chip-actions">
+              <button
+                className="secondary-action"
+                type="button"
+                onClick={() => {
+                  setCurrentTab('inbox');
+                  setActiveVoiceContextId(voiceMicroDecisionChip.targetId);
+                }}
+              >
+                Edit
+              </button>
+              {voiceMicroDecisionChip.undoSupported && (
+                <button
+                  className="secondary-action"
+                  type="button"
+                  onClick={undoLastInboxAction}
+                  disabled={inboxBusyId === 'undo-last'}
+                >
+                  Undo
+                </button>
+              )}
+            </div>
+          </div>
+        )}
+      </section>
+    );
+  };
+
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
@@ -3654,6 +3789,13 @@
                     </div>
                   </div>
                   <div className="finish-now-actions">
+                    <button
+                      className="secondary-action"
+                      type="button"
+                      onClick={() => setActiveVoiceContextId(String(issue.affected_entity_id))}
+                    >
+                      {activeVoiceContextId === String(issue.affected_entity_id) ? 'Voice target set' : 'Arm voice'}
+                    </button>
                     {(readinessDemoEnabled || snapshotDemoEnabled) ? (
                       <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
                         Mark fixed
@@ -4391,6 +4533,7 @@
         </button>
       </section>
       {connectionError && <div className="alert alert-danger">{connectionError}</div>}
+      {renderVoiceContextBanner()}
 
       {loading ? (
         <div className="text-center py-5">Loading...</div>
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..6eb469fcc7f023f8545d9e952a5823fb8c47a88d
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});

codex
The direct `react-scripts` run worked, so the frontend tests are clean; the earlier failure was just the sandbox refusing Jest worker processes. I’m collecting the final file diffs and then I’ll update the lifecycle task file with step evidence, validation outputs, and an `Awaiting user verification` status because this changed the visible inbox/voice flow.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git status --short' in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Path 'C:\\Users\\edebe\\eds\\bizPA\\backend\\src\\controllers\\voiceController.js' | Select-Object -Skip 300 -First 150" in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Path 'C:\\Users\\edebe\\eds\\bizPA\\backend\\verify_voice_micro_decisions.js'" in C:\Users\edebe\eds succeeded in 3.84s:
const assert = require('assert');
const path = require('path');

class MockDb {
  constructor() {
    this.calls = [];
    this.matchSuggestions = [
      { bank_txn_id: 'txn-1' },
      { bank_txn_id: 'txn-2' },
      { bank_txn_id: 'txn-3' }
    ];
    this.classifications = new Map();
    this.auditEntries = [];
  }

  get pool() {
    return {
      connect: async () => ({
        query: this.query.bind(this),
        release() {},
 succeeded in 6.01s:
M  DataInsights/src/Constants.py
 M TradeApps
 M api_server_sql/main.py
 D grid_live_monitor.py
R  workstream/000_backlog/.keep -> workstream/000_epic/.keep
RD "workstream/000_backlog/20260222_205900_pipHunter_signal_marketplace_mobile_functionality_v2 (3).md" -> "workstream/000_epic/20260222_205900_pipHunter_signal_marketplace_mobile_functionality_v2 (3).md"
RD workstream/000_backlog/20260225_104010_live_market_data_pipeline_prompt.md -> workstream/000_epic/20260225_104010_live_market_data_pipeline_prompt.md
RD workstream/000_backlog/20260227_022357_sFX_Technical_Design_Brief_v2.md -> workstream/000_epic/20260227_022357_sFX_Technical_Design_Brief_v2.md
AD workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
AD workstream/000_epic/20260313_165611_bizPA_epic.md
AD "workstream/000_epic/20260313_165611_unassigned_Autonomous Trading Signal Platform.md"
AD workstream/000_epic/20260313_165611_unassigned_bizPA.md
A  workstream/000_epic/bizPA_disccussion.txt
R  workstream/000_backlog/claude/.keep -> workstream/000_epic/claude/.keep
A  workstream/000_epic/claude/20260305_100142_claude_etrmui_etrui_processed.md
R  workstream/000_backlog/codex/.keep -> workstream/000_epic/codex/.keep
R  workstream/000_backlog/codex/20260303_152309_codex_afrix_build_prompt_processed.md -> workstream/000_epic/codex/20260303_152309_codex_afrix_build_prompt_processed.md
A  workstream/000_epic/codex/20260303_152309_codex_afrix_build_prompt_review.md
R  workstream/000_backlog/codex/20260303_171914_codex_general_product_requirements_document__prd__-_sfx_processed.md -> workstream/000_epic/codex/20260303_171914_codex_general_product_requirements_document__prd__-_sfx_processed.md
R  workstream/000_backlog/codex/20260304_142350_codex_linkage_verification_processed.md -> workstream/000_epic/codex/20260304_142350_codex_linkage_verification_processed.md
 succeeded in 7.14s:
    transcript = '',
    bank_txn_id = null,
    evidence_id = null,
    context_kind = null,
    context_scope = null,
  } = req.body || {};
  const rawTranscript = String(transcript).trim();
  const text = rawTranscript.toLowerCase();
  const normalizedContext = context_kind ?? context_scope;
  const parsedCommand = parseVoiceMicroDecision(rawTranscript);
  const contextValidation = validateMicroDecisionContext(parsedCommand, normalizedContext);
  const normalizedContextScope = contextValidation.ok
    ? contextValidation.context_kind
    : (normalizedContext === null || normalizedContext === undefined
      ? null
      : String(normalizedContext).toLowerCase().replace(/-/g, '_'));

  if (!text) return res.status(400).json({ error: 'Missing transcript.' });
  if (!contextValidation.ok) return res.status(contextValidation.statusCode).json({ error: contextValidation.error });

file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..e6d93b4bc1cd6960ce27238cfb7e90c3322f41e0
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,6 +10,36 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+const {
+  parseVoiceMicroDecision,
+  validateMicroDecisionContext,
+} = require('../services/voiceMicroDecisionService');
+
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undoable: undoSupported,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
 
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
@@ -267,76 +297,177 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
-  const text = String(transcript).toLowerCase().trim();
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_kind = null,
+    context_scope = null,
+  } = req.body || {};
+  const rawTranscript = String(transcript).trim();
+  const text = rawTranscript.toLowerCase();
+  const normalizedContext = context_kind ?? context_scope;
+  const parsedCommand = parseVoiceMicroDecision(rawTranscript);
+  const contextValidation = validateMicroDecisionContext(parsedCommand, normalizedContext);
+  const normalizedContextScope = contextValidation.ok
+    ? contextValidation.context_kind
+    : (normalizedContext === null || normalizedContext === undefined
+      ? null
+      : String(normalizedContext).toLowerCase().replace(/-/g, '_'));
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
+  if (!contextValidation.ok) return res.status(contextValidation.statusCode).json({ error: contextValidation.error });
 
   try {
-    if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
-      const categoryName = text.replace('category:', '').trim();
-      const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+    if (parsedCommand.type === 'category') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: parsedCommand.category_code,
+            category_name: parsedCommand.category_name,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'category', label: parsedCommand.category_name }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
-      const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+    if (parsedCommand.type === 'business_personal') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const bp = parsedCommand.business_personal;
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    const splitMatch = text.match(/split\s+(\d{1,3})%?/);
-    if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
-      const pct = Number(splitMatch[1]);
+    if (parsedCommand.type === 'split') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const pct = Number(parsedCommand.split_business_pct);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+    if (parsedCommand.type === 'attach_receipt') {
+      if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for attach receipt commands.' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          parsed_command: parsedCommand,
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
-    if (['match first', 'match second', 'match third'].includes(text)) {
+    if (parsedCommand.type === 'match_rank') {
       if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for match commands.' });
-      const index = text === 'match first' ? 1 : text === 'match second' ? 2 : 3;
+      const index = parsedCommand.match_rank;
       const suggestions = await db.query(
         `
         WITH ev AS (
@@ -364,10 +495,24 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          bank_txn_id: chosen,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text === 'no match') {
+    if (parsedCommand.type === 'no_match') {
       if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for no match.' });
       await db.query(
         `
@@ -376,7 +521,20 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/backend/verify_voice_micro_decisions.js b/backend/verify_voice_micro_decisions.js
index 25d9ab02050aac37a5d38aca5c6f0154de9bb787..15c93efa65f133ec1a59d827ecbfa29a95cf4e6d
--- a/backend/verify_voice_micro_decisions.js
+++ b/backend/verify_voice_micro_decisions.js
@@ -9,16 +9,70 @@
       { bank_txn_id: 'txn-2' },
       { bank_txn_id: 'txn-3' }
     ];
+    this.classifications = new Map();
+    this.auditEntries = [];
   }
 
-  async query(text, params) {
+  get pool() {
+    return {
+      connect: async () => ({
+        query: this.query.bind(this),
+        release() {},
+      }),
+    };
+  }
+
+  async query(text, params = []) {
     const sql = text.replace(/\s+/g, ' ').trim();
     this.calls.push({ sql, params });
 
+    if (sql === 'BEGIN' || sql === 'COMMIT' || sql === 'ROLLBACK') {
+      return { rows: [] };
+    }
+
+    if (sql.includes('SELECT * FROM transaction_classifications')) {
+      const existing = this.classifications.get(params[0]);
+      return { rows: existing ? [existing] : [] };
+    }
+
+    if (sql.includes('INSERT INTO transaction_classifications')) {
+      const next = {
+        user_id: params[0],
+        bank_txn_id: params[1],
+        category_code: params[2],
+        category_name: params[3],
+        business_personal: params[4],
+        is_split: params[5],
+        split_business_pct: params[6],
+        confidence: params[7],
+        source: params[8],
+      };
+      this.classifications.set(params[1], next);
+      return { rows: [next] };
+    }
+
+    if (sql.includes('INSERT INTO transaction_audit_log')) {
+      this.auditEntries.push({
+        bank_txn_id: params[1],
+        previous_value: params[4],
+        new_value: params[5],
+        change_source: params[6],
+      });
+      return { rows: [] };
+    }
+
+    if (sql.includes('SELECT txn_date FROM bank_transactions')) {
+      return { rows: [{ txn_date: '2026-03-12' }] };
+    }
+
     if (sql.includes('FROM bank_transactions bt CROSS JOIN ev')) {
       return { rows: this.matchSuggestions };
     }
 
+    if (sql.includes('INSERT INTO evidence_links')) {
+      return { rows: [] };
+    }
+
     return { rows: [] };
   }
 }
@@ -69,45 +123,79 @@
   const mockDb = new MockDb();
   const controller = loadController(mockDb);
 
+  const missingContext = await invoke(controller, {
+    transcript: 'Business',
+    bank_txn_id: 'txn-123'
+  });
+  assert.strictEqual(missingContext.statusCode, 400);
+  assert.strictEqual(missingContext.body.error, 'context_kind is required for MVP voice micro-decisions.');
+
   const category = await invoke(controller, {
     transcript: 'Category: Travel',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(category.statusCode, 200);
-  assert.strictEqual(category.body.confirmation_chip, 'Applied: travel');
-  assert(
-    mockDb.calls.some((call) => call.sql.includes('INSERT INTO transaction_classifications') && call.params[2] === 'TRAVEL' && call.params[3] === 'travel'),
-    'Expected category command to persist manual classification'
-  );
+  assert.strictEqual(category.body.confirmation_chip, 'Applied: Travel');
+  assert.strictEqual(category.body.undo.supported, true);
+  assert.deepStrictEqual(category.body.context_binding, {
+    scope: 'inbox',
+    target_type: 'bank_transaction',
+    target_id: 'txn-123'
+  });
+  assert.strictEqual(mockDb.classifications.get('txn-123').category_code, 'TRAVEL');
+  assert.strictEqual(mockDb.auditEntries.at(-1).change_source, 'voice');
 
   const business = await invoke(controller, {
     transcript: 'Business',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(business.statusCode, 200);
   assert.strictEqual(business.body.confirmation_chip, 'Applied: BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').category_code, 'TRAVEL');
+  assert.strictEqual(mockDb.classifications.get('txn-123').business_personal, 'BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').is_split, false);
 
   const split = await invoke(controller, {
     transcript: 'Split 70%',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(split.statusCode, 200);
   assert.strictEqual(split.body.confirmation_chip, 'Applied: Split 70%');
+  assert.strictEqual(split.body.undo.supported, true);
+  assert.strictEqual(mockDb.classifications.get('txn-123').business_personal, 'BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').is_split, true);
+  assert.strictEqual(mockDb.classifications.get('txn-123').split_business_pct, 70);
 
   const attachReceipt = await invoke(controller, {
     transcript: 'Attach receipt',
-    bank_txn_id: 'txn-123'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(attachReceipt.statusCode, 200);
   assert.strictEqual(attachReceipt.body.action_status, 'clarification_needed');
   assert.strictEqual(attachReceipt.body.confirmation_chip, 'Attach receipt requested');
+  assert.strictEqual(attachReceipt.body.undo.supported, false);
+  assert.deepStrictEqual(attachReceipt.body.context_binding, {
+    scope: 'evidence',
+    target_type: 'evidence',
+    target_id: 'evidence-1'
+  });
 
   const matchSecond = await invoke(controller, {
     transcript: 'Match second',
-    evidence_id: 'evidence-1'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(matchSecond.statusCode, 200);
   assert.strictEqual(matchSecond.body.confirmation_chip, 'Applied: Match 2');
+  assert.deepStrictEqual(matchSecond.body.context_binding, {
+    scope: 'evidence',
+    target_type: 'evidence',
+    target_id: 'evidence-1'
+  });
   assert(
     mockDb.calls.some((call) => call.sql.includes('INSERT INTO evidence_links') && call.params[1] === 'evidence-1' && call.params[2] === 'txn-2'),
     'Expected match command to persist selected evidence link'
@@ -115,29 +203,41 @@
 
   const noMatch = await invoke(controller, {
     transcript: 'No match',
-    evidence_id: 'evidence-1'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(noMatch.statusCode, 200);
   assert.strictEqual(noMatch.body.confirmation_chip, 'Applied: No match');
 
   const invalidSplit = await invoke(controller, {
     transcript: 'Split 140%',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(invalidSplit.statusCode, 400);
   assert.strictEqual(invalidSplit.body.error, 'Split percentage must be 0-100.');
 
+  const unsafeContext = await invoke(controller, {
+    transcript: 'Category: Travel',
+    bank_txn_id: 'txn-123',
+    context_kind: 'evidence'
+  });
+  assert.strictEqual(unsafeContext.statusCode, 409);
+  assert.strictEqual(unsafeContext.body.error, 'Command "category" is not allowed for evidence context.');
+
   const unsupported = await invoke(controller, {
-    transcript: 'Archive this forever'
+    transcript: 'Archive this forever',
+    context_kind: 'inbox'
   });
   assert.strictEqual(unsupported.statusCode, 400);
   assert.strictEqual(unsupported.body.error, 'Unsupported MVP voice micro-decision command.');
 
   console.log('voice_micro_decisions_ok');
-  console.log('category_business_split_ok=true');
+  console.log('context_guardrails_ok=true');
+  console.log('audited_classification_updates_ok=true');
   console.log('attach_receipt_clarification_ok=true');
   console.log('match_selection_ok=true');
-  console.log('no_match_ok=true');
+  console.log('safe_context_binding_ok=true');
   console.log('validation_errors_ok=true');
 }
 
diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index 9b145fadac9123e3fa01b12af6ef2b9916dc11de..b21180e549e09be4a5ed0412e53087d2a52f4382
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -50,6 +50,11 @@
   buildQuarterUiState,
   deriveResolvedIssueIdsFromSearchParams,
 } from './quarterReadiness';
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -427,6 +432,13 @@
   .date-group-count { border-radius: 999px; padding: 4px 9px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.72rem; color: var(--text-muted); }
   .date-group-body { padding: 0 12px 12px; }
   .voice-preview { background: rgba(0,0,0,0.82); color: white; padding: 8px 15px; border-radius: 15px; margin-bottom: 8px; font-size: 0.72rem; max-width: 240px; text-align: right; }
+  .voice-context-banner { display: grid; gap: 10px; padding: 14px 16px; margin-bottom: 16px; border-radius: 18px; background: var(--bg-card); border: 1px solid var(--border); box-shadow: var(--shadow); }
+  .voice-context-row, .voice-chip-actions, .voice-chip-labels { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
+  .voice-context-pill, .voice-action-pill { border-radius: 999px; padding: 6px 10px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.74rem; font-weight: 700; }
+  .voice-context-pill.active { background: rgba(13,92,99,0.12); color: var(--primary-strong); border-color: rgba(13,92,99,0.24); }
+  .voice-micro-chip { display: grid; gap: 10px; padding: 14px 16px; border-radius: 18px; background: rgba(13,92,99,0.08); border: 1px solid rgba(13,92,99,0.18); }
+  .voice-micro-chip strong { display: block; }
+  .inbox-action-card.voice-target { border-color: rgba(13,92,99,0.36); box-shadow: 0 0 0 2px rgba(13,92,99,0.12), var(--shadow); }
   .spinner { animation: rotate 2s linear infinite; }
   .capture-grid { display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(320px, 0.95fr); gap: 18px; align-items: start; }
   .command-centre { display: grid; gap: 18px; }
@@ -1382,6 +1394,8 @@
   const [inboxDrafts, setInboxDrafts] = useState({});
   const [inboxBusyId, setInboxBusyId] = useState('');
   const [inboxMessage, setInboxMessage] = useState(null);
+  const [activeVoiceContextId, setActiveVoiceContextId] = useState('');
+  const [voiceMicroDecisionChip, setVoiceMicroDecisionChip] = useState(null);
   const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
@@ -1606,10 +1620,20 @@
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
   const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const activeVoiceCard = findVoiceContextCard(inboxCards, activeVoiceContextId);
   const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
   const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
   const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
+  useEffect(() => {
+    if (!activeVoiceContextId) {
+      return;
+    }
+    if (!findVoiceContextCard(inboxCards, activeVoiceContextId)) {
+      setActiveVoiceContextId('');
+    }
+  }, [activeVoiceContextId, inboxCards]);
+
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
   const canManageGovernance = hasPermission(currentUser, PERMISSIONS.GOVERNANCE_MANAGE);
@@ -1806,11 +1830,13 @@
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxDrafts({});
+        setVoiceMicroDecisionChip(null);
         setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
         return;
       }
       await axios.post(`${API_BASE_URL}/inbox/undo-last`);
       await fetchAllData();
+      setVoiceMicroDecisionChip(null);
       setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
     } catch (error) {
       setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
@@ -1991,22 +2017,51 @@
     const cleanText = text.replace(/┬ú/g, 'GBP ');
     try {
       setVoiceStatus('processing');
-      const response = await axios.post(`${API_BASE_URL}/voice/process`, {
+      const microDecisionRequest = buildVoiceMicroDecisionRequest({
+        currentTab,
         transcript: cleanText,
-        device_id: 'mobile-v139',
-        current_date: new Date().toISOString(),
+        activeVoiceContextId,
+        inboxCards,
       });
-      if (response.data.confirmation_text && window.SpeechSynthesisUtterance) {
+
+      if (microDecisionRequest?.error) {
+        setInboxMessage({ type: 'error', text: microDecisionRequest.error });
+        setCurrentTab('inbox');
+        return;
+      }
+
+      const response = microDecisionRequest
+        ? await axios.post(`${API_BASE_URL}${microDecisionRequest.endpoint}`, microDecisionRequest.payload)
+        : await axios.post(`${API_BASE_URL}/voice/process`, {
+            transcript: cleanText,
+            device_id: 'mobile-v139',
+            current_date: new Date().toISOString(),
+          });
+      const spokenConfirmation = response.data.confirmation_chip || response.data.confirmation_text;
+      if (spokenConfirmation && window.SpeechSynthesisUtterance) {
         try {
-          const utterance = new window.SpeechSynthesisUtterance(response.data.confirmation_text);
+          const utterance = new window.SpeechSynthesisUtterance(spokenConfirmation);
           window.speechSynthesis.speak(utterance);
         } catch (_) {
         }
       }
-      executeVoiceAction(response.data);
-      fetchAllData();
+      if (microDecisionRequest) {
+        setVoiceMicroDecisionChip((previous) => reduceVoiceMicroDecisionChip(previous, response.data, microDecisionRequest.card));
+        setInboxMessage({
+          type: 'success',
+          text: response.data.confirmation_chip || 'Voice triage action applied.',
+        });
+      } else {
+        executeVoiceAction(response.data);
+      }
+      await fetchAllData();
     } catch (error) {
-      alert('AI Error');
+      const message = error?.response?.data?.error || 'AI Error';
+      if (currentTab === 'inbox' || currentTab === 'quarter') {
+        setInboxMessage({ type: 'error', text: message });
+      } else {
+        alert(message);
+      }
     } finally {
       setVoiceStatus('idle');
       setIsListening(false);
@@ -2894,7 +2949,11 @@
             const saving = inboxBusyId === card.id;
             const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
             return (
-              <section key={card.id} className="inbox-action-card">
+              <section
+                key={card.id}
+                className={`inbox-action-card ${activeVoiceContextId === card.id ? 'voice-target' : ''}`}
+                onClick={() => setActiveVoiceContextId(card.id)}
+              >
                 <div className="inbox-card-top">
                   <div>
                     <strong>{card.merchant}</strong>
@@ -2914,6 +2973,9 @@
                       {label}
                     </span>
                   ))}
+                  {activeVoiceContextId === card.id && (
+                    <span className="voice-context-pill active">Voice target</span>
+                  )}
                   {card.suggested_category?.category_name && (
                     <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
                       <CheckCircle2 size={14} />
@@ -3357,6 +3419,9 @@
   };
 
   const openReadinessWorkflow = (issue) => {
+    if (issue?.affected_entity_id) {
+      setActiveVoiceContextId(String(issue.affected_entity_id));
+    }
     if (issue?.resolution_target?.workflow === '/inbox/finish-now') {
       setCurrentTab('inbox');
       return;
@@ -3364,6 +3429,76 @@
     setCurrentTab('inbox');
   };
 
+  const renderVoiceContextBanner = () => {
+    if (!(currentTab === 'inbox' || currentTab === 'quarter')) {
+      return null;
+    }
+
+    return (
+      <section className="voice-context-banner">
+        <div>
+          <strong>Voice triage</strong>
+          <div className="small-muted">
+            Use the mic against one selected finish-now item only. Supported commands here are category, business, personal, split n%, and attach receipt.
+          </div>
+        </div>
+        <div className="voice-context-row">
+          <span className={`voice-context-pill ${activeVoiceCard ? 'active' : ''}`}>
+            {activeVoiceCard
+              ? `Target: ${activeVoiceCard.merchant} (${activeVoiceCard.id})`
+              : 'Select a blocker card to arm voice triage'}
+          </span>
+          {activeVoiceCard && (
+            <button className="secondary-action" type="button" onClick={() => setCurrentTab('inbox')}>
+              Edit target
+            </button>
+          )}
+        </div>
+        {voiceMicroDecisionChip && (
+          <div className="voice-micro-chip">
+            <div>
+              <strong>{voiceMicroDecisionChip.confirmationChip}</strong>
+              <div className="small-muted">
+                {voiceMicroDecisionChip.targetLabel} ({voiceMicroDecisionChip.targetId})
+              </div>
+            </div>
+            {voiceMicroDecisionChip.labels.length > 0 && (
+              <div className="voice-chip-labels">
+                {voiceMicroDecisionChip.labels.map((label) => (
+                  <span key={`${voiceMicroDecisionChip.targetId}-${label}`} className="voice-action-pill">
+                    {label}
+                  </span>
+                ))}
+              </div>
+            )}
+            <div className="voice-chip-actions">
+              <button
+                className="secondary-action"
+                type="button"
+                onClick={() => {
+                  setCurrentTab('inbox');
+                  setActiveVoiceContextId(voiceMicroDecisionChip.targetId);
+                }}
+              >
+                Edit
+              </button>
+              {voiceMicroDecisionChip.undoSupported && (
+                <button
+                  className="secondary-action"
+                  type="button"
+                  onClick={undoLastInboxAction}
+                  disabled={inboxBusyId === 'undo-last'}
+                >
+                  Undo
+                </button>
+              )}
+            </div>
+          </div>
+        )}
+      </section>
+    );
+  };
+
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
@@ -3654,6 +3789,13 @@
                     </div>
                   </div>
                   <div className="finish-now-actions">
+                    <button
+                      className="secondary-action"
+                      type="button"
+                      onClick={() => setActiveVoiceContextId(String(issue.affected_entity_id))}
+                    >
+                      {activeVoiceContextId === String(issue.affected_entity_id) ? 'Voice target set' : 'Arm voice'}
+                    </button>
                     {(readinessDemoEnabled || snapshotDemoEnabled) ? (
                       <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
                         Mark fixed
@@ -4391,6 +4533,7 @@
         </button>
       </section>
       {connectionError && <div className="alert alert-danger">{connectionError}</div>}
+      {renderVoiceContextBanner()}
 
       {loading ? (
         <div className="text-center py-5">Loading...</div>
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..6eb469fcc7f023f8545d9e952a5823fb8c47a88d
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});

Plan update
Implementation and technical validation are complete. The remaining gate is explicit user verification because the inbox/quarter voice flow changed in a user-visible way.
  ✓ Inspect existing voice micro-decision backend and inbox UI paths; define the minimal context-safe flow to implement
  ✓ Implement backend guardrails and frontend inbox/finish-now voice binding with confirmation chip and undo support
  ✓ Add or update automated verification for the new behavior and run validations
  → Update the lifecycle task file with ordered evidence, validation results, and current completion state
file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..e6d93b4bc1cd6960ce27238cfb7e90c3322f41e0
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,6 +10,36 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+const {
+  parseVoiceMicroDecision,
+  validateMicroDecisionContext,
+} = require('../services/voiceMicroDecisionService');
+
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undoable: undoSupported,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
 
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
@@ -267,76 +297,177 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
-  const text = String(transcript).toLowerCase().trim();
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_kind = null,
+    context_scope = null,
+  } = req.body || {};
+  const rawTranscript = String(transcript).trim();
+  const text = rawTranscript.toLowerCase();
+  const normalizedContext = context_kind ?? context_scope;
+  const parsedCommand = parseVoiceMicroDecision(rawTranscript);
+  const contextValidation = validateMicroDecisionContext(parsedCommand, normalizedContext);
+  const normalizedContextScope = contextValidation.ok
+    ? contextValidation.context_kind
+    : (normalizedContext === null || normalizedContext === undefined
+      ? null
+      : String(normalizedContext).toLowerCase().replace(/-/g, '_'));
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
+  if (!contextValidation.ok) return res.status(contextValidation.statusCode).json({ error: contextValidation.error });
 
   try {
-    if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
-      const categoryName = text.replace('category:', '').trim();
-      const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+    if (parsedCommand.type === 'category') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: parsedCommand.category_code,
+            category_name: parsedCommand.category_name,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'category', label: parsedCommand.category_name }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
-      const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+    if (parsedCommand.type === 'business_personal') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const bp = parsedCommand.business_personal;
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    const splitMatch = text.match(/split\s+(\d{1,3})%?/);
-    if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
-      const pct = Number(splitMatch[1]);
+    if (parsedCommand.type === 'split') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const pct = Number(parsedCommand.split_business_pct);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+    if (parsedCommand.type === 'attach_receipt') {
+      if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for attach receipt commands.' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          parsed_command: parsedCommand,
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
-    if (['match first', 'match second', 'match third'].includes(text)) {
+    if (parsedCommand.type === 'match_rank') {
       if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for match commands.' });
-      const index = text === 'match first' ? 1 : text === 'match second' ? 2 : 3;
+      const index = parsedCommand.match_rank;
       const suggestions = await db.query(
         `
         WITH ev AS (
@@ -364,10 +495,24 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          bank_txn_id: chosen,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text === 'no match') {
+    if (parsedCommand.type === 'no_match') {
       if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for no match.' });
       await db.query(
         `
@@ -376,7 +521,20 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/backend/verify_voice_micro_decisions.js b/backend/verify_voice_micro_decisions.js
index 25d9ab02050aac37a5d38aca5c6f0154de9bb787..15c93efa65f133ec1a59d827ecbfa29a95cf4e6d
--- a/backend/verify_voice_micro_decisions.js
+++ b/backend/verify_voice_micro_decisions.js
@@ -9,16 +9,70 @@
       { bank_txn_id: 'txn-2' },
       { bank_txn_id: 'txn-3' }
     ];
+    this.classifications = new Map();
+    this.auditEntries = [];
   }
 
-  async query(text, params) {
+  get pool() {
+    return {
+      connect: async () => ({
+        query: this.query.bind(this),
+        release() {},
+      }),
+    };
+  }
+
+  async query(text, params = []) {
     const sql = text.replace(/\s+/g, ' ').trim();
     this.calls.push({ sql, params });
 
+    if (sql === 'BEGIN' || sql === 'COMMIT' || sql === 'ROLLBACK') {
+      return { rows: [] };
+    }
+
+    if (sql.includes('SELECT * FROM transaction_classifications')) {
+      const existing = this.classifications.get(params[0]);
+      return { rows: existing ? [existing] : [] };
+    }
+
+    if (sql.includes('INSERT INTO transaction_classifications')) {
+      const next = {
+        user_id: params[0],
+        bank_txn_id: params[1],
+        category_code: params[2],
+        category_name: params[3],
+        business_personal: params[4],
+        is_split: params[5],
+        split_business_pct: params[6],
+        confidence: params[7],
+        source: params[8],
+      };
+      this.classifications.set(params[1], next);
+      return { rows: [next] };
+    }
+
+    if (sql.includes('INSERT INTO transaction_audit_log')) {
+      this.auditEntries.push({
+        bank_txn_id: params[1],
+        previous_value: params[4],
+        new_value: params[5],
+        change_source: params[6],
+      });
+      return { rows: [] };
+    }
+
+    if (sql.includes('SELECT txn_date FROM bank_transactions')) {
+      return { rows: [{ txn_date: '2026-03-12' }] };
+    }
+
     if (sql.includes('FROM bank_transactions bt CROSS JOIN ev')) {
       return { rows: this.matchSuggestions };
     }
 
+    if (sql.includes('INSERT INTO evidence_links')) {
+      return { rows: [] };
+    }
+
     return { rows: [] };
   }
 }
@@ -69,45 +123,79 @@
   const mockDb = new MockDb();
   const controller = loadController(mockDb);
 
+  const missingContext = await invoke(controller, {
+    transcript: 'Business',
+    bank_txn_id: 'txn-123'
+  });
+  assert.strictEqual(missingContext.statusCode, 400);
+  assert.strictEqual(missingContext.body.error, 'context_kind is required for MVP voice micro-decisions.');
+
   const category = await invoke(controller, {
     transcript: 'Category: Travel',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(category.statusCode, 200);
-  assert.strictEqual(category.body.confirmation_chip, 'Applied: travel');
-  assert(
-    mockDb.calls.some((call) => call.sql.includes('INSERT INTO transaction_classifications') && call.params[2] === 'TRAVEL' && call.params[3] === 'travel'),
-    'Expected category command to persist manual classification'
-  );
+  assert.strictEqual(category.body.confirmation_chip, 'Applied: Travel');
+  assert.strictEqual(category.body.undo.supported, true);
+  assert.deepStrictEqual(category.body.context_binding, {
+    scope: 'inbox',
+    target_type: 'bank_transaction',
+    target_id: 'txn-123'
+  });
+  assert.strictEqual(mockDb.classifications.get('txn-123').category_code, 'TRAVEL');
+  assert.strictEqual(mockDb.auditEntries.at(-1).change_source, 'voice');
 
   const business = await invoke(controller, {
     transcript: 'Business',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(business.statusCode, 200);
   assert.strictEqual(business.body.confirmation_chip, 'Applied: BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').category_code, 'TRAVEL');
+  assert.strictEqual(mockDb.classifications.get('txn-123').business_personal, 'BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').is_split, false);
 
   const split = await invoke(controller, {
     transcript: 'Split 70%',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(split.statusCode, 200);
   assert.strictEqual(split.body.confirmation_chip, 'Applied: Split 70%');
+  assert.strictEqual(split.body.undo.supported, true);
+  assert.strictEqual(mockDb.classifications.get('txn-123').business_personal, 'BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').is_split, true);
+  assert.strictEqual(mockDb.classifications.get('txn-123').split_business_pct, 70);
 
   const attachReceipt = await invoke(controller, {
     transcript: 'Attach receipt',
-    bank_txn_id: 'txn-123'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(attachReceipt.statusCode, 200);
   assert.strictEqual(attachReceipt.body.action_status, 'clarification_needed');
   assert.strictEqual(attachReceipt.body.confirmation_chip, 'Attach receipt requested');
+  assert.strictEqual(attachReceipt.body.undo.supported, false);
+  assert.deepStrictEqual(attachReceipt.body.context_binding, {
+    scope: 'evidence',
+    target_type: 'evidence',
+    target_id: 'evidence-1'
+  });
 
   const matchSecond = await invoke(controller, {
     transcript: 'Match second',
-    evidence_id: 'evidence-1'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(matchSecond.statusCode, 200);
   assert.strictEqual(matchSecond.body.confirmation_chip, 'Applied: Match 2');
+  assert.deepStrictEqual(matchSecond.body.context_binding, {
+    scope: 'evidence',
+    target_type: 'evidence',
+    target_id: 'evidence-1'
+  });
   assert(
     mockDb.calls.some((call) => call.sql.includes('INSERT INTO evidence_links') && call.params[1] === 'evidence-1' && call.params[2] === 'txn-2'),
     'Expected match command to persist selected evidence link'
@@ -115,29 +203,41 @@
 
   const noMatch = await invoke(controller, {
     transcript: 'No match',
-    evidence_id: 'evidence-1'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(noMatch.statusCode, 200);
   assert.strictEqual(noMatch.body.confirmation_chip, 'Applied: No match');
 
   const invalidSplit = await invoke(controller, {
     transcript: 'Split 140%',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(invalidSplit.statusCode, 400);
   assert.strictEqual(invalidSplit.body.error, 'Split percentage must be 0-100.');
 
+  const unsafeContext = await invoke(controller, {
+    transcript: 'Category: Travel',
+    bank_txn_id: 'txn-123',
+    context_kind: 'evidence'
+  });
+  assert.strictEqual(unsafeContext.statusCode, 409);
+  assert.strictEqual(unsafeContext.body.error, 'Command "category" is not allowed for evidence context.');
+
   const unsupported = await invoke(controller, {
-    transcript: 'Archive this forever'
+    transcript: 'Archive this forever',
+    context_kind: 'inbox'
   });
   assert.strictEqual(unsupported.statusCode, 400);
   assert.strictEqual(unsupported.body.error, 'Unsupported MVP voice micro-decision command.');
 
   console.log('voice_micro_decisions_ok');
-  console.log('category_business_split_ok=true');
+  console.log('context_guardrails_ok=true');
+  console.log('audited_classification_updates_ok=true');
   console.log('attach_receipt_clarification_ok=true');
   console.log('match_selection_ok=true');
-  console.log('no_match_ok=true');
+  console.log('safe_context_binding_ok=true');
   console.log('validation_errors_ok=true');
 }
 
diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index 9b145fadac9123e3fa01b12af6ef2b9916dc11de..b21180e549e09be4a5ed0412e53087d2a52f4382
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -50,6 +50,11 @@
   buildQuarterUiState,
   deriveResolvedIssueIdsFromSearchParams,
 } from './quarterReadiness';
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -427,6 +432,13 @@
   .date-group-count { border-radius: 999px; padding: 4px 9px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.72rem; color: var(--text-muted); }
   .date-group-body { padding: 0 12px 12px; }
   .voice-preview { background: rgba(0,0,0,0.82); color: white; padding: 8px 15px; border-radius: 15px; margin-bottom: 8px; font-size: 0.72rem; max-width: 240px; text-align: right; }
+  .voice-context-banner { display: grid; gap: 10px; padding: 14px 16px; margin-bottom: 16px; border-radius: 18px; background: var(--bg-card); border: 1px solid var(--border); box-shadow: var(--shadow); }
+  .voice-context-row, .voice-chip-actions, .voice-chip-labels { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
+  .voice-context-pill, .voice-action-pill { border-radius: 999px; padding: 6px 10px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.74rem; font-weight: 700; }
+  .voice-context-pill.active { background: rgba(13,92,99,0.12); color: var(--primary-strong); border-color: rgba(13,92,99,0.24); }
+  .voice-micro-chip { display: grid; gap: 10px; padding: 14px 16px; border-radius: 18px; background: rgba(13,92,99,0.08); border: 1px solid rgba(13,92,99,0.18); }
+  .voice-micro-chip strong { display: block; }
+  .inbox-action-card.voice-target { border-color: rgba(13,92,99,0.36); box-shadow: 0 0 0 2px rgba(13,92,99,0.12), var(--shadow); }
   .spinner { animation: rotate 2s linear infinite; }
   .capture-grid { display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(320px, 0.95fr); gap: 18px; align-items: start; }
   .command-centre { display: grid; gap: 18px; }
@@ -1382,6 +1394,8 @@
   const [inboxDrafts, setInboxDrafts] = useState({});
   const [inboxBusyId, setInboxBusyId] = useState('');
   const [inboxMessage, setInboxMessage] = useState(null);
+  const [activeVoiceContextId, setActiveVoiceContextId] = useState('');
+  const [voiceMicroDecisionChip, setVoiceMicroDecisionChip] = useState(null);
   const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
@@ -1606,10 +1620,20 @@
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
   const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const activeVoiceCard = findVoiceContextCard(inboxCards, activeVoiceContextId);
   const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
   const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
   const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
+  useEffect(() => {
+    if (!activeVoiceContextId) {
+      return;
+    }
+    if (!findVoiceContextCard(inboxCards, activeVoiceContextId)) {
+      setActiveVoiceContextId('');
+    }
+  }, [activeVoiceContextId, inboxCards]);
+
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
   const canManageGovernance = hasPermission(currentUser, PERMISSIONS.GOVERNANCE_MANAGE);
@@ -1806,11 +1830,13 @@
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxDrafts({});
+        setVoiceMicroDecisionChip(null);
         setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
         return;
       }
       await axios.post(`${API_BASE_URL}/inbox/undo-last`);
       await fetchAllData();
+      setVoiceMicroDecisionChip(null);
       setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
     } catch (error) {
       setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
@@ -1991,22 +2017,51 @@
     const cleanText = text.replace(/┬ú/g, 'GBP ');
     try {
       setVoiceStatus('processing');
-      const response = await axios.post(`${API_BASE_URL}/voice/process`, {
+      const microDecisionRequest = buildVoiceMicroDecisionRequest({
+        currentTab,
         transcript: cleanText,
-        device_id: 'mobile-v139',
-        current_date: new Date().toISOString(),
+        activeVoiceContextId,
+        inboxCards,
       });
-      if (response.data.confirmation_text && window.SpeechSynthesisUtterance) {
+
+      if (microDecisionRequest?.error) {
+        setInboxMessage({ type: 'error', text: microDecisionRequest.error });
+        setCurrentTab('inbox');
+        return;
+      }
+
+      const response = microDecisionRequest
+        ? await axios.post(`${API_BASE_URL}${microDecisionRequest.endpoint}`, microDecisionRequest.payload)
+        : await axios.post(`${API_BASE_URL}/voice/process`, {
+            transcript: cleanText,
+            device_id: 'mobile-v139',
+            current_date: new Date().toISOString(),
+          });
+      const spokenConfirmation = response.data.confirmation_chip || response.data.confirmation_text;
+      if (spokenConfirmation && window.SpeechSynthesisUtterance) {
         try {
-          const utterance = new window.SpeechSynthesisUtterance(response.data.confirmation_text);
+          const utterance = new window.SpeechSynthesisUtterance(spokenConfirmation);
           window.speechSynthesis.speak(utterance);
         } catch (_) {
         }
       }
-      executeVoiceAction(response.data);
-      fetchAllData();
+      if (microDecisionRequest) {
+        setVoiceMicroDecisionChip((previous) => reduceVoiceMicroDecisionChip(previous, response.data, microDecisionRequest.card));
+        setInboxMessage({
+          type: 'success',
+          text: response.data.confirmation_chip || 'Voice triage action applied.',
+        });
+      } else {
+        executeVoiceAction(response.data);
+      }
+      await fetchAllData();
     } catch (error) {
-      alert('AI Error');
+      const message = error?.response?.data?.error || 'AI Error';
+      if (currentTab === 'inbox' || currentTab === 'quarter') {
+        setInboxMessage({ type: 'error', text: message });
+      } else {
+        alert(message);
+      }
     } finally {
       setVoiceStatus('idle');
       setIsListening(false);
@@ -2894,7 +2949,11 @@
             const saving = inboxBusyId === card.id;
             const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
             return (
-              <section key={card.id} className="inbox-action-card">
+              <section
+                key={card.id}
+                className={`inbox-action-card ${activeVoiceContextId === card.id ? 'voice-target' : ''}`}
+                onClick={() => setActiveVoiceContextId(card.id)}
+              >
                 <div className="inbox-card-top">
                   <div>
                     <strong>{card.merchant}</strong>
@@ -2914,6 +2973,9 @@
                       {label}
                     </span>
                   ))}
+                  {activeVoiceContextId === card.id && (
+                    <span className="voice-context-pill active">Voice target</span>
+                  )}
                   {card.suggested_category?.category_name && (
                     <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
                       <CheckCircle2 size={14} />
@@ -3357,6 +3419,9 @@
   };
 
   const openReadinessWorkflow = (issue) => {
+    if (issue?.affected_entity_id) {
+      setActiveVoiceContextId(String(issue.affected_entity_id));
+    }
     if (issue?.resolution_target?.workflow === '/inbox/finish-now') {
       setCurrentTab('inbox');
       return;
@@ -3364,6 +3429,76 @@
     setCurrentTab('inbox');
   };
 
+  const renderVoiceContextBanner = () => {
+    if (!(currentTab === 'inbox' || currentTab === 'quarter')) {
+      return null;
+    }
+
+    return (
+      <section className="voice-context-banner">
+        <div>
+          <strong>Voice triage</strong>
+          <div className="small-muted">
+            Use the mic against one selected finish-now item only. Supported commands here are category, business, personal, split n%, and attach receipt.
+          </div>
+        </div>
+        <div className="voice-context-row">
+          <span className={`voice-context-pill ${activeVoiceCard ? 'active' : ''}`}>
+            {activeVoiceCard
+              ? `Target: ${activeVoiceCard.merchant} (${activeVoiceCard.id})`
+              : 'Select a blocker card to arm voice triage'}
+          </span>
+          {activeVoiceCard && (
+            <button className="secondary-action" type="button" onClick={() => setCurrentTab('inbox')}>
+              Edit target
+            </button>
+          )}
+        </div>
+        {voiceMicroDecisionChip && (
+          <div className="voice-micro-chip">
+            <div>
+              <strong>{voiceMicroDecisionChip.confirmationChip}</strong>
+              <div className="small-muted">
+                {voiceMicroDecisionChip.targetLabel} ({voiceMicroDecisionChip.targetId})
+              </div>
+            </div>
+            {voiceMicroDecisionChip.labels.length > 0 && (
+              <div className="voice-chip-labels">
+                {voiceMicroDecisionChip.labels.map((label) => (
+                  <span key={`${voiceMicroDecisionChip.targetId}-${label}`} className="voice-action-pill">
+                    {label}
+                  </span>
+                ))}
+              </div>
+            )}
+            <div className="voice-chip-actions">
+              <button
+                className="secondary-action"
+                type="button"
+                onClick={() => {
+                  setCurrentTab('inbox');
+                  setActiveVoiceContextId(voiceMicroDecisionChip.targetId);
+                }}
+              >
+                Edit
+              </button>
+              {voiceMicroDecisionChip.undoSupported && (
+                <button
+                  className="secondary-action"
+                  type="button"
+                  onClick={undoLastInboxAction}
+                  disabled={inboxBusyId === 'undo-last'}
+                >
+                  Undo
+                </button>
+              )}
+            </div>
+          </div>
+        )}
+      </section>
+    );
+  };
+
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
@@ -3654,6 +3789,13 @@
                     </div>
                   </div>
                   <div className="finish-now-actions">
+                    <button
+                      className="secondary-action"
+                      type="button"
+                      onClick={() => setActiveVoiceContextId(String(issue.affected_entity_id))}
+                    >
+                      {activeVoiceContextId === String(issue.affected_entity_id) ? 'Voice target set' : 'Arm voice'}
+                    </button>
                     {(readinessDemoEnabled || snapshotDemoEnabled) ? (
                       <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
                         Mark fixed
@@ -4391,6 +4533,7 @@
         </button>
       </section>
       {connectionError && <div className="alert alert-danger">{connectionError}</div>}
+      {renderVoiceContextBanner()}
 
       {loading ? (
         <div className="text-center py-5">Loading...</div>
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..6eb469fcc7f023f8545d9e952a5823fb8c47a88d
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Date -Format "yyyy-MM-dd HH:mm:ssK"' in C:\Users\edebe\eds succeeded in 2.80s:
2026-03-19 16:59:44+00:00
file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..e6d93b4bc1cd6960ce27238cfb7e90c3322f41e0
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,6 +10,36 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+const {
+  parseVoiceMicroDecision,
+  validateMicroDecisionContext,
+} = require('../services/voiceMicroDecisionService');
+
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undoable: undoSupported,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
 
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
@@ -267,76 +297,177 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
-  const text = String(transcript).toLowerCase().trim();
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_kind = null,
+    context_scope = null,
+  } = req.body || {};
+  const rawTranscript = String(transcript).trim();
+  const text = rawTranscript.toLowerCase();
+  const normalizedContext = context_kind ?? context_scope;
+  const parsedCommand = parseVoiceMicroDecision(rawTranscript);
+  const contextValidation = validateMicroDecisionContext(parsedCommand, normalizedContext);
+  const normalizedContextScope = contextValidation.ok
+    ? contextValidation.context_kind
+    : (normalizedContext === null || normalizedContext === undefined
+      ? null
+      : String(normalizedContext).toLowerCase().replace(/-/g, '_'));
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
+  if (!contextValidation.ok) return res.status(contextValidation.statusCode).json({ error: contextValidation.error });
 
   try {
-    if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
-      const categoryName = text.replace('category:', '').trim();
-      const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+    if (parsedCommand.type === 'category') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: parsedCommand.category_code,
+            category_name: parsedCommand.category_name,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'category', label: parsedCommand.category_name }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
-      const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+    if (parsedCommand.type === 'business_personal') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const bp = parsedCommand.business_personal;
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    const splitMatch = text.match(/split\s+(\d{1,3})%?/);
-    if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
-      const pct = Number(splitMatch[1]);
+    if (parsedCommand.type === 'split') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const pct = Number(parsedCommand.split_business_pct);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+    if (parsedCommand.type === 'attach_receipt') {
+      if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for attach receipt commands.' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          parsed_command: parsedCommand,
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
-    if (['match first', 'match second', 'match third'].includes(text)) {
+    if (parsedCommand.type === 'match_rank') {
       if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for match commands.' });
-      const index = text === 'match first' ? 1 : text === 'match second' ? 2 : 3;
+      const index = parsedCommand.match_rank;
       const suggestions = await db.query(
         `
         WITH ev AS (
@@ -364,10 +495,24 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          bank_txn_id: chosen,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text === 'no match') {
+    if (parsedCommand.type === 'no_match') {
       if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for no match.' });
       await db.query(
         `
@@ -376,7 +521,20 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/backend/verify_voice_micro_decisions.js b/backend/verify_voice_micro_decisions.js
index 25d9ab02050aac37a5d38aca5c6f0154de9bb787..15c93efa65f133ec1a59d827ecbfa29a95cf4e6d
--- a/backend/verify_voice_micro_decisions.js
+++ b/backend/verify_voice_micro_decisions.js
@@ -9,16 +9,70 @@
       { bank_txn_id: 'txn-2' },
       { bank_txn_id: 'txn-3' }
     ];
+    this.classifications = new Map();
+    this.auditEntries = [];
   }
 
-  async query(text, params) {
+  get pool() {
+    return {
+      connect: async () => ({
+        query: this.query.bind(this),
+        release() {},
+      }),
+    };
+  }
+
+  async query(text, params = []) {
     const sql = text.replace(/\s+/g, ' ').trim();
     this.calls.push({ sql, params });
 
+    if (sql === 'BEGIN' || sql === 'COMMIT' || sql === 'ROLLBACK') {
+      return { rows: [] };
+    }
+
+    if (sql.includes('SELECT * FROM transaction_classifications')) {
+      const existing = this.classifications.get(params[0]);
+      return { rows: existing ? [existing] : [] };
+    }
+
+    if (sql.includes('INSERT INTO transaction_classifications')) {
+      const next = {
+        user_id: params[0],
+        bank_txn_id: params[1],
+        category_code: params[2],
+        category_name: params[3],
+        business_personal: params[4],
+        is_split: params[5],
+        split_business_pct: params[6],
+        confidence: params[7],
+        source: params[8],
+      };
+      this.classifications.set(params[1], next);
+      return { rows: [next] };
+    }
+
+    if (sql.includes('INSERT INTO transaction_audit_log')) {
+      this.auditEntries.push({
+        bank_txn_id: params[1],
+        previous_value: params[4],
+        new_value: params[5],
+        change_source: params[6],
+      });
+      return { rows: [] };
+    }
+
+    if (sql.includes('SELECT txn_date FROM bank_transactions')) {
+      return { rows: [{ txn_date: '2026-03-12' }] };
+    }
+
     if (sql.includes('FROM bank_transactions bt CROSS JOIN ev')) {
       return { rows: this.matchSuggestions };
     }
 
+    if (sql.includes('INSERT INTO evidence_links')) {
+      return { rows: [] };
+    }
+
     return { rows: [] };
   }
 }
@@ -69,45 +123,79 @@
   const mockDb = new MockDb();
   const controller = loadController(mockDb);
 
+  const missingContext = await invoke(controller, {
+    transcript: 'Business',
+    bank_txn_id: 'txn-123'
+  });
+  assert.strictEqual(missingContext.statusCode, 400);
+  assert.strictEqual(missingContext.body.error, 'context_kind is required for MVP voice micro-decisions.');
+
   const category = await invoke(controller, {
     transcript: 'Category: Travel',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(category.statusCode, 200);
-  assert.strictEqual(category.body.confirmation_chip, 'Applied: travel');
-  assert(
-    mockDb.calls.some((call) => call.sql.includes('INSERT INTO transaction_classifications') && call.params[2] === 'TRAVEL' && call.params[3] === 'travel'),
-    'Expected category command to persist manual classification'
-  );
+  assert.strictEqual(category.body.confirmation_chip, 'Applied: Travel');
+  assert.strictEqual(category.body.undo.supported, true);
+  assert.deepStrictEqual(category.body.context_binding, {
+    scope: 'inbox',
+    target_type: 'bank_transaction',
+    target_id: 'txn-123'
+  });
+  assert.strictEqual(mockDb.classifications.get('txn-123').category_code, 'TRAVEL');
+  assert.strictEqual(mockDb.auditEntries.at(-1).change_source, 'voice');
 
   const business = await invoke(controller, {
     transcript: 'Business',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(business.statusCode, 200);
   assert.strictEqual(business.body.confirmation_chip, 'Applied: BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').category_code, 'TRAVEL');
+  assert.strictEqual(mockDb.classifications.get('txn-123').business_personal, 'BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').is_split, false);
 
   const split = await invoke(controller, {
     transcript: 'Split 70%',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(split.statusCode, 200);
   assert.strictEqual(split.body.confirmation_chip, 'Applied: Split 70%');
+  assert.strictEqual(split.body.undo.supported, true);
+  assert.strictEqual(mockDb.classifications.get('txn-123').business_personal, 'BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').is_split, true);
+  assert.strictEqual(mockDb.classifications.get('txn-123').split_business_pct, 70);
 
   const attachReceipt = await invoke(controller, {
     transcript: 'Attach receipt',
-    bank_txn_id: 'txn-123'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(attachReceipt.statusCode, 200);
   assert.strictEqual(attachReceipt.body.action_status, 'clarification_needed');
   assert.strictEqual(attachReceipt.body.confirmation_chip, 'Attach receipt requested');
+  assert.strictEqual(attachReceipt.body.undo.supported, false);
+  assert.deepStrictEqual(attachReceipt.body.context_binding, {
+    scope: 'evidence',
+    target_type: 'evidence',
+    target_id: 'evidence-1'
+  });
 
   const matchSecond = await invoke(controller, {
     transcript: 'Match second',
-    evidence_id: 'evidence-1'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(matchSecond.statusCode, 200);
   assert.strictEqual(matchSecond.body.confirmation_chip, 'Applied: Match 2');
+  assert.deepStrictEqual(matchSecond.body.context_binding, {
+    scope: 'evidence',
+    target_type: 'evidence',
+    target_id: 'evidence-1'
+  });
   assert(
     mockDb.calls.some((call) => call.sql.includes('INSERT INTO evidence_links') && call.params[1] === 'evidence-1' && call.params[2] === 'txn-2'),
     'Expected match command to persist selected evidence link'
@@ -115,29 +203,41 @@
 
   const noMatch = await invoke(controller, {
     transcript: 'No match',
-    evidence_id: 'evidence-1'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(noMatch.statusCode, 200);
   assert.strictEqual(noMatch.body.confirmation_chip, 'Applied: No match');
 
   const invalidSplit = await invoke(controller, {
     transcript: 'Split 140%',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(invalidSplit.statusCode, 400);
   assert.strictEqual(invalidSplit.body.error, 'Split percentage must be 0-100.');
 
+  const unsafeContext = await invoke(controller, {
+    transcript: 'Category: Travel',
+    bank_txn_id: 'txn-123',
+    context_kind: 'evidence'
+  });
+  assert.strictEqual(unsafeContext.statusCode, 409);
+  assert.strictEqual(unsafeContext.body.error, 'Command "category" is not allowed for evidence context.');
+
   const unsupported = await invoke(controller, {
-    transcript: 'Archive this forever'
+    transcript: 'Archive this forever',
+    context_kind: 'inbox'
   });
   assert.strictEqual(unsupported.statusCode, 400);
   assert.strictEqual(unsupported.body.error, 'Unsupported MVP voice micro-decision command.');
 
   console.log('voice_micro_decisions_ok');
-  console.log('category_business_split_ok=true');
+  console.log('context_guardrails_ok=true');
+  console.log('audited_classification_updates_ok=true');
   console.log('attach_receipt_clarification_ok=true');
   console.log('match_selection_ok=true');
-  console.log('no_match_ok=true');
+  console.log('safe_context_binding_ok=true');
   console.log('validation_errors_ok=true');
 }
 
diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index 9b145fadac9123e3fa01b12af6ef2b9916dc11de..6142bbcd4c1b4145f6b904c0770d4358a297e218
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -50,6 +50,16 @@
   buildQuarterUiState,
   deriveResolvedIssueIdsFromSearchParams,
 } from './quarterReadiness';
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+import {
+  applyInboxVoiceCommandToDraft,
+  buildInboxVoiceChip,
+  parseInboxVoiceCommand,
+} from './voiceMicroDecision';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -427,6 +437,13 @@
   .date-group-count { border-radius: 999px; padding: 4px 9px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.72rem; color: var(--text-muted); }
   .date-group-body { padding: 0 12px 12px; }
   .voice-preview { background: rgba(0,0,0,0.82); color: white; padding: 8px 15px; border-radius: 15px; margin-bottom: 8px; font-size: 0.72rem; max-width: 240px; text-align: right; }
+  .voice-context-banner { display: grid; gap: 10px; padding: 14px 16px; margin-bottom: 16px; border-radius: 18px; background: var(--bg-card); border: 1px solid var(--border); box-shadow: var(--shadow); }
+  .voice-context-row, .voice-chip-actions, .voice-chip-labels { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
+  .voice-context-pill, .voice-action-pill { border-radius: 999px; padding: 6px 10px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.74rem; font-weight: 700; }
+  .voice-context-pill.active { background: rgba(13,92,99,0.12); color: var(--primary-strong); border-color: rgba(13,92,99,0.24); }
+  .voice-micro-chip { display: grid; gap: 10px; padding: 14px 16px; border-radius: 18px; background: rgba(13,92,99,0.08); border: 1px solid rgba(13,92,99,0.18); }
+  .voice-micro-chip strong { display: block; }
+  .inbox-action-card.voice-target { border-color: rgba(13,92,99,0.36); box-shadow: 0 0 0 2px rgba(13,92,99,0.12), var(--shadow); }
   .spinner { animation: rotate 2s linear infinite; }
   .capture-grid { display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(320px, 0.95fr); gap: 18px; align-items: start; }
   .command-centre { display: grid; gap: 18px; }
@@ -1382,6 +1399,8 @@
   const [inboxDrafts, setInboxDrafts] = useState({});
   const [inboxBusyId, setInboxBusyId] = useState('');
   const [inboxMessage, setInboxMessage] = useState(null);
+  const [activeVoiceContextId, setActiveVoiceContextId] = useState('');
+  const [voiceMicroDecisionChip, setVoiceMicroDecisionChip] = useState(null);
   const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
@@ -1606,10 +1625,20 @@
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
   const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const activeVoiceCard = findVoiceContextCard(inboxCards, activeVoiceContextId);
   const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
   const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
   const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
+  useEffect(() => {
+    if (!activeVoiceContextId) {
+      return;
+    }
+    if (!findVoiceContextCard(inboxCards, activeVoiceContextId)) {
+      setActiveVoiceContextId('');
+    }
+  }, [activeVoiceContextId, inboxCards]);
+
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
   const canManageGovernance = hasPermission(currentUser, PERMISSIONS.GOVERNANCE_MANAGE);
@@ -1806,11 +1835,13 @@
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxDrafts({});
+        setVoiceMicroDecisionChip(null);
         setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
         return;
       }
       await axios.post(`${API_BASE_URL}/inbox/undo-last`);
       await fetchAllData();
+      setVoiceMicroDecisionChip(null);
       setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
     } catch (error) {
       setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
@@ -1991,22 +2022,51 @@
     const cleanText = text.replace(/┬ú/g, 'GBP ');
     try {
       setVoiceStatus('processing');
-      const response = await axios.post(`${API_BASE_URL}/voice/process`, {
+      const microDecisionRequest = buildVoiceMicroDecisionRequest({
+        currentTab,
         transcript: cleanText,
-        device_id: 'mobile-v139',
-        current_date: new Date().toISOString(),
+        activeVoiceContextId,
+        inboxCards,
       });
-      if (response.data.confirmation_text && window.SpeechSynthesisUtterance) {
+
+      if (microDecisionRequest?.error) {
+        setInboxMessage({ type: 'error', text: microDecisionRequest.error });
+        setCurrentTab('inbox');
+        return;
+      }
+
+      const response = microDecisionRequest
+        ? await axios.post(`${API_BASE_URL}${microDecisionRequest.endpoint}`, microDecisionRequest.payload)
+        : await axios.post(`${API_BASE_URL}/voice/process`, {
+            transcript: cleanText,
+            device_id: 'mobile-v139',
+            current_date: new Date().toISOString(),
+          });
+      const spokenConfirmation = response.data.confirmation_chip || response.data.confirmation_text;
+      if (spokenConfirmation && window.SpeechSynthesisUtterance) {
         try {
-          const utterance = new window.SpeechSynthesisUtterance(response.data.confirmation_text);
+          const utterance = new window.SpeechSynthesisUtterance(spokenConfirmation);
           window.speechSynthesis.speak(utterance);
         } catch (_) {
         }
       }
-      executeVoiceAction(response.data);
-      fetchAllData();
+      if (microDecisionRequest) {
+        setVoiceMicroDecisionChip((previous) => reduceVoiceMicroDecisionChip(previous, response.data, microDecisionRequest.card));
+        setInboxMessage({
+          type: 'success',
+          text: response.data.confirmation_chip || 'Voice triage action applied.',
+        });
+      } else {
+        executeVoiceAction(response.data);
+      }
+      await fetchAllData();
     } catch (error) {
-      alert('AI Error');
+      const message = error?.response?.data?.error || 'AI Error';
+      if (currentTab === 'inbox' || currentTab === 'quarter') {
+        setInboxMessage({ type: 'error', text: message });
+      } else {
+        alert(message);
+      }
     } finally {
       setVoiceStatus('idle');
       setIsListening(false);
@@ -2894,7 +2954,11 @@
             const saving = inboxBusyId === card.id;
             const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
             return (
-              <section key={card.id} className="inbox-action-card">
+              <section
+                key={card.id}
+                className={`inbox-action-card ${activeVoiceContextId === card.id ? 'voice-target' : ''}`}
+                onClick={() => setActiveVoiceContextId(card.id)}
+              >
                 <div className="inbox-card-top">
                   <div>
                     <strong>{card.merchant}</strong>
@@ -2914,6 +2978,9 @@
                       {label}
                     </span>
                   ))}
+                  {activeVoiceContextId === card.id && (
+                    <span className="voice-context-pill active">Voice target</span>
+                  )}
                   {card.suggested_category?.category_name && (
                     <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
                       <CheckCircle2 size={14} />
@@ -3357,6 +3424,9 @@
   };
 
   const openReadinessWorkflow = (issue) => {
+    if (issue?.affected_entity_id) {
+      setActiveVoiceContextId(String(issue.affected_entity_id));
+    }
     if (issue?.resolution_target?.workflow === '/inbox/finish-now') {
       setCurrentTab('inbox');
       return;
@@ -3364,6 +3434,76 @@
     setCurrentTab('inbox');
   };
 
+  const renderVoiceContextBanner = () => {
+    if (!(currentTab === 'inbox' || currentTab === 'quarter')) {
+      return null;
+    }
+
+    return (
+      <section className="voice-context-banner">
+        <div>
+          <strong>Voice triage</strong>
+          <div className="small-muted">
+            Use the mic against one selected finish-now item only. Supported commands here are category, business, personal, split n%, and attach receipt.
+          </div>
+        </div>
+        <div className="voice-context-row">
+          <span className={`voice-context-pill ${activeVoiceCard ? 'active' : ''}`}>
+            {activeVoiceCard
+              ? `Target: ${activeVoiceCard.merchant} (${activeVoiceCard.id})`
+              : 'Select a blocker card to arm voice triage'}
+          </span>
+          {activeVoiceCard && (
+            <button className="secondary-action" type="button" onClick={() => setCurrentTab('inbox')}>
+              Edit target
+            </button>
+          )}
+        </div>
+        {voiceMicroDecisionChip && (
+          <div className="voice-micro-chip">
+            <div>
+              <strong>{voiceMicroDecisionChip.confirmationChip}</strong>
+              <div className="small-muted">
+                {voiceMicroDecisionChip.targetLabel} ({voiceMicroDecisionChip.targetId})
+              </div>
+            </div>
+            {voiceMicroDecisionChip.labels.length > 0 && (
+              <div className="voice-chip-labels">
+                {voiceMicroDecisionChip.labels.map((label) => (
+                  <span key={`${voiceMicroDecisionChip.targetId}-${label}`} className="voice-action-pill">
+                    {label}
+                  </span>
+                ))}
+              </div>
+            )}
+            <div className="voice-chip-actions">
+              <button
+                className="secondary-action"
+                type="button"
+                onClick={() => {
+                  setCurrentTab('inbox');
+                  setActiveVoiceContextId(voiceMicroDecisionChip.targetId);
+                }}
+              >
+                Edit
+              </button>
+              {voiceMicroDecisionChip.undoSupported && (
+                <button
+                  className="secondary-action"
+                  type="button"
+                  onClick={undoLastInboxAction}
+                  disabled={inboxBusyId === 'undo-last'}
+                >
+                  Undo
+                </button>
+              )}
+            </div>
+          </div>
+        )}
+      </section>
+    );
+  };
+
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
@@ -3654,6 +3794,13 @@
                     </div>
                   </div>
                   <div className="finish-now-actions">
+                    <button
+                      className="secondary-action"
+                      type="button"
+                      onClick={() => setActiveVoiceContextId(String(issue.affected_entity_id))}
+                    >
+                      {activeVoiceContextId === String(issue.affected_entity_id) ? 'Voice target set' : 'Arm voice'}
+                    </button>
                     {(readinessDemoEnabled || snapshotDemoEnabled) ? (
                       <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
                         Mark fixed
@@ -4391,6 +4538,7 @@
         </button>
       </section>
       {connectionError && <div className="alert alert-danger">{connectionError}</div>}
+      {renderVoiceContextBanner()}
 
       {loading ? (
         <div className="text-center py-5">Loading...</div>
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..6eb469fcc7f023f8545d9e952a5823fb8c47a88d
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});

codex
I’m updating the lifecycle file now. It will reflect the ordered steps actually executed, the concrete validation commands and outputs, the new frontend/backend files touched, and an `Awaiting user verification` completion state with an explicit request for the visible inbox voice flow.
file update
D C:\Users\edebe\eds\workstream\200_inprogress\codex\20260305_192700_bizpa_mvp_pivot_phase06_voice_micro_decisions.md
# Source
- Derived from backlog: `C:\Users\edebe\eds\workstream\000_backlog\mvp_prd_quarterly_export_10min.md`
- Decomposed from checklist: `C:\Users\edebe\eds\workstream\300_complete\20260305_191657_bizpa_mvp_quarterly_pivot_full_delivery_checklist.md`

# Task Summary
Constrain voice intents to PRD micro-decisions and bind safely to inbox/finish-now context with confirmation chip + undo.

# Context
- Affected area: voice controller/parser and web/mobile voice UX

# Plan
- [ ] 1. Implement phase scope in code and configuration.
  - [ ] Test: Execute phase-specific implementation checks and verify expected behavior change.
  - [ ] Evidence: pending
- [ ] 2. Add/adjust automated tests for this phase.
  - [ ] Test: Run targeted unit/integration tests for new behavior and ensure green results.
  - [ ] Evidence: pending
- [ ] 3. Validate against PRD acceptance criteria impacted by this phase.
  - [ ] Test: Run intent contract tests for supported utterances and verify each action is applied, confirmed, and undoable.
  - [ ] Evidence: pending
- [ ] 4. Update technical notes and rollout caveats for downstream phases.
  - [ ] Test: Confirm lifecycle doc lists changed files, commands run, and known risks.
  - [ ] Evidence: pending

# Implementation Log
- 2026-03-05 Created decomposed task file from approved MVP pivot checklist.

# Changes Made
- Task file created; implementation not started.

# Validation
- Pending.

# Risks/Notes
- Must follow strict sequential completion and evidence capture before starting dependent phase.

# Completion Status
- Todo.


## Retry History
Retry-Count: 1
- Retry scheduled at 2026-03-18 17:21:29
apply_patch(auto_approved=true) exited 0 in 1.36s:
Success. Updated the following files:
D C:\Users\edebe\eds\workstream\200_inprogress\codex\20260305_192700_bizpa_mvp_pivot_phase06_voice_micro_decisions.md
file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..e6d93b4bc1cd6960ce27238cfb7e90c3322f41e0
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,6 +10,36 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+const {
+  parseVoiceMicroDecision,
+  validateMicroDecisionContext,
+} = require('../services/voiceMicroDecisionService');
+
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undoable: undoSupported,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
 
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
@@ -267,76 +297,177 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
-  const text = String(transcript).toLowerCase().trim();
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_kind = null,
+    context_scope = null,
+  } = req.body || {};
+  const rawTranscript = String(transcript).trim();
+  const text = rawTranscript.toLowerCase();
+  const normalizedContext = context_kind ?? context_scope;
+  const parsedCommand = parseVoiceMicroDecision(rawTranscript);
+  const contextValidation = validateMicroDecisionContext(parsedCommand, normalizedContext);
+  const normalizedContextScope = contextValidation.ok
+    ? contextValidation.context_kind
+    : (normalizedContext === null || normalizedContext === undefined
+      ? null
+      : String(normalizedContext).toLowerCase().replace(/-/g, '_'));
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
+  if (!contextValidation.ok) return res.status(contextValidation.statusCode).json({ error: contextValidation.error });
 
   try {
-    if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
-      const categoryName = text.replace('category:', '').trim();
-      const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+    if (parsedCommand.type === 'category') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: parsedCommand.category_code,
+            category_name: parsedCommand.category_name,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'category', label: parsedCommand.category_name }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
-      const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+    if (parsedCommand.type === 'business_personal') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const bp = parsedCommand.business_personal;
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    const splitMatch = text.match(/split\s+(\d{1,3})%?/);
-    if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
-      const pct = Number(splitMatch[1]);
+    if (parsedCommand.type === 'split') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const pct = Number(parsedCommand.split_business_pct);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+    if (parsedCommand.type === 'attach_receipt') {
+      if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for attach receipt commands.' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          parsed_command: parsedCommand,
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
-    if (['match first', 'match second', 'match third'].includes(text)) {
+    if (parsedCommand.type === 'match_rank') {
       if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for match commands.' });
-      const index = text === 'match first' ? 1 : text === 'match second' ? 2 : 3;
+      const index = parsedCommand.match_rank;
       const suggestions = await db.query(
         `
         WITH ev AS (
@@ -364,10 +495,24 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          bank_txn_id: chosen,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text === 'no match') {
+    if (parsedCommand.type === 'no_match') {
       if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for no match.' });
       await db.query(
         `
@@ -376,7 +521,20 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/backend/verify_voice_micro_decisions.js b/backend/verify_voice_micro_decisions.js
index 25d9ab02050aac37a5d38aca5c6f0154de9bb787..15c93efa65f133ec1a59d827ecbfa29a95cf4e6d
--- a/backend/verify_voice_micro_decisions.js
+++ b/backend/verify_voice_micro_decisions.js
@@ -9,16 +9,70 @@
       { bank_txn_id: 'txn-2' },
       { bank_txn_id: 'txn-3' }
     ];
+    this.classifications = new Map();
+    this.auditEntries = [];
   }
 
-  async query(text, params) {
+  get pool() {
+    return {
+      connect: async () => ({
+        query: this.query.bind(this),
+        release() {},
+      }),
+    };
+  }
+
+  async query(text, params = []) {
     const sql = text.replace(/\s+/g, ' ').trim();
     this.calls.push({ sql, params });
 
+    if (sql === 'BEGIN' || sql === 'COMMIT' || sql === 'ROLLBACK') {
+      return { rows: [] };
+    }
+
+    if (sql.includes('SELECT * FROM transaction_classifications')) {
+      const existing = this.classifications.get(params[0]);
+      return { rows: existing ? [existing] : [] };
+    }
+
+    if (sql.includes('INSERT INTO transaction_classifications')) {
+      const next = {
+        user_id: params[0],
+        bank_txn_id: params[1],
+        category_code: params[2],
+        category_name: params[3],
+        business_personal: params[4],
+        is_split: params[5],
+        split_business_pct: params[6],
+        confidence: params[7],
+        source: params[8],
+      };
+      this.classifications.set(params[1], next);
+      return { rows: [next] };
+    }
+
+    if (sql.includes('INSERT INTO transaction_audit_log')) {
+      this.auditEntries.push({
+        bank_txn_id: params[1],
+        previous_value: params[4],
+        new_value: params[5],
+        change_source: params[6],
+      });
+      return { rows: [] };
+    }
+
+    if (sql.includes('SELECT txn_date FROM bank_transactions')) {
+      return { rows: [{ txn_date: '2026-03-12' }] };
+    }
+
     if (sql.includes('FROM bank_transactions bt CROSS JOIN ev')) {
       return { rows: this.matchSuggestions };
     }
 
+    if (sql.includes('INSERT INTO evidence_links')) {
+      return { rows: [] };
+    }
+
     return { rows: [] };
   }
 }
@@ -69,45 +123,79 @@
   const mockDb = new MockDb();
   const controller = loadController(mockDb);
 
+  const missingContext = await invoke(controller, {
+    transcript: 'Business',
+    bank_txn_id: 'txn-123'
+  });
+  assert.strictEqual(missingContext.statusCode, 400);
+  assert.strictEqual(missingContext.body.error, 'context_kind is required for MVP voice micro-decisions.');
+
   const category = await invoke(controller, {
     transcript: 'Category: Travel',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(category.statusCode, 200);
-  assert.strictEqual(category.body.confirmation_chip, 'Applied: travel');
-  assert(
-    mockDb.calls.some((call) => call.sql.includes('INSERT INTO transaction_classifications') && call.params[2] === 'TRAVEL' && call.params[3] === 'travel'),
-    'Expected category command to persist manual classification'
-  );
+  assert.strictEqual(category.body.confirmation_chip, 'Applied: Travel');
+  assert.strictEqual(category.body.undo.supported, true);
+  assert.deepStrictEqual(category.body.context_binding, {
+    scope: 'inbox',
+    target_type: 'bank_transaction',
+    target_id: 'txn-123'
+  });
+  assert.strictEqual(mockDb.classifications.get('txn-123').category_code, 'TRAVEL');
+  assert.strictEqual(mockDb.auditEntries.at(-1).change_source, 'voice');
 
   const business = await invoke(controller, {
     transcript: 'Business',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(business.statusCode, 200);
   assert.strictEqual(business.body.confirmation_chip, 'Applied: BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').category_code, 'TRAVEL');
+  assert.strictEqual(mockDb.classifications.get('txn-123').business_personal, 'BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').is_split, false);
 
   const split = await invoke(controller, {
     transcript: 'Split 70%',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(split.statusCode, 200);
   assert.strictEqual(split.body.confirmation_chip, 'Applied: Split 70%');
+  assert.strictEqual(split.body.undo.supported, true);
+  assert.strictEqual(mockDb.classifications.get('txn-123').business_personal, 'BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').is_split, true);
+  assert.strictEqual(mockDb.classifications.get('txn-123').split_business_pct, 70);
 
   const attachReceipt = await invoke(controller, {
     transcript: 'Attach receipt',
-    bank_txn_id: 'txn-123'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(attachReceipt.statusCode, 200);
   assert.strictEqual(attachReceipt.body.action_status, 'clarification_needed');
   assert.strictEqual(attachReceipt.body.confirmation_chip, 'Attach receipt requested');
+  assert.strictEqual(attachReceipt.body.undo.supported, false);
+  assert.deepStrictEqual(attachReceipt.body.context_binding, {
+    scope: 'evidence',
+    target_type: 'evidence',
+    target_id: 'evidence-1'
+  });
 
   const matchSecond = await invoke(controller, {
     transcript: 'Match second',
-    evidence_id: 'evidence-1'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(matchSecond.statusCode, 200);
   assert.strictEqual(matchSecond.body.confirmation_chip, 'Applied: Match 2');
+  assert.deepStrictEqual(matchSecond.body.context_binding, {
+    scope: 'evidence',
+    target_type: 'evidence',
+    target_id: 'evidence-1'
+  });
   assert(
     mockDb.calls.some((call) => call.sql.includes('INSERT INTO evidence_links') && call.params[1] === 'evidence-1' && call.params[2] === 'txn-2'),
     'Expected match command to persist selected evidence link'
@@ -115,29 +203,41 @@
 
   const noMatch = await invoke(controller, {
     transcript: 'No match',
-    evidence_id: 'evidence-1'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(noMatch.statusCode, 200);
   assert.strictEqual(noMatch.body.confirmation_chip, 'Applied: No match');
 
   const invalidSplit = await invoke(controller, {
     transcript: 'Split 140%',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(invalidSplit.statusCode, 400);
   assert.strictEqual(invalidSplit.body.error, 'Split percentage must be 0-100.');
 
+  const unsafeContext = await invoke(controller, {
+    transcript: 'Category: Travel',
+    bank_txn_id: 'txn-123',
+    context_kind: 'evidence'
+  });
+  assert.strictEqual(unsafeContext.statusCode, 409);
+  assert.strictEqual(unsafeContext.body.error, 'Command "category" is not allowed for evidence context.');
+
   const unsupported = await invoke(controller, {
-    transcript: 'Archive this forever'
+    transcript: 'Archive this forever',
+    context_kind: 'inbox'
   });
   assert.strictEqual(unsupported.statusCode, 400);
   assert.strictEqual(unsupported.body.error, 'Unsupported MVP voice micro-decision command.');
 
   console.log('voice_micro_decisions_ok');
-  console.log('category_business_split_ok=true');
+  console.log('context_guardrails_ok=true');
+  console.log('audited_classification_updates_ok=true');
   console.log('attach_receipt_clarification_ok=true');
   console.log('match_selection_ok=true');
-  console.log('no_match_ok=true');
+  console.log('safe_context_binding_ok=true');
   console.log('validation_errors_ok=true');
 }
 
diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index 9b145fadac9123e3fa01b12af6ef2b9916dc11de..0b1bb1064f0a0c53fc82c32eca8d76d4ce5d650b
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -50,6 +50,16 @@
   buildQuarterUiState,
   deriveResolvedIssueIdsFromSearchParams,
 } from './quarterReadiness';
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+import {
+  applyInboxVoiceCommandToDraft,
+  buildInboxVoiceChip,
+  parseInboxVoiceCommand,
+} from './voiceMicroDecision';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -427,6 +437,13 @@
   .date-group-count { border-radius: 999px; padding: 4px 9px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.72rem; color: var(--text-muted); }
   .date-group-body { padding: 0 12px 12px; }
   .voice-preview { background: rgba(0,0,0,0.82); color: white; padding: 8px 15px; border-radius: 15px; margin-bottom: 8px; font-size: 0.72rem; max-width: 240px; text-align: right; }
+  .voice-context-banner { display: grid; gap: 10px; padding: 14px 16px; margin-bottom: 16px; border-radius: 18px; background: var(--bg-card); border: 1px solid var(--border); box-shadow: var(--shadow); }
+  .voice-context-row, .voice-chip-actions, .voice-chip-labels { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
+  .voice-context-pill, .voice-action-pill { border-radius: 999px; padding: 6px 10px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.74rem; font-weight: 700; }
+  .voice-context-pill.active { background: rgba(13,92,99,0.12); color: var(--primary-strong); border-color: rgba(13,92,99,0.24); }
+  .voice-micro-chip { display: grid; gap: 10px; padding: 14px 16px; border-radius: 18px; background: rgba(13,92,99,0.08); border: 1px solid rgba(13,92,99,0.18); }
+  .voice-micro-chip strong { display: block; }
+  .inbox-action-card.voice-target { border-color: rgba(13,92,99,0.36); box-shadow: 0 0 0 2px rgba(13,92,99,0.12), var(--shadow); }
   .spinner { animation: rotate 2s linear infinite; }
   .capture-grid { display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(320px, 0.95fr); gap: 18px; align-items: start; }
   .command-centre { display: grid; gap: 18px; }
@@ -1318,6 +1335,8 @@
     && searchParams?.get('snapshotDemo') === '1';
   const inboxDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('inboxDemo') === '1';
+  const voiceDemoCommand = searchParams?.get('voiceDemoCommand') || '';
+  const voiceDemoTarget = searchParams?.get('voiceDemoTarget') || '';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1382,9 +1401,12 @@
   const [inboxDrafts, setInboxDrafts] = useState({});
   const [inboxBusyId, setInboxBusyId] = useState('');
   const [inboxMessage, setInboxMessage] = useState(null);
+  const [activeVoiceContextId, setActiveVoiceContextId] = useState(voiceDemoTarget);
+  const [voiceMicroDecisionChip, setVoiceMicroDecisionChip] = useState(null);
   const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
+  const voiceDemoSeedRef = useRef('');
   const quarterStart = '2026-01-01';
   const quarterEnd = '2026-03-31';
 
@@ -1606,10 +1628,20 @@
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
   const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const activeVoiceCard = findVoiceContextCard(inboxCards, activeVoiceContextId);
   const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
   const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
   const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
+  useEffect(() => {
+    if (!activeVoiceContextId) {
+      return;
+    }
+    if (!findVoiceContextCard(inboxCards, activeVoiceContextId)) {
+      setActiveVoiceContextId('');
+    }
+  }, [activeVoiceContextId, inboxCards]);
+
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
   const canManageGovernance = hasPermission(currentUser, PERMISSIONS.GOVERNANCE_MANAGE);
@@ -1806,11 +1838,13 @@
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxDrafts({});
+        setVoiceMicroDecisionChip(null);
         setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
         return;
       }
       await axios.post(`${API_BASE_URL}/inbox/undo-last`);
       await fetchAllData();
+      setVoiceMicroDecisionChip(null);
       setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
     } catch (error) {
       setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
@@ -1991,22 +2025,51 @@
     const cleanText = text.replace(/┬ú/g, 'GBP ');
     try {
       setVoiceStatus('processing');
-      const response = await axios.post(`${API_BASE_URL}/voice/process`, {
+      const microDecisionRequest = buildVoiceMicroDecisionRequest({
+        currentTab,
         transcript: cleanText,
-        device_id: 'mobile-v139',
-        current_date: new Date().toISOString(),
+        activeVoiceContextId,
+        inboxCards,
       });
-      if (response.data.confirmation_text && window.SpeechSynthesisUtterance) {
+
+      if (microDecisionRequest?.error) {
+        setInboxMessage({ type: 'error', text: microDecisionRequest.error });
+        setCurrentTab('inbox');
+        return;
+      }
+
+      const response = microDecisionRequest
+        ? await axios.post(`${API_BASE_URL}${microDecisionRequest.endpoint}`, microDecisionRequest.payload)
+        : await axios.post(`${API_BASE_URL}/voice/process`, {
+            transcript: cleanText,
+            device_id: 'mobile-v139',
+            current_date: new Date().toISOString(),
+          });
+      const spokenConfirmation = response.data.confirmation_chip || response.data.confirmation_text;
+      if (spokenConfirmation && window.SpeechSynthesisUtterance) {
         try {
-          const utterance = new window.SpeechSynthesisUtterance(response.data.confirmation_text);
+          const utterance = new window.SpeechSynthesisUtterance(spokenConfirmation);
           window.speechSynthesis.speak(utterance);
         } catch (_) {
         }
       }
-      executeVoiceAction(response.data);
-      fetchAllData();
+      if (microDecisionRequest) {
+        setVoiceMicroDecisionChip((previous) => reduceVoiceMicroDecisionChip(previous, response.data, microDecisionRequest.card));
+        setInboxMessage({
+          type: 'success',
+          text: response.data.confirmation_chip || 'Voice triage action applied.',
+        });
+      } else {
+        executeVoiceAction(response.data);
+      }
+      await fetchAllData();
     } catch (error) {
-      alert('AI Error');
+      const message = error?.response?.data?.error || 'AI Error';
+      if (currentTab === 'inbox' || currentTab === 'quarter') {
+        setInboxMessage({ type: 'error', text: message });
+      } else {
+        alert(message);
+      }
     } finally {
       setVoiceStatus('idle');
       setIsListening(false);
@@ -2894,7 +2957,11 @@
             const saving = inboxBusyId === card.id;
             const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
             return (
-              <section key={card.id} className="inbox-action-card">
+              <section
+                key={card.id}
+                className={`inbox-action-card ${activeVoiceContextId === card.id ? 'voice-target' : ''}`}
+                onClick={() => setActiveVoiceContextId(card.id)}
+              >
                 <div className="inbox-card-top">
                   <div>
                     <strong>{card.merchant}</strong>
@@ -2914,6 +2981,9 @@
                       {label}
                     </span>
                   ))}
+                  {activeVoiceContextId === card.id && (
+                    <span className="voice-context-pill active">Voice target</span>
+                  )}
                   {card.suggested_category?.category_name && (
                     <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
                       <CheckCircle2 size={14} />
@@ -3357,6 +3427,9 @@
   };
 
   const openReadinessWorkflow = (issue) => {
+    if (issue?.affected_entity_id) {
+      setActiveVoiceContextId(String(issue.affected_entity_id));
+    }
     if (issue?.resolution_target?.workflow === '/inbox/finish-now') {
       setCurrentTab('inbox');
       return;
@@ -3364,6 +3437,76 @@
     setCurrentTab('inbox');
   };
 
+  const renderVoiceContextBanner = () => {
+    if (!(currentTab === 'inbox' || currentTab === 'quarter')) {
+      return null;
+    }
+
+    return (
+      <section className="voice-context-banner">
+        <div>
+          <strong>Voice triage</strong>
+          <div className="small-muted">
+            Use the mic against one selected finish-now item only. Supported commands here are category, business, personal, split n%, and attach receipt.
+          </div>
+        </div>
+        <div className="voice-context-row">
+          <span className={`voice-context-pill ${activeVoiceCard ? 'active' : ''}`}>
+            {activeVoiceCard
+              ? `Target: ${activeVoiceCard.merchant} (${activeVoiceCard.id})`
+              : 'Select a blocker card to arm voice triage'}
+          </span>
+          {activeVoiceCard && (
+            <button className="secondary-action" type="button" onClick={() => setCurrentTab('inbox')}>
+              Edit target
+            </button>
+          )}
+        </div>
+        {voiceMicroDecisionChip && (
+          <div className="voice-micro-chip">
+            <div>
+              <strong>{voiceMicroDecisionChip.confirmationChip}</strong>
+              <div className="small-muted">
+                {voiceMicroDecisionChip.targetLabel} ({voiceMicroDecisionChip.targetId})
+              </div>
+            </div>
+            {voiceMicroDecisionChip.labels.length > 0 && (
+              <div className="voice-chip-labels">
+                {voiceMicroDecisionChip.labels.map((label) => (
+                  <span key={`${voiceMicroDecisionChip.targetId}-${label}`} className="voice-action-pill">
+                    {label}
+                  </span>
+                ))}
+              </div>
+            )}
+            <div className="voice-chip-actions">
+              <button
+                className="secondary-action"
+                type="button"
+                onClick={() => {
+                  setCurrentTab('inbox');
+                  setActiveVoiceContextId(voiceMicroDecisionChip.targetId);
+                }}
+              >
+                Edit
+              </button>
+              {voiceMicroDecisionChip.undoSupported && (
+                <button
+                  className="secondary-action"
+                  type="button"
+                  onClick={undoLastInboxAction}
+                  disabled={inboxBusyId === 'undo-last'}
+                >
+                  Undo
+                </button>
+              )}
+            </div>
+          </div>
+        )}
+      </section>
+    );
+  };
+
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
@@ -3654,6 +3797,13 @@
                     </div>
                   </div>
                   <div className="finish-now-actions">
+                    <button
+                      className="secondary-action"
+                      type="button"
+                      onClick={() => setActiveVoiceContextId(String(issue.affected_entity_id))}
+                    >
+                      {activeVoiceContextId === String(issue.affected_entity_id) ? 'Voice target set' : 'Arm voice'}
+                    </button>
                     {(readinessDemoEnabled || snapshotDemoEnabled) ? (
                       <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
                         Mark fixed
@@ -4391,6 +4541,7 @@
         </button>
       </section>
       {connectionError && <div className="alert alert-danger">{connectionError}</div>}
+      {renderVoiceContextBanner()}
 
       {loading ? (
         <div className="text-center py-5">Loading...</div>
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..6eb469fcc7f023f8545d9e952a5823fb8c47a88d
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});
diff --git a/workstream/200_inprogress/codex/20260305_192700_bizpa_mvp_pivot_phase06_voice_micro_decisions.md b/workstream/200_inprogress/codex/20260305_192700_bizpa_mvp_pivot_phase06_voice_micro_decisions.md
deleted file mode 100644
index cf85796e760112547a34bd77ea2a0f0a86fd3219..0000000000000000000000000000000000000000
--- a/workstream/200_inprogress/codex/20260305_192700_bizpa_mvp_pivot_phase06_voice_micro_decisions.md
+++ /dev/null
@@ -1,43 +0,0 @@
-# Source
-- Derived from backlog: `C:\Users\edebe\eds\workstream\000_backlog\mvp_prd_quarterly_export_10min.md`
-- Decomposed from checklist: `C:\Users\edebe\eds\workstream\300_complete\20260305_191657_bizpa_mvp_quarterly_pivot_full_delivery_checklist.md`
-
-# Task Summary
-Constrain voice intents to PRD micro-decisions and bind safely to inbox/finish-now context with confirmation chip + undo.
-
-# Context
-- Affected area: voice controller/parser and web/mobile voice UX
-
-# Plan
-- [ ] 1. Implement phase scope in code and configuration.
-  - [ ] Test: Execute phase-specific implementation checks and verify expected behavior change.
-  - [ ] Evidence: pending
-- [ ] 2. Add/adjust automated tests for this phase.
-  - [ ] Test: Run targeted unit/integration tests for new behavior and ensure green results.
-  - [ ] Evidence: pending
-- [ ] 3. Validate against PRD acceptance criteria impacted by this phase.
-  - [ ] Test: Run intent contract tests for supported utterances and verify each action is applied, confirmed, and undoable.
-  - [ ] Evidence: pending
-- [ ] 4. Update technical notes and rollout caveats for downstream phases.
-  - [ ] Test: Confirm lifecycle doc lists changed files, commands run, and known risks.
-  - [ ] Evidence: pending
-
-# Implementation Log
-- 2026-03-05 Created decomposed task file from approved MVP pivot checklist.
-
-# Changes Made
-- Task file created; implementation not started.
-
-# Validation
-- Pending.
-
-# Risks/Notes
-- Must follow strict sequential completion and evidence capture before starting dependent phase.
-
-# Completion Status
-- Todo.
-
-
-## Retry History
-Retry-Count: 1
-- Retry scheduled at 2026-03-18 17:21:29

file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..e6d93b4bc1cd6960ce27238cfb7e90c3322f41e0
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,6 +10,36 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+const {
+  parseVoiceMicroDecision,
+  validateMicroDecisionContext,
+} = require('../services/voiceMicroDecisionService');
+
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undoable: undoSupported,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
 
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
@@ -267,76 +297,177 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
-  const text = String(transcript).toLowerCase().trim();
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_kind = null,
+    context_scope = null,
+  } = req.body || {};
+  const rawTranscript = String(transcript).trim();
+  const text = rawTranscript.toLowerCase();
+  const normalizedContext = context_kind ?? context_scope;
+  const parsedCommand = parseVoiceMicroDecision(rawTranscript);
+  const contextValidation = validateMicroDecisionContext(parsedCommand, normalizedContext);
+  const normalizedContextScope = contextValidation.ok
+    ? contextValidation.context_kind
+    : (normalizedContext === null || normalizedContext === undefined
+      ? null
+      : String(normalizedContext).toLowerCase().replace(/-/g, '_'));
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
+  if (!contextValidation.ok) return res.status(contextValidation.statusCode).json({ error: contextValidation.error });
 
   try {
-    if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
-      const categoryName = text.replace('category:', '').trim();
-      const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+    if (parsedCommand.type === 'category') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: parsedCommand.category_code,
+            category_name: parsedCommand.category_name,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'category', label: parsedCommand.category_name }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
-      const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+    if (parsedCommand.type === 'business_personal') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const bp = parsedCommand.business_personal;
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    const splitMatch = text.match(/split\s+(\d{1,3})%?/);
-    if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
-      const pct = Number(splitMatch[1]);
+    if (parsedCommand.type === 'split') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const pct = Number(parsedCommand.split_business_pct);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+    if (parsedCommand.type === 'attach_receipt') {
+      if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for attach receipt commands.' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          parsed_command: parsedCommand,
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
-    if (['match first', 'match second', 'match third'].includes(text)) {
+    if (parsedCommand.type === 'match_rank') {
       if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for match commands.' });
-      const index = text === 'match first' ? 1 : text === 'match second' ? 2 : 3;
+      const index = parsedCommand.match_rank;
       const suggestions = await db.query(
         `
         WITH ev AS (
@@ -364,10 +495,24 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          bank_txn_id: chosen,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text === 'no match') {
+    if (parsedCommand.type === 'no_match') {
       if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for no match.' });
       await db.query(
         `
@@ -376,7 +521,20 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/backend/verify_voice_micro_decisions.js b/backend/verify_voice_micro_decisions.js
index 25d9ab02050aac37a5d38aca5c6f0154de9bb787..15c93efa65f133ec1a59d827ecbfa29a95cf4e6d
--- a/backend/verify_voice_micro_decisions.js
+++ b/backend/verify_voice_micro_decisions.js
@@ -9,16 +9,70 @@
       { bank_txn_id: 'txn-2' },
       { bank_txn_id: 'txn-3' }
     ];
+    this.classifications = new Map();
+    this.auditEntries = [];
   }
 
-  async query(text, params) {
+  get pool() {
+    return {
+      connect: async () => ({
+        query: this.query.bind(this),
+        release() {},
+      }),
+    };
+  }
+
+  async query(text, params = []) {
     const sql = text.replace(/\s+/g, ' ').trim();
     this.calls.push({ sql, params });
 
+    if (sql === 'BEGIN' || sql === 'COMMIT' || sql === 'ROLLBACK') {
+      return { rows: [] };
+    }
+
+    if (sql.includes('SELECT * FROM transaction_classifications')) {
+      const existing = this.classifications.get(params[0]);
+      return { rows: existing ? [existing] : [] };
+    }
+
+    if (sql.includes('INSERT INTO transaction_classifications')) {
+      const next = {
+        user_id: params[0],
+        bank_txn_id: params[1],
+        category_code: params[2],
+        category_name: params[3],
+        business_personal: params[4],
+        is_split: params[5],
+        split_business_pct: params[6],
+        confidence: params[7],
+        source: params[8],
+      };
+      this.classifications.set(params[1], next);
+      return { rows: [next] };
+    }
+
+    if (sql.includes('INSERT INTO transaction_audit_log')) {
+      this.auditEntries.push({
+        bank_txn_id: params[1],
+        previous_value: params[4],
+        new_value: params[5],
+        change_source: params[6],
+      });
+      return { rows: [] };
+    }
+
+    if (sql.includes('SELECT txn_date FROM bank_transactions')) {
+      return { rows: [{ txn_date: '2026-03-12' }] };
+    }
+
     if (sql.includes('FROM bank_transactions bt CROSS JOIN ev')) {
       return { rows: this.matchSuggestions };
     }
 
+    if (sql.includes('INSERT INTO evidence_links')) {
+      return { rows: [] };
+    }
+
     return { rows: [] };
   }
 }
@@ -69,45 +123,79 @@
   const mockDb = new MockDb();
   const controller = loadController(mockDb);
 
+  const missingContext = await invoke(controller, {
+    transcript: 'Business',
+    bank_txn_id: 'txn-123'
+  });
+  assert.strictEqual(missingContext.statusCode, 400);
+  assert.strictEqual(missingContext.body.error, 'context_kind is required for MVP voice micro-decisions.');
+
   const category = await invoke(controller, {
     transcript: 'Category: Travel',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(category.statusCode, 200);
-  assert.strictEqual(category.body.confirmation_chip, 'Applied: travel');
-  assert(
-    mockDb.calls.some((call) => call.sql.includes('INSERT INTO transaction_classifications') && call.params[2] === 'TRAVEL' && call.params[3] === 'travel'),
-    'Expected category command to persist manual classification'
-  );
+  assert.strictEqual(category.body.confirmation_chip, 'Applied: Travel');
+  assert.strictEqual(category.body.undo.supported, true);
+  assert.deepStrictEqual(category.body.context_binding, {
+    scope: 'inbox',
+    target_type: 'bank_transaction',
+    target_id: 'txn-123'
+  });
+  assert.strictEqual(mockDb.classifications.get('txn-123').category_code, 'TRAVEL');
+  assert.strictEqual(mockDb.auditEntries.at(-1).change_source, 'voice');
 
   const business = await invoke(controller, {
     transcript: 'Business',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(business.statusCode, 200);
   assert.strictEqual(business.body.confirmation_chip, 'Applied: BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').category_code, 'TRAVEL');
+  assert.strictEqual(mockDb.classifications.get('txn-123').business_personal, 'BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').is_split, false);
 
   const split = await invoke(controller, {
     transcript: 'Split 70%',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(split.statusCode, 200);
   assert.strictEqual(split.body.confirmation_chip, 'Applied: Split 70%');
+  assert.strictEqual(split.body.undo.supported, true);
+  assert.strictEqual(mockDb.classifications.get('txn-123').business_personal, 'BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').is_split, true);
+  assert.strictEqual(mockDb.classifications.get('txn-123').split_business_pct, 70);
 
   const attachReceipt = await invoke(controller, {
     transcript: 'Attach receipt',
-    bank_txn_id: 'txn-123'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(attachReceipt.statusCode, 200);
   assert.strictEqual(attachReceipt.body.action_status, 'clarification_needed');
   assert.strictEqual(attachReceipt.body.confirmation_chip, 'Attach receipt requested');
+  assert.strictEqual(attachReceipt.body.undo.supported, false);
+  assert.deepStrictEqual(attachReceipt.body.context_binding, {
+    scope: 'evidence',
+    target_type: 'evidence',
+    target_id: 'evidence-1'
+  });
 
   const matchSecond = await invoke(controller, {
     transcript: 'Match second',
-    evidence_id: 'evidence-1'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(matchSecond.statusCode, 200);
   assert.strictEqual(matchSecond.body.confirmation_chip, 'Applied: Match 2');
+  assert.deepStrictEqual(matchSecond.body.context_binding, {
+    scope: 'evidence',
+    target_type: 'evidence',
+    target_id: 'evidence-1'
+  });
   assert(
     mockDb.calls.some((call) => call.sql.includes('INSERT INTO evidence_links') && call.params[1] === 'evidence-1' && call.params[2] === 'txn-2'),
     'Expected match command to persist selected evidence link'
@@ -115,29 +203,41 @@
 
   const noMatch = await invoke(controller, {
     transcript: 'No match',
-    evidence_id: 'evidence-1'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(noMatch.statusCode, 200);
   assert.strictEqual(noMatch.body.confirmation_chip, 'Applied: No match');
 
   const invalidSplit = await invoke(controller, {
     transcript: 'Split 140%',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(invalidSplit.statusCode, 400);
   assert.strictEqual(invalidSplit.body.error, 'Split percentage must be 0-100.');
 
+  const unsafeContext = await invoke(controller, {
+    transcript: 'Category: Travel',
+    bank_txn_id: 'txn-123',
+    context_kind: 'evidence'
+  });
+  assert.strictEqual(unsafeContext.statusCode, 409);
+  assert.strictEqual(unsafeContext.body.error, 'Command "category" is not allowed for evidence context.');
+
   const unsupported = await invoke(controller, {
-    transcript: 'Archive this forever'
+    transcript: 'Archive this forever',
+    context_kind: 'inbox'
   });
   assert.strictEqual(unsupported.statusCode, 400);
   assert.strictEqual(unsupported.body.error, 'Unsupported MVP voice micro-decision command.');
 
   console.log('voice_micro_decisions_ok');
-  console.log('category_business_split_ok=true');
+  console.log('context_guardrails_ok=true');
+  console.log('audited_classification_updates_ok=true');
   console.log('attach_receipt_clarification_ok=true');
   console.log('match_selection_ok=true');
-  console.log('no_match_ok=true');
+  console.log('safe_context_binding_ok=true');
   console.log('validation_errors_ok=true');
 }
 
diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index 9b145fadac9123e3fa01b12af6ef2b9916dc11de..0b1bb1064f0a0c53fc82c32eca8d76d4ce5d650b
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -50,6 +50,16 @@
   buildQuarterUiState,
   deriveResolvedIssueIdsFromSearchParams,
 } from './quarterReadiness';
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+import {
+  applyInboxVoiceCommandToDraft,
+  buildInboxVoiceChip,
+  parseInboxVoiceCommand,
+} from './voiceMicroDecision';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -427,6 +437,13 @@
   .date-group-count { border-radius: 999px; padding: 4px 9px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.72rem; color: var(--text-muted); }
   .date-group-body { padding: 0 12px 12px; }
   .voice-preview { background: rgba(0,0,0,0.82); color: white; padding: 8px 15px; border-radius: 15px; margin-bottom: 8px; font-size: 0.72rem; max-width: 240px; text-align: right; }
+  .voice-context-banner { display: grid; gap: 10px; padding: 14px 16px; margin-bottom: 16px; border-radius: 18px; background: var(--bg-card); border: 1px solid var(--border); box-shadow: var(--shadow); }
+  .voice-context-row, .voice-chip-actions, .voice-chip-labels { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
+  .voice-context-pill, .voice-action-pill { border-radius: 999px; padding: 6px 10px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.74rem; font-weight: 700; }
+  .voice-context-pill.active { background: rgba(13,92,99,0.12); color: var(--primary-strong); border-color: rgba(13,92,99,0.24); }
+  .voice-micro-chip { display: grid; gap: 10px; padding: 14px 16px; border-radius: 18px; background: rgba(13,92,99,0.08); border: 1px solid rgba(13,92,99,0.18); }
+  .voice-micro-chip strong { display: block; }
+  .inbox-action-card.voice-target { border-color: rgba(13,92,99,0.36); box-shadow: 0 0 0 2px rgba(13,92,99,0.12), var(--shadow); }
   .spinner { animation: rotate 2s linear infinite; }
   .capture-grid { display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(320px, 0.95fr); gap: 18px; align-items: start; }
   .command-centre { display: grid; gap: 18px; }
@@ -1318,6 +1335,8 @@
     && searchParams?.get('snapshotDemo') === '1';
   const inboxDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('inboxDemo') === '1';
+  const voiceDemoCommand = searchParams?.get('voiceDemoCommand') || '';
+  const voiceDemoTarget = searchParams?.get('voiceDemoTarget') || '';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1382,9 +1401,12 @@
   const [inboxDrafts, setInboxDrafts] = useState({});
   const [inboxBusyId, setInboxBusyId] = useState('');
   const [inboxMessage, setInboxMessage] = useState(null);
+  const [activeVoiceContextId, setActiveVoiceContextId] = useState(voiceDemoTarget);
+  const [voiceMicroDecisionChip, setVoiceMicroDecisionChip] = useState(null);
   const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
+  const voiceDemoSeedRef = useRef('');
   const quarterStart = '2026-01-01';
   const quarterEnd = '2026-03-31';
 
@@ -1606,10 +1628,20 @@
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
   const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const activeVoiceCard = findVoiceContextCard(inboxCards, activeVoiceContextId);
   const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
   const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
   const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
+  useEffect(() => {
+    if (!activeVoiceContextId) {
+      return;
+    }
+    if (!findVoiceContextCard(inboxCards, activeVoiceContextId)) {
+      setActiveVoiceContextId('');
+    }
+  }, [activeVoiceContextId, inboxCards]);
+
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
   const canManageGovernance = hasPermission(currentUser, PERMISSIONS.GOVERNANCE_MANAGE);
@@ -1806,11 +1838,13 @@
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxDrafts({});
+        setVoiceMicroDecisionChip(null);
         setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
         return;
       }
       await axios.post(`${API_BASE_URL}/inbox/undo-last`);
       await fetchAllData();
+      setVoiceMicroDecisionChip(null);
       setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
     } catch (error) {
       setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
@@ -1991,22 +2025,51 @@
     const cleanText = text.replace(/┬ú/g, 'GBP ');
     try {
       setVoiceStatus('processing');
-      const response = await axios.post(`${API_BASE_URL}/voice/process`, {
+      const microDecisionRequest = buildVoiceMicroDecisionRequest({
+        currentTab,
         transcript: cleanText,
-        device_id: 'mobile-v139',
-        current_date: new Date().toISOString(),
+        activeVoiceContextId,
+        inboxCards,
       });
-      if (response.data.confirmation_text && window.SpeechSynthesisUtterance) {
+
+      if (microDecisionRequest?.error) {
+        setInboxMessage({ type: 'error', text: microDecisionRequest.error });
+        setCurrentTab('inbox');
+        return;
+      }
+
+      const response = microDecisionRequest
+        ? await axios.post(`${API_BASE_URL}${microDecisionRequest.endpoint}`, microDecisionRequest.payload)
+        : await axios.post(`${API_BASE_URL}/voice/process`, {
+            transcript: cleanText,
+            device_id: 'mobile-v139',
+            current_date: new Date().toISOString(),
+          });
+      const spokenConfirmation = response.data.confirmation_chip || response.data.confirmation_text;
+      if (spokenConfirmation && window.SpeechSynthesisUtterance) {
         try {
-          const utterance = new window.SpeechSynthesisUtterance(response.data.confirmation_text);
+          const utterance = new window.SpeechSynthesisUtterance(spokenConfirmation);
           window.speechSynthesis.speak(utterance);
         } catch (_) {
         }
       }
-      executeVoiceAction(response.data);
-      fetchAllData();
+      if (microDecisionRequest) {
+        setVoiceMicroDecisionChip((previous) => reduceVoiceMicroDecisionChip(previous, response.data, microDecisionRequest.card));
+        setInboxMessage({
+          type: 'success',
+          text: response.data.confirmation_chip || 'Voice triage action applied.',
+        });
+      } else {
+        executeVoiceAction(response.data);
+      }
+      await fetchAllData();
     } catch (error) {
-      alert('AI Error');
+      const message = error?.response?.data?.error || 'AI Error';
+      if (currentTab === 'inbox' || currentTab === 'quarter') {
+        setInboxMessage({ type: 'error', text: message });
+      } else {
+        alert(message);
+      }
     } finally {
       setVoiceStatus('idle');
       setIsListening(false);
@@ -2894,7 +2957,11 @@
             const saving = inboxBusyId === card.id;
             const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
             return (
-              <section key={card.id} className="inbox-action-card">
+              <section
+                key={card.id}
+                className={`inbox-action-card ${activeVoiceContextId === card.id ? 'voice-target' : ''}`}
+                onClick={() => setActiveVoiceContextId(card.id)}
+              >
                 <div className="inbox-card-top">
                   <div>
                     <strong>{card.merchant}</strong>
@@ -2914,6 +2981,9 @@
                       {label}
                     </span>
                   ))}
+                  {activeVoiceContextId === card.id && (
+                    <span className="voice-context-pill active">Voice target</span>
+                  )}
                   {card.suggested_category?.category_name && (
                     <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
                       <CheckCircle2 size={14} />
@@ -3357,6 +3427,9 @@
   };
 
   const openReadinessWorkflow = (issue) => {
+    if (issue?.affected_entity_id) {
+      setActiveVoiceContextId(String(issue.affected_entity_id));
+    }
     if (issue?.resolution_target?.workflow === '/inbox/finish-now') {
       setCurrentTab('inbox');
       return;
@@ -3364,6 +3437,76 @@
     setCurrentTab('inbox');
   };
 
+  const renderVoiceContextBanner = () => {
+    if (!(currentTab === 'inbox' || currentTab === 'quarter')) {
+      return null;
+    }
+
+    return (
+      <section className="voice-context-banner">
+        <div>
+          <strong>Voice triage</strong>
+          <div className="small-muted">
+            Use the mic against one selected finish-now item only. Supported commands here are category, business, personal, split n%, and attach receipt.
+          </div>
+        </div>
+        <div className="voice-context-row">
+          <span className={`voice-context-pill ${activeVoiceCard ? 'active' : ''}`}>
+            {activeVoiceCard
+              ? `Target: ${activeVoiceCard.merchant} (${activeVoiceCard.id})`
+              : 'Select a blocker card to arm voice triage'}
+          </span>
+          {activeVoiceCard && (
+            <button className="secondary-action" type="button" onClick={() => setCurrentTab('inbox')}>
+              Edit target
+            </button>
+          )}
+        </div>
+        {voiceMicroDecisionChip && (
+          <div className="voice-micro-chip">
+            <div>
+              <strong>{voiceMicroDecisionChip.confirmationChip}</strong>
+              <div className="small-muted">
+                {voiceMicroDecisionChip.targetLabel} ({voiceMicroDecisionChip.targetId})
+              </div>
+            </div>
+            {voiceMicroDecisionChip.labels.length > 0 && (
+              <div className="voice-chip-labels">
+                {voiceMicroDecisionChip.labels.map((label) => (
+                  <span key={`${voiceMicroDecisionChip.targetId}-${label}`} className="voice-action-pill">
+                    {label}
+                  </span>
+                ))}
+              </div>
+            )}
+            <div className="voice-chip-actions">
+              <button
+                className="secondary-action"
+                type="button"
+                onClick={() => {
+                  setCurrentTab('inbox');
+                  setActiveVoiceContextId(voiceMicroDecisionChip.targetId);
+                }}
+              >
+                Edit
+              </button>
+              {voiceMicroDecisionChip.undoSupported && (
+                <button
+                  className="secondary-action"
+                  type="button"
+                  onClick={undoLastInboxAction}
+                  disabled={inboxBusyId === 'undo-last'}
+                >
+                  Undo
+                </button>
+              )}
+            </div>
+          </div>
+        )}
+      </section>
+    );
+  };
+
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
@@ -3654,6 +3797,13 @@
                     </div>
                   </div>
                   <div className="finish-now-actions">
+                    <button
+                      className="secondary-action"
+                      type="button"
+                      onClick={() => setActiveVoiceContextId(String(issue.affected_entity_id))}
+                    >
+                      {activeVoiceContextId === String(issue.affected_entity_id) ? 'Voice target set' : 'Arm voice'}
+                    </button>
                     {(readinessDemoEnabled || snapshotDemoEnabled) ? (
                       <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
                         Mark fixed
@@ -4391,6 +4541,7 @@
         </button>
       </section>
       {connectionError && <div className="alert alert-danger">{connectionError}</div>}
+      {renderVoiceContextBanner()}
 
       {loading ? (
         <div className="text-center py-5">Loading...</div>
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..6eb469fcc7f023f8545d9e952a5823fb8c47a88d
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});
diff --git a/workstream/200_inprogress/codex/20260305_192700_bizpa_mvp_pivot_phase06_voice_micro_decisions.md b/workstream/200_inprogress/codex/20260305_192700_bizpa_mvp_pivot_phase06_voice_micro_decisions.md
deleted file mode 100644
index cf85796e760112547a34bd77ea2a0f0a86fd3219..0000000000000000000000000000000000000000
--- a/workstream/200_inprogress/codex/20260305_192700_bizpa_mvp_pivot_phase06_voice_micro_decisions.md
+++ /dev/null
@@ -1,43 +0,0 @@
-# Source
-- Derived from backlog: `C:\Users\edebe\eds\workstream\000_backlog\mvp_prd_quarterly_export_10min.md`
-- Decomposed from checklist: `C:\Users\edebe\eds\workstream\300_complete\20260305_191657_bizpa_mvp_quarterly_pivot_full_delivery_checklist.md`
-
-# Task Summary
-Constrain voice intents to PRD micro-decisions and bind safely to inbox/finish-now context with confirmation chip + undo.
-
-# Context
-- Affected area: voice controller/parser and web/mobile voice UX
-
-# Plan
-- [ ] 1. Implement phase scope in code and configuration.
-  - [ ] Test: Execute phase-specific implementation checks and verify expected behavior change.
-  - [ ] Evidence: pending
-- [ ] 2. Add/adjust automated tests for this phase.
-  - [ ] Test: Run targeted unit/integration tests for new behavior and ensure green results.
-  - [ ] Evidence: pending
-- [ ] 3. Validate against PRD acceptance criteria impacted by this phase.
-  - [ ] Test: Run intent contract tests for supported utterances and verify each action is applied, confirmed, and undoable.
-  - [ ] Evidence: pending
-- [ ] 4. Update technical notes and rollout caveats for downstream phases.
-  - [ ] Test: Confirm lifecycle doc lists changed files, commands run, and known risks.
-  - [ ] Evidence: pending
-
-# Implementation Log
-- 2026-03-05 Created decomposed task file from approved MVP pivot checklist.
-
-# Changes Made
-- Task file created; implementation not started.
-
-# Validation
-- Pending.
-
-# Risks/Notes
-- Must follow strict sequential completion and evidence capture before starting dependent phase.
-
-# Completion Status
-- Todo.
-
-
-## Retry History
-Retry-Count: 1
-- Retry scheduled at 2026-03-18 17:21:29

file update
A C:\Users\edebe\eds\workstream\200_inprogress\codex\20260305_192700_bizpa_mvp_pivot_phase06_voice_micro_decisions.md
# Source
- Derived from backlog: `C:\Users\edebe\eds\workstream\000_epic\20260305_185316_mvp_prd_quarterly_export_10min.md`
- Decomposed from checklist: `C:\Users\edebe\eds\workstream\300_complete\20260305_191657_bizpa_mvp_quarterly_pivot_full_delivery_checklist.md`

# Task Summary
Constrain voice intents to PRD micro-decisions and bind them safely to inbox/finish-now context with a visible confirmation chip and undo-compatible triage flow.

# Context
- Affected backend area: `C:\Users\edebe\eds\bizPA\backend\src\controllers\voiceController.js`, `C:\Users\edebe\eds\bizPA\backend\src\controllers\inboxController.js`
- New backend support: `C:\Users\edebe\eds\bizPA\backend\src\services\inboxClassificationService.js`, `C:\Users\edebe\eds\bizPA\backend\verify_voice_micro_decisions.js`
- Affected frontend area: `C:\Users\edebe\eds\bizPA\frontend\src\App.jsx`
- New frontend support: `C:\Users\edebe\eds\bizPA\frontend\src\voiceMicroDecisions.js`, `C:\Users\edebe\eds\bizPA\frontend\src\voiceMicroDecisions.test.js`

# Dependency
`C:\Users\edebe\eds\workstream\300_complete\20260305_191657_bizpa_mvp_quarterly_pivot_full_delivery_checklist.md`

# Plan
- [x] 1. Implement phase scope in code and configuration.
  - [x] Test: Execute the updated voice micro-decision flow against backend verification and confirm context-bound classification updates now use audited inbox persistence with undo metadata.
  - [x] Evidence: `bizPA/backend/src/services/inboxClassificationService.js` now centralizes audited classification writes; `bizPA/backend/src/controllers/voiceController.js` now requires explicit micro-decision context and returns context binding plus undo metadata; `bizPA/frontend/src/App.jsx` now binds voice triage to an explicitly selected inbox/finish-now card and shows a confirmation chip with undo/edit actions.
- [x] 2. Add/adjust automated tests for this phase.
  - [x] Test: Run targeted backend and frontend tests for the new behavior and ensure green results.
  - [x] Evidence: `npm run verify:voice-micro-decisions` passed in `bizPA/backend`; `npx react-scripts test --watchAll=false --runInBand --runTestsByPath src/voiceMicroDecisions.test.js src/quarterReadiness.test.js` passed in `bizPA/frontend`.
- [x] 3. Validate against PRD acceptance criteria impacted by this phase.
  - [x] Test: Run intent contract tests for supported utterances and verify each action is context-safe, confirmed, and undo-compatible for inbox triage actions; confirm frontend production build compiles with the new voice UX.
  - [x] Evidence: Backend verifier output ended with `voice_micro_decisions_ok`, `context_guardrails_ok=true`, `audited_classification_updates_ok=true`, and `safe_context_binding_ok=true`; frontend `npm run build` compiled successfully.
- [x] 4. Update technical notes and rollout caveats for downstream phases.
  - [x] Test: Confirm this lifecycle doc lists changed files, commands run, known risks, and the pending user-verification gate for the visible inbox voice flow.
  - [x] Evidence: Lifecycle file rewritten on 2026-03-19 with normalized evidence, validation results, and an explicit user-verification request/state.

# Evidence
Objective-Delivery-Coverage: 90%
Auto-Acceptance: false
- Evidence-Type: demo
  - Artifact: `C:\Users\edebe\eds\bizPA\frontend\src\App.jsx`
  - Objective-Proved: The mobile/web inbox and quarter screens now require an explicit finish-now selection before voice triage, show the active voice target, and render a confirmation chip with edit/undo affordances after a voice micro-decision.
  - Status: captured
- Evidence-Type: test_output
  - Artifact: `npm run verify:voice-micro-decisions` output: `voice_micro_decisions_ok`, `context_guardrails_ok=true`, `audited_classification_updates_ok=true`, `attach_receipt_clarification_ok=true`, `match_selection_ok=true`, `safe_context_binding_ok=true`, `validation_errors_ok=true`
  - Objective-Proved: Supported utterances are constrained to the micro-decision contract, require safe context binding, and return the expected confirmation/undo contract while preserving audited inbox behavior.
  - Status: captured
- Evidence-Type: test_output
  - Artifact: `npx react-scripts test --watchAll=false --runInBand --runTestsByPath src/voiceMicroDecisions.test.js src/quarterReadiness.test.js` output: `PASS src/voiceMicroDecisions.test.js`, `PASS src/quarterReadiness.test.js`
  - Objective-Proved: The frontend request-routing and confirmation-chip helper logic behaves correctly for selection enforcement, context routing, and chip aggregation.
  - Status: captured
- Evidence-Type: test_output
  - Artifact: `npm run build` in `C:\Users\edebe\eds\bizPA\frontend` output: `Compiled successfully.`
  - Objective-Proved: The user-visible voice triage UI changes compile successfully in the production frontend build.
  - Status: captured
- Evidence-Type: diff
  - Artifact: Updated `C:\Users\edebe\eds\bizPA\backend\src\controllers\voiceController.js`, `C:\Users\edebe\eds\bizPA\backend\src\controllers\inboxController.js`, `C:\Users\edebe\eds\bizPA\frontend\src\App.jsx`; added `C:\Users\edebe\eds\bizPA\backend\src\services\inboxClassificationService.js`, `C:\Users\edebe\eds\bizPA\frontend\src\voiceMicroDecisions.js`, and `C:\Users\edebe\eds\bizPA\frontend\src\voiceMicroDecisions.test.js`
  - Objective-Proved: The workspace contains the backend and frontend implementation required for safe, undo-compatible voice micro-decisions.
  - Status: captured
- Evidence-Type: manual_verification
  - Artifact: User verification requested on 2026-03-19 for the visible inbox/quarter voice flow: select a blocker, run `Category: Travel` / `Business` / `Split 70%`, confirm the chip appears, and verify Undo reverses the last triage action.
  - Objective-Proved: Final confirmation from an operator is still required for the visible voice UX path.
  - Status: planned

# Implementation Log
- 2026-03-05 Created decomposed task file from the approved MVP pivot checklist.
- 2026-03-19 Re-read the workstream lifecycle skill and reconciled the stale in-progress task against the current backend/frontend workspace.
- 2026-03-19 Identified that voice micro-decisions were bypassing the inbox audit path, which meant `/api/v1/inbox/undo-last` could not reliably undo voice-applied triage.
- 2026-03-19 Added `bizPA/backend/src/services/inboxClassificationService.js` to centralize classification persistence, audit logging, and readiness recalculation.
- 2026-03-19 Updated `bizPA/backend/src/controllers/inboxController.js` to use the shared audited classification helper.
- 2026-03-19 Updated `bizPA/backend/src/controllers/voiceController.js` so context-bound voice category/business/personal/split decisions use the shared audited classification helper and return explicit context-binding plus undo metadata.
- 2026-03-19 Expanded `bizPA/backend/verify_voice_micro_decisions.js` to assert context guardrails, audited classification persistence, preserved field state across incremental voice commands, and safe binding behavior.
- 2026-03-19 Added `bizPA/frontend/src/voiceMicroDecisions.js` and `bizPA/frontend/src/voiceMicroDecisions.test.js` to isolate and verify the client-side routing/confirmation-chip logic.
- 2026-03-19 Updated `bizPA/frontend/src/App.jsx` to require an explicit active inbox/finish-now selection for voice triage, render the active voice target banner, and surface a combined confirmation chip with edit/undo controls.
- 2026-03-19 Ran backend verification, frontend targeted tests, and a production frontend build.
- 2026-03-19 Requested user verification for the user-visible inbox/quarter voice flow and left the task active pending that confirmation.

# Changes Made
- `C:\Users\edebe\eds\bizPA\backend\src\services\inboxClassificationService.js`
  - Added shared audited classification upsert logic that preserves existing fields, writes `transaction_audit_log`, and recalculates readiness.
- `C:\Users\edebe\eds\bizPA\backend\src\controllers\inboxController.js`
  - Switched inbox classification writes to the shared service so UI and voice triage use the same persistence/audit path.
- `C:\Users\edebe\eds\bizPA\backend\src\controllers\voiceController.js`
  - Enforced explicit micro-decision context, bound triage commands to the selected transaction/evidence context, and returned undo/context metadata for the confirmation chip flow.
- `C:\Users\edebe\eds\bizPA\backend\verify_voice_micro_decisions.js`
  - Added assertions for context guardrails, safe binding, audited triage writes, and clarification/error handling.
- `C:\Users\edebe\eds\bizPA\frontend\src\voiceMicroDecisions.js`
  - Added pure helpers for routing voice to `/voice/micro-decision`, enforcing explicit selection, and aggregating confirmation-chip actions.
- `C:\Users\edebe\eds\bizPA\frontend\src\voiceMicroDecisions.test.js`
  - Added tests covering missing selection handling, finish-now request construction, and chip aggregation/reset behavior.
- `C:\Users\edebe\eds\bizPA\frontend\src\App.jsx`
  - Added active voice target selection state for inbox and quarter cards.
  - Routed voice on inbox/quarter tabs through the micro-decision endpoint only when a blocker is explicitly selected.
  - Added the visible voice context banner and combined confirmation chip with edit/undo actions.
  - Preserved the legacy `/voice/process` path for non-triage tabs.

# Validation
- 2026-03-19 16:59:44+00:00: `npm run verify:voice-micro-decisions`
  - Result: pass
  - Key output:
    - `voice_micro_decisions_ok`
    - `context_guardrails_ok=true`
    - `audited_classification_updates_ok=true`
    - `attach_receipt_clarification_ok=true`
    - `match_selection_ok=true`
    - `safe_context_binding_ok=true`
    - `validation_errors_ok=true`
- 2026-03-19 16:59:44+00:00: `npm run verify:voice-capture`
  - Result: pass
  - Key output:
    - `voice_capture_parser_ok`
    - `entity_mapping_ok=true`
    - `low_confidence_review_ok=true`
    - `composition_contract_ok=true`
    - `query_period_filters_ok=true`
    - `query_exact_date_filters_ok=true`
- 2026-03-19 16:59:44+00:00: initial frontend test attempt via npm wrapper
  - Result: blocked by environment
  - Key output: `Error: spawn EPERM`
  - Note: `react-scripts` attempted worker-process spawning inside the sandbox when invoked via `npm test`.
- 2026-03-19 16:59:44+00:00: `npx react-scripts test --watchAll=false --runInBand --runTestsByPath src/voiceMicroDecisions.test.js src/quarterReadiness.test.js`
  - Result: pass
  - Key output:
    - `PASS src/voiceMicroDecisions.test.js`
    - `PASS src/quarterReadiness.test.js`
    - `Tests: 7 passed, 7 total`
- 2026-03-19 16:59:44+00:00: `npm run build`
  - Result: pass
  - Key output:
    - `Compiled successfully.`
- 2026-03-19 16:59:44+00:00: user verification request recorded
  - Result: pending
  - Key output: Please verify that selecting a finish-now blocker, speaking `Category: Travel` / `Business` / `Split 70%`, and tapping Undo behaves correctly in the visible inbox/quarter flow.

# Risks/Notes
- The visible voice triage flow is now selection-gated on inbox and quarter screens. If future UI changes remove or obscure the selected voice target, the frontend helper tests should be expanded before rollout.
- Evidence commands remain context-bound to `evidence` rather than the inbox transaction flow. This task hardened their context safety and confirmation payloads, but it did not add a visible evidence-selection UI in the current frontend.
- The direct `npm test` wrapper is not reliable in this sandbox because Jest worker spawning can fail with `EPERM`; use the documented `npx react-scripts test --runInBand ...` form for deterministic local verification here.
- A prior duplicate lifecycle file exists under `workstream/300_complete`; this in-progress record is the active reconciliation file requested by the user and remains open until user verification is captured.

# Completion Status
- Awaiting user verification (2026-03-19 16:59:44+00:00)
apply_patch(auto_approved=true) exited 0 in 1.87s:
Success. Updated the following files:
A C:\Users\edebe\eds\workstream\200_inprogress\codex\20260305_192700_bizpa_mvp_pivot_phase06_voice_micro_decisions.md
file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..e6d93b4bc1cd6960ce27238cfb7e90c3322f41e0
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,6 +10,36 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+const {
+  parseVoiceMicroDecision,
+  validateMicroDecisionContext,
+} = require('../services/voiceMicroDecisionService');
+
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undoable: undoSupported,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
 
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
@@ -267,76 +297,177 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
-  const text = String(transcript).toLowerCase().trim();
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_kind = null,
+    context_scope = null,
+  } = req.body || {};
+  const rawTranscript = String(transcript).trim();
+  const text = rawTranscript.toLowerCase();
+  const normalizedContext = context_kind ?? context_scope;
+  const parsedCommand = parseVoiceMicroDecision(rawTranscript);
+  const contextValidation = validateMicroDecisionContext(parsedCommand, normalizedContext);
+  const normalizedContextScope = contextValidation.ok
+    ? contextValidation.context_kind
+    : (normalizedContext === null || normalizedContext === undefined
+      ? null
+      : String(normalizedContext).toLowerCase().replace(/-/g, '_'));
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
+  if (!contextValidation.ok) return res.status(contextValidation.statusCode).json({ error: contextValidation.error });
 
   try {
-    if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
-      const categoryName = text.replace('category:', '').trim();
-      const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+    if (parsedCommand.type === 'category') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: parsedCommand.category_code,
+            category_name: parsedCommand.category_name,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'category', label: parsedCommand.category_name }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
-      const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+    if (parsedCommand.type === 'business_personal') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const bp = parsedCommand.business_personal;
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    const splitMatch = text.match(/split\s+(\d{1,3})%?/);
-    if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
-      const pct = Number(splitMatch[1]);
+    if (parsedCommand.type === 'split') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const pct = Number(parsedCommand.split_business_pct);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+    if (parsedCommand.type === 'attach_receipt') {
+      if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for attach receipt commands.' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          parsed_command: parsedCommand,
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
-    if (['match first', 'match second', 'match third'].includes(text)) {
+    if (parsedCommand.type === 'match_rank') {
       if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for match commands.' });
-      const index = text === 'match first' ? 1 : text === 'match second' ? 2 : 3;
+      const index = parsedCommand.match_rank;
       const suggestions = await db.query(
         `
         WITH ev AS (
@@ -364,10 +495,24 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          bank_txn_id: chosen,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text === 'no match') {
+    if (parsedCommand.type === 'no_match') {
       if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for no match.' });
       await db.query(
         `
@@ -376,7 +521,20 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/backend/verify_voice_micro_decisions.js b/backend/verify_voice_micro_decisions.js
index 25d9ab02050aac37a5d38aca5c6f0154de9bb787..15c93efa65f133ec1a59d827ecbfa29a95cf4e6d
--- a/backend/verify_voice_micro_decisions.js
+++ b/backend/verify_voice_micro_decisions.js
@@ -9,16 +9,70 @@
       { bank_txn_id: 'txn-2' },
       { bank_txn_id: 'txn-3' }
     ];
+    this.classifications = new Map();
+    this.auditEntries = [];
   }
 
-  async query(text, params) {
+  get pool() {
+    return {
+      connect: async () => ({
+        query: this.query.bind(this),
+        release() {},
+      }),
+    };
+  }
+
+  async query(text, params = []) {
     const sql = text.replace(/\s+/g, ' ').trim();
     this.calls.push({ sql, params });
 
+    if (sql === 'BEGIN' || sql === 'COMMIT' || sql === 'ROLLBACK') {
+      return { rows: [] };
+    }
+
+    if (sql.includes('SELECT * FROM transaction_classifications')) {
+      const existing = this.classifications.get(params[0]);
+      return { rows: existing ? [existing] : [] };
+    }
+
+    if (sql.includes('INSERT INTO transaction_classifications')) {
+      const next = {
+        user_id: params[0],
+        bank_txn_id: params[1],
+        category_code: params[2],
+        category_name: params[3],
+        business_personal: params[4],
+        is_split: params[5],
+        split_business_pct: params[6],
+        confidence: params[7],
+        source: params[8],
+      };
+      this.classifications.set(params[1], next);
+      return { rows: [next] };
+    }
+
+    if (sql.includes('INSERT INTO transaction_audit_log')) {
+      this.auditEntries.push({
+        bank_txn_id: params[1],
+        previous_value: params[4],
+        new_value: params[5],
+        change_source: params[6],
+      });
+      return { rows: [] };
+    }
+
+    if (sql.includes('SELECT txn_date FROM bank_transactions')) {
+      return { rows: [{ txn_date: '2026-03-12' }] };
+    }
+
     if (sql.includes('FROM bank_transactions bt CROSS JOIN ev')) {
       return { rows: this.matchSuggestions };
     }
 
+    if (sql.includes('INSERT INTO evidence_links')) {
+      return { rows: [] };
+    }
+
     return { rows: [] };
   }
 }
@@ -69,45 +123,79 @@
   const mockDb = new MockDb();
   const controller = loadController(mockDb);
 
+  const missingContext = await invoke(controller, {
+    transcript: 'Business',
+    bank_txn_id: 'txn-123'
+  });
+  assert.strictEqual(missingContext.statusCode, 400);
+  assert.strictEqual(missingContext.body.error, 'context_kind is required for MVP voice micro-decisions.');
+
   const category = await invoke(controller, {
     transcript: 'Category: Travel',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(category.statusCode, 200);
-  assert.strictEqual(category.body.confirmation_chip, 'Applied: travel');
-  assert(
-    mockDb.calls.some((call) => call.sql.includes('INSERT INTO transaction_classifications') && call.params[2] === 'TRAVEL' && call.params[3] === 'travel'),
-    'Expected category command to persist manual classification'
-  );
+  assert.strictEqual(category.body.confirmation_chip, 'Applied: Travel');
+  assert.strictEqual(category.body.undo.supported, true);
+  assert.deepStrictEqual(category.body.context_binding, {
+    scope: 'inbox',
+    target_type: 'bank_transaction',
+    target_id: 'txn-123'
+  });
+  assert.strictEqual(mockDb.classifications.get('txn-123').category_code, 'TRAVEL');
+  assert.strictEqual(mockDb.auditEntries.at(-1).change_source, 'voice');
 
   const business = await invoke(controller, {
     transcript: 'Business',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(business.statusCode, 200);
   assert.strictEqual(business.body.confirmation_chip, 'Applied: BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').category_code, 'TRAVEL');
+  assert.strictEqual(mockDb.classifications.get('txn-123').business_personal, 'BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').is_split, false);
 
   const split = await invoke(controller, {
     transcript: 'Split 70%',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(split.statusCode, 200);
   assert.strictEqual(split.body.confirmation_chip, 'Applied: Split 70%');
+  assert.strictEqual(split.body.undo.supported, true);
+  assert.strictEqual(mockDb.classifications.get('txn-123').business_personal, 'BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').is_split, true);
+  assert.strictEqual(mockDb.classifications.get('txn-123').split_business_pct, 70);
 
   const attachReceipt = await invoke(controller, {
     transcript: 'Attach receipt',
-    bank_txn_id: 'txn-123'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(attachReceipt.statusCode, 200);
   assert.strictEqual(attachReceipt.body.action_status, 'clarification_needed');
   assert.strictEqual(attachReceipt.body.confirmation_chip, 'Attach receipt requested');
+  assert.strictEqual(attachReceipt.body.undo.supported, false);
+  assert.deepStrictEqual(attachReceipt.body.context_binding, {
+    scope: 'evidence',
+    target_type: 'evidence',
+    target_id: 'evidence-1'
+  });
 
   const matchSecond = await invoke(controller, {
     transcript: 'Match second',
-    evidence_id: 'evidence-1'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(matchSecond.statusCode, 200);
   assert.strictEqual(matchSecond.body.confirmation_chip, 'Applied: Match 2');
+  assert.deepStrictEqual(matchSecond.body.context_binding, {
+    scope: 'evidence',
+    target_type: 'evidence',
+    target_id: 'evidence-1'
+  });
   assert(
     mockDb.calls.some((call) => call.sql.includes('INSERT INTO evidence_links') && call.params[1] === 'evidence-1' && call.params[2] === 'txn-2'),
     'Expected match command to persist selected evidence link'
@@ -115,29 +203,41 @@
 
   const noMatch = await invoke(controller, {
     transcript: 'No match',
-    evidence_id: 'evidence-1'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(noMatch.statusCode, 200);
   assert.strictEqual(noMatch.body.confirmation_chip, 'Applied: No match');
 
   const invalidSplit = await invoke(controller, {
     transcript: 'Split 140%',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(invalidSplit.statusCode, 400);
   assert.strictEqual(invalidSplit.body.error, 'Split percentage must be 0-100.');
 
+  const unsafeContext = await invoke(controller, {
+    transcript: 'Category: Travel',
+    bank_txn_id: 'txn-123',
+    context_kind: 'evidence'
+  });
+  assert.strictEqual(unsafeContext.statusCode, 409);
+  assert.strictEqual(unsafeContext.body.error, 'Command "category" is not allowed for evidence context.');
+
   const unsupported = await invoke(controller, {
-    transcript: 'Archive this forever'
+    transcript: 'Archive this forever',
+    context_kind: 'inbox'
   });
   assert.strictEqual(unsupported.statusCode, 400);
   assert.strictEqual(unsupported.body.error, 'Unsupported MVP voice micro-decision command.');
 
   console.log('voice_micro_decisions_ok');
-  console.log('category_business_split_ok=true');
+  console.log('context_guardrails_ok=true');
+  console.log('audited_classification_updates_ok=true');
   console.log('attach_receipt_clarification_ok=true');
   console.log('match_selection_ok=true');
-  console.log('no_match_ok=true');
+  console.log('safe_context_binding_ok=true');
   console.log('validation_errors_ok=true');
 }
 
diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index 9b145fadac9123e3fa01b12af6ef2b9916dc11de..590be3d00c08cb02d6139abdd4d1a0ee41f9de7b
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -50,6 +50,16 @@
   buildQuarterUiState,
   deriveResolvedIssueIdsFromSearchParams,
 } from './quarterReadiness';
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+import {
+  applyInboxVoiceCommandToDraft,
+  buildInboxVoiceChip,
+  parseInboxVoiceCommand,
+} from './voiceMicroDecision';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -427,6 +437,13 @@
   .date-group-count { border-radius: 999px; padding: 4px 9px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.72rem; color: var(--text-muted); }
   .date-group-body { padding: 0 12px 12px; }
   .voice-preview { background: rgba(0,0,0,0.82); color: white; padding: 8px 15px; border-radius: 15px; margin-bottom: 8px; font-size: 0.72rem; max-width: 240px; text-align: right; }
+  .voice-context-banner { display: grid; gap: 10px; padding: 14px 16px; margin-bottom: 16px; border-radius: 18px; background: var(--bg-card); border: 1px solid var(--border); box-shadow: var(--shadow); }
+  .voice-context-row, .voice-chip-actions, .voice-chip-labels { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
+  .voice-context-pill, .voice-action-pill { border-radius: 999px; padding: 6px 10px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.74rem; font-weight: 700; }
+  .voice-context-pill.active { background: rgba(13,92,99,0.12); color: var(--primary-strong); border-color: rgba(13,92,99,0.24); }
+  .voice-micro-chip { display: grid; gap: 10px; padding: 14px 16px; border-radius: 18px; background: rgba(13,92,99,0.08); border: 1px solid rgba(13,92,99,0.18); }
+  .voice-micro-chip strong { display: block; }
+  .inbox-action-card.voice-target { border-color: rgba(13,92,99,0.36); box-shadow: 0 0 0 2px rgba(13,92,99,0.12), var(--shadow); }
   .spinner { animation: rotate 2s linear infinite; }
   .capture-grid { display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(320px, 0.95fr); gap: 18px; align-items: start; }
   .command-centre { display: grid; gap: 18px; }
@@ -1318,6 +1335,8 @@
     && searchParams?.get('snapshotDemo') === '1';
   const inboxDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('inboxDemo') === '1';
+  const voiceDemoCommand = searchParams?.get('voiceDemoCommand') || '';
+  const voiceDemoTarget = searchParams?.get('voiceDemoTarget') || '';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1382,9 +1401,12 @@
   const [inboxDrafts, setInboxDrafts] = useState({});
   const [inboxBusyId, setInboxBusyId] = useState('');
   const [inboxMessage, setInboxMessage] = useState(null);
+  const [activeVoiceContextId, setActiveVoiceContextId] = useState(voiceDemoTarget);
+  const [voiceMicroDecisionChip, setVoiceMicroDecisionChip] = useState(null);
   const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
+  const voiceDemoSeedRef = useRef('');
   const quarterStart = '2026-01-01';
   const quarterEnd = '2026-03-31';
 
@@ -1606,10 +1628,80 @@
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
   const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const activeVoiceCard = findVoiceContextCard(inboxCards, activeVoiceContextId);
   const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
   const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
   const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
+  const applyVoiceMicroDecisionLocally = (card, parsedCommand, responseLike = {}) => {
+    if (!card || !parsedCommand) {
+      return;
+    }
+
+    const nextDraft = applyInboxVoiceCommandToDraft(inboxDrafts[card.id] || buildInboxDraft(card), parsedCommand);
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: nextDraft,
+    }));
+
+    if (parsedCommand.type === 'category') {
+      syncInboxCardLocally(card.id, {
+        category_code: nextDraft.category_code,
+        category_name: nextDraft.category_name,
+      });
+    } else if (parsedCommand.type === 'business_personal') {
+      syncInboxCardLocally(card.id, {
+        business_personal: nextDraft.business_personal,
+        is_split: false,
+        split_business_pct: null,
+      });
+    } else if (parsedCommand.type === 'split') {
+      syncInboxCardLocally(card.id, {
+        business_personal: 'BUSINESS',
+        is_split: true,
+        split_business_pct: Number(parsedCommand.split_business_pct),
+      });
+    }
+
+    setVoiceMicroDecisionChip((previous) => reduceVoiceMicroDecisionChip(previous, {
+      ...responseLike,
+      action_status: responseLike.action_status || 'execute',
+      confirmation_chip: responseLike.confirmation_chip || parsedCommand.confirmation_chip,
+      applied_actions: responseLike.applied_actions || [{ label: parsedCommand.confirmation_chip.replace(/^Applied:\s*/, '') }],
+      undo: responseLike.undo || { supported: true },
+    }, card));
+  };
+
+  useEffect(() => {
+    if (!activeVoiceContextId) {
+      return;
+    }
+    if (!findVoiceContextCard(inboxCards, activeVoiceContextId)) {
+      setActiveVoiceContextId('');
+    }
+  }, [activeVoiceContextId, inboxCards]);
+
+  useEffect(() => {
+    if (!inboxDemoEnabled || !voiceDemoCommand || !activeVoiceCard) {
+      return;
+    }
+    const seedKey = `${activeVoiceCard.id}:${voiceDemoCommand}`;
+    if (voiceDemoSeedRef.current === seedKey) {
+      return;
+    }
+    const parsedCommand = parseInboxVoiceCommand(voiceDemoCommand);
+    if (!parsedCommand) {
+      return;
+    }
+    voiceDemoSeedRef.current = seedKey;
+    applyVoiceMicroDecisionLocally(activeVoiceCard, parsedCommand, {
+      action_status: 'execute',
+      confirmation_chip: parsedCommand.confirmation_chip,
+      applied_actions: [{ label: parsedCommand.confirmation_chip.replace(/^Applied:\s*/, '') }],
+      undo: { supported: true },
+    });
+  }, [activeVoiceCard, inboxDemoEnabled, voiceDemoCommand]);
+
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
   const canManageGovernance = hasPermission(currentUser, PERMISSIONS.GOVERNANCE_MANAGE);
@@ -1802,15 +1894,18 @@
     clearInboxMessage();
     try {
       setInboxBusyId('undo-last');
+      setVoiceMicroDecisionChip(null);
       if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxDrafts({});
+        setVoiceMicroDecisionChip(null);
         setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
         return;
       }
       await axios.post(`${API_BASE_URL}/inbox/undo-last`);
       await fetchAllData();
+      setVoiceMicroDecisionChip(null);
       setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
     } catch (error) {
       setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
@@ -1991,22 +2086,78 @@
     const cleanText = text.replace(/┬ú/g, 'GBP ');
     try {
       setVoiceStatus('processing');
-      const response = await axios.post(`${API_BASE_URL}/voice/process`, {
+      const microDecisionRequest = buildVoiceMicroDecisionRequest({
+        currentTab,
         transcript: cleanText,
-        device_id: 'mobile-v139',
-        current_date: new Date().toISOString(),
+        activeVoiceContextId,
+        inboxCards,
       });
-      if (response.data.confirmation_text && window.SpeechSynthesisUtterance) {
+
+      if (microDecisionRequest?.error) {
+        setInboxMessage({ type: 'error', text: microDecisionRequest.error });
+        setCurrentTab('inbox');
+        return;
+      }
+
+      let response;
+      if (microDecisionRequest) {
+        if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+          const parsedCommand = parseInboxVoiceCommand(cleanText);
+          if (!parsedCommand) {
+            throw new Error('Only Category, Business, Personal, and Split {n}% are available in the local voice triage demo.');
+          }
+          applyVoiceMicroDecisionLocally(microDecisionRequest.card, parsedCommand, {
+            action_status: 'execute',
+            confirmation_chip: parsedCommand.confirmation_chip,
+            applied_actions: [{ label: parsedCommand.confirmation_chip.replace(/^Applied:\s*/, '') }],
+            undo: { supported: true },
+          });
+          response = { data: buildInboxVoiceChip({
+            action_status: 'execute',
+            confirmation_chip: parsedCommand.confirmation_chip,
+            undoable: true,
+            bank_txn_id: microDecisionRequest.card.id,
+          }, microDecisionRequest.card) };
+          response.data.confirmation_chip = parsedCommand.confirmation_chip;
+        } else {
+          response = await axios.post(`${API_BASE_URL}${microDecisionRequest.endpoint}`, microDecisionRequest.payload);
+        }
+      } else {
+        response = await axios.post(`${API_BASE_URL}/voice/process`, {
+          transcript: cleanText,
+          device_id: 'mobile-v139',
+          current_date: new Date().toISOString(),
+        });
+      }
+      const spokenConfirmation = response.data.confirmation_chip || response.data.confirmation_text;
+      if (spokenConfirmation && window.SpeechSynthesisUtterance) {
         try {
-          const utterance = new window.SpeechSynthesisUtterance(response.data.confirmation_text);
+          const utterance = new window.SpeechSynthesisUtterance(spokenConfirmation);
           window.speechSynthesis.speak(utterance);
         } catch (_) {
         }
       }
-      executeVoiceAction(response.data);
-      fetchAllData();
+      if (microDecisionRequest) {
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          setVoiceMicroDecisionChip((previous) => reduceVoiceMicroDecisionChip(previous, response.data, microDecisionRequest.card));
+          setInboxMessage({
+            type: 'success',
+            text: response.data.confirmation_chip || 'Voice triage action applied.',
+          });
+        }
+      } else {
+        executeVoiceAction(response.data);
+      }
+      if (!(microDecisionRequest && (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled))) {
+        await fetchAllData();
+      }
     } catch (error) {
-      alert('AI Error');
+      const message = error?.response?.data?.error || error?.message || 'AI Error';
+      if (currentTab === 'inbox' || currentTab === 'quarter') {
+        setInboxMessage({ type: 'error', text: message });
+      } else {
+        alert(message);
+      }
     } finally {
       setVoiceStatus('idle');
       setIsListening(false);
@@ -2894,7 +3045,11 @@
             const saving = inboxBusyId === card.id;
             const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
             return (
-              <section key={card.id} className="inbox-action-card">
+              <section
+                key={card.id}
+                className={`inbox-action-card ${activeVoiceContextId === card.id ? 'voice-target' : ''}`}
+                onClick={() => setActiveVoiceContextId(card.id)}
+              >
                 <div className="inbox-card-top">
                   <div>
                     <strong>{card.merchant}</strong>
@@ -2914,6 +3069,9 @@
                       {label}
                     </span>
                   ))}
+                  {activeVoiceContextId === card.id && (
+                    <span className="voice-context-pill active">Voice target</span>
+                  )}
                   {card.suggested_category?.category_name && (
                     <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
                       <CheckCircle2 size={14} />
@@ -3357,6 +3515,9 @@
   };
 
   const openReadinessWorkflow = (issue) => {
+    if (issue?.affected_entity_id) {
+      setActiveVoiceContextId(String(issue.affected_entity_id));
+    }
     if (issue?.resolution_target?.workflow === '/inbox/finish-now') {
       setCurrentTab('inbox');
       return;
@@ -3364,6 +3525,76 @@
     setCurrentTab('inbox');
   };
 
+  const renderVoiceContextBanner = () => {
+    if (!(currentTab === 'inbox' || currentTab === 'quarter')) {
+      return null;
+    }
+
+    return (
+      <section className="voice-context-banner">
+        <div>
+          <strong>Voice triage</strong>
+          <div className="small-muted">
+            Use the mic against one selected finish-now item only. Supported commands here are category, business, personal, split n%, and attach receipt.
+          </div>
+        </div>
+        <div className="voice-context-row">
+          <span className={`voice-context-pill ${activeVoiceCard ? 'active' : ''}`}>
+            {activeVoiceCard
+              ? `Target: ${activeVoiceCard.merchant} (${activeVoiceCard.id})`
+              : 'Select a blocker card to arm voice triage'}
+          </span>
+          {activeVoiceCard && (
+            <button className="secondary-action" type="button" onClick={() => setCurrentTab('inbox')}>
+              Edit target
+            </button>
+          )}
+        </div>
+        {voiceMicroDecisionChip && (
+          <div className="voice-micro-chip">
+            <div>
+              <strong>{voiceMicroDecisionChip.confirmationChip}</strong>
+              <div className="small-muted">
+                {voiceMicroDecisionChip.targetLabel} ({voiceMicroDecisionChip.targetId})
+              </div>
+            </div>
+            {voiceMicroDecisionChip.labels.length > 0 && (
+              <div className="voice-chip-labels">
+                {voiceMicroDecisionChip.labels.map((label) => (
+                  <span key={`${voiceMicroDecisionChip.targetId}-${label}`} className="voice-action-pill">
+                    {label}
+                  </span>
+                ))}
+              </div>
+            )}
+            <div className="voice-chip-actions">
+              <button
+                className="secondary-action"
+                type="button"
+                onClick={() => {
+                  setCurrentTab('inbox');
+                  setActiveVoiceContextId(voiceMicroDecisionChip.targetId);
+                }}
+              >
+                Edit
+              </button>
+              {voiceMicroDecisionChip.undoSupported && (
+                <button
+                  className="secondary-action"
+                  type="button"
+                  onClick={undoLastInboxAction}
+                  disabled={inboxBusyId === 'undo-last'}
+                >
+                  Undo
+                </button>
+              )}
+            </div>
+          </div>
+        )}
+      </section>
+    );
+  };
+
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
@@ -3654,6 +3885,13 @@
                     </div>
                   </div>
                   <div className="finish-now-actions">
+                    <button
+                      className="secondary-action"
+                      type="button"
+                      onClick={() => setActiveVoiceContextId(String(issue.affected_entity_id))}
+                    >
+                      {activeVoiceContextId === String(issue.affected_entity_id) ? 'Voice target set' : 'Arm voice'}
+                    </button>
                     {(readinessDemoEnabled || snapshotDemoEnabled) ? (
                       <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
                         Mark fixed
@@ -4391,6 +4629,7 @@
         </button>
       </section>
       {connectionError && <div className="alert alert-danger">{connectionError}</div>}
+      {renderVoiceContextBanner()}
 
       {loading ? (
         <div className="text-center py-5">Loading...</div>
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..6eb469fcc7f023f8545d9e952a5823fb8c47a88d
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});
diff --git a/workstream/200_inprogress/codex/20260305_192700_bizpa_mvp_pivot_phase06_voice_micro_decisions.md b/workstream/200_inprogress/codex/20260305_192700_bizpa_mvp_pivot_phase06_voice_micro_decisions.md
index cf85796e760112547a34bd77ea2a0f0a86fd3219..c02d5e36a241a1c98d7e97bf7c319cce1837c2ce
--- a/workstream/200_inprogress/codex/20260305_192700_bizpa_mvp_pivot_phase06_voice_micro_decisions.md
+++ b/workstream/200_inprogress/codex/20260305_192700_bizpa_mvp_pivot_phase06_voice_micro_decisions.md
@@ -1,43 +1,136 @@
-# Source
-- Derived from backlog: `C:\Users\edebe\eds\workstream\000_backlog\mvp_prd_quarterly_export_10min.md`
-- Decomposed from checklist: `C:\Users\edebe\eds\workstream\300_complete\20260305_191657_bizpa_mvp_quarterly_pivot_full_delivery_checklist.md`
-
-# Task Summary
-Constrain voice intents to PRD micro-decisions and bind safely to inbox/finish-now context with confirmation chip + undo.
-
-# Context
-- Affected area: voice controller/parser and web/mobile voice UX
-
-# Plan
-- [ ] 1. Implement phase scope in code and configuration.
-  - [ ] Test: Execute phase-specific implementation checks and verify expected behavior change.
-  - [ ] Evidence: pending
-- [ ] 2. Add/adjust automated tests for this phase.
-  - [ ] Test: Run targeted unit/integration tests for new behavior and ensure green results.
-  - [ ] Evidence: pending
-- [ ] 3. Validate against PRD acceptance criteria impacted by this phase.
-  - [ ] Test: Run intent contract tests for supported utterances and verify each action is applied, confirmed, and undoable.
-  - [ ] Evidence: pending
-- [ ] 4. Update technical notes and rollout caveats for downstream phases.
-  - [ ] Test: Confirm lifecycle doc lists changed files, commands run, and known risks.
-  - [ ] Evidence: pending
-
-# Implementation Log
-- 2026-03-05 Created decomposed task file from approved MVP pivot checklist.
-
-# Changes Made
-- Task file created; implementation not started.
-
-# Validation
-- Pending.
-
-# Risks/Notes
-- Must follow strict sequential completion and evidence capture before starting dependent phase.
-
-# Completion Status
-- Todo.
-
-
-## Retry History
-Retry-Count: 1
-- Retry scheduled at 2026-03-18 17:21:29
+# Source
+- Derived from backlog: `C:\Users\edebe\eds\workstream\000_epic\20260305_185316_mvp_prd_quarterly_export_10min.md`
+- Decomposed from checklist: `C:\Users\edebe\eds\workstream\300_complete\20260305_191657_bizpa_mvp_quarterly_pivot_full_delivery_checklist.md`
+
+# Task Summary
+Constrain voice intents to PRD micro-decisions and bind them safely to inbox/finish-now context with a visible confirmation chip and undo-compatible triage flow.
+
+# Context
+- Affected backend area: `C:\Users\edebe\eds\bizPA\backend\src\controllers\voiceController.js`, `C:\Users\edebe\eds\bizPA\backend\src\controllers\inboxController.js`
+- New backend support: `C:\Users\edebe\eds\bizPA\backend\src\services\inboxClassificationService.js`, `C:\Users\edebe\eds\bizPA\backend\verify_voice_micro_decisions.js`
+- Affected frontend area: `C:\Users\edebe\eds\bizPA\frontend\src\App.jsx`
+- New frontend support: `C:\Users\edebe\eds\bizPA\frontend\src\voiceMicroDecisions.js`, `C:\Users\edebe\eds\bizPA\frontend\src\voiceMicroDecisions.test.js`
+
+# Dependency
+`C:\Users\edebe\eds\workstream\300_complete\20260305_191657_bizpa_mvp_quarterly_pivot_full_delivery_checklist.md`
+
+# Plan
+- [x] 1. Implement phase scope in code and configuration.
+  - [x] Test: Execute the updated voice micro-decision flow against backend verification and confirm context-bound classification updates now use audited inbox persistence with undo metadata.
+  - [x] Evidence: `bizPA/backend/src/services/inboxClassificationService.js` now centralizes audited classification writes; `bizPA/backend/src/controllers/voiceController.js` now requires explicit micro-decision context and returns context binding plus undo metadata; `bizPA/frontend/src/App.jsx` now binds voice triage to an explicitly selected inbox/finish-now card and shows a confirmation chip with undo/edit actions.
+- [x] 2. Add/adjust automated tests for this phase.
+  - [x] Test: Run targeted backend and frontend tests for the new behavior and ensure green results.
+  - [x] Evidence: `npm run verify:voice-micro-decisions` passed in `bizPA/backend`; `npx react-scripts test --watchAll=false --runInBand --runTestsByPath src/voiceMicroDecisions.test.js src/quarterReadiness.test.js` passed in `bizPA/frontend`.
+- [x] 3. Validate against PRD acceptance criteria impacted by this phase.
+  - [x] Test: Run intent contract tests for supported utterances and verify each action is context-safe, confirmed, and undo-compatible for inbox triage actions; confirm frontend production build compiles with the new voice UX.
+  - [x] Evidence: Backend verifier output ended with `voice_micro_decisions_ok`, `context_guardrails_ok=true`, `audited_classification_updates_ok=true`, and `safe_context_binding_ok=true`; frontend `npm run build` compiled successfully.
+- [x] 4. Update technical notes and rollout caveats for downstream phases.
+  - [x] Test: Confirm this lifecycle doc lists changed files, commands run, known risks, and the pending user-verification gate for the visible inbox voice flow.
+  - [x] Evidence: Lifecycle file rewritten on 2026-03-19 with normalized evidence, validation results, and an explicit user-verification request/state.
+
+# Evidence
+Objective-Delivery-Coverage: 90%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `C:\Users\edebe\eds\bizPA\frontend\src\App.jsx`
+  - Objective-Proved: The mobile/web inbox and quarter screens now require an explicit finish-now selection before voice triage, show the active voice target, and render a confirmation chip with edit/undo affordances after a voice micro-decision.
+  - Status: captured
+- Evidence-Type: test_output
+  - Artifact: `npm run verify:voice-micro-decisions` output: `voice_micro_decisions_ok`, `context_guardrails_ok=true`, `audited_classification_updates_ok=true`, `attach_receipt_clarification_ok=true`, `match_selection_ok=true`, `safe_context_binding_ok=true`, `validation_errors_ok=true`
+  - Objective-Proved: Supported utterances are constrained to the micro-decision contract, require safe context binding, and return the expected confirmation/undo contract while preserving audited inbox behavior.
+  - Status: captured
+- Evidence-Type: test_output
+  - Artifact: `npx react-scripts test --watchAll=false --runInBand --runTestsByPath src/voiceMicroDecisions.test.js src/quarterReadiness.test.js` output: `PASS src/voiceMicroDecisions.test.js`, `PASS src/quarterReadiness.test.js`
+  - Objective-Proved: The frontend request-routing and confirmation-chip helper logic behaves correctly for selection enforcement, context routing, and chip aggregation.
+  - Status: captured
+- Evidence-Type: test_output
+  - Artifact: `npm run build` in `C:\Users\edebe\eds\bizPA\frontend` output: `Compiled successfully.`
+  - Objective-Proved: The user-visible voice triage UI changes compile successfully in the production frontend build.
+  - Status: captured
+- Evidence-Type: diff
+  - Artifact: Updated `C:\Users\edebe\eds\bizPA\backend\src\controllers\voiceController.js`, `C:\Users\edebe\eds\bizPA\backend\src\controllers\inboxController.js`, `C:\Users\edebe\eds\bizPA\frontend\src\App.jsx`; added `C:\Users\edebe\eds\bizPA\backend\src\services\inboxClassificationService.js`, `C:\Users\edebe\eds\bizPA\frontend\src\voiceMicroDecisions.js`, and `C:\Users\edebe\eds\bizPA\frontend\src\voiceMicroDecisions.test.js`
+  - Objective-Proved: The workspace contains the backend and frontend implementation required for safe, undo-compatible voice micro-decisions.
+  - Status: captured
+- Evidence-Type: manual_verification
+  - Artifact: User verification requested on 2026-03-19 for the visible inbox/quarter voice flow: select a blocker, run `Category: Travel` / `Business` / `Split 70%`, confirm the chip appears, and verify Undo reverses the last triage action.
+  - Objective-Proved: Final confirmation from an operator is still required for the visible voice UX path.
+  - Status: planned
+
+# Implementation Log
+- 2026-03-05 Created decomposed task file from the approved MVP pivot checklist.
+- 2026-03-19 Re-read the workstream lifecycle skill and reconciled the stale in-progress task against the current backend/frontend workspace.
+- 2026-03-19 Identified that voice micro-decisions were bypassing the inbox audit path, which meant `/api/v1/inbox/undo-last` could not reliably undo voice-applied triage.
+- 2026-03-19 Added `bizPA/backend/src/services/inboxClassificationService.js` to centralize classification persistence, audit logging, and readiness recalculation.
+- 2026-03-19 Updated `bizPA/backend/src/controllers/inboxController.js` to use the shared audited classification helper.
+- 2026-03-19 Updated `bizPA/backend/src/controllers/voiceController.js` so context-bound voice category/business/personal/split decisions use the shared audited classification helper and return explicit context-binding plus undo metadata.
+- 2026-03-19 Expanded `bizPA/backend/verify_voice_micro_decisions.js` to assert context guardrails, audited classification persistence, preserved field state across incremental voice commands, and safe binding behavior.
+- 2026-03-19 Added `bizPA/frontend/src/voiceMicroDecisions.js` and `bizPA/frontend/src/voiceMicroDecisions.test.js` to isolate and verify the client-side routing/confirmation-chip logic.
+- 2026-03-19 Updated `bizPA/frontend/src/App.jsx` to require an explicit active inbox/finish-now selection for voice triage, render the active voice target banner, and surface a combined confirmation chip with edit/undo controls.
+- 2026-03-19 Ran backend verification, frontend targeted tests, and a production frontend build.
+- 2026-03-19 Requested user verification for the user-visible inbox/quarter voice flow and left the task active pending that confirmation.
+
+# Changes Made
+- `C:\Users\edebe\eds\bizPA\backend\src\services\inboxClassificationService.js`
+  - Added shared audited classification upsert logic that preserves existing fields, writes `transaction_audit_log`, and recalculates readiness.
+- `C:\Users\edebe\eds\bizPA\backend\src\controllers\inboxController.js`
+  - Switched inbox classification writes to the shared service so UI and voice triage use the same persistence/audit path.
+- `C:\Users\edebe\eds\bizPA\backend\src\controllers\voiceController.js`
+  - Enforced explicit micro-decision context, bound triage commands to the selected transaction/evidence context, and returned undo/context metadata for the confirmation chip flow.
+- `C:\Users\edebe\eds\bizPA\backend\verify_voice_micro_decisions.js`
+  - Added assertions for context guardrails, safe binding, audited triage writes, and clarification/error handling.
+- `C:\Users\edebe\eds\bizPA\frontend\src\voiceMicroDecisions.js`
+  - Added pure helpers for routing voice to `/voice/micro-decision`, enforcing explicit selection, and aggregating confirmation-chip actions.
+- `C:\Users\edebe\eds\bizPA\frontend\src\voiceMicroDecisions.test.js`
+  - Added tests covering missing selection handling, finish-now request construction, and chip aggregation/reset behavior.
+- `C:\Users\edebe\eds\bizPA\frontend\src\App.jsx`
+  - Added active voice target selection state for inbox and quarter cards.
+  - Routed voice on inbox/quarter tabs through the micro-decision endpoint only when a blocker is explicitly selected.
+  - Added the visible voice context banner and combined confirmation chip with edit/undo actions.
+  - Preserved the legacy `/voice/process` path for non-triage tabs.
+
+# Validation
+- 2026-03-19 16:59:44+00:00: `npm run verify:voice-micro-decisions`
+  - Result: pass
+  - Key output:
+    - `voice_micro_decisions_ok`
+    - `context_guardrails_ok=true`
+    - `audited_classification_updates_ok=true`
+    - `attach_receipt_clarification_ok=true`
+    - `match_selection_ok=true`
+    - `safe_context_binding_ok=true`
+    - `validation_errors_ok=true`
+- 2026-03-19 16:59:44+00:00: `npm run verify:voice-capture`
+  - Result: pass
+  - Key output:
+    - `voice_capture_parser_ok`
+    - `entity_mapping_ok=true`
+    - `low_confidence_review_ok=true`
+    - `composition_contract_ok=true`
+    - `query_period_filters_ok=true`
+    - `query_exact_date_filters_ok=true`
+- 2026-03-19 16:59:44+00:00: initial frontend test attempt via npm wrapper
+  - Result: blocked by environment
+  - Key output: `Error: spawn EPERM`
+  - Note: `react-scripts` attempted worker-process spawning inside the sandbox when invoked via `npm test`.
+- 2026-03-19 16:59:44+00:00: `npx react-scripts test --watchAll=false --runInBand --runTestsByPath src/voiceMicroDecisions.test.js src/quarterReadiness.test.js`
+  - Result: pass
+  - Key output:
+    - `PASS src/voiceMicroDecisions.test.js`
+    - `PASS src/quarterReadiness.test.js`
+    - `Tests: 7 passed, 7 total`
+- 2026-03-19 16:59:44+00:00: `npm run build`
+  - Result: pass
+  - Key output:
+    - `Compiled successfully.`
+- 2026-03-19 16:59:44+00:00: user verification request recorded
+  - Result: pending
+  - Key output: Please verify that selecting a finish-now blocker, speaking `Category: Travel` / `Business` / `Split 70%`, and tapping Undo behaves correctly in the visible inbox/quarter flow.
+
+# Risks/Notes
+- The visible voice triage flow is now selection-gated on inbox and quarter screens. If future UI changes remove or obscure the selected voice target, the frontend helper tests should be expanded before rollout.
+- Evidence commands remain context-bound to `evidence` rather than the inbox transaction flow. This task hardened their context safety and confirmation payloads, but it did not add a visible evidence-selection UI in the current frontend.
+- The direct `npm test` wrapper is not reliable in this sandbox because Jest worker spawning can fail with `EPERM`; use the documented `npx react-scripts test --runInBand ...` form for deterministic local verification here.
+- A prior duplicate lifecycle file exists under `workstream/300_complete`; this in-progress record is the active reconciliation file requested by the user and remains open until user verification is captured.
+
+# Completion Status
+- Awaiting user verification (2026-03-19 16:59:44+00:00)

file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..e6d93b4bc1cd6960ce27238cfb7e90c3322f41e0
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,6 +10,36 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+const {
+  parseVoiceMicroDecision,
+  validateMicroDecisionContext,
+} = require('../services/voiceMicroDecisionService');
+
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undoable: undoSupported,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
 
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
@@ -267,76 +297,177 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
-  const text = String(transcript).toLowerCase().trim();
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_kind = null,
+    context_scope = null,
+  } = req.body || {};
+  const rawTranscript = String(transcript).trim();
+  const text = rawTranscript.toLowerCase();
+  const normalizedContext = context_kind ?? context_scope;
+  const parsedCommand = parseVoiceMicroDecision(rawTranscript);
+  const contextValidation = validateMicroDecisionContext(parsedCommand, normalizedContext);
+  const normalizedContextScope = contextValidation.ok
+    ? contextValidation.context_kind
+    : (normalizedContext === null || normalizedContext === undefined
+      ? null
+      : String(normalizedContext).toLowerCase().replace(/-/g, '_'));
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
+  if (!contextValidation.ok) return res.status(contextValidation.statusCode).json({ error: contextValidation.error });
 
   try {
-    if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
-      const categoryName = text.replace('category:', '').trim();
-      const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+    if (parsedCommand.type === 'category') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: parsedCommand.category_code,
+            category_name: parsedCommand.category_name,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'category', label: parsedCommand.category_name }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
-      const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+    if (parsedCommand.type === 'business_personal') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const bp = parsedCommand.business_personal;
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    const splitMatch = text.match(/split\s+(\d{1,3})%?/);
-    if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
-      const pct = Number(splitMatch[1]);
+    if (parsedCommand.type === 'split') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const pct = Number(parsedCommand.split_business_pct);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+    if (parsedCommand.type === 'attach_receipt') {
+      if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for attach receipt commands.' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          parsed_command: parsedCommand,
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
-    if (['match first', 'match second', 'match third'].includes(text)) {
+    if (parsedCommand.type === 'match_rank') {
       if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for match commands.' });
-      const index = text === 'match first' ? 1 : text === 'match second' ? 2 : 3;
+      const index = parsedCommand.match_rank;
       const suggestions = await db.query(
         `
         WITH ev AS (
@@ -364,10 +495,24 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          bank_txn_id: chosen,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text === 'no match') {
+    if (parsedCommand.type === 'no_match') {
       if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for no match.' });
       await db.query(
         `
@@ -376,7 +521,20 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/backend/verify_voice_micro_decisions.js b/backend/verify_voice_micro_decisions.js
index 25d9ab02050aac37a5d38aca5c6f0154de9bb787..15c93efa65f133ec1a59d827ecbfa29a95cf4e6d
--- a/backend/verify_voice_micro_decisions.js
+++ b/backend/verify_voice_micro_decisions.js
@@ -9,16 +9,70 @@
       { bank_txn_id: 'txn-2' },
       { bank_txn_id: 'txn-3' }
     ];
+    this.classifications = new Map();
+    this.auditEntries = [];
   }
 
-  async query(text, params) {
+  get pool() {
+    return {
+      connect: async () => ({
+        query: this.query.bind(this),
+        release() {},
+      }),
+    };
+  }
+
+  async query(text, params = []) {
     const sql = text.replace(/\s+/g, ' ').trim();
     this.calls.push({ sql, params });
 
+    if (sql === 'BEGIN' || sql === 'COMMIT' || sql === 'ROLLBACK') {
+      return { rows: [] };
+    }
+
+    if (sql.includes('SELECT * FROM transaction_classifications')) {
+      const existing = this.classifications.get(params[0]);
+      return { rows: existing ? [existing] : [] };
+    }
+
+    if (sql.includes('INSERT INTO transaction_classifications')) {
+      const next = {
+        user_id: params[0],
+        bank_txn_id: params[1],
+        category_code: params[2],
+        category_name: params[3],
+        business_personal: params[4],
+        is_split: params[5],
+        split_business_pct: params[6],
+        confidence: params[7],
+        source: params[8],
+      };
+      this.classifications.set(params[1], next);
+      return { rows: [next] };
+    }
+
+    if (sql.includes('INSERT INTO transaction_audit_log')) {
+      this.auditEntries.push({
+        bank_txn_id: params[1],
+        previous_value: params[4],
+        new_value: params[5],
+        change_source: params[6],
+      });
+      return { rows: [] };
+    }
+
+    if (sql.includes('SELECT txn_date FROM bank_transactions')) {
+      return { rows: [{ txn_date: '2026-03-12' }] };
+    }
+
     if (sql.includes('FROM bank_transactions bt CROSS JOIN ev')) {
       return { rows: this.matchSuggestions };
     }
 
+    if (sql.includes('INSERT INTO evidence_links')) {
+      return { rows: [] };
+    }
+
     return { rows: [] };
   }
 }
@@ -69,45 +123,79 @@
   const mockDb = new MockDb();
   const controller = loadController(mockDb);
 
+  const missingContext = await invoke(controller, {
+    transcript: 'Business',
+    bank_txn_id: 'txn-123'
+  });
+  assert.strictEqual(missingContext.statusCode, 400);
+  assert.strictEqual(missingContext.body.error, 'context_kind is required for MVP voice micro-decisions.');
+
   const category = await invoke(controller, {
     transcript: 'Category: Travel',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(category.statusCode, 200);
-  assert.strictEqual(category.body.confirmation_chip, 'Applied: travel');
-  assert(
-    mockDb.calls.some((call) => call.sql.includes('INSERT INTO transaction_classifications') && call.params[2] === 'TRAVEL' && call.params[3] === 'travel'),
-    'Expected category command to persist manual classification'
-  );
+  assert.strictEqual(category.body.confirmation_chip, 'Applied: Travel');
+  assert.strictEqual(category.body.undo.supported, true);
+  assert.deepStrictEqual(category.body.context_binding, {
+    scope: 'inbox',
+    target_type: 'bank_transaction',
+    target_id: 'txn-123'
+  });
+  assert.strictEqual(mockDb.classifications.get('txn-123').category_code, 'TRAVEL');
+  assert.strictEqual(mockDb.auditEntries.at(-1).change_source, 'voice');
 
   const business = await invoke(controller, {
     transcript: 'Business',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(business.statusCode, 200);
   assert.strictEqual(business.body.confirmation_chip, 'Applied: BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').category_code, 'TRAVEL');
+  assert.strictEqual(mockDb.classifications.get('txn-123').business_personal, 'BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').is_split, false);
 
   const split = await invoke(controller, {
     transcript: 'Split 70%',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(split.statusCode, 200);
   assert.strictEqual(split.body.confirmation_chip, 'Applied: Split 70%');
+  assert.strictEqual(split.body.undo.supported, true);
+  assert.strictEqual(mockDb.classifications.get('txn-123').business_personal, 'BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').is_split, true);
+  assert.strictEqual(mockDb.classifications.get('txn-123').split_business_pct, 70);
 
   const attachReceipt = await invoke(controller, {
     transcript: 'Attach receipt',
-    bank_txn_id: 'txn-123'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(attachReceipt.statusCode, 200);
   assert.strictEqual(attachReceipt.body.action_status, 'clarification_needed');
   assert.strictEqual(attachReceipt.body.confirmation_chip, 'Attach receipt requested');
+  assert.strictEqual(attachReceipt.body.undo.supported, false);
+  assert.deepStrictEqual(attachReceipt.body.context_binding, {
+    scope: 'evidence',
+    target_type: 'evidence',
+    target_id: 'evidence-1'
+  });
 
   const matchSecond = await invoke(controller, {
     transcript: 'Match second',
-    evidence_id: 'evidence-1'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(matchSecond.statusCode, 200);
   assert.strictEqual(matchSecond.body.confirmation_chip, 'Applied: Match 2');
+  assert.deepStrictEqual(matchSecond.body.context_binding, {
+    scope: 'evidence',
+    target_type: 'evidence',
+    target_id: 'evidence-1'
+  });
   assert(
     mockDb.calls.some((call) => call.sql.includes('INSERT INTO evidence_links') && call.params[1] === 'evidence-1' && call.params[2] === 'txn-2'),
     'Expected match command to persist selected evidence link'
@@ -115,29 +203,41 @@
 
   const noMatch = await invoke(controller, {
     transcript: 'No match',
-    evidence_id: 'evidence-1'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(noMatch.statusCode, 200);
   assert.strictEqual(noMatch.body.confirmation_chip, 'Applied: No match');
 
   const invalidSplit = await invoke(controller, {
     transcript: 'Split 140%',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(invalidSplit.statusCode, 400);
   assert.strictEqual(invalidSplit.body.error, 'Split percentage must be 0-100.');
 
+  const unsafeContext = await invoke(controller, {
+    transcript: 'Category: Travel',
+    bank_txn_id: 'txn-123',
+    context_kind: 'evidence'
+  });
+  assert.strictEqual(unsafeContext.statusCode, 409);
+  assert.strictEqual(unsafeContext.body.error, 'Command "category" is not allowed for evidence context.');
+
   const unsupported = await invoke(controller, {
-    transcript: 'Archive this forever'
+    transcript: 'Archive this forever',
+    context_kind: 'inbox'
   });
   assert.strictEqual(unsupported.statusCode, 400);
   assert.strictEqual(unsupported.body.error, 'Unsupported MVP voice micro-decision command.');
 
   console.log('voice_micro_decisions_ok');
-  console.log('category_business_split_ok=true');
+  console.log('context_guardrails_ok=true');
+  console.log('audited_classification_updates_ok=true');
   console.log('attach_receipt_clarification_ok=true');
   console.log('match_selection_ok=true');
-  console.log('no_match_ok=true');
+  console.log('safe_context_binding_ok=true');
   console.log('validation_errors_ok=true');
 }
 
diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index 9b145fadac9123e3fa01b12af6ef2b9916dc11de..590be3d00c08cb02d6139abdd4d1a0ee41f9de7b
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -50,6 +50,16 @@
   buildQuarterUiState,
   deriveResolvedIssueIdsFromSearchParams,
 } from './quarterReadiness';
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+import {
+  applyInboxVoiceCommandToDraft,
+  buildInboxVoiceChip,
+  parseInboxVoiceCommand,
+} from './voiceMicroDecision';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -427,6 +437,13 @@
   .date-group-count { border-radius: 999px; padding: 4px 9px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.72rem; color: var(--text-muted); }
   .date-group-body { padding: 0 12px 12px; }
   .voice-preview { background: rgba(0,0,0,0.82); color: white; padding: 8px 15px; border-radius: 15px; margin-bottom: 8px; font-size: 0.72rem; max-width: 240px; text-align: right; }
+  .voice-context-banner { display: grid; gap: 10px; padding: 14px 16px; margin-bottom: 16px; border-radius: 18px; background: var(--bg-card); border: 1px solid var(--border); box-shadow: var(--shadow); }
+  .voice-context-row, .voice-chip-actions, .voice-chip-labels { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
+  .voice-context-pill, .voice-action-pill { border-radius: 999px; padding: 6px 10px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.74rem; font-weight: 700; }
+  .voice-context-pill.active { background: rgba(13,92,99,0.12); color: var(--primary-strong); border-color: rgba(13,92,99,0.24); }
+  .voice-micro-chip { display: grid; gap: 10px; padding: 14px 16px; border-radius: 18px; background: rgba(13,92,99,0.08); border: 1px solid rgba(13,92,99,0.18); }
+  .voice-micro-chip strong { display: block; }
+  .inbox-action-card.voice-target { border-color: rgba(13,92,99,0.36); box-shadow: 0 0 0 2px rgba(13,92,99,0.12), var(--shadow); }
   .spinner { animation: rotate 2s linear infinite; }
   .capture-grid { display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(320px, 0.95fr); gap: 18px; align-items: start; }
   .command-centre { display: grid; gap: 18px; }
@@ -1318,6 +1335,8 @@
     && searchParams?.get('snapshotDemo') === '1';
   const inboxDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('inboxDemo') === '1';
+  const voiceDemoCommand = searchParams?.get('voiceDemoCommand') || '';
+  const voiceDemoTarget = searchParams?.get('voiceDemoTarget') || '';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1382,9 +1401,12 @@
   const [inboxDrafts, setInboxDrafts] = useState({});
   const [inboxBusyId, setInboxBusyId] = useState('');
   const [inboxMessage, setInboxMessage] = useState(null);
+  const [activeVoiceContextId, setActiveVoiceContextId] = useState(voiceDemoTarget);
+  const [voiceMicroDecisionChip, setVoiceMicroDecisionChip] = useState(null);
   const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
+  const voiceDemoSeedRef = useRef('');
   const quarterStart = '2026-01-01';
   const quarterEnd = '2026-03-31';
 
@@ -1606,10 +1628,80 @@
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
   const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const activeVoiceCard = findVoiceContextCard(inboxCards, activeVoiceContextId);
   const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
   const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
   const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
+  const applyVoiceMicroDecisionLocally = (card, parsedCommand, responseLike = {}) => {
+    if (!card || !parsedCommand) {
+      return;
+    }
+
+    const nextDraft = applyInboxVoiceCommandToDraft(inboxDrafts[card.id] || buildInboxDraft(card), parsedCommand);
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: nextDraft,
+    }));
+
+    if (parsedCommand.type === 'category') {
+      syncInboxCardLocally(card.id, {
+        category_code: nextDraft.category_code,
+        category_name: nextDraft.category_name,
+      });
+    } else if (parsedCommand.type === 'business_personal') {
+      syncInboxCardLocally(card.id, {
+        business_personal: nextDraft.business_personal,
+        is_split: false,
+        split_business_pct: null,
+      });
+    } else if (parsedCommand.type === 'split') {
+      syncInboxCardLocally(card.id, {
+        business_personal: 'BUSINESS',
+        is_split: true,
+        split_business_pct: Number(parsedCommand.split_business_pct),
+      });
+    }
+
+    setVoiceMicroDecisionChip((previous) => reduceVoiceMicroDecisionChip(previous, {
+      ...responseLike,
+      action_status: responseLike.action_status || 'execute',
+      confirmation_chip: responseLike.confirmation_chip || parsedCommand.confirmation_chip,
+      applied_actions: responseLike.applied_actions || [{ label: parsedCommand.confirmation_chip.replace(/^Applied:\s*/, '') }],
+      undo: responseLike.undo || { supported: true },
+    }, card));
+  };
+
+  useEffect(() => {
+    if (!activeVoiceContextId) {
+      return;
+    }
+    if (!findVoiceContextCard(inboxCards, activeVoiceContextId)) {
+      setActiveVoiceContextId('');
+    }
+  }, [activeVoiceContextId, inboxCards]);
+
+  useEffect(() => {
+    if (!inboxDemoEnabled || !voiceDemoCommand || !activeVoiceCard) {
+      return;
+    }
+    const seedKey = `${activeVoiceCard.id}:${voiceDemoCommand}`;
+    if (voiceDemoSeedRef.current === seedKey) {
+      return;
+    }
+    const parsedCommand = parseInboxVoiceCommand(voiceDemoCommand);
+    if (!parsedCommand) {
+      return;
+    }
+    voiceDemoSeedRef.current = seedKey;
+    applyVoiceMicroDecisionLocally(activeVoiceCard, parsedCommand, {
+      action_status: 'execute',
+      confirmation_chip: parsedCommand.confirmation_chip,
+      applied_actions: [{ label: parsedCommand.confirmation_chip.replace(/^Applied:\s*/, '') }],
+      undo: { supported: true },
+    });
+  }, [activeVoiceCard, inboxDemoEnabled, voiceDemoCommand]);
+
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
   const canManageGovernance = hasPermission(currentUser, PERMISSIONS.GOVERNANCE_MANAGE);
@@ -1802,15 +1894,18 @@
     clearInboxMessage();
     try {
       setInboxBusyId('undo-last');
+      setVoiceMicroDecisionChip(null);
       if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxDrafts({});
+        setVoiceMicroDecisionChip(null);
         setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
         return;
       }
       await axios.post(`${API_BASE_URL}/inbox/undo-last`);
       await fetchAllData();
+      setVoiceMicroDecisionChip(null);
       setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
     } catch (error) {
       setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
@@ -1991,22 +2086,78 @@
     const cleanText = text.replace(/┬ú/g, 'GBP ');
     try {
       setVoiceStatus('processing');
-      const response = await axios.post(`${API_BASE_URL}/voice/process`, {
+      const microDecisionRequest = buildVoiceMicroDecisionRequest({
+        currentTab,
         transcript: cleanText,
-        device_id: 'mobile-v139',
-        current_date: new Date().toISOString(),
+        activeVoiceContextId,
+        inboxCards,
       });
-      if (response.data.confirmation_text && window.SpeechSynthesisUtterance) {
+
+      if (microDecisionRequest?.error) {
+        setInboxMessage({ type: 'error', text: microDecisionRequest.error });
+        setCurrentTab('inbox');
+        return;
+      }
+
+      let response;
+      if (microDecisionRequest) {
+        if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+          const parsedCommand = parseInboxVoiceCommand(cleanText);
+          if (!parsedCommand) {
+            throw new Error('Only Category, Business, Personal, and Split {n}% are available in the local voice triage demo.');
+          }
+          applyVoiceMicroDecisionLocally(microDecisionRequest.card, parsedCommand, {
+            action_status: 'execute',
+            confirmation_chip: parsedCommand.confirmation_chip,
+            applied_actions: [{ label: parsedCommand.confirmation_chip.replace(/^Applied:\s*/, '') }],
+            undo: { supported: true },
+          });
+          response = { data: buildInboxVoiceChip({
+            action_status: 'execute',
+            confirmation_chip: parsedCommand.confirmation_chip,
+            undoable: true,
+            bank_txn_id: microDecisionRequest.card.id,
+          }, microDecisionRequest.card) };
+          response.data.confirmation_chip = parsedCommand.confirmation_chip;
+        } else {
+          response = await axios.post(`${API_BASE_URL}${microDecisionRequest.endpoint}`, microDecisionRequest.payload);
+        }
+      } else {
+        response = await axios.post(`${API_BASE_URL}/voice/process`, {
+          transcript: cleanText,
+          device_id: 'mobile-v139',
+          current_date: new Date().toISOString(),
+        });
+      }
+      const spokenConfirmation = response.data.confirmation_chip || response.data.confirmation_text;
+      if (spokenConfirmation && window.SpeechSynthesisUtterance) {
         try {
-          const utterance = new window.SpeechSynthesisUtterance(response.data.confirmation_text);
+          const utterance = new window.SpeechSynthesisUtterance(spokenConfirmation);
           window.speechSynthesis.speak(utterance);
         } catch (_) {
         }
       }
-      executeVoiceAction(response.data);
-      fetchAllData();
+      if (microDecisionRequest) {
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          setVoiceMicroDecisionChip((previous) => reduceVoiceMicroDecisionChip(previous, response.data, microDecisionRequest.card));
+          setInboxMessage({
+            type: 'success',
+            text: response.data.confirmation_chip || 'Voice triage action applied.',
+          });
+        }
+      } else {
+        executeVoiceAction(response.data);
+      }
+      if (!(microDecisionRequest && (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled))) {
+        await fetchAllData();
+      }
     } catch (error) {
-      alert('AI Error');
+      const message = error?.response?.data?.error || error?.message || 'AI Error';
+      if (currentTab === 'inbox' || currentTab === 'quarter') {
+        setInboxMessage({ type: 'error', text: message });
+      } else {
+        alert(message);
+      }
     } finally {
       setVoiceStatus('idle');
       setIsListening(false);
@@ -2894,7 +3045,11 @@
             const saving = inboxBusyId === card.id;
             const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
             return (
-              <section key={card.id} className="inbox-action-card">
+              <section
+                key={card.id}
+                className={`inbox-action-card ${activeVoiceContextId === card.id ? 'voice-target' : ''}`}
+                onClick={() => setActiveVoiceContextId(card.id)}
+              >
                 <div className="inbox-card-top">
                   <div>
                     <strong>{card.merchant}</strong>
@@ -2914,6 +3069,9 @@
                       {label}
                     </span>
                   ))}
+                  {activeVoiceContextId === card.id && (
+                    <span className="voice-context-pill active">Voice target</span>
+                  )}
                   {card.suggested_category?.category_name && (
                     <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
                       <CheckCircle2 size={14} />
@@ -3357,6 +3515,9 @@
   };
 
   const openReadinessWorkflow = (issue) => {
+    if (issue?.affected_entity_id) {
+      setActiveVoiceContextId(String(issue.affected_entity_id));
+    }
     if (issue?.resolution_target?.workflow === '/inbox/finish-now') {
       setCurrentTab('inbox');
       return;
@@ -3364,6 +3525,76 @@
     setCurrentTab('inbox');
   };
 
+  const renderVoiceContextBanner = () => {
+    if (!(currentTab === 'inbox' || currentTab === 'quarter')) {
+      return null;
+    }
+
+    return (
+      <section className="voice-context-banner">
+        <div>
+          <strong>Voice triage</strong>
+          <div className="small-muted">
+            Use the mic against one selected finish-now item only. Supported commands here are category, business, personal, split n%, and attach receipt.
+          </div>
+        </div>
+        <div className="voice-context-row">
+          <span className={`voice-context-pill ${activeVoiceCard ? 'active' : ''}`}>
+            {activeVoiceCard
+              ? `Target: ${activeVoiceCard.merchant} (${activeVoiceCard.id})`
+              : 'Select a blocker card to arm voice triage'}
+          </span>
+          {activeVoiceCard && (
+            <button className="secondary-action" type="button" onClick={() => setCurrentTab('inbox')}>
+              Edit target
+            </button>
+          )}
+        </div>
+        {voiceMicroDecisionChip && (
+          <div className="voice-micro-chip">
+            <div>
+              <strong>{voiceMicroDecisionChip.confirmationChip}</strong>
+              <div className="small-muted">
+                {voiceMicroDecisionChip.targetLabel} ({voiceMicroDecisionChip.targetId})
+              </div>
+            </div>
+            {voiceMicroDecisionChip.labels.length > 0 && (
+              <div className="voice-chip-labels">
+                {voiceMicroDecisionChip.labels.map((label) => (
+                  <span key={`${voiceMicroDecisionChip.targetId}-${label}`} className="voice-action-pill">
+                    {label}
+                  </span>
+                ))}
+              </div>
+            )}
+            <div className="voice-chip-actions">
+              <button
+                className="secondary-action"
+                type="button"
+                onClick={() => {
+                  setCurrentTab('inbox');
+                  setActiveVoiceContextId(voiceMicroDecisionChip.targetId);
+                }}
+              >
+                Edit
+              </button>
+              {voiceMicroDecisionChip.undoSupported && (
+                <button
+                  className="secondary-action"
+                  type="button"
+                  onClick={undoLastInboxAction}
+                  disabled={inboxBusyId === 'undo-last'}
+                >
+                  Undo
+                </button>
+              )}
+            </div>
+          </div>
+        )}
+      </section>
+    );
+  };
+
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
@@ -3654,6 +3885,13 @@
                     </div>
                   </div>
                   <div className="finish-now-actions">
+                    <button
+                      className="secondary-action"
+                      type="button"
+                      onClick={() => setActiveVoiceContextId(String(issue.affected_entity_id))}
+                    >
+                      {activeVoiceContextId === String(issue.affected_entity_id) ? 'Voice target set' : 'Arm voice'}
+                    </button>
                     {(readinessDemoEnabled || snapshotDemoEnabled) ? (
                       <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
                         Mark fixed
@@ -4391,6 +4629,7 @@
         </button>
       </section>
       {connectionError && <div className="alert alert-danger">{connectionError}</div>}
+      {renderVoiceContextBanner()}
 
       {loading ? (
         <div className="text-center py-5">Loading...</div>
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..6eb469fcc7f023f8545d9e952a5823fb8c47a88d
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});
diff --git a/workstream/200_inprogress/codex/20260305_192700_bizpa_mvp_pivot_phase06_voice_micro_decisions.md b/workstream/200_inprogress/codex/20260305_192700_bizpa_mvp_pivot_phase06_voice_micro_decisions.md
index cf85796e760112547a34bd77ea2a0f0a86fd3219..c02d5e36a241a1c98d7e97bf7c319cce1837c2ce
--- a/workstream/200_inprogress/codex/20260305_192700_bizpa_mvp_pivot_phase06_voice_micro_decisions.md
+++ b/workstream/200_inprogress/codex/20260305_192700_bizpa_mvp_pivot_phase06_voice_micro_decisions.md
@@ -1,43 +1,136 @@
-# Source
-- Derived from backlog: `C:\Users\edebe\eds\workstream\000_backlog\mvp_prd_quarterly_export_10min.md`
-- Decomposed from checklist: `C:\Users\edebe\eds\workstream\300_complete\20260305_191657_bizpa_mvp_quarterly_pivot_full_delivery_checklist.md`
-
-# Task Summary
-Constrain voice intents to PRD micro-decisions and bind safely to inbox/finish-now context with confirmation chip + undo.
-
-# Context
-- Affected area: voice controller/parser and web/mobile voice UX
-
-# Plan
-- [ ] 1. Implement phase scope in code and configuration.
-  - [ ] Test: Execute phase-specific implementation checks and verify expected behavior change.
-  - [ ] Evidence: pending
-- [ ] 2. Add/adjust automated tests for this phase.
-  - [ ] Test: Run targeted unit/integration tests for new behavior and ensure green results.
-  - [ ] Evidence: pending
-- [ ] 3. Validate against PRD acceptance criteria impacted by this phase.
-  - [ ] Test: Run intent contract tests for supported utterances and verify each action is applied, confirmed, and undoable.
-  - [ ] Evidence: pending
-- [ ] 4. Update technical notes and rollout caveats for downstream phases.
-  - [ ] Test: Confirm lifecycle doc lists changed files, commands run, and known risks.
-  - [ ] Evidence: pending
-
-# Implementation Log
-- 2026-03-05 Created decomposed task file from approved MVP pivot checklist.
-
-# Changes Made
-- Task file created; implementation not started.
-
-# Validation
-- Pending.
-
-# Risks/Notes
-- Must follow strict sequential completion and evidence capture before starting dependent phase.
-
-# Completion Status
-- Todo.
-
-
-## Retry History
-Retry-Count: 1
-- Retry scheduled at 2026-03-18 17:21:29
+# Source
+- Derived from backlog: `C:\Users\edebe\eds\workstream\000_epic\20260305_185316_mvp_prd_quarterly_export_10min.md`
+- Decomposed from checklist: `C:\Users\edebe\eds\workstream\300_complete\20260305_191657_bizpa_mvp_quarterly_pivot_full_delivery_checklist.md`
+
+# Task Summary
+Constrain voice intents to PRD micro-decisions and bind them safely to inbox/finish-now context with a visible confirmation chip and undo-compatible triage flow.
+
+# Context
+- Affected backend area: `C:\Users\edebe\eds\bizPA\backend\src\controllers\voiceController.js`, `C:\Users\edebe\eds\bizPA\backend\src\controllers\inboxController.js`
+- New backend support: `C:\Users\edebe\eds\bizPA\backend\src\services\inboxClassificationService.js`, `C:\Users\edebe\eds\bizPA\backend\verify_voice_micro_decisions.js`
+- Affected frontend area: `C:\Users\edebe\eds\bizPA\frontend\src\App.jsx`
+- New frontend support: `C:\Users\edebe\eds\bizPA\frontend\src\voiceMicroDecisions.js`, `C:\Users\edebe\eds\bizPA\frontend\src\voiceMicroDecisions.test.js`
+
+# Dependency
+`C:\Users\edebe\eds\workstream\300_complete\20260305_191657_bizpa_mvp_quarterly_pivot_full_delivery_checklist.md`
+
+# Plan
+- [x] 1. Implement phase scope in code and configuration.
+  - [x] Test: Execute the updated voice micro-decision flow against backend verification and confirm context-bound classification updates now use audited inbox persistence with undo metadata.
+  - [x] Evidence: `bizPA/backend/src/services/inboxClassificationService.js` now centralizes audited classification writes; `bizPA/backend/src/controllers/voiceController.js` now requires explicit micro-decision context and returns context binding plus undo metadata; `bizPA/frontend/src/App.jsx` now binds voice triage to an explicitly selected inbox/finish-now card and shows a confirmation chip with undo/edit actions.
+- [x] 2. Add/adjust automated tests for this phase.
+  - [x] Test: Run targeted backend and frontend tests for the new behavior and ensure green results.
+  - [x] Evidence: `npm run verify:voice-micro-decisions` passed in `bizPA/backend`; `npx react-scripts test --watchAll=false --runInBand --runTestsByPath src/voiceMicroDecisions.test.js src/quarterReadiness.test.js` passed in `bizPA/frontend`.
+- [x] 3. Validate against PRD acceptance criteria impacted by this phase.
+  - [x] Test: Run intent contract tests for supported utterances and verify each action is context-safe, confirmed, and undo-compatible for inbox triage actions; confirm frontend production build compiles with the new voice UX.
+  - [x] Evidence: Backend verifier output ended with `voice_micro_decisions_ok`, `context_guardrails_ok=true`, `audited_classification_updates_ok=true`, and `safe_context_binding_ok=true`; frontend `npm run build` compiled successfully.
+- [x] 4. Update technical notes and rollout caveats for downstream phases.
+  - [x] Test: Confirm this lifecycle doc lists changed files, commands run, known risks, and the pending user-verification gate for the visible inbox voice flow.
+  - [x] Evidence: Lifecycle file rewritten on 2026-03-19 with normalized evidence, validation results, and an explicit user-verification request/state.
+
+# Evidence
+Objective-Delivery-Coverage: 90%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `C:\Users\edebe\eds\bizPA\frontend\src\App.jsx`
+  - Objective-Proved: The mobile/web inbox and quarter screens now require an explicit finish-now selection before voice triage, show the active voice target, and render a confirmation chip with edit/undo affordances after a voice micro-decision.
+  - Status: captured
+- Evidence-Type: test_output
+  - Artifact: `npm run verify:voice-micro-decisions` output: `voice_micro_decisions_ok`, `context_guardrails_ok=true`, `audited_classification_updates_ok=true`, `attach_receipt_clarification_ok=true`, `match_selection_ok=true`, `safe_context_binding_ok=true`, `validation_errors_ok=true`
+  - Objective-Proved: Supported utterances are constrained to the micro-decision contract, require safe context binding, and return the expected confirmation/undo contract while preserving audited inbox behavior.
+  - Status: captured
+- Evidence-Type: test_output
+  - Artifact: `npx react-scripts test --watchAll=false --runInBand --runTestsByPath src/voiceMicroDecisions.test.js src/quarterReadiness.test.js` output: `PASS src/voiceMicroDecisions.test.js`, `PASS src/quarterReadiness.test.js`
+  - Objective-Proved: The frontend request-routing and confirmation-chip helper logic behaves correctly for selection enforcement, context routing, and chip aggregation.
+  - Status: captured
+- Evidence-Type: test_output
+  - Artifact: `npm run build` in `C:\Users\edebe\eds\bizPA\frontend` output: `Compiled successfully.`
+  - Objective-Proved: The user-visible voice triage UI changes compile successfully in the production frontend build.
+  - Status: captured
+- Evidence-Type: diff
+  - Artifact: Updated `C:\Users\edebe\eds\bizPA\backend\src\controllers\voiceController.js`, `C:\Users\edebe\eds\bizPA\backend\src\controllers\inboxController.js`, `C:\Users\edebe\eds\bizPA\frontend\src\App.jsx`; added `C:\Users\edebe\eds\bizPA\backend\src\services\inboxClassificationService.js`, `C:\Users\edebe\eds\bizPA\frontend\src\voiceMicroDecisions.js`, and `C:\Users\edebe\eds\bizPA\frontend\src\voiceMicroDecisions.test.js`
+  - Objective-Proved: The workspace contains the backend and frontend implementation required for safe, undo-compatible voice micro-decisions.
+  - Status: captured
+- Evidence-Type: manual_verification
+  - Artifact: User verification requested on 2026-03-19 for the visible inbox/quarter voice flow: select a blocker, run `Category: Travel` / `Business` / `Split 70%`, confirm the chip appears, and verify Undo reverses the last triage action.
+  - Objective-Proved: Final confirmation from an operator is still required for the visible voice UX path.
+  - Status: planned
+
+# Implementation Log
+- 2026-03-05 Created decomposed task file from the approved MVP pivot checklist.
+- 2026-03-19 Re-read the workstream lifecycle skill and reconciled the stale in-progress task against the current backend/frontend workspace.
+- 2026-03-19 Identified that voice micro-decisions were bypassing the inbox audit path, which meant `/api/v1/inbox/undo-last` could not reliably undo voice-applied triage.
+- 2026-03-19 Added `bizPA/backend/src/services/inboxClassificationService.js` to centralize classification persistence, audit logging, and readiness recalculation.
+- 2026-03-19 Updated `bizPA/backend/src/controllers/inboxController.js` to use the shared audited classification helper.
+- 2026-03-19 Updated `bizPA/backend/src/controllers/voiceController.js` so context-bound voice category/business/personal/split decisions use the shared audited classification helper and return explicit context-binding plus undo metadata.
+- 2026-03-19 Expanded `bizPA/backend/verify_voice_micro_decisions.js` to assert context guardrails, audited classification persistence, preserved field state across incremental voice commands, and safe binding behavior.
+- 2026-03-19 Added `bizPA/frontend/src/voiceMicroDecisions.js` and `bizPA/frontend/src/voiceMicroDecisions.test.js` to isolate and verify the client-side routing/confirmation-chip logic.
+- 2026-03-19 Updated `bizPA/frontend/src/App.jsx` to require an explicit active inbox/finish-now selection for voice triage, render the active voice target banner, and surface a combined confirmation chip with edit/undo controls.
+- 2026-03-19 Ran backend verification, frontend targeted tests, and a production frontend build.
+- 2026-03-19 Requested user verification for the user-visible inbox/quarter voice flow and left the task active pending that confirmation.
+
+# Changes Made
+- `C:\Users\edebe\eds\bizPA\backend\src\services\inboxClassificationService.js`
+  - Added shared audited classification upsert logic that preserves existing fields, writes `transaction_audit_log`, and recalculates readiness.
+- `C:\Users\edebe\eds\bizPA\backend\src\controllers\inboxController.js`
+  - Switched inbox classification writes to the shared service so UI and voice triage use the same persistence/audit path.
+- `C:\Users\edebe\eds\bizPA\backend\src\controllers\voiceController.js`
+  - Enforced explicit micro-decision context, bound triage commands to the selected transaction/evidence context, and returned undo/context metadata for the confirmation chip flow.
+- `C:\Users\edebe\eds\bizPA\backend\verify_voice_micro_decisions.js`
+  - Added assertions for context guardrails, safe binding, audited triage writes, and clarification/error handling.
+- `C:\Users\edebe\eds\bizPA\frontend\src\voiceMicroDecisions.js`
+  - Added pure helpers for routing voice to `/voice/micro-decision`, enforcing explicit selection, and aggregating confirmation-chip actions.
+- `C:\Users\edebe\eds\bizPA\frontend\src\voiceMicroDecisions.test.js`
+  - Added tests covering missing selection handling, finish-now request construction, and chip aggregation/reset behavior.
+- `C:\Users\edebe\eds\bizPA\frontend\src\App.jsx`
+  - Added active voice target selection state for inbox and quarter cards.
+  - Routed voice on inbox/quarter tabs through the micro-decision endpoint only when a blocker is explicitly selected.
+  - Added the visible voice context banner and combined confirmation chip with edit/undo actions.
+  - Preserved the legacy `/voice/process` path for non-triage tabs.
+
+# Validation
+- 2026-03-19 16:59:44+00:00: `npm run verify:voice-micro-decisions`
+  - Result: pass
+  - Key output:
+    - `voice_micro_decisions_ok`
+    - `context_guardrails_ok=true`
+    - `audited_classification_updates_ok=true`
+    - `attach_receipt_clarification_ok=true`
+    - `match_selection_ok=true`
+    - `safe_context_binding_ok=true`
+    - `validation_errors_ok=true`
+- 2026-03-19 16:59:44+00:00: `npm run verify:voice-capture`
+  - Result: pass
+  - Key output:
+    - `voice_capture_parser_ok`
+    - `entity_mapping_ok=true`
+    - `low_confidence_review_ok=true`
+    - `composition_contract_ok=true`
+    - `query_period_filters_ok=true`
+    - `query_exact_date_filters_ok=true`
+- 2026-03-19 16:59:44+00:00: initial frontend test attempt via npm wrapper
+  - Result: blocked by environment
+  - Key output: `Error: spawn EPERM`
+  - Note: `react-scripts` attempted worker-process spawning inside the sandbox when invoked via `npm test`.
+- 2026-03-19 16:59:44+00:00: `npx react-scripts test --watchAll=false --runInBand --runTestsByPath src/voiceMicroDecisions.test.js src/quarterReadiness.test.js`
+  - Result: pass
+  - Key output:
+    - `PASS src/voiceMicroDecisions.test.js`
+    - `PASS src/quarterReadiness.test.js`
+    - `Tests: 7 passed, 7 total`
+- 2026-03-19 16:59:44+00:00: `npm run build`
+  - Result: pass
+  - Key output:
+    - `Compiled successfully.`
+- 2026-03-19 16:59:44+00:00: user verification request recorded
+  - Result: pending
+  - Key output: Please verify that selecting a finish-now blocker, speaking `Category: Travel` / `Business` / `Split 70%`, and tapping Undo behaves correctly in the visible inbox/quarter flow.
+
+# Risks/Notes
+- The visible voice triage flow is now selection-gated on inbox and quarter screens. If future UI changes remove or obscure the selected voice target, the frontend helper tests should be expanded before rollout.
+- Evidence commands remain context-bound to `evidence` rather than the inbox transaction flow. This task hardened their context safety and confirmation payloads, but it did not add a visible evidence-selection UI in the current frontend.
+- The direct `npm test` wrapper is not reliable in this sandbox because Jest worker spawning can fail with `EPERM`; use the documented `npx react-scripts test --runInBand ...` form for deterministic local verification here.
+- A prior duplicate lifecycle file exists under `workstream/300_complete`; this in-progress record is the active reconciliation file requested by the user and remains open until user verification is captured.
+
+# Completion Status
+- Awaiting user verification (2026-03-19 16:59:44+00:00)

Plan update
Lifecycle documentation is updated with evidence and validation. The implementation is technically complete and now waiting on the required user-visible verification step.
  ✓ Inspect existing voice micro-decision backend and inbox UI paths; define the minimal context-safe flow to implement
  ✓ Implement backend guardrails and frontend inbox/finish-now voice binding with confirmation chip and undo support
  ✓ Add or update automated verification for the new behavior and run validations
  ✓ Update the lifecycle task file with ordered evidence, validation results, and current completion state
file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..e6d93b4bc1cd6960ce27238cfb7e90c3322f41e0
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,6 +10,36 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+const {
+  parseVoiceMicroDecision,
+  validateMicroDecisionContext,
+} = require('../services/voiceMicroDecisionService');
+
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undoable: undoSupported,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
 
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
@@ -267,76 +297,177 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
-  const text = String(transcript).toLowerCase().trim();
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_kind = null,
+    context_scope = null,
+  } = req.body || {};
+  const rawTranscript = String(transcript).trim();
+  const text = rawTranscript.toLowerCase();
+  const normalizedContext = context_kind ?? context_scope;
+  const parsedCommand = parseVoiceMicroDecision(rawTranscript);
+  const contextValidation = validateMicroDecisionContext(parsedCommand, normalizedContext);
+  const normalizedContextScope = contextValidation.ok
+    ? contextValidation.context_kind
+    : (normalizedContext === null || normalizedContext === undefined
+      ? null
+      : String(normalizedContext).toLowerCase().replace(/-/g, '_'));
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
+  if (!contextValidation.ok) return res.status(contextValidation.statusCode).json({ error: contextValidation.error });
 
   try {
-    if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
-      const categoryName = text.replace('category:', '').trim();
-      const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+    if (parsedCommand.type === 'category') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: parsedCommand.category_code,
+            category_name: parsedCommand.category_name,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'category', label: parsedCommand.category_name }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
-      const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+    if (parsedCommand.type === 'business_personal') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const bp = parsedCommand.business_personal;
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    const splitMatch = text.match(/split\s+(\d{1,3})%?/);
-    if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
-      const pct = Number(splitMatch[1]);
+    if (parsedCommand.type === 'split') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const pct = Number(parsedCommand.split_business_pct);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+    if (parsedCommand.type === 'attach_receipt') {
+      if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for attach receipt commands.' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          parsed_command: parsedCommand,
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
-    if (['match first', 'match second', 'match third'].includes(text)) {
+    if (parsedCommand.type === 'match_rank') {
       if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for match commands.' });
-      const index = text === 'match first' ? 1 : text === 'match second' ? 2 : 3;
+      const index = parsedCommand.match_rank;
       const suggestions = await db.query(
         `
         WITH ev AS (
@@ -364,10 +495,24 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          bank_txn_id: chosen,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text === 'no match') {
+    if (parsedCommand.type === 'no_match') {
       if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for no match.' });
       await db.query(
         `
@@ -376,7 +521,20 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/backend/verify_voice_micro_decisions.js b/backend/verify_voice_micro_decisions.js
index 25d9ab02050aac37a5d38aca5c6f0154de9bb787..15c93efa65f133ec1a59d827ecbfa29a95cf4e6d
--- a/backend/verify_voice_micro_decisions.js
+++ b/backend/verify_voice_micro_decisions.js
@@ -9,16 +9,70 @@
       { bank_txn_id: 'txn-2' },
       { bank_txn_id: 'txn-3' }
     ];
+    this.classifications = new Map();
+    this.auditEntries = [];
   }
 
-  async query(text, params) {
+  get pool() {
+    return {
+      connect: async () => ({
+        query: this.query.bind(this),
+        release() {},
+      }),
+    };
+  }
+
+  async query(text, params = []) {
     const sql = text.replace(/\s+/g, ' ').trim();
     this.calls.push({ sql, params });
 
+    if (sql === 'BEGIN' || sql === 'COMMIT' || sql === 'ROLLBACK') {
+      return { rows: [] };
+    }
+
+    if (sql.includes('SELECT * FROM transaction_classifications')) {
+      const existing = this.classifications.get(params[0]);
+      return { rows: existing ? [existing] : [] };
+    }
+
+    if (sql.includes('INSERT INTO transaction_classifications')) {
+      const next = {
+        user_id: params[0],
+        bank_txn_id: params[1],
+        category_code: params[2],
+        category_name: params[3],
+        business_personal: params[4],
+        is_split: params[5],
+        split_business_pct: params[6],
+        confidence: params[7],
+        source: params[8],
+      };
+      this.classifications.set(params[1], next);
+      return { rows: [next] };
+    }
+
+    if (sql.includes('INSERT INTO transaction_audit_log')) {
+      this.auditEntries.push({
+        bank_txn_id: params[1],
+        previous_value: params[4],
+        new_value: params[5],
+        change_source: params[6],
+      });
+      return { rows: [] };
+    }
+
+    if (sql.includes('SELECT txn_date FROM bank_transactions')) {
+      return { rows: [{ txn_date: '2026-03-12' }] };
+    }
+
     if (sql.includes('FROM bank_transactions bt CROSS JOIN ev')) {
       return { rows: this.matchSuggestions };
     }
 
+    if (sql.includes('INSERT INTO evidence_links')) {
+      return { rows: [] };
+    }
+
     return { rows: [] };
   }
 }
@@ -69,45 +123,79 @@
   const mockDb = new MockDb();
   const controller = loadController(mockDb);
 
+  const missingContext = await invoke(controller, {
+    transcript: 'Business',
+    bank_txn_id: 'txn-123'
+  });
+  assert.strictEqual(missingContext.statusCode, 400);
+  assert.strictEqual(missingContext.body.error, 'context_kind is required for MVP voice micro-decisions.');
+
   const category = await invoke(controller, {
     transcript: 'Category: Travel',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(category.statusCode, 200);
-  assert.strictEqual(category.body.confirmation_chip, 'Applied: travel');
-  assert(
-    mockDb.calls.some((call) => call.sql.includes('INSERT INTO transaction_classifications') && call.params[2] === 'TRAVEL' && call.params[3] === 'travel'),
-    'Expected category command to persist manual classification'
-  );
+  assert.strictEqual(category.body.confirmation_chip, 'Applied: Travel');
+  assert.strictEqual(category.body.undo.supported, true);
+  assert.deepStrictEqual(category.body.context_binding, {
+    scope: 'inbox',
+    target_type: 'bank_transaction',
+    target_id: 'txn-123'
+  });
+  assert.strictEqual(mockDb.classifications.get('txn-123').category_code, 'TRAVEL');
+  assert.strictEqual(mockDb.auditEntries.at(-1).change_source, 'voice');
 
   const business = await invoke(controller, {
     transcript: 'Business',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(business.statusCode, 200);
   assert.strictEqual(business.body.confirmation_chip, 'Applied: BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').category_code, 'TRAVEL');
+  assert.strictEqual(mockDb.classifications.get('txn-123').business_personal, 'BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').is_split, false);
 
   const split = await invoke(controller, {
     transcript: 'Split 70%',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(split.statusCode, 200);
   assert.strictEqual(split.body.confirmation_chip, 'Applied: Split 70%');
+  assert.strictEqual(split.body.undo.supported, true);
+  assert.strictEqual(mockDb.classifications.get('txn-123').business_personal, 'BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').is_split, true);
+  assert.strictEqual(mockDb.classifications.get('txn-123').split_business_pct, 70);
 
   const attachReceipt = await invoke(controller, {
     transcript: 'Attach receipt',
-    bank_txn_id: 'txn-123'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(attachReceipt.statusCode, 200);
   assert.strictEqual(attachReceipt.body.action_status, 'clarification_needed');
   assert.strictEqual(attachReceipt.body.confirmation_chip, 'Attach receipt requested');
+  assert.strictEqual(attachReceipt.body.undo.supported, false);
+  assert.deepStrictEqual(attachReceipt.body.context_binding, {
+    scope: 'evidence',
+    target_type: 'evidence',
+    target_id: 'evidence-1'
+  });
 
   const matchSecond = await invoke(controller, {
     transcript: 'Match second',
-    evidence_id: 'evidence-1'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(matchSecond.statusCode, 200);
   assert.strictEqual(matchSecond.body.confirmation_chip, 'Applied: Match 2');
+  assert.deepStrictEqual(matchSecond.body.context_binding, {
+    scope: 'evidence',
+    target_type: 'evidence',
+    target_id: 'evidence-1'
+  });
   assert(
     mockDb.calls.some((call) => call.sql.includes('INSERT INTO evidence_links') && call.params[1] === 'evidence-1' && call.params[2] === 'txn-2'),
     'Expected match command to persist selected evidence link'
@@ -115,29 +203,41 @@
 
   const noMatch = await invoke(controller, {
     transcript: 'No match',
-    evidence_id: 'evidence-1'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(noMatch.statusCode, 200);
   assert.strictEqual(noMatch.body.confirmation_chip, 'Applied: No match');
 
   const invalidSplit = await invoke(controller, {
     transcript: 'Split 140%',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(invalidSplit.statusCode, 400);
   assert.strictEqual(invalidSplit.body.error, 'Split percentage must be 0-100.');
 
+  const unsafeContext = await invoke(controller, {
+    transcript: 'Category: Travel',
+    bank_txn_id: 'txn-123',
+    context_kind: 'evidence'
+  });
+  assert.strictEqual(unsafeContext.statusCode, 409);
+  assert.strictEqual(unsafeContext.body.error, 'Command "category" is not allowed for evidence context.');
+
   const unsupported = await invoke(controller, {
-    transcript: 'Archive this forever'
+    transcript: 'Archive this forever',
+    context_kind: 'inbox'
   });
   assert.strictEqual(unsupported.statusCode, 400);
   assert.strictEqual(unsupported.body.error, 'Unsupported MVP voice micro-decision command.');
 
   console.log('voice_micro_decisions_ok');
-  console.log('category_business_split_ok=true');
+  console.log('context_guardrails_ok=true');
+  console.log('audited_classification_updates_ok=true');
   console.log('attach_receipt_clarification_ok=true');
   console.log('match_selection_ok=true');
-  console.log('no_match_ok=true');
+  console.log('safe_context_binding_ok=true');
   console.log('validation_errors_ok=true');
 }
 
diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index 9b145fadac9123e3fa01b12af6ef2b9916dc11de..590be3d00c08cb02d6139abdd4d1a0ee41f9de7b
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -50,6 +50,16 @@
   buildQuarterUiState,
   deriveResolvedIssueIdsFromSearchParams,
 } from './quarterReadiness';
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+import {
+  applyInboxVoiceCommandToDraft,
+  buildInboxVoiceChip,
+  parseInboxVoiceCommand,
+} from './voiceMicroDecision';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -427,6 +437,13 @@
   .date-group-count { border-radius: 999px; padding: 4px 9px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.72rem; color: var(--text-muted); }
   .date-group-body { padding: 0 12px 12px; }
   .voice-preview { background: rgba(0,0,0,0.82); color: white; padding: 8px 15px; border-radius: 15px; margin-bottom: 8px; font-size: 0.72rem; max-width: 240px; text-align: right; }
+  .voice-context-banner { display: grid; gap: 10px; padding: 14px 16px; margin-bottom: 16px; border-radius: 18px; background: var(--bg-card); border: 1px solid var(--border); box-shadow: var(--shadow); }
+  .voice-context-row, .voice-chip-actions, .voice-chip-labels { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
+  .voice-context-pill, .voice-action-pill { border-radius: 999px; padding: 6px 10px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.74rem; font-weight: 700; }
+  .voice-context-pill.active { background: rgba(13,92,99,0.12); color: var(--primary-strong); border-color: rgba(13,92,99,0.24); }
+  .voice-micro-chip { display: grid; gap: 10px; padding: 14px 16px; border-radius: 18px; background: rgba(13,92,99,0.08); border: 1px solid rgba(13,92,99,0.18); }
+  .voice-micro-chip strong { display: block; }
+  .inbox-action-card.voice-target { border-color: rgba(13,92,99,0.36); box-shadow: 0 0 0 2px rgba(13,92,99,0.12), var(--shadow); }
   .spinner { animation: rotate 2s linear infinite; }
   .capture-grid { display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(320px, 0.95fr); gap: 18px; align-items: start; }
   .command-centre { display: grid; gap: 18px; }
@@ -1318,6 +1335,8 @@
     && searchParams?.get('snapshotDemo') === '1';
   const inboxDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('inboxDemo') === '1';
+  const voiceDemoCommand = searchParams?.get('voiceDemoCommand') || '';
+  const voiceDemoTarget = searchParams?.get('voiceDemoTarget') || '';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1382,9 +1401,12 @@
   const [inboxDrafts, setInboxDrafts] = useState({});
   const [inboxBusyId, setInboxBusyId] = useState('');
   const [inboxMessage, setInboxMessage] = useState(null);
+  const [activeVoiceContextId, setActiveVoiceContextId] = useState(voiceDemoTarget);
+  const [voiceMicroDecisionChip, setVoiceMicroDecisionChip] = useState(null);
   const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
+  const voiceDemoSeedRef = useRef('');
   const quarterStart = '2026-01-01';
   const quarterEnd = '2026-03-31';
 
@@ -1606,10 +1628,80 @@
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
   const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const activeVoiceCard = findVoiceContextCard(inboxCards, activeVoiceContextId);
   const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
   const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
   const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
+  const applyVoiceMicroDecisionLocally = (card, parsedCommand, responseLike = {}) => {
+    if (!card || !parsedCommand) {
+      return;
+    }
+
+    const nextDraft = applyInboxVoiceCommandToDraft(inboxDrafts[card.id] || buildInboxDraft(card), parsedCommand);
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: nextDraft,
+    }));
+
+    if (parsedCommand.type === 'category') {
+      syncInboxCardLocally(card.id, {
+        category_code: nextDraft.category_code,
+        category_name: nextDraft.category_name,
+      });
+    } else if (parsedCommand.type === 'business_personal') {
+      syncInboxCardLocally(card.id, {
+        business_personal: nextDraft.business_personal,
+        is_split: false,
+        split_business_pct: null,
+      });
+    } else if (parsedCommand.type === 'split') {
+      syncInboxCardLocally(card.id, {
+        business_personal: 'BUSINESS',
+        is_split: true,
+        split_business_pct: Number(parsedCommand.split_business_pct),
+      });
+    }
+
+    setVoiceMicroDecisionChip((previous) => reduceVoiceMicroDecisionChip(previous, {
+      ...responseLike,
+      action_status: responseLike.action_status || 'execute',
+      confirmation_chip: responseLike.confirmation_chip || parsedCommand.confirmation_chip,
+      applied_actions: responseLike.applied_actions || [{ label: parsedCommand.confirmation_chip.replace(/^Applied:\s*/, '') }],
+      undo: responseLike.undo || { supported: true },
+    }, card));
+  };
+
+  useEffect(() => {
+    if (!activeVoiceContextId) {
+      return;
+    }
+    if (!findVoiceContextCard(inboxCards, activeVoiceContextId)) {
+      setActiveVoiceContextId('');
+    }
+  }, [activeVoiceContextId, inboxCards]);
+
+  useEffect(() => {
+    if (!inboxDemoEnabled || !voiceDemoCommand || !activeVoiceCard) {
+      return;
+    }
+    const seedKey = `${activeVoiceCard.id}:${voiceDemoCommand}`;
+    if (voiceDemoSeedRef.current === seedKey) {
+      return;
+    }
+    const parsedCommand = parseInboxVoiceCommand(voiceDemoCommand);
+    if (!parsedCommand) {
+      return;
+    }
+    voiceDemoSeedRef.current = seedKey;
+    applyVoiceMicroDecisionLocally(activeVoiceCard, parsedCommand, {
+      action_status: 'execute',
+      confirmation_chip: parsedCommand.confirmation_chip,
+      applied_actions: [{ label: parsedCommand.confirmation_chip.replace(/^Applied:\s*/, '') }],
+      undo: { supported: true },
+    });
+  }, [activeVoiceCard, inboxDemoEnabled, voiceDemoCommand]);
+
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
   const canManageGovernance = hasPermission(currentUser, PERMISSIONS.GOVERNANCE_MANAGE);
@@ -1802,15 +1894,18 @@
     clearInboxMessage();
     try {
       setInboxBusyId('undo-last');
+      setVoiceMicroDecisionChip(null);
       if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxDrafts({});
+        setVoiceMicroDecisionChip(null);
         setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
         return;
       }
       await axios.post(`${API_BASE_URL}/inbox/undo-last`);
       await fetchAllData();
+      setVoiceMicroDecisionChip(null);
       setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
     } catch (error) {
       setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
@@ -1991,22 +2086,78 @@
     const cleanText = text.replace(/┬ú/g, 'GBP ');
     try {
       setVoiceStatus('processing');
-      const response = await axios.post(`${API_BASE_URL}/voice/process`, {
+      const microDecisionRequest = buildVoiceMicroDecisionRequest({
+        currentTab,
         transcript: cleanText,
-        device_id: 'mobile-v139',
-        current_date: new Date().toISOString(),
+        activeVoiceContextId,
+        inboxCards,
       });
-      if (response.data.confirmation_text && window.SpeechSynthesisUtterance) {
+
+      if (microDecisionRequest?.error) {
+        setInboxMessage({ type: 'error', text: microDecisionRequest.error });
+        setCurrentTab('inbox');
+        return;
+      }
+
+      let response;
+      if (microDecisionRequest) {
+        if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+          const parsedCommand = parseInboxVoiceCommand(cleanText);
+          if (!parsedCommand) {
+            throw new Error('Only Category, Business, Personal, and Split {n}% are available in the local voice triage demo.');
+          }
+          applyVoiceMicroDecisionLocally(microDecisionRequest.card, parsedCommand, {
+            action_status: 'execute',
+            confirmation_chip: parsedCommand.confirmation_chip,
+            applied_actions: [{ label: parsedCommand.confirmation_chip.replace(/^Applied:\s*/, '') }],
+            undo: { supported: true },
+          });
+          response = { data: buildInboxVoiceChip({
+            action_status: 'execute',
+            confirmation_chip: parsedCommand.confirmation_chip,
+            undoable: true,
+            bank_txn_id: microDecisionRequest.card.id,
+          }, microDecisionRequest.card) };
+          response.data.confirmation_chip = parsedCommand.confirmation_chip;
+        } else {
+          response = await axios.post(`${API_BASE_URL}${microDecisionRequest.endpoint}`, microDecisionRequest.payload);
+        }
+      } else {
+        response = await axios.post(`${API_BASE_URL}/voice/process`, {
+          transcript: cleanText,
+          device_id: 'mobile-v139',
+          current_date: new Date().toISOString(),
+        });
+      }
+      const spokenConfirmation = response.data.confirmation_chip || response.data.confirmation_text;
+      if (spokenConfirmation && window.SpeechSynthesisUtterance) {
         try {
-          const utterance = new window.SpeechSynthesisUtterance(response.data.confirmation_text);
+          const utterance = new window.SpeechSynthesisUtterance(spokenConfirmation);
           window.speechSynthesis.speak(utterance);
         } catch (_) {
         }
       }
-      executeVoiceAction(response.data);
-      fetchAllData();
+      if (microDecisionRequest) {
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          setVoiceMicroDecisionChip((previous) => reduceVoiceMicroDecisionChip(previous, response.data, microDecisionRequest.card));
+          setInboxMessage({
+            type: 'success',
+            text: response.data.confirmation_chip || 'Voice triage action applied.',
+          });
+        }
+      } else {
+        executeVoiceAction(response.data);
+      }
+      if (!(microDecisionRequest && (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled))) {
+        await fetchAllData();
+      }
     } catch (error) {
-      alert('AI Error');
+      const message = error?.response?.data?.error || error?.message || 'AI Error';
+      if (currentTab === 'inbox' || currentTab === 'quarter') {
+        setInboxMessage({ type: 'error', text: message });
+      } else {
+        alert(message);
+      }
     } finally {
       setVoiceStatus('idle');
       setIsListening(false);
@@ -2894,7 +3045,11 @@
             const saving = inboxBusyId === card.id;
             const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
             return (
-              <section key={card.id} className="inbox-action-card">
+              <section
+                key={card.id}
+                className={`inbox-action-card ${activeVoiceContextId === card.id ? 'voice-target' : ''}`}
+                onClick={() => setActiveVoiceContextId(card.id)}
+              >
                 <div className="inbox-card-top">
                   <div>
                     <strong>{card.merchant}</strong>
@@ -2914,6 +3069,9 @@
                       {label}
                     </span>
                   ))}
+                  {activeVoiceContextId === card.id && (
+                    <span className="voice-context-pill active">Voice target</span>
+                  )}
                   {card.suggested_category?.category_name && (
                     <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
                       <CheckCircle2 size={14} />
@@ -3357,6 +3515,9 @@
   };
 
   const openReadinessWorkflow = (issue) => {
+    if (issue?.affected_entity_id) {
+      setActiveVoiceContextId(String(issue.affected_entity_id));
+    }
     if (issue?.resolution_target?.workflow === '/inbox/finish-now') {
       setCurrentTab('inbox');
       return;
@@ -3364,6 +3525,76 @@
     setCurrentTab('inbox');
   };
 
+  const renderVoiceContextBanner = () => {
+    if (!(currentTab === 'inbox' || currentTab === 'quarter')) {
+      return null;
+    }
+
+    return (
+      <section className="voice-context-banner">
+        <div>
+          <strong>Voice triage</strong>
+          <div className="small-muted">
+            Use the mic against one selected finish-now item only. Supported commands here are category, business, personal, split n%, and attach receipt.
+          </div>
+        </div>
+        <div className="voice-context-row">
+          <span className={`voice-context-pill ${activeVoiceCard ? 'active' : ''}`}>
+            {activeVoiceCard
+              ? `Target: ${activeVoiceCard.merchant} (${activeVoiceCard.id})`
+              : 'Select a blocker card to arm voice triage'}
+          </span>
+          {activeVoiceCard && (
+            <button className="secondary-action" type="button" onClick={() => setCurrentTab('inbox')}>
+              Edit target
+            </button>
+          )}
+        </div>
+        {voiceMicroDecisionChip && (
+          <div className="voice-micro-chip">
+            <div>
+              <strong>{voiceMicroDecisionChip.confirmationChip}</strong>
+              <div className="small-muted">
+                {voiceMicroDecisionChip.targetLabel} ({voiceMicroDecisionChip.targetId})
+              </div>
+            </div>
+            {voiceMicroDecisionChip.labels.length > 0 && (
+              <div className="voice-chip-labels">
+                {voiceMicroDecisionChip.labels.map((label) => (
+                  <span key={`${voiceMicroDecisionChip.targetId}-${label}`} className="voice-action-pill">
+                    {label}
+                  </span>
+                ))}
+              </div>
+            )}
+            <div className="voice-chip-actions">
+              <button
+                className="secondary-action"
+                type="button"
+                onClick={() => {
+                  setCurrentTab('inbox');
+                  setActiveVoiceContextId(voiceMicroDecisionChip.targetId);
+                }}
+              >
+                Edit
+              </button>
+              {voiceMicroDecisionChip.undoSupported && (
+                <button
+                  className="secondary-action"
+                  type="button"
+                  onClick={undoLastInboxAction}
+                  disabled={inboxBusyId === 'undo-last'}
+                >
+                  Undo
+                </button>
+              )}
+            </div>
+          </div>
+        )}
+      </section>
+    );
+  };
+
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
@@ -3654,6 +3885,13 @@
                     </div>
                   </div>
                   <div className="finish-now-actions">
+                    <button
+                      className="secondary-action"
+                      type="button"
+                      onClick={() => setActiveVoiceContextId(String(issue.affected_entity_id))}
+                    >
+                      {activeVoiceContextId === String(issue.affected_entity_id) ? 'Voice target set' : 'Arm voice'}
+                    </button>
                     {(readinessDemoEnabled || snapshotDemoEnabled) ? (
                       <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
                         Mark fixed
@@ -4391,6 +4629,7 @@
         </button>
       </section>
       {connectionError && <div className="alert alert-danger">{connectionError}</div>}
+      {renderVoiceContextBanner()}
 
       {loading ? (
         <div className="text-center py-5">Loading...</div>
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..6eb469fcc7f023f8545d9e952a5823fb8c47a88d
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});
diff --git a/workstream/200_inprogress/codex/20260305_192700_bizpa_mvp_pivot_phase06_voice_micro_decisions.md b/workstream/200_inprogress/codex/20260305_192700_bizpa_mvp_pivot_phase06_voice_micro_decisions.md
index cf85796e760112547a34bd77ea2a0f0a86fd3219..c02d5e36a241a1c98d7e97bf7c319cce1837c2ce
--- a/workstream/200_inprogress/codex/20260305_192700_bizpa_mvp_pivot_phase06_voice_micro_decisions.md
+++ b/workstream/200_inprogress/codex/20260305_192700_bizpa_mvp_pivot_phase06_voice_micro_decisions.md
@@ -1,43 +1,136 @@
-# Source
-- Derived from backlog: `C:\Users\edebe\eds\workstream\000_backlog\mvp_prd_quarterly_export_10min.md`
-- Decomposed from checklist: `C:\Users\edebe\eds\workstream\300_complete\20260305_191657_bizpa_mvp_quarterly_pivot_full_delivery_checklist.md`
-
-# Task Summary
-Constrain voice intents to PRD micro-decisions and bind safely to inbox/finish-now context with confirmation chip + undo.
-
-# Context
-- Affected area: voice controller/parser and web/mobile voice UX
-
-# Plan
-- [ ] 1. Implement phase scope in code and configuration.
-  - [ ] Test: Execute phase-specific implementation checks and verify expected behavior change.
-  - [ ] Evidence: pending
-- [ ] 2. Add/adjust automated tests for this phase.
-  - [ ] Test: Run targeted unit/integration tests for new behavior and ensure green results.
-  - [ ] Evidence: pending
-- [ ] 3. Validate against PRD acceptance criteria impacted by this phase.
-  - [ ] Test: Run intent contract tests for supported utterances and verify each action is applied, confirmed, and undoable.
-  - [ ] Evidence: pending
-- [ ] 4. Update technical notes and rollout caveats for downstream phases.
-  - [ ] Test: Confirm lifecycle doc lists changed files, commands run, and known risks.
-  - [ ] Evidence: pending
-
-# Implementation Log
-- 2026-03-05 Created decomposed task file from approved MVP pivot checklist.
-
-# Changes Made
-- Task file created; implementation not started.
-
-# Validation
-- Pending.
-
-# Risks/Notes
-- Must follow strict sequential completion and evidence capture before starting dependent phase.
-
-# Completion Status
-- Todo.
-
-
-## Retry History
-Retry-Count: 1
-- Retry scheduled at 2026-03-18 17:21:29
+# Source
+- Derived from backlog: `C:\Users\edebe\eds\workstream\000_epic\20260305_185316_mvp_prd_quarterly_export_10min.md`
+- Decomposed from checklist: `C:\Users\edebe\eds\workstream\300_complete\20260305_191657_bizpa_mvp_quarterly_pivot_full_delivery_checklist.md`
+
+# Task Summary
+Constrain voice intents to PRD micro-decisions and bind them safely to inbox/finish-now context with a visible confirmation chip and undo-compatible triage flow.
+
+# Context
+- Affected backend area: `C:\Users\edebe\eds\bizPA\backend\src\controllers\voiceController.js`, `C:\Users\edebe\eds\bizPA\backend\src\controllers\inboxController.js`
+- New backend support: `C:\Users\edebe\eds\bizPA\backend\src\services\inboxClassificationService.js`, `C:\Users\edebe\eds\bizPA\backend\verify_voice_micro_decisions.js`
+- Affected frontend area: `C:\Users\edebe\eds\bizPA\frontend\src\App.jsx`
+- New frontend support: `C:\Users\edebe\eds\bizPA\frontend\src\voiceMicroDecisions.js`, `C:\Users\edebe\eds\bizPA\frontend\src\voiceMicroDecisions.test.js`
+
+# Dependency
+`C:\Users\edebe\eds\workstream\300_complete\20260305_191657_bizpa_mvp_quarterly_pivot_full_delivery_checklist.md`
+
+# Plan
+- [x] 1. Implement phase scope in code and configuration.
+  - [x] Test: Execute the updated voice micro-decision flow against backend verification and confirm context-bound classification updates now use audited inbox persistence with undo metadata.
+  - [x] Evidence: `bizPA/backend/src/services/inboxClassificationService.js` now centralizes audited classification writes; `bizPA/backend/src/controllers/voiceController.js` now requires explicit micro-decision context and returns context binding plus undo metadata; `bizPA/frontend/src/App.jsx` now binds voice triage to an explicitly selected inbox/finish-now card and shows a confirmation chip with undo/edit actions.
+- [x] 2. Add/adjust automated tests for this phase.
+  - [x] Test: Run targeted backend and frontend tests for the new behavior and ensure green results.
+  - [x] Evidence: `npm run verify:voice-micro-decisions` passed in `bizPA/backend`; `npx react-scripts test --watchAll=false --runInBand --runTestsByPath src/voiceMicroDecisions.test.js src/quarterReadiness.test.js` passed in `bizPA/frontend`.
+- [x] 3. Validate against PRD acceptance criteria impacted by this phase.
+  - [x] Test: Run intent contract tests for supported utterances and verify each action is context-safe, confirmed, and undo-compatible for inbox triage actions; confirm frontend production build compiles with the new voice UX.
+  - [x] Evidence: Backend verifier output ended with `voice_micro_decisions_ok`, `context_guardrails_ok=true`, `audited_classification_updates_ok=true`, and `safe_context_binding_ok=true`; frontend `npm run build` compiled successfully.
+- [x] 4. Update technical notes and rollout caveats for downstream phases.
+  - [x] Test: Confirm this lifecycle doc lists changed files, commands run, known risks, and the pending user-verification gate for the visible inbox voice flow.
+  - [x] Evidence: Lifecycle file rewritten on 2026-03-19 with normalized evidence, validation results, and an explicit user-verification request/state.
+
+# Evidence
+Objective-Delivery-Coverage: 90%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `C:\Users\edebe\eds\bizPA\frontend\src\App.jsx`
+  - Objective-Proved: The mobile/web inbox and quarter screens now require an explicit finish-now selection before voice triage, show the active voice target, and render a confirmation chip with edit/undo affordances after a voice micro-decision.
+  - Status: captured
+- Evidence-Type: test_output
+  - Artifact: `npm run verify:voice-micro-decisions` output: `voice_micro_decisions_ok`, `context_guardrails_ok=true`, `audited_classification_updates_ok=true`, `attach_receipt_clarification_ok=true`, `match_selection_ok=true`, `safe_context_binding_ok=true`, `validation_errors_ok=true`
+  - Objective-Proved: Supported utterances are constrained to the micro-decision contract, require safe context binding, and return the expected confirmation/undo contract while preserving audited inbox behavior.
+  - Status: captured
+- Evidence-Type: test_output
+  - Artifact: `npx react-scripts test --watchAll=false --runInBand --runTestsByPath src/voiceMicroDecisions.test.js src/quarterReadiness.test.js` output: `PASS src/voiceMicroDecisions.test.js`, `PASS src/quarterReadiness.test.js`
+  - Objective-Proved: The frontend request-routing and confirmation-chip helper logic behaves correctly for selection enforcement, context routing, and chip aggregation.
+  - Status: captured
+- Evidence-Type: test_output
+  - Artifact: `npm run build` in `C:\Users\edebe\eds\bizPA\frontend` output: `Compiled successfully.`
+  - Objective-Proved: The user-visible voice triage UI changes compile successfully in the production frontend build.
+  - Status: captured
+- Evidence-Type: diff
+  - Artifact: Updated `C:\Users\edebe\eds\bizPA\backend\src\controllers\voiceController.js`, `C:\Users\edebe\eds\bizPA\backend\src\controllers\inboxController.js`, `C:\Users\edebe\eds\bizPA\frontend\src\App.jsx`; added `C:\Users\edebe\eds\bizPA\backend\src\services\inboxClassificationService.js`, `C:\Users\edebe\eds\bizPA\frontend\src\voiceMicroDecisions.js`, and `C:\Users\edebe\eds\bizPA\frontend\src\voiceMicroDecisions.test.js`
+  - Objective-Proved: The workspace contains the backend and frontend implementation required for safe, undo-compatible voice micro-decisions.
+  - Status: captured
+- Evidence-Type: manual_verification
+  - Artifact: User verification requested on 2026-03-19 for the visible inbox/quarter voice flow: select a blocker, run `Category: Travel` / `Business` / `Split 70%`, confirm the chip appears, and verify Undo reverses the last triage action.
+  - Objective-Proved: Final confirmation from an operator is still required for the visible voice UX path.
+  - Status: planned
+
+# Implementation Log
+- 2026-03-05 Created decomposed task file from the approved MVP pivot checklist.
+- 2026-03-19 Re-read the workstream lifecycle skill and reconciled the stale in-progress task against the current backend/frontend workspace.
+- 2026-03-19 Identified that voice micro-decisions were bypassing the inbox audit path, which meant `/api/v1/inbox/undo-last` could not reliably undo voice-applied triage.
+- 2026-03-19 Added `bizPA/backend/src/services/inboxClassificationService.js` to centralize classification persistence, audit logging, and readiness recalculation.
+- 2026-03-19 Updated `bizPA/backend/src/controllers/inboxController.js` to use the shared audited classification helper.
+- 2026-03-19 Updated `bizPA/backend/src/controllers/voiceController.js` so context-bound voice category/business/personal/split decisions use the shared audited classification helper and return explicit context-binding plus undo metadata.
+- 2026-03-19 Expanded `bizPA/backend/verify_voice_micro_decisions.js` to assert context guardrails, audited classification persistence, preserved field state across incremental voice commands, and safe binding behavior.
+- 2026-03-19 Added `bizPA/frontend/src/voiceMicroDecisions.js` and `bizPA/frontend/src/voiceMicroDecisions.test.js` to isolate and verify the client-side routing/confirmation-chip logic.
+- 2026-03-19 Updated `bizPA/frontend/src/App.jsx` to require an explicit active inbox/finish-now selection for voice triage, render the active voice target banner, and surface a combined confirmation chip with edit/undo controls.
+- 2026-03-19 Ran backend verification, frontend targeted tests, and a production frontend build.
+- 2026-03-19 Requested user verification for the user-visible inbox/quarter voice flow and left the task active pending that confirmation.
+
+# Changes Made
+- `C:\Users\edebe\eds\bizPA\backend\src\services\inboxClassificationService.js`
+  - Added shared audited classification upsert logic that preserves existing fields, writes `transaction_audit_log`, and recalculates readiness.
+- `C:\Users\edebe\eds\bizPA\backend\src\controllers\inboxController.js`
+  - Switched inbox classification writes to the shared service so UI and voice triage use the same persistence/audit path.
+- `C:\Users\edebe\eds\bizPA\backend\src\controllers\voiceController.js`
+  - Enforced explicit micro-decision context, bound triage commands to the selected transaction/evidence context, and returned undo/context metadata for the confirmation chip flow.
+- `C:\Users\edebe\eds\bizPA\backend\verify_voice_micro_decisions.js`
+  - Added assertions for context guardrails, safe binding, audited triage writes, and clarification/error handling.
+- `C:\Users\edebe\eds\bizPA\frontend\src\voiceMicroDecisions.js`
+  - Added pure helpers for routing voice to `/voice/micro-decision`, enforcing explicit selection, and aggregating confirmation-chip actions.
+- `C:\Users\edebe\eds\bizPA\frontend\src\voiceMicroDecisions.test.js`
+  - Added tests covering missing selection handling, finish-now request construction, and chip aggregation/reset behavior.
+- `C:\Users\edebe\eds\bizPA\frontend\src\App.jsx`
+  - Added active voice target selection state for inbox and quarter cards.
+  - Routed voice on inbox/quarter tabs through the micro-decision endpoint only when a blocker is explicitly selected.
+  - Added the visible voice context banner and combined confirmation chip with edit/undo actions.
+  - Preserved the legacy `/voice/process` path for non-triage tabs.
+
+# Validation
+- 2026-03-19 16:59:44+00:00: `npm run verify:voice-micro-decisions`
+  - Result: pass
+  - Key output:
+    - `voice_micro_decisions_ok`
+    - `context_guardrails_ok=true`
+    - `audited_classification_updates_ok=true`
+    - `attach_receipt_clarification_ok=true`
+    - `match_selection_ok=true`
+    - `safe_context_binding_ok=true`
+    - `validation_errors_ok=true`
+- 2026-03-19 16:59:44+00:00: `npm run verify:voice-capture`
+  - Result: pass
+  - Key output:
+    - `voice_capture_parser_ok`
+    - `entity_mapping_ok=true`
+    - `low_confidence_review_ok=true`
+    - `composition_contract_ok=true`
+    - `query_period_filters_ok=true`
+    - `query_exact_date_filters_ok=true`
+- 2026-03-19 16:59:44+00:00: initial frontend test attempt via npm wrapper
+  - Result: blocked by environment
+  - Key output: `Error: spawn EPERM`
+  - Note: `react-scripts` attempted worker-process spawning inside the sandbox when invoked via `npm test`.
+- 2026-03-19 16:59:44+00:00: `npx react-scripts test --watchAll=false --runInBand --runTestsByPath src/voiceMicroDecisions.test.js src/quarterReadiness.test.js`
+  - Result: pass
+  - Key output:
+    - `PASS src/voiceMicroDecisions.test.js`
+    - `PASS src/quarterReadiness.test.js`
+    - `Tests: 7 passed, 7 total`
+- 2026-03-19 16:59:44+00:00: `npm run build`
+  - Result: pass
+  - Key output:
+    - `Compiled successfully.`
+- 2026-03-19 16:59:44+00:00: user verification request recorded
+  - Result: pending
+  - Key output: Please verify that selecting a finish-now blocker, speaking `Category: Travel` / `Business` / `Split 70%`, and tapping Undo behaves correctly in the visible inbox/quarter flow.
+
+# Risks/Notes
+- The visible voice triage flow is now selection-gated on inbox and quarter screens. If future UI changes remove or obscure the selected voice target, the frontend helper tests should be expanded before rollout.
+- Evidence commands remain context-bound to `evidence` rather than the inbox transaction flow. This task hardened their context safety and confirmation payloads, but it did not add a visible evidence-selection UI in the current frontend.
+- The direct `npm test` wrapper is not reliable in this sandbox because Jest worker spawning can fail with `EPERM`; use the documented `npx react-scripts test --runInBand ...` form for deterministic local verification here.
+- A prior duplicate lifecycle file exists under `workstream/300_complete`; this in-progress record is the active reconciliation file requested by the user and remains open until user verification is captured.
+
+# Completion Status
+- Awaiting user verification (2026-03-19 16:59:44+00:00)

codex
Implemented the Phase 6 reconciliation across backend and frontend. Voice micro-decisions now use the audited inbox classification path, preserve existing classification fields, return explicit context/undo metadata, and the UI only routes voice triage through `/voice/micro-decision` when a specific inbox or finish-now blocker is selected. The visible confirmation chip and edit/undo affordance were added in [App.jsx](/C:/Users/edebe/eds/bizPA/frontend/src/App.jsx), with shared client routing logic in [voiceMicroDecisions.js](/C:/Users/edebe/eds/bizPA/frontend/src/voiceMicroDecisions.js). Backend persistence was centralized in [inboxClassificationService.js](/C:/Users/edebe/eds/bizPA/backend/src/services/inboxClassificationService.js) and wired into [voiceController.js](/C:/Users/edebe/eds/bizPA/backend/src/controllers/voiceController.js) and [inboxController.js](/C:/Users/edebe/eds/bizPA/backend/src/controllers/inboxController.js). The lifecycle file was updated in [20260305_192700_bizpa_mvp_pivot_phase06_voice_micro_decisions.md](/C:/Users/edebe/eds/workstream/200_inprogress/codex/20260305_192700_bizpa_mvp_pivot_phase06_voice_micro_decisions.md).

Validation passed with `npm run verify:voice-micro-decisions`, `npm run verify:voice-capture`, `npx react-scripts test --watchAll=false --runInBand --runTestsByPath src/voiceMicroDecisions.test.js src/quarterReadiness.test.js`, and `npm run build`. The plain `npm test` wrapper hit a sandbox `spawn EPERM`, so I used direct `react-scripts` execution for the frontend tests.

User verification is still required before closing the task. Please check these and reply pass/fail for each:
1. Select a blocker on Inbox or Quarter, then speak `Category: Travel` and confirm the chip appears for that selected item only.
2. Speak `Business` or `Split 70%` on the same selected blocker and confirm the chip aggregates the applied actions.
3. Tap `Undo` from the chip and confirm the last voice-applied triage action is reversed.
file update:
diff --git a/backend/src/controllers/inboxController.js b/backend/src/controllers/inboxController.js
index e0b89da263df19a1cf03be9e86a4ea8c9301bc25..56733376b7e9d3aa5746c7bf27095de24b9071d2
--- a/backend/src/controllers/inboxController.js
+++ b/backend/src/controllers/inboxController.js
@@ -7,6 +7,9 @@
   recordReadinessRecalculated,
   recordStatusChange
 } = require('../services/businessEventLogService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
 
 const DEFAULT_USER_ID = '00000000-0000-0000-0000-000000000000';
 
@@ -200,62 +203,23 @@
   const client = await db.pool.connect();
   try {
     await client.query('BEGIN');
-    const existing = await client.query(
-      'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-    const previous = existing.rows[0] || null;
-
-    const upsert = await client.query(
-      `
-      INSERT INTO transaction_classifications
-      (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
-      ON CONFLICT (bank_txn_id)
-      DO UPDATE SET
-        category_code = EXCLUDED.category_code,
-        category_name = EXCLUDED.category_name,
-        business_personal = EXCLUDED.business_personal,
-        is_split = EXCLUDED.is_split,
-        split_business_pct = EXCLUDED.split_business_pct,
-        confidence = EXCLUDED.confidence,
-        source = EXCLUDED.source,
-        updated_at = CURRENT_TIMESTAMP
-      RETURNING *
-      `,
-      [userId, bankTxnId, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source]
-    );
-
-    await client.query(
-      `
-      INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
-      VALUES ($1,$2,$3,$4,$5,$6,$7)
-      `,
-      [userId, bankTxnId, userId, 'classification', previous, upsert.rows[0], source === 'manual' ? 'ui' : source]
-    );
-
-    const bankTxn = await client.query(
-      'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
-      [bankTxnId, userId]
-    );
-
-    await recordReadinessForTransaction(
-      client,
+    const upserted = await upsertTransactionClassification(client, {
       userId,
       bankTxnId,
-      bankTxn.rows[0]?.txn_date,
-      {
+      updates: {
         category_code,
+        category_name,
         business_personal,
         is_split,
         split_business_pct,
-        source
+        confidence,
+        source,
       },
-      `Inbox classification updated for transaction ${bankTxnId}`
-    );
+      auditSource: source === 'manual' ? 'ui' : source,
+    });
 
     await client.query('COMMIT');
-    return res.status(200).json(upsert.rows[0]);
+    return res.status(200).json(upserted);
   } catch (err) {
     await client.query('ROLLBACK');
     console.error('[InboxController] classifyTransaction failed:', err);
diff --git a/backend/src/controllers/voiceController.js b/backend/src/controllers/voiceController.js
index 0824f60f2bcf266fb41ac97449f4f0eb6add4188..e6d93b4bc1cd6960ce27238cfb7e90c3322f41e0
--- a/backend/src/controllers/voiceController.js
+++ b/backend/src/controllers/voiceController.js
@@ -10,6 +10,36 @@
   buildCaptureParseResult,
   toLegacyIntentShape
 } = require('../services/voiceCaptureParserService');
+const {
+  upsertTransactionClassification,
+} = require('../services/inboxClassificationService');
+const {
+  parseVoiceMicroDecision,
+  validateMicroDecisionContext,
+} = require('../services/voiceMicroDecisionService');
+
+function buildMicroDecisionResponse({
+  actionStatus,
+  confirmationChip,
+  appliedActions = [],
+  contextBinding = null,
+  undoSupported = false,
+  extra = {},
+}) {
+  return {
+    action_status: actionStatus,
+    confirmation_chip: confirmationChip,
+    applied_actions: appliedActions,
+    context_binding: contextBinding,
+    undoable: undoSupported,
+    undo: {
+      supported: undoSupported,
+      route: undoSupported ? '/api/v1/inbox/undo-last' : null,
+      method: undoSupported ? 'POST' : null,
+    },
+    ...extra,
+  };
+}
 
 /**
  * Simple Intent Parser and Slot Extractor (NLU Mock)
@@ -267,76 +297,177 @@
 
 const processMicroDecision = async (req, res) => {
   const userId = req.user?.id || '00000000-0000-0000-0000-000000000000';
-  const { transcript = '', bank_txn_id = null, evidence_id = null } = req.body || {};
-  const text = String(transcript).toLowerCase().trim();
+  const {
+    transcript = '',
+    bank_txn_id = null,
+    evidence_id = null,
+    context_kind = null,
+    context_scope = null,
+  } = req.body || {};
+  const rawTranscript = String(transcript).trim();
+  const text = rawTranscript.toLowerCase();
+  const normalizedContext = context_kind ?? context_scope;
+  const parsedCommand = parseVoiceMicroDecision(rawTranscript);
+  const contextValidation = validateMicroDecisionContext(parsedCommand, normalizedContext);
+  const normalizedContextScope = contextValidation.ok
+    ? contextValidation.context_kind
+    : (normalizedContext === null || normalizedContext === undefined
+      ? null
+      : String(normalizedContext).toLowerCase().replace(/-/g, '_'));
 
   if (!text) return res.status(400).json({ error: 'Missing transcript.' });
+  if (!contextValidation.ok) return res.status(contextValidation.statusCode).json({ error: contextValidation.error });
 
   try {
-    if (text.startsWith('category:')) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for category commands.' });
-      const categoryName = text.replace('category:', '').trim();
-      const categoryCode = categoryName.toUpperCase().replace(/[^A-Z0-9]+/g, '_');
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, category_code, category_name, source)
-        VALUES ($1,$2,$3,$4,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET category_code=EXCLUDED.category_code, category_name=EXCLUDED.category_name, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, categoryCode, categoryName]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${categoryName}` });
+    if (parsedCommand.type === 'category') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            category_code: parsedCommand.category_code,
+            category_name: parsedCommand.category_name,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice category micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'category', label: parsedCommand.category_name }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text === 'business' || text === 'personal') {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for business/personal commands.' });
-      const bp = text === 'business' ? 'BUSINESS' : 'PERSONAL';
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, business_personal, source)
-        VALUES ($1,$2,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET business_personal=EXCLUDED.business_personal, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, bp]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: ${bp}` });
+    if (parsedCommand.type === 'business_personal') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const bp = parsedCommand.business_personal;
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: bp,
+            is_split: false,
+            split_business_pct: null,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice ${bp.toLowerCase()} micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'business_personal', label: bp }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    const splitMatch = text.match(/split\s+(\d{1,3})%?/);
-    if (splitMatch) {
-      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for split commands.' });
-      const pct = Number(splitMatch[1]);
+    if (parsedCommand.type === 'split') {
+      if (!bank_txn_id) return res.status(400).json({ error: 'bank_txn_id is required for inbox voice commands.' });
+      const pct = Number(parsedCommand.split_business_pct);
       if (Number.isNaN(pct) || pct < 0 || pct > 100) {
         return res.status(400).json({ error: 'Split percentage must be 0-100.' });
       }
-      await db.query(
-        `
-        INSERT INTO transaction_classifications
-        (user_id, bank_txn_id, is_split, split_business_pct, source)
-        VALUES ($1,$2,true,$3,'manual')
-        ON CONFLICT (bank_txn_id) DO UPDATE
-        SET is_split=true, split_business_pct=EXCLUDED.split_business_pct, source='manual', updated_at=CURRENT_TIMESTAMP
-        `,
-        [userId, bank_txn_id, pct]
-      );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Split ${pct}%` });
+      const client = await db.pool.connect();
+      try {
+        await client.query('BEGIN');
+        await upsertTransactionClassification(client, {
+          userId,
+          bankTxnId: bank_txn_id,
+          updates: {
+            business_personal: 'BUSINESS',
+            is_split: true,
+            split_business_pct: pct,
+            source: 'voice',
+          },
+          auditSource: 'voice',
+          readinessDescription: `Voice split micro-decision applied for transaction ${bank_txn_id}`,
+        });
+        await client.query('COMMIT');
+      } catch (err) {
+        await client.query('ROLLBACK');
+        throw err;
+      } finally {
+        client.release();
+      }
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'split_business_pct', label: `Split ${pct}%` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'bank_transaction',
+          target_id: bank_txn_id,
+        },
+        undoSupported: true,
+        extra: {
+          bank_txn_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text.includes('attach receipt')) {
-      return res.status(200).json({
-        action_status: 'clarification_needed',
-        confirmation_chip: 'Attach receipt requested',
-        instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
-      });
+    if (parsedCommand.type === 'attach_receipt') {
+      if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for attach receipt commands.' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'clarification_needed',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'attach_receipt', label: 'Attach receipt' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          parsed_command: parsedCommand,
+          instruction: 'Use /api/v1/evidence/upload with file + metadata to attach receipt.'
+        }
+      }));
     }
 
-    if (['match first', 'match second', 'match third'].includes(text)) {
+    if (parsedCommand.type === 'match_rank') {
       if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for match commands.' });
-      const index = text === 'match first' ? 1 : text === 'match second' ? 2 : 3;
+      const index = parsedCommand.match_rank;
       const suggestions = await db.query(
         `
         WITH ev AS (
@@ -364,10 +495,24 @@
         `,
         [userId, evidence_id, chosen]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: `Applied: Match ${index}` });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'evidence_match', label: `Match ${index}` }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          bank_txn_id: chosen,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
-    if (text === 'no match') {
+    if (parsedCommand.type === 'no_match') {
       if (!evidence_id) return res.status(400).json({ error: 'evidence_id is required for no match.' });
       await db.query(
         `
@@ -376,7 +521,20 @@
         `,
         [userId, evidence_id]
       );
-      return res.status(200).json({ action_status: 'execute', confirmation_chip: 'Applied: No match' });
+      return res.status(200).json(buildMicroDecisionResponse({
+        actionStatus: 'execute',
+        confirmationChip: parsedCommand.confirmation_chip,
+        appliedActions: [{ type: 'evidence_match', label: 'No match' }],
+        contextBinding: {
+          scope: normalizedContextScope,
+          target_type: 'evidence',
+          target_id: evidence_id,
+        },
+        extra: {
+          evidence_id,
+          parsed_command: parsedCommand,
+        },
+      }));
     }
 
     return res.status(400).json({ error: 'Unsupported MVP voice micro-decision command.' });
diff --git a/backend/src/services/inboxClassificationService.js b/backend/src/services/inboxClassificationService.js
new file mode 100644
index 0000000000000000000000000000000000000000..23b669283b7c4f11972481998a15bd6a52cdbf1e
--- /dev/null
+++ b/backend/src/services/inboxClassificationService.js
@@ -0,0 +1,114 @@
+const {
+  deriveQuarterFromDate,
+  recordReadinessRecalculated,
+} = require('./businessEventLogService');
+
+async function recordReadinessForTransaction(executor, userId, bankTxnId, txnDate, summary, description) {
+  await recordReadinessRecalculated(executor, {
+    user_id: userId,
+    actor_id: userId,
+    source_type: summary?.source || 'manual',
+    quarter_reference: deriveQuarterFromDate(txnDate),
+    entity_id: bankTxnId,
+    entity_type: 'bank_transaction',
+    description,
+    metadata: summary,
+  });
+}
+
+async function upsertTransactionClassification(executor, {
+  userId,
+  bankTxnId,
+  changedBy = userId,
+  updates = {},
+  auditSource = 'ui',
+  readinessDescription = null,
+}) {
+  const existing = await executor.query(
+    'SELECT * FROM transaction_classifications WHERE bank_txn_id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+  const previous = existing.rows[0] || null;
+
+  const next = {
+    category_code: updates.category_code !== undefined ? updates.category_code : previous?.category_code || null,
+    category_name: updates.category_name !== undefined ? updates.category_name : previous?.category_name || null,
+    business_personal: updates.business_personal !== undefined ? updates.business_personal : previous?.business_personal || null,
+    is_split: updates.is_split !== undefined ? Boolean(updates.is_split) : Boolean(previous?.is_split),
+    split_business_pct: null,
+    confidence: updates.confidence !== undefined ? updates.confidence : previous?.confidence ?? null,
+    source: updates.source !== undefined ? updates.source : previous?.source || 'manual',
+  };
+
+  if (next.is_split) {
+    if (updates.split_business_pct !== undefined) {
+      next.split_business_pct = updates.split_business_pct;
+    } else {
+      next.split_business_pct = previous?.split_business_pct ?? null;
+    }
+  }
+
+  const upsert = await executor.query(
+    `
+    INSERT INTO transaction_classifications
+    (user_id, bank_txn_id, category_code, category_name, business_personal, is_split, split_business_pct, confidence, source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
+    ON CONFLICT (bank_txn_id)
+    DO UPDATE SET
+      category_code = EXCLUDED.category_code,
+      category_name = EXCLUDED.category_name,
+      business_personal = EXCLUDED.business_personal,
+      is_split = EXCLUDED.is_split,
+      split_business_pct = EXCLUDED.split_business_pct,
+      confidence = EXCLUDED.confidence,
+      source = EXCLUDED.source,
+      updated_at = CURRENT_TIMESTAMP
+    RETURNING *
+    `,
+    [
+      userId,
+      bankTxnId,
+      next.category_code,
+      next.category_name,
+      next.business_personal,
+      next.is_split,
+      next.split_business_pct,
+      next.confidence,
+      next.source,
+    ]
+  );
+
+  await executor.query(
+    `
+    INSERT INTO transaction_audit_log (user_id, bank_txn_id, changed_by, field_name, previous_value, new_value, change_source)
+    VALUES ($1,$2,$3,$4,$5,$6,$7)
+    `,
+    [userId, bankTxnId, changedBy, 'classification', previous, upsert.rows[0], auditSource]
+  );
+
+  const bankTxn = await executor.query(
+    'SELECT txn_date FROM bank_transactions WHERE id = $1 AND user_id = $2',
+    [bankTxnId, userId]
+  );
+
+  await recordReadinessForTransaction(
+    executor,
+    userId,
+    bankTxnId,
+    bankTxn.rows[0]?.txn_date,
+    {
+      category_code: upsert.rows[0].category_code,
+      business_personal: upsert.rows[0].business_personal,
+      is_split: upsert.rows[0].is_split,
+      split_business_pct: upsert.rows[0].split_business_pct,
+      source: upsert.rows[0].source,
+    },
+    readinessDescription || `Inbox classification updated for transaction ${bankTxnId}`
+  );
+
+  return upsert.rows[0];
+}
+
+module.exports = {
+  upsertTransactionClassification,
+};
diff --git a/backend/verify_voice_micro_decisions.js b/backend/verify_voice_micro_decisions.js
index 25d9ab02050aac37a5d38aca5c6f0154de9bb787..15c93efa65f133ec1a59d827ecbfa29a95cf4e6d
--- a/backend/verify_voice_micro_decisions.js
+++ b/backend/verify_voice_micro_decisions.js
@@ -9,16 +9,70 @@
       { bank_txn_id: 'txn-2' },
       { bank_txn_id: 'txn-3' }
     ];
+    this.classifications = new Map();
+    this.auditEntries = [];
   }
 
-  async query(text, params) {
+  get pool() {
+    return {
+      connect: async () => ({
+        query: this.query.bind(this),
+        release() {},
+      }),
+    };
+  }
+
+  async query(text, params = []) {
     const sql = text.replace(/\s+/g, ' ').trim();
     this.calls.push({ sql, params });
 
+    if (sql === 'BEGIN' || sql === 'COMMIT' || sql === 'ROLLBACK') {
+      return { rows: [] };
+    }
+
+    if (sql.includes('SELECT * FROM transaction_classifications')) {
+      const existing = this.classifications.get(params[0]);
+      return { rows: existing ? [existing] : [] };
+    }
+
+    if (sql.includes('INSERT INTO transaction_classifications')) {
+      const next = {
+        user_id: params[0],
+        bank_txn_id: params[1],
+        category_code: params[2],
+        category_name: params[3],
+        business_personal: params[4],
+        is_split: params[5],
+        split_business_pct: params[6],
+        confidence: params[7],
+        source: params[8],
+      };
+      this.classifications.set(params[1], next);
+      return { rows: [next] };
+    }
+
+    if (sql.includes('INSERT INTO transaction_audit_log')) {
+      this.auditEntries.push({
+        bank_txn_id: params[1],
+        previous_value: params[4],
+        new_value: params[5],
+        change_source: params[6],
+      });
+      return { rows: [] };
+    }
+
+    if (sql.includes('SELECT txn_date FROM bank_transactions')) {
+      return { rows: [{ txn_date: '2026-03-12' }] };
+    }
+
     if (sql.includes('FROM bank_transactions bt CROSS JOIN ev')) {
       return { rows: this.matchSuggestions };
     }
 
+    if (sql.includes('INSERT INTO evidence_links')) {
+      return { rows: [] };
+    }
+
     return { rows: [] };
   }
 }
@@ -69,45 +123,79 @@
   const mockDb = new MockDb();
   const controller = loadController(mockDb);
 
+  const missingContext = await invoke(controller, {
+    transcript: 'Business',
+    bank_txn_id: 'txn-123'
+  });
+  assert.strictEqual(missingContext.statusCode, 400);
+  assert.strictEqual(missingContext.body.error, 'context_kind is required for MVP voice micro-decisions.');
+
   const category = await invoke(controller, {
     transcript: 'Category: Travel',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(category.statusCode, 200);
-  assert.strictEqual(category.body.confirmation_chip, 'Applied: travel');
-  assert(
-    mockDb.calls.some((call) => call.sql.includes('INSERT INTO transaction_classifications') && call.params[2] === 'TRAVEL' && call.params[3] === 'travel'),
-    'Expected category command to persist manual classification'
-  );
+  assert.strictEqual(category.body.confirmation_chip, 'Applied: Travel');
+  assert.strictEqual(category.body.undo.supported, true);
+  assert.deepStrictEqual(category.body.context_binding, {
+    scope: 'inbox',
+    target_type: 'bank_transaction',
+    target_id: 'txn-123'
+  });
+  assert.strictEqual(mockDb.classifications.get('txn-123').category_code, 'TRAVEL');
+  assert.strictEqual(mockDb.auditEntries.at(-1).change_source, 'voice');
 
   const business = await invoke(controller, {
     transcript: 'Business',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(business.statusCode, 200);
   assert.strictEqual(business.body.confirmation_chip, 'Applied: BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').category_code, 'TRAVEL');
+  assert.strictEqual(mockDb.classifications.get('txn-123').business_personal, 'BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').is_split, false);
 
   const split = await invoke(controller, {
     transcript: 'Split 70%',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(split.statusCode, 200);
   assert.strictEqual(split.body.confirmation_chip, 'Applied: Split 70%');
+  assert.strictEqual(split.body.undo.supported, true);
+  assert.strictEqual(mockDb.classifications.get('txn-123').business_personal, 'BUSINESS');
+  assert.strictEqual(mockDb.classifications.get('txn-123').is_split, true);
+  assert.strictEqual(mockDb.classifications.get('txn-123').split_business_pct, 70);
 
   const attachReceipt = await invoke(controller, {
     transcript: 'Attach receipt',
-    bank_txn_id: 'txn-123'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(attachReceipt.statusCode, 200);
   assert.strictEqual(attachReceipt.body.action_status, 'clarification_needed');
   assert.strictEqual(attachReceipt.body.confirmation_chip, 'Attach receipt requested');
+  assert.strictEqual(attachReceipt.body.undo.supported, false);
+  assert.deepStrictEqual(attachReceipt.body.context_binding, {
+    scope: 'evidence',
+    target_type: 'evidence',
+    target_id: 'evidence-1'
+  });
 
   const matchSecond = await invoke(controller, {
     transcript: 'Match second',
-    evidence_id: 'evidence-1'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(matchSecond.statusCode, 200);
   assert.strictEqual(matchSecond.body.confirmation_chip, 'Applied: Match 2');
+  assert.deepStrictEqual(matchSecond.body.context_binding, {
+    scope: 'evidence',
+    target_type: 'evidence',
+    target_id: 'evidence-1'
+  });
   assert(
     mockDb.calls.some((call) => call.sql.includes('INSERT INTO evidence_links') && call.params[1] === 'evidence-1' && call.params[2] === 'txn-2'),
     'Expected match command to persist selected evidence link'
@@ -115,29 +203,41 @@
 
   const noMatch = await invoke(controller, {
     transcript: 'No match',
-    evidence_id: 'evidence-1'
+    evidence_id: 'evidence-1',
+    context_kind: 'evidence'
   });
   assert.strictEqual(noMatch.statusCode, 200);
   assert.strictEqual(noMatch.body.confirmation_chip, 'Applied: No match');
 
   const invalidSplit = await invoke(controller, {
     transcript: 'Split 140%',
-    bank_txn_id: 'txn-123'
+    bank_txn_id: 'txn-123',
+    context_kind: 'inbox'
   });
   assert.strictEqual(invalidSplit.statusCode, 400);
   assert.strictEqual(invalidSplit.body.error, 'Split percentage must be 0-100.');
 
+  const unsafeContext = await invoke(controller, {
+    transcript: 'Category: Travel',
+    bank_txn_id: 'txn-123',
+    context_kind: 'evidence'
+  });
+  assert.strictEqual(unsafeContext.statusCode, 409);
+  assert.strictEqual(unsafeContext.body.error, 'Command "category" is not allowed for evidence context.');
+
   const unsupported = await invoke(controller, {
-    transcript: 'Archive this forever'
+    transcript: 'Archive this forever',
+    context_kind: 'inbox'
   });
   assert.strictEqual(unsupported.statusCode, 400);
   assert.strictEqual(unsupported.body.error, 'Unsupported MVP voice micro-decision command.');
 
   console.log('voice_micro_decisions_ok');
-  console.log('category_business_split_ok=true');
+  console.log('context_guardrails_ok=true');
+  console.log('audited_classification_updates_ok=true');
   console.log('attach_receipt_clarification_ok=true');
   console.log('match_selection_ok=true');
-  console.log('no_match_ok=true');
+  console.log('safe_context_binding_ok=true');
   console.log('validation_errors_ok=true');
 }
 
diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index 9b145fadac9123e3fa01b12af6ef2b9916dc11de..590be3d00c08cb02d6139abdd4d1a0ee41f9de7b
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -50,6 +50,16 @@
   buildQuarterUiState,
   deriveResolvedIssueIdsFromSearchParams,
 } from './quarterReadiness';
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+import {
+  applyInboxVoiceCommandToDraft,
+  buildInboxVoiceChip,
+  parseInboxVoiceCommand,
+} from './voiceMicroDecision';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -427,6 +437,13 @@
   .date-group-count { border-radius: 999px; padding: 4px 9px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.72rem; color: var(--text-muted); }
   .date-group-body { padding: 0 12px 12px; }
   .voice-preview { background: rgba(0,0,0,0.82); color: white; padding: 8px 15px; border-radius: 15px; margin-bottom: 8px; font-size: 0.72rem; max-width: 240px; text-align: right; }
+  .voice-context-banner { display: grid; gap: 10px; padding: 14px 16px; margin-bottom: 16px; border-radius: 18px; background: var(--bg-card); border: 1px solid var(--border); box-shadow: var(--shadow); }
+  .voice-context-row, .voice-chip-actions, .voice-chip-labels { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
+  .voice-context-pill, .voice-action-pill { border-radius: 999px; padding: 6px 10px; background: var(--chip-bg); border: 1px solid var(--border); font-size: 0.74rem; font-weight: 700; }
+  .voice-context-pill.active { background: rgba(13,92,99,0.12); color: var(--primary-strong); border-color: rgba(13,92,99,0.24); }
+  .voice-micro-chip { display: grid; gap: 10px; padding: 14px 16px; border-radius: 18px; background: rgba(13,92,99,0.08); border: 1px solid rgba(13,92,99,0.18); }
+  .voice-micro-chip strong { display: block; }
+  .inbox-action-card.voice-target { border-color: rgba(13,92,99,0.36); box-shadow: 0 0 0 2px rgba(13,92,99,0.12), var(--shadow); }
   .spinner { animation: rotate 2s linear infinite; }
   .capture-grid { display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(320px, 0.95fr); gap: 18px; align-items: start; }
   .command-centre { display: grid; gap: 18px; }
@@ -1318,6 +1335,8 @@
     && searchParams?.get('snapshotDemo') === '1';
   const inboxDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('inboxDemo') === '1';
+  const voiceDemoCommand = searchParams?.get('voiceDemoCommand') || '';
+  const voiceDemoTarget = searchParams?.get('voiceDemoTarget') || '';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1382,9 +1401,12 @@
   const [inboxDrafts, setInboxDrafts] = useState({});
   const [inboxBusyId, setInboxBusyId] = useState('');
   const [inboxMessage, setInboxMessage] = useState(null);
+  const [activeVoiceContextId, setActiveVoiceContextId] = useState(voiceDemoTarget);
+  const [voiceMicroDecisionChip, setVoiceMicroDecisionChip] = useState(null);
   const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
+  const voiceDemoSeedRef = useRef('');
   const quarterStart = '2026-01-01';
   const quarterEnd = '2026-03-31';
 
@@ -1606,10 +1628,80 @@
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
   const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const activeVoiceCard = findVoiceContextCard(inboxCards, activeVoiceContextId);
   const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
   const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
   const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
+  const applyVoiceMicroDecisionLocally = (card, parsedCommand, responseLike = {}) => {
+    if (!card || !parsedCommand) {
+      return;
+    }
+
+    const nextDraft = applyInboxVoiceCommandToDraft(inboxDrafts[card.id] || buildInboxDraft(card), parsedCommand);
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: nextDraft,
+    }));
+
+    if (parsedCommand.type === 'category') {
+      syncInboxCardLocally(card.id, {
+        category_code: nextDraft.category_code,
+        category_name: nextDraft.category_name,
+      });
+    } else if (parsedCommand.type === 'business_personal') {
+      syncInboxCardLocally(card.id, {
+        business_personal: nextDraft.business_personal,
+        is_split: false,
+        split_business_pct: null,
+      });
+    } else if (parsedCommand.type === 'split') {
+      syncInboxCardLocally(card.id, {
+        business_personal: 'BUSINESS',
+        is_split: true,
+        split_business_pct: Number(parsedCommand.split_business_pct),
+      });
+    }
+
+    setVoiceMicroDecisionChip((previous) => reduceVoiceMicroDecisionChip(previous, {
+      ...responseLike,
+      action_status: responseLike.action_status || 'execute',
+      confirmation_chip: responseLike.confirmation_chip || parsedCommand.confirmation_chip,
+      applied_actions: responseLike.applied_actions || [{ label: parsedCommand.confirmation_chip.replace(/^Applied:\s*/, '') }],
+      undo: responseLike.undo || { supported: true },
+    }, card));
+  };
+
+  useEffect(() => {
+    if (!activeVoiceContextId) {
+      return;
+    }
+    if (!findVoiceContextCard(inboxCards, activeVoiceContextId)) {
+      setActiveVoiceContextId('');
+    }
+  }, [activeVoiceContextId, inboxCards]);
+
+  useEffect(() => {
+    if (!inboxDemoEnabled || !voiceDemoCommand || !activeVoiceCard) {
+      return;
+    }
+    const seedKey = `${activeVoiceCard.id}:${voiceDemoCommand}`;
+    if (voiceDemoSeedRef.current === seedKey) {
+      return;
+    }
+    const parsedCommand = parseInboxVoiceCommand(voiceDemoCommand);
+    if (!parsedCommand) {
+      return;
+    }
+    voiceDemoSeedRef.current = seedKey;
+    applyVoiceMicroDecisionLocally(activeVoiceCard, parsedCommand, {
+      action_status: 'execute',
+      confirmation_chip: parsedCommand.confirmation_chip,
+      applied_actions: [{ label: parsedCommand.confirmation_chip.replace(/^Applied:\s*/, '') }],
+      undo: { supported: true },
+    });
+  }, [activeVoiceCard, inboxDemoEnabled, voiceDemoCommand]);
+
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
   const canManageGovernance = hasPermission(currentUser, PERMISSIONS.GOVERNANCE_MANAGE);
@@ -1802,15 +1894,18 @@
     clearInboxMessage();
     try {
       setInboxBusyId('undo-last');
+      setVoiceMicroDecisionChip(null);
       if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxDrafts({});
+        setVoiceMicroDecisionChip(null);
         setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
         return;
       }
       await axios.post(`${API_BASE_URL}/inbox/undo-last`);
       await fetchAllData();
+      setVoiceMicroDecisionChip(null);
       setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
     } catch (error) {
       setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
@@ -1991,22 +2086,78 @@
     const cleanText = text.replace(/┬ú/g, 'GBP ');
     try {
       setVoiceStatus('processing');
-      const response = await axios.post(`${API_BASE_URL}/voice/process`, {
+      const microDecisionRequest = buildVoiceMicroDecisionRequest({
+        currentTab,
         transcript: cleanText,
-        device_id: 'mobile-v139',
-        current_date: new Date().toISOString(),
+        activeVoiceContextId,
+        inboxCards,
       });
-      if (response.data.confirmation_text && window.SpeechSynthesisUtterance) {
+
+      if (microDecisionRequest?.error) {
+        setInboxMessage({ type: 'error', text: microDecisionRequest.error });
+        setCurrentTab('inbox');
+        return;
+      }
+
+      let response;
+      if (microDecisionRequest) {
+        if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+          const parsedCommand = parseInboxVoiceCommand(cleanText);
+          if (!parsedCommand) {
+            throw new Error('Only Category, Business, Personal, and Split {n}% are available in the local voice triage demo.');
+          }
+          applyVoiceMicroDecisionLocally(microDecisionRequest.card, parsedCommand, {
+            action_status: 'execute',
+            confirmation_chip: parsedCommand.confirmation_chip,
+            applied_actions: [{ label: parsedCommand.confirmation_chip.replace(/^Applied:\s*/, '') }],
+            undo: { supported: true },
+          });
+          response = { data: buildInboxVoiceChip({
+            action_status: 'execute',
+            confirmation_chip: parsedCommand.confirmation_chip,
+            undoable: true,
+            bank_txn_id: microDecisionRequest.card.id,
+          }, microDecisionRequest.card) };
+          response.data.confirmation_chip = parsedCommand.confirmation_chip;
+        } else {
+          response = await axios.post(`${API_BASE_URL}${microDecisionRequest.endpoint}`, microDecisionRequest.payload);
+        }
+      } else {
+        response = await axios.post(`${API_BASE_URL}/voice/process`, {
+          transcript: cleanText,
+          device_id: 'mobile-v139',
+          current_date: new Date().toISOString(),
+        });
+      }
+      const spokenConfirmation = response.data.confirmation_chip || response.data.confirmation_text;
+      if (spokenConfirmation && window.SpeechSynthesisUtterance) {
         try {
-          const utterance = new window.SpeechSynthesisUtterance(response.data.confirmation_text);
+          const utterance = new window.SpeechSynthesisUtterance(spokenConfirmation);
           window.speechSynthesis.speak(utterance);
         } catch (_) {
         }
       }
-      executeVoiceAction(response.data);
-      fetchAllData();
+      if (microDecisionRequest) {
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          setVoiceMicroDecisionChip((previous) => reduceVoiceMicroDecisionChip(previous, response.data, microDecisionRequest.card));
+          setInboxMessage({
+            type: 'success',
+            text: response.data.confirmation_chip || 'Voice triage action applied.',
+          });
+        }
+      } else {
+        executeVoiceAction(response.data);
+      }
+      if (!(microDecisionRequest && (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled))) {
+        await fetchAllData();
+      }
     } catch (error) {
-      alert('AI Error');
+      const message = error?.response?.data?.error || error?.message || 'AI Error';
+      if (currentTab === 'inbox' || currentTab === 'quarter') {
+        setInboxMessage({ type: 'error', text: message });
+      } else {
+        alert(message);
+      }
     } finally {
       setVoiceStatus('idle');
       setIsListening(false);
@@ -2894,7 +3045,11 @@
             const saving = inboxBusyId === card.id;
             const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
             return (
-              <section key={card.id} className="inbox-action-card">
+              <section
+                key={card.id}
+                className={`inbox-action-card ${activeVoiceContextId === card.id ? 'voice-target' : ''}`}
+                onClick={() => setActiveVoiceContextId(card.id)}
+              >
                 <div className="inbox-card-top">
                   <div>
                     <strong>{card.merchant}</strong>
@@ -2914,6 +3069,9 @@
                       {label}
                     </span>
                   ))}
+                  {activeVoiceContextId === card.id && (
+                    <span className="voice-context-pill active">Voice target</span>
+                  )}
                   {card.suggested_category?.category_name && (
                     <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
                       <CheckCircle2 size={14} />
@@ -3357,6 +3515,9 @@
   };
 
   const openReadinessWorkflow = (issue) => {
+    if (issue?.affected_entity_id) {
+      setActiveVoiceContextId(String(issue.affected_entity_id));
+    }
     if (issue?.resolution_target?.workflow === '/inbox/finish-now') {
       setCurrentTab('inbox');
       return;
@@ -3364,6 +3525,76 @@
     setCurrentTab('inbox');
   };
 
+  const renderVoiceContextBanner = () => {
+    if (!(currentTab === 'inbox' || currentTab === 'quarter')) {
+      return null;
+    }
+
+    return (
+      <section className="voice-context-banner">
+        <div>
+          <strong>Voice triage</strong>
+          <div className="small-muted">
+            Use the mic against one selected finish-now item only. Supported commands here are category, business, personal, split n%, and attach receipt.
+          </div>
+        </div>
+        <div className="voice-context-row">
+          <span className={`voice-context-pill ${activeVoiceCard ? 'active' : ''}`}>
+            {activeVoiceCard
+              ? `Target: ${activeVoiceCard.merchant} (${activeVoiceCard.id})`
+              : 'Select a blocker card to arm voice triage'}
+          </span>
+          {activeVoiceCard && (
+            <button className="secondary-action" type="button" onClick={() => setCurrentTab('inbox')}>
+              Edit target
+            </button>
+          )}
+        </div>
+        {voiceMicroDecisionChip && (
+          <div className="voice-micro-chip">
+            <div>
+              <strong>{voiceMicroDecisionChip.confirmationChip}</strong>
+              <div className="small-muted">
+                {voiceMicroDecisionChip.targetLabel} ({voiceMicroDecisionChip.targetId})
+              </div>
+            </div>
+            {voiceMicroDecisionChip.labels.length > 0 && (
+              <div className="voice-chip-labels">
+                {voiceMicroDecisionChip.labels.map((label) => (
+                  <span key={`${voiceMicroDecisionChip.targetId}-${label}`} className="voice-action-pill">
+                    {label}
+                  </span>
+                ))}
+              </div>
+            )}
+            <div className="voice-chip-actions">
+              <button
+                className="secondary-action"
+                type="button"
+                onClick={() => {
+                  setCurrentTab('inbox');
+                  setActiveVoiceContextId(voiceMicroDecisionChip.targetId);
+                }}
+              >
+                Edit
+              </button>
+              {voiceMicroDecisionChip.undoSupported && (
+                <button
+                  className="secondary-action"
+                  type="button"
+                  onClick={undoLastInboxAction}
+                  disabled={inboxBusyId === 'undo-last'}
+                >
+                  Undo
+                </button>
+              )}
+            </div>
+          </div>
+        )}
+      </section>
+    );
+  };
+
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
@@ -3654,6 +3885,13 @@
                     </div>
                   </div>
                   <div className="finish-now-actions">
+                    <button
+                      className="secondary-action"
+                      type="button"
+                      onClick={() => setActiveVoiceContextId(String(issue.affected_entity_id))}
+                    >
+                      {activeVoiceContextId === String(issue.affected_entity_id) ? 'Voice target set' : 'Arm voice'}
+                    </button>
                     {(readinessDemoEnabled || snapshotDemoEnabled) ? (
                       <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
                         Mark fixed
@@ -4391,6 +4629,7 @@
         </button>
       </section>
       {connectionError && <div className="alert alert-danger">{connectionError}</div>}
+      {renderVoiceContextBanner()}
 
       {loading ? (
         <div className="text-center py-5">Loading...</div>
diff --git a/frontend/src/voiceMicroDecisions.js b/frontend/src/voiceMicroDecisions.js
new file mode 100644
index 0000000000000000000000000000000000000000..506f3e983a3f1159472e2460767a62676307ad25
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.js
@@ -0,0 +1,66 @@
+const VOICE_MICRO_DECISION_TABS = new Set(['inbox', 'quarter']);
+
+function findVoiceContextCard(inboxCards = [], activeVoiceContextId = '') {
+  if (!activeVoiceContextId) {
+    return null;
+  }
+  return inboxCards.find((card) => String(card.id) === String(activeVoiceContextId)) || null;
+}
+
+function buildVoiceMicroDecisionRequest({
+  currentTab,
+  transcript,
+  activeVoiceContextId,
+  inboxCards = [],
+}) {
+  if (!VOICE_MICRO_DECISION_TABS.has(currentTab)) {
+    return null;
+  }
+
+  const card = findVoiceContextCard(inboxCards, activeVoiceContextId);
+  if (!card) {
+    return {
+      error: 'Select a finish-now item before using voice triage.',
+      card: null,
+    };
+  }
+
+  return {
+    endpoint: '/voice/micro-decision',
+    card,
+    payload: {
+      transcript,
+      bank_txn_id: card.id,
+      context_scope: currentTab === 'quarter' ? 'finish_now' : 'inbox',
+    },
+  };
+}
+
+function reduceVoiceMicroDecisionChip(previousChip, response = {}, card = null) {
+  if (!card) {
+    return previousChip;
+  }
+
+  const nextLabels = Array.isArray(response.applied_actions)
+    ? response.applied_actions.map((action) => action?.label).filter(Boolean)
+    : [];
+  const previousLabels = previousChip?.targetId === card.id && Array.isArray(previousChip.labels)
+    ? previousChip.labels
+    : [];
+  const labels = Array.from(new Set([...previousLabels, ...nextLabels]));
+
+  return {
+    targetId: card.id,
+    targetLabel: card.merchant || 'Selected blocker',
+    confirmationChip: response.confirmation_chip || '',
+    labels,
+    actionStatus: response.action_status || 'execute',
+    undoSupported: Boolean(response.undo?.supported),
+  };
+}
+
+export {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+};
diff --git a/frontend/src/voiceMicroDecisions.test.js b/frontend/src/voiceMicroDecisions.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..6eb469fcc7f023f8545d9e952a5823fb8c47a88d
--- /dev/null
+++ b/frontend/src/voiceMicroDecisions.test.js
@@ -0,0 +1,74 @@
+import {
+  buildVoiceMicroDecisionRequest,
+  findVoiceContextCard,
+  reduceVoiceMicroDecisionChip,
+} from './voiceMicroDecisions';
+
+const cards = [
+  { id: 'txn-1', merchant: 'Fuel Stop' },
+  { id: 'txn-2', merchant: 'Builder Depot' },
+];
+
+describe('voice micro-decision helpers', () => {
+  test('requires explicit finish-now selection before routing voice triage', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'inbox',
+      transcript: 'Business',
+      activeVoiceContextId: '',
+      inboxCards: cards,
+    });
+
+    expect(request.error).toBe('Select a finish-now item before using voice triage.');
+    expect(request.card).toBeNull();
+  });
+
+  test('builds a finish-now scoped request for the selected blocker', () => {
+    const request = buildVoiceMicroDecisionRequest({
+      currentTab: 'quarter',
+      transcript: 'Split 70%',
+      activeVoiceContextId: 'txn-2',
+      inboxCards: cards,
+    });
+
+    expect(request.endpoint).toBe('/voice/micro-decision');
+    expect(request.card).toEqual(cards[1]);
+    expect(request.payload).toEqual({
+      transcript: 'Split 70%',
+      bank_txn_id: 'txn-2',
+      context_scope: 'finish_now',
+    });
+  });
+
+  test('keeps the selected voice target discoverable', () => {
+    expect(findVoiceContextCard(cards, 'txn-1')).toEqual(cards[0]);
+    expect(findVoiceContextCard(cards, 'missing')).toBeNull();
+  });
+
+  test('combines applied actions for the same target chip and resets for a new target', () => {
+    const first = reduceVoiceMicroDecisionChip(null, {
+      confirmation_chip: 'Applied: Travel',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Travel' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const second = reduceVoiceMicroDecisionChip(first, {
+      confirmation_chip: 'Applied: BUSINESS',
+      action_status: 'execute',
+      applied_actions: [{ label: 'BUSINESS' }],
+      undo: { supported: true },
+    }, cards[0]);
+
+    const third = reduceVoiceMicroDecisionChip(second, {
+      confirmation_chip: 'Applied: Split 70%',
+      action_status: 'execute',
+      applied_actions: [{ label: 'Split 70%' }],
+      undo: { supported: true },
+    }, cards[1]);
+
+    expect(second.labels).toEqual(['Travel', 'BUSINESS']);
+    expect(second.undoSupported).toBe(true);
+    expect(third.targetId).toBe('txn-2');
+    expect(third.labels).toEqual(['Split 70%']);
+  });
+});
diff --git a/workstream/200_inprogress/codex/20260305_192700_bizpa_mvp_pivot_phase06_voice_micro_decisions.md b/workstream/200_inprogress/codex/20260305_192700_bizpa_mvp_pivot_phase06_voice_micro_decisions.md
index cf85796e760112547a34bd77ea2a0f0a86fd3219..c02d5e36a241a1c98d7e97bf7c319cce1837c2ce
--- a/workstream/200_inprogress/codex/20260305_192700_bizpa_mvp_pivot_phase06_voice_micro_decisions.md
+++ b/workstream/200_inprogress/codex/20260305_192700_bizpa_mvp_pivot_phase06_voice_micro_decisions.md
@@ -1,43 +1,136 @@
-# Source
-- Derived from backlog: `C:\Users\edebe\eds\workstream\000_backlog\mvp_prd_quarterly_export_10min.md`
-- Decomposed from checklist: `C:\Users\edebe\eds\workstream\300_complete\20260305_191657_bizpa_mvp_quarterly_pivot_full_delivery_checklist.md`
-
-# Task Summary
-Constrain voice intents to PRD micro-decisions and bind safely to inbox/finish-now context with confirmation chip + undo.
-
-# Context
-- Affected area: voice controller/parser and web/mobile voice UX
-
-# Plan
-- [ ] 1. Implement phase scope in code and configuration.
-  - [ ] Test: Execute phase-specific implementation checks and verify expected behavior change.
-  - [ ] Evidence: pending
-- [ ] 2. Add/adjust automated tests for this phase.
-  - [ ] Test: Run targeted unit/integration tests for new behavior and ensure green results.
-  - [ ] Evidence: pending
-- [ ] 3. Validate against PRD acceptance criteria impacted by this phase.
-  - [ ] Test: Run intent contract tests for supported utterances and verify each action is applied, confirmed, and undoable.
-  - [ ] Evidence: pending
-- [ ] 4. Update technical notes and rollout caveats for downstream phases.
-  - [ ] Test: Confirm lifecycle doc lists changed files, commands run, and known risks.
-  - [ ] Evidence: pending
-
-# Implementation Log
-- 2026-03-05 Created decomposed task file from approved MVP pivot checklist.
-
-# Changes Made
-- Task file created; implementation not started.
-
-# Validation
-- Pending.
-
-# Risks/Notes
-- Must follow strict sequential completion and evidence capture before starting dependent phase.
-
-# Completion Status
-- Todo.
-
-
-## Retry History
-Retry-Count: 1
-- Retry scheduled at 2026-03-18 17:21:29
+# Source
+- Derived from backlog: `C:\Users\edebe\eds\workstream\000_epic\20260305_185316_mvp_prd_quarterly_export_10min.md`
+- Decomposed from checklist: `C:\Users\edebe\eds\workstream\300_complete\20260305_191657_bizpa_mvp_quarterly_pivot_full_delivery_checklist.md`
+
+# Task Summary
+Constrain voice intents to PRD micro-decisions and bind them safely to inbox/finish-now context with a visible confirmation chip and undo-compatible triage flow.
+
+# Context
+- Affected backend area: `C:\Users\edebe\eds\bizPA\backend\src\controllers\voiceController.js`, `C:\Users\edebe\eds\bizPA\backend\src\controllers\inboxController.js`
+- New backend support: `C:\Users\edebe\eds\bizPA\backend\src\services\inboxClassificationService.js`, `C:\Users\edebe\eds\bizPA\backend\verify_voice_micro_decisions.js`
+- Affected frontend area: `C:\Users\edebe\eds\bizPA\frontend\src\App.jsx`
+- New frontend support: `C:\Users\edebe\eds\bizPA\frontend\src\voiceMicroDecisions.js`, `C:\Users\edebe\eds\bizPA\frontend\src\voiceMicroDecisions.test.js`
+
+# Dependency
+`C:\Users\edebe\eds\workstream\300_complete\20260305_191657_bizpa_mvp_quarterly_pivot_full_delivery_checklist.md`
+
+# Plan
+- [x] 1. Implement phase scope in code and configuration.
+  - [x] Test: Execute the updated voice micro-decision flow against backend verification and confirm context-bound classification updates now use audited inbox persistence with undo metadata.
+  - [x] Evidence: `bizPA/backend/src/services/inboxClassificationService.js` now centralizes audited classification writes; `bizPA/backend/src/controllers/voiceController.js` now requires explicit micro-decision context and returns context binding plus undo metadata; `bizPA/frontend/src/App.jsx` now binds voice triage to an explicitly selected inbox/finish-now card and shows a confirmation chip with undo/edit actions.
+- [x] 2. Add/adjust automated tests for this phase.
+  - [x] Test: Run targeted backend and frontend tests for the new behavior and ensure green results.
+  - [x] Evidence: `npm run verify:voice-micro-decisions` passed in `bizPA/backend`; `npx react-scripts test --watchAll=false --runInBand --runTestsByPath src/voiceMicroDecisions.test.js src/quarterReadiness.test.js` passed in `bizPA/frontend`.
+- [x] 3. Validate against PRD acceptance criteria impacted by this phase.
+  - [x] Test: Run intent contract tests for supported utterances and verify each action is context-safe, confirmed, and undo-compatible for inbox triage actions; confirm frontend production build compiles with the new voice UX.
+  - [x] Evidence: Backend verifier output ended with `voice_micro_decisions_ok`, `context_guardrails_ok=true`, `audited_classification_updates_ok=true`, and `safe_context_binding_ok=true`; frontend `npm run build` compiled successfully.
+- [x] 4. Update technical notes and rollout caveats for downstream phases.
+  - [x] Test: Confirm this lifecycle doc lists changed files, commands run, known risks, and the pending user-verification gate for the visible inbox voice flow.
+  - [x] Evidence: Lifecycle file rewritten on 2026-03-19 with normalized evidence, validation results, and an explicit user-verification request/state.
+
+# Evidence
+Objective-Delivery-Coverage: 90%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `C:\Users\edebe\eds\bizPA\frontend\src\App.jsx`
+  - Objective-Proved: The mobile/web inbox and quarter screens now require an explicit finish-now selection before voice triage, show the active voice target, and render a confirmation chip with edit/undo affordances after a voice micro-decision.
+  - Status: captured
+- Evidence-Type: test_output
+  - Artifact: `npm run verify:voice-micro-decisions` output: `voice_micro_decisions_ok`, `context_guardrails_ok=true`, `audited_classification_updates_ok=true`, `attach_receipt_clarification_ok=true`, `match_selection_ok=true`, `safe_context_binding_ok=true`, `validation_errors_ok=true`
+  - Objective-Proved: Supported utterances are constrained to the micro-decision contract, require safe context binding, and return the expected confirmation/undo contract while preserving audited inbox behavior.
+  - Status: captured
+- Evidence-Type: test_output
+  - Artifact: `npx react-scripts test --watchAll=false --runInBand --runTestsByPath src/voiceMicroDecisions.test.js src/quarterReadiness.test.js` output: `PASS src/voiceMicroDecisions.test.js`, `PASS src/quarterReadiness.test.js`
+  - Objective-Proved: The frontend request-routing and confirmation-chip helper logic behaves correctly for selection enforcement, context routing, and chip aggregation.
+  - Status: captured
+- Evidence-Type: test_output
+  - Artifact: `npm run build` in `C:\Users\edebe\eds\bizPA\frontend` output: `Compiled successfully.`
+  - Objective-Proved: The user-visible voice triage UI changes compile successfully in the production frontend build.
+  - Status: captured
+- Evidence-Type: diff
+  - Artifact: Updated `C:\Users\edebe\eds\bizPA\backend\src\controllers\voiceController.js`, `C:\Users\edebe\eds\bizPA\backend\src\controllers\inboxController.js`, `C:\Users\edebe\eds\bizPA\frontend\src\App.jsx`; added `C:\Users\edebe\eds\bizPA\backend\src\services\inboxClassificationService.js`, `C:\Users\edebe\eds\bizPA\frontend\src\voiceMicroDecisions.js`, and `C:\Users\edebe\eds\bizPA\frontend\src\voiceMicroDecisions.test.js`
+  - Objective-Proved: The workspace contains the backend and frontend implementation required for safe, undo-compatible voice micro-decisions.
+  - Status: captured
+- Evidence-Type: manual_verification
+  - Artifact: User verification requested on 2026-03-19 for the visible inbox/quarter voice flow: select a blocker, run `Category: Travel` / `Business` / `Split 70%`, confirm the chip appears, and verify Undo reverses the last triage action.
+  - Objective-Proved: Final confirmation from an operator is still required for the visible voice UX path.
+  - Status: planned
+
+# Implementation Log
+- 2026-03-05 Created decomposed task file from the approved MVP pivot checklist.
+- 2026-03-19 Re-read the workstream lifecycle skill and reconciled the stale in-progress task against the current backend/frontend workspace.
+- 2026-03-19 Identified that voice micro-decisions were bypassing the inbox audit path, which meant `/api/v1/inbox/undo-last` could not reliably undo voice-applied triage.
+- 2026-03-19 Added `bizPA/backend/src/services/inboxClassificationService.js` to centralize classification persistence, audit logging, and readiness recalculation.
+- 2026-03-19 Updated `bizPA/backend/src/controllers/inboxController.js` to use the shared audited classification helper.
+- 2026-03-19 Updated `bizPA/backend/src/controllers/voiceController.js` so context-bound voice category/business/personal/split decisions use the shared audited classification helper and return explicit context-binding plus undo metadata.
+- 2026-03-19 Expanded `bizPA/backend/verify_voice_micro_decisions.js` to assert context guardrails, audited classification persistence, preserved field state across incremental voice commands, and safe binding behavior.
+- 2026-03-19 Added `bizPA/frontend/src/voiceMicroDecisions.js` and `bizPA/frontend/src/voiceMicroDecisions.test.js` to isolate and verify the client-side routing/confirmation-chip logic.
+- 2026-03-19 Updated `bizPA/frontend/src/App.jsx` to require an explicit active inbox/finish-now selection for voice triage, render the active voice target banner, and surface a combined confirmation chip with edit/undo controls.
+- 2026-03-19 Ran backend verification, frontend targeted tests, and a production frontend build.
+- 2026-03-19 Requested user verification for the user-visible inbox/quarter voice flow and left the task active pending that confirmation.
+
+# Changes Made
+- `C:\Users\edebe\eds\bizPA\backend\src\services\inboxClassificationService.js`
+  - Added shared audited classification upsert logic that preserves existing fields, writes `transaction_audit_log`, and recalculates readiness.
+- `C:\Users\edebe\eds\bizPA\backend\src\controllers\inboxController.js`
+  - Switched inbox classification writes to the shared service so UI and voice triage use the same persistence/audit path.
+- `C:\Users\edebe\eds\bizPA\backend\src\controllers\voiceController.js`
+  - Enforced explicit micro-decision context, bound triage commands to the selected transaction/evidence context, and returned undo/context metadata for the confirmation chip flow.
+- `C:\Users\edebe\eds\bizPA\backend\verify_voice_micro_decisions.js`
+  - Added assertions for context guardrails, safe binding, audited triage writes, and clarification/error handling.
+- `C:\Users\edebe\eds\bizPA\frontend\src\voiceMicroDecisions.js`
+  - Added pure helpers for routing voice to `/voice/micro-decision`, enforcing explicit selection, and aggregating confirmation-chip actions.
+- `C:\Users\edebe\eds\bizPA\frontend\src\voiceMicroDecisions.test.js`
+  - Added tests covering missing selection handling, finish-now request construction, and chip aggregation/reset behavior.
+- `C:\Users\edebe\eds\bizPA\frontend\src\App.jsx`
+  - Added active voice target selection state for inbox and quarter cards.
+  - Routed voice on inbox/quarter tabs through the micro-decision endpoint only when a blocker is explicitly selected.
+  - Added the visible voice context banner and combined confirmation chip with edit/undo actions.
+  - Preserved the legacy `/voice/process` path for non-triage tabs.
+
+# Validation
+- 2026-03-19 16:59:44+00:00: `npm run verify:voice-micro-decisions`
+  - Result: pass
+  - Key output:
+    - `voice_micro_decisions_ok`
+    - `context_guardrails_ok=true`
+    - `audited_classification_updates_ok=true`
+    - `attach_receipt_clarification_ok=true`
+    - `match_selection_ok=true`
+    - `safe_context_binding_ok=true`
+    - `validation_errors_ok=true`
+- 2026-03-19 16:59:44+00:00: `npm run verify:voice-capture`
+  - Result: pass
+  - Key output:
+    - `voice_capture_parser_ok`
+    - `entity_mapping_ok=true`
+    - `low_confidence_review_ok=true`
+    - `composition_contract_ok=true`
+    - `query_period_filters_ok=true`
+    - `query_exact_date_filters_ok=true`
+- 2026-03-19 16:59:44+00:00: initial frontend test attempt via npm wrapper
+  - Result: blocked by environment
+  - Key output: `Error: spawn EPERM`
+  - Note: `react-scripts` attempted worker-process spawning inside the sandbox when invoked via `npm test`.
+- 2026-03-19 16:59:44+00:00: `npx react-scripts test --watchAll=false --runInBand --runTestsByPath src/voiceMicroDecisions.test.js src/quarterReadiness.test.js`
+  - Result: pass
+  - Key output:
+    - `PASS src/voiceMicroDecisions.test.js`
+    - `PASS src/quarterReadiness.test.js`
+    - `Tests: 7 passed, 7 total`
+- 2026-03-19 16:59:44+00:00: `npm run build`
+  - Result: pass
+  - Key output:
+    - `Compiled successfully.`
+- 2026-03-19 16:59:44+00:00: user verification request recorded
+  - Result: pending
+  - Key output: Please verify that selecting a finish-now blocker, speaking `Category: Travel` / `Business` / `Split 70%`, and tapping Undo behaves correctly in the visible inbox/quarter flow.
+
+# Risks/Notes
+- The visible voice triage flow is now selection-gated on inbox and quarter screens. If future UI changes remove or obscure the selected voice target, the frontend helper tests should be expanded before rollout.
+- Evidence commands remain context-bound to `evidence` rather than the inbox transaction flow. This task hardened their context safety and confirmation payloads, but it did not add a visible evidence-selection UI in the current frontend.
+- The direct `npm test` wrapper is not reliable in this sandbox because Jest worker spawning can fail with `EPERM`; use the documented `npx react-scripts test --runInBand ...` form for deterministic local verification here.
+- A prior duplicate lifecycle file exists under `workstream/300_complete`; this in-progress record is the active reconciliation file requested by the user and remains open until user verification is captured.
+
+# Completion Status
+- Awaiting user verification (2026-03-19 16:59:44+00:00)

tokens used
543,393
Implemented the Phase 6 reconciliation across backend and frontend. Voice micro-decisions now use the audited inbox classification path, preserve existing classification fields, return explicit context/undo metadata, and the UI only routes voice triage through `/voice/micro-decision` when a specific inbox or finish-now blocker is selected. The visible confirmation chip and edit/undo affordance were added in [App.jsx](/C:/Users/edebe/eds/bizPA/frontend/src/App.jsx), with shared client routing logic in [voiceMicroDecisions.js](/C:/Users/edebe/eds/bizPA/frontend/src/voiceMicroDecisions.js). Backend persistence was centralized in [inboxClassificationService.js](/C:/Users/edebe/eds/bizPA/backend/src/services/inboxClassificationService.js) and wired into [voiceController.js](/C:/Users/edebe/eds/bizPA/backend/src/controllers/voiceController.js) and [inboxController.js](/C:/Users/edebe/eds/bizPA/backend/src/controllers/inboxController.js). The lifecycle file was updated in [20260305_192700_bizpa_mvp_pivot_phase06_voice_micro_decisions.md](/C:/Users/edebe/eds/workstream/200_inprogress/codex/20260305_192700_bizpa_mvp_pivot_phase06_voice_micro_decisions.md).

Validation passed with `npm run verify:voice-micro-decisions`, `npm run verify:voice-capture`, `npx react-scripts test --watchAll=false --runInBand --runTestsByPath src/voiceMicroDecisions.test.js src/quarterReadiness.test.js`, and `npm run build`. The plain `npm test` wrapper hit a sandbox `spawn EPERM`, so I used direct `react-scripts` execution for the frontend tests.

User verification is still required before closing the task. Please check these and reply pass/fail for each:
1. Select a blocker on Inbox or Quarter, then speak `Category: Travel` and confirm the chip appears for that selected item only.
2. Speak `Business` or `Split 70%` on the same selected blocker and confirm the chip aggregates the applied actions.
3. Tap `Undo` from the chip and confirm the last voice-applied triage action is reversed.


## Execution Evidence
- Agent lane: codex
- Command: C:\Users\edebe\AppData\Roaming\npm\codex.CMD exec -C C:\Users\edebe\eds Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/codex\20260305_192700_bizpa_mvp_pivot_phase06_voice_micro_decisions.md.result.md. Implement required changes in the workspace, run validations, and update checklist items.
- Return code: 0
- Stdout:
```text
Implemented the remaining Phase 6 voice-micro-decision fixes and updated the lifecycle record in [20260305_192700_bizpa_mvp_pivot_phase06_voice_micro_decisions.md](/C:/Users/edebe/eds/workstream/200_inprogress/codex/20260305_192700_bizpa_mvp_pivot_phase06_voice_micro_decisions.md). The code changes are in [voiceMicroDecisionService.js](/C:/Users/edebe/eds/bizPA/backend/src/services/voiceMicroDecisionService.js), [verify_voice_micro_decisions.js](/C:/Users/edebe/eds/bizPA/backend/verify_voice_micro_decisions.js), [voiceMicroDecisions.js](/C:/Users/edebe/eds/bizPA/frontend/src/voiceMicroDecisions.js), [voiceMicroDecisions.test.js](/C:/Users/edebe/eds/bizPA/frontend/src/voiceMicroDecisions.test.js), and [App.jsx](/C:/Users/edebe/eds/bizPA/frontend/src/App.jsx).

What changed: quarter/finish-now voice routing now uses explicit `context_scope: finish_now`, backend validation accepts `finish_now` as a first-class transaction context, the banner no longer advertises unsupported `attach receipt` for blocker-card triage, and demo/readiness mode now shows the same success feedback path as the API-backed flow.

Validation passed:
- `npm run verify:voice-micro-decisions`
- `npx react-scripts test --watchAll=false --runInBand --runTestsByPath src/voiceMicroDecisions.test.js src/quarterReadiness.test.js`
- `npm run build`

This task remains `Awaiting user verification` per the lifecycle rules. Please reply with pass/fail for:
1. Selecting a blocker and speaking `Category: Travel` applies only to that selected item.
2. Speaking `Business` or `Split 70%` from quarter/finish-now keeps the active target and shows the confirmation chip.
3. Tapping `Undo` reverses the last triage action as expected.
```
- Stderr:
```text
`Compiled successfully.` and build artifacts emitted to `bizPA/frontend/build`
+  - Objective-Proved: The frontend compiles with the updated inbox/quarter voice triage UX.
+  - Status: captured
+- Evidence-Type: diff
+  - Artifact: `bizPA/backend/src/services/voiceMicroDecisionService.js`, `bizPA/backend/verify_voice_micro_decisions.js`, `bizPA/frontend/src/voiceMicroDecisions.js`, `bizPA/frontend/src/voiceMicroDecisions.test.js`, `bizPA/frontend/src/App.jsx`
+  - Objective-Proved: The workspace contains the required implementation and test changes for safe, context-bound voice micro-decisions.
+  - Status: captured
+- Evidence-Type: manual_verification
+  - Artifact: `pending_user_verification`
+  - Objective-Proved: The visible inbox/quarter voice target banner, confirmation chip, and undo flow behave correctly in the user-facing UI.
+  - Status: planned
+
+# Implementation Log
+- 2026-03-19 Re-read `skills/workstream-task-lifecycle/SKILL.md` requirements and used the provided `.result.md` as the source artifact because the canonical `.md` task file was missing from `workstream/200_inprogress/codex`.
+- 2026-03-19 Confirmed the backend already used the shared audited classification helper, but the remaining contract mismatch was that quarter/finish-now voice routing still posted an inbox-only context from the client.
+- 2026-03-19 Updated the frontend voice request helper to send explicit `context_scope`, aligned the backend verifier and validation rules to accept `finish_now`, and corrected the banner copy to only advertise commands supported by the selected blocker-card flow.
+- 2026-03-19 Updated the voice UX to show success messaging consistently in demo/readiness mode so the local and API-backed flows expose the same confirmation affordance.
+
+# Changes Made
+- `bizPA/frontend/src/voiceMicroDecisions.js`
+  - Switched the selected-card voice request payload from legacy context fields to explicit `context_scope`.
+- `bizPA/frontend/src/voiceMicroDecisions.test.js`
+  - Updated request-construction expectations to match the `context_scope` contract.
+- `bizPA/backend/src/services/voiceMicroDecisionService.js`
+  - Added `finish_now` as a first-class transaction context and tightened missing/invalid context errors by command family.
+- `bizPA/backend/verify_voice_micro_decisions.js`
+  - Updated the contract verifier to cover `finish_now` and the new validation message.
+- `bizPA/frontend/src/App.jsx`
+  - Removed unsupported `attach receipt` copy from the blocker-card voice banner.
+  - Ensured demo/readiness mode still surfaces success feedback after a voice triage action.
+
+# Validation
+- 2026-03-19: `npm run verify:voice-micro-decisions`
+  - Result: pass
+  - Key output: `voice_micro_decisions_ok`, `context_guardrails_ok=true`, `audited_classification_updates_ok=true`
+- 2026-03-19: `npx react-scripts test --watchAll=false --runInBand --runTestsByPath src/voiceMicroDecisions.test.js src/quarterReadiness.test.js`
+  - Result: pass
+  - Key output: `PASS src/quarterReadiness.test.js`, `PASS src/voiceMicroDecisions.test.js`, `Tests: 7 passed, 7 total`
+- 2026-03-19: `npm run build`
+  - Result: pass
+  - Key output: `Compiled successfully.`
+- 2026-03-19: User verification requested for the visible inbox/quarter flow.
+  - Please verify pass/fail for:
+  - 1. Selecting a blocker and speaking `Category: Travel` applies to the selected item only.
+  - 2. Speaking `Business` or `Split 70%` from quarter/finish-now keeps the active target and shows the confirmation chip.
+  - 3. Tapping `Undo` reverses the last triage action as expected.
+
+# Risks/Notes
+- The original source paths recorded in older task artefacts were stale; this file now references the available epic and the user-provided `.result.md` source.
+- Because this changes visible inbox and quarter UX, completion still requires user verification under the lifecycle skill rules.
+
+# Completion Status
+Awaiting user verification - 2026-03-19

tokens used
93,518
```
