OpenAI Codex v0.114.0 (research preview)
--------
workdir: C:\Users\edebe\eds
model: gpt-5.4
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR, C:\Users\edebe\.codex\memories]
reasoning effort: medium
reasoning summaries: none
session id: 019d021d-d248-7b83-acf9-b5d9e70e965a
--------
user
Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress\codex\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items.
mcp startup: no servers
codex
I’m loading the lifecycle skill and the specified task file first so I can follow the required process before touching code.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Raw 'C:\\Users\\edebe\\eds\\skills\\workstream-task-lifecycle\\SKILL.md'" in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Raw 'C:\\Users\\edebe\\eds\\workstream\\200_inprogress\\codex\\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md'" in C:\Users\edebe\eds succeeded in 1.50s:
---
name: workstream-task-lifecycle
description: Create and maintain timestamped task documentation files in the workstream lifecycle folders (`workstream/000_epic`, `workstream/100_backlog`, `workstream/200_inprogress`, `workstream/300_complete`). Use when a user asks to document changes, track progress for a distinct task, or enforce file movement across backlog/in-progress/complete states.
---

# Workstream Task Lifecycle

Execute this workflow whenever a distinct task needs a documented trail.

## Folder Structure
```
workstream/
├── 000_epic/        ← High-level specs, PRDs, strategic documents
│                      Source material for task decomposition
│                      Items remain here permanently as reference
│
├── 100_backlog/     ← Atomic tasks decomposed from epics
│                      Ready to be picked up
│
├── 200_inprogress/  ← Currently active work
 succeeded in 1.88s:
# TASK E2: Deliver quarter readiness and Finish Now screen

**Workstream:** E — Mobile UX And Voice
**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
**Priority:** 1
**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
**Suggested Agent:** codex
**UI Deliverable:** Yes
**Status:** [ ] Not Started
**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.

---

## Purpose

Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.

## Input

codex
I have the lifecycle requirements and the task contents. Next I’m locating the mobile/export UI code, the existing startup path, and the current task-file state so I can execute the work in the required order and keep the lifecycle file current.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Raw 'C:\\Users\\edebe\\eds\\AGENTS.md'" in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'rg --files' in C:\Users\edebe\eds succeeded in 1.26s:
# Repository Guidelines

## Project Structure & Module Organization
-  hosts Python automation for ingestion, analytics, and execution; scripts expect cached CSVs in  and  subfolders.
-  powers the desktop trade panel;  contains the Tk UI, agents, and orchestration, while  holds runnable strategy scripts; launch via  for simulated feeds.
-  stores versioned SQL stored procedures; sync changes with the DBA before deployment and validate against staging databases first.
- Plans, exports, diagnostics, and logs live in , , , and ; review these artefacts before refactoring or pruning history.

## Build, Test, and Development Commands
-  — standard virtual environment used across Python components.
-  — installs quantitative and RL dependencies shared by automation and viewer modules.
-  — starts the trade panel controller; add  for sandboxed data feeds.
-  — quick regression suite ensuring UI imports and background threads survive refactors.
-  — executes every  strategy; stop with Ctrl+C once logs are reviewed.

## Coding Style & Naming Conventions
- Follow PEP 8 with 4-space indentation and explicit type hints on new public functions; prefer  for filesystem work and  for logging.
- Modules and functions use , classes stick to PascalCase, and strategies follow the  naming used under .
- Store configuration in  and load secrets from environment variables or OS vaults—never hard-code credentials.

 exited 1 in 1.53s:
AGENTS.md
advanced_forex_signal_generator.py
algo_viewer_20250728_0024.zip
algo_viewer_20250727_2056.zip(current version)
algo_viewer_20250726_0154_refactor.zip
algo_viewer_20250726_0114.zip
AI_trades_agents\config.py
analyze_recent_performance.py
analyse_extract_model_output_to_csv2.py
alter_view_v4.sql
alter_view_v2.sql
algo_zone_viewer.zip
AI_trades_agents\master_agent.py
AI_trades_agents\config_trading.py
AI_trades_agents\README.md
AI_voice\prd\parallel_agent_assignments.md
AI_voice\prd\implementation_plan.md
AI_voice\prd\implementation_checklist.md
AI_voice\prd\deployment_strategy.md
AI_voice\prd\ai_voice_agent_telesales_prd.md
codex
The quarter-close work is in `bizPA`; I’m narrowing to the app, backend verification scripts, and any existing tax-readiness startup helpers so I can wire the new screen into the current flow instead of creating a parallel path.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Raw 'C:\\Users\\edebe\\eds\\bizPA\\tax_readiness_ui_smoke.ps1'" in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n \"quarter|readiness|Finish Now|export\" bizPA\\app bizPA\\frontend bizPA\\backend --glob '"'!**/node_modules/**'"'" in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-ChildItem 'C:\\Users\\edebe\\eds\\bizPA'" in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Raw 'C:\\Users\\edebe\\eds\\bizPA\\start_bizpa_tax_readiness_ui.ps1'" in C:\Users\edebe\eds succeeded in 1.59s:
bizPA\backend\apply_mvp_quarterly_migration.js:6:  const sqlPath = path.join(__dirname, 'src', 'models', 'mvp_quarterly_export_migration.sql');
bizPA\backend\apply_mvp_quarterly_migration.js:10:  console.log('[MIGRATION] MVP quarterly export migration applied successfully.');
bizPA\frontend\capacitor.config.ts:9:export default config;
bizPA\app\android\app\build.gradle:21:    bundleCommand = "export:embed"
bizPA\backend\package.json:9:    "migrate:mvp-quarterly": "node apply_mvp_quarterly_migration.js",
bizPA\backend\package.json:10:    "verify:mvp-quarterly": "node verify_mvp_quarterly_flow.js",
bizPA\backend\package.json:14:    "verify:quarter-governance": "node verify_quarter_governance_flow.js",
bizPA\backend\package.json:23:    "verify:export-compatibility": "node verify_export_compatibility.js",
bizPA\frontend\package-lock.json:1184:    "node_modules/@babel/plugin-transform-export-namespace-from": {
bizPA\frontend\package-lock.json:1186:      "resolved": "https://registry.npmjs.org/@babel/plugin-transform-export-namespace-from/-/plugin-transform-export-namespace-from-7.27.1.tgz",
bizPA\frontend\package-lock.json:1925:        "@babel/plugin-transform-export-namespace-from": "^7.27.1",
bizPA\frontend\package-lock.json:7806:        "node-exports-info": "^1.6.0",
bizPA\frontend\package-lock.json:10611:        "resolve.exports": "^1.1.0",
bizPA\frontend\package-lock.json:11907:    "node_modules/node-exports-info": {
bizPA\frontend\package-lock.json:11909:      "resolved": "https://registry.npmjs.org/node-exports-info/-/node-exports-info-1.6.0.tgz",
bizPA\frontend\package-lock.json:11925:    "node_modules/node-exports-info/node_modules/semver": {
bizPA\frontend\package-lock.json:14649:    "node_modules/resolve.exports": {
bizPA\frontend\package-lock.json:14651:      "resolved": "https://registry.npmjs.org/resolve.exports/-/resolve.exports-1.1.1.tgz",
bizPA\frontend\src\OnboardingGate.jsx:34:export default function OnboardingGate({ busy, errorMessage, onSubmit }) {
bizPA\backend\regression_fixtures\readiness_export_fixture.json:2:  "readiness": {
 succeeded in 1.58s:
$ErrorActionPreference = 'Stop'

$repoRoot = Split-Path -Parent $MyInvocation.MyCommand.Path
$frontendPath = Join-Path $repoRoot 'frontend'
$frontendUrl = 'http://127.0.0.1:3002'
$readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'

Write-Host 'Starting bizPA tax readiness UI...' -ForegroundColor Cyan

$frontendCommand = "Set-Location '$frontendPath'; `$env:PORT='3002'; npm.cmd start"
Start-Process powershell -ArgumentList '-NoExit', '-Command', $frontendCommand | Out-Null

Write-Host ''
Write-Host 'Tax readiness dashboard URL:' -ForegroundColor Green
Write-Host "  $readinessUrl" -ForegroundColor Yellow
Write-Host ''
Write-Host 'Base frontend URL:' -ForegroundColor Green
Write-Host "  $frontendUrl" -ForegroundColor Yellow
Write-Host ''
Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI can be reviewed without backend setup.' -ForegroundColor Cyan
 succeeded in 1.66s:
$ErrorActionPreference = 'Stop'

Add-Type -AssemblyName System.Windows.Forms
Add-Type -AssemblyName System.Drawing
Add-Type @"
using System;
using System.Runtime.InteropServices;

public static class WindowCaptureNative {
    [DllImport("user32.dll")]
    public static extern bool SetForegroundWindow(IntPtr hWnd);

    [DllImport("user32.dll")]
    public static extern bool ShowWindow(IntPtr hWnd, int nCmdShow);

    [DllImport("user32.dll")]
    public static extern bool GetWindowRect(IntPtr hWnd, out RECT rect);

    [StructLayout(LayoutKind.Sequential)]
    public struct RECT {
 succeeded in 1.75s:

    Directory: C:\Users\edebe\eds\bizPA

[32;1mMode   [0m[32;1m              LastWriteTime[0m [32;1;3m        Length[0m[32;1m Name[0m
[32;1m----   [0m [32;1m             -------------[0m [32;1m        ------[0m [32;1m----[0m
d----          11/03/2026    20:49                [44;1mapp[0m
d----          18/03/2026    17:56                [44;1mbackend[0m
d----          27/02/2026    20:26                [44;1mbuilds[0m
d----          11/03/2026    16:43                [44;1mdocs[0m
d----          06/03/2026    11:35                [44;1mfrontend[0m
-a---          25/02/2026    14:41            467 .gitignore
-a---          17/02/2026    22:46          10341 260217_2229_small_trader_capture_app_build_prompt_voice_first.md
-a---          22/02/2026    23:53          11755 260217_2300_voice_first_deliverables_breakdown_checklist.md
-a---          17/02/2026    23:27           1376 260217_2310_WS1_PRODUCT_UX_Acceptance_Baseline_draft.md
-a---          17/02/2026    23:27           2626 260217_2310_WS1_PRODUCT_UX_Flows_and_Dialogues_draft.md
-a---          17/02/2026    23:27           2590 260217_2310_WS1_PRODUCT_UX_PRD_draft.md
-a---          17/02/2026    23:31           1048 260217_2331_WS3_VOICE_Dialogue_and_Safety_Playbook_draft.md
-a---          17/02/2026    23:31           2556 260217_2331_WS3_VOICE_Intent_and_Slot_Spec_draft.md
-a---          17/02/2026    23:31            909 260217_2331_WS3_VOICE_Test_Matrix_draft.md
-a---          19/02/2026    23:23           7831 260217_2334_all_deliverables_test_criteria.md
codex
I found an existing local start path and smoke harness for the tax readiness screen. I’m reading the quarter view implementation now to see what is missing against this task: headline formatting, live blocker queue updates, and export gating behavior.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$p='"'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\App.jsx'; (Get-Content "'$p | Select-Object -Skip 2020 -First 220) -join "`n"' in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$p='"'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\App.jsx'; (Get-Content "'$p | Select-Object -Skip 1000 -First 260) -join "`n"' in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$p='"'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\App.jsx'; (Get-Content "'$p | Select-Object -Skip 1580 -First 160) -join "`n"' in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Raw 'C:\\Users\\edebe\\eds\\bizPA\\frontend\\package.json'" in C:\Users\edebe\eds succeeded in 2.00s:
                </div>
              </div>
            ))}
          </div>
        ))}
      </div>
    );
  };

  const renderCalendar = () => {
    const groups = groupDataByDate(calendarEvents, 'start_at');
    return (
      <div>
        <h4>Schedule</h4>
        {Object.keys(groups).map((dateLabel) => (
          <div key={dateLabel} className="mb-2">
            <div
              className="date-group-header"
              onClick={() => setCollapsedDates((previous) => ({ ...previous, [dateLabel]: !previous[dateLabel] }))}
            >
 succeeded in 2.20s:
  const [insights, setInsights] = useState([]);
  const [loading, setLoading] = useState(true);
  const [connectionError, setConnectionError] = useState(null);
  const [transcript, setTranscript] = useState('');
  const [isListening, setIsListening] = useState(false);
  const [currentTab, setCurrentTab] = useState(requestedTab || (commandCentreDemoEnabled ? 'control' : 'home'));
  const [activityTypeFilter, setActivityTypeFilter] = useState(null);
  const [activityClientFilter, setActivityClientFilter] = useState('');
  const [searchQuery, setSearchQuery] = useState('');
  const [activityDateFilter, setActivityDateFilter] = useState('');
  const [timePeriodFilter, setTimePeriodFilter] = useState(null);
  const [clients, setClients] = useState([]);
  const [jobs, setJobs] = useState([]);
  const [calendarEvents, setCalendarEvents] = useState([]);
  const [diaryEntries, setDiaryEntries] = useState([]);
  const [theme, setTheme] = useState('light');
  const [appUsers, setAppUsers] = useState(USERS);
  const [currentUser, setCurrentUser] = useState(USERS[0]);
  const [sessionToken, setSessionToken] = useState('');
  const [onboardingSession, setOnboardingSession] = useState(null);
 succeeded in 2.13s:
      const nextType = previewForm.entityType || manualForm.type;
      resetManualCapture(nextType);
      await fetchAllData();
      setCurrentTab('activity');
      alert(`${nextType} confirmed.`);
    } catch (error) {
      setPreviewError(error.response?.data?.error || 'Failed to confirm draft.');
    } finally {
      setPreviewBusy(false);
    }
  };

  const cancelPreviewDraft = async () => {
    if (!previewDraft?.compositionId || previewBusy) {
      return;
    }

    try {
      setPreviewBusy(true);
      setPreviewError('');
 succeeded in 1.78s:
{
  "name": "frontend",
  "version": "1.3.6",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@capacitor-community/speech-recognition": "^7.0.1",
    "@capacitor/preferences": "^8.0.1",
    "axios": "1.13.5",
    "bootstrap": "5.3.8",
    "lucide-react": "0.575.0",
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n \"renderQuarter|Finish Now Queue|can_export|92% ready|items left|Finish now\" 'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\App.jsx'" in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$p='"'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\App.jsx'; (Get-Content "'$p | Select-Object -Skip 2060 -First 240) -join "`n"' in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Raw 'C:\\Users\\edebe\\eds\\bizPA\\backend\\verify_readiness_drilldown.js'" in C:\Users\edebe\eds succeeded in 1.53s:
117:  can_export: false,
1619:      ? { readiness_pct: 82, blocking_txns_count: 3, can_export: false }
2087:      <h4>Finish Now Queue</h4>
2612:  const renderQuarter = () => {
2670:                <strong>{report?.can_export ? 'Ready' : 'Blocked'}</strong>
2676:                Finish now queue
2678:              <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
3415:          {currentTab === 'quarter' && renderQuarter()}
 succeeded in 1.36s:
const assert = require('assert');
const { buildReadinessReport } = require('./src/services/readinessService');

const activeQuarterTransactions = [
  {
    id: 'txn-001',
    txn_date: '2026-03-03',
    merchant: 'Fuel Stop',
    amount: 42.5,
    direction: 'out',
    category_code: null,
    business_personal: null,
    is_split: false,
    split_business_pct: null,
    duplicate_flag: false,
    duplicate_resolution: null
  },
  {
    id: 'txn-002',
    txn_date: '2026-03-05',
 succeeded in 1.68s:
      ))}
    </div>
  );

  const renderDiary = () => (
    <div>
      <h4>Diary</h4>
      {diaryEntries.map((entry) => (
        <div key={entry.id} className="insight-card">
          <b>{entry.entry_date}</b>
          <br />
          {entry.content}
        </div>
      ))}
    </div>
  );

  const renderTax = () => (
    <div>
      <h4>Tax</h4>
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$p='"'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\App.jsx'; (Get-Content "'$p | Select-Object -Skip 2600 -First 220) -join "`n"' in C:\Users\edebe\eds succeeded in 1.13s:
      await refreshQuarterWorkspace();
      setQuarterActionMessage(`Quarter ${quarterReference} reopened.`);
      setQuarterActionError('');
    } catch (error) {
      setQuarterActionError(error.response?.data?.error || 'Failed to reopen quarter.');
      setQuarterActionMessage('');
    } finally {
      setQuarterActionBusy(false);
    }
  };

  const renderQuarter = () => {
    const report = quarterReadiness || READINESS_DEMO_REPORT;
    const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
    const severityBreakdown = buildSeverityBreakdown(issueSummary);
    const historicalSnapshots = buildHistoricalSnapshots(report);
    const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
    const workspace = snapshotWorkspace || buildDemoSnapshotWorkspace();
    const lifecycle = workspace?.quarter_lifecycle || {
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$p='"'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\App.jsx'; (Get-Content "'$p | Select-Object -Skip 2820 -First 220) -join "`n"' in C:\Users\edebe\eds succeeded in 992ms:
                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="issue-row">
                  <div className="issue-head">
                    <div>
                      <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
                      <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
                      <div className="small-muted">{issue.explanation}</div>
                    </div>
                    <div className="text-end">
                      <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
                      <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
                    </div>
                  </div>
                  <div className="issue-meta">
                    <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
                    <span className="meta-chip">Entity: {issue.affected_entity_type}</span>
                    <span className="meta-chip">Weight: {issue.weight || 0}</span>
                    <span className="meta-chip">Route: {issue.resolution_target?.route || 'n/a'}</span>
                  </div>
                  <div className="issue-links">
                    <button className="issue-link" onClick={() => openReadinessWorkflow(issue)}>
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$p='"'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\App.jsx'; (Get-Content "'$p | Select-Object -Skip 60 -First 320) -join "`n"' in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$p='"'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\App.jsx'; (Get-Content "'$p | Select-Object -Skip 2340 -First 280) -join "`n"' in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n \"openReadinessWorkflow|exportQuarterlyPack|refreshQuarterWorkspace|createSnapshotVersion|selectedSnapshot|buildSeverityBreakdown|READINESS_DEMO_REPORT\" 'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\App.jsx'" in C:\Users\edebe\eds succeeded in 1.21s:
  const tightenAutoCommitCaps = () => {
    const dailyCapUpdated = applyTenantPolicyChange({
      policyKey: 'auto_commit_daily_cap_gbp',
      policyValue: 250,
      reason: 'Tighten daily governed auto-commit cap before quarter close.',
    });
    if (!dailyCapUpdated) {
      return;
    }
    applyTenantPolicyChange({
      policyKey: 'auto_commit_single_txn_cap_gbp',
      policyValue: 75,
      reason: 'Tighten single-transaction auto-commit cap before quarter close.',
    });
  };

  const disableAutoCommit = () => {
    applyTenantPolicyChange({
      policyKey: 'auto_commit_enabled',
      policyValue: false,
 succeeded in 1.19s:
const TENANT_ID = 'tenant-bizpa-demo';

const USERS = [
  {
    id: '11111111-1111-1111-1111-111111111111',
    name: 'Alice (Owner)',
    email: 'alice@bizpa.local',
    role: 'owner',
    tenant_id: TENANT_ID,
  },
  {
    id: '22222222-2222-2222-2222-222222222222',
    name: 'Cara (Admin)',
    email: 'cara@bizpa.local',
    role: 'admin',
    tenant_id: TENANT_ID,
  },
  {
    id: '33333333-3333-3333-3333-333333333333',
    name: 'Bob (Staff)',
 succeeded in 1.19s:
106:const READINESS_DEMO_REPORT = {
772:const buildSeverityBreakdown = (issueSummary = []) => {
828:const buildIntegrityWarnings = ({ snapshotStatus, lifecycle, selectedSnapshot }) => {
830:  const selectedWarnings = Array.isArray(selectedSnapshot?.integrity_warnings) ? selectedSnapshot.integrity_warnings : [];
890:  const selectedSnapshot = snapshots[0] || null;
900:      selectedSnapshot
908:    quarter_reference: READINESS_DEMO_REPORT.quarter_reference,
925:    quarter_reference: READINESS_DEMO_REPORT.quarter_reference,
950:      selectedSnapshot: SNAPSHOT_DEMO_HISTORY[0]
1037:  const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
1063:        setQuarterReadiness(READINESS_DEMO_REPORT);
1064:        setInboxItems(READINESS_DEMO_REPORT.issue_list);
1161:    const matchedSnapshot = snapshotWorkspace.snapshots.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId);
1165:  }, [selectedSnapshotId, snapshotWorkspace]);
2399:  const exportQuarterlyPack = () => {
2424:  const openReadinessWorkflow = (issue) => {
2432:  const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
2436:  const refreshQuarterWorkspace = async () => {
2437:    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
2465:  const createSnapshotVersion = async () => {
codex
The current quarter view exists, but it does not yet satisfy the task cleanly: the hero headline is only `% ready`, the Finish Now queue is a separate tab, and the export button state is tied to the raw report instead of an immediately updated live blocker model. I’m updating the lifecycle file now with an ordered plan before I edit code, then I’ll implement the quarter-screen state model and validation path.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Raw 'C:\\Users\\edebe\\eds\\workstream\\200_inprogress\\codex\\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md'" in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-ChildItem 'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src' -Filter '*.test.js' | Select-Object -ExpandProperty FullName" in C:\Users\edebe\eds succeeded in 1.05s:
C:\Users\edebe\eds\bizPA\frontend\src\controlCentre.test.js
C:\Users\edebe\eds\bizPA\frontend\src\governance.test.js
C:\Users\edebe\eds\bizPA\frontend\src\onboarding.test.js
 succeeded in 1.02s:
# TASK E2: Deliver quarter readiness and Finish Now screen

**Workstream:** E — Mobile UX And Voice
**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
**Priority:** 1
**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
**Suggested Agent:** codex
**UI Deliverable:** Yes
**Status:** [ ] Not Started
**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.

---

## Purpose

Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.

## Input

Plan update
Executing the lifecycle in order: document the task state first, then implement the quarter-screen changes, then validate and capture evidence before closing status.
  → Normalize the in-progress lifecycle file into the required template and record the ordered implementation plan for this task.
  • Implement the quarter readiness screen updates, including inline Finish Now queue behavior, live readiness/export state updates, and any needed start/smoke script adjustments.
  • Run targeted validations, capture screenshot/evidence artifacts, update the lifecycle checklist/evidence/results, and determine final completion state.
codex
I’m updating the lifecycle file first so the task has the required dependency, plan, evidence inventory, and execution log before implementation starts.
file update
A C:\Users\edebe\eds\workstream\200_inprogress\codex\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
# TASK E2: Deliver quarter readiness and Finish Now screen

Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
Dependency: C1 quarter metrics and C4 export flow.

## Plan
- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
  - Evidence: pending
- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
  - Evidence: pending
- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
  - Evidence: pending

## Evidence
Objective-Delivery-Coverage: 0%
Auto-Acceptance: false
- Evidence-Type: demo
  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
  - Status: planned
- Evidence-Type: screenshot
  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
  - Status: planned
- Evidence-Type: screenshot
  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
  - Status: planned
- Evidence-Type: test_output
  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
  - Status: planned
- Evidence-Type: log_output
  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
  - Status: planned

## Implementation Log
- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.

## Changes Made
- Pending.

## Validation
- Pending.

## Risks/Notes
- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
- The existing task brief and prior retry history are preserved below for reference.

## Completion Status
In progress - 2026-03-18 18:07

---

## Original Task Brief

**Workstream:** E — Mobile UX And Voice
**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
**Priority:** 1
**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
**Suggested Agent:** codex
**UI Deliverable:** Yes
**Status:** [ ] Not Started
**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.

### Purpose
Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.

### Input
C1 quarter metrics, C4 export flow, and epic quarter screen requirements.

### Output
Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.

### Fields / Components
- readiness_pct
- items_left
- finish_now_cta
- blocking_queue
- export_enabled

### Dependencies
- C1
- C4

### Action
Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.

### Verification
- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.

### Notes
- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
- This task is intended for Epic Review allocation before execution.
- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.

### Retry History
Retry-Count: 2
- Retry scheduled at 2026-03-18 17:21:30

### Execution Evidence
- Agent lane: claude
- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
- Return code: 1
- Stdout:
```text
You've hit your limit · resets 7pm (Europe/London)
```
- Retry scheduled at 2026-03-18 17:30:31
apply_patch(auto_approved=true) exited 0 in 477ms:
Success. Updated the following files:
A C:\Users\edebe\eds\workstream\200_inprogress\codex\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
D C:\Users\edebe\eds\workstream\200_inprogress\codex\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
file update:
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update:
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Raw 'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\governance.test.js'" in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Raw 'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\controlCentre.test.js'" in C:\Users\edebe\eds succeeded in 871ms:
import {
  buildAutoCommitVisibility,
  buildControlCentreSummary,
  buildQueueBacklogSummary,
} from './controlCentre';
import { buildDefaultPolicyRecords } from './governance';

describe('control centre helpers', () => {
  test('buildQueueBacklogSummary counts severity buckets', () => {
    const summary = buildQueueBacklogSummary([
      { blocker_reason: 'missing_category', severity: 'high' },
      { blocker_reason: 'unresolved_duplicate', severity: 'medium' },
      { blocker_reason: 'missing_business_personal', severity: 'high' },
    ]);

    expect(summary.total).toBe(3);
    expect(summary.high).toBe(2);
    expect(summary.medium).toBe(1);
    expect(summary.low).toBe(0);
    expect(summary.headline).toContain('3 blockers');
 succeeded in 917ms:
import {
  ROLE_LABELS,
  PERMISSIONS,
  applyPolicyUpdate,
  buildDefaultPolicyRecords,
  getPermissionsForRole,
  hasPermission,
  runGovernedAction,
} from './governance';

const tenantId = 'tenant-bizpa-demo';
const owner = { id: 'user-owner', name: 'Alice Owner', role: 'owner' };
const admin = { id: 'user-admin', name: 'Cara Admin', role: 'admin' };
const staff = { id: 'user-staff', name: 'Bob Staff', role: 'staff' };
const accountant = { id: 'user-accountant', name: 'Dana Accountant', role: 'accountant_readonly' };

describe('governance role model', () => {
  test('distinguishes owner, admin, staff, and accountant read-only capabilities', () => {
    expect(ROLE_LABELS.owner).toBe('Owner');
    expect(getPermissionsForRole(owner.role)).toContain(PERMISSIONS.GOVERNANCE_MANAGE);
file update:
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

codex
I’ve settled the implementation approach: derive a live quarter state from the current blocker list, expose an inline Finish Now queue on the quarter screen, and add a demo query path for the zero-blocker/export-ready state so the smoke script can prove both sides of the gating behavior. Now I’m patching the frontend and the review scripts.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n \"const formatIssueAmount|function formatIssueAmount|const formatCurrency|buildHistoricalSnapshots|buildDemoSnapshotWorkspace\" 'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\App.jsx'" in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$p='"'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\App.jsx'; (Get-Content "'$p | Select-Object -First 80) -join "`n"' in C:\Users\edebe\eds succeeded in 932ms:
729:const formatCurrency = (value, digits = 0) => {
755:const formatIssueAmount = (amount, direction) => {
788:const buildHistoricalSnapshots = (report) => {
906:const buildDemoSnapshotWorkspace = () => {
1065:        setSnapshotWorkspace(buildDemoSnapshotWorkspace());
1169:      return buildDemoSnapshotWorkspace();
2617:    const historicalSnapshots = buildHistoricalSnapshots(report);
2619:    const workspace = snapshotWorkspace || buildDemoSnapshotWorkspace();
 succeeded in 1.04s:
import React, { useEffect, useRef, useState } from 'react';
import axios from 'axios';
import {
  ArchiveRestore,
  ArrowRight,
  Bell,
  Briefcase,
  Calendar,
  CheckCircle2,
  ChevronDown,
  ChevronUp,
  Clock,
  Download,
  Flame,
  FileText,
  History,
  LayoutGrid,
  Lock,
  LockOpen,
  Mic,
file update:
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update
A C:\Users\edebe\eds\bizPA\frontend\src\quarterReadiness.js
const SEVERITY_ORDER = {
  high: 0,
  medium: 1,
  low: 2,
};

const normalizeNumber = (value, fallback = 0) => {
  const parsed = Number(value);
  return Number.isFinite(parsed) ? parsed : fallback;
};

const sortIssues = (issues = []) => [...issues].sort((left, right) => {
  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
  if (severityDelta !== 0) {
    return severityDelta;
  }

  const leftDate = String(left.txn_date || '');
  const rightDate = String(right.txn_date || '');
  if (leftDate !== rightDate) {
    return leftDate.localeCompare(rightDate);
  }

  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
});

const buildIssueSummary = (issues = []) => {
  const groups = new Map();

  issues.forEach((issue) => {
    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
    const existing = groups.get(key);
    if (existing) {
      existing.count += 1;
      existing.total_weight += normalizeNumber(issue.weight, 0);
      return;
    }

    groups.set(key, {
      issue_type: key,
      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
      severity: issue.severity || 'low',
      count: 1,
      weight: normalizeNumber(issue.weight, 0),
      total_weight: normalizeNumber(issue.weight, 0),
      explanation: issue.explanation || '',
    });
  });

  return [...groups.values()].sort((left, right) => {
    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
    if (severityDelta !== 0) {
      return severityDelta;
    }
    return right.count - left.count;
  });
};

const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
  reason: entry.issue_type,
  label: entry.label,
  count: entry.count,
}));

const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
  const blockerCount = activeIssues.length;
  const quarterStart = report.period_start || 'current quarter start';
  const quarterEnd = report.period_end || 'current quarter end';
  const reasonSummary = issueSummary.length
    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
    : 'No blocking reasons remain.';

  return [
    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
    blockerCount > 0
      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
      : 'No blocking transactions remain. Export is ready now.',
    `Blocking reasons: ${reasonSummary}`,
  ];
};

export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
  const resolvedValue = searchParams?.get('resolvedDemoIssues');
  if (!resolvedValue) {
    return [];
  }

  if (resolvedValue === 'all') {
    return fallbackIssues
      .map((issue) => issue?.affected_entity_id)
      .filter(Boolean);
  }

  return resolvedValue
    .split(',')
    .map((value) => value.trim())
    .filter(Boolean);
};

export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
  if (!report) {
    return {
      report: null,
      activeIssues: [],
      headline: '0% ready - 0 items left',
      itemsLeft: 0,
      exportEnabled: false,
    };
  }

  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
  const resolvedSet = new Set(resolvedIssueIds);
  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
  const readinessLoss = Math.max(0, 100 - baseReadiness);
  const readinessPct = activeIssues.length === 0
    ? 100
    : originalPenalty > 0
      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
      : baseReadiness;
  const issueSummary = buildIssueSummary(activeIssues);
  const blockersByReason = buildBlockersByReason(issueSummary);
  const itemsLeft = activeIssues.length;
  const exportEnabled = itemsLeft === 0;
  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;

  return {
    report: {
      ...report,
      readiness_pct: readinessPct,
      blocking_txns_count: itemsLeft,
      can_export: exportEnabled,
      issue_list: activeIssues,
      issue_count: itemsLeft,
      issue_summary: issueSummary,
      blockers_by_reason: blockersByReason,
      explanation_lines: buildExplanationLines({
        readinessPct,
        report,
        activeIssues,
        issueSummary,
      }),
    },
    activeIssues,
    headline,
    itemsLeft,
    exportEnabled,
  };
};
apply_patch(auto_approved=true) exited 0 in 504ms:
Success. Updated the following files:
A C:\Users\edebe\eds\bizPA\frontend\src\quarterReadiness.js
file update:
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update:
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n \"searchParams|requestedTab|readinessDemoEnabled|snapshotDemoEnabled|commandCentreDemoEnabled|captureDemoEnabled\" 'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\App.jsx'" in C:\Users\edebe\eds succeeded in 821ms:
1220:  const searchParams = typeof window !== 'undefined' ? new URLSearchParams(window.location.search) : null;
1221:  const captureDemoEnabled = typeof window !== 'undefined'
1222:    && searchParams?.get('captureDemo') === '1';
1223:  const commandCentreDemoEnabled = typeof window !== 'undefined'
1224:    && searchParams?.get('commandCentreDemo') === '1';
1225:  const readinessDemoEnabled = typeof window !== 'undefined'
1226:    && searchParams?.get('readinessDemo') === '1';
1227:  const snapshotDemoEnabled = typeof window !== 'undefined'
1228:    && searchParams?.get('snapshotDemo') === '1';
1229:  const requestedTab = searchParams?.get('tab');
1240:  const [currentTab, setCurrentTab] = useState(requestedTab || (commandCentreDemoEnabled ? 'control' : 'home'));
1296:      if (readinessDemoEnabled || snapshotDemoEnabled) {
1339:  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
1354:    if (!captureDemoEnabled) {
1376:  }, [captureDemoEnabled]);
1379:    if (commandCentreDemoEnabled && !captureDemoEnabled) {
1380:      setCurrentTab(requestedTab || 'control');
1382:  }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
1385:    if (readinessDemoEnabled || snapshotDemoEnabled) {
1388:  }, [readinessDemoEnabled, snapshotDemoEnabled]);
file update:
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
@@ -48,2 +48,6 @@
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
@@ -1269,2 +1273,6 @@
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
apply_patch(auto_approved=true) exited 0 in 502ms:
Success. Updated the following files:
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..d4fe2bc7056c8c25cea42c2cc8f92365bc6c632c
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -1267,6 +1271,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..d4fe2bc7056c8c25cea42c2cc8f92365bc6c632c
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -1267,6 +1271,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
@@ -2676,5 +2676,23 @@
     || null;
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
apply_patch(auto_approved=true) exited 0 in 464ms:
Success. Updated the following files:
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..e389dd0657f3f352d0c952d18878572410080b29
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -1267,6 +1271,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -2666,9 +2674,27 @@
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
 
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..e389dd0657f3f352d0c952d18878572410080b29
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -1267,6 +1271,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -2666,9 +2674,27 @@
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
 
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..e389dd0657f3f352d0c952d18878572410080b29
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -1267,6 +1271,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -2666,9 +2674,27 @@
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
 
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$p='"'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\App.jsx'; (Get-Content "'$p | Select-Object -Skip 1636 -First 40) -join "`n"' in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$p='"'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\App.jsx'; (Get-Content "'$p | Select-Object -Skip 2078 -First 32) -join "`n"' in C:\Users\edebe\eds succeeded in 1.19s:
                </div>
                <div>
                  <strong>{item.title}</strong>
                  <small>{item.detail}</small>
                </div>
                <button className="attention-action" onClick={item.action}>{item.actionLabel}</button>
              </div>
            ))}
          </div>
        </section>

        <section>
          <div className="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-2">
            <div>
              <h5 style={{ marginBottom: 4 }}>Performance Grid</h5>
              <small className="small-muted">Compact metrics for momentum, cash, readiness, and workload.</small>
            </div>
          </div>
          <div className="performance-grid">
            {performanceCards.map((card) => (
 succeeded in 1.21s:
          setTranscript('');
          finalTranscriptRef.current = '';
          setIsListening(true);
          setVoiceStatus('listening');
          webRecognitionRef.current.start();
        }
        return;
      }

      if (isListening) {
        setIsListening(false);
        setVoiceStatus('processing');
        SpeechRecognition.stop().catch(() => {});
        const finalText = finalTranscriptRef.current.trim();
        if (finalText) {
          handleVoiceProcess(finalText);
        } else {
          setVoiceStatus('idle');
        }
      } else {
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..5c486375eda97cce9a948060466a6384261b1228
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -1226,6 +1236,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1279,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1298,10 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1313,11 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1356,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1402,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1419,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1476,6 +1496,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(inboxItems, quarterReadiness);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (quarterReadiness?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1573,6 +1597,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -2666,9 +2876,27 @@
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
 
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$p='"'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\App.jsx'; (Get-Content "'$p | Select-Object -Skip 1840 -First 44) -join "`n"' in C:\Users\edebe\eds succeeded in 870ms:
          setIsListening(true);
          setVoiceStatus('listening');
          webRecognitionRef.current.start();
        }
        return;
      }

      if (isListening) {
        setIsListening(false);
        setVoiceStatus('processing');
        SpeechRecognition.stop().catch(() => {});
        const finalText = finalTranscriptRef.current.trim();
        if (finalText) {
          handleVoiceProcess(finalText);
        } else {
          setVoiceStatus('idle');
        }
      } else {
        const permissions = await SpeechRecognition.checkPermissions();
        if (permissions.speechRecognition !== 'granted') {
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..5c486375eda97cce9a948060466a6384261b1228
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -1226,6 +1236,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1279,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1298,10 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1313,11 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1356,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1402,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1419,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1476,6 +1496,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(inboxItems, quarterReadiness);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (quarterReadiness?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1573,6 +1597,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -2666,9 +2876,27 @@
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
 
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n \"const renderDashboard = \\(\\) =>\" 'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\App.jsx'" in C:\Users\edebe\eds succeeded in 876ms:
2055:  const renderDashboard = () => {
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..5c486375eda97cce9a948060466a6384261b1228
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -1226,6 +1236,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1279,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1298,10 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1313,11 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1356,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1402,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1419,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1476,6 +1496,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(inboxItems, quarterReadiness);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (quarterReadiness?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1573,6 +1597,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -2666,9 +2876,27 @@
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
 
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$p='"'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\App.jsx'; (Get-Content "'$p | Select-Object -Skip 2054 -First 50) -join "`n"' in C:\Users\edebe\eds succeeded in 1.01s:
  const renderDashboard = () => {
    const demoMomentum = commandCentreDemoEnabled
      ? {
        thisWeek: { incoming: 4820, outgoing: 1360 },
        deltas: { incoming: 18 },
      }
      : null;
    const demoReadiness = commandCentreDemoEnabled
      ? { readiness_pct: 82, blocking_txns_count: 3, can_export: false }
      : null;
    const liveIncoming = momentum?.thisWeek?.incoming ?? demoMomentum?.thisWeek?.incoming ?? 0;
    const liveOutgoing = momentum?.thisWeek?.outgoing ?? demoMomentum?.thisWeek?.outgoing ?? 0;
    const deltaIncoming = momentum?.deltas?.incoming ?? demoMomentum?.deltas?.incoming ?? 0;
    const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
    const invoiceCount = items.filter((item) => item.type === 'invoice').length;
    const receiptCount = items.filter((item) => item.type === 'receipt').length;
    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
    const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
    const liveVatPayable = vatSummary?.vat_boxes?.payable;
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..5c486375eda97cce9a948060466a6384261b1228
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -1226,6 +1236,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1279,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1298,10 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1313,11 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1356,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1402,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1419,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1476,6 +1496,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(inboxItems, quarterReadiness);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (quarterReadiness?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1573,6 +1597,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -2666,9 +2876,27 @@
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
 
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..5c486375eda97cce9a948060466a6384261b1228
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -1226,6 +1236,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1279,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1298,10 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1313,11 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1356,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1402,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1419,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1476,6 +1496,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(inboxItems, quarterReadiness);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (quarterReadiness?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1573,6 +1597,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -2666,9 +2876,27 @@
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
 
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
@@ -2533,4 +2533,4 @@
       <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
+        {activeFinishNowQueue.length === 0 && <div className="alert alert-success">No blocking items.</div>}
+        {activeFinishNowQueue.map((item) => (
           <div key={item.id} className="attention-item">
apply_patch(auto_approved=true) exited 0 in 589ms:
Success. Updated the following files:
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..edaa0397784adf46908cd6777b6cb5c3aa36a456
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -1226,6 +1236,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1279,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1298,10 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1313,11 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1356,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1402,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1419,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1476,6 +1496,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(inboxItems, quarterReadiness);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (quarterReadiness?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1573,6 +1597,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -2321,8 +2531,8 @@
       <h4>Finish Now Queue</h4>
       <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
       <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
+        {activeFinishNowQueue.length === 0 && <div className="alert alert-success">No blocking items.</div>}
+        {activeFinishNowQueue.map((item) => (
           <div key={item.id} className="attention-item">
             <div>
               <b>{item.merchant || 'Unknown'}</b>
@@ -2666,9 +2876,27 @@
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
 
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..edaa0397784adf46908cd6777b6cb5c3aa36a456
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -1226,6 +1236,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1279,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1298,10 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1313,11 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1356,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1402,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1419,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1476,6 +1496,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(inboxItems, quarterReadiness);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (quarterReadiness?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1573,6 +1597,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -2321,8 +2531,8 @@
       <h4>Finish Now Queue</h4>
       <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
       <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
+        {activeFinishNowQueue.length === 0 && <div className="alert alert-success">No blocking items.</div>}
+        {activeFinishNowQueue.map((item) => (
           <div key={item.id} className="attention-item">
             <div>
               <b>{item.merchant || 'Unknown'}</b>
@@ -2666,9 +2876,27 @@
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
 
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
@@ -2070,4 +2070,4 @@
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
apply_patch(auto_approved=true) exited 0 in 601ms:
Success. Updated the following files:
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..e8d0e6a5e766664651f318e9b333772e7d0f5d21
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -1226,6 +1236,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1279,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1298,10 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1313,11 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1356,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1402,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1419,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1476,6 +1496,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(inboxItems, quarterReadiness);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (quarterReadiness?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1573,6 +1597,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2068,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2321,8 +2531,8 @@
       <h4>Finish Now Queue</h4>
       <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
       <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
+        {activeFinishNowQueue.length === 0 && <div className="alert alert-success">No blocking items.</div>}
+        {activeFinishNowQueue.map((item) => (
           <div key={item.id} className="attention-item">
             <div>
               <b>{item.merchant || 'Unknown'}</b>
@@ -2666,9 +2876,27 @@
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
 
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..e8d0e6a5e766664651f318e9b333772e7d0f5d21
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -1226,6 +1236,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1279,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1298,10 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1313,11 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1356,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1402,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1419,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1476,6 +1496,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(inboxItems, quarterReadiness);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (quarterReadiness?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1573,6 +1597,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2068,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2321,8 +2531,8 @@
       <h4>Finish Now Queue</h4>
       <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
       <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
+        {activeFinishNowQueue.length === 0 && <div className="alert alert-success">No blocking items.</div>}
+        {activeFinishNowQueue.map((item) => (
           <div key={item.id} className="attention-item">
             <div>
               <b>{item.merchant || 'Unknown'}</b>
@@ -2666,9 +2876,27 @@
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
 
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
@@ -3074,5 +3074,5 @@
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
@@ -3114,3 +3114,3 @@
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
@@ -3126,3 +3126,3 @@
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
@@ -3137,6 +3137,6 @@
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
@@ -3151,2 +3151,55 @@
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
apply_patch(auto_approved=true) exited 0 in 716ms:
Success. Updated the following files:
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..fd1084b5460a5a1fb52deccd6a90ff5cd0a0003c
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -1226,6 +1236,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1279,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1298,10 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1313,11 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1356,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1402,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1419,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1476,6 +1496,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(inboxItems, quarterReadiness);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (quarterReadiness?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1573,6 +1597,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2068,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2321,8 +2531,8 @@
       <h4>Finish Now Queue</h4>
       <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
       <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
+        {activeFinishNowQueue.length === 0 && <div className="alert alert-success">No blocking items.</div>}
+        {activeFinishNowQueue.map((item) => (
           <div key={item.id} className="attention-item">
             <div>
               <b>{item.merchant || 'Unknown'}</b>
@@ -2666,9 +2876,27 @@
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
 
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2844,9 +3072,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3112,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3124,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3135,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3149,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..fd1084b5460a5a1fb52deccd6a90ff5cd0a0003c
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -1226,6 +1236,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1279,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1298,10 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1313,11 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1356,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1402,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1419,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1476,6 +1496,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(inboxItems, quarterReadiness);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (quarterReadiness?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1573,6 +1597,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2068,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2321,8 +2531,8 @@
       <h4>Finish Now Queue</h4>
       <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
       <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
+        {activeFinishNowQueue.length === 0 && <div className="alert alert-success">No blocking items.</div>}
+        {activeFinishNowQueue.map((item) => (
           <div key={item.id} className="attention-item">
             <div>
               <b>{item.merchant || 'Unknown'}</b>
@@ -2666,9 +2876,27 @@
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
 
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2844,9 +3072,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3112,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3124,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3135,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3149,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..fd1084b5460a5a1fb52deccd6a90ff5cd0a0003c
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -1226,6 +1236,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1279,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1298,10 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1313,11 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1356,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1402,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1419,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1476,6 +1496,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(inboxItems, quarterReadiness);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (quarterReadiness?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1573,6 +1597,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2068,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2321,8 +2531,8 @@
       <h4>Finish Now Queue</h4>
       <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
       <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
+        {activeFinishNowQueue.length === 0 && <div className="alert alert-success">No blocking items.</div>}
+        {activeFinishNowQueue.map((item) => (
           <div key={item.id} className="attention-item">
             <div>
               <b>{item.merchant || 'Unknown'}</b>
@@ -2666,9 +2876,27 @@
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
 
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2844,9 +3072,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3112,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3124,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3135,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3149,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$p='"'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\App.jsx'; (Get-Content "'$p | Select-Object -Skip 468 -First 40) -join "`n"' in C:\Users\edebe\eds succeeded in 983ms:
  .capture-inline-meta { display: flex; flex-wrap: wrap; gap: 8px; margin-bottom: 12px; }
  .meta-chip { background: var(--chip-bg); border: 1px solid var(--border); border-radius: 999px; color: var(--text-muted); padding: 6px 10px; font-size: 0.78rem; }
  .preview-header { display: flex; justify-content: space-between; gap: 16px; align-items: start; margin-bottom: 16px; }
  .preview-header h5, .capture-panel h4, .empty-panel h5 { margin: 0; }
  .preview-badge { border-radius: 999px; padding: 6px 10px; font-size: 0.76rem; font-weight: 700; text-transform: uppercase; background: var(--chip-bg); color: var(--text-muted); border: 1px solid var(--border); }
  .preview-badge.high { background: rgba(45,143,91,0.14); color: var(--success); border-color: rgba(45,143,91,0.28); }
  .preview-badge.medium { background: rgba(244,184,96,0.18); color: #9b6610; border-color: rgba(244,184,96,0.28); }
  .preview-badge.low { background: rgba(204,68,75,0.14); color: var(--danger); border-color: rgba(204,68,75,0.28); }
  .preview-field-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 12px; }
  .preview-field { display: flex; flex-direction: column; gap: 6px; }
  .preview-value { min-height: 48px; border-radius: 14px; border: 1px solid var(--border); background: var(--chip-bg); color: var(--text-main); padding: 12px 14px; }
  .preview-actions { display: flex; flex-wrap: wrap; gap: 10px; margin-top: 18px; }
  .preview-note { margin-top: 12px; color: var(--text-muted); font-size: 0.88rem; }
  .panel-warning { border-radius: 14px; border: 1px solid rgba(244,184,96,0.32); background: rgba(244,184,96,0.12); color: #8a5c12; padding: 12px 14px; margin-bottom: 14px; }
  .panel-error { border-radius: 14px; border: 1px solid rgba(204,68,75,0.32); background: rgba(204,68,75,0.12); color: var(--danger); padding: 12px 14px; margin-bottom: 14px; }
  .small-muted { color: var(--text-muted); font-size: 0.9rem; }
  .status-pill { display: inline-flex; align-items: center; gap: 8px; background: rgba(13,92,99,0.1); color: var(--primary); border: 1px solid rgba(13,92,99,0.2); border-radius: 999px; padding: 6px 12px; font-size: 0.78rem; font-weight: 700; }
  .readiness-layout { display: grid; grid-template-columns: minmax(0, 1.25fr) minmax(320px, 0.75fr); gap: 18px; align-items: start; }
  .readiness-hero { background: linear-gradient(145deg, #173135 0%, #0d5c63 55%, #9ed8bf 140%); border-radius: 28px; color: white; padding: 26px; box-shadow: var(--shadow); position: relative; overflow: hidden; }
  .readiness-hero::after { content: ''; position: absolute; right: -50px; top: -35px; width: 180px; height: 180px; border-radius: 50%; background: rgba(255,255,255,0.1); }
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..fd1084b5460a5a1fb52deccd6a90ff5cd0a0003c
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -1226,6 +1236,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1279,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1298,10 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1313,11 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1356,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1402,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1419,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1476,6 +1496,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(inboxItems, quarterReadiness);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (quarterReadiness?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1573,6 +1597,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2068,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2321,8 +2531,8 @@
       <h4>Finish Now Queue</h4>
       <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
       <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
+        {activeFinishNowQueue.length === 0 && <div className="alert alert-success">No blocking items.</div>}
+        {activeFinishNowQueue.map((item) => (
           <div key={item.id} className="attention-item">
             <div>
               <b>{item.merchant || 'Unknown'}</b>
@@ -2666,9 +2876,27 @@
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
 
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2844,9 +3072,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3112,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3124,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3135,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3149,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$p='"'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\App.jsx'; (Get-Content "'$p | Select-Object -Skip 548 -First 18) -join "`n"' in C:\Users\edebe\eds succeeded in 907ms:
  .inbox-mobile-hero::after { content: ''; position: absolute; right: -55px; top: -35px; width: 170px; height: 170px; border-radius: 50%; background: rgba(255,255,255,0.12); }
  .inbox-mobile-hero h4 { margin: 12px 0 8px; font-size: 2rem; line-height: 1; }
  .inbox-mobile-hero p { margin: 0; max-width: 48ch; color: rgba(255,255,255,0.84); }
  .inbox-kicker { display: inline-flex; align-items: center; gap: 8px; border-radius: 999px; background: rgba(255,255,255,0.14); padding: 7px 12px; font-size: 0.74rem; font-weight: 700; letter-spacing: 0.04em; text-transform: uppercase; }
  .inbox-mobile-stats { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 12px; margin-top: 18px; }
  .inbox-mobile-stat { border-radius: 18px; background: rgba(255,255,255,0.12); border: 1px solid rgba(255,255,255,0.16); padding: 14px; }
  .inbox-mobile-stat strong { display: block; font-size: 1.25rem; margin-top: 4px; }
  .inbox-filter-row { display: flex; flex-wrap: wrap; gap: 10px; }
  .inbox-filter-chip { border-radius: 999px; border: 1px solid var(--border); background: var(--chip-bg); color: var(--text-main); padding: 10px 14px; font-weight: 700; display: inline-flex; align-items: center; gap: 8px; }
  .inbox-filter-chip.active { background: var(--primary); color: white; border-color: var(--primary); }
  .inbox-filter-chip small { opacity: 0.84; font-size: 0.74rem; }
  .inbox-card-list { display: grid; gap: 14px; }
  .inbox-action-card { border-radius: 24px; border: 1px solid var(--border); background: var(--bg-card); box-shadow: var(--shadow); padding: 18px; display: grid; gap: 14px; }
  .inbox-card-top { display: flex; justify-content: space-between; gap: 12px; align-items: start; }
  .inbox-card-top strong { display: block; font-size: 1.08rem; margin-bottom: 4px; }
  .inbox-card-subline { color: var(--text-muted); font-size: 0.88rem; }
  .inbox-chip-row { display: flex; flex-wrap: wrap; gap: 8px; }
  .inbox-issue-chip, .inbox-suggestion-chip { display: inline-flex; align-items: center; gap: 6px; border-radius: 999px; padding: 7px 10px; font-size: 0.74rem; font-weight: 700; border: 1px solid var(--border); background: var(--chip-bg); color: var(--text-muted); }
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..fd1084b5460a5a1fb52deccd6a90ff5cd0a0003c
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -1226,6 +1236,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1279,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1298,10 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1313,11 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1356,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1402,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1419,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1476,6 +1496,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(inboxItems, quarterReadiness);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (quarterReadiness?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1573,6 +1597,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2068,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2321,8 +2531,8 @@
       <h4>Finish Now Queue</h4>
       <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
       <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
+        {activeFinishNowQueue.length === 0 && <div className="alert alert-success">No blocking items.</div>}
+        {activeFinishNowQueue.map((item) => (
           <div key={item.id} className="attention-item">
             <div>
               <b>{item.merchant || 'Unknown'}</b>
@@ -2666,9 +2876,27 @@
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
 
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2844,9 +3072,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3112,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3124,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3135,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3149,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n \"@media \\(max-width: 860px\\)\" 'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\App.jsx'" in C:\Users\edebe\eds succeeded in 778ms:
617:  @media (max-width: 860px) {
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..fd1084b5460a5a1fb52deccd6a90ff5cd0a0003c
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -1226,6 +1236,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1279,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1298,10 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1313,11 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1356,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1402,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1419,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1476,6 +1496,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(inboxItems, quarterReadiness);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (quarterReadiness?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1573,6 +1597,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2068,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2321,8 +2531,8 @@
       <h4>Finish Now Queue</h4>
       <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
       <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
+        {activeFinishNowQueue.length === 0 && <div className="alert alert-success">No blocking items.</div>}
+        {activeFinishNowQueue.map((item) => (
           <div key={item.id} className="attention-item">
             <div>
               <b>{item.merchant || 'Unknown'}</b>
@@ -2666,9 +2876,27 @@
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
 
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2844,9 +3072,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3112,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3124,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3135,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3149,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$p='"'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\App.jsx'; (Get-Content "'$p | Select-Object -Skip 617 -First 12) -join "`n"' in C:\Users\edebe\eds succeeded in 967ms:
    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
    .diff-columns { display: grid; }
  }
  @media (max-width: 640px) {
    .capture-btn-container { right: 12px; bottom: 84px; }
    .log-something-btn { padding: 10px 16px; min-height: 44px; }
    .capture-type-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
    .mini-stat-grid { grid-template-columns: 1fr; }
    .inbox-mobile-hero { padding: 20px; }
    .inbox-mobile-hero h4 { font-size: 1.7rem; }
    .inbox-action-card { padding: 16px; }
  }
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..fd1084b5460a5a1fb52deccd6a90ff5cd0a0003c
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -1226,6 +1236,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1279,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1298,10 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1313,11 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1356,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1402,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1419,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1476,6 +1496,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(inboxItems, quarterReadiness);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (quarterReadiness?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1573,6 +1597,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2068,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2321,8 +2531,8 @@
       <h4>Finish Now Queue</h4>
       <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
       <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
+        {activeFinishNowQueue.length === 0 && <div className="alert alert-success">No blocking items.</div>}
+        {activeFinishNowQueue.map((item) => (
           <div key={item.id} className="attention-item">
             <div>
               <b>{item.merchant || 'Unknown'}</b>
@@ -2666,9 +2876,27 @@
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
 
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2844,9 +3072,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3112,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3124,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3135,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3149,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
@@ -500,2 +500,8 @@
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
apply_patch(auto_approved=true) exited 0 in 547ms:
Success. Updated the following files:
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..4335d8bf9a969901dc6d236d9811053425ebae45
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -1226,6 +1242,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1285,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1304,10 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1319,11 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1362,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1408,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1425,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1476,6 +1502,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(inboxItems, quarterReadiness);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (quarterReadiness?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1573,6 +1603,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2074,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2321,8 +2537,8 @@
       <h4>Finish Now Queue</h4>
       <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
       <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
+        {activeFinishNowQueue.length === 0 && <div className="alert alert-success">No blocking items.</div>}
+        {activeFinishNowQueue.map((item) => (
           <div key={item.id} className="attention-item">
             <div>
               <b>{item.merchant || 'Unknown'}</b>
@@ -2666,9 +2882,27 @@
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2844,9 +3078,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3118,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3130,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3141,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3155,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..4335d8bf9a969901dc6d236d9811053425ebae45
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -1226,6 +1242,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1285,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1304,10 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1319,11 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1362,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1408,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1425,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1476,6 +1502,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(inboxItems, quarterReadiness);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (quarterReadiness?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1573,6 +1603,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2074,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2321,8 +2537,8 @@
       <h4>Finish Now Queue</h4>
       <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
       <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
+        {activeFinishNowQueue.length === 0 && <div className="alert alert-success">No blocking items.</div>}
+        {activeFinishNowQueue.map((item) => (
           <div key={item.id} className="attention-item">
             <div>
               <b>{item.merchant || 'Unknown'}</b>
@@ -2666,9 +2882,27 @@
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2844,9 +3078,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3118,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3130,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3141,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3155,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
@@ -623,4 +623,5 @@
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
apply_patch(auto_approved=true) exited 0 in 452ms:
Success. Updated the following files:
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..1ada6637c586db39c5d67cf6e69934b93f2e33cb
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1476,6 +1512,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(inboxItems, quarterReadiness);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (quarterReadiness?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1573,6 +1613,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2084,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2321,8 +2547,8 @@
       <h4>Finish Now Queue</h4>
       <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
       <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
+        {activeFinishNowQueue.length === 0 && <div className="alert alert-success">No blocking items.</div>}
+        {activeFinishNowQueue.map((item) => (
           <div key={item.id} className="attention-item">
             <div>
               <b>{item.merchant || 'Unknown'}</b>
@@ -2666,9 +2892,27 @@
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
 
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2844,9 +3088,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3128,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3140,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3151,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3165,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..1ada6637c586db39c5d67cf6e69934b93f2e33cb
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1476,6 +1512,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(inboxItems, quarterReadiness);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (quarterReadiness?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1573,6 +1613,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2084,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2321,8 +2547,8 @@
       <h4>Finish Now Queue</h4>
       <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
       <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
+        {activeFinishNowQueue.length === 0 && <div className="alert alert-success">No blocking items.</div>}
+        {activeFinishNowQueue.map((item) => (
           <div key={item.id} className="attention-item">
             <div>
               <b>{item.merchant || 'Unknown'}</b>
@@ -2666,9 +2892,27 @@
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
 
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2844,9 +3088,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3128,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3140,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3151,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3165,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
@@ -2551,3 +2551,3 @@
         {activeFinishNowQueue.map((item) => (
-          <div key={item.id} className="attention-item">
+          <div key={item.id || item.affected_entity_id} className="attention-item">
             <div>
@@ -2559,3 +2559,3 @@
               <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
+              <small className="text-danger">Blocker: {item.blocker_reason || item.issue_type}</small>
             </div>
apply_patch(auto_approved=true) exited 0 in 651ms:
Success. Updated the following files:
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..705603b5cf577a8a72f4ad868630a7bb2f556a95
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1476,6 +1512,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(inboxItems, quarterReadiness);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (quarterReadiness?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1573,6 +1613,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2084,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2321,9 +2547,9 @@
       <h4>Finish Now Queue</h4>
       <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
       <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
+        {activeFinishNowQueue.length === 0 && <div className="alert alert-success">No blocking items.</div>}
+        {activeFinishNowQueue.map((item) => (
+          <div key={item.id || item.affected_entity_id} className="attention-item">
             <div>
               <b>{item.merchant || 'Unknown'}</b>
               <br />
@@ -2331,7 +2557,7 @@
                 {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
               </small>
               <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
+              <small className="text-danger">Blocker: {item.blocker_reason || item.issue_type}</small>
             </div>
           </div>
         ))}
@@ -2666,9 +2892,27 @@
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
 
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2844,9 +3088,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3128,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3140,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3151,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3165,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..705603b5cf577a8a72f4ad868630a7bb2f556a95
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1476,6 +1512,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(inboxItems, quarterReadiness);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (quarterReadiness?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1573,6 +1613,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2084,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2321,9 +2547,9 @@
       <h4>Finish Now Queue</h4>
       <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
       <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
+        {activeFinishNowQueue.length === 0 && <div className="alert alert-success">No blocking items.</div>}
+        {activeFinishNowQueue.map((item) => (
+          <div key={item.id || item.affected_entity_id} className="attention-item">
             <div>
               <b>{item.merchant || 'Unknown'}</b>
               <br />
@@ -2331,7 +2557,7 @@
                 {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
               </small>
               <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
+              <small className="text-danger">Blocker: {item.blocker_reason || item.issue_type}</small>
             </div>
           </div>
         ))}
@@ -2666,9 +2892,27 @@
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
 
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2844,9 +3088,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3128,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3140,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3151,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3165,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n \"quarterReadiness\\?\\.|inboxItems\\.length \\|\\||setInboxItems\\(|issueList = Array.isArray\\(report\\?\\.issue_list\\)\" 'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\App.jsx'" in C:\Users\edebe\eds succeeded in 828ms:
1326:        setInboxItems(READINESS_DEMO_REPORT.issue_list);
1488:      setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
1518:  const readyToExportCount = Math.max(0, (quarterReadiness?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
1632:    setInboxItems((previous) => previous
1713:        setInboxItems(READINESS_DEMO_REPORT.issue_list);
2944:    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
2966:          readiness_pct: quarterReadiness?.readiness_pct ?? null
2981:    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
3032:    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..705603b5cf577a8a72f4ad868630a7bb2f556a95
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1476,6 +1512,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(inboxItems, quarterReadiness);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (quarterReadiness?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1573,6 +1613,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2084,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2321,9 +2547,9 @@
       <h4>Finish Now Queue</h4>
       <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
       <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
+        {activeFinishNowQueue.length === 0 && <div className="alert alert-success">No blocking items.</div>}
+        {activeFinishNowQueue.map((item) => (
+          <div key={item.id || item.affected_entity_id} className="attention-item">
             <div>
               <b>{item.merchant || 'Unknown'}</b>
               <br />
@@ -2331,7 +2557,7 @@
                 {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
               </small>
               <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
+              <small className="text-danger">Blocker: {item.blocker_reason || item.issue_type}</small>
             </div>
           </div>
         ))}
@@ -2666,9 +2892,27 @@
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
 
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2844,9 +3088,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3128,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3140,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3151,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3165,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$p='"'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\App.jsx'; (Get-Content "'$p | Select-Object -Skip 1502 -First 28) -join "`n"' in C:\Users\edebe\eds succeeded in 778ms:
    } finally {
      setLoading(false);
    }
  };

  const toggleTheme = async () => {
    const nextTheme = theme === 'light' ? 'dark' : 'light';
    setTheme(nextTheme);
    await Preferences.set({ key: 'theme', value: nextTheme });
  };

  const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
  const inboxCards = buildInboxCards(inboxItems, quarterReadiness);
  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
  const readyToExportCount = Math.max(0, (quarterReadiness?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);

  const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
  const currentPermissions = getPermissionsForRole(currentUser.role);
  const canManageGovernance = hasPermission(currentUser, PERMISSIONS.GOVERNANCE_MANAGE);
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..705603b5cf577a8a72f4ad868630a7bb2f556a95
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1476,6 +1512,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(inboxItems, quarterReadiness);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (quarterReadiness?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1573,6 +1613,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2084,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2321,9 +2547,9 @@
       <h4>Finish Now Queue</h4>
       <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
       <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
+        {activeFinishNowQueue.length === 0 && <div className="alert alert-success">No blocking items.</div>}
+        {activeFinishNowQueue.map((item) => (
+          <div key={item.id || item.affected_entity_id} className="attention-item">
             <div>
               <b>{item.merchant || 'Unknown'}</b>
               <br />
@@ -2331,7 +2557,7 @@
                 {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
               </small>
               <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
+              <small className="text-danger">Blocker: {item.blocker_reason || item.issue_type}</small>
             </div>
           </div>
         ))}
@@ -2666,9 +2892,27 @@
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
 
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2844,9 +3088,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3128,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3140,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3151,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3165,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

codex
I hit a consistency gap while wiring the live state: some inbox/dashboard calculations still read the raw report, while the new quarter model needs to become the single source of truth. I’m normalizing that now so the quarter screen, inbox cards, and export counters all move together.
file update
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
@@ -1507,2 +1507,21 @@
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
apply_patch(auto_approved=true) exited 0 in 455ms:
Success. Updated the following files:
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..5f156a3db8567177c09753edbeb5b16a000c7a42
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,25 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1531,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(inboxItems, quarterReadiness);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (quarterReadiness?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1573,6 +1632,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2103,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2321,9 +2566,9 @@
       <h4>Finish Now Queue</h4>
       <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
       <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
+        {activeFinishNowQueue.length === 0 && <div className="alert alert-success">No blocking items.</div>}
+        {activeFinishNowQueue.map((item) => (
+          <div key={item.id || item.affected_entity_id} className="attention-item">
             <div>
               <b>{item.merchant || 'Unknown'}</b>
               <br />
@@ -2331,7 +2576,7 @@
                 {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
               </small>
               <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
+              <small className="text-danger">Blocker: {item.blocker_reason || item.issue_type}</small>
             </div>
           </div>
         ))}
@@ -2666,9 +2911,27 @@
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
 
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2844,9 +3107,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3147,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3159,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3170,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3184,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..5f156a3db8567177c09753edbeb5b16a000c7a42
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,25 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1531,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(inboxItems, quarterReadiness);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (quarterReadiness?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1573,6 +1632,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2103,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2321,9 +2566,9 @@
       <h4>Finish Now Queue</h4>
       <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
       <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
+        {activeFinishNowQueue.length === 0 && <div className="alert alert-success">No blocking items.</div>}
+        {activeFinishNowQueue.map((item) => (
+          <div key={item.id || item.affected_entity_id} className="attention-item">
             <div>
               <b>{item.merchant || 'Unknown'}</b>
               <br />
@@ -2331,7 +2576,7 @@
                 {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
               </small>
               <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
+              <small className="text-danger">Blocker: {item.blocker_reason || item.issue_type}</small>
             </div>
           </div>
         ))}
@@ -2666,9 +2911,27 @@
   const selectedSnapshot = snapshotWorkspace?.snapshots?.find((snapshot) => snapshot.snapshot_id === selectedSnapshotId)
     || snapshotWorkspace?.snapshots?.[0]
     || null;
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
 
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2844,9 +3107,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3147,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3159,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3170,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3184,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
@@ -2913,20 +2913,2 @@
     || null;
-  const quarterUiState = buildQuarterUiState({
-    report: quarterReadiness || READINESS_DEMO_REPORT,
-    resolvedIssueIds: resolvedQuarterIssueIds,
-  });
-  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
-  const activeFinishNowQueue = quarterUiState.activeIssues;
-
-  const resolveQuarterIssue = (issueId) => {
-    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
-      return;
-    }
-
-    setResolvedQuarterIssueIds((previous) => (
-      previous.includes(issueId) ? previous : [...previous, issueId]
-    ));
-    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
-    setQuarterActionError('');
-  };
 
apply_patch(auto_approved=true) exited 0 in 560ms:
Success. Updated the following files:
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..5c0350b1198d677857472190d1e2d1ac4157bbb4
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,25 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1531,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(inboxItems, quarterReadiness);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (quarterReadiness?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1573,6 +1632,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2103,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2321,9 +2566,9 @@
       <h4>Finish Now Queue</h4>
       <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
       <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
+        {activeFinishNowQueue.length === 0 && <div className="alert alert-success">No blocking items.</div>}
+        {activeFinishNowQueue.map((item) => (
+          <div key={item.id || item.affected_entity_id} className="attention-item">
             <div>
               <b>{item.merchant || 'Unknown'}</b>
               <br />
@@ -2331,7 +2576,7 @@
                 {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
               </small>
               <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
+              <small className="text-danger">Blocker: {item.blocker_reason || item.issue_type}</small>
             </div>
           </div>
         ))}
@@ -2668,7 +2913,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2844,9 +3089,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3129,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3141,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3152,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3166,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..5c0350b1198d677857472190d1e2d1ac4157bbb4
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,25 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1531,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(inboxItems, quarterReadiness);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (quarterReadiness?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1573,6 +1632,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2103,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2321,9 +2566,9 @@
       <h4>Finish Now Queue</h4>
       <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
       <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
+        {activeFinishNowQueue.length === 0 && <div className="alert alert-success">No blocking items.</div>}
+        {activeFinishNowQueue.map((item) => (
+          <div key={item.id || item.affected_entity_id} className="attention-item">
             <div>
               <b>{item.merchant || 'Unknown'}</b>
               <br />
@@ -2331,7 +2576,7 @@
                 {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
               </small>
               <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
+              <small className="text-danger">Blocker: {item.blocker_reason || item.issue_type}</small>
             </div>
           </div>
         ))}
@@ -2668,7 +2913,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2844,9 +3089,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3129,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3141,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3152,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3166,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
@@ -1533,6 +1533,6 @@
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
-  const inboxCards = buildInboxCards(inboxItems, quarterReadiness);
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
   const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
   const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
-  const readyToExportCount = Math.max(0, (quarterReadiness?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
@@ -1548,3 +1548,3 @@
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
apply_patch(auto_approved=true) exited 0 in 611ms:
Success. Updated the following files:
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..9aeb189af2b6847c7add067d936535ebe5b9b4de
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,25 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1531,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1546,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1632,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2103,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2321,9 +2566,9 @@
       <h4>Finish Now Queue</h4>
       <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
       <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
+        {activeFinishNowQueue.length === 0 && <div className="alert alert-success">No blocking items.</div>}
+        {activeFinishNowQueue.map((item) => (
+          <div key={item.id || item.affected_entity_id} className="attention-item">
             <div>
               <b>{item.merchant || 'Unknown'}</b>
               <br />
@@ -2331,7 +2576,7 @@
                 {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
               </small>
               <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
+              <small className="text-danger">Blocker: {item.blocker_reason || item.issue_type}</small>
             </div>
           </div>
         ))}
@@ -2668,7 +2913,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2844,9 +3089,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3129,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3141,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3152,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3166,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..9aeb189af2b6847c7add067d936535ebe5b9b4de
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,25 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1531,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1546,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1632,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2103,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2321,9 +2566,9 @@
       <h4>Finish Now Queue</h4>
       <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
       <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
+        {activeFinishNowQueue.length === 0 && <div className="alert alert-success">No blocking items.</div>}
+        {activeFinishNowQueue.map((item) => (
+          <div key={item.id || item.affected_entity_id} className="attention-item">
             <div>
               <b>{item.merchant || 'Unknown'}</b>
               <br />
@@ -2331,7 +2576,7 @@
                 {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
               </small>
               <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
+              <small className="text-danger">Blocker: {item.blocker_reason || item.issue_type}</small>
             </div>
           </div>
         ))}
@@ -2668,7 +2913,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2844,9 +3089,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3129,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3141,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3152,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3166,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n \"inboxItems\" 'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\App.jsx'" in C:\Users\edebe\eds succeeded in 991ms:
1287:  const [inboxItems, setInboxItems] = useState([]);
1549:    inboxItems: activeFinishNowQueue,
2758:              {inboxItems.slice(0, 4).map((item) => (
2770:              {inboxItems.length === 0 && (
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..9aeb189af2b6847c7add067d936535ebe5b9b4de
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,25 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1531,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1546,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1632,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2103,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2321,9 +2566,9 @@
       <h4>Finish Now Queue</h4>
       <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
       <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
+        {activeFinishNowQueue.length === 0 && <div className="alert alert-success">No blocking items.</div>}
+        {activeFinishNowQueue.map((item) => (
+          <div key={item.id || item.affected_entity_id} className="attention-item">
             <div>
               <b>{item.merchant || 'Unknown'}</b>
               <br />
@@ -2331,7 +2576,7 @@
                 {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
               </small>
               <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
+              <small className="text-danger">Blocker: {item.blocker_reason || item.issue_type}</small>
             </div>
           </div>
         ))}
@@ -2668,7 +2913,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2844,9 +3089,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3129,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3141,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3152,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3166,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
@@ -2757,3 +2757,3 @@
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
                 <div key={item.id} className="control-row">
@@ -2761,3 +2761,3 @@
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
@@ -2769,3 +2769,3 @@
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
apply_patch(auto_approved=true) exited 0 in 511ms:
Success. Updated the following files:
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..f70eeda170a4ae73fda01f201860560dc652cbec
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,25 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1531,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1546,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1632,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2103,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2321,9 +2566,9 @@
       <h4>Finish Now Queue</h4>
       <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
       <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
+        {activeFinishNowQueue.length === 0 && <div className="alert alert-success">No blocking items.</div>}
+        {activeFinishNowQueue.map((item) => (
+          <div key={item.id || item.affected_entity_id} className="attention-item">
             <div>
               <b>{item.merchant || 'Unknown'}</b>
               <br />
@@ -2331,7 +2576,7 @@
                 {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
               </small>
               <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
+              <small className="text-danger">Blocker: {item.blocker_reason || item.issue_type}</small>
             </div>
           </div>
         ))}
@@ -2510,11 +2755,11 @@
           <section className="readiness-panel">
             <h5 style={{ marginBottom: 12 }}>Queue Backlog</h5>
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
                 <div key={item.id} className="control-row">
                   <div>
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
                   <div className="text-end">
                     <div className="small-muted">{item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date}</div>
@@ -2522,7 +2767,7 @@
                   </div>
                 </div>
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
               )}
             </div>
@@ -2668,7 +2913,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2844,9 +3089,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3129,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3141,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3152,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3166,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..f70eeda170a4ae73fda01f201860560dc652cbec
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,25 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1531,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1546,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1632,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2103,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2321,9 +2566,9 @@
       <h4>Finish Now Queue</h4>
       <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
       <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
+        {activeFinishNowQueue.length === 0 && <div className="alert alert-success">No blocking items.</div>}
+        {activeFinishNowQueue.map((item) => (
+          <div key={item.id || item.affected_entity_id} className="attention-item">
             <div>
               <b>{item.merchant || 'Unknown'}</b>
               <br />
@@ -2331,7 +2576,7 @@
                 {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
               </small>
               <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
+              <small className="text-danger">Blocker: {item.blocker_reason || item.issue_type}</small>
             </div>
           </div>
         ))}
@@ -2510,11 +2755,11 @@
           <section className="readiness-panel">
             <h5 style={{ marginBottom: 12 }}>Queue Backlog</h5>
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
                 <div key={item.id} className="control-row">
                   <div>
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
                   <div className="text-end">
                     <div className="small-muted">{item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date}</div>
@@ -2522,7 +2767,7 @@
                   </div>
                 </div>
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
               )}
             </div>
@@ -2668,7 +2913,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2844,9 +3089,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3129,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3141,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3152,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3166,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
@@ -2758,3 +2758,3 @@
               {activeFinishNowQueue.slice(0, 4).map((item) => (
-                <div key={item.id} className="control-row">
+                <div key={item.id || item.affected_entity_id} className="control-row">
                   <div>
apply_patch(auto_approved=true) exited 0 in 823ms:
Success. Updated the following files:
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..50dccb3d576f05c84fb2736dd2a491c61cd933eb
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,25 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1531,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1546,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1632,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2103,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2321,9 +2566,9 @@
       <h4>Finish Now Queue</h4>
       <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
       <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
+        {activeFinishNowQueue.length === 0 && <div className="alert alert-success">No blocking items.</div>}
+        {activeFinishNowQueue.map((item) => (
+          <div key={item.id || item.affected_entity_id} className="attention-item">
             <div>
               <b>{item.merchant || 'Unknown'}</b>
               <br />
@@ -2331,7 +2576,7 @@
                 {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
               </small>
               <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
+              <small className="text-danger">Blocker: {item.blocker_reason || item.issue_type}</small>
             </div>
           </div>
         ))}
@@ -2510,11 +2755,11 @@
           <section className="readiness-panel">
             <h5 style={{ marginBottom: 12 }}>Queue Backlog</h5>
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
-                <div key={item.id} className="control-row">
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
+                <div key={item.id || item.affected_entity_id} className="control-row">
                   <div>
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
                   <div className="text-end">
                     <div className="small-muted">{item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date}</div>
@@ -2522,7 +2767,7 @@
                   </div>
                 </div>
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
               )}
             </div>
@@ -2668,7 +2913,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2844,9 +3089,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3129,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3141,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3152,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3166,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..50dccb3d576f05c84fb2736dd2a491c61cd933eb
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,25 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1531,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1546,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1632,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2103,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2321,9 +2566,9 @@
       <h4>Finish Now Queue</h4>
       <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
       <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
+        {activeFinishNowQueue.length === 0 && <div className="alert alert-success">No blocking items.</div>}
+        {activeFinishNowQueue.map((item) => (
+          <div key={item.id || item.affected_entity_id} className="attention-item">
             <div>
               <b>{item.merchant || 'Unknown'}</b>
               <br />
@@ -2331,7 +2576,7 @@
                 {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
               </small>
               <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
+              <small className="text-danger">Blocker: {item.blocker_reason || item.issue_type}</small>
             </div>
           </div>
         ))}
@@ -2510,11 +2755,11 @@
           <section className="readiness-panel">
             <h5 style={{ marginBottom: 12 }}>Queue Backlog</h5>
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
-                <div key={item.id} className="control-row">
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
+                <div key={item.id || item.affected_entity_id} className="control-row">
                   <div>
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
                   <div className="text-end">
                     <div className="small-muted">{item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date}</div>
@@ -2522,7 +2767,7 @@
                   </div>
                 </div>
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
               )}
             </div>
@@ -2668,7 +2913,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2844,9 +3089,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3129,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3141,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3152,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3166,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
@@ -2944,3 +2944,3 @@
   const createSnapshotVersion = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     const status = snapshotWorkspace?.snapshot_status;
@@ -2966,3 +2966,3 @@
           source_type: 'manual',
-          readiness_pct: quarterReadiness?.readiness_pct ?? null
+          readiness_pct: activeQuarterReport?.readiness_pct ?? null
         }
@@ -2981,3 +2981,3 @@
   const closeQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(closeReason || '').trim()) {
@@ -3032,3 +3032,3 @@
   const reopenQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(reopenReason || '').trim()) {
apply_patch(auto_approved=true) exited 0 in 524ms:
Success. Updated the following files:
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..701218c1422c616f1b36e1a166c290d13cddf55f
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,25 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1531,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1546,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1632,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2103,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2321,9 +2566,9 @@
       <h4>Finish Now Queue</h4>
       <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
       <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
+        {activeFinishNowQueue.length === 0 && <div className="alert alert-success">No blocking items.</div>}
+        {activeFinishNowQueue.map((item) => (
+          <div key={item.id || item.affected_entity_id} className="attention-item">
             <div>
               <b>{item.merchant || 'Unknown'}</b>
               <br />
@@ -2331,7 +2576,7 @@
                 {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
               </small>
               <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
+              <small className="text-danger">Blocker: {item.blocker_reason || item.issue_type}</small>
             </div>
           </div>
         ))}
@@ -2510,11 +2755,11 @@
           <section className="readiness-panel">
             <h5 style={{ marginBottom: 12 }}>Queue Backlog</h5>
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
-                <div key={item.id} className="control-row">
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
+                <div key={item.id || item.affected_entity_id} className="control-row">
                   <div>
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
                   <div className="text-end">
                     <div className="small-muted">{item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date}</div>
@@ -2522,7 +2767,7 @@
                   </div>
                 </div>
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
               )}
             </div>
@@ -2668,7 +2913,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2697,7 +2942,7 @@
   };
 
   const createSnapshotVersion = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     const status = snapshotWorkspace?.snapshot_status;
 
     if (!status?.can_create_snapshot_version) {
@@ -2719,7 +2964,7 @@
         description: `Quarter snapshot generated from Quarter workspace`,
         metadata: {
           source_type: 'manual',
-          readiness_pct: quarterReadiness?.readiness_pct ?? null
+          readiness_pct: activeQuarterReport?.readiness_pct ?? null
         }
       });
       await refreshQuarterWorkspace();
@@ -2734,7 +2979,7 @@
   };
 
   const closeQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(closeReason || '').trim()) {
       setQuarterActionError('Close reason is required.');
       setQuarterActionMessage('');
@@ -2785,7 +3030,7 @@
   };
 
   const reopenQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(reopenReason || '').trim()) {
       setQuarterActionError('Reopen reason is required.');
       setQuarterActionMessage('');
@@ -2844,9 +3089,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3129,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3141,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3152,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3166,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..701218c1422c616f1b36e1a166c290d13cddf55f
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,25 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1531,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1546,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1632,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2103,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2321,9 +2566,9 @@
       <h4>Finish Now Queue</h4>
       <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
       <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
+        {activeFinishNowQueue.length === 0 && <div className="alert alert-success">No blocking items.</div>}
+        {activeFinishNowQueue.map((item) => (
+          <div key={item.id || item.affected_entity_id} className="attention-item">
             <div>
               <b>{item.merchant || 'Unknown'}</b>
               <br />
@@ -2331,7 +2576,7 @@
                 {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
               </small>
               <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
+              <small className="text-danger">Blocker: {item.blocker_reason || item.issue_type}</small>
             </div>
           </div>
         ))}
@@ -2510,11 +2755,11 @@
           <section className="readiness-panel">
             <h5 style={{ marginBottom: 12 }}>Queue Backlog</h5>
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
-                <div key={item.id} className="control-row">
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
+                <div key={item.id || item.affected_entity_id} className="control-row">
                   <div>
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
                   <div className="text-end">
                     <div className="small-muted">{item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date}</div>
@@ -2522,7 +2767,7 @@
                   </div>
                 </div>
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
               )}
             </div>
@@ -2668,7 +2913,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2697,7 +2942,7 @@
   };
 
   const createSnapshotVersion = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     const status = snapshotWorkspace?.snapshot_status;
 
     if (!status?.can_create_snapshot_version) {
@@ -2719,7 +2964,7 @@
         description: `Quarter snapshot generated from Quarter workspace`,
         metadata: {
           source_type: 'manual',
-          readiness_pct: quarterReadiness?.readiness_pct ?? null
+          readiness_pct: activeQuarterReport?.readiness_pct ?? null
         }
       });
       await refreshQuarterWorkspace();
@@ -2734,7 +2979,7 @@
   };
 
   const closeQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(closeReason || '').trim()) {
       setQuarterActionError('Close reason is required.');
       setQuarterActionMessage('');
@@ -2785,7 +3030,7 @@
   };
 
   const reopenQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(reopenReason || '').trim()) {
       setQuarterActionError('Reopen reason is required.');
       setQuarterActionMessage('');
@@ -2844,9 +3089,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3129,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3141,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3152,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3166,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update
A C:\Users\edebe\eds\bizPA\frontend\src\quarterReadiness.test.js
import {
  buildQuarterUiState,
  deriveResolvedIssueIdsFromSearchParams,
} from './quarterReadiness';

const reportFixture = {
  quarter_reference: 'Q1-2026',
  period_start: '2026-01-01',
  period_end: '2026-03-31',
  total_txns_in_period: 18,
  readiness_pct: 78,
  can_export: false,
  issue_list: [
    {
      issue_type: 'missing_category',
      affected_entity_id: 'txn-9101',
      severity: 'high',
      weight: 35,
      explanation: 'Category missing for Fuel Stop.',
      txn_date: '2026-02-04',
      merchant: 'Fuel Stop',
      amount: 64.18,
      direction: 'out',
      resolution_target: {
        label: 'Classify transaction',
        workflow: '/inbox/finish-now',
      },
    },
    {
      issue_type: 'missing_business_personal',
      affected_entity_id: 'txn-9103',
      severity: 'high',
      weight: 30,
      explanation: 'Business/personal decision missing.',
      txn_date: '2026-02-15',
      merchant: 'Mobile Contract',
      amount: 42.5,
      direction: 'out',
      resolution_target: {
        label: 'Set business or personal',
        workflow: '/inbox/finish-now',
      },
    },
    {
      issue_type: 'unresolved_duplicate',
      affected_entity_id: 'txn-9104',
      severity: 'medium',
      weight: 15,
      explanation: 'Duplicate not resolved.',
      txn_date: '2026-03-02',
      merchant: 'Client Payment',
      amount: 820,
      direction: 'in',
      resolution_target: {
        label: 'Resolve duplicate',
        workflow: '/inbox/finish-now',
      },
    },
  ],
};

describe('quarter readiness helpers', () => {
  test('keeps blockers ordered and updates headline while blockers remain', () => {
    const state = buildQuarterUiState({ report: reportFixture, resolvedIssueIds: ['txn-9104'] });

    expect(state.itemsLeft).toBe(2);
    expect(state.exportEnabled).toBe(false);
    expect(state.report.readiness_pct).toBeGreaterThan(78);
    expect(state.headline).toContain('items left');
    expect(state.activeIssues.map((issue) => issue.affected_entity_id)).toEqual(['txn-9101', 'txn-9103']);
    expect(state.report.explanation_lines[0]).toContain('Readiness is');
  });

  test('unlocks export immediately when every blocker is resolved', () => {
    const state = buildQuarterUiState({
      report: reportFixture,
      resolvedIssueIds: ['txn-9101', 'txn-9103', 'txn-9104'],
    });

    expect(state.itemsLeft).toBe(0);
    expect(state.exportEnabled).toBe(true);
    expect(state.report.readiness_pct).toBe(100);
    expect(state.report.can_export).toBe(true);
    expect(state.report.issue_summary).toEqual([]);
  });

  test('supports demo query params that resolve all blockers', () => {
    const params = new URLSearchParams('resolvedDemoIssues=all');
    const resolvedIds = deriveResolvedIssueIdsFromSearchParams(params, reportFixture.issue_list);

    expect(resolvedIds).toEqual(['txn-9101', 'txn-9103', 'txn-9104']);
  });
});
apply_patch(auto_approved=true) exited 0 in 638ms:
Success. Updated the following files:
A C:\Users\edebe\eds\bizPA\frontend\src\quarterReadiness.test.js
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..701218c1422c616f1b36e1a166c290d13cddf55f
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,25 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1531,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1546,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1632,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2103,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2321,9 +2566,9 @@
       <h4>Finish Now Queue</h4>
       <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
       <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
+        {activeFinishNowQueue.length === 0 && <div className="alert alert-success">No blocking items.</div>}
+        {activeFinishNowQueue.map((item) => (
+          <div key={item.id || item.affected_entity_id} className="attention-item">
             <div>
               <b>{item.merchant || 'Unknown'}</b>
               <br />
@@ -2331,7 +2576,7 @@
                 {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
               </small>
               <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
+              <small className="text-danger">Blocker: {item.blocker_reason || item.issue_type}</small>
             </div>
           </div>
         ))}
@@ -2510,11 +2755,11 @@
           <section className="readiness-panel">
             <h5 style={{ marginBottom: 12 }}>Queue Backlog</h5>
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
-                <div key={item.id} className="control-row">
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
+                <div key={item.id || item.affected_entity_id} className="control-row">
                   <div>
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
                   <div className="text-end">
                     <div className="small-muted">{item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date}</div>
@@ -2522,7 +2767,7 @@
                   </div>
                 </div>
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
               )}
             </div>
@@ -2668,7 +2913,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2697,7 +2942,7 @@
   };
 
   const createSnapshotVersion = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     const status = snapshotWorkspace?.snapshot_status;
 
     if (!status?.can_create_snapshot_version) {
@@ -2719,7 +2964,7 @@
         description: `Quarter snapshot generated from Quarter workspace`,
         metadata: {
           source_type: 'manual',
-          readiness_pct: quarterReadiness?.readiness_pct ?? null
+          readiness_pct: activeQuarterReport?.readiness_pct ?? null
         }
       });
       await refreshQuarterWorkspace();
@@ -2734,7 +2979,7 @@
   };
 
   const closeQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(closeReason || '').trim()) {
       setQuarterActionError('Close reason is required.');
       setQuarterActionMessage('');
@@ -2785,7 +3030,7 @@
   };
 
   const reopenQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(reopenReason || '').trim()) {
       setQuarterActionError('Reopen reason is required.');
       setQuarterActionMessage('');
@@ -2844,9 +3089,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3129,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3141,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3152,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3166,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/bizPA/frontend/src/quarterReadiness.test.js b/bizPA/frontend/src/quarterReadiness.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..0b810191a89d8b82091bf5de1408afcc23c14e65
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.test.js
@@ -0,0 +1,93 @@
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
+
+const reportFixture = {
+  quarter_reference: 'Q1-2026',
+  period_start: '2026-01-01',
+  period_end: '2026-03-31',
+  total_txns_in_period: 18,
+  readiness_pct: 78,
+  can_export: false,
+  issue_list: [
+    {
+      issue_type: 'missing_category',
+      affected_entity_id: 'txn-9101',
+      severity: 'high',
+      weight: 35,
+      explanation: 'Category missing for Fuel Stop.',
+      txn_date: '2026-02-04',
+      merchant: 'Fuel Stop',
+      amount: 64.18,
+      direction: 'out',
+      resolution_target: {
+        label: 'Classify transaction',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'missing_business_personal',
+      affected_entity_id: 'txn-9103',
+      severity: 'high',
+      weight: 30,
+      explanation: 'Business/personal decision missing.',
+      txn_date: '2026-02-15',
+      merchant: 'Mobile Contract',
+      amount: 42.5,
+      direction: 'out',
+      resolution_target: {
+        label: 'Set business or personal',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'unresolved_duplicate',
+      affected_entity_id: 'txn-9104',
+      severity: 'medium',
+      weight: 15,
+      explanation: 'Duplicate not resolved.',
+      txn_date: '2026-03-02',
+      merchant: 'Client Payment',
+      amount: 820,
+      direction: 'in',
+      resolution_target: {
+        label: 'Resolve duplicate',
+        workflow: '/inbox/finish-now',
+      },
+    },
+  ],
+};
+
+describe('quarter readiness helpers', () => {
+  test('keeps blockers ordered and updates headline while blockers remain', () => {
+    const state = buildQuarterUiState({ report: reportFixture, resolvedIssueIds: ['txn-9104'] });
+
+    expect(state.itemsLeft).toBe(2);
+    expect(state.exportEnabled).toBe(false);
+    expect(state.report.readiness_pct).toBeGreaterThan(78);
+    expect(state.headline).toContain('items left');
+    expect(state.activeIssues.map((issue) => issue.affected_entity_id)).toEqual(['txn-9101', 'txn-9103']);
+    expect(state.report.explanation_lines[0]).toContain('Readiness is');
+  });
+
+  test('unlocks export immediately when every blocker is resolved', () => {
+    const state = buildQuarterUiState({
+      report: reportFixture,
+      resolvedIssueIds: ['txn-9101', 'txn-9103', 'txn-9104'],
+    });
+
+    expect(state.itemsLeft).toBe(0);
+    expect(state.exportEnabled).toBe(true);
+    expect(state.report.readiness_pct).toBe(100);
+    expect(state.report.can_export).toBe(true);
+    expect(state.report.issue_summary).toEqual([]);
+  });
+
+  test('supports demo query params that resolve all blockers', () => {
+    const params = new URLSearchParams('resolvedDemoIssues=all');
+    const resolvedIds = deriveResolvedIssueIdsFromSearchParams(params, reportFixture.issue_list);
+
+    expect(resolvedIds).toEqual(['txn-9101', 'txn-9103', 'txn-9104']);
+  });
+});
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..701218c1422c616f1b36e1a166c290d13cddf55f
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,25 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1531,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1546,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1632,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2103,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2321,9 +2566,9 @@
       <h4>Finish Now Queue</h4>
       <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
       <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
+        {activeFinishNowQueue.length === 0 && <div className="alert alert-success">No blocking items.</div>}
+        {activeFinishNowQueue.map((item) => (
+          <div key={item.id || item.affected_entity_id} className="attention-item">
             <div>
               <b>{item.merchant || 'Unknown'}</b>
               <br />
@@ -2331,7 +2576,7 @@
                 {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
               </small>
               <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
+              <small className="text-danger">Blocker: {item.blocker_reason || item.issue_type}</small>
             </div>
           </div>
         ))}
@@ -2510,11 +2755,11 @@
           <section className="readiness-panel">
             <h5 style={{ marginBottom: 12 }}>Queue Backlog</h5>
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
-                <div key={item.id} className="control-row">
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
+                <div key={item.id || item.affected_entity_id} className="control-row">
                   <div>
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
                   <div className="text-end">
                     <div className="small-muted">{item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date}</div>
@@ -2522,7 +2767,7 @@
                   </div>
                 </div>
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
               )}
             </div>
@@ -2668,7 +2913,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2697,7 +2942,7 @@
   };
 
   const createSnapshotVersion = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     const status = snapshotWorkspace?.snapshot_status;
 
     if (!status?.can_create_snapshot_version) {
@@ -2719,7 +2964,7 @@
         description: `Quarter snapshot generated from Quarter workspace`,
         metadata: {
           source_type: 'manual',
-          readiness_pct: quarterReadiness?.readiness_pct ?? null
+          readiness_pct: activeQuarterReport?.readiness_pct ?? null
         }
       });
       await refreshQuarterWorkspace();
@@ -2734,7 +2979,7 @@
   };
 
   const closeQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(closeReason || '').trim()) {
       setQuarterActionError('Close reason is required.');
       setQuarterActionMessage('');
@@ -2785,7 +3030,7 @@
   };
 
   const reopenQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(reopenReason || '').trim()) {
       setQuarterActionError('Reopen reason is required.');
       setQuarterActionMessage('');
@@ -2844,9 +3089,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3129,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3141,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3152,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3166,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/bizPA/frontend/src/quarterReadiness.test.js b/bizPA/frontend/src/quarterReadiness.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..0b810191a89d8b82091bf5de1408afcc23c14e65
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.test.js
@@ -0,0 +1,93 @@
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
+
+const reportFixture = {
+  quarter_reference: 'Q1-2026',
+  period_start: '2026-01-01',
+  period_end: '2026-03-31',
+  total_txns_in_period: 18,
+  readiness_pct: 78,
+  can_export: false,
+  issue_list: [
+    {
+      issue_type: 'missing_category',
+      affected_entity_id: 'txn-9101',
+      severity: 'high',
+      weight: 35,
+      explanation: 'Category missing for Fuel Stop.',
+      txn_date: '2026-02-04',
+      merchant: 'Fuel Stop',
+      amount: 64.18,
+      direction: 'out',
+      resolution_target: {
+        label: 'Classify transaction',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'missing_business_personal',
+      affected_entity_id: 'txn-9103',
+      severity: 'high',
+      weight: 30,
+      explanation: 'Business/personal decision missing.',
+      txn_date: '2026-02-15',
+      merchant: 'Mobile Contract',
+      amount: 42.5,
+      direction: 'out',
+      resolution_target: {
+        label: 'Set business or personal',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'unresolved_duplicate',
+      affected_entity_id: 'txn-9104',
+      severity: 'medium',
+      weight: 15,
+      explanation: 'Duplicate not resolved.',
+      txn_date: '2026-03-02',
+      merchant: 'Client Payment',
+      amount: 820,
+      direction: 'in',
+      resolution_target: {
+        label: 'Resolve duplicate',
+        workflow: '/inbox/finish-now',
+      },
+    },
+  ],
+};
+
+describe('quarter readiness helpers', () => {
+  test('keeps blockers ordered and updates headline while blockers remain', () => {
+    const state = buildQuarterUiState({ report: reportFixture, resolvedIssueIds: ['txn-9104'] });
+
+    expect(state.itemsLeft).toBe(2);
+    expect(state.exportEnabled).toBe(false);
+    expect(state.report.readiness_pct).toBeGreaterThan(78);
+    expect(state.headline).toContain('items left');
+    expect(state.activeIssues.map((issue) => issue.affected_entity_id)).toEqual(['txn-9101', 'txn-9103']);
+    expect(state.report.explanation_lines[0]).toContain('Readiness is');
+  });
+
+  test('unlocks export immediately when every blocker is resolved', () => {
+    const state = buildQuarterUiState({
+      report: reportFixture,
+      resolvedIssueIds: ['txn-9101', 'txn-9103', 'txn-9104'],
+    });
+
+    expect(state.itemsLeft).toBe(0);
+    expect(state.exportEnabled).toBe(true);
+    expect(state.report.readiness_pct).toBe(100);
+    expect(state.report.can_export).toBe(true);
+    expect(state.report.issue_summary).toEqual([]);
+  });
+
+  test('supports demo query params that resolve all blockers', () => {
+    const params = new URLSearchParams('resolvedDemoIssues=all');
+    const resolvedIds = deriveResolvedIssueIdsFromSearchParams(params, reportFixture.issue_list);
+
+    expect(resolvedIds).toEqual(['txn-9101', 'txn-9103', 'txn-9104']);
+  });
+});
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update
M C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1
@@ -6,2 +6,3 @@
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
 
@@ -16,2 +17,5 @@
 Write-Host ''
+Write-Host 'Export-ready verification URL:' -ForegroundColor Green
+Write-Host "  $exportReadyUrl" -ForegroundColor Yellow
+Write-Host ''
 Write-Host 'Base frontend URL:' -ForegroundColor Green
@@ -19,2 +23,2 @@
 Write-Host ''
-Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI can be reviewed without backend setup.' -ForegroundColor Cyan
+Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI and zero-blocker export-ready state can be reviewed without backend setup.' -ForegroundColor Cyan
apply_patch(auto_approved=true) exited 0 in 694ms:
Success. Updated the following files:
M C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..701218c1422c616f1b36e1a166c290d13cddf55f
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,25 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1531,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1546,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1632,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2103,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2321,9 +2566,9 @@
       <h4>Finish Now Queue</h4>
       <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
       <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
+        {activeFinishNowQueue.length === 0 && <div className="alert alert-success">No blocking items.</div>}
+        {activeFinishNowQueue.map((item) => (
+          <div key={item.id || item.affected_entity_id} className="attention-item">
             <div>
               <b>{item.merchant || 'Unknown'}</b>
               <br />
@@ -2331,7 +2576,7 @@
                 {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
               </small>
               <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
+              <small className="text-danger">Blocker: {item.blocker_reason || item.issue_type}</small>
             </div>
           </div>
         ))}
@@ -2510,11 +2755,11 @@
           <section className="readiness-panel">
             <h5 style={{ marginBottom: 12 }}>Queue Backlog</h5>
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
-                <div key={item.id} className="control-row">
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
+                <div key={item.id || item.affected_entity_id} className="control-row">
                   <div>
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
                   <div className="text-end">
                     <div className="small-muted">{item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date}</div>
@@ -2522,7 +2767,7 @@
                   </div>
                 </div>
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
               )}
             </div>
@@ -2668,7 +2913,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2697,7 +2942,7 @@
   };
 
   const createSnapshotVersion = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     const status = snapshotWorkspace?.snapshot_status;
 
     if (!status?.can_create_snapshot_version) {
@@ -2719,7 +2964,7 @@
         description: `Quarter snapshot generated from Quarter workspace`,
         metadata: {
           source_type: 'manual',
-          readiness_pct: quarterReadiness?.readiness_pct ?? null
+          readiness_pct: activeQuarterReport?.readiness_pct ?? null
         }
       });
       await refreshQuarterWorkspace();
@@ -2734,7 +2979,7 @@
   };
 
   const closeQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(closeReason || '').trim()) {
       setQuarterActionError('Close reason is required.');
       setQuarterActionMessage('');
@@ -2785,7 +3030,7 @@
   };
 
   const reopenQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(reopenReason || '').trim()) {
       setQuarterActionError('Reopen reason is required.');
       setQuarterActionMessage('');
@@ -2844,9 +3089,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3129,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3141,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3152,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3166,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/bizPA/frontend/src/quarterReadiness.test.js b/bizPA/frontend/src/quarterReadiness.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..0b810191a89d8b82091bf5de1408afcc23c14e65
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.test.js
@@ -0,0 +1,93 @@
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
+
+const reportFixture = {
+  quarter_reference: 'Q1-2026',
+  period_start: '2026-01-01',
+  period_end: '2026-03-31',
+  total_txns_in_period: 18,
+  readiness_pct: 78,
+  can_export: false,
+  issue_list: [
+    {
+      issue_type: 'missing_category',
+      affected_entity_id: 'txn-9101',
+      severity: 'high',
+      weight: 35,
+      explanation: 'Category missing for Fuel Stop.',
+      txn_date: '2026-02-04',
+      merchant: 'Fuel Stop',
+      amount: 64.18,
+      direction: 'out',
+      resolution_target: {
+        label: 'Classify transaction',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'missing_business_personal',
+      affected_entity_id: 'txn-9103',
+      severity: 'high',
+      weight: 30,
+      explanation: 'Business/personal decision missing.',
+      txn_date: '2026-02-15',
+      merchant: 'Mobile Contract',
+      amount: 42.5,
+      direction: 'out',
+      resolution_target: {
+        label: 'Set business or personal',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'unresolved_duplicate',
+      affected_entity_id: 'txn-9104',
+      severity: 'medium',
+      weight: 15,
+      explanation: 'Duplicate not resolved.',
+      txn_date: '2026-03-02',
+      merchant: 'Client Payment',
+      amount: 820,
+      direction: 'in',
+      resolution_target: {
+        label: 'Resolve duplicate',
+        workflow: '/inbox/finish-now',
+      },
+    },
+  ],
+};
+
+describe('quarter readiness helpers', () => {
+  test('keeps blockers ordered and updates headline while blockers remain', () => {
+    const state = buildQuarterUiState({ report: reportFixture, resolvedIssueIds: ['txn-9104'] });
+
+    expect(state.itemsLeft).toBe(2);
+    expect(state.exportEnabled).toBe(false);
+    expect(state.report.readiness_pct).toBeGreaterThan(78);
+    expect(state.headline).toContain('items left');
+    expect(state.activeIssues.map((issue) => issue.affected_entity_id)).toEqual(['txn-9101', 'txn-9103']);
+    expect(state.report.explanation_lines[0]).toContain('Readiness is');
+  });
+
+  test('unlocks export immediately when every blocker is resolved', () => {
+    const state = buildQuarterUiState({
+      report: reportFixture,
+      resolvedIssueIds: ['txn-9101', 'txn-9103', 'txn-9104'],
+    });
+
+    expect(state.itemsLeft).toBe(0);
+    expect(state.exportEnabled).toBe(true);
+    expect(state.report.readiness_pct).toBe(100);
+    expect(state.report.can_export).toBe(true);
+    expect(state.report.issue_summary).toEqual([]);
+  });
+
+  test('supports demo query params that resolve all blockers', () => {
+    const params = new URLSearchParams('resolvedDemoIssues=all');
+    const resolvedIds = deriveResolvedIssueIdsFromSearchParams(params, reportFixture.issue_list);
+
+    expect(resolvedIds).toEqual(['txn-9101', 'txn-9103', 'txn-9104']);
+  });
+});
diff --git a/bizPA/start_bizpa_tax_readiness_ui.ps1 b/bizPA/start_bizpa_tax_readiness_ui.ps1
index 1fc4bef8cd172c31fdc3db52d1acc9d4775c5485..7216e44eac386fe436a9af8e9b417cd2d61e37fe
--- a/bizPA/start_bizpa_tax_readiness_ui.ps1
+++ b/bizPA/start_bizpa_tax_readiness_ui.ps1
@@ -4,6 +4,7 @@
 $frontendPath = Join-Path $repoRoot 'frontend'
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
 
 Write-Host 'Starting bizPA tax readiness UI...' -ForegroundColor Cyan
 
@@ -14,7 +15,10 @@
 Write-Host 'Tax readiness dashboard URL:' -ForegroundColor Green
 Write-Host "  $readinessUrl" -ForegroundColor Yellow
 Write-Host ''
+Write-Host 'Export-ready verification URL:' -ForegroundColor Green
+Write-Host "  $exportReadyUrl" -ForegroundColor Yellow
+Write-Host ''
 Write-Host 'Base frontend URL:' -ForegroundColor Green
 Write-Host "  $frontendUrl" -ForegroundColor Yellow
 Write-Host ''
-Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI can be reviewed without backend setup.' -ForegroundColor Cyan
+Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI and zero-blocker export-ready state can be reviewed without backend setup.' -ForegroundColor Cyan
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..701218c1422c616f1b36e1a166c290d13cddf55f
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,25 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1531,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1546,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1632,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2103,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2321,9 +2566,9 @@
       <h4>Finish Now Queue</h4>
       <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
       <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
+        {activeFinishNowQueue.length === 0 && <div className="alert alert-success">No blocking items.</div>}
+        {activeFinishNowQueue.map((item) => (
+          <div key={item.id || item.affected_entity_id} className="attention-item">
             <div>
               <b>{item.merchant || 'Unknown'}</b>
               <br />
@@ -2331,7 +2576,7 @@
                 {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
               </small>
               <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
+              <small className="text-danger">Blocker: {item.blocker_reason || item.issue_type}</small>
             </div>
           </div>
         ))}
@@ -2510,11 +2755,11 @@
           <section className="readiness-panel">
             <h5 style={{ marginBottom: 12 }}>Queue Backlog</h5>
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
-                <div key={item.id} className="control-row">
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
+                <div key={item.id || item.affected_entity_id} className="control-row">
                   <div>
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
                   <div className="text-end">
                     <div className="small-muted">{item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date}</div>
@@ -2522,7 +2767,7 @@
                   </div>
                 </div>
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
               )}
             </div>
@@ -2668,7 +2913,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2697,7 +2942,7 @@
   };
 
   const createSnapshotVersion = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     const status = snapshotWorkspace?.snapshot_status;
 
     if (!status?.can_create_snapshot_version) {
@@ -2719,7 +2964,7 @@
         description: `Quarter snapshot generated from Quarter workspace`,
         metadata: {
           source_type: 'manual',
-          readiness_pct: quarterReadiness?.readiness_pct ?? null
+          readiness_pct: activeQuarterReport?.readiness_pct ?? null
         }
       });
       await refreshQuarterWorkspace();
@@ -2734,7 +2979,7 @@
   };
 
   const closeQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(closeReason || '').trim()) {
       setQuarterActionError('Close reason is required.');
       setQuarterActionMessage('');
@@ -2785,7 +3030,7 @@
   };
 
   const reopenQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(reopenReason || '').trim()) {
       setQuarterActionError('Reopen reason is required.');
       setQuarterActionMessage('');
@@ -2844,9 +3089,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3129,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3141,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3152,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3166,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/bizPA/frontend/src/quarterReadiness.test.js b/bizPA/frontend/src/quarterReadiness.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..0b810191a89d8b82091bf5de1408afcc23c14e65
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.test.js
@@ -0,0 +1,93 @@
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
+
+const reportFixture = {
+  quarter_reference: 'Q1-2026',
+  period_start: '2026-01-01',
+  period_end: '2026-03-31',
+  total_txns_in_period: 18,
+  readiness_pct: 78,
+  can_export: false,
+  issue_list: [
+    {
+      issue_type: 'missing_category',
+      affected_entity_id: 'txn-9101',
+      severity: 'high',
+      weight: 35,
+      explanation: 'Category missing for Fuel Stop.',
+      txn_date: '2026-02-04',
+      merchant: 'Fuel Stop',
+      amount: 64.18,
+      direction: 'out',
+      resolution_target: {
+        label: 'Classify transaction',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'missing_business_personal',
+      affected_entity_id: 'txn-9103',
+      severity: 'high',
+      weight: 30,
+      explanation: 'Business/personal decision missing.',
+      txn_date: '2026-02-15',
+      merchant: 'Mobile Contract',
+      amount: 42.5,
+      direction: 'out',
+      resolution_target: {
+        label: 'Set business or personal',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'unresolved_duplicate',
+      affected_entity_id: 'txn-9104',
+      severity: 'medium',
+      weight: 15,
+      explanation: 'Duplicate not resolved.',
+      txn_date: '2026-03-02',
+      merchant: 'Client Payment',
+      amount: 820,
+      direction: 'in',
+      resolution_target: {
+        label: 'Resolve duplicate',
+        workflow: '/inbox/finish-now',
+      },
+    },
+  ],
+};
+
+describe('quarter readiness helpers', () => {
+  test('keeps blockers ordered and updates headline while blockers remain', () => {
+    const state = buildQuarterUiState({ report: reportFixture, resolvedIssueIds: ['txn-9104'] });
+
+    expect(state.itemsLeft).toBe(2);
+    expect(state.exportEnabled).toBe(false);
+    expect(state.report.readiness_pct).toBeGreaterThan(78);
+    expect(state.headline).toContain('items left');
+    expect(state.activeIssues.map((issue) => issue.affected_entity_id)).toEqual(['txn-9101', 'txn-9103']);
+    expect(state.report.explanation_lines[0]).toContain('Readiness is');
+  });
+
+  test('unlocks export immediately when every blocker is resolved', () => {
+    const state = buildQuarterUiState({
+      report: reportFixture,
+      resolvedIssueIds: ['txn-9101', 'txn-9103', 'txn-9104'],
+    });
+
+    expect(state.itemsLeft).toBe(0);
+    expect(state.exportEnabled).toBe(true);
+    expect(state.report.readiness_pct).toBe(100);
+    expect(state.report.can_export).toBe(true);
+    expect(state.report.issue_summary).toEqual([]);
+  });
+
+  test('supports demo query params that resolve all blockers', () => {
+    const params = new URLSearchParams('resolvedDemoIssues=all');
+    const resolvedIds = deriveResolvedIssueIdsFromSearchParams(params, reportFixture.issue_list);
+
+    expect(resolvedIds).toEqual(['txn-9101', 'txn-9103', 'txn-9104']);
+  });
+});
diff --git a/bizPA/start_bizpa_tax_readiness_ui.ps1 b/bizPA/start_bizpa_tax_readiness_ui.ps1
index 1fc4bef8cd172c31fdc3db52d1acc9d4775c5485..7216e44eac386fe436a9af8e9b417cd2d61e37fe
--- a/bizPA/start_bizpa_tax_readiness_ui.ps1
+++ b/bizPA/start_bizpa_tax_readiness_ui.ps1
@@ -4,6 +4,7 @@
 $frontendPath = Join-Path $repoRoot 'frontend'
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
 
 Write-Host 'Starting bizPA tax readiness UI...' -ForegroundColor Cyan
 
@@ -14,7 +15,10 @@
 Write-Host 'Tax readiness dashboard URL:' -ForegroundColor Green
 Write-Host "  $readinessUrl" -ForegroundColor Yellow
 Write-Host ''
+Write-Host 'Export-ready verification URL:' -ForegroundColor Green
+Write-Host "  $exportReadyUrl" -ForegroundColor Yellow
+Write-Host ''
 Write-Host 'Base frontend URL:' -ForegroundColor Green
 Write-Host "  $frontendUrl" -ForegroundColor Yellow
 Write-Host ''
-Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI can be reviewed without backend setup.' -ForegroundColor Cyan
+Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI and zero-blocker export-ready state can be reviewed without backend setup.' -ForegroundColor Cyan
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update
A C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1
$ErrorActionPreference = 'Stop'

Add-Type -AssemblyName System.Windows.Forms
Add-Type -AssemblyName System.Drawing
Add-Type @"
using System;
using System.Runtime.InteropServices;

public static class WindowCaptureNative {
    [DllImport("user32.dll")]
    public static extern bool SetForegroundWindow(IntPtr hWnd);

    [DllImport("user32.dll")]
    public static extern bool ShowWindow(IntPtr hWnd, int nCmdShow);

    [DllImport("user32.dll")]
    public static extern bool GetWindowRect(IntPtr hWnd, out RECT rect);

    [StructLayout(LayoutKind.Sequential)]
    public struct RECT {
        public int Left;
        public int Top;
        public int Right;
        public int Bottom;
    }
}
"@

$frontendUrl = 'http://127.0.0.1:3002'
$readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
$verificationRoot = 'C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification'
$quarterScreenshotPath = Join-Path $verificationRoot 'quarter_readiness_screen.png'
$finishNowScreenshotPath = Join-Path $verificationRoot 'finish_now_queue_zero_blockers.png'
$chromeProfilePath = 'C:\Users\edebe\eds\workstream\verification\chrome_capture_profile_tax_readiness'
$chromePath = 'C:\Program Files\Google\Chrome\Application\chrome.exe'

function Wait-FrontendReady {
  param (
    [string]$Url
  )

  for ($i = 0; $i -lt 45; $i++) {
    Start-Sleep -Seconds 2
    try {
      $response = Invoke-WebRequest -UseBasicParsing $Url
      if ($response.StatusCode -eq 200) {
        return $response
      }
    } catch {
    }
  }

  throw "Frontend did not become ready on $Url"
}

function Capture-ChromeAppScreenshot {
  param (
    [string]$Url,
    [string]$OutputPath
  )

  $chromeArgs = @(
    "--user-data-dir=$chromeProfilePath",
    '--new-window',
    '--window-size=1600,1500',
    '--no-first-run',
    '--disable-session-crashed-bubble',
    '--disable-infobars',
    '--disable-breakpad',
    '--disable-crash-reporter',
    '--noerrdialogs',
    "--app=$Url"
  )
  Start-Process $chromePath -ArgumentList $chromeArgs | Out-Null

  $chromeWindow = $null
  for ($i = 0; $i -lt 20; $i++) {
    Start-Sleep -Seconds 1
    $chromeWindow = Get-Process chrome -ErrorAction SilentlyContinue |
      ForEach-Object {
        try {
          if ($_.MainWindowHandle -and ([int64]$_.MainWindowHandle -ne 0)) {
            [pscustomobject]@{
              Process = $_
              StartTime = $_.StartTime
            }
          }
        } catch {
        }
      } |
      Sort-Object StartTime -Descending |
      Select-Object -First 1
    if ($chromeWindow) {
      $chromeWindow = $chromeWindow.Process
      break
    }
  }

  if (-not $chromeWindow) {
    throw "Chrome window did not expose a main window handle for $Url"
  }

  [WindowCaptureNative]::ShowWindow($chromeWindow.MainWindowHandle, 3) | Out-Null
  [WindowCaptureNative]::SetForegroundWindow($chromeWindow.MainWindowHandle) | Out-Null
  Start-Sleep -Seconds 1

  $rect = New-Object WindowCaptureNative+RECT
  [WindowCaptureNative]::GetWindowRect($chromeWindow.MainWindowHandle, [ref]$rect) | Out-Null
  $width = [Math]::Max(1, $rect.Right - $rect.Left)
  $height = [Math]::Max(1, $rect.Bottom - $rect.Top)
  $bitmap = New-Object System.Drawing.Bitmap $width, $height
  $graphics = [System.Drawing.Graphics]::FromImage($bitmap)
  $graphics.CopyFromScreen($rect.Left, $rect.Top, 0, 0, $bitmap.Size)
  $bitmap.Save($OutputPath, [System.Drawing.Imaging.ImageFormat]::Png)
  $graphics.Dispose()
  $bitmap.Dispose()

  Get-Process chrome -ErrorAction SilentlyContinue |
    Where-Object { $_.StartTime -ge $chromeWindow.StartTime.AddSeconds(-2) } |
    Stop-Process -Force -ErrorAction SilentlyContinue
}

New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
New-Item -ItemType Directory -Force -Path $verificationRoot | Out-Null

$frontendJob = Start-Job -ScriptBlock {
  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
  $env:PORT = '3002'
  npm.cmd start
}

try {
  $frontendResponse = Wait-FrontendReady -Url $readinessUrl
  Capture-ChromeAppScreenshot -Url $readinessUrl -OutputPath $quarterScreenshotPath
  Capture-ChromeAppScreenshot -Url $exportReadyUrl -OutputPath $finishNowScreenshotPath

  "READINESS_URL=$readinessUrl"
  "EXPORT_READY_URL=$exportReadyUrl"
  "FRONTEND_STATUS=$($frontendResponse.StatusCode)"
  "SCREENSHOT_QUARTER=$quarterScreenshotPath"
  "SCREENSHOT_EXPORT_READY=$finishNowScreenshotPath"
  'FRONTEND_LOG_START'
  Receive-Job $frontendJob -Keep -ErrorAction SilentlyContinue 2>&1
  'FRONTEND_LOG_END'
} finally {
  Stop-Job $frontendJob -ErrorAction SilentlyContinue | Out-Null
  Remove-Job $frontendJob -Force -ErrorAction SilentlyContinue | Out-Null
}
apply_patch(auto_approved=true) exited 0 in 738ms:
Success. Updated the following files:
A C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1
D C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..0253d40f85fe6785090b08aa24db3a6f2a6d0909
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,25 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1531,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1546,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1632,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2103,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2317,25 +2562,236 @@
   );
 
   const renderInbox = () => (
-    <div>
-      <h4>Finish Now Queue</h4>
-      <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
-      <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
-            <div>
-              <b>{item.merchant || 'Unknown'}</b>
-              <br />
-              <small>
-                {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
-              </small>
-              <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
-            </div>
+    <div className="inbox-mobile-shell">
+      <section className="inbox-mobile-hero">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2">
+          <div className="inbox-kicker">
+            <Bell size={14} />
+            Mobile-first exception queue
           </div>
-        ))}
-      </div>
+          <div className="status-pill">{filteredInboxCards.length} visible</div>
+        </div>
+        <h4>Finish now</h4>
+        <p>
+          Clear the blockers that stop the quarterly export: category, business or personal, split treatment, and duplicate resolution.
+        </p>
+        <div className="inbox-mobile-stats">
+          <div className="inbox-mobile-stat">
+            <small>Queue</small>
+            <strong>{inboxCards.length}</strong>
+            <small>{activeQuarterReport.quarter_reference || READINESS_DEMO_REPORT.quarter_reference}</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Critical</small>
+            <strong>{highSeverityCount}</strong>
+            <small>High-severity blockers</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Ready now</small>
+            <strong>{readyToExportCount}</strong>
+            <small>Transactions already export-safe</small>
+          </div>
+        </div>
+        <div className="hero-actions">
+          <button className="hero-action-main" onClick={() => setCurrentTab('quarter')}>
+            <ArrowRight size={16} />
+            Review quarter readiness
+          </button>
+          <button className="hero-action-secondary" onClick={undoLastInboxAction} disabled={inboxBusyId === 'undo-last'}>
+            {inboxBusyId === 'undo-last' ? <RefreshCcw size={16} className="spinner" /> : <ArchiveRestore size={16} />}
+            Undo last action
+          </button>
+        </div>
+      </section>
+
+      {inboxMessage && (
+        <div className={`readiness-panel inbox-message ${inboxMessage.type}`}>
+          {inboxMessage.text}
+        </div>
+      )}
+
+      <section className="readiness-panel">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-3">
+          <div>
+            <h5 style={{ marginBottom: 4 }}>Queue filters</h5>
+            <small className="small-muted">Keep the mobile pass focused on one blocker type at a time.</small>
+          </div>
+          <div className="status-pill">{activeQuarterReport.readiness_pct ?? READINESS_DEMO_REPORT.readiness_pct}% ready</div>
+        </div>
+        <div className="inbox-filter-row">
+          {INBOX_FILTERS.map((filter) => {
+            const count = filter.key === 'all'
+              ? inboxCards.length
+              : inboxCards.filter((card) => card.issue_types.includes(filter.key)).length;
+            return (
+              <button
+                key={filter.key}
+                type="button"
+                className={`inbox-filter-chip ${inboxFilter === filter.key ? 'active' : ''}`}
+                onClick={() => setInboxFilter(filter.key)}
+              >
+                <span>{filter.label}</span>
+                <small>{count}</small>
+              </button>
+            );
+          })}
+        </div>
+      </section>
+
+      {filteredInboxCards.length === 0 ? (
+        <div className="inbox-empty">
+          No blockers match this filter. Reset to All blockers or return to the quarter screen to confirm readiness.
+        </div>
+      ) : (
+        <div className="inbox-card-list">
+          {filteredInboxCards.map((card) => {
+            const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+            const saving = inboxBusyId === card.id;
+            const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+            return (
+              <section key={card.id} className="inbox-action-card">
+                <div className="inbox-card-top">
+                  <div>
+                    <strong>{card.merchant}</strong>
+                    <div className="inbox-card-subline">
+                      {card.txn_date ? String(card.txn_date).slice(0, 10) : 'Date pending'} · {formatIssueAmount(card.amount, card.direction)}
+                    </div>
+                  </div>
+                  <span className={`inbox-issue-chip ${card.severity}`}>
+                    <TriangleAlert size={14} />
+                    {INBOX_REASON_LABELS[card.blocker_reason] || card.blocker_reason.replace(/_/g, ' ')}
+                  </span>
+                </div>
+
+                <div className="inbox-chip-row">
+                  {card.issue_labels.map((label) => (
+                    <span key={`${card.id}-${label}`} className={`inbox-issue-chip ${card.severity}`}>
+                      {label}
+                    </span>
+                  ))}
+                  {card.suggested_category?.category_name && (
+                    <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
+                      <CheckCircle2 size={14} />
+                      Suggested: {card.suggested_category.category_name}
+                    </button>
+                  )}
+                </div>
+
+                <p className="inbox-explainer">{card.explanation || 'Resolve the blocking fields to clear this transaction from the queue.'}</p>
+
+                {(card.supports_category || card.supports_business_personal || card.supports_split) && (
+                  <div className="inbox-action-grid">
+                    <div className="inbox-field">
+                      <label>Category code</label>
+                      <input
+                        value={draft.category_code}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_code', event.target.value)}
+                        placeholder="MATERIALS"
+                      />
+                    </div>
+                    <div className="inbox-field">
+                      <label>Category label</label>
+                      <input
+                        value={draft.category_name}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_name', event.target.value)}
+                        placeholder="Materials"
+                      />
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_business_personal || card.supports_split) && (
+                  <div>
+                    <div className="inbox-field">
+                      <label>Business treatment</label>
+                    </div>
+                    <div className="inbox-toggle-row">
+                      {[
+                        { value: 'BUSINESS', label: 'Business' },
+                        { value: 'PERSONAL', label: 'Personal' },
+                        { value: 'SPLIT', label: 'Split' },
+                      ].map((option) => (
+                        <button
+                          key={option.value}
+                          type="button"
+                          className={`inbox-toggle ${draft.business_personal === option.value ? 'active' : ''}`}
+                          onClick={() => setInboxDraftField(card.id, 'business_personal', option.value)}
+                        >
+                          {option.label}
+                        </button>
+                      ))}
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_split || isSplit) && (
+                  <div className="inbox-field">
+                    <label>Business split %</label>
+                    <input
+                      type="number"
+                      min="0"
+                      max="100"
+                      value={draft.split_business_pct}
+                      onChange={(event) => setInboxDraftField(card.id, 'split_business_pct', event.target.value)}
+                      placeholder="80"
+                    />
+                  </div>
+                )}
+
+                {card.supports_duplicate && (
+                  <>
+                    <div>
+                      <div className="inbox-field">
+                        <label>Duplicate action</label>
+                      </div>
+                      <div className="inbox-duplicate-row">
+                        {[
+                          { value: 'dismiss', label: 'Dismiss duplicate' },
+                          { value: 'merge', label: 'Merge to original' },
+                        ].map((option) => (
+                          <button
+                            key={option.value}
+                            type="button"
+                            className={`inbox-toggle ${draft.duplicate_action === option.value ? 'active' : ''}`}
+                            onClick={() => setInboxDraftField(card.id, 'duplicate_action', option.value)}
+                          >
+                            {option.label}
+                          </button>
+                        ))}
+                      </div>
+                    </div>
+                    {draft.duplicate_action === 'merge' && (
+                      <div className="inbox-field">
+                        <label>Original transaction id</label>
+                        <input
+                          value={draft.duplicate_of_txn_id}
+                          onChange={(event) => setInboxDraftField(card.id, 'duplicate_of_txn_id', event.target.value)}
+                          placeholder="txn-9001"
+                        />
+                      </div>
+                    )}
+                  </>
+                )}
+
+                <div className="inbox-footer-row">
+                  <div className="inbox-inline-status">
+                    Route: {card.resolution_target?.route || `/api/v1/inbox/${card.id}`}
+                  </div>
+                  <div className="inbox-footer-actions">
+                    <button className="secondary-action" onClick={clearInboxMessage} disabled={saving}>
+                      Clear banner
+                    </button>
+                    <button className="primary-action" onClick={() => submitInboxAction(card)} disabled={saving}>
+                      {saving ? <RefreshCcw size={16} className="spinner" /> : <CheckCircle2 size={16} />}
+                      Save and clear blocker
+                    </button>
+                  </div>
+                </div>
+              </section>
+            );
+          })}
+        </div>
+      )}
     </div>
   );
 
@@ -2510,11 +2966,11 @@
           <section className="readiness-panel">
             <h5 style={{ marginBottom: 12 }}>Queue Backlog</h5>
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
-                <div key={item.id} className="control-row">
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
+                <div key={item.id || item.affected_entity_id} className="control-row">
                   <div>
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
                   <div className="text-end">
                     <div className="small-muted">{item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date}</div>
@@ -2522,7 +2978,7 @@
                   </div>
                 </div>
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
               )}
             </div>
@@ -2668,7 +3124,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2697,7 +3153,7 @@
   };
 
   const createSnapshotVersion = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     const status = snapshotWorkspace?.snapshot_status;
 
     if (!status?.can_create_snapshot_version) {
@@ -2719,7 +3175,7 @@
         description: `Quarter snapshot generated from Quarter workspace`,
         metadata: {
           source_type: 'manual',
-          readiness_pct: quarterReadiness?.readiness_pct ?? null
+          readiness_pct: activeQuarterReport?.readiness_pct ?? null
         }
       });
       await refreshQuarterWorkspace();
@@ -2734,7 +3190,7 @@
   };
 
   const closeQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(closeReason || '').trim()) {
       setQuarterActionError('Close reason is required.');
       setQuarterActionMessage('');
@@ -2785,7 +3241,7 @@
   };
 
   const reopenQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(reopenReason || '').trim()) {
       setQuarterActionError('Reopen reason is required.');
       setQuarterActionMessage('');
@@ -2844,9 +3300,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3340,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3352,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3363,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3377,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/bizPA/frontend/src/quarterReadiness.test.js b/bizPA/frontend/src/quarterReadiness.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..0b810191a89d8b82091bf5de1408afcc23c14e65
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.test.js
@@ -0,0 +1,93 @@
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
+
+const reportFixture = {
+  quarter_reference: 'Q1-2026',
+  period_start: '2026-01-01',
+  period_end: '2026-03-31',
+  total_txns_in_period: 18,
+  readiness_pct: 78,
+  can_export: false,
+  issue_list: [
+    {
+      issue_type: 'missing_category',
+      affected_entity_id: 'txn-9101',
+      severity: 'high',
+      weight: 35,
+      explanation: 'Category missing for Fuel Stop.',
+      txn_date: '2026-02-04',
+      merchant: 'Fuel Stop',
+      amount: 64.18,
+      direction: 'out',
+      resolution_target: {
+        label: 'Classify transaction',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'missing_business_personal',
+      affected_entity_id: 'txn-9103',
+      severity: 'high',
+      weight: 30,
+      explanation: 'Business/personal decision missing.',
+      txn_date: '2026-02-15',
+      merchant: 'Mobile Contract',
+      amount: 42.5,
+      direction: 'out',
+      resolution_target: {
+        label: 'Set business or personal',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'unresolved_duplicate',
+      affected_entity_id: 'txn-9104',
+      severity: 'medium',
+      weight: 15,
+      explanation: 'Duplicate not resolved.',
+      txn_date: '2026-03-02',
+      merchant: 'Client Payment',
+      amount: 820,
+      direction: 'in',
+      resolution_target: {
+        label: 'Resolve duplicate',
+        workflow: '/inbox/finish-now',
+      },
+    },
+  ],
+};
+
+describe('quarter readiness helpers', () => {
+  test('keeps blockers ordered and updates headline while blockers remain', () => {
+    const state = buildQuarterUiState({ report: reportFixture, resolvedIssueIds: ['txn-9104'] });
+
+    expect(state.itemsLeft).toBe(2);
+    expect(state.exportEnabled).toBe(false);
+    expect(state.report.readiness_pct).toBeGreaterThan(78);
+    expect(state.headline).toContain('items left');
+    expect(state.activeIssues.map((issue) => issue.affected_entity_id)).toEqual(['txn-9101', 'txn-9103']);
+    expect(state.report.explanation_lines[0]).toContain('Readiness is');
+  });
+
+  test('unlocks export immediately when every blocker is resolved', () => {
+    const state = buildQuarterUiState({
+      report: reportFixture,
+      resolvedIssueIds: ['txn-9101', 'txn-9103', 'txn-9104'],
+    });
+
+    expect(state.itemsLeft).toBe(0);
+    expect(state.exportEnabled).toBe(true);
+    expect(state.report.readiness_pct).toBe(100);
+    expect(state.report.can_export).toBe(true);
+    expect(state.report.issue_summary).toEqual([]);
+  });
+
+  test('supports demo query params that resolve all blockers', () => {
+    const params = new URLSearchParams('resolvedDemoIssues=all');
+    const resolvedIds = deriveResolvedIssueIdsFromSearchParams(params, reportFixture.issue_list);
+
+    expect(resolvedIds).toEqual(['txn-9101', 'txn-9103', 'txn-9104']);
+  });
+});
diff --git a/bizPA/start_bizpa_tax_readiness_ui.ps1 b/bizPA/start_bizpa_tax_readiness_ui.ps1
index 1fc4bef8cd172c31fdc3db52d1acc9d4775c5485..7216e44eac386fe436a9af8e9b417cd2d61e37fe
--- a/bizPA/start_bizpa_tax_readiness_ui.ps1
+++ b/bizPA/start_bizpa_tax_readiness_ui.ps1
@@ -4,6 +4,7 @@
 $frontendPath = Join-Path $repoRoot 'frontend'
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
 
 Write-Host 'Starting bizPA tax readiness UI...' -ForegroundColor Cyan
 
@@ -14,7 +15,10 @@
 Write-Host 'Tax readiness dashboard URL:' -ForegroundColor Green
 Write-Host "  $readinessUrl" -ForegroundColor Yellow
 Write-Host ''
+Write-Host 'Export-ready verification URL:' -ForegroundColor Green
+Write-Host "  $exportReadyUrl" -ForegroundColor Yellow
+Write-Host ''
 Write-Host 'Base frontend URL:' -ForegroundColor Green
 Write-Host "  $frontendUrl" -ForegroundColor Yellow
 Write-Host ''
-Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI can be reviewed without backend setup.' -ForegroundColor Cyan
+Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI and zero-blocker export-ready state can be reviewed without backend setup.' -ForegroundColor Cyan
diff --git a/bizPA/tax_readiness_ui_smoke.ps1 b/bizPA/tax_readiness_ui_smoke.ps1
index 56d67892a51b22572503dec560d17123b370c2b7..269c2a3221adfe9095130883c89fd76428a65989
--- a/bizPA/tax_readiness_ui_smoke.ps1
+++ b/bizPA/tax_readiness_ui_smoke.ps1
@@ -28,36 +28,38 @@
 
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
-$screenshotPath = 'C:\Users\edebe\eds\workstream\verification\20260311_201500_bizpa_tax_readiness_ui.png'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
+$verificationRoot = 'C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification'
+$quarterScreenshotPath = Join-Path $verificationRoot 'quarter_readiness_screen.png'
+$finishNowScreenshotPath = Join-Path $verificationRoot 'finish_now_queue_zero_blockers.png'
 $chromeProfilePath = 'C:\Users\edebe\eds\workstream\verification\chrome_capture_profile_tax_readiness'
-
-New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
+$chromePath = 'C:\Program Files\Google\Chrome\Application\chrome.exe'
 
-$frontendJob = Start-Job -ScriptBlock {
-  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
-  $env:PORT = '3002'
-  npm.cmd start
-}
+function Wait-FrontendReady {
+  param (
+    [string]$Url
+  )
 
-try {
-  $frontendResponse = $null
-  $frontendReady = $false
   for ($i = 0; $i -lt 45; $i++) {
     Start-Sleep -Seconds 2
     try {
-      $frontendResponse = Invoke-WebRequest -UseBasicParsing $readinessUrl
-      if ($frontendResponse.StatusCode -eq 200) {
-        $frontendReady = $true
-        break
+      $response = Invoke-WebRequest -UseBasicParsing $Url
+      if ($response.StatusCode -eq 200) {
+        return $response
       }
     } catch {
     }
   }
 
-  if (-not $frontendReady) {
-    throw "Frontend did not become ready on $readinessUrl"
-  }
+  throw "Frontend did not become ready on $Url"
+}
 
+function Capture-ChromeAppScreenshot {
+  param (
+    [string]$Url,
+    [string]$OutputPath
+  )
+
   $chromeArgs = @(
     "--user-data-dir=$chromeProfilePath",
     '--new-window',
@@ -68,9 +70,9 @@
     '--disable-breakpad',
     '--disable-crash-reporter',
     '--noerrdialogs',
-    "--app=$readinessUrl"
+    "--app=$Url"
   )
-  Start-Process 'C:\Program Files\Google\Chrome\Application\chrome.exe' -ArgumentList $chromeArgs | Out-Null
+  Start-Process $chromePath -ArgumentList $chromeArgs | Out-Null
 
   $chromeWindow = $null
   for ($i = 0; $i -lt 20; $i++) {
@@ -96,7 +98,7 @@
   }
 
   if (-not $chromeWindow) {
-    throw 'Chrome readiness window did not expose a main window handle for screenshot capture.'
+    throw "Chrome window did not expose a main window handle for $Url"
   }
 
   [WindowCaptureNative]::ShowWindow($chromeWindow.MainWindowHandle, 3) | Out-Null
@@ -110,17 +112,34 @@
   $bitmap = New-Object System.Drawing.Bitmap $width, $height
   $graphics = [System.Drawing.Graphics]::FromImage($bitmap)
   $graphics.CopyFromScreen($rect.Left, $rect.Top, 0, 0, $bitmap.Size)
-  $bitmap.Save($screenshotPath, [System.Drawing.Imaging.ImageFormat]::Png)
+  $bitmap.Save($OutputPath, [System.Drawing.Imaging.ImageFormat]::Png)
   $graphics.Dispose()
   $bitmap.Dispose()
 
   Get-Process chrome -ErrorAction SilentlyContinue |
     Where-Object { $_.StartTime -ge $chromeWindow.StartTime.AddSeconds(-2) } |
     Stop-Process -Force -ErrorAction SilentlyContinue
+}
+
+New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
+New-Item -ItemType Directory -Force -Path $verificationRoot | Out-Null
+
+$frontendJob = Start-Job -ScriptBlock {
+  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
+  $env:PORT = '3002'
+  npm.cmd start
+}
+
+try {
+  $frontendResponse = Wait-FrontendReady -Url $readinessUrl
+  Capture-ChromeAppScreenshot -Url $readinessUrl -OutputPath $quarterScreenshotPath
+  Capture-ChromeAppScreenshot -Url $exportReadyUrl -OutputPath $finishNowScreenshotPath
 
   "READINESS_URL=$readinessUrl"
+  "EXPORT_READY_URL=$exportReadyUrl"
   "FRONTEND_STATUS=$($frontendResponse.StatusCode)"
-  "SCREENSHOT=$screenshotPath"
+  "SCREENSHOT_QUARTER=$quarterScreenshotPath"
+  "SCREENSHOT_EXPORT_READY=$finishNowScreenshotPath"
   'FRONTEND_LOG_START'
   Receive-Job $frontendJob -Keep -ErrorAction SilentlyContinue 2>&1
   'FRONTEND_LOG_END'
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..0253d40f85fe6785090b08aa24db3a6f2a6d0909
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,25 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1531,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1546,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1632,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2103,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2317,25 +2562,236 @@
   );
 
   const renderInbox = () => (
-    <div>
-      <h4>Finish Now Queue</h4>
-      <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
-      <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
-            <div>
-              <b>{item.merchant || 'Unknown'}</b>
-              <br />
-              <small>
-                {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
-              </small>
-              <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
-            </div>
+    <div className="inbox-mobile-shell">
+      <section className="inbox-mobile-hero">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2">
+          <div className="inbox-kicker">
+            <Bell size={14} />
+            Mobile-first exception queue
           </div>
-        ))}
-      </div>
+          <div className="status-pill">{filteredInboxCards.length} visible</div>
+        </div>
+        <h4>Finish now</h4>
+        <p>
+          Clear the blockers that stop the quarterly export: category, business or personal, split treatment, and duplicate resolution.
+        </p>
+        <div className="inbox-mobile-stats">
+          <div className="inbox-mobile-stat">
+            <small>Queue</small>
+            <strong>{inboxCards.length}</strong>
+            <small>{activeQuarterReport.quarter_reference || READINESS_DEMO_REPORT.quarter_reference}</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Critical</small>
+            <strong>{highSeverityCount}</strong>
+            <small>High-severity blockers</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Ready now</small>
+            <strong>{readyToExportCount}</strong>
+            <small>Transactions already export-safe</small>
+          </div>
+        </div>
+        <div className="hero-actions">
+          <button className="hero-action-main" onClick={() => setCurrentTab('quarter')}>
+            <ArrowRight size={16} />
+            Review quarter readiness
+          </button>
+          <button className="hero-action-secondary" onClick={undoLastInboxAction} disabled={inboxBusyId === 'undo-last'}>
+            {inboxBusyId === 'undo-last' ? <RefreshCcw size={16} className="spinner" /> : <ArchiveRestore size={16} />}
+            Undo last action
+          </button>
+        </div>
+      </section>
+
+      {inboxMessage && (
+        <div className={`readiness-panel inbox-message ${inboxMessage.type}`}>
+          {inboxMessage.text}
+        </div>
+      )}
+
+      <section className="readiness-panel">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-3">
+          <div>
+            <h5 style={{ marginBottom: 4 }}>Queue filters</h5>
+            <small className="small-muted">Keep the mobile pass focused on one blocker type at a time.</small>
+          </div>
+          <div className="status-pill">{activeQuarterReport.readiness_pct ?? READINESS_DEMO_REPORT.readiness_pct}% ready</div>
+        </div>
+        <div className="inbox-filter-row">
+          {INBOX_FILTERS.map((filter) => {
+            const count = filter.key === 'all'
+              ? inboxCards.length
+              : inboxCards.filter((card) => card.issue_types.includes(filter.key)).length;
+            return (
+              <button
+                key={filter.key}
+                type="button"
+                className={`inbox-filter-chip ${inboxFilter === filter.key ? 'active' : ''}`}
+                onClick={() => setInboxFilter(filter.key)}
+              >
+                <span>{filter.label}</span>
+                <small>{count}</small>
+              </button>
+            );
+          })}
+        </div>
+      </section>
+
+      {filteredInboxCards.length === 0 ? (
+        <div className="inbox-empty">
+          No blockers match this filter. Reset to All blockers or return to the quarter screen to confirm readiness.
+        </div>
+      ) : (
+        <div className="inbox-card-list">
+          {filteredInboxCards.map((card) => {
+            const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+            const saving = inboxBusyId === card.id;
+            const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+            return (
+              <section key={card.id} className="inbox-action-card">
+                <div className="inbox-card-top">
+                  <div>
+                    <strong>{card.merchant}</strong>
+                    <div className="inbox-card-subline">
+                      {card.txn_date ? String(card.txn_date).slice(0, 10) : 'Date pending'} · {formatIssueAmount(card.amount, card.direction)}
+                    </div>
+                  </div>
+                  <span className={`inbox-issue-chip ${card.severity}`}>
+                    <TriangleAlert size={14} />
+                    {INBOX_REASON_LABELS[card.blocker_reason] || card.blocker_reason.replace(/_/g, ' ')}
+                  </span>
+                </div>
+
+                <div className="inbox-chip-row">
+                  {card.issue_labels.map((label) => (
+                    <span key={`${card.id}-${label}`} className={`inbox-issue-chip ${card.severity}`}>
+                      {label}
+                    </span>
+                  ))}
+                  {card.suggested_category?.category_name && (
+                    <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
+                      <CheckCircle2 size={14} />
+                      Suggested: {card.suggested_category.category_name}
+                    </button>
+                  )}
+                </div>
+
+                <p className="inbox-explainer">{card.explanation || 'Resolve the blocking fields to clear this transaction from the queue.'}</p>
+
+                {(card.supports_category || card.supports_business_personal || card.supports_split) && (
+                  <div className="inbox-action-grid">
+                    <div className="inbox-field">
+                      <label>Category code</label>
+                      <input
+                        value={draft.category_code}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_code', event.target.value)}
+                        placeholder="MATERIALS"
+                      />
+                    </div>
+                    <div className="inbox-field">
+                      <label>Category label</label>
+                      <input
+                        value={draft.category_name}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_name', event.target.value)}
+                        placeholder="Materials"
+                      />
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_business_personal || card.supports_split) && (
+                  <div>
+                    <div className="inbox-field">
+                      <label>Business treatment</label>
+                    </div>
+                    <div className="inbox-toggle-row">
+                      {[
+                        { value: 'BUSINESS', label: 'Business' },
+                        { value: 'PERSONAL', label: 'Personal' },
+                        { value: 'SPLIT', label: 'Split' },
+                      ].map((option) => (
+                        <button
+                          key={option.value}
+                          type="button"
+                          className={`inbox-toggle ${draft.business_personal === option.value ? 'active' : ''}`}
+                          onClick={() => setInboxDraftField(card.id, 'business_personal', option.value)}
+                        >
+                          {option.label}
+                        </button>
+                      ))}
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_split || isSplit) && (
+                  <div className="inbox-field">
+                    <label>Business split %</label>
+                    <input
+                      type="number"
+                      min="0"
+                      max="100"
+                      value={draft.split_business_pct}
+                      onChange={(event) => setInboxDraftField(card.id, 'split_business_pct', event.target.value)}
+                      placeholder="80"
+                    />
+                  </div>
+                )}
+
+                {card.supports_duplicate && (
+                  <>
+                    <div>
+                      <div className="inbox-field">
+                        <label>Duplicate action</label>
+                      </div>
+                      <div className="inbox-duplicate-row">
+                        {[
+                          { value: 'dismiss', label: 'Dismiss duplicate' },
+                          { value: 'merge', label: 'Merge to original' },
+                        ].map((option) => (
+                          <button
+                            key={option.value}
+                            type="button"
+                            className={`inbox-toggle ${draft.duplicate_action === option.value ? 'active' : ''}`}
+                            onClick={() => setInboxDraftField(card.id, 'duplicate_action', option.value)}
+                          >
+                            {option.label}
+                          </button>
+                        ))}
+                      </div>
+                    </div>
+                    {draft.duplicate_action === 'merge' && (
+                      <div className="inbox-field">
+                        <label>Original transaction id</label>
+                        <input
+                          value={draft.duplicate_of_txn_id}
+                          onChange={(event) => setInboxDraftField(card.id, 'duplicate_of_txn_id', event.target.value)}
+                          placeholder="txn-9001"
+                        />
+                      </div>
+                    )}
+                  </>
+                )}
+
+                <div className="inbox-footer-row">
+                  <div className="inbox-inline-status">
+                    Route: {card.resolution_target?.route || `/api/v1/inbox/${card.id}`}
+                  </div>
+                  <div className="inbox-footer-actions">
+                    <button className="secondary-action" onClick={clearInboxMessage} disabled={saving}>
+                      Clear banner
+                    </button>
+                    <button className="primary-action" onClick={() => submitInboxAction(card)} disabled={saving}>
+                      {saving ? <RefreshCcw size={16} className="spinner" /> : <CheckCircle2 size={16} />}
+                      Save and clear blocker
+                    </button>
+                  </div>
+                </div>
+              </section>
+            );
+          })}
+        </div>
+      )}
     </div>
   );
 
@@ -2510,11 +2966,11 @@
           <section className="readiness-panel">
             <h5 style={{ marginBottom: 12 }}>Queue Backlog</h5>
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
-                <div key={item.id} className="control-row">
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
+                <div key={item.id || item.affected_entity_id} className="control-row">
                   <div>
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
                   <div className="text-end">
                     <div className="small-muted">{item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date}</div>
@@ -2522,7 +2978,7 @@
                   </div>
                 </div>
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
               )}
             </div>
@@ -2668,7 +3124,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2697,7 +3153,7 @@
   };
 
   const createSnapshotVersion = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     const status = snapshotWorkspace?.snapshot_status;
 
     if (!status?.can_create_snapshot_version) {
@@ -2719,7 +3175,7 @@
         description: `Quarter snapshot generated from Quarter workspace`,
         metadata: {
           source_type: 'manual',
-          readiness_pct: quarterReadiness?.readiness_pct ?? null
+          readiness_pct: activeQuarterReport?.readiness_pct ?? null
         }
       });
       await refreshQuarterWorkspace();
@@ -2734,7 +3190,7 @@
   };
 
   const closeQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(closeReason || '').trim()) {
       setQuarterActionError('Close reason is required.');
       setQuarterActionMessage('');
@@ -2785,7 +3241,7 @@
   };
 
   const reopenQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(reopenReason || '').trim()) {
       setQuarterActionError('Reopen reason is required.');
       setQuarterActionMessage('');
@@ -2844,9 +3300,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3340,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3352,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3363,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3377,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/bizPA/frontend/src/quarterReadiness.test.js b/bizPA/frontend/src/quarterReadiness.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..0b810191a89d8b82091bf5de1408afcc23c14e65
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.test.js
@@ -0,0 +1,93 @@
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
+
+const reportFixture = {
+  quarter_reference: 'Q1-2026',
+  period_start: '2026-01-01',
+  period_end: '2026-03-31',
+  total_txns_in_period: 18,
+  readiness_pct: 78,
+  can_export: false,
+  issue_list: [
+    {
+      issue_type: 'missing_category',
+      affected_entity_id: 'txn-9101',
+      severity: 'high',
+      weight: 35,
+      explanation: 'Category missing for Fuel Stop.',
+      txn_date: '2026-02-04',
+      merchant: 'Fuel Stop',
+      amount: 64.18,
+      direction: 'out',
+      resolution_target: {
+        label: 'Classify transaction',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'missing_business_personal',
+      affected_entity_id: 'txn-9103',
+      severity: 'high',
+      weight: 30,
+      explanation: 'Business/personal decision missing.',
+      txn_date: '2026-02-15',
+      merchant: 'Mobile Contract',
+      amount: 42.5,
+      direction: 'out',
+      resolution_target: {
+        label: 'Set business or personal',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'unresolved_duplicate',
+      affected_entity_id: 'txn-9104',
+      severity: 'medium',
+      weight: 15,
+      explanation: 'Duplicate not resolved.',
+      txn_date: '2026-03-02',
+      merchant: 'Client Payment',
+      amount: 820,
+      direction: 'in',
+      resolution_target: {
+        label: 'Resolve duplicate',
+        workflow: '/inbox/finish-now',
+      },
+    },
+  ],
+};
+
+describe('quarter readiness helpers', () => {
+  test('keeps blockers ordered and updates headline while blockers remain', () => {
+    const state = buildQuarterUiState({ report: reportFixture, resolvedIssueIds: ['txn-9104'] });
+
+    expect(state.itemsLeft).toBe(2);
+    expect(state.exportEnabled).toBe(false);
+    expect(state.report.readiness_pct).toBeGreaterThan(78);
+    expect(state.headline).toContain('items left');
+    expect(state.activeIssues.map((issue) => issue.affected_entity_id)).toEqual(['txn-9101', 'txn-9103']);
+    expect(state.report.explanation_lines[0]).toContain('Readiness is');
+  });
+
+  test('unlocks export immediately when every blocker is resolved', () => {
+    const state = buildQuarterUiState({
+      report: reportFixture,
+      resolvedIssueIds: ['txn-9101', 'txn-9103', 'txn-9104'],
+    });
+
+    expect(state.itemsLeft).toBe(0);
+    expect(state.exportEnabled).toBe(true);
+    expect(state.report.readiness_pct).toBe(100);
+    expect(state.report.can_export).toBe(true);
+    expect(state.report.issue_summary).toEqual([]);
+  });
+
+  test('supports demo query params that resolve all blockers', () => {
+    const params = new URLSearchParams('resolvedDemoIssues=all');
+    const resolvedIds = deriveResolvedIssueIdsFromSearchParams(params, reportFixture.issue_list);
+
+    expect(resolvedIds).toEqual(['txn-9101', 'txn-9103', 'txn-9104']);
+  });
+});
diff --git a/bizPA/start_bizpa_tax_readiness_ui.ps1 b/bizPA/start_bizpa_tax_readiness_ui.ps1
index 1fc4bef8cd172c31fdc3db52d1acc9d4775c5485..7216e44eac386fe436a9af8e9b417cd2d61e37fe
--- a/bizPA/start_bizpa_tax_readiness_ui.ps1
+++ b/bizPA/start_bizpa_tax_readiness_ui.ps1
@@ -4,6 +4,7 @@
 $frontendPath = Join-Path $repoRoot 'frontend'
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
 
 Write-Host 'Starting bizPA tax readiness UI...' -ForegroundColor Cyan
 
@@ -14,7 +15,10 @@
 Write-Host 'Tax readiness dashboard URL:' -ForegroundColor Green
 Write-Host "  $readinessUrl" -ForegroundColor Yellow
 Write-Host ''
+Write-Host 'Export-ready verification URL:' -ForegroundColor Green
+Write-Host "  $exportReadyUrl" -ForegroundColor Yellow
+Write-Host ''
 Write-Host 'Base frontend URL:' -ForegroundColor Green
 Write-Host "  $frontendUrl" -ForegroundColor Yellow
 Write-Host ''
-Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI can be reviewed without backend setup.' -ForegroundColor Cyan
+Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI and zero-blocker export-ready state can be reviewed without backend setup.' -ForegroundColor Cyan
diff --git a/bizPA/tax_readiness_ui_smoke.ps1 b/bizPA/tax_readiness_ui_smoke.ps1
index 56d67892a51b22572503dec560d17123b370c2b7..269c2a3221adfe9095130883c89fd76428a65989
--- a/bizPA/tax_readiness_ui_smoke.ps1
+++ b/bizPA/tax_readiness_ui_smoke.ps1
@@ -28,36 +28,38 @@
 
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
-$screenshotPath = 'C:\Users\edebe\eds\workstream\verification\20260311_201500_bizpa_tax_readiness_ui.png'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
+$verificationRoot = 'C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification'
+$quarterScreenshotPath = Join-Path $verificationRoot 'quarter_readiness_screen.png'
+$finishNowScreenshotPath = Join-Path $verificationRoot 'finish_now_queue_zero_blockers.png'
 $chromeProfilePath = 'C:\Users\edebe\eds\workstream\verification\chrome_capture_profile_tax_readiness'
-
-New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
+$chromePath = 'C:\Program Files\Google\Chrome\Application\chrome.exe'
 
-$frontendJob = Start-Job -ScriptBlock {
-  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
-  $env:PORT = '3002'
-  npm.cmd start
-}
+function Wait-FrontendReady {
+  param (
+    [string]$Url
+  )
 
-try {
-  $frontendResponse = $null
-  $frontendReady = $false
   for ($i = 0; $i -lt 45; $i++) {
     Start-Sleep -Seconds 2
     try {
-      $frontendResponse = Invoke-WebRequest -UseBasicParsing $readinessUrl
-      if ($frontendResponse.StatusCode -eq 200) {
-        $frontendReady = $true
-        break
+      $response = Invoke-WebRequest -UseBasicParsing $Url
+      if ($response.StatusCode -eq 200) {
+        return $response
       }
     } catch {
     }
   }
 
-  if (-not $frontendReady) {
-    throw "Frontend did not become ready on $readinessUrl"
-  }
+  throw "Frontend did not become ready on $Url"
+}
 
+function Capture-ChromeAppScreenshot {
+  param (
+    [string]$Url,
+    [string]$OutputPath
+  )
+
   $chromeArgs = @(
     "--user-data-dir=$chromeProfilePath",
     '--new-window',
@@ -68,9 +70,9 @@
     '--disable-breakpad',
     '--disable-crash-reporter',
     '--noerrdialogs',
-    "--app=$readinessUrl"
+    "--app=$Url"
   )
-  Start-Process 'C:\Program Files\Google\Chrome\Application\chrome.exe' -ArgumentList $chromeArgs | Out-Null
+  Start-Process $chromePath -ArgumentList $chromeArgs | Out-Null
 
   $chromeWindow = $null
   for ($i = 0; $i -lt 20; $i++) {
@@ -96,7 +98,7 @@
   }
 
   if (-not $chromeWindow) {
-    throw 'Chrome readiness window did not expose a main window handle for screenshot capture.'
+    throw "Chrome window did not expose a main window handle for $Url"
   }
 
   [WindowCaptureNative]::ShowWindow($chromeWindow.MainWindowHandle, 3) | Out-Null
@@ -110,17 +112,34 @@
   $bitmap = New-Object System.Drawing.Bitmap $width, $height
   $graphics = [System.Drawing.Graphics]::FromImage($bitmap)
   $graphics.CopyFromScreen($rect.Left, $rect.Top, 0, 0, $bitmap.Size)
-  $bitmap.Save($screenshotPath, [System.Drawing.Imaging.ImageFormat]::Png)
+  $bitmap.Save($OutputPath, [System.Drawing.Imaging.ImageFormat]::Png)
   $graphics.Dispose()
   $bitmap.Dispose()
 
   Get-Process chrome -ErrorAction SilentlyContinue |
     Where-Object { $_.StartTime -ge $chromeWindow.StartTime.AddSeconds(-2) } |
     Stop-Process -Force -ErrorAction SilentlyContinue
+}
+
+New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
+New-Item -ItemType Directory -Force -Path $verificationRoot | Out-Null
+
+$frontendJob = Start-Job -ScriptBlock {
+  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
+  $env:PORT = '3002'
+  npm.cmd start
+}
+
+try {
+  $frontendResponse = Wait-FrontendReady -Url $readinessUrl
+  Capture-ChromeAppScreenshot -Url $readinessUrl -OutputPath $quarterScreenshotPath
+  Capture-ChromeAppScreenshot -Url $exportReadyUrl -OutputPath $finishNowScreenshotPath
 
   "READINESS_URL=$readinessUrl"
+  "EXPORT_READY_URL=$exportReadyUrl"
   "FRONTEND_STATUS=$($frontendResponse.StatusCode)"
-  "SCREENSHOT=$screenshotPath"
+  "SCREENSHOT_QUARTER=$quarterScreenshotPath"
+  "SCREENSHOT_EXPORT_READY=$finishNowScreenshotPath"
   'FRONTEND_LOG_START'
   Receive-Job $frontendJob -Keep -ErrorAction SilentlyContinue 2>&1
   'FRONTEND_LOG_END'
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n \"activeQuarterReport|activeFinishNowQueue|resolvedQuarterIssueIds|resolveQuarterIssue|quarterReadiness\\?\\.|inboxItems\" 'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\App.jsx'" in C:\Users\edebe\eds succeeded in 838ms:
1287:  const [inboxItems, setInboxItems] = useState([]);
1289:  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
1510:    resolvedIssueIds: resolvedQuarterIssueIds,
1512:  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
1513:  const activeFinishNowQueue = quarterUiState.activeIssues;
1515:  const resolveQuarterIssue = (issueId) => {
1534:  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
1537:  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
1549:    inboxItems: activeFinishNowQueue,
2106:    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
2107:    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
2582:            <small>{activeQuarterReport.quarter_reference || READINESS_DEMO_REPORT.quarter_reference}</small>
2619:          <div className="status-pill">{activeQuarterReport.readiness_pct ?? READINESS_DEMO_REPORT.readiness_pct}% ready</div>
2969:              {activeFinishNowQueue.slice(0, 4).map((item) => (
2981:              {activeFinishNowQueue.length === 0 && (
3127:    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
3156:    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
3178:          readiness_pct: activeQuarterReport?.readiness_pct ?? null
3193:    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
3244:    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..0253d40f85fe6785090b08aa24db3a6f2a6d0909
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,25 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1531,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1546,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1632,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2103,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2317,25 +2562,236 @@
   );
 
   const renderInbox = () => (
-    <div>
-      <h4>Finish Now Queue</h4>
-      <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
-      <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
-            <div>
-              <b>{item.merchant || 'Unknown'}</b>
-              <br />
-              <small>
-                {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
-              </small>
-              <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
-            </div>
+    <div className="inbox-mobile-shell">
+      <section className="inbox-mobile-hero">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2">
+          <div className="inbox-kicker">
+            <Bell size={14} />
+            Mobile-first exception queue
           </div>
-        ))}
-      </div>
+          <div className="status-pill">{filteredInboxCards.length} visible</div>
+        </div>
+        <h4>Finish now</h4>
+        <p>
+          Clear the blockers that stop the quarterly export: category, business or personal, split treatment, and duplicate resolution.
+        </p>
+        <div className="inbox-mobile-stats">
+          <div className="inbox-mobile-stat">
+            <small>Queue</small>
+            <strong>{inboxCards.length}</strong>
+            <small>{activeQuarterReport.quarter_reference || READINESS_DEMO_REPORT.quarter_reference}</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Critical</small>
+            <strong>{highSeverityCount}</strong>
+            <small>High-severity blockers</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Ready now</small>
+            <strong>{readyToExportCount}</strong>
+            <small>Transactions already export-safe</small>
+          </div>
+        </div>
+        <div className="hero-actions">
+          <button className="hero-action-main" onClick={() => setCurrentTab('quarter')}>
+            <ArrowRight size={16} />
+            Review quarter readiness
+          </button>
+          <button className="hero-action-secondary" onClick={undoLastInboxAction} disabled={inboxBusyId === 'undo-last'}>
+            {inboxBusyId === 'undo-last' ? <RefreshCcw size={16} className="spinner" /> : <ArchiveRestore size={16} />}
+            Undo last action
+          </button>
+        </div>
+      </section>
+
+      {inboxMessage && (
+        <div className={`readiness-panel inbox-message ${inboxMessage.type}`}>
+          {inboxMessage.text}
+        </div>
+      )}
+
+      <section className="readiness-panel">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-3">
+          <div>
+            <h5 style={{ marginBottom: 4 }}>Queue filters</h5>
+            <small className="small-muted">Keep the mobile pass focused on one blocker type at a time.</small>
+          </div>
+          <div className="status-pill">{activeQuarterReport.readiness_pct ?? READINESS_DEMO_REPORT.readiness_pct}% ready</div>
+        </div>
+        <div className="inbox-filter-row">
+          {INBOX_FILTERS.map((filter) => {
+            const count = filter.key === 'all'
+              ? inboxCards.length
+              : inboxCards.filter((card) => card.issue_types.includes(filter.key)).length;
+            return (
+              <button
+                key={filter.key}
+                type="button"
+                className={`inbox-filter-chip ${inboxFilter === filter.key ? 'active' : ''}`}
+                onClick={() => setInboxFilter(filter.key)}
+              >
+                <span>{filter.label}</span>
+                <small>{count}</small>
+              </button>
+            );
+          })}
+        </div>
+      </section>
+
+      {filteredInboxCards.length === 0 ? (
+        <div className="inbox-empty">
+          No blockers match this filter. Reset to All blockers or return to the quarter screen to confirm readiness.
+        </div>
+      ) : (
+        <div className="inbox-card-list">
+          {filteredInboxCards.map((card) => {
+            const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+            const saving = inboxBusyId === card.id;
+            const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+            return (
+              <section key={card.id} className="inbox-action-card">
+                <div className="inbox-card-top">
+                  <div>
+                    <strong>{card.merchant}</strong>
+                    <div className="inbox-card-subline">
+                      {card.txn_date ? String(card.txn_date).slice(0, 10) : 'Date pending'} · {formatIssueAmount(card.amount, card.direction)}
+                    </div>
+                  </div>
+                  <span className={`inbox-issue-chip ${card.severity}`}>
+                    <TriangleAlert size={14} />
+                    {INBOX_REASON_LABELS[card.blocker_reason] || card.blocker_reason.replace(/_/g, ' ')}
+                  </span>
+                </div>
+
+                <div className="inbox-chip-row">
+                  {card.issue_labels.map((label) => (
+                    <span key={`${card.id}-${label}`} className={`inbox-issue-chip ${card.severity}`}>
+                      {label}
+                    </span>
+                  ))}
+                  {card.suggested_category?.category_name && (
+                    <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
+                      <CheckCircle2 size={14} />
+                      Suggested: {card.suggested_category.category_name}
+                    </button>
+                  )}
+                </div>
+
+                <p className="inbox-explainer">{card.explanation || 'Resolve the blocking fields to clear this transaction from the queue.'}</p>
+
+                {(card.supports_category || card.supports_business_personal || card.supports_split) && (
+                  <div className="inbox-action-grid">
+                    <div className="inbox-field">
+                      <label>Category code</label>
+                      <input
+                        value={draft.category_code}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_code', event.target.value)}
+                        placeholder="MATERIALS"
+                      />
+                    </div>
+                    <div className="inbox-field">
+                      <label>Category label</label>
+                      <input
+                        value={draft.category_name}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_name', event.target.value)}
+                        placeholder="Materials"
+                      />
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_business_personal || card.supports_split) && (
+                  <div>
+                    <div className="inbox-field">
+                      <label>Business treatment</label>
+                    </div>
+                    <div className="inbox-toggle-row">
+                      {[
+                        { value: 'BUSINESS', label: 'Business' },
+                        { value: 'PERSONAL', label: 'Personal' },
+                        { value: 'SPLIT', label: 'Split' },
+                      ].map((option) => (
+                        <button
+                          key={option.value}
+                          type="button"
+                          className={`inbox-toggle ${draft.business_personal === option.value ? 'active' : ''}`}
+                          onClick={() => setInboxDraftField(card.id, 'business_personal', option.value)}
+                        >
+                          {option.label}
+                        </button>
+                      ))}
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_split || isSplit) && (
+                  <div className="inbox-field">
+                    <label>Business split %</label>
+                    <input
+                      type="number"
+                      min="0"
+                      max="100"
+                      value={draft.split_business_pct}
+                      onChange={(event) => setInboxDraftField(card.id, 'split_business_pct', event.target.value)}
+                      placeholder="80"
+                    />
+                  </div>
+                )}
+
+                {card.supports_duplicate && (
+                  <>
+                    <div>
+                      <div className="inbox-field">
+                        <label>Duplicate action</label>
+                      </div>
+                      <div className="inbox-duplicate-row">
+                        {[
+                          { value: 'dismiss', label: 'Dismiss duplicate' },
+                          { value: 'merge', label: 'Merge to original' },
+                        ].map((option) => (
+                          <button
+                            key={option.value}
+                            type="button"
+                            className={`inbox-toggle ${draft.duplicate_action === option.value ? 'active' : ''}`}
+                            onClick={() => setInboxDraftField(card.id, 'duplicate_action', option.value)}
+                          >
+                            {option.label}
+                          </button>
+                        ))}
+                      </div>
+                    </div>
+                    {draft.duplicate_action === 'merge' && (
+                      <div className="inbox-field">
+                        <label>Original transaction id</label>
+                        <input
+                          value={draft.duplicate_of_txn_id}
+                          onChange={(event) => setInboxDraftField(card.id, 'duplicate_of_txn_id', event.target.value)}
+                          placeholder="txn-9001"
+                        />
+                      </div>
+                    )}
+                  </>
+                )}
+
+                <div className="inbox-footer-row">
+                  <div className="inbox-inline-status">
+                    Route: {card.resolution_target?.route || `/api/v1/inbox/${card.id}`}
+                  </div>
+                  <div className="inbox-footer-actions">
+                    <button className="secondary-action" onClick={clearInboxMessage} disabled={saving}>
+                      Clear banner
+                    </button>
+                    <button className="primary-action" onClick={() => submitInboxAction(card)} disabled={saving}>
+                      {saving ? <RefreshCcw size={16} className="spinner" /> : <CheckCircle2 size={16} />}
+                      Save and clear blocker
+                    </button>
+                  </div>
+                </div>
+              </section>
+            );
+          })}
+        </div>
+      )}
     </div>
   );
 
@@ -2510,11 +2966,11 @@
           <section className="readiness-panel">
             <h5 style={{ marginBottom: 12 }}>Queue Backlog</h5>
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
-                <div key={item.id} className="control-row">
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
+                <div key={item.id || item.affected_entity_id} className="control-row">
                   <div>
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
                   <div className="text-end">
                     <div className="small-muted">{item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date}</div>
@@ -2522,7 +2978,7 @@
                   </div>
                 </div>
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
               )}
             </div>
@@ -2668,7 +3124,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2697,7 +3153,7 @@
   };
 
   const createSnapshotVersion = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     const status = snapshotWorkspace?.snapshot_status;
 
     if (!status?.can_create_snapshot_version) {
@@ -2719,7 +3175,7 @@
         description: `Quarter snapshot generated from Quarter workspace`,
         metadata: {
           source_type: 'manual',
-          readiness_pct: quarterReadiness?.readiness_pct ?? null
+          readiness_pct: activeQuarterReport?.readiness_pct ?? null
         }
       });
       await refreshQuarterWorkspace();
@@ -2734,7 +3190,7 @@
   };
 
   const closeQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(closeReason || '').trim()) {
       setQuarterActionError('Close reason is required.');
       setQuarterActionMessage('');
@@ -2785,7 +3241,7 @@
   };
 
   const reopenQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(reopenReason || '').trim()) {
       setQuarterActionError('Reopen reason is required.');
       setQuarterActionMessage('');
@@ -2844,9 +3300,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3340,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3352,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3363,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3377,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..6ed29cb8fd5890c5ad8a645c7dc19c7e69511027
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,152 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/bizPA/frontend/src/quarterReadiness.test.js b/bizPA/frontend/src/quarterReadiness.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..0b810191a89d8b82091bf5de1408afcc23c14e65
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.test.js
@@ -0,0 +1,93 @@
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
+
+const reportFixture = {
+  quarter_reference: 'Q1-2026',
+  period_start: '2026-01-01',
+  period_end: '2026-03-31',
+  total_txns_in_period: 18,
+  readiness_pct: 78,
+  can_export: false,
+  issue_list: [
+    {
+      issue_type: 'missing_category',
+      affected_entity_id: 'txn-9101',
+      severity: 'high',
+      weight: 35,
+      explanation: 'Category missing for Fuel Stop.',
+      txn_date: '2026-02-04',
+      merchant: 'Fuel Stop',
+      amount: 64.18,
+      direction: 'out',
+      resolution_target: {
+        label: 'Classify transaction',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'missing_business_personal',
+      affected_entity_id: 'txn-9103',
+      severity: 'high',
+      weight: 30,
+      explanation: 'Business/personal decision missing.',
+      txn_date: '2026-02-15',
+      merchant: 'Mobile Contract',
+      amount: 42.5,
+      direction: 'out',
+      resolution_target: {
+        label: 'Set business or personal',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'unresolved_duplicate',
+      affected_entity_id: 'txn-9104',
+      severity: 'medium',
+      weight: 15,
+      explanation: 'Duplicate not resolved.',
+      txn_date: '2026-03-02',
+      merchant: 'Client Payment',
+      amount: 820,
+      direction: 'in',
+      resolution_target: {
+        label: 'Resolve duplicate',
+        workflow: '/inbox/finish-now',
+      },
+    },
+  ],
+};
+
+describe('quarter readiness helpers', () => {
+  test('keeps blockers ordered and updates headline while blockers remain', () => {
+    const state = buildQuarterUiState({ report: reportFixture, resolvedIssueIds: ['txn-9104'] });
+
+    expect(state.itemsLeft).toBe(2);
+    expect(state.exportEnabled).toBe(false);
+    expect(state.report.readiness_pct).toBeGreaterThan(78);
+    expect(state.headline).toContain('items left');
+    expect(state.activeIssues.map((issue) => issue.affected_entity_id)).toEqual(['txn-9101', 'txn-9103']);
+    expect(state.report.explanation_lines[0]).toContain('Readiness is');
+  });
+
+  test('unlocks export immediately when every blocker is resolved', () => {
+    const state = buildQuarterUiState({
+      report: reportFixture,
+      resolvedIssueIds: ['txn-9101', 'txn-9103', 'txn-9104'],
+    });
+
+    expect(state.itemsLeft).toBe(0);
+    expect(state.exportEnabled).toBe(true);
+    expect(state.report.readiness_pct).toBe(100);
+    expect(state.report.can_export).toBe(true);
+    expect(state.report.issue_summary).toEqual([]);
+  });
+
+  test('supports demo query params that resolve all blockers', () => {
+    const params = new URLSearchParams('resolvedDemoIssues=all');
+    const resolvedIds = deriveResolvedIssueIdsFromSearchParams(params, reportFixture.issue_list);
+
+    expect(resolvedIds).toEqual(['txn-9101', 'txn-9103', 'txn-9104']);
+  });
+});
diff --git a/bizPA/start_bizpa_tax_readiness_ui.ps1 b/bizPA/start_bizpa_tax_readiness_ui.ps1
index 1fc4bef8cd172c31fdc3db52d1acc9d4775c5485..7216e44eac386fe436a9af8e9b417cd2d61e37fe
--- a/bizPA/start_bizpa_tax_readiness_ui.ps1
+++ b/bizPA/start_bizpa_tax_readiness_ui.ps1
@@ -4,6 +4,7 @@
 $frontendPath = Join-Path $repoRoot 'frontend'
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
 
 Write-Host 'Starting bizPA tax readiness UI...' -ForegroundColor Cyan
 
@@ -14,7 +15,10 @@
 Write-Host 'Tax readiness dashboard URL:' -ForegroundColor Green
 Write-Host "  $readinessUrl" -ForegroundColor Yellow
 Write-Host ''
+Write-Host 'Export-ready verification URL:' -ForegroundColor Green
+Write-Host "  $exportReadyUrl" -ForegroundColor Yellow
+Write-Host ''
 Write-Host 'Base frontend URL:' -ForegroundColor Green
 Write-Host "  $frontendUrl" -ForegroundColor Yellow
 Write-Host ''
-Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI can be reviewed without backend setup.' -ForegroundColor Cyan
+Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI and zero-blocker export-ready state can be reviewed without backend setup.' -ForegroundColor Cyan
diff --git a/bizPA/tax_readiness_ui_smoke.ps1 b/bizPA/tax_readiness_ui_smoke.ps1
index 56d67892a51b22572503dec560d17123b370c2b7..269c2a3221adfe9095130883c89fd76428a65989
--- a/bizPA/tax_readiness_ui_smoke.ps1
+++ b/bizPA/tax_readiness_ui_smoke.ps1
@@ -28,36 +28,38 @@
 
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
-$screenshotPath = 'C:\Users\edebe\eds\workstream\verification\20260311_201500_bizpa_tax_readiness_ui.png'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
+$verificationRoot = 'C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification'
+$quarterScreenshotPath = Join-Path $verificationRoot 'quarter_readiness_screen.png'
+$finishNowScreenshotPath = Join-Path $verificationRoot 'finish_now_queue_zero_blockers.png'
 $chromeProfilePath = 'C:\Users\edebe\eds\workstream\verification\chrome_capture_profile_tax_readiness'
-
-New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
+$chromePath = 'C:\Program Files\Google\Chrome\Application\chrome.exe'
 
-$frontendJob = Start-Job -ScriptBlock {
-  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
-  $env:PORT = '3002'
-  npm.cmd start
-}
+function Wait-FrontendReady {
+  param (
+    [string]$Url
+  )
 
-try {
-  $frontendResponse = $null
-  $frontendReady = $false
   for ($i = 0; $i -lt 45; $i++) {
     Start-Sleep -Seconds 2
     try {
-      $frontendResponse = Invoke-WebRequest -UseBasicParsing $readinessUrl
-      if ($frontendResponse.StatusCode -eq 200) {
-        $frontendReady = $true
-        break
+      $response = Invoke-WebRequest -UseBasicParsing $Url
+      if ($response.StatusCode -eq 200) {
+        return $response
       }
     } catch {
     }
   }
 
-  if (-not $frontendReady) {
-    throw "Frontend did not become ready on $readinessUrl"
-  }
+  throw "Frontend did not become ready on $Url"
+}
 
+function Capture-ChromeAppScreenshot {
+  param (
+    [string]$Url,
+    [string]$OutputPath
+  )
+
   $chromeArgs = @(
     "--user-data-dir=$chromeProfilePath",
     '--new-window',
@@ -68,9 +70,9 @@
     '--disable-breakpad',
     '--disable-crash-reporter',
     '--noerrdialogs',
-    "--app=$readinessUrl"
+    "--app=$Url"
   )
-  Start-Process 'C:\Program Files\Google\Chrome\Application\chrome.exe' -ArgumentList $chromeArgs | Out-Null
+  Start-Process $chromePath -ArgumentList $chromeArgs | Out-Null
 
   $chromeWindow = $null
   for ($i = 0; $i -lt 20; $i++) {
@@ -96,7 +98,7 @@
   }
 
   if (-not $chromeWindow) {
-    throw 'Chrome readiness window did not expose a main window handle for screenshot capture.'
+    throw "Chrome window did not expose a main window handle for $Url"
   }
 
   [WindowCaptureNative]::ShowWindow($chromeWindow.MainWindowHandle, 3) | Out-Null
@@ -110,17 +112,34 @@
   $bitmap = New-Object System.Drawing.Bitmap $width, $height
   $graphics = [System.Drawing.Graphics]::FromImage($bitmap)
   $graphics.CopyFromScreen($rect.Left, $rect.Top, 0, 0, $bitmap.Size)
-  $bitmap.Save($screenshotPath, [System.Drawing.Imaging.ImageFormat]::Png)
+  $bitmap.Save($OutputPath, [System.Drawing.Imaging.ImageFormat]::Png)
   $graphics.Dispose()
   $bitmap.Dispose()
 
   Get-Process chrome -ErrorAction SilentlyContinue |
     Where-Object { $_.StartTime -ge $chromeWindow.StartTime.AddSeconds(-2) } |
     Stop-Process -Force -ErrorAction SilentlyContinue
+}
+
+New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
+New-Item -ItemType Directory -Force -Path $verificationRoot | Out-Null
+
+$frontendJob = Start-Job -ScriptBlock {
+  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
+  $env:PORT = '3002'
+  npm.cmd start
+}
+
+try {
+  $frontendResponse = Wait-FrontendReady -Url $readinessUrl
+  Capture-ChromeAppScreenshot -Url $readinessUrl -OutputPath $quarterScreenshotPath
+  Capture-ChromeAppScreenshot -Url $exportReadyUrl -OutputPath $finishNowScreenshotPath
 
   "READINESS_URL=$readinessUrl"
+  "EXPORT_READY_URL=$exportReadyUrl"
   "FRONTEND_STATUS=$($frontendResponse.StatusCode)"
-  "SCREENSHOT=$screenshotPath"
+  "SCREENSHOT_QUARTER=$quarterScreenshotPath"
+  "SCREENSHOT_EXPORT_READY=$finishNowScreenshotPath"
   'FRONTEND_LOG_START'
   Receive-Job $frontendJob -Keep -ErrorAction SilentlyContinue 2>&1
   'FRONTEND_LOG_END'
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update
M C:\Users\edebe\eds\bizPA\frontend\src\quarterReadiness.js
@@ -100,3 +100,3 @@
 
-export const buildQuarterUiState = ({ report = null, resolvedIssueIds = [] }) => {
+export const buildQuarterUiState = ({ report = null, fallbackIssues = [], resolvedIssueIds = [] }) => {
   if (!report) {
@@ -111,3 +111,6 @@
 
-  const allIssues = sortIssues(Array.isArray(report.issue_list) ? report.issue_list : []);
+  const issueSource = Array.isArray(report.issue_list) && report.issue_list.length > 0
+    ? report.issue_list
+    : fallbackIssues;
+  const allIssues = sortIssues(Array.isArray(issueSource) ? issueSource : []);
   const resolvedSet = new Set(resolvedIssueIds);
apply_patch(auto_approved=true) exited 0 in 599ms:
Success. Updated the following files:
M C:\Users\edebe\eds\bizPA\frontend\src\quarterReadiness.js
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..0253d40f85fe6785090b08aa24db3a6f2a6d0909
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,25 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1531,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1546,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1632,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2103,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2317,25 +2562,236 @@
   );
 
   const renderInbox = () => (
-    <div>
-      <h4>Finish Now Queue</h4>
-      <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
-      <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
-            <div>
-              <b>{item.merchant || 'Unknown'}</b>
-              <br />
-              <small>
-                {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
-              </small>
-              <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
-            </div>
+    <div className="inbox-mobile-shell">
+      <section className="inbox-mobile-hero">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2">
+          <div className="inbox-kicker">
+            <Bell size={14} />
+            Mobile-first exception queue
           </div>
-        ))}
-      </div>
+          <div className="status-pill">{filteredInboxCards.length} visible</div>
+        </div>
+        <h4>Finish now</h4>
+        <p>
+          Clear the blockers that stop the quarterly export: category, business or personal, split treatment, and duplicate resolution.
+        </p>
+        <div className="inbox-mobile-stats">
+          <div className="inbox-mobile-stat">
+            <small>Queue</small>
+            <strong>{inboxCards.length}</strong>
+            <small>{activeQuarterReport.quarter_reference || READINESS_DEMO_REPORT.quarter_reference}</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Critical</small>
+            <strong>{highSeverityCount}</strong>
+            <small>High-severity blockers</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Ready now</small>
+            <strong>{readyToExportCount}</strong>
+            <small>Transactions already export-safe</small>
+          </div>
+        </div>
+        <div className="hero-actions">
+          <button className="hero-action-main" onClick={() => setCurrentTab('quarter')}>
+            <ArrowRight size={16} />
+            Review quarter readiness
+          </button>
+          <button className="hero-action-secondary" onClick={undoLastInboxAction} disabled={inboxBusyId === 'undo-last'}>
+            {inboxBusyId === 'undo-last' ? <RefreshCcw size={16} className="spinner" /> : <ArchiveRestore size={16} />}
+            Undo last action
+          </button>
+        </div>
+      </section>
+
+      {inboxMessage && (
+        <div className={`readiness-panel inbox-message ${inboxMessage.type}`}>
+          {inboxMessage.text}
+        </div>
+      )}
+
+      <section className="readiness-panel">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-3">
+          <div>
+            <h5 style={{ marginBottom: 4 }}>Queue filters</h5>
+            <small className="small-muted">Keep the mobile pass focused on one blocker type at a time.</small>
+          </div>
+          <div className="status-pill">{activeQuarterReport.readiness_pct ?? READINESS_DEMO_REPORT.readiness_pct}% ready</div>
+        </div>
+        <div className="inbox-filter-row">
+          {INBOX_FILTERS.map((filter) => {
+            const count = filter.key === 'all'
+              ? inboxCards.length
+              : inboxCards.filter((card) => card.issue_types.includes(filter.key)).length;
+            return (
+              <button
+                key={filter.key}
+                type="button"
+                className={`inbox-filter-chip ${inboxFilter === filter.key ? 'active' : ''}`}
+                onClick={() => setInboxFilter(filter.key)}
+              >
+                <span>{filter.label}</span>
+                <small>{count}</small>
+              </button>
+            );
+          })}
+        </div>
+      </section>
+
+      {filteredInboxCards.length === 0 ? (
+        <div className="inbox-empty">
+          No blockers match this filter. Reset to All blockers or return to the quarter screen to confirm readiness.
+        </div>
+      ) : (
+        <div className="inbox-card-list">
+          {filteredInboxCards.map((card) => {
+            const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+            const saving = inboxBusyId === card.id;
+            const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+            return (
+              <section key={card.id} className="inbox-action-card">
+                <div className="inbox-card-top">
+                  <div>
+                    <strong>{card.merchant}</strong>
+                    <div className="inbox-card-subline">
+                      {card.txn_date ? String(card.txn_date).slice(0, 10) : 'Date pending'} · {formatIssueAmount(card.amount, card.direction)}
+                    </div>
+                  </div>
+                  <span className={`inbox-issue-chip ${card.severity}`}>
+                    <TriangleAlert size={14} />
+                    {INBOX_REASON_LABELS[card.blocker_reason] || card.blocker_reason.replace(/_/g, ' ')}
+                  </span>
+                </div>
+
+                <div className="inbox-chip-row">
+                  {card.issue_labels.map((label) => (
+                    <span key={`${card.id}-${label}`} className={`inbox-issue-chip ${card.severity}`}>
+                      {label}
+                    </span>
+                  ))}
+                  {card.suggested_category?.category_name && (
+                    <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
+                      <CheckCircle2 size={14} />
+                      Suggested: {card.suggested_category.category_name}
+                    </button>
+                  )}
+                </div>
+
+                <p className="inbox-explainer">{card.explanation || 'Resolve the blocking fields to clear this transaction from the queue.'}</p>
+
+                {(card.supports_category || card.supports_business_personal || card.supports_split) && (
+                  <div className="inbox-action-grid">
+                    <div className="inbox-field">
+                      <label>Category code</label>
+                      <input
+                        value={draft.category_code}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_code', event.target.value)}
+                        placeholder="MATERIALS"
+                      />
+                    </div>
+                    <div className="inbox-field">
+                      <label>Category label</label>
+                      <input
+                        value={draft.category_name}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_name', event.target.value)}
+                        placeholder="Materials"
+                      />
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_business_personal || card.supports_split) && (
+                  <div>
+                    <div className="inbox-field">
+                      <label>Business treatment</label>
+                    </div>
+                    <div className="inbox-toggle-row">
+                      {[
+                        { value: 'BUSINESS', label: 'Business' },
+                        { value: 'PERSONAL', label: 'Personal' },
+                        { value: 'SPLIT', label: 'Split' },
+                      ].map((option) => (
+                        <button
+                          key={option.value}
+                          type="button"
+                          className={`inbox-toggle ${draft.business_personal === option.value ? 'active' : ''}`}
+                          onClick={() => setInboxDraftField(card.id, 'business_personal', option.value)}
+                        >
+                          {option.label}
+                        </button>
+                      ))}
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_split || isSplit) && (
+                  <div className="inbox-field">
+                    <label>Business split %</label>
+                    <input
+                      type="number"
+                      min="0"
+                      max="100"
+                      value={draft.split_business_pct}
+                      onChange={(event) => setInboxDraftField(card.id, 'split_business_pct', event.target.value)}
+                      placeholder="80"
+                    />
+                  </div>
+                )}
+
+                {card.supports_duplicate && (
+                  <>
+                    <div>
+                      <div className="inbox-field">
+                        <label>Duplicate action</label>
+                      </div>
+                      <div className="inbox-duplicate-row">
+                        {[
+                          { value: 'dismiss', label: 'Dismiss duplicate' },
+                          { value: 'merge', label: 'Merge to original' },
+                        ].map((option) => (
+                          <button
+                            key={option.value}
+                            type="button"
+                            className={`inbox-toggle ${draft.duplicate_action === option.value ? 'active' : ''}`}
+                            onClick={() => setInboxDraftField(card.id, 'duplicate_action', option.value)}
+                          >
+                            {option.label}
+                          </button>
+                        ))}
+                      </div>
+                    </div>
+                    {draft.duplicate_action === 'merge' && (
+                      <div className="inbox-field">
+                        <label>Original transaction id</label>
+                        <input
+                          value={draft.duplicate_of_txn_id}
+                          onChange={(event) => setInboxDraftField(card.id, 'duplicate_of_txn_id', event.target.value)}
+                          placeholder="txn-9001"
+                        />
+                      </div>
+                    )}
+                  </>
+                )}
+
+                <div className="inbox-footer-row">
+                  <div className="inbox-inline-status">
+                    Route: {card.resolution_target?.route || `/api/v1/inbox/${card.id}`}
+                  </div>
+                  <div className="inbox-footer-actions">
+                    <button className="secondary-action" onClick={clearInboxMessage} disabled={saving}>
+                      Clear banner
+                    </button>
+                    <button className="primary-action" onClick={() => submitInboxAction(card)} disabled={saving}>
+                      {saving ? <RefreshCcw size={16} className="spinner" /> : <CheckCircle2 size={16} />}
+                      Save and clear blocker
+                    </button>
+                  </div>
+                </div>
+              </section>
+            );
+          })}
+        </div>
+      )}
     </div>
   );
 
@@ -2510,11 +2966,11 @@
           <section className="readiness-panel">
             <h5 style={{ marginBottom: 12 }}>Queue Backlog</h5>
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
-                <div key={item.id} className="control-row">
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
+                <div key={item.id || item.affected_entity_id} className="control-row">
                   <div>
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
                   <div className="text-end">
                     <div className="small-muted">{item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date}</div>
@@ -2522,7 +2978,7 @@
                   </div>
                 </div>
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
               )}
             </div>
@@ -2668,7 +3124,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2697,7 +3153,7 @@
   };
 
   const createSnapshotVersion = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     const status = snapshotWorkspace?.snapshot_status;
 
     if (!status?.can_create_snapshot_version) {
@@ -2719,7 +3175,7 @@
         description: `Quarter snapshot generated from Quarter workspace`,
         metadata: {
           source_type: 'manual',
-          readiness_pct: quarterReadiness?.readiness_pct ?? null
+          readiness_pct: activeQuarterReport?.readiness_pct ?? null
         }
       });
       await refreshQuarterWorkspace();
@@ -2734,7 +3190,7 @@
   };
 
   const closeQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(closeReason || '').trim()) {
       setQuarterActionError('Close reason is required.');
       setQuarterActionMessage('');
@@ -2785,7 +3241,7 @@
   };
 
   const reopenQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(reopenReason || '').trim()) {
       setQuarterActionError('Reopen reason is required.');
       setQuarterActionMessage('');
@@ -2844,9 +3300,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3340,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3352,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3363,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3377,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..669173cd6176ee9ee5c86c5895ea49cb9524ebc4
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,155 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, fallbackIssues = [], resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const issueSource = Array.isArray(report.issue_list) && report.issue_list.length > 0
+    ? report.issue_list
+    : fallbackIssues;
+  const allIssues = sortIssues(Array.isArray(issueSource) ? issueSource : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/bizPA/frontend/src/quarterReadiness.test.js b/bizPA/frontend/src/quarterReadiness.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..0b810191a89d8b82091bf5de1408afcc23c14e65
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.test.js
@@ -0,0 +1,93 @@
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
+
+const reportFixture = {
+  quarter_reference: 'Q1-2026',
+  period_start: '2026-01-01',
+  period_end: '2026-03-31',
+  total_txns_in_period: 18,
+  readiness_pct: 78,
+  can_export: false,
+  issue_list: [
+    {
+      issue_type: 'missing_category',
+      affected_entity_id: 'txn-9101',
+      severity: 'high',
+      weight: 35,
+      explanation: 'Category missing for Fuel Stop.',
+      txn_date: '2026-02-04',
+      merchant: 'Fuel Stop',
+      amount: 64.18,
+      direction: 'out',
+      resolution_target: {
+        label: 'Classify transaction',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'missing_business_personal',
+      affected_entity_id: 'txn-9103',
+      severity: 'high',
+      weight: 30,
+      explanation: 'Business/personal decision missing.',
+      txn_date: '2026-02-15',
+      merchant: 'Mobile Contract',
+      amount: 42.5,
+      direction: 'out',
+      resolution_target: {
+        label: 'Set business or personal',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'unresolved_duplicate',
+      affected_entity_id: 'txn-9104',
+      severity: 'medium',
+      weight: 15,
+      explanation: 'Duplicate not resolved.',
+      txn_date: '2026-03-02',
+      merchant: 'Client Payment',
+      amount: 820,
+      direction: 'in',
+      resolution_target: {
+        label: 'Resolve duplicate',
+        workflow: '/inbox/finish-now',
+      },
+    },
+  ],
+};
+
+describe('quarter readiness helpers', () => {
+  test('keeps blockers ordered and updates headline while blockers remain', () => {
+    const state = buildQuarterUiState({ report: reportFixture, resolvedIssueIds: ['txn-9104'] });
+
+    expect(state.itemsLeft).toBe(2);
+    expect(state.exportEnabled).toBe(false);
+    expect(state.report.readiness_pct).toBeGreaterThan(78);
+    expect(state.headline).toContain('items left');
+    expect(state.activeIssues.map((issue) => issue.affected_entity_id)).toEqual(['txn-9101', 'txn-9103']);
+    expect(state.report.explanation_lines[0]).toContain('Readiness is');
+  });
+
+  test('unlocks export immediately when every blocker is resolved', () => {
+    const state = buildQuarterUiState({
+      report: reportFixture,
+      resolvedIssueIds: ['txn-9101', 'txn-9103', 'txn-9104'],
+    });
+
+    expect(state.itemsLeft).toBe(0);
+    expect(state.exportEnabled).toBe(true);
+    expect(state.report.readiness_pct).toBe(100);
+    expect(state.report.can_export).toBe(true);
+    expect(state.report.issue_summary).toEqual([]);
+  });
+
+  test('supports demo query params that resolve all blockers', () => {
+    const params = new URLSearchParams('resolvedDemoIssues=all');
+    const resolvedIds = deriveResolvedIssueIdsFromSearchParams(params, reportFixture.issue_list);
+
+    expect(resolvedIds).toEqual(['txn-9101', 'txn-9103', 'txn-9104']);
+  });
+});
diff --git a/bizPA/start_bizpa_tax_readiness_ui.ps1 b/bizPA/start_bizpa_tax_readiness_ui.ps1
index 1fc4bef8cd172c31fdc3db52d1acc9d4775c5485..7216e44eac386fe436a9af8e9b417cd2d61e37fe
--- a/bizPA/start_bizpa_tax_readiness_ui.ps1
+++ b/bizPA/start_bizpa_tax_readiness_ui.ps1
@@ -4,6 +4,7 @@
 $frontendPath = Join-Path $repoRoot 'frontend'
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
 
 Write-Host 'Starting bizPA tax readiness UI...' -ForegroundColor Cyan
 
@@ -14,7 +15,10 @@
 Write-Host 'Tax readiness dashboard URL:' -ForegroundColor Green
 Write-Host "  $readinessUrl" -ForegroundColor Yellow
 Write-Host ''
+Write-Host 'Export-ready verification URL:' -ForegroundColor Green
+Write-Host "  $exportReadyUrl" -ForegroundColor Yellow
+Write-Host ''
 Write-Host 'Base frontend URL:' -ForegroundColor Green
 Write-Host "  $frontendUrl" -ForegroundColor Yellow
 Write-Host ''
-Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI can be reviewed without backend setup.' -ForegroundColor Cyan
+Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI and zero-blocker export-ready state can be reviewed without backend setup.' -ForegroundColor Cyan
diff --git a/bizPA/tax_readiness_ui_smoke.ps1 b/bizPA/tax_readiness_ui_smoke.ps1
index 56d67892a51b22572503dec560d17123b370c2b7..269c2a3221adfe9095130883c89fd76428a65989
--- a/bizPA/tax_readiness_ui_smoke.ps1
+++ b/bizPA/tax_readiness_ui_smoke.ps1
@@ -28,36 +28,38 @@
 
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
-$screenshotPath = 'C:\Users\edebe\eds\workstream\verification\20260311_201500_bizpa_tax_readiness_ui.png'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
+$verificationRoot = 'C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification'
+$quarterScreenshotPath = Join-Path $verificationRoot 'quarter_readiness_screen.png'
+$finishNowScreenshotPath = Join-Path $verificationRoot 'finish_now_queue_zero_blockers.png'
 $chromeProfilePath = 'C:\Users\edebe\eds\workstream\verification\chrome_capture_profile_tax_readiness'
-
-New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
+$chromePath = 'C:\Program Files\Google\Chrome\Application\chrome.exe'
 
-$frontendJob = Start-Job -ScriptBlock {
-  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
-  $env:PORT = '3002'
-  npm.cmd start
-}
+function Wait-FrontendReady {
+  param (
+    [string]$Url
+  )
 
-try {
-  $frontendResponse = $null
-  $frontendReady = $false
   for ($i = 0; $i -lt 45; $i++) {
     Start-Sleep -Seconds 2
     try {
-      $frontendResponse = Invoke-WebRequest -UseBasicParsing $readinessUrl
-      if ($frontendResponse.StatusCode -eq 200) {
-        $frontendReady = $true
-        break
+      $response = Invoke-WebRequest -UseBasicParsing $Url
+      if ($response.StatusCode -eq 200) {
+        return $response
       }
     } catch {
     }
   }
 
-  if (-not $frontendReady) {
-    throw "Frontend did not become ready on $readinessUrl"
-  }
+  throw "Frontend did not become ready on $Url"
+}
 
+function Capture-ChromeAppScreenshot {
+  param (
+    [string]$Url,
+    [string]$OutputPath
+  )
+
   $chromeArgs = @(
     "--user-data-dir=$chromeProfilePath",
     '--new-window',
@@ -68,9 +70,9 @@
     '--disable-breakpad',
     '--disable-crash-reporter',
     '--noerrdialogs',
-    "--app=$readinessUrl"
+    "--app=$Url"
   )
-  Start-Process 'C:\Program Files\Google\Chrome\Application\chrome.exe' -ArgumentList $chromeArgs | Out-Null
+  Start-Process $chromePath -ArgumentList $chromeArgs | Out-Null
 
   $chromeWindow = $null
   for ($i = 0; $i -lt 20; $i++) {
@@ -96,7 +98,7 @@
   }
 
   if (-not $chromeWindow) {
-    throw 'Chrome readiness window did not expose a main window handle for screenshot capture.'
+    throw "Chrome window did not expose a main window handle for $Url"
   }
 
   [WindowCaptureNative]::ShowWindow($chromeWindow.MainWindowHandle, 3) | Out-Null
@@ -110,17 +112,34 @@
   $bitmap = New-Object System.Drawing.Bitmap $width, $height
   $graphics = [System.Drawing.Graphics]::FromImage($bitmap)
   $graphics.CopyFromScreen($rect.Left, $rect.Top, 0, 0, $bitmap.Size)
-  $bitmap.Save($screenshotPath, [System.Drawing.Imaging.ImageFormat]::Png)
+  $bitmap.Save($OutputPath, [System.Drawing.Imaging.ImageFormat]::Png)
   $graphics.Dispose()
   $bitmap.Dispose()
 
   Get-Process chrome -ErrorAction SilentlyContinue |
     Where-Object { $_.StartTime -ge $chromeWindow.StartTime.AddSeconds(-2) } |
     Stop-Process -Force -ErrorAction SilentlyContinue
+}
+
+New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
+New-Item -ItemType Directory -Force -Path $verificationRoot | Out-Null
+
+$frontendJob = Start-Job -ScriptBlock {
+  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
+  $env:PORT = '3002'
+  npm.cmd start
+}
+
+try {
+  $frontendResponse = Wait-FrontendReady -Url $readinessUrl
+  Capture-ChromeAppScreenshot -Url $readinessUrl -OutputPath $quarterScreenshotPath
+  Capture-ChromeAppScreenshot -Url $exportReadyUrl -OutputPath $finishNowScreenshotPath
 
   "READINESS_URL=$readinessUrl"
+  "EXPORT_READY_URL=$exportReadyUrl"
   "FRONTEND_STATUS=$($frontendResponse.StatusCode)"
-  "SCREENSHOT=$screenshotPath"
+  "SCREENSHOT_QUARTER=$quarterScreenshotPath"
+  "SCREENSHOT_EXPORT_READY=$finishNowScreenshotPath"
   'FRONTEND_LOG_START'
   Receive-Job $frontendJob -Keep -ErrorAction SilentlyContinue 2>&1
   'FRONTEND_LOG_END'
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..0253d40f85fe6785090b08aa24db3a6f2a6d0909
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,25 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1531,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1546,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1632,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2103,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2317,25 +2562,236 @@
   );
 
   const renderInbox = () => (
-    <div>
-      <h4>Finish Now Queue</h4>
-      <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
-      <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
-            <div>
-              <b>{item.merchant || 'Unknown'}</b>
-              <br />
-              <small>
-                {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
-              </small>
-              <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
-            </div>
+    <div className="inbox-mobile-shell">
+      <section className="inbox-mobile-hero">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2">
+          <div className="inbox-kicker">
+            <Bell size={14} />
+            Mobile-first exception queue
           </div>
-        ))}
-      </div>
+          <div className="status-pill">{filteredInboxCards.length} visible</div>
+        </div>
+        <h4>Finish now</h4>
+        <p>
+          Clear the blockers that stop the quarterly export: category, business or personal, split treatment, and duplicate resolution.
+        </p>
+        <div className="inbox-mobile-stats">
+          <div className="inbox-mobile-stat">
+            <small>Queue</small>
+            <strong>{inboxCards.length}</strong>
+            <small>{activeQuarterReport.quarter_reference || READINESS_DEMO_REPORT.quarter_reference}</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Critical</small>
+            <strong>{highSeverityCount}</strong>
+            <small>High-severity blockers</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Ready now</small>
+            <strong>{readyToExportCount}</strong>
+            <small>Transactions already export-safe</small>
+          </div>
+        </div>
+        <div className="hero-actions">
+          <button className="hero-action-main" onClick={() => setCurrentTab('quarter')}>
+            <ArrowRight size={16} />
+            Review quarter readiness
+          </button>
+          <button className="hero-action-secondary" onClick={undoLastInboxAction} disabled={inboxBusyId === 'undo-last'}>
+            {inboxBusyId === 'undo-last' ? <RefreshCcw size={16} className="spinner" /> : <ArchiveRestore size={16} />}
+            Undo last action
+          </button>
+        </div>
+      </section>
+
+      {inboxMessage && (
+        <div className={`readiness-panel inbox-message ${inboxMessage.type}`}>
+          {inboxMessage.text}
+        </div>
+      )}
+
+      <section className="readiness-panel">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-3">
+          <div>
+            <h5 style={{ marginBottom: 4 }}>Queue filters</h5>
+            <small className="small-muted">Keep the mobile pass focused on one blocker type at a time.</small>
+          </div>
+          <div className="status-pill">{activeQuarterReport.readiness_pct ?? READINESS_DEMO_REPORT.readiness_pct}% ready</div>
+        </div>
+        <div className="inbox-filter-row">
+          {INBOX_FILTERS.map((filter) => {
+            const count = filter.key === 'all'
+              ? inboxCards.length
+              : inboxCards.filter((card) => card.issue_types.includes(filter.key)).length;
+            return (
+              <button
+                key={filter.key}
+                type="button"
+                className={`inbox-filter-chip ${inboxFilter === filter.key ? 'active' : ''}`}
+                onClick={() => setInboxFilter(filter.key)}
+              >
+                <span>{filter.label}</span>
+                <small>{count}</small>
+              </button>
+            );
+          })}
+        </div>
+      </section>
+
+      {filteredInboxCards.length === 0 ? (
+        <div className="inbox-empty">
+          No blockers match this filter. Reset to All blockers or return to the quarter screen to confirm readiness.
+        </div>
+      ) : (
+        <div className="inbox-card-list">
+          {filteredInboxCards.map((card) => {
+            const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+            const saving = inboxBusyId === card.id;
+            const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+            return (
+              <section key={card.id} className="inbox-action-card">
+                <div className="inbox-card-top">
+                  <div>
+                    <strong>{card.merchant}</strong>
+                    <div className="inbox-card-subline">
+                      {card.txn_date ? String(card.txn_date).slice(0, 10) : 'Date pending'} · {formatIssueAmount(card.amount, card.direction)}
+                    </div>
+                  </div>
+                  <span className={`inbox-issue-chip ${card.severity}`}>
+                    <TriangleAlert size={14} />
+                    {INBOX_REASON_LABELS[card.blocker_reason] || card.blocker_reason.replace(/_/g, ' ')}
+                  </span>
+                </div>
+
+                <div className="inbox-chip-row">
+                  {card.issue_labels.map((label) => (
+                    <span key={`${card.id}-${label}`} className={`inbox-issue-chip ${card.severity}`}>
+                      {label}
+                    </span>
+                  ))}
+                  {card.suggested_category?.category_name && (
+                    <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
+                      <CheckCircle2 size={14} />
+                      Suggested: {card.suggested_category.category_name}
+                    </button>
+                  )}
+                </div>
+
+                <p className="inbox-explainer">{card.explanation || 'Resolve the blocking fields to clear this transaction from the queue.'}</p>
+
+                {(card.supports_category || card.supports_business_personal || card.supports_split) && (
+                  <div className="inbox-action-grid">
+                    <div className="inbox-field">
+                      <label>Category code</label>
+                      <input
+                        value={draft.category_code}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_code', event.target.value)}
+                        placeholder="MATERIALS"
+                      />
+                    </div>
+                    <div className="inbox-field">
+                      <label>Category label</label>
+                      <input
+                        value={draft.category_name}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_name', event.target.value)}
+                        placeholder="Materials"
+                      />
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_business_personal || card.supports_split) && (
+                  <div>
+                    <div className="inbox-field">
+                      <label>Business treatment</label>
+                    </div>
+                    <div className="inbox-toggle-row">
+                      {[
+                        { value: 'BUSINESS', label: 'Business' },
+                        { value: 'PERSONAL', label: 'Personal' },
+                        { value: 'SPLIT', label: 'Split' },
+                      ].map((option) => (
+                        <button
+                          key={option.value}
+                          type="button"
+                          className={`inbox-toggle ${draft.business_personal === option.value ? 'active' : ''}`}
+                          onClick={() => setInboxDraftField(card.id, 'business_personal', option.value)}
+                        >
+                          {option.label}
+                        </button>
+                      ))}
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_split || isSplit) && (
+                  <div className="inbox-field">
+                    <label>Business split %</label>
+                    <input
+                      type="number"
+                      min="0"
+                      max="100"
+                      value={draft.split_business_pct}
+                      onChange={(event) => setInboxDraftField(card.id, 'split_business_pct', event.target.value)}
+                      placeholder="80"
+                    />
+                  </div>
+                )}
+
+                {card.supports_duplicate && (
+                  <>
+                    <div>
+                      <div className="inbox-field">
+                        <label>Duplicate action</label>
+                      </div>
+                      <div className="inbox-duplicate-row">
+                        {[
+                          { value: 'dismiss', label: 'Dismiss duplicate' },
+                          { value: 'merge', label: 'Merge to original' },
+                        ].map((option) => (
+                          <button
+                            key={option.value}
+                            type="button"
+                            className={`inbox-toggle ${draft.duplicate_action === option.value ? 'active' : ''}`}
+                            onClick={() => setInboxDraftField(card.id, 'duplicate_action', option.value)}
+                          >
+                            {option.label}
+                          </button>
+                        ))}
+                      </div>
+                    </div>
+                    {draft.duplicate_action === 'merge' && (
+                      <div className="inbox-field">
+                        <label>Original transaction id</label>
+                        <input
+                          value={draft.duplicate_of_txn_id}
+                          onChange={(event) => setInboxDraftField(card.id, 'duplicate_of_txn_id', event.target.value)}
+                          placeholder="txn-9001"
+                        />
+                      </div>
+                    )}
+                  </>
+                )}
+
+                <div className="inbox-footer-row">
+                  <div className="inbox-inline-status">
+                    Route: {card.resolution_target?.route || `/api/v1/inbox/${card.id}`}
+                  </div>
+                  <div className="inbox-footer-actions">
+                    <button className="secondary-action" onClick={clearInboxMessage} disabled={saving}>
+                      Clear banner
+                    </button>
+                    <button className="primary-action" onClick={() => submitInboxAction(card)} disabled={saving}>
+                      {saving ? <RefreshCcw size={16} className="spinner" /> : <CheckCircle2 size={16} />}
+                      Save and clear blocker
+                    </button>
+                  </div>
+                </div>
+              </section>
+            );
+          })}
+        </div>
+      )}
     </div>
   );
 
@@ -2510,11 +2966,11 @@
           <section className="readiness-panel">
             <h5 style={{ marginBottom: 12 }}>Queue Backlog</h5>
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
-                <div key={item.id} className="control-row">
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
+                <div key={item.id || item.affected_entity_id} className="control-row">
                   <div>
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
                   <div className="text-end">
                     <div className="small-muted">{item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date}</div>
@@ -2522,7 +2978,7 @@
                   </div>
                 </div>
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
               )}
             </div>
@@ -2668,7 +3124,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2697,7 +3153,7 @@
   };
 
   const createSnapshotVersion = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     const status = snapshotWorkspace?.snapshot_status;
 
     if (!status?.can_create_snapshot_version) {
@@ -2719,7 +3175,7 @@
         description: `Quarter snapshot generated from Quarter workspace`,
         metadata: {
           source_type: 'manual',
-          readiness_pct: quarterReadiness?.readiness_pct ?? null
+          readiness_pct: activeQuarterReport?.readiness_pct ?? null
         }
       });
       await refreshQuarterWorkspace();
@@ -2734,7 +3190,7 @@
   };
 
   const closeQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(closeReason || '').trim()) {
       setQuarterActionError('Close reason is required.');
       setQuarterActionMessage('');
@@ -2785,7 +3241,7 @@
   };
 
   const reopenQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(reopenReason || '').trim()) {
       setQuarterActionError('Reopen reason is required.');
       setQuarterActionMessage('');
@@ -2844,9 +3300,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3340,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3352,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3363,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3377,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..669173cd6176ee9ee5c86c5895ea49cb9524ebc4
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,155 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, fallbackIssues = [], resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const issueSource = Array.isArray(report.issue_list) && report.issue_list.length > 0
+    ? report.issue_list
+    : fallbackIssues;
+  const allIssues = sortIssues(Array.isArray(issueSource) ? issueSource : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/bizPA/frontend/src/quarterReadiness.test.js b/bizPA/frontend/src/quarterReadiness.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..0b810191a89d8b82091bf5de1408afcc23c14e65
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.test.js
@@ -0,0 +1,93 @@
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
+
+const reportFixture = {
+  quarter_reference: 'Q1-2026',
+  period_start: '2026-01-01',
+  period_end: '2026-03-31',
+  total_txns_in_period: 18,
+  readiness_pct: 78,
+  can_export: false,
+  issue_list: [
+    {
+      issue_type: 'missing_category',
+      affected_entity_id: 'txn-9101',
+      severity: 'high',
+      weight: 35,
+      explanation: 'Category missing for Fuel Stop.',
+      txn_date: '2026-02-04',
+      merchant: 'Fuel Stop',
+      amount: 64.18,
+      direction: 'out',
+      resolution_target: {
+        label: 'Classify transaction',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'missing_business_personal',
+      affected_entity_id: 'txn-9103',
+      severity: 'high',
+      weight: 30,
+      explanation: 'Business/personal decision missing.',
+      txn_date: '2026-02-15',
+      merchant: 'Mobile Contract',
+      amount: 42.5,
+      direction: 'out',
+      resolution_target: {
+        label: 'Set business or personal',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'unresolved_duplicate',
+      affected_entity_id: 'txn-9104',
+      severity: 'medium',
+      weight: 15,
+      explanation: 'Duplicate not resolved.',
+      txn_date: '2026-03-02',
+      merchant: 'Client Payment',
+      amount: 820,
+      direction: 'in',
+      resolution_target: {
+        label: 'Resolve duplicate',
+        workflow: '/inbox/finish-now',
+      },
+    },
+  ],
+};
+
+describe('quarter readiness helpers', () => {
+  test('keeps blockers ordered and updates headline while blockers remain', () => {
+    const state = buildQuarterUiState({ report: reportFixture, resolvedIssueIds: ['txn-9104'] });
+
+    expect(state.itemsLeft).toBe(2);
+    expect(state.exportEnabled).toBe(false);
+    expect(state.report.readiness_pct).toBeGreaterThan(78);
+    expect(state.headline).toContain('items left');
+    expect(state.activeIssues.map((issue) => issue.affected_entity_id)).toEqual(['txn-9101', 'txn-9103']);
+    expect(state.report.explanation_lines[0]).toContain('Readiness is');
+  });
+
+  test('unlocks export immediately when every blocker is resolved', () => {
+    const state = buildQuarterUiState({
+      report: reportFixture,
+      resolvedIssueIds: ['txn-9101', 'txn-9103', 'txn-9104'],
+    });
+
+    expect(state.itemsLeft).toBe(0);
+    expect(state.exportEnabled).toBe(true);
+    expect(state.report.readiness_pct).toBe(100);
+    expect(state.report.can_export).toBe(true);
+    expect(state.report.issue_summary).toEqual([]);
+  });
+
+  test('supports demo query params that resolve all blockers', () => {
+    const params = new URLSearchParams('resolvedDemoIssues=all');
+    const resolvedIds = deriveResolvedIssueIdsFromSearchParams(params, reportFixture.issue_list);
+
+    expect(resolvedIds).toEqual(['txn-9101', 'txn-9103', 'txn-9104']);
+  });
+});
diff --git a/bizPA/start_bizpa_tax_readiness_ui.ps1 b/bizPA/start_bizpa_tax_readiness_ui.ps1
index 1fc4bef8cd172c31fdc3db52d1acc9d4775c5485..7216e44eac386fe436a9af8e9b417cd2d61e37fe
--- a/bizPA/start_bizpa_tax_readiness_ui.ps1
+++ b/bizPA/start_bizpa_tax_readiness_ui.ps1
@@ -4,6 +4,7 @@
 $frontendPath = Join-Path $repoRoot 'frontend'
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
 
 Write-Host 'Starting bizPA tax readiness UI...' -ForegroundColor Cyan
 
@@ -14,7 +15,10 @@
 Write-Host 'Tax readiness dashboard URL:' -ForegroundColor Green
 Write-Host "  $readinessUrl" -ForegroundColor Yellow
 Write-Host ''
+Write-Host 'Export-ready verification URL:' -ForegroundColor Green
+Write-Host "  $exportReadyUrl" -ForegroundColor Yellow
+Write-Host ''
 Write-Host 'Base frontend URL:' -ForegroundColor Green
 Write-Host "  $frontendUrl" -ForegroundColor Yellow
 Write-Host ''
-Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI can be reviewed without backend setup.' -ForegroundColor Cyan
+Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI and zero-blocker export-ready state can be reviewed without backend setup.' -ForegroundColor Cyan
diff --git a/bizPA/tax_readiness_ui_smoke.ps1 b/bizPA/tax_readiness_ui_smoke.ps1
index 56d67892a51b22572503dec560d17123b370c2b7..269c2a3221adfe9095130883c89fd76428a65989
--- a/bizPA/tax_readiness_ui_smoke.ps1
+++ b/bizPA/tax_readiness_ui_smoke.ps1
@@ -28,36 +28,38 @@
 
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
-$screenshotPath = 'C:\Users\edebe\eds\workstream\verification\20260311_201500_bizpa_tax_readiness_ui.png'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
+$verificationRoot = 'C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification'
+$quarterScreenshotPath = Join-Path $verificationRoot 'quarter_readiness_screen.png'
+$finishNowScreenshotPath = Join-Path $verificationRoot 'finish_now_queue_zero_blockers.png'
 $chromeProfilePath = 'C:\Users\edebe\eds\workstream\verification\chrome_capture_profile_tax_readiness'
-
-New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
+$chromePath = 'C:\Program Files\Google\Chrome\Application\chrome.exe'
 
-$frontendJob = Start-Job -ScriptBlock {
-  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
-  $env:PORT = '3002'
-  npm.cmd start
-}
+function Wait-FrontendReady {
+  param (
+    [string]$Url
+  )
 
-try {
-  $frontendResponse = $null
-  $frontendReady = $false
   for ($i = 0; $i -lt 45; $i++) {
     Start-Sleep -Seconds 2
     try {
-      $frontendResponse = Invoke-WebRequest -UseBasicParsing $readinessUrl
-      if ($frontendResponse.StatusCode -eq 200) {
-        $frontendReady = $true
-        break
+      $response = Invoke-WebRequest -UseBasicParsing $Url
+      if ($response.StatusCode -eq 200) {
+        return $response
       }
     } catch {
     }
   }
 
-  if (-not $frontendReady) {
-    throw "Frontend did not become ready on $readinessUrl"
-  }
+  throw "Frontend did not become ready on $Url"
+}
 
+function Capture-ChromeAppScreenshot {
+  param (
+    [string]$Url,
+    [string]$OutputPath
+  )
+
   $chromeArgs = @(
     "--user-data-dir=$chromeProfilePath",
     '--new-window',
@@ -68,9 +70,9 @@
     '--disable-breakpad',
     '--disable-crash-reporter',
     '--noerrdialogs',
-    "--app=$readinessUrl"
+    "--app=$Url"
   )
-  Start-Process 'C:\Program Files\Google\Chrome\Application\chrome.exe' -ArgumentList $chromeArgs | Out-Null
+  Start-Process $chromePath -ArgumentList $chromeArgs | Out-Null
 
   $chromeWindow = $null
   for ($i = 0; $i -lt 20; $i++) {
@@ -96,7 +98,7 @@
   }
 
   if (-not $chromeWindow) {
-    throw 'Chrome readiness window did not expose a main window handle for screenshot capture.'
+    throw "Chrome window did not expose a main window handle for $Url"
   }
 
   [WindowCaptureNative]::ShowWindow($chromeWindow.MainWindowHandle, 3) | Out-Null
@@ -110,17 +112,34 @@
   $bitmap = New-Object System.Drawing.Bitmap $width, $height
   $graphics = [System.Drawing.Graphics]::FromImage($bitmap)
   $graphics.CopyFromScreen($rect.Left, $rect.Top, 0, 0, $bitmap.Size)
-  $bitmap.Save($screenshotPath, [System.Drawing.Imaging.ImageFormat]::Png)
+  $bitmap.Save($OutputPath, [System.Drawing.Imaging.ImageFormat]::Png)
   $graphics.Dispose()
   $bitmap.Dispose()
 
   Get-Process chrome -ErrorAction SilentlyContinue |
     Where-Object { $_.StartTime -ge $chromeWindow.StartTime.AddSeconds(-2) } |
     Stop-Process -Force -ErrorAction SilentlyContinue
+}
+
+New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
+New-Item -ItemType Directory -Force -Path $verificationRoot | Out-Null
+
+$frontendJob = Start-Job -ScriptBlock {
+  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
+  $env:PORT = '3002'
+  npm.cmd start
+}
+
+try {
+  $frontendResponse = Wait-FrontendReady -Url $readinessUrl
+  Capture-ChromeAppScreenshot -Url $readinessUrl -OutputPath $quarterScreenshotPath
+  Capture-ChromeAppScreenshot -Url $exportReadyUrl -OutputPath $finishNowScreenshotPath
 
   "READINESS_URL=$readinessUrl"
+  "EXPORT_READY_URL=$exportReadyUrl"
   "FRONTEND_STATUS=$($frontendResponse.StatusCode)"
-  "SCREENSHOT=$screenshotPath"
+  "SCREENSHOT_QUARTER=$quarterScreenshotPath"
+  "SCREENSHOT_EXPORT_READY=$finishNowScreenshotPath"
   'FRONTEND_LOG_START'
   Receive-Job $frontendJob -Keep -ErrorAction SilentlyContinue 2>&1
   'FRONTEND_LOG_END'
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
@@ -1509,2 +1509,3 @@
     report: quarterReadiness || READINESS_DEMO_REPORT,
+    fallbackIssues: inboxItems,
     resolvedIssueIds: resolvedQuarterIssueIds,
apply_patch(auto_approved=true) exited 0 in 785ms:
Success. Updated the following files:
M C:\Users\edebe\eds\bizPA\frontend\src\App.jsx
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..af21307c83db35a88b75e7d7aae29ff31ac7f730
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,26 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    fallbackIssues: inboxItems,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1532,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1547,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1633,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2104,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2317,25 +2563,236 @@
   );
 
   const renderInbox = () => (
-    <div>
-      <h4>Finish Now Queue</h4>
-      <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
-      <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
-            <div>
-              <b>{item.merchant || 'Unknown'}</b>
-              <br />
-              <small>
-                {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
-              </small>
-              <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
-            </div>
+    <div className="inbox-mobile-shell">
+      <section className="inbox-mobile-hero">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2">
+          <div className="inbox-kicker">
+            <Bell size={14} />
+            Mobile-first exception queue
+          </div>
+          <div className="status-pill">{filteredInboxCards.length} visible</div>
+        </div>
+        <h4>Finish now</h4>
+        <p>
+          Clear the blockers that stop the quarterly export: category, business or personal, split treatment, and duplicate resolution.
+        </p>
+        <div className="inbox-mobile-stats">
+          <div className="inbox-mobile-stat">
+            <small>Queue</small>
+            <strong>{inboxCards.length}</strong>
+            <small>{activeQuarterReport.quarter_reference || READINESS_DEMO_REPORT.quarter_reference}</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Critical</small>
+            <strong>{highSeverityCount}</strong>
+            <small>High-severity blockers</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Ready now</small>
+            <strong>{readyToExportCount}</strong>
+            <small>Transactions already export-safe</small>
+          </div>
+        </div>
+        <div className="hero-actions">
+          <button className="hero-action-main" onClick={() => setCurrentTab('quarter')}>
+            <ArrowRight size={16} />
+            Review quarter readiness
+          </button>
+          <button className="hero-action-secondary" onClick={undoLastInboxAction} disabled={inboxBusyId === 'undo-last'}>
+            {inboxBusyId === 'undo-last' ? <RefreshCcw size={16} className="spinner" /> : <ArchiveRestore size={16} />}
+            Undo last action
+          </button>
+        </div>
+      </section>
+
+      {inboxMessage && (
+        <div className={`readiness-panel inbox-message ${inboxMessage.type}`}>
+          {inboxMessage.text}
+        </div>
+      )}
+
+      <section className="readiness-panel">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-3">
+          <div>
+            <h5 style={{ marginBottom: 4 }}>Queue filters</h5>
+            <small className="small-muted">Keep the mobile pass focused on one blocker type at a time.</small>
           </div>
-        ))}
-      </div>
+          <div className="status-pill">{activeQuarterReport.readiness_pct ?? READINESS_DEMO_REPORT.readiness_pct}% ready</div>
+        </div>
+        <div className="inbox-filter-row">
+          {INBOX_FILTERS.map((filter) => {
+            const count = filter.key === 'all'
+              ? inboxCards.length
+              : inboxCards.filter((card) => card.issue_types.includes(filter.key)).length;
+            return (
+              <button
+                key={filter.key}
+                type="button"
+                className={`inbox-filter-chip ${inboxFilter === filter.key ? 'active' : ''}`}
+                onClick={() => setInboxFilter(filter.key)}
+              >
+                <span>{filter.label}</span>
+                <small>{count}</small>
+              </button>
+            );
+          })}
+        </div>
+      </section>
+
+      {filteredInboxCards.length === 0 ? (
+        <div className="inbox-empty">
+          No blockers match this filter. Reset to All blockers or return to the quarter screen to confirm readiness.
+        </div>
+      ) : (
+        <div className="inbox-card-list">
+          {filteredInboxCards.map((card) => {
+            const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+            const saving = inboxBusyId === card.id;
+            const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+            return (
+              <section key={card.id} className="inbox-action-card">
+                <div className="inbox-card-top">
+                  <div>
+                    <strong>{card.merchant}</strong>
+                    <div className="inbox-card-subline">
+                      {card.txn_date ? String(card.txn_date).slice(0, 10) : 'Date pending'} · {formatIssueAmount(card.amount, card.direction)}
+                    </div>
+                  </div>
+                  <span className={`inbox-issue-chip ${card.severity}`}>
+                    <TriangleAlert size={14} />
+                    {INBOX_REASON_LABELS[card.blocker_reason] || card.blocker_reason.replace(/_/g, ' ')}
+                  </span>
+                </div>
+
+                <div className="inbox-chip-row">
+                  {card.issue_labels.map((label) => (
+                    <span key={`${card.id}-${label}`} className={`inbox-issue-chip ${card.severity}`}>
+                      {label}
+                    </span>
+                  ))}
+                  {card.suggested_category?.category_name && (
+                    <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
+                      <CheckCircle2 size={14} />
+                      Suggested: {card.suggested_category.category_name}
+                    </button>
+                  )}
+                </div>
+
+                <p className="inbox-explainer">{card.explanation || 'Resolve the blocking fields to clear this transaction from the queue.'}</p>
+
+                {(card.supports_category || card.supports_business_personal || card.supports_split) && (
+                  <div className="inbox-action-grid">
+                    <div className="inbox-field">
+                      <label>Category code</label>
+                      <input
+                        value={draft.category_code}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_code', event.target.value)}
+                        placeholder="MATERIALS"
+                      />
+                    </div>
+                    <div className="inbox-field">
+                      <label>Category label</label>
+                      <input
+                        value={draft.category_name}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_name', event.target.value)}
+                        placeholder="Materials"
+                      />
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_business_personal || card.supports_split) && (
+                  <div>
+                    <div className="inbox-field">
+                      <label>Business treatment</label>
+                    </div>
+                    <div className="inbox-toggle-row">
+                      {[
+                        { value: 'BUSINESS', label: 'Business' },
+                        { value: 'PERSONAL', label: 'Personal' },
+                        { value: 'SPLIT', label: 'Split' },
+                      ].map((option) => (
+                        <button
+                          key={option.value}
+                          type="button"
+                          className={`inbox-toggle ${draft.business_personal === option.value ? 'active' : ''}`}
+                          onClick={() => setInboxDraftField(card.id, 'business_personal', option.value)}
+                        >
+                          {option.label}
+                        </button>
+                      ))}
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_split || isSplit) && (
+                  <div className="inbox-field">
+                    <label>Business split %</label>
+                    <input
+                      type="number"
+                      min="0"
+                      max="100"
+                      value={draft.split_business_pct}
+                      onChange={(event) => setInboxDraftField(card.id, 'split_business_pct', event.target.value)}
+                      placeholder="80"
+                    />
+                  </div>
+                )}
+
+                {card.supports_duplicate && (
+                  <>
+                    <div>
+                      <div className="inbox-field">
+                        <label>Duplicate action</label>
+                      </div>
+                      <div className="inbox-duplicate-row">
+                        {[
+                          { value: 'dismiss', label: 'Dismiss duplicate' },
+                          { value: 'merge', label: 'Merge to original' },
+                        ].map((option) => (
+                          <button
+                            key={option.value}
+                            type="button"
+                            className={`inbox-toggle ${draft.duplicate_action === option.value ? 'active' : ''}`}
+                            onClick={() => setInboxDraftField(card.id, 'duplicate_action', option.value)}
+                          >
+                            {option.label}
+                          </button>
+                        ))}
+                      </div>
+                    </div>
+                    {draft.duplicate_action === 'merge' && (
+                      <div className="inbox-field">
+                        <label>Original transaction id</label>
+                        <input
+                          value={draft.duplicate_of_txn_id}
+                          onChange={(event) => setInboxDraftField(card.id, 'duplicate_of_txn_id', event.target.value)}
+                          placeholder="txn-9001"
+                        />
+                      </div>
+                    )}
+                  </>
+                )}
+
+                <div className="inbox-footer-row">
+                  <div className="inbox-inline-status">
+                    Route: {card.resolution_target?.route || `/api/v1/inbox/${card.id}`}
+                  </div>
+                  <div className="inbox-footer-actions">
+                    <button className="secondary-action" onClick={clearInboxMessage} disabled={saving}>
+                      Clear banner
+                    </button>
+                    <button className="primary-action" onClick={() => submitInboxAction(card)} disabled={saving}>
+                      {saving ? <RefreshCcw size={16} className="spinner" /> : <CheckCircle2 size={16} />}
+                      Save and clear blocker
+                    </button>
+                  </div>
+                </div>
+              </section>
+            );
+          })}
+        </div>
+      )}
     </div>
   );
 
@@ -2510,11 +2967,11 @@
           <section className="readiness-panel">
             <h5 style={{ marginBottom: 12 }}>Queue Backlog</h5>
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
-                <div key={item.id} className="control-row">
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
+                <div key={item.id || item.affected_entity_id} className="control-row">
                   <div>
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
                   <div className="text-end">
                     <div className="small-muted">{item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date}</div>
@@ -2522,7 +2979,7 @@
                   </div>
                 </div>
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
               )}
             </div>
@@ -2668,7 +3125,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2697,7 +3154,7 @@
   };
 
   const createSnapshotVersion = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     const status = snapshotWorkspace?.snapshot_status;
 
     if (!status?.can_create_snapshot_version) {
@@ -2719,7 +3176,7 @@
         description: `Quarter snapshot generated from Quarter workspace`,
         metadata: {
           source_type: 'manual',
-          readiness_pct: quarterReadiness?.readiness_pct ?? null
+          readiness_pct: activeQuarterReport?.readiness_pct ?? null
         }
       });
       await refreshQuarterWorkspace();
@@ -2734,7 +3191,7 @@
   };
 
   const closeQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(closeReason || '').trim()) {
       setQuarterActionError('Close reason is required.');
       setQuarterActionMessage('');
@@ -2785,7 +3242,7 @@
   };
 
   const reopenQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(reopenReason || '').trim()) {
       setQuarterActionError('Reopen reason is required.');
       setQuarterActionMessage('');
@@ -2844,9 +3301,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3341,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3353,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3364,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3378,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..669173cd6176ee9ee5c86c5895ea49cb9524ebc4
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,155 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, fallbackIssues = [], resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const issueSource = Array.isArray(report.issue_list) && report.issue_list.length > 0
+    ? report.issue_list
+    : fallbackIssues;
+  const allIssues = sortIssues(Array.isArray(issueSource) ? issueSource : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/bizPA/frontend/src/quarterReadiness.test.js b/bizPA/frontend/src/quarterReadiness.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..0b810191a89d8b82091bf5de1408afcc23c14e65
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.test.js
@@ -0,0 +1,93 @@
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
+
+const reportFixture = {
+  quarter_reference: 'Q1-2026',
+  period_start: '2026-01-01',
+  period_end: '2026-03-31',
+  total_txns_in_period: 18,
+  readiness_pct: 78,
+  can_export: false,
+  issue_list: [
+    {
+      issue_type: 'missing_category',
+      affected_entity_id: 'txn-9101',
+      severity: 'high',
+      weight: 35,
+      explanation: 'Category missing for Fuel Stop.',
+      txn_date: '2026-02-04',
+      merchant: 'Fuel Stop',
+      amount: 64.18,
+      direction: 'out',
+      resolution_target: {
+        label: 'Classify transaction',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'missing_business_personal',
+      affected_entity_id: 'txn-9103',
+      severity: 'high',
+      weight: 30,
+      explanation: 'Business/personal decision missing.',
+      txn_date: '2026-02-15',
+      merchant: 'Mobile Contract',
+      amount: 42.5,
+      direction: 'out',
+      resolution_target: {
+        label: 'Set business or personal',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'unresolved_duplicate',
+      affected_entity_id: 'txn-9104',
+      severity: 'medium',
+      weight: 15,
+      explanation: 'Duplicate not resolved.',
+      txn_date: '2026-03-02',
+      merchant: 'Client Payment',
+      amount: 820,
+      direction: 'in',
+      resolution_target: {
+        label: 'Resolve duplicate',
+        workflow: '/inbox/finish-now',
+      },
+    },
+  ],
+};
+
+describe('quarter readiness helpers', () => {
+  test('keeps blockers ordered and updates headline while blockers remain', () => {
+    const state = buildQuarterUiState({ report: reportFixture, resolvedIssueIds: ['txn-9104'] });
+
+    expect(state.itemsLeft).toBe(2);
+    expect(state.exportEnabled).toBe(false);
+    expect(state.report.readiness_pct).toBeGreaterThan(78);
+    expect(state.headline).toContain('items left');
+    expect(state.activeIssues.map((issue) => issue.affected_entity_id)).toEqual(['txn-9101', 'txn-9103']);
+    expect(state.report.explanation_lines[0]).toContain('Readiness is');
+  });
+
+  test('unlocks export immediately when every blocker is resolved', () => {
+    const state = buildQuarterUiState({
+      report: reportFixture,
+      resolvedIssueIds: ['txn-9101', 'txn-9103', 'txn-9104'],
+    });
+
+    expect(state.itemsLeft).toBe(0);
+    expect(state.exportEnabled).toBe(true);
+    expect(state.report.readiness_pct).toBe(100);
+    expect(state.report.can_export).toBe(true);
+    expect(state.report.issue_summary).toEqual([]);
+  });
+
+  test('supports demo query params that resolve all blockers', () => {
+    const params = new URLSearchParams('resolvedDemoIssues=all');
+    const resolvedIds = deriveResolvedIssueIdsFromSearchParams(params, reportFixture.issue_list);
+
+    expect(resolvedIds).toEqual(['txn-9101', 'txn-9103', 'txn-9104']);
+  });
+});
diff --git a/bizPA/start_bizpa_tax_readiness_ui.ps1 b/bizPA/start_bizpa_tax_readiness_ui.ps1
index 1fc4bef8cd172c31fdc3db52d1acc9d4775c5485..7216e44eac386fe436a9af8e9b417cd2d61e37fe
--- a/bizPA/start_bizpa_tax_readiness_ui.ps1
+++ b/bizPA/start_bizpa_tax_readiness_ui.ps1
@@ -4,6 +4,7 @@
 $frontendPath = Join-Path $repoRoot 'frontend'
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
 
 Write-Host 'Starting bizPA tax readiness UI...' -ForegroundColor Cyan
 
@@ -14,7 +15,10 @@
 Write-Host 'Tax readiness dashboard URL:' -ForegroundColor Green
 Write-Host "  $readinessUrl" -ForegroundColor Yellow
 Write-Host ''
+Write-Host 'Export-ready verification URL:' -ForegroundColor Green
+Write-Host "  $exportReadyUrl" -ForegroundColor Yellow
+Write-Host ''
 Write-Host 'Base frontend URL:' -ForegroundColor Green
 Write-Host "  $frontendUrl" -ForegroundColor Yellow
 Write-Host ''
-Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI can be reviewed without backend setup.' -ForegroundColor Cyan
+Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI and zero-blocker export-ready state can be reviewed without backend setup.' -ForegroundColor Cyan
diff --git a/bizPA/tax_readiness_ui_smoke.ps1 b/bizPA/tax_readiness_ui_smoke.ps1
index 56d67892a51b22572503dec560d17123b370c2b7..269c2a3221adfe9095130883c89fd76428a65989
--- a/bizPA/tax_readiness_ui_smoke.ps1
+++ b/bizPA/tax_readiness_ui_smoke.ps1
@@ -28,36 +28,38 @@
 
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
-$screenshotPath = 'C:\Users\edebe\eds\workstream\verification\20260311_201500_bizpa_tax_readiness_ui.png'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
+$verificationRoot = 'C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification'
+$quarterScreenshotPath = Join-Path $verificationRoot 'quarter_readiness_screen.png'
+$finishNowScreenshotPath = Join-Path $verificationRoot 'finish_now_queue_zero_blockers.png'
 $chromeProfilePath = 'C:\Users\edebe\eds\workstream\verification\chrome_capture_profile_tax_readiness'
-
-New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
+$chromePath = 'C:\Program Files\Google\Chrome\Application\chrome.exe'
 
-$frontendJob = Start-Job -ScriptBlock {
-  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
-  $env:PORT = '3002'
-  npm.cmd start
-}
+function Wait-FrontendReady {
+  param (
+    [string]$Url
+  )
 
-try {
-  $frontendResponse = $null
-  $frontendReady = $false
   for ($i = 0; $i -lt 45; $i++) {
     Start-Sleep -Seconds 2
     try {
-      $frontendResponse = Invoke-WebRequest -UseBasicParsing $readinessUrl
-      if ($frontendResponse.StatusCode -eq 200) {
-        $frontendReady = $true
-        break
+      $response = Invoke-WebRequest -UseBasicParsing $Url
+      if ($response.StatusCode -eq 200) {
+        return $response
       }
     } catch {
     }
   }
 
-  if (-not $frontendReady) {
-    throw "Frontend did not become ready on $readinessUrl"
-  }
+  throw "Frontend did not become ready on $Url"
+}
 
+function Capture-ChromeAppScreenshot {
+  param (
+    [string]$Url,
+    [string]$OutputPath
+  )
+
   $chromeArgs = @(
     "--user-data-dir=$chromeProfilePath",
     '--new-window',
@@ -68,9 +70,9 @@
     '--disable-breakpad',
     '--disable-crash-reporter',
     '--noerrdialogs',
-    "--app=$readinessUrl"
+    "--app=$Url"
   )
-  Start-Process 'C:\Program Files\Google\Chrome\Application\chrome.exe' -ArgumentList $chromeArgs | Out-Null
+  Start-Process $chromePath -ArgumentList $chromeArgs | Out-Null
 
   $chromeWindow = $null
   for ($i = 0; $i -lt 20; $i++) {
@@ -96,7 +98,7 @@
   }
 
   if (-not $chromeWindow) {
-    throw 'Chrome readiness window did not expose a main window handle for screenshot capture.'
+    throw "Chrome window did not expose a main window handle for $Url"
   }
 
   [WindowCaptureNative]::ShowWindow($chromeWindow.MainWindowHandle, 3) | Out-Null
@@ -110,17 +112,34 @@
   $bitmap = New-Object System.Drawing.Bitmap $width, $height
   $graphics = [System.Drawing.Graphics]::FromImage($bitmap)
   $graphics.CopyFromScreen($rect.Left, $rect.Top, 0, 0, $bitmap.Size)
-  $bitmap.Save($screenshotPath, [System.Drawing.Imaging.ImageFormat]::Png)
+  $bitmap.Save($OutputPath, [System.Drawing.Imaging.ImageFormat]::Png)
   $graphics.Dispose()
   $bitmap.Dispose()
 
   Get-Process chrome -ErrorAction SilentlyContinue |
     Where-Object { $_.StartTime -ge $chromeWindow.StartTime.AddSeconds(-2) } |
     Stop-Process -Force -ErrorAction SilentlyContinue
+}
+
+New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
+New-Item -ItemType Directory -Force -Path $verificationRoot | Out-Null
+
+$frontendJob = Start-Job -ScriptBlock {
+  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
+  $env:PORT = '3002'
+  npm.cmd start
+}
+
+try {
+  $frontendResponse = Wait-FrontendReady -Url $readinessUrl
+  Capture-ChromeAppScreenshot -Url $readinessUrl -OutputPath $quarterScreenshotPath
+  Capture-ChromeAppScreenshot -Url $exportReadyUrl -OutputPath $finishNowScreenshotPath
 
   "READINESS_URL=$readinessUrl"
+  "EXPORT_READY_URL=$exportReadyUrl"
   "FRONTEND_STATUS=$($frontendResponse.StatusCode)"
-  "SCREENSHOT=$screenshotPath"
+  "SCREENSHOT_QUARTER=$quarterScreenshotPath"
+  "SCREENSHOT_EXPORT_READY=$finishNowScreenshotPath"
   'FRONTEND_LOG_START'
   Receive-Job $frontendJob -Keep -ErrorAction SilentlyContinue 2>&1
   'FRONTEND_LOG_END'
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..af21307c83db35a88b75e7d7aae29ff31ac7f730
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,26 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    fallbackIssues: inboxItems,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1532,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1547,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1633,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2104,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2317,25 +2563,236 @@
   );
 
   const renderInbox = () => (
-    <div>
-      <h4>Finish Now Queue</h4>
-      <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
-      <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
-            <div>
-              <b>{item.merchant || 'Unknown'}</b>
-              <br />
-              <small>
-                {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
-              </small>
-              <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
-            </div>
+    <div className="inbox-mobile-shell">
+      <section className="inbox-mobile-hero">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2">
+          <div className="inbox-kicker">
+            <Bell size={14} />
+            Mobile-first exception queue
+          </div>
+          <div className="status-pill">{filteredInboxCards.length} visible</div>
+        </div>
+        <h4>Finish now</h4>
+        <p>
+          Clear the blockers that stop the quarterly export: category, business or personal, split treatment, and duplicate resolution.
+        </p>
+        <div className="inbox-mobile-stats">
+          <div className="inbox-mobile-stat">
+            <small>Queue</small>
+            <strong>{inboxCards.length}</strong>
+            <small>{activeQuarterReport.quarter_reference || READINESS_DEMO_REPORT.quarter_reference}</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Critical</small>
+            <strong>{highSeverityCount}</strong>
+            <small>High-severity blockers</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Ready now</small>
+            <strong>{readyToExportCount}</strong>
+            <small>Transactions already export-safe</small>
+          </div>
+        </div>
+        <div className="hero-actions">
+          <button className="hero-action-main" onClick={() => setCurrentTab('quarter')}>
+            <ArrowRight size={16} />
+            Review quarter readiness
+          </button>
+          <button className="hero-action-secondary" onClick={undoLastInboxAction} disabled={inboxBusyId === 'undo-last'}>
+            {inboxBusyId === 'undo-last' ? <RefreshCcw size={16} className="spinner" /> : <ArchiveRestore size={16} />}
+            Undo last action
+          </button>
+        </div>
+      </section>
+
+      {inboxMessage && (
+        <div className={`readiness-panel inbox-message ${inboxMessage.type}`}>
+          {inboxMessage.text}
+        </div>
+      )}
+
+      <section className="readiness-panel">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-3">
+          <div>
+            <h5 style={{ marginBottom: 4 }}>Queue filters</h5>
+            <small className="small-muted">Keep the mobile pass focused on one blocker type at a time.</small>
           </div>
-        ))}
-      </div>
+          <div className="status-pill">{activeQuarterReport.readiness_pct ?? READINESS_DEMO_REPORT.readiness_pct}% ready</div>
+        </div>
+        <div className="inbox-filter-row">
+          {INBOX_FILTERS.map((filter) => {
+            const count = filter.key === 'all'
+              ? inboxCards.length
+              : inboxCards.filter((card) => card.issue_types.includes(filter.key)).length;
+            return (
+              <button
+                key={filter.key}
+                type="button"
+                className={`inbox-filter-chip ${inboxFilter === filter.key ? 'active' : ''}`}
+                onClick={() => setInboxFilter(filter.key)}
+              >
+                <span>{filter.label}</span>
+                <small>{count}</small>
+              </button>
+            );
+          })}
+        </div>
+      </section>
+
+      {filteredInboxCards.length === 0 ? (
+        <div className="inbox-empty">
+          No blockers match this filter. Reset to All blockers or return to the quarter screen to confirm readiness.
+        </div>
+      ) : (
+        <div className="inbox-card-list">
+          {filteredInboxCards.map((card) => {
+            const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+            const saving = inboxBusyId === card.id;
+            const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+            return (
+              <section key={card.id} className="inbox-action-card">
+                <div className="inbox-card-top">
+                  <div>
+                    <strong>{card.merchant}</strong>
+                    <div className="inbox-card-subline">
+                      {card.txn_date ? String(card.txn_date).slice(0, 10) : 'Date pending'} · {formatIssueAmount(card.amount, card.direction)}
+                    </div>
+                  </div>
+                  <span className={`inbox-issue-chip ${card.severity}`}>
+                    <TriangleAlert size={14} />
+                    {INBOX_REASON_LABELS[card.blocker_reason] || card.blocker_reason.replace(/_/g, ' ')}
+                  </span>
+                </div>
+
+                <div className="inbox-chip-row">
+                  {card.issue_labels.map((label) => (
+                    <span key={`${card.id}-${label}`} className={`inbox-issue-chip ${card.severity}`}>
+                      {label}
+                    </span>
+                  ))}
+                  {card.suggested_category?.category_name && (
+                    <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
+                      <CheckCircle2 size={14} />
+                      Suggested: {card.suggested_category.category_name}
+                    </button>
+                  )}
+                </div>
+
+                <p className="inbox-explainer">{card.explanation || 'Resolve the blocking fields to clear this transaction from the queue.'}</p>
+
+                {(card.supports_category || card.supports_business_personal || card.supports_split) && (
+                  <div className="inbox-action-grid">
+                    <div className="inbox-field">
+                      <label>Category code</label>
+                      <input
+                        value={draft.category_code}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_code', event.target.value)}
+                        placeholder="MATERIALS"
+                      />
+                    </div>
+                    <div className="inbox-field">
+                      <label>Category label</label>
+                      <input
+                        value={draft.category_name}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_name', event.target.value)}
+                        placeholder="Materials"
+                      />
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_business_personal || card.supports_split) && (
+                  <div>
+                    <div className="inbox-field">
+                      <label>Business treatment</label>
+                    </div>
+                    <div className="inbox-toggle-row">
+                      {[
+                        { value: 'BUSINESS', label: 'Business' },
+                        { value: 'PERSONAL', label: 'Personal' },
+                        { value: 'SPLIT', label: 'Split' },
+                      ].map((option) => (
+                        <button
+                          key={option.value}
+                          type="button"
+                          className={`inbox-toggle ${draft.business_personal === option.value ? 'active' : ''}`}
+                          onClick={() => setInboxDraftField(card.id, 'business_personal', option.value)}
+                        >
+                          {option.label}
+                        </button>
+                      ))}
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_split || isSplit) && (
+                  <div className="inbox-field">
+                    <label>Business split %</label>
+                    <input
+                      type="number"
+                      min="0"
+                      max="100"
+                      value={draft.split_business_pct}
+                      onChange={(event) => setInboxDraftField(card.id, 'split_business_pct', event.target.value)}
+                      placeholder="80"
+                    />
+                  </div>
+                )}
+
+                {card.supports_duplicate && (
+                  <>
+                    <div>
+                      <div className="inbox-field">
+                        <label>Duplicate action</label>
+                      </div>
+                      <div className="inbox-duplicate-row">
+                        {[
+                          { value: 'dismiss', label: 'Dismiss duplicate' },
+                          { value: 'merge', label: 'Merge to original' },
+                        ].map((option) => (
+                          <button
+                            key={option.value}
+                            type="button"
+                            className={`inbox-toggle ${draft.duplicate_action === option.value ? 'active' : ''}`}
+                            onClick={() => setInboxDraftField(card.id, 'duplicate_action', option.value)}
+                          >
+                            {option.label}
+                          </button>
+                        ))}
+                      </div>
+                    </div>
+                    {draft.duplicate_action === 'merge' && (
+                      <div className="inbox-field">
+                        <label>Original transaction id</label>
+                        <input
+                          value={draft.duplicate_of_txn_id}
+                          onChange={(event) => setInboxDraftField(card.id, 'duplicate_of_txn_id', event.target.value)}
+                          placeholder="txn-9001"
+                        />
+                      </div>
+                    )}
+                  </>
+                )}
+
+                <div className="inbox-footer-row">
+                  <div className="inbox-inline-status">
+                    Route: {card.resolution_target?.route || `/api/v1/inbox/${card.id}`}
+                  </div>
+                  <div className="inbox-footer-actions">
+                    <button className="secondary-action" onClick={clearInboxMessage} disabled={saving}>
+                      Clear banner
+                    </button>
+                    <button className="primary-action" onClick={() => submitInboxAction(card)} disabled={saving}>
+                      {saving ? <RefreshCcw size={16} className="spinner" /> : <CheckCircle2 size={16} />}
+                      Save and clear blocker
+                    </button>
+                  </div>
+                </div>
+              </section>
+            );
+          })}
+        </div>
+      )}
     </div>
   );
 
@@ -2510,11 +2967,11 @@
           <section className="readiness-panel">
             <h5 style={{ marginBottom: 12 }}>Queue Backlog</h5>
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
-                <div key={item.id} className="control-row">
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
+                <div key={item.id || item.affected_entity_id} className="control-row">
                   <div>
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
                   <div className="text-end">
                     <div className="small-muted">{item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date}</div>
@@ -2522,7 +2979,7 @@
                   </div>
                 </div>
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
               )}
             </div>
@@ -2668,7 +3125,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2697,7 +3154,7 @@
   };
 
   const createSnapshotVersion = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     const status = snapshotWorkspace?.snapshot_status;
 
     if (!status?.can_create_snapshot_version) {
@@ -2719,7 +3176,7 @@
         description: `Quarter snapshot generated from Quarter workspace`,
         metadata: {
           source_type: 'manual',
-          readiness_pct: quarterReadiness?.readiness_pct ?? null
+          readiness_pct: activeQuarterReport?.readiness_pct ?? null
         }
       });
       await refreshQuarterWorkspace();
@@ -2734,7 +3191,7 @@
   };
 
   const closeQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(closeReason || '').trim()) {
       setQuarterActionError('Close reason is required.');
       setQuarterActionMessage('');
@@ -2785,7 +3242,7 @@
   };
 
   const reopenQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(reopenReason || '').trim()) {
       setQuarterActionError('Reopen reason is required.');
       setQuarterActionMessage('');
@@ -2844,9 +3301,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3341,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3353,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3364,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3378,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..669173cd6176ee9ee5c86c5895ea49cb9524ebc4
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,155 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, fallbackIssues = [], resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const issueSource = Array.isArray(report.issue_list) && report.issue_list.length > 0
+    ? report.issue_list
+    : fallbackIssues;
+  const allIssues = sortIssues(Array.isArray(issueSource) ? issueSource : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/bizPA/frontend/src/quarterReadiness.test.js b/bizPA/frontend/src/quarterReadiness.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..0b810191a89d8b82091bf5de1408afcc23c14e65
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.test.js
@@ -0,0 +1,93 @@
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
+
+const reportFixture = {
+  quarter_reference: 'Q1-2026',
+  period_start: '2026-01-01',
+  period_end: '2026-03-31',
+  total_txns_in_period: 18,
+  readiness_pct: 78,
+  can_export: false,
+  issue_list: [
+    {
+      issue_type: 'missing_category',
+      affected_entity_id: 'txn-9101',
+      severity: 'high',
+      weight: 35,
+      explanation: 'Category missing for Fuel Stop.',
+      txn_date: '2026-02-04',
+      merchant: 'Fuel Stop',
+      amount: 64.18,
+      direction: 'out',
+      resolution_target: {
+        label: 'Classify transaction',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'missing_business_personal',
+      affected_entity_id: 'txn-9103',
+      severity: 'high',
+      weight: 30,
+      explanation: 'Business/personal decision missing.',
+      txn_date: '2026-02-15',
+      merchant: 'Mobile Contract',
+      amount: 42.5,
+      direction: 'out',
+      resolution_target: {
+        label: 'Set business or personal',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'unresolved_duplicate',
+      affected_entity_id: 'txn-9104',
+      severity: 'medium',
+      weight: 15,
+      explanation: 'Duplicate not resolved.',
+      txn_date: '2026-03-02',
+      merchant: 'Client Payment',
+      amount: 820,
+      direction: 'in',
+      resolution_target: {
+        label: 'Resolve duplicate',
+        workflow: '/inbox/finish-now',
+      },
+    },
+  ],
+};
+
+describe('quarter readiness helpers', () => {
+  test('keeps blockers ordered and updates headline while blockers remain', () => {
+    const state = buildQuarterUiState({ report: reportFixture, resolvedIssueIds: ['txn-9104'] });
+
+    expect(state.itemsLeft).toBe(2);
+    expect(state.exportEnabled).toBe(false);
+    expect(state.report.readiness_pct).toBeGreaterThan(78);
+    expect(state.headline).toContain('items left');
+    expect(state.activeIssues.map((issue) => issue.affected_entity_id)).toEqual(['txn-9101', 'txn-9103']);
+    expect(state.report.explanation_lines[0]).toContain('Readiness is');
+  });
+
+  test('unlocks export immediately when every blocker is resolved', () => {
+    const state = buildQuarterUiState({
+      report: reportFixture,
+      resolvedIssueIds: ['txn-9101', 'txn-9103', 'txn-9104'],
+    });
+
+    expect(state.itemsLeft).toBe(0);
+    expect(state.exportEnabled).toBe(true);
+    expect(state.report.readiness_pct).toBe(100);
+    expect(state.report.can_export).toBe(true);
+    expect(state.report.issue_summary).toEqual([]);
+  });
+
+  test('supports demo query params that resolve all blockers', () => {
+    const params = new URLSearchParams('resolvedDemoIssues=all');
+    const resolvedIds = deriveResolvedIssueIdsFromSearchParams(params, reportFixture.issue_list);
+
+    expect(resolvedIds).toEqual(['txn-9101', 'txn-9103', 'txn-9104']);
+  });
+});
diff --git a/bizPA/start_bizpa_tax_readiness_ui.ps1 b/bizPA/start_bizpa_tax_readiness_ui.ps1
index 1fc4bef8cd172c31fdc3db52d1acc9d4775c5485..7216e44eac386fe436a9af8e9b417cd2d61e37fe
--- a/bizPA/start_bizpa_tax_readiness_ui.ps1
+++ b/bizPA/start_bizpa_tax_readiness_ui.ps1
@@ -4,6 +4,7 @@
 $frontendPath = Join-Path $repoRoot 'frontend'
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
 
 Write-Host 'Starting bizPA tax readiness UI...' -ForegroundColor Cyan
 
@@ -14,7 +15,10 @@
 Write-Host 'Tax readiness dashboard URL:' -ForegroundColor Green
 Write-Host "  $readinessUrl" -ForegroundColor Yellow
 Write-Host ''
+Write-Host 'Export-ready verification URL:' -ForegroundColor Green
+Write-Host "  $exportReadyUrl" -ForegroundColor Yellow
+Write-Host ''
 Write-Host 'Base frontend URL:' -ForegroundColor Green
 Write-Host "  $frontendUrl" -ForegroundColor Yellow
 Write-Host ''
-Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI can be reviewed without backend setup.' -ForegroundColor Cyan
+Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI and zero-blocker export-ready state can be reviewed without backend setup.' -ForegroundColor Cyan
diff --git a/bizPA/tax_readiness_ui_smoke.ps1 b/bizPA/tax_readiness_ui_smoke.ps1
index 56d67892a51b22572503dec560d17123b370c2b7..269c2a3221adfe9095130883c89fd76428a65989
--- a/bizPA/tax_readiness_ui_smoke.ps1
+++ b/bizPA/tax_readiness_ui_smoke.ps1
@@ -28,36 +28,38 @@
 
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
-$screenshotPath = 'C:\Users\edebe\eds\workstream\verification\20260311_201500_bizpa_tax_readiness_ui.png'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
+$verificationRoot = 'C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification'
+$quarterScreenshotPath = Join-Path $verificationRoot 'quarter_readiness_screen.png'
+$finishNowScreenshotPath = Join-Path $verificationRoot 'finish_now_queue_zero_blockers.png'
 $chromeProfilePath = 'C:\Users\edebe\eds\workstream\verification\chrome_capture_profile_tax_readiness'
-
-New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
+$chromePath = 'C:\Program Files\Google\Chrome\Application\chrome.exe'
 
-$frontendJob = Start-Job -ScriptBlock {
-  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
-  $env:PORT = '3002'
-  npm.cmd start
-}
+function Wait-FrontendReady {
+  param (
+    [string]$Url
+  )
 
-try {
-  $frontendResponse = $null
-  $frontendReady = $false
   for ($i = 0; $i -lt 45; $i++) {
     Start-Sleep -Seconds 2
     try {
-      $frontendResponse = Invoke-WebRequest -UseBasicParsing $readinessUrl
-      if ($frontendResponse.StatusCode -eq 200) {
-        $frontendReady = $true
-        break
+      $response = Invoke-WebRequest -UseBasicParsing $Url
+      if ($response.StatusCode -eq 200) {
+        return $response
       }
     } catch {
     }
   }
 
-  if (-not $frontendReady) {
-    throw "Frontend did not become ready on $readinessUrl"
-  }
+  throw "Frontend did not become ready on $Url"
+}
 
+function Capture-ChromeAppScreenshot {
+  param (
+    [string]$Url,
+    [string]$OutputPath
+  )
+
   $chromeArgs = @(
     "--user-data-dir=$chromeProfilePath",
     '--new-window',
@@ -68,9 +70,9 @@
     '--disable-breakpad',
     '--disable-crash-reporter',
     '--noerrdialogs',
-    "--app=$readinessUrl"
+    "--app=$Url"
   )
-  Start-Process 'C:\Program Files\Google\Chrome\Application\chrome.exe' -ArgumentList $chromeArgs | Out-Null
+  Start-Process $chromePath -ArgumentList $chromeArgs | Out-Null
 
   $chromeWindow = $null
   for ($i = 0; $i -lt 20; $i++) {
@@ -96,7 +98,7 @@
   }
 
   if (-not $chromeWindow) {
-    throw 'Chrome readiness window did not expose a main window handle for screenshot capture.'
+    throw "Chrome window did not expose a main window handle for $Url"
   }
 
   [WindowCaptureNative]::ShowWindow($chromeWindow.MainWindowHandle, 3) | Out-Null
@@ -110,17 +112,34 @@
   $bitmap = New-Object System.Drawing.Bitmap $width, $height
   $graphics = [System.Drawing.Graphics]::FromImage($bitmap)
   $graphics.CopyFromScreen($rect.Left, $rect.Top, 0, 0, $bitmap.Size)
-  $bitmap.Save($screenshotPath, [System.Drawing.Imaging.ImageFormat]::Png)
+  $bitmap.Save($OutputPath, [System.Drawing.Imaging.ImageFormat]::Png)
   $graphics.Dispose()
   $bitmap.Dispose()
 
   Get-Process chrome -ErrorAction SilentlyContinue |
     Where-Object { $_.StartTime -ge $chromeWindow.StartTime.AddSeconds(-2) } |
     Stop-Process -Force -ErrorAction SilentlyContinue
+}
+
+New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
+New-Item -ItemType Directory -Force -Path $verificationRoot | Out-Null
+
+$frontendJob = Start-Job -ScriptBlock {
+  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
+  $env:PORT = '3002'
+  npm.cmd start
+}
+
+try {
+  $frontendResponse = Wait-FrontendReady -Url $readinessUrl
+  Capture-ChromeAppScreenshot -Url $readinessUrl -OutputPath $quarterScreenshotPath
+  Capture-ChromeAppScreenshot -Url $exportReadyUrl -OutputPath $finishNowScreenshotPath
 
   "READINESS_URL=$readinessUrl"
+  "EXPORT_READY_URL=$exportReadyUrl"
   "FRONTEND_STATUS=$($frontendResponse.StatusCode)"
-  "SCREENSHOT=$screenshotPath"
+  "SCREENSHOT_QUARTER=$quarterScreenshotPath"
+  "SCREENSHOT_EXPORT_READY=$finishNowScreenshotPath"
   'FRONTEND_LOG_START'
   Receive-Job $frontendJob -Keep -ErrorAction SilentlyContinue 2>&1
   'FRONTEND_LOG_END'
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

codex
The code changes are in place. I’m doing a quick read-through of the touched areas now, then I’ll run the frontend tests and the quarter UI smoke path so I can capture artifacts and finish the lifecycle updates with real evidence.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Raw 'C:\\Users\\edebe\\eds\\bizPA\\frontend\\src\\quarterReadiness.js'" in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git diff -- bizPA/frontend/src/App.jsx bizPA/frontend/src/quarterReadiness.js bizPA/frontend/src/quarterReadiness.test.js bizPA/start_bizpa_tax_readiness_ui.ps1 bizPA/tax_readiness_ui_smoke.ps1 workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md' in C:\Users\edebe\eds succeeded in 1.01s:

 succeeded in 996ms:
const SEVERITY_ORDER = {
  high: 0,
  medium: 1,
  low: 2,
};

const normalizeNumber = (value, fallback = 0) => {
  const parsed = Number(value);
  return Number.isFinite(parsed) ? parsed : fallback;
};

const sortIssues = (issues = []) => [...issues].sort((left, right) => {
  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
  if (severityDelta !== 0) {
    return severityDelta;
  }

  const leftDate = String(left.txn_date || '');
  const rightDate = String(right.txn_date || '');
  if (leftDate !== rightDate) {
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..af21307c83db35a88b75e7d7aae29ff31ac7f730
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,26 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    fallbackIssues: inboxItems,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1532,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1547,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1633,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2104,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2317,25 +2563,236 @@
   );
 
   const renderInbox = () => (
-    <div>
-      <h4>Finish Now Queue</h4>
-      <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
-      <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
-            <div>
-              <b>{item.merchant || 'Unknown'}</b>
-              <br />
-              <small>
-                {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
-              </small>
-              <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
-            </div>
+    <div className="inbox-mobile-shell">
+      <section className="inbox-mobile-hero">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2">
+          <div className="inbox-kicker">
+            <Bell size={14} />
+            Mobile-first exception queue
+          </div>
+          <div className="status-pill">{filteredInboxCards.length} visible</div>
+        </div>
+        <h4>Finish now</h4>
+        <p>
+          Clear the blockers that stop the quarterly export: category, business or personal, split treatment, and duplicate resolution.
+        </p>
+        <div className="inbox-mobile-stats">
+          <div className="inbox-mobile-stat">
+            <small>Queue</small>
+            <strong>{inboxCards.length}</strong>
+            <small>{activeQuarterReport.quarter_reference || READINESS_DEMO_REPORT.quarter_reference}</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Critical</small>
+            <strong>{highSeverityCount}</strong>
+            <small>High-severity blockers</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Ready now</small>
+            <strong>{readyToExportCount}</strong>
+            <small>Transactions already export-safe</small>
+          </div>
+        </div>
+        <div className="hero-actions">
+          <button className="hero-action-main" onClick={() => setCurrentTab('quarter')}>
+            <ArrowRight size={16} />
+            Review quarter readiness
+          </button>
+          <button className="hero-action-secondary" onClick={undoLastInboxAction} disabled={inboxBusyId === 'undo-last'}>
+            {inboxBusyId === 'undo-last' ? <RefreshCcw size={16} className="spinner" /> : <ArchiveRestore size={16} />}
+            Undo last action
+          </button>
+        </div>
+      </section>
+
+      {inboxMessage && (
+        <div className={`readiness-panel inbox-message ${inboxMessage.type}`}>
+          {inboxMessage.text}
+        </div>
+      )}
+
+      <section className="readiness-panel">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-3">
+          <div>
+            <h5 style={{ marginBottom: 4 }}>Queue filters</h5>
+            <small className="small-muted">Keep the mobile pass focused on one blocker type at a time.</small>
           </div>
-        ))}
-      </div>
+          <div className="status-pill">{activeQuarterReport.readiness_pct ?? READINESS_DEMO_REPORT.readiness_pct}% ready</div>
+        </div>
+        <div className="inbox-filter-row">
+          {INBOX_FILTERS.map((filter) => {
+            const count = filter.key === 'all'
+              ? inboxCards.length
+              : inboxCards.filter((card) => card.issue_types.includes(filter.key)).length;
+            return (
+              <button
+                key={filter.key}
+                type="button"
+                className={`inbox-filter-chip ${inboxFilter === filter.key ? 'active' : ''}`}
+                onClick={() => setInboxFilter(filter.key)}
+              >
+                <span>{filter.label}</span>
+                <small>{count}</small>
+              </button>
+            );
+          })}
+        </div>
+      </section>
+
+      {filteredInboxCards.length === 0 ? (
+        <div className="inbox-empty">
+          No blockers match this filter. Reset to All blockers or return to the quarter screen to confirm readiness.
+        </div>
+      ) : (
+        <div className="inbox-card-list">
+          {filteredInboxCards.map((card) => {
+            const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+            const saving = inboxBusyId === card.id;
+            const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+            return (
+              <section key={card.id} className="inbox-action-card">
+                <div className="inbox-card-top">
+                  <div>
+                    <strong>{card.merchant}</strong>
+                    <div className="inbox-card-subline">
+                      {card.txn_date ? String(card.txn_date).slice(0, 10) : 'Date pending'} · {formatIssueAmount(card.amount, card.direction)}
+                    </div>
+                  </div>
+                  <span className={`inbox-issue-chip ${card.severity}`}>
+                    <TriangleAlert size={14} />
+                    {INBOX_REASON_LABELS[card.blocker_reason] || card.blocker_reason.replace(/_/g, ' ')}
+                  </span>
+                </div>
+
+                <div className="inbox-chip-row">
+                  {card.issue_labels.map((label) => (
+                    <span key={`${card.id}-${label}`} className={`inbox-issue-chip ${card.severity}`}>
+                      {label}
+                    </span>
+                  ))}
+                  {card.suggested_category?.category_name && (
+                    <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
+                      <CheckCircle2 size={14} />
+                      Suggested: {card.suggested_category.category_name}
+                    </button>
+                  )}
+                </div>
+
+                <p className="inbox-explainer">{card.explanation || 'Resolve the blocking fields to clear this transaction from the queue.'}</p>
+
+                {(card.supports_category || card.supports_business_personal || card.supports_split) && (
+                  <div className="inbox-action-grid">
+                    <div className="inbox-field">
+                      <label>Category code</label>
+                      <input
+                        value={draft.category_code}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_code', event.target.value)}
+                        placeholder="MATERIALS"
+                      />
+                    </div>
+                    <div className="inbox-field">
+                      <label>Category label</label>
+                      <input
+                        value={draft.category_name}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_name', event.target.value)}
+                        placeholder="Materials"
+                      />
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_business_personal || card.supports_split) && (
+                  <div>
+                    <div className="inbox-field">
+                      <label>Business treatment</label>
+                    </div>
+                    <div className="inbox-toggle-row">
+                      {[
+                        { value: 'BUSINESS', label: 'Business' },
+                        { value: 'PERSONAL', label: 'Personal' },
+                        { value: 'SPLIT', label: 'Split' },
+                      ].map((option) => (
+                        <button
+                          key={option.value}
+                          type="button"
+                          className={`inbox-toggle ${draft.business_personal === option.value ? 'active' : ''}`}
+                          onClick={() => setInboxDraftField(card.id, 'business_personal', option.value)}
+                        >
+                          {option.label}
+                        </button>
+                      ))}
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_split || isSplit) && (
+                  <div className="inbox-field">
+                    <label>Business split %</label>
+                    <input
+                      type="number"
+                      min="0"
+                      max="100"
+                      value={draft.split_business_pct}
+                      onChange={(event) => setInboxDraftField(card.id, 'split_business_pct', event.target.value)}
+                      placeholder="80"
+                    />
+                  </div>
+                )}
+
+                {card.supports_duplicate && (
+                  <>
+                    <div>
+                      <div className="inbox-field">
+                        <label>Duplicate action</label>
+                      </div>
+                      <div className="inbox-duplicate-row">
+                        {[
+                          { value: 'dismiss', label: 'Dismiss duplicate' },
+                          { value: 'merge', label: 'Merge to original' },
+                        ].map((option) => (
+                          <button
+                            key={option.value}
+                            type="button"
+                            className={`inbox-toggle ${draft.duplicate_action === option.value ? 'active' : ''}`}
+                            onClick={() => setInboxDraftField(card.id, 'duplicate_action', option.value)}
+                          >
+                            {option.label}
+                          </button>
+                        ))}
+                      </div>
+                    </div>
+                    {draft.duplicate_action === 'merge' && (
+                      <div className="inbox-field">
+                        <label>Original transaction id</label>
+                        <input
+                          value={draft.duplicate_of_txn_id}
+                          onChange={(event) => setInboxDraftField(card.id, 'duplicate_of_txn_id', event.target.value)}
+                          placeholder="txn-9001"
+                        />
+                      </div>
+                    )}
+                  </>
+                )}
+
+                <div className="inbox-footer-row">
+                  <div className="inbox-inline-status">
+                    Route: {card.resolution_target?.route || `/api/v1/inbox/${card.id}`}
+                  </div>
+                  <div className="inbox-footer-actions">
+                    <button className="secondary-action" onClick={clearInboxMessage} disabled={saving}>
+                      Clear banner
+                    </button>
+                    <button className="primary-action" onClick={() => submitInboxAction(card)} disabled={saving}>
+                      {saving ? <RefreshCcw size={16} className="spinner" /> : <CheckCircle2 size={16} />}
+                      Save and clear blocker
+                    </button>
+                  </div>
+                </div>
+              </section>
+            );
+          })}
+        </div>
+      )}
     </div>
   );
 
@@ -2510,11 +2967,11 @@
           <section className="readiness-panel">
             <h5 style={{ marginBottom: 12 }}>Queue Backlog</h5>
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
-                <div key={item.id} className="control-row">
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
+                <div key={item.id || item.affected_entity_id} className="control-row">
                   <div>
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
                   <div className="text-end">
                     <div className="small-muted">{item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date}</div>
@@ -2522,7 +2979,7 @@
                   </div>
                 </div>
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
               )}
             </div>
@@ -2668,7 +3125,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2697,7 +3154,7 @@
   };
 
   const createSnapshotVersion = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     const status = snapshotWorkspace?.snapshot_status;
 
     if (!status?.can_create_snapshot_version) {
@@ -2719,7 +3176,7 @@
         description: `Quarter snapshot generated from Quarter workspace`,
         metadata: {
           source_type: 'manual',
-          readiness_pct: quarterReadiness?.readiness_pct ?? null
+          readiness_pct: activeQuarterReport?.readiness_pct ?? null
         }
       });
       await refreshQuarterWorkspace();
@@ -2734,7 +3191,7 @@
   };
 
   const closeQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(closeReason || '').trim()) {
       setQuarterActionError('Close reason is required.');
       setQuarterActionMessage('');
@@ -2785,7 +3242,7 @@
   };
 
   const reopenQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(reopenReason || '').trim()) {
       setQuarterActionError('Reopen reason is required.');
       setQuarterActionMessage('');
@@ -2844,9 +3301,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3341,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3353,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3364,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3378,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..669173cd6176ee9ee5c86c5895ea49cb9524ebc4
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,155 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, fallbackIssues = [], resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const issueSource = Array.isArray(report.issue_list) && report.issue_list.length > 0
+    ? report.issue_list
+    : fallbackIssues;
+  const allIssues = sortIssues(Array.isArray(issueSource) ? issueSource : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/bizPA/frontend/src/quarterReadiness.test.js b/bizPA/frontend/src/quarterReadiness.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..0b810191a89d8b82091bf5de1408afcc23c14e65
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.test.js
@@ -0,0 +1,93 @@
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
+
+const reportFixture = {
+  quarter_reference: 'Q1-2026',
+  period_start: '2026-01-01',
+  period_end: '2026-03-31',
+  total_txns_in_period: 18,
+  readiness_pct: 78,
+  can_export: false,
+  issue_list: [
+    {
+      issue_type: 'missing_category',
+      affected_entity_id: 'txn-9101',
+      severity: 'high',
+      weight: 35,
+      explanation: 'Category missing for Fuel Stop.',
+      txn_date: '2026-02-04',
+      merchant: 'Fuel Stop',
+      amount: 64.18,
+      direction: 'out',
+      resolution_target: {
+        label: 'Classify transaction',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'missing_business_personal',
+      affected_entity_id: 'txn-9103',
+      severity: 'high',
+      weight: 30,
+      explanation: 'Business/personal decision missing.',
+      txn_date: '2026-02-15',
+      merchant: 'Mobile Contract',
+      amount: 42.5,
+      direction: 'out',
+      resolution_target: {
+        label: 'Set business or personal',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'unresolved_duplicate',
+      affected_entity_id: 'txn-9104',
+      severity: 'medium',
+      weight: 15,
+      explanation: 'Duplicate not resolved.',
+      txn_date: '2026-03-02',
+      merchant: 'Client Payment',
+      amount: 820,
+      direction: 'in',
+      resolution_target: {
+        label: 'Resolve duplicate',
+        workflow: '/inbox/finish-now',
+      },
+    },
+  ],
+};
+
+describe('quarter readiness helpers', () => {
+  test('keeps blockers ordered and updates headline while blockers remain', () => {
+    const state = buildQuarterUiState({ report: reportFixture, resolvedIssueIds: ['txn-9104'] });
+
+    expect(state.itemsLeft).toBe(2);
+    expect(state.exportEnabled).toBe(false);
+    expect(state.report.readiness_pct).toBeGreaterThan(78);
+    expect(state.headline).toContain('items left');
+    expect(state.activeIssues.map((issue) => issue.affected_entity_id)).toEqual(['txn-9101', 'txn-9103']);
+    expect(state.report.explanation_lines[0]).toContain('Readiness is');
+  });
+
+  test('unlocks export immediately when every blocker is resolved', () => {
+    const state = buildQuarterUiState({
+      report: reportFixture,
+      resolvedIssueIds: ['txn-9101', 'txn-9103', 'txn-9104'],
+    });
+
+    expect(state.itemsLeft).toBe(0);
+    expect(state.exportEnabled).toBe(true);
+    expect(state.report.readiness_pct).toBe(100);
+    expect(state.report.can_export).toBe(true);
+    expect(state.report.issue_summary).toEqual([]);
+  });
+
+  test('supports demo query params that resolve all blockers', () => {
+    const params = new URLSearchParams('resolvedDemoIssues=all');
+    const resolvedIds = deriveResolvedIssueIdsFromSearchParams(params, reportFixture.issue_list);
+
+    expect(resolvedIds).toEqual(['txn-9101', 'txn-9103', 'txn-9104']);
+  });
+});
diff --git a/bizPA/start_bizpa_tax_readiness_ui.ps1 b/bizPA/start_bizpa_tax_readiness_ui.ps1
index 1fc4bef8cd172c31fdc3db52d1acc9d4775c5485..7216e44eac386fe436a9af8e9b417cd2d61e37fe
--- a/bizPA/start_bizpa_tax_readiness_ui.ps1
+++ b/bizPA/start_bizpa_tax_readiness_ui.ps1
@@ -4,6 +4,7 @@
 $frontendPath = Join-Path $repoRoot 'frontend'
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
 
 Write-Host 'Starting bizPA tax readiness UI...' -ForegroundColor Cyan
 
@@ -14,7 +15,10 @@
 Write-Host 'Tax readiness dashboard URL:' -ForegroundColor Green
 Write-Host "  $readinessUrl" -ForegroundColor Yellow
 Write-Host ''
+Write-Host 'Export-ready verification URL:' -ForegroundColor Green
+Write-Host "  $exportReadyUrl" -ForegroundColor Yellow
+Write-Host ''
 Write-Host 'Base frontend URL:' -ForegroundColor Green
 Write-Host "  $frontendUrl" -ForegroundColor Yellow
 Write-Host ''
-Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI can be reviewed without backend setup.' -ForegroundColor Cyan
+Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI and zero-blocker export-ready state can be reviewed without backend setup.' -ForegroundColor Cyan
diff --git a/bizPA/tax_readiness_ui_smoke.ps1 b/bizPA/tax_readiness_ui_smoke.ps1
index 56d67892a51b22572503dec560d17123b370c2b7..269c2a3221adfe9095130883c89fd76428a65989
--- a/bizPA/tax_readiness_ui_smoke.ps1
+++ b/bizPA/tax_readiness_ui_smoke.ps1
@@ -28,36 +28,38 @@
 
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
-$screenshotPath = 'C:\Users\edebe\eds\workstream\verification\20260311_201500_bizpa_tax_readiness_ui.png'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
+$verificationRoot = 'C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification'
+$quarterScreenshotPath = Join-Path $verificationRoot 'quarter_readiness_screen.png'
+$finishNowScreenshotPath = Join-Path $verificationRoot 'finish_now_queue_zero_blockers.png'
 $chromeProfilePath = 'C:\Users\edebe\eds\workstream\verification\chrome_capture_profile_tax_readiness'
-
-New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
+$chromePath = 'C:\Program Files\Google\Chrome\Application\chrome.exe'
 
-$frontendJob = Start-Job -ScriptBlock {
-  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
-  $env:PORT = '3002'
-  npm.cmd start
-}
+function Wait-FrontendReady {
+  param (
+    [string]$Url
+  )
 
-try {
-  $frontendResponse = $null
-  $frontendReady = $false
   for ($i = 0; $i -lt 45; $i++) {
     Start-Sleep -Seconds 2
     try {
-      $frontendResponse = Invoke-WebRequest -UseBasicParsing $readinessUrl
-      if ($frontendResponse.StatusCode -eq 200) {
-        $frontendReady = $true
-        break
+      $response = Invoke-WebRequest -UseBasicParsing $Url
+      if ($response.StatusCode -eq 200) {
+        return $response
       }
     } catch {
     }
   }
 
-  if (-not $frontendReady) {
-    throw "Frontend did not become ready on $readinessUrl"
-  }
+  throw "Frontend did not become ready on $Url"
+}
 
+function Capture-ChromeAppScreenshot {
+  param (
+    [string]$Url,
+    [string]$OutputPath
+  )
+
   $chromeArgs = @(
     "--user-data-dir=$chromeProfilePath",
     '--new-window',
@@ -68,9 +70,9 @@
     '--disable-breakpad',
     '--disable-crash-reporter',
     '--noerrdialogs',
-    "--app=$readinessUrl"
+    "--app=$Url"
   )
-  Start-Process 'C:\Program Files\Google\Chrome\Application\chrome.exe' -ArgumentList $chromeArgs | Out-Null
+  Start-Process $chromePath -ArgumentList $chromeArgs | Out-Null
 
   $chromeWindow = $null
   for ($i = 0; $i -lt 20; $i++) {
@@ -96,7 +98,7 @@
   }
 
   if (-not $chromeWindow) {
-    throw 'Chrome readiness window did not expose a main window handle for screenshot capture.'
+    throw "Chrome window did not expose a main window handle for $Url"
   }
 
   [WindowCaptureNative]::ShowWindow($chromeWindow.MainWindowHandle, 3) | Out-Null
@@ -110,17 +112,34 @@
   $bitmap = New-Object System.Drawing.Bitmap $width, $height
   $graphics = [System.Drawing.Graphics]::FromImage($bitmap)
   $graphics.CopyFromScreen($rect.Left, $rect.Top, 0, 0, $bitmap.Size)
-  $bitmap.Save($screenshotPath, [System.Drawing.Imaging.ImageFormat]::Png)
+  $bitmap.Save($OutputPath, [System.Drawing.Imaging.ImageFormat]::Png)
   $graphics.Dispose()
   $bitmap.Dispose()
 
   Get-Process chrome -ErrorAction SilentlyContinue |
     Where-Object { $_.StartTime -ge $chromeWindow.StartTime.AddSeconds(-2) } |
     Stop-Process -Force -ErrorAction SilentlyContinue
+}
+
+New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
+New-Item -ItemType Directory -Force -Path $verificationRoot | Out-Null
+
+$frontendJob = Start-Job -ScriptBlock {
+  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
+  $env:PORT = '3002'
+  npm.cmd start
+}
+
+try {
+  $frontendResponse = Wait-FrontendReady -Url $readinessUrl
+  Capture-ChromeAppScreenshot -Url $readinessUrl -OutputPath $quarterScreenshotPath
+  Capture-ChromeAppScreenshot -Url $exportReadyUrl -OutputPath $finishNowScreenshotPath
 
   "READINESS_URL=$readinessUrl"
+  "EXPORT_READY_URL=$exportReadyUrl"
   "FRONTEND_STATUS=$($frontendResponse.StatusCode)"
-  "SCREENSHOT=$screenshotPath"
+  "SCREENSHOT_QUARTER=$quarterScreenshotPath"
+  "SCREENSHOT_EXPORT_READY=$finishNowScreenshotPath"
   'FRONTEND_LOG_START'
   Receive-Job $frontendJob -Keep -ErrorAction SilentlyContinue 2>&1
   'FRONTEND_LOG_END'
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$env:CI='"'true'; npm.cmd test -- --runInBand --watchAll=false" in C:\Users\edebe\eds\bizPA\frontend succeeded in 6.77s:

> frontend@1.3.6 test
> react-scripts test --runInBand --watchAll=false

PASS src/onboarding.test.js
PASS src/controlCentre.test.js
PASS src/governance.test.js
PASS src/quarterReadiness.test.js

Test Suites: 4 passed, 4 total
Tests:       14 passed, 14 total
Snapshots:   0 total
Time:        4.097 s
Ran all test suites.
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..af21307c83db35a88b75e7d7aae29ff31ac7f730
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,26 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    fallbackIssues: inboxItems,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1532,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1547,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1633,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2104,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2317,25 +2563,236 @@
   );
 
   const renderInbox = () => (
-    <div>
-      <h4>Finish Now Queue</h4>
-      <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
-      <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
-            <div>
-              <b>{item.merchant || 'Unknown'}</b>
-              <br />
-              <small>
-                {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
-              </small>
-              <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
-            </div>
+    <div className="inbox-mobile-shell">
+      <section className="inbox-mobile-hero">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2">
+          <div className="inbox-kicker">
+            <Bell size={14} />
+            Mobile-first exception queue
+          </div>
+          <div className="status-pill">{filteredInboxCards.length} visible</div>
+        </div>
+        <h4>Finish now</h4>
+        <p>
+          Clear the blockers that stop the quarterly export: category, business or personal, split treatment, and duplicate resolution.
+        </p>
+        <div className="inbox-mobile-stats">
+          <div className="inbox-mobile-stat">
+            <small>Queue</small>
+            <strong>{inboxCards.length}</strong>
+            <small>{activeQuarterReport.quarter_reference || READINESS_DEMO_REPORT.quarter_reference}</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Critical</small>
+            <strong>{highSeverityCount}</strong>
+            <small>High-severity blockers</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Ready now</small>
+            <strong>{readyToExportCount}</strong>
+            <small>Transactions already export-safe</small>
+          </div>
+        </div>
+        <div className="hero-actions">
+          <button className="hero-action-main" onClick={() => setCurrentTab('quarter')}>
+            <ArrowRight size={16} />
+            Review quarter readiness
+          </button>
+          <button className="hero-action-secondary" onClick={undoLastInboxAction} disabled={inboxBusyId === 'undo-last'}>
+            {inboxBusyId === 'undo-last' ? <RefreshCcw size={16} className="spinner" /> : <ArchiveRestore size={16} />}
+            Undo last action
+          </button>
+        </div>
+      </section>
+
+      {inboxMessage && (
+        <div className={`readiness-panel inbox-message ${inboxMessage.type}`}>
+          {inboxMessage.text}
+        </div>
+      )}
+
+      <section className="readiness-panel">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-3">
+          <div>
+            <h5 style={{ marginBottom: 4 }}>Queue filters</h5>
+            <small className="small-muted">Keep the mobile pass focused on one blocker type at a time.</small>
           </div>
-        ))}
-      </div>
+          <div className="status-pill">{activeQuarterReport.readiness_pct ?? READINESS_DEMO_REPORT.readiness_pct}% ready</div>
+        </div>
+        <div className="inbox-filter-row">
+          {INBOX_FILTERS.map((filter) => {
+            const count = filter.key === 'all'
+              ? inboxCards.length
+              : inboxCards.filter((card) => card.issue_types.includes(filter.key)).length;
+            return (
+              <button
+                key={filter.key}
+                type="button"
+                className={`inbox-filter-chip ${inboxFilter === filter.key ? 'active' : ''}`}
+                onClick={() => setInboxFilter(filter.key)}
+              >
+                <span>{filter.label}</span>
+                <small>{count}</small>
+              </button>
+            );
+          })}
+        </div>
+      </section>
+
+      {filteredInboxCards.length === 0 ? (
+        <div className="inbox-empty">
+          No blockers match this filter. Reset to All blockers or return to the quarter screen to confirm readiness.
+        </div>
+      ) : (
+        <div className="inbox-card-list">
+          {filteredInboxCards.map((card) => {
+            const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+            const saving = inboxBusyId === card.id;
+            const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+            return (
+              <section key={card.id} className="inbox-action-card">
+                <div className="inbox-card-top">
+                  <div>
+                    <strong>{card.merchant}</strong>
+                    <div className="inbox-card-subline">
+                      {card.txn_date ? String(card.txn_date).slice(0, 10) : 'Date pending'} · {formatIssueAmount(card.amount, card.direction)}
+                    </div>
+                  </div>
+                  <span className={`inbox-issue-chip ${card.severity}`}>
+                    <TriangleAlert size={14} />
+                    {INBOX_REASON_LABELS[card.blocker_reason] || card.blocker_reason.replace(/_/g, ' ')}
+                  </span>
+                </div>
+
+                <div className="inbox-chip-row">
+                  {card.issue_labels.map((label) => (
+                    <span key={`${card.id}-${label}`} className={`inbox-issue-chip ${card.severity}`}>
+                      {label}
+                    </span>
+                  ))}
+                  {card.suggested_category?.category_name && (
+                    <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
+                      <CheckCircle2 size={14} />
+                      Suggested: {card.suggested_category.category_name}
+                    </button>
+                  )}
+                </div>
+
+                <p className="inbox-explainer">{card.explanation || 'Resolve the blocking fields to clear this transaction from the queue.'}</p>
+
+                {(card.supports_category || card.supports_business_personal || card.supports_split) && (
+                  <div className="inbox-action-grid">
+                    <div className="inbox-field">
+                      <label>Category code</label>
+                      <input
+                        value={draft.category_code}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_code', event.target.value)}
+                        placeholder="MATERIALS"
+                      />
+                    </div>
+                    <div className="inbox-field">
+                      <label>Category label</label>
+                      <input
+                        value={draft.category_name}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_name', event.target.value)}
+                        placeholder="Materials"
+                      />
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_business_personal || card.supports_split) && (
+                  <div>
+                    <div className="inbox-field">
+                      <label>Business treatment</label>
+                    </div>
+                    <div className="inbox-toggle-row">
+                      {[
+                        { value: 'BUSINESS', label: 'Business' },
+                        { value: 'PERSONAL', label: 'Personal' },
+                        { value: 'SPLIT', label: 'Split' },
+                      ].map((option) => (
+                        <button
+                          key={option.value}
+                          type="button"
+                          className={`inbox-toggle ${draft.business_personal === option.value ? 'active' : ''}`}
+                          onClick={() => setInboxDraftField(card.id, 'business_personal', option.value)}
+                        >
+                          {option.label}
+                        </button>
+                      ))}
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_split || isSplit) && (
+                  <div className="inbox-field">
+                    <label>Business split %</label>
+                    <input
+                      type="number"
+                      min="0"
+                      max="100"
+                      value={draft.split_business_pct}
+                      onChange={(event) => setInboxDraftField(card.id, 'split_business_pct', event.target.value)}
+                      placeholder="80"
+                    />
+                  </div>
+                )}
+
+                {card.supports_duplicate && (
+                  <>
+                    <div>
+                      <div className="inbox-field">
+                        <label>Duplicate action</label>
+                      </div>
+                      <div className="inbox-duplicate-row">
+                        {[
+                          { value: 'dismiss', label: 'Dismiss duplicate' },
+                          { value: 'merge', label: 'Merge to original' },
+                        ].map((option) => (
+                          <button
+                            key={option.value}
+                            type="button"
+                            className={`inbox-toggle ${draft.duplicate_action === option.value ? 'active' : ''}`}
+                            onClick={() => setInboxDraftField(card.id, 'duplicate_action', option.value)}
+                          >
+                            {option.label}
+                          </button>
+                        ))}
+                      </div>
+                    </div>
+                    {draft.duplicate_action === 'merge' && (
+                      <div className="inbox-field">
+                        <label>Original transaction id</label>
+                        <input
+                          value={draft.duplicate_of_txn_id}
+                          onChange={(event) => setInboxDraftField(card.id, 'duplicate_of_txn_id', event.target.value)}
+                          placeholder="txn-9001"
+                        />
+                      </div>
+                    )}
+                  </>
+                )}
+
+                <div className="inbox-footer-row">
+                  <div className="inbox-inline-status">
+                    Route: {card.resolution_target?.route || `/api/v1/inbox/${card.id}`}
+                  </div>
+                  <div className="inbox-footer-actions">
+                    <button className="secondary-action" onClick={clearInboxMessage} disabled={saving}>
+                      Clear banner
+                    </button>
+                    <button className="primary-action" onClick={() => submitInboxAction(card)} disabled={saving}>
+                      {saving ? <RefreshCcw size={16} className="spinner" /> : <CheckCircle2 size={16} />}
+                      Save and clear blocker
+                    </button>
+                  </div>
+                </div>
+              </section>
+            );
+          })}
+        </div>
+      )}
     </div>
   );
 
@@ -2510,11 +2967,11 @@
           <section className="readiness-panel">
             <h5 style={{ marginBottom: 12 }}>Queue Backlog</h5>
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
-                <div key={item.id} className="control-row">
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
+                <div key={item.id || item.affected_entity_id} className="control-row">
                   <div>
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
                   <div className="text-end">
                     <div className="small-muted">{item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date}</div>
@@ -2522,7 +2979,7 @@
                   </div>
                 </div>
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
               )}
             </div>
@@ -2668,7 +3125,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2697,7 +3154,7 @@
   };
 
   const createSnapshotVersion = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     const status = snapshotWorkspace?.snapshot_status;
 
     if (!status?.can_create_snapshot_version) {
@@ -2719,7 +3176,7 @@
         description: `Quarter snapshot generated from Quarter workspace`,
         metadata: {
           source_type: 'manual',
-          readiness_pct: quarterReadiness?.readiness_pct ?? null
+          readiness_pct: activeQuarterReport?.readiness_pct ?? null
         }
       });
       await refreshQuarterWorkspace();
@@ -2734,7 +3191,7 @@
   };
 
   const closeQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(closeReason || '').trim()) {
       setQuarterActionError('Close reason is required.');
       setQuarterActionMessage('');
@@ -2785,7 +3242,7 @@
   };
 
   const reopenQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(reopenReason || '').trim()) {
       setQuarterActionError('Reopen reason is required.');
       setQuarterActionMessage('');
@@ -2844,9 +3301,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3341,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3353,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3364,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3378,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..669173cd6176ee9ee5c86c5895ea49cb9524ebc4
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,155 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, fallbackIssues = [], resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const issueSource = Array.isArray(report.issue_list) && report.issue_list.length > 0
+    ? report.issue_list
+    : fallbackIssues;
+  const allIssues = sortIssues(Array.isArray(issueSource) ? issueSource : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/bizPA/frontend/src/quarterReadiness.test.js b/bizPA/frontend/src/quarterReadiness.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..0b810191a89d8b82091bf5de1408afcc23c14e65
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.test.js
@@ -0,0 +1,93 @@
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
+
+const reportFixture = {
+  quarter_reference: 'Q1-2026',
+  period_start: '2026-01-01',
+  period_end: '2026-03-31',
+  total_txns_in_period: 18,
+  readiness_pct: 78,
+  can_export: false,
+  issue_list: [
+    {
+      issue_type: 'missing_category',
+      affected_entity_id: 'txn-9101',
+      severity: 'high',
+      weight: 35,
+      explanation: 'Category missing for Fuel Stop.',
+      txn_date: '2026-02-04',
+      merchant: 'Fuel Stop',
+      amount: 64.18,
+      direction: 'out',
+      resolution_target: {
+        label: 'Classify transaction',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'missing_business_personal',
+      affected_entity_id: 'txn-9103',
+      severity: 'high',
+      weight: 30,
+      explanation: 'Business/personal decision missing.',
+      txn_date: '2026-02-15',
+      merchant: 'Mobile Contract',
+      amount: 42.5,
+      direction: 'out',
+      resolution_target: {
+        label: 'Set business or personal',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'unresolved_duplicate',
+      affected_entity_id: 'txn-9104',
+      severity: 'medium',
+      weight: 15,
+      explanation: 'Duplicate not resolved.',
+      txn_date: '2026-03-02',
+      merchant: 'Client Payment',
+      amount: 820,
+      direction: 'in',
+      resolution_target: {
+        label: 'Resolve duplicate',
+        workflow: '/inbox/finish-now',
+      },
+    },
+  ],
+};
+
+describe('quarter readiness helpers', () => {
+  test('keeps blockers ordered and updates headline while blockers remain', () => {
+    const state = buildQuarterUiState({ report: reportFixture, resolvedIssueIds: ['txn-9104'] });
+
+    expect(state.itemsLeft).toBe(2);
+    expect(state.exportEnabled).toBe(false);
+    expect(state.report.readiness_pct).toBeGreaterThan(78);
+    expect(state.headline).toContain('items left');
+    expect(state.activeIssues.map((issue) => issue.affected_entity_id)).toEqual(['txn-9101', 'txn-9103']);
+    expect(state.report.explanation_lines[0]).toContain('Readiness is');
+  });
+
+  test('unlocks export immediately when every blocker is resolved', () => {
+    const state = buildQuarterUiState({
+      report: reportFixture,
+      resolvedIssueIds: ['txn-9101', 'txn-9103', 'txn-9104'],
+    });
+
+    expect(state.itemsLeft).toBe(0);
+    expect(state.exportEnabled).toBe(true);
+    expect(state.report.readiness_pct).toBe(100);
+    expect(state.report.can_export).toBe(true);
+    expect(state.report.issue_summary).toEqual([]);
+  });
+
+  test('supports demo query params that resolve all blockers', () => {
+    const params = new URLSearchParams('resolvedDemoIssues=all');
+    const resolvedIds = deriveResolvedIssueIdsFromSearchParams(params, reportFixture.issue_list);
+
+    expect(resolvedIds).toEqual(['txn-9101', 'txn-9103', 'txn-9104']);
+  });
+});
diff --git a/bizPA/start_bizpa_tax_readiness_ui.ps1 b/bizPA/start_bizpa_tax_readiness_ui.ps1
index 1fc4bef8cd172c31fdc3db52d1acc9d4775c5485..7216e44eac386fe436a9af8e9b417cd2d61e37fe
--- a/bizPA/start_bizpa_tax_readiness_ui.ps1
+++ b/bizPA/start_bizpa_tax_readiness_ui.ps1
@@ -4,6 +4,7 @@
 $frontendPath = Join-Path $repoRoot 'frontend'
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
 
 Write-Host 'Starting bizPA tax readiness UI...' -ForegroundColor Cyan
 
@@ -14,7 +15,10 @@
 Write-Host 'Tax readiness dashboard URL:' -ForegroundColor Green
 Write-Host "  $readinessUrl" -ForegroundColor Yellow
 Write-Host ''
+Write-Host 'Export-ready verification URL:' -ForegroundColor Green
+Write-Host "  $exportReadyUrl" -ForegroundColor Yellow
+Write-Host ''
 Write-Host 'Base frontend URL:' -ForegroundColor Green
 Write-Host "  $frontendUrl" -ForegroundColor Yellow
 Write-Host ''
-Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI can be reviewed without backend setup.' -ForegroundColor Cyan
+Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI and zero-blocker export-ready state can be reviewed without backend setup.' -ForegroundColor Cyan
diff --git a/bizPA/tax_readiness_ui_smoke.ps1 b/bizPA/tax_readiness_ui_smoke.ps1
index 56d67892a51b22572503dec560d17123b370c2b7..269c2a3221adfe9095130883c89fd76428a65989
--- a/bizPA/tax_readiness_ui_smoke.ps1
+++ b/bizPA/tax_readiness_ui_smoke.ps1
@@ -28,36 +28,38 @@
 
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
-$screenshotPath = 'C:\Users\edebe\eds\workstream\verification\20260311_201500_bizpa_tax_readiness_ui.png'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
+$verificationRoot = 'C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification'
+$quarterScreenshotPath = Join-Path $verificationRoot 'quarter_readiness_screen.png'
+$finishNowScreenshotPath = Join-Path $verificationRoot 'finish_now_queue_zero_blockers.png'
 $chromeProfilePath = 'C:\Users\edebe\eds\workstream\verification\chrome_capture_profile_tax_readiness'
-
-New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
+$chromePath = 'C:\Program Files\Google\Chrome\Application\chrome.exe'
 
-$frontendJob = Start-Job -ScriptBlock {
-  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
-  $env:PORT = '3002'
-  npm.cmd start
-}
+function Wait-FrontendReady {
+  param (
+    [string]$Url
+  )
 
-try {
-  $frontendResponse = $null
-  $frontendReady = $false
   for ($i = 0; $i -lt 45; $i++) {
     Start-Sleep -Seconds 2
     try {
-      $frontendResponse = Invoke-WebRequest -UseBasicParsing $readinessUrl
-      if ($frontendResponse.StatusCode -eq 200) {
-        $frontendReady = $true
-        break
+      $response = Invoke-WebRequest -UseBasicParsing $Url
+      if ($response.StatusCode -eq 200) {
+        return $response
       }
     } catch {
     }
   }
 
-  if (-not $frontendReady) {
-    throw "Frontend did not become ready on $readinessUrl"
-  }
+  throw "Frontend did not become ready on $Url"
+}
 
+function Capture-ChromeAppScreenshot {
+  param (
+    [string]$Url,
+    [string]$OutputPath
+  )
+
   $chromeArgs = @(
     "--user-data-dir=$chromeProfilePath",
     '--new-window',
@@ -68,9 +70,9 @@
     '--disable-breakpad',
     '--disable-crash-reporter',
     '--noerrdialogs',
-    "--app=$readinessUrl"
+    "--app=$Url"
   )
-  Start-Process 'C:\Program Files\Google\Chrome\Application\chrome.exe' -ArgumentList $chromeArgs | Out-Null
+  Start-Process $chromePath -ArgumentList $chromeArgs | Out-Null
 
   $chromeWindow = $null
   for ($i = 0; $i -lt 20; $i++) {
@@ -96,7 +98,7 @@
   }
 
   if (-not $chromeWindow) {
-    throw 'Chrome readiness window did not expose a main window handle for screenshot capture.'
+    throw "Chrome window did not expose a main window handle for $Url"
   }
 
   [WindowCaptureNative]::ShowWindow($chromeWindow.MainWindowHandle, 3) | Out-Null
@@ -110,17 +112,34 @@
   $bitmap = New-Object System.Drawing.Bitmap $width, $height
   $graphics = [System.Drawing.Graphics]::FromImage($bitmap)
   $graphics.CopyFromScreen($rect.Left, $rect.Top, 0, 0, $bitmap.Size)
-  $bitmap.Save($screenshotPath, [System.Drawing.Imaging.ImageFormat]::Png)
+  $bitmap.Save($OutputPath, [System.Drawing.Imaging.ImageFormat]::Png)
   $graphics.Dispose()
   $bitmap.Dispose()
 
   Get-Process chrome -ErrorAction SilentlyContinue |
     Where-Object { $_.StartTime -ge $chromeWindow.StartTime.AddSeconds(-2) } |
     Stop-Process -Force -ErrorAction SilentlyContinue
+}
+
+New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
+New-Item -ItemType Directory -Force -Path $verificationRoot | Out-Null
+
+$frontendJob = Start-Job -ScriptBlock {
+  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
+  $env:PORT = '3002'
+  npm.cmd start
+}
+
+try {
+  $frontendResponse = Wait-FrontendReady -Url $readinessUrl
+  Capture-ChromeAppScreenshot -Url $readinessUrl -OutputPath $quarterScreenshotPath
+  Capture-ChromeAppScreenshot -Url $exportReadyUrl -OutputPath $finishNowScreenshotPath
 
   "READINESS_URL=$readinessUrl"
+  "EXPORT_READY_URL=$exportReadyUrl"
   "FRONTEND_STATUS=$($frontendResponse.StatusCode)"
-  "SCREENSHOT=$screenshotPath"
+  "SCREENSHOT_QUARTER=$quarterScreenshotPath"
+  "SCREENSHOT_EXPORT_READY=$finishNowScreenshotPath"
   'FRONTEND_LOG_START'
   Receive-Job $frontendJob -Keep -ErrorAction SilentlyContinue 2>&1
   'FRONTEND_LOG_END'
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "powershell -ExecutionPolicy Bypass -File 'C:\\Users\\edebe\\eds\\bizPA\\start_bizpa_tax_readiness_ui.ps1'" in C:\Users\edebe\eds succeeded in 2.38s:
Starting bizPA tax readiness UI...

Tax readiness dashboard URL:
  http://127.0.0.1:3002/?readinessDemo=1&tab=quarter

Export-ready verification URL:
  http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all

Base frontend URL:
  http://127.0.0.1:3002

This launch path uses the local readiness demo state so the active-quarter UI and zero-blocker export-ready state can be reviewed without backend setup.
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..af21307c83db35a88b75e7d7aae29ff31ac7f730
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,26 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    fallbackIssues: inboxItems,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1532,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1547,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1633,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2104,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2317,25 +2563,236 @@
   );
 
   const renderInbox = () => (
-    <div>
-      <h4>Finish Now Queue</h4>
-      <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
-      <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
-            <div>
-              <b>{item.merchant || 'Unknown'}</b>
-              <br />
-              <small>
-                {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
-              </small>
-              <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
-            </div>
+    <div className="inbox-mobile-shell">
+      <section className="inbox-mobile-hero">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2">
+          <div className="inbox-kicker">
+            <Bell size={14} />
+            Mobile-first exception queue
+          </div>
+          <div className="status-pill">{filteredInboxCards.length} visible</div>
+        </div>
+        <h4>Finish now</h4>
+        <p>
+          Clear the blockers that stop the quarterly export: category, business or personal, split treatment, and duplicate resolution.
+        </p>
+        <div className="inbox-mobile-stats">
+          <div className="inbox-mobile-stat">
+            <small>Queue</small>
+            <strong>{inboxCards.length}</strong>
+            <small>{activeQuarterReport.quarter_reference || READINESS_DEMO_REPORT.quarter_reference}</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Critical</small>
+            <strong>{highSeverityCount}</strong>
+            <small>High-severity blockers</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Ready now</small>
+            <strong>{readyToExportCount}</strong>
+            <small>Transactions already export-safe</small>
+          </div>
+        </div>
+        <div className="hero-actions">
+          <button className="hero-action-main" onClick={() => setCurrentTab('quarter')}>
+            <ArrowRight size={16} />
+            Review quarter readiness
+          </button>
+          <button className="hero-action-secondary" onClick={undoLastInboxAction} disabled={inboxBusyId === 'undo-last'}>
+            {inboxBusyId === 'undo-last' ? <RefreshCcw size={16} className="spinner" /> : <ArchiveRestore size={16} />}
+            Undo last action
+          </button>
+        </div>
+      </section>
+
+      {inboxMessage && (
+        <div className={`readiness-panel inbox-message ${inboxMessage.type}`}>
+          {inboxMessage.text}
+        </div>
+      )}
+
+      <section className="readiness-panel">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-3">
+          <div>
+            <h5 style={{ marginBottom: 4 }}>Queue filters</h5>
+            <small className="small-muted">Keep the mobile pass focused on one blocker type at a time.</small>
           </div>
-        ))}
-      </div>
+          <div className="status-pill">{activeQuarterReport.readiness_pct ?? READINESS_DEMO_REPORT.readiness_pct}% ready</div>
+        </div>
+        <div className="inbox-filter-row">
+          {INBOX_FILTERS.map((filter) => {
+            const count = filter.key === 'all'
+              ? inboxCards.length
+              : inboxCards.filter((card) => card.issue_types.includes(filter.key)).length;
+            return (
+              <button
+                key={filter.key}
+                type="button"
+                className={`inbox-filter-chip ${inboxFilter === filter.key ? 'active' : ''}`}
+                onClick={() => setInboxFilter(filter.key)}
+              >
+                <span>{filter.label}</span>
+                <small>{count}</small>
+              </button>
+            );
+          })}
+        </div>
+      </section>
+
+      {filteredInboxCards.length === 0 ? (
+        <div className="inbox-empty">
+          No blockers match this filter. Reset to All blockers or return to the quarter screen to confirm readiness.
+        </div>
+      ) : (
+        <div className="inbox-card-list">
+          {filteredInboxCards.map((card) => {
+            const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+            const saving = inboxBusyId === card.id;
+            const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+            return (
+              <section key={card.id} className="inbox-action-card">
+                <div className="inbox-card-top">
+                  <div>
+                    <strong>{card.merchant}</strong>
+                    <div className="inbox-card-subline">
+                      {card.txn_date ? String(card.txn_date).slice(0, 10) : 'Date pending'} · {formatIssueAmount(card.amount, card.direction)}
+                    </div>
+                  </div>
+                  <span className={`inbox-issue-chip ${card.severity}`}>
+                    <TriangleAlert size={14} />
+                    {INBOX_REASON_LABELS[card.blocker_reason] || card.blocker_reason.replace(/_/g, ' ')}
+                  </span>
+                </div>
+
+                <div className="inbox-chip-row">
+                  {card.issue_labels.map((label) => (
+                    <span key={`${card.id}-${label}`} className={`inbox-issue-chip ${card.severity}`}>
+                      {label}
+                    </span>
+                  ))}
+                  {card.suggested_category?.category_name && (
+                    <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
+                      <CheckCircle2 size={14} />
+                      Suggested: {card.suggested_category.category_name}
+                    </button>
+                  )}
+                </div>
+
+                <p className="inbox-explainer">{card.explanation || 'Resolve the blocking fields to clear this transaction from the queue.'}</p>
+
+                {(card.supports_category || card.supports_business_personal || card.supports_split) && (
+                  <div className="inbox-action-grid">
+                    <div className="inbox-field">
+                      <label>Category code</label>
+                      <input
+                        value={draft.category_code}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_code', event.target.value)}
+                        placeholder="MATERIALS"
+                      />
+                    </div>
+                    <div className="inbox-field">
+                      <label>Category label</label>
+                      <input
+                        value={draft.category_name}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_name', event.target.value)}
+                        placeholder="Materials"
+                      />
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_business_personal || card.supports_split) && (
+                  <div>
+                    <div className="inbox-field">
+                      <label>Business treatment</label>
+                    </div>
+                    <div className="inbox-toggle-row">
+                      {[
+                        { value: 'BUSINESS', label: 'Business' },
+                        { value: 'PERSONAL', label: 'Personal' },
+                        { value: 'SPLIT', label: 'Split' },
+                      ].map((option) => (
+                        <button
+                          key={option.value}
+                          type="button"
+                          className={`inbox-toggle ${draft.business_personal === option.value ? 'active' : ''}`}
+                          onClick={() => setInboxDraftField(card.id, 'business_personal', option.value)}
+                        >
+                          {option.label}
+                        </button>
+                      ))}
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_split || isSplit) && (
+                  <div className="inbox-field">
+                    <label>Business split %</label>
+                    <input
+                      type="number"
+                      min="0"
+                      max="100"
+                      value={draft.split_business_pct}
+                      onChange={(event) => setInboxDraftField(card.id, 'split_business_pct', event.target.value)}
+                      placeholder="80"
+                    />
+                  </div>
+                )}
+
+                {card.supports_duplicate && (
+                  <>
+                    <div>
+                      <div className="inbox-field">
+                        <label>Duplicate action</label>
+                      </div>
+                      <div className="inbox-duplicate-row">
+                        {[
+                          { value: 'dismiss', label: 'Dismiss duplicate' },
+                          { value: 'merge', label: 'Merge to original' },
+                        ].map((option) => (
+                          <button
+                            key={option.value}
+                            type="button"
+                            className={`inbox-toggle ${draft.duplicate_action === option.value ? 'active' : ''}`}
+                            onClick={() => setInboxDraftField(card.id, 'duplicate_action', option.value)}
+                          >
+                            {option.label}
+                          </button>
+                        ))}
+                      </div>
+                    </div>
+                    {draft.duplicate_action === 'merge' && (
+                      <div className="inbox-field">
+                        <label>Original transaction id</label>
+                        <input
+                          value={draft.duplicate_of_txn_id}
+                          onChange={(event) => setInboxDraftField(card.id, 'duplicate_of_txn_id', event.target.value)}
+                          placeholder="txn-9001"
+                        />
+                      </div>
+                    )}
+                  </>
+                )}
+
+                <div className="inbox-footer-row">
+                  <div className="inbox-inline-status">
+                    Route: {card.resolution_target?.route || `/api/v1/inbox/${card.id}`}
+                  </div>
+                  <div className="inbox-footer-actions">
+                    <button className="secondary-action" onClick={clearInboxMessage} disabled={saving}>
+                      Clear banner
+                    </button>
+                    <button className="primary-action" onClick={() => submitInboxAction(card)} disabled={saving}>
+                      {saving ? <RefreshCcw size={16} className="spinner" /> : <CheckCircle2 size={16} />}
+                      Save and clear blocker
+                    </button>
+                  </div>
+                </div>
+              </section>
+            );
+          })}
+        </div>
+      )}
     </div>
   );
 
@@ -2510,11 +2967,11 @@
           <section className="readiness-panel">
             <h5 style={{ marginBottom: 12 }}>Queue Backlog</h5>
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
-                <div key={item.id} className="control-row">
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
+                <div key={item.id || item.affected_entity_id} className="control-row">
                   <div>
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
                   <div className="text-end">
                     <div className="small-muted">{item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date}</div>
@@ -2522,7 +2979,7 @@
                   </div>
                 </div>
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
               )}
             </div>
@@ -2668,7 +3125,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2697,7 +3154,7 @@
   };
 
   const createSnapshotVersion = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     const status = snapshotWorkspace?.snapshot_status;
 
     if (!status?.can_create_snapshot_version) {
@@ -2719,7 +3176,7 @@
         description: `Quarter snapshot generated from Quarter workspace`,
         metadata: {
           source_type: 'manual',
-          readiness_pct: quarterReadiness?.readiness_pct ?? null
+          readiness_pct: activeQuarterReport?.readiness_pct ?? null
         }
       });
       await refreshQuarterWorkspace();
@@ -2734,7 +3191,7 @@
   };
 
   const closeQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(closeReason || '').trim()) {
       setQuarterActionError('Close reason is required.');
       setQuarterActionMessage('');
@@ -2785,7 +3242,7 @@
   };
 
   const reopenQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(reopenReason || '').trim()) {
       setQuarterActionError('Reopen reason is required.');
       setQuarterActionMessage('');
@@ -2844,9 +3301,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3341,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3353,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3364,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3378,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..669173cd6176ee9ee5c86c5895ea49cb9524ebc4
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,155 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, fallbackIssues = [], resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const issueSource = Array.isArray(report.issue_list) && report.issue_list.length > 0
+    ? report.issue_list
+    : fallbackIssues;
+  const allIssues = sortIssues(Array.isArray(issueSource) ? issueSource : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/bizPA/frontend/src/quarterReadiness.test.js b/bizPA/frontend/src/quarterReadiness.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..0b810191a89d8b82091bf5de1408afcc23c14e65
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.test.js
@@ -0,0 +1,93 @@
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
+
+const reportFixture = {
+  quarter_reference: 'Q1-2026',
+  period_start: '2026-01-01',
+  period_end: '2026-03-31',
+  total_txns_in_period: 18,
+  readiness_pct: 78,
+  can_export: false,
+  issue_list: [
+    {
+      issue_type: 'missing_category',
+      affected_entity_id: 'txn-9101',
+      severity: 'high',
+      weight: 35,
+      explanation: 'Category missing for Fuel Stop.',
+      txn_date: '2026-02-04',
+      merchant: 'Fuel Stop',
+      amount: 64.18,
+      direction: 'out',
+      resolution_target: {
+        label: 'Classify transaction',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'missing_business_personal',
+      affected_entity_id: 'txn-9103',
+      severity: 'high',
+      weight: 30,
+      explanation: 'Business/personal decision missing.',
+      txn_date: '2026-02-15',
+      merchant: 'Mobile Contract',
+      amount: 42.5,
+      direction: 'out',
+      resolution_target: {
+        label: 'Set business or personal',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'unresolved_duplicate',
+      affected_entity_id: 'txn-9104',
+      severity: 'medium',
+      weight: 15,
+      explanation: 'Duplicate not resolved.',
+      txn_date: '2026-03-02',
+      merchant: 'Client Payment',
+      amount: 820,
+      direction: 'in',
+      resolution_target: {
+        label: 'Resolve duplicate',
+        workflow: '/inbox/finish-now',
+      },
+    },
+  ],
+};
+
+describe('quarter readiness helpers', () => {
+  test('keeps blockers ordered and updates headline while blockers remain', () => {
+    const state = buildQuarterUiState({ report: reportFixture, resolvedIssueIds: ['txn-9104'] });
+
+    expect(state.itemsLeft).toBe(2);
+    expect(state.exportEnabled).toBe(false);
+    expect(state.report.readiness_pct).toBeGreaterThan(78);
+    expect(state.headline).toContain('items left');
+    expect(state.activeIssues.map((issue) => issue.affected_entity_id)).toEqual(['txn-9101', 'txn-9103']);
+    expect(state.report.explanation_lines[0]).toContain('Readiness is');
+  });
+
+  test('unlocks export immediately when every blocker is resolved', () => {
+    const state = buildQuarterUiState({
+      report: reportFixture,
+      resolvedIssueIds: ['txn-9101', 'txn-9103', 'txn-9104'],
+    });
+
+    expect(state.itemsLeft).toBe(0);
+    expect(state.exportEnabled).toBe(true);
+    expect(state.report.readiness_pct).toBe(100);
+    expect(state.report.can_export).toBe(true);
+    expect(state.report.issue_summary).toEqual([]);
+  });
+
+  test('supports demo query params that resolve all blockers', () => {
+    const params = new URLSearchParams('resolvedDemoIssues=all');
+    const resolvedIds = deriveResolvedIssueIdsFromSearchParams(params, reportFixture.issue_list);
+
+    expect(resolvedIds).toEqual(['txn-9101', 'txn-9103', 'txn-9104']);
+  });
+});
diff --git a/bizPA/start_bizpa_tax_readiness_ui.ps1 b/bizPA/start_bizpa_tax_readiness_ui.ps1
index 1fc4bef8cd172c31fdc3db52d1acc9d4775c5485..7216e44eac386fe436a9af8e9b417cd2d61e37fe
--- a/bizPA/start_bizpa_tax_readiness_ui.ps1
+++ b/bizPA/start_bizpa_tax_readiness_ui.ps1
@@ -4,6 +4,7 @@
 $frontendPath = Join-Path $repoRoot 'frontend'
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
 
 Write-Host 'Starting bizPA tax readiness UI...' -ForegroundColor Cyan
 
@@ -14,7 +15,10 @@
 Write-Host 'Tax readiness dashboard URL:' -ForegroundColor Green
 Write-Host "  $readinessUrl" -ForegroundColor Yellow
 Write-Host ''
+Write-Host 'Export-ready verification URL:' -ForegroundColor Green
+Write-Host "  $exportReadyUrl" -ForegroundColor Yellow
+Write-Host ''
 Write-Host 'Base frontend URL:' -ForegroundColor Green
 Write-Host "  $frontendUrl" -ForegroundColor Yellow
 Write-Host ''
-Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI can be reviewed without backend setup.' -ForegroundColor Cyan
+Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI and zero-blocker export-ready state can be reviewed without backend setup.' -ForegroundColor Cyan
diff --git a/bizPA/tax_readiness_ui_smoke.ps1 b/bizPA/tax_readiness_ui_smoke.ps1
index 56d67892a51b22572503dec560d17123b370c2b7..269c2a3221adfe9095130883c89fd76428a65989
--- a/bizPA/tax_readiness_ui_smoke.ps1
+++ b/bizPA/tax_readiness_ui_smoke.ps1
@@ -28,36 +28,38 @@
 
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
-$screenshotPath = 'C:\Users\edebe\eds\workstream\verification\20260311_201500_bizpa_tax_readiness_ui.png'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
+$verificationRoot = 'C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification'
+$quarterScreenshotPath = Join-Path $verificationRoot 'quarter_readiness_screen.png'
+$finishNowScreenshotPath = Join-Path $verificationRoot 'finish_now_queue_zero_blockers.png'
 $chromeProfilePath = 'C:\Users\edebe\eds\workstream\verification\chrome_capture_profile_tax_readiness'
-
-New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
+$chromePath = 'C:\Program Files\Google\Chrome\Application\chrome.exe'
 
-$frontendJob = Start-Job -ScriptBlock {
-  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
-  $env:PORT = '3002'
-  npm.cmd start
-}
+function Wait-FrontendReady {
+  param (
+    [string]$Url
+  )
 
-try {
-  $frontendResponse = $null
-  $frontendReady = $false
   for ($i = 0; $i -lt 45; $i++) {
     Start-Sleep -Seconds 2
     try {
-      $frontendResponse = Invoke-WebRequest -UseBasicParsing $readinessUrl
-      if ($frontendResponse.StatusCode -eq 200) {
-        $frontendReady = $true
-        break
+      $response = Invoke-WebRequest -UseBasicParsing $Url
+      if ($response.StatusCode -eq 200) {
+        return $response
       }
     } catch {
     }
   }
 
-  if (-not $frontendReady) {
-    throw "Frontend did not become ready on $readinessUrl"
-  }
+  throw "Frontend did not become ready on $Url"
+}
 
+function Capture-ChromeAppScreenshot {
+  param (
+    [string]$Url,
+    [string]$OutputPath
+  )
+
   $chromeArgs = @(
     "--user-data-dir=$chromeProfilePath",
     '--new-window',
@@ -68,9 +70,9 @@
     '--disable-breakpad',
     '--disable-crash-reporter',
     '--noerrdialogs',
-    "--app=$readinessUrl"
+    "--app=$Url"
   )
-  Start-Process 'C:\Program Files\Google\Chrome\Application\chrome.exe' -ArgumentList $chromeArgs | Out-Null
+  Start-Process $chromePath -ArgumentList $chromeArgs | Out-Null
 
   $chromeWindow = $null
   for ($i = 0; $i -lt 20; $i++) {
@@ -96,7 +98,7 @@
   }
 
   if (-not $chromeWindow) {
-    throw 'Chrome readiness window did not expose a main window handle for screenshot capture.'
+    throw "Chrome window did not expose a main window handle for $Url"
   }
 
   [WindowCaptureNative]::ShowWindow($chromeWindow.MainWindowHandle, 3) | Out-Null
@@ -110,17 +112,34 @@
   $bitmap = New-Object System.Drawing.Bitmap $width, $height
   $graphics = [System.Drawing.Graphics]::FromImage($bitmap)
   $graphics.CopyFromScreen($rect.Left, $rect.Top, 0, 0, $bitmap.Size)
-  $bitmap.Save($screenshotPath, [System.Drawing.Imaging.ImageFormat]::Png)
+  $bitmap.Save($OutputPath, [System.Drawing.Imaging.ImageFormat]::Png)
   $graphics.Dispose()
   $bitmap.Dispose()
 
   Get-Process chrome -ErrorAction SilentlyContinue |
     Where-Object { $_.StartTime -ge $chromeWindow.StartTime.AddSeconds(-2) } |
     Stop-Process -Force -ErrorAction SilentlyContinue
+}
+
+New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
+New-Item -ItemType Directory -Force -Path $verificationRoot | Out-Null
+
+$frontendJob = Start-Job -ScriptBlock {
+  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
+  $env:PORT = '3002'
+  npm.cmd start
+}
+
+try {
+  $frontendResponse = Wait-FrontendReady -Url $readinessUrl
+  Capture-ChromeAppScreenshot -Url $readinessUrl -OutputPath $quarterScreenshotPath
+  Capture-ChromeAppScreenshot -Url $exportReadyUrl -OutputPath $finishNowScreenshotPath
 
   "READINESS_URL=$readinessUrl"
+  "EXPORT_READY_URL=$exportReadyUrl"
   "FRONTEND_STATUS=$($frontendResponse.StatusCode)"
-  "SCREENSHOT=$screenshotPath"
+  "SCREENSHOT_QUARTER=$quarterScreenshotPath"
+  "SCREENSHOT_EXPORT_READY=$finishNowScreenshotPath"
   'FRONTEND_LOG_START'
   Receive-Job $frontendJob -Keep -ErrorAction SilentlyContinue 2>&1
   'FRONTEND_LOG_END'
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "powershell -ExecutionPolicy Bypass -File 'C:\\Users\\edebe\\eds\\bizPA\\tax_readiness_ui_smoke.ps1'" in C:\Users\edebe\eds exited 1 in 1m 06s:
Chrome window did not expose a main window handle for 
http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all
At C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1:101 char:5
+     throw "Chrome window did not expose a main window handle for $Url ...
+     ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : OperationStopped: (Chrome window d...dDemoIssues=all:String) [], RuntimeException
    + FullyQualifiedErrorId : Chrome window did not expose a main window handle for http://127.0.0.1:3002/?readinessDe 
   mo=1&tab=quarter&resolvedDemoIssues=all
 
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..b0b6a961dcb80123d7634a661b59a1e944c1c7a1
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,26 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    fallbackIssues: inboxItems,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1532,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1547,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1633,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2104,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2316,29 +2562,241 @@
     </div>
   );
 
-  const renderInbox = () => (
-    <div>
-      <h4>Finish Now Queue</h4>
-      <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
-      <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
-            <div>
-              <b>{item.merchant || 'Unknown'}</b>
-              <br />
-              <small>
-                {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
-              </small>
-              <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
-            </div>
+  const renderInbox = () => {
+    return (
+    <div className="inbox-mobile-shell">
+      <section className="inbox-mobile-hero">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2">
+          <div className="inbox-kicker">
+            <Bell size={14} />
+            Mobile-first exception queue
           </div>
-        ))}
-      </div>
-    </div>
-  );
+          <div className="status-pill">{filteredInboxCards.length} visible</div>
+        </div>
+        <h4>Finish now</h4>
+        <p>
+          Clear the blockers that stop the quarterly export: category, business or personal, split treatment, and duplicate resolution.
+        </p>
+        <div className="inbox-mobile-stats">
+          <div className="inbox-mobile-stat">
+            <small>Queue</small>
+            <strong>{inboxCards.length}</strong>
+            <small>{activeQuarterReport.quarter_reference || READINESS_DEMO_REPORT.quarter_reference}</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Critical</small>
+            <strong>{highSeverityCount}</strong>
+            <small>High-severity blockers</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Ready now</small>
+            <strong>{readyToExportCount}</strong>
+            <small>Transactions already export-safe</small>
+          </div>
+        </div>
+        <div className="hero-actions">
+          <button className="hero-action-main" onClick={() => setCurrentTab('quarter')}>
+            <ArrowRight size={16} />
+            Review quarter readiness
+          </button>
+          <button className="hero-action-secondary" onClick={undoLastInboxAction} disabled={inboxBusyId === 'undo-last'}>
+            {inboxBusyId === 'undo-last' ? <RefreshCcw size={16} className="spinner" /> : <ArchiveRestore size={16} />}
+            Undo last action
+          </button>
+        </div>
+      </section>
+
+      {inboxMessage && (
+        <div className={`readiness-panel inbox-message ${inboxMessage.type}`}>
+          {inboxMessage.text}
+        </div>
+      )}
+
+      <section className="readiness-panel">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-3">
+          <div>
+            <h5 style={{ marginBottom: 4 }}>Queue filters</h5>
+            <small className="small-muted">Keep the mobile pass focused on one blocker type at a time.</small>
+          </div>
+          <div className="status-pill">{activeQuarterReport.readiness_pct ?? READINESS_DEMO_REPORT.readiness_pct}% ready</div>
+        </div>
+        <div className="inbox-filter-row">
+          {INBOX_FILTERS.map((filter) => {
+            const count = filter.key === 'all'
+              ? inboxCards.length
+              : inboxCards.filter((card) => card.issue_types.includes(filter.key)).length;
+            return (
+              <button
+                key={filter.key}
+                type="button"
+                className={`inbox-filter-chip ${inboxFilter === filter.key ? 'active' : ''}`}
+                onClick={() => setInboxFilter(filter.key)}
+              >
+                <span>{filter.label}</span>
+                <small>{count}</small>
+              </button>
+            );
+          })}
+        </div>
+      </section>
+
+      {filteredInboxCards.length === 0 ? (
+        <div className="inbox-empty">
+          No blockers match this filter. Reset to All blockers or return to the quarter screen to confirm readiness.
+        </div>
+      ) : (
+        <div className="inbox-card-list">
+          {filteredInboxCards.map((card) => {
+            const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+            const saving = inboxBusyId === card.id;
+            const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+            return (
+              <section key={card.id} className="inbox-action-card">
+                <div className="inbox-card-top">
+                  <div>
+                    <strong>{card.merchant}</strong>
+                    <div className="inbox-card-subline">
+                      {card.txn_date ? String(card.txn_date).slice(0, 10) : 'Date pending'} · {formatIssueAmount(card.amount, card.direction)}
+                    </div>
+                  </div>
+                  <span className={`inbox-issue-chip ${card.severity}`}>
+                    <TriangleAlert size={14} />
+                    {INBOX_REASON_LABELS[card.blocker_reason] || card.blocker_reason.replace(/_/g, ' ')}
+                  </span>
+                </div>
+
+                <div className="inbox-chip-row">
+                  {card.issue_labels.map((label) => (
+                    <span key={`${card.id}-${label}`} className={`inbox-issue-chip ${card.severity}`}>
+                      {label}
+                    </span>
+                  ))}
+                  {card.suggested_category?.category_name && (
+                    <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
+                      <CheckCircle2 size={14} />
+                      Suggested: {card.suggested_category.category_name}
+                    </button>
+                  )}
+                </div>
+
+                <p className="inbox-explainer">{card.explanation || 'Resolve the blocking fields to clear this transaction from the queue.'}</p>
+
+                {(card.supports_category || card.supports_business_personal || card.supports_split) && (
+                  <div className="inbox-action-grid">
+                    <div className="inbox-field">
+                      <label>Category code</label>
+                      <input
+                        value={draft.category_code}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_code', event.target.value)}
+                        placeholder="MATERIALS"
+                      />
+                    </div>
+                    <div className="inbox-field">
+                      <label>Category label</label>
+                      <input
+                        value={draft.category_name}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_name', event.target.value)}
+                        placeholder="Materials"
+                      />
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_business_personal || card.supports_split) && (
+                  <div>
+                    <div className="inbox-field">
+                      <label>Business treatment</label>
+                    </div>
+                    <div className="inbox-toggle-row">
+                      {[
+                        { value: 'BUSINESS', label: 'Business' },
+                        { value: 'PERSONAL', label: 'Personal' },
+                        { value: 'SPLIT', label: 'Split' },
+                      ].map((option) => (
+                        <button
+                          key={option.value}
+                          type="button"
+                          className={`inbox-toggle ${draft.business_personal === option.value ? 'active' : ''}`}
+                          onClick={() => setInboxDraftField(card.id, 'business_personal', option.value)}
+                        >
+                          {option.label}
+                        </button>
+                      ))}
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_split || isSplit) && (
+                  <div className="inbox-field">
+                    <label>Business split %</label>
+                    <input
+                      type="number"
+                      min="0"
+                      max="100"
+                      value={draft.split_business_pct}
+                      onChange={(event) => setInboxDraftField(card.id, 'split_business_pct', event.target.value)}
+                      placeholder="80"
+                    />
+                  </div>
+                )}
+
+                {card.supports_duplicate && (
+                  <>
+                    <div>
+                      <div className="inbox-field">
+                        <label>Duplicate action</label>
+                      </div>
+                      <div className="inbox-duplicate-row">
+                        {[
+                          { value: 'dismiss', label: 'Dismiss duplicate' },
+                          { value: 'merge', label: 'Merge to original' },
+                        ].map((option) => (
+                          <button
+                            key={option.value}
+                            type="button"
+                            className={`inbox-toggle ${draft.duplicate_action === option.value ? 'active' : ''}`}
+                            onClick={() => setInboxDraftField(card.id, 'duplicate_action', option.value)}
+                          >
+                            {option.label}
+                          </button>
+                        ))}
+                      </div>
+                    </div>
+                    {draft.duplicate_action === 'merge' && (
+                      <div className="inbox-field">
+                        <label>Original transaction id</label>
+                        <input
+                          value={draft.duplicate_of_txn_id}
+                          onChange={(event) => setInboxDraftField(card.id, 'duplicate_of_txn_id', event.target.value)}
+                          placeholder="txn-9001"
+                        />
+                      </div>
+                    )}
+                  </>
+                )}
 
+                <div className="inbox-footer-row">
+                  <div className="inbox-inline-status">
+                    Route: {card.resolution_target?.route || `/api/v1/inbox/${card.id}`}
+                  </div>
+                  <div className="inbox-footer-actions">
+                    <button className="secondary-action" onClick={clearInboxMessage} disabled={saving}>
+                      Clear banner
+                    </button>
+                    <button className="primary-action" onClick={() => submitInboxAction(card)} disabled={saving}>
+                      {saving ? <RefreshCcw size={16} className="spinner" /> : <CheckCircle2 size={16} />}
+                      Save and clear blocker
+                    </button>
+                  </div>
+                </div>
+              </section>
+            );
+          })}
+        </div>
+      )}
+    </div>
+    );
+  };
   const renderControlCentre = () => {
     const policyLabels = {
       vat_scheme: 'VAT scheme',
@@ -2510,11 +2968,11 @@
           <section className="readiness-panel">
             <h5 style={{ marginBottom: 12 }}>Queue Backlog</h5>
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
-                <div key={item.id} className="control-row">
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
+                <div key={item.id || item.affected_entity_id} className="control-row">
                   <div>
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
                   <div className="text-end">
                     <div className="small-muted">{item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date}</div>
@@ -2522,7 +2980,7 @@
                   </div>
                 </div>
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
               )}
             </div>
@@ -2668,7 +3126,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2697,7 +3155,7 @@
   };
 
   const createSnapshotVersion = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     const status = snapshotWorkspace?.snapshot_status;
 
     if (!status?.can_create_snapshot_version) {
@@ -2719,7 +3177,7 @@
         description: `Quarter snapshot generated from Quarter workspace`,
         metadata: {
           source_type: 'manual',
-          readiness_pct: quarterReadiness?.readiness_pct ?? null
+          readiness_pct: activeQuarterReport?.readiness_pct ?? null
         }
       });
       await refreshQuarterWorkspace();
@@ -2734,7 +3192,7 @@
   };
 
   const closeQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(closeReason || '').trim()) {
       setQuarterActionError('Close reason is required.');
       setQuarterActionMessage('');
@@ -2785,7 +3243,7 @@
   };
 
   const reopenQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(reopenReason || '').trim()) {
       setQuarterActionError('Reopen reason is required.');
       setQuarterActionMessage('');
@@ -2844,9 +3302,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3342,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3354,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3365,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3379,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..669173cd6176ee9ee5c86c5895ea49cb9524ebc4
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,155 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, fallbackIssues = [], resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const issueSource = Array.isArray(report.issue_list) && report.issue_list.length > 0
+    ? report.issue_list
+    : fallbackIssues;
+  const allIssues = sortIssues(Array.isArray(issueSource) ? issueSource : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/bizPA/frontend/src/quarterReadiness.test.js b/bizPA/frontend/src/quarterReadiness.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..0b810191a89d8b82091bf5de1408afcc23c14e65
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.test.js
@@ -0,0 +1,93 @@
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
+
+const reportFixture = {
+  quarter_reference: 'Q1-2026',
+  period_start: '2026-01-01',
+  period_end: '2026-03-31',
+  total_txns_in_period: 18,
+  readiness_pct: 78,
+  can_export: false,
+  issue_list: [
+    {
+      issue_type: 'missing_category',
+      affected_entity_id: 'txn-9101',
+      severity: 'high',
+      weight: 35,
+      explanation: 'Category missing for Fuel Stop.',
+      txn_date: '2026-02-04',
+      merchant: 'Fuel Stop',
+      amount: 64.18,
+      direction: 'out',
+      resolution_target: {
+        label: 'Classify transaction',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'missing_business_personal',
+      affected_entity_id: 'txn-9103',
+      severity: 'high',
+      weight: 30,
+      explanation: 'Business/personal decision missing.',
+      txn_date: '2026-02-15',
+      merchant: 'Mobile Contract',
+      amount: 42.5,
+      direction: 'out',
+      resolution_target: {
+        label: 'Set business or personal',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'unresolved_duplicate',
+      affected_entity_id: 'txn-9104',
+      severity: 'medium',
+      weight: 15,
+      explanation: 'Duplicate not resolved.',
+      txn_date: '2026-03-02',
+      merchant: 'Client Payment',
+      amount: 820,
+      direction: 'in',
+      resolution_target: {
+        label: 'Resolve duplicate',
+        workflow: '/inbox/finish-now',
+      },
+    },
+  ],
+};
+
+describe('quarter readiness helpers', () => {
+  test('keeps blockers ordered and updates headline while blockers remain', () => {
+    const state = buildQuarterUiState({ report: reportFixture, resolvedIssueIds: ['txn-9104'] });
+
+    expect(state.itemsLeft).toBe(2);
+    expect(state.exportEnabled).toBe(false);
+    expect(state.report.readiness_pct).toBeGreaterThan(78);
+    expect(state.headline).toContain('items left');
+    expect(state.activeIssues.map((issue) => issue.affected_entity_id)).toEqual(['txn-9101', 'txn-9103']);
+    expect(state.report.explanation_lines[0]).toContain('Readiness is');
+  });
+
+  test('unlocks export immediately when every blocker is resolved', () => {
+    const state = buildQuarterUiState({
+      report: reportFixture,
+      resolvedIssueIds: ['txn-9101', 'txn-9103', 'txn-9104'],
+    });
+
+    expect(state.itemsLeft).toBe(0);
+    expect(state.exportEnabled).toBe(true);
+    expect(state.report.readiness_pct).toBe(100);
+    expect(state.report.can_export).toBe(true);
+    expect(state.report.issue_summary).toEqual([]);
+  });
+
+  test('supports demo query params that resolve all blockers', () => {
+    const params = new URLSearchParams('resolvedDemoIssues=all');
+    const resolvedIds = deriveResolvedIssueIdsFromSearchParams(params, reportFixture.issue_list);
+
+    expect(resolvedIds).toEqual(['txn-9101', 'txn-9103', 'txn-9104']);
+  });
+});
diff --git a/bizPA/start_bizpa_tax_readiness_ui.ps1 b/bizPA/start_bizpa_tax_readiness_ui.ps1
index 1fc4bef8cd172c31fdc3db52d1acc9d4775c5485..7216e44eac386fe436a9af8e9b417cd2d61e37fe
--- a/bizPA/start_bizpa_tax_readiness_ui.ps1
+++ b/bizPA/start_bizpa_tax_readiness_ui.ps1
@@ -4,6 +4,7 @@
 $frontendPath = Join-Path $repoRoot 'frontend'
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
 
 Write-Host 'Starting bizPA tax readiness UI...' -ForegroundColor Cyan
 
@@ -14,7 +15,10 @@
 Write-Host 'Tax readiness dashboard URL:' -ForegroundColor Green
 Write-Host "  $readinessUrl" -ForegroundColor Yellow
 Write-Host ''
+Write-Host 'Export-ready verification URL:' -ForegroundColor Green
+Write-Host "  $exportReadyUrl" -ForegroundColor Yellow
+Write-Host ''
 Write-Host 'Base frontend URL:' -ForegroundColor Green
 Write-Host "  $frontendUrl" -ForegroundColor Yellow
 Write-Host ''
-Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI can be reviewed without backend setup.' -ForegroundColor Cyan
+Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI and zero-blocker export-ready state can be reviewed without backend setup.' -ForegroundColor Cyan
diff --git a/bizPA/tax_readiness_ui_smoke.ps1 b/bizPA/tax_readiness_ui_smoke.ps1
index 56d67892a51b22572503dec560d17123b370c2b7..269c2a3221adfe9095130883c89fd76428a65989
--- a/bizPA/tax_readiness_ui_smoke.ps1
+++ b/bizPA/tax_readiness_ui_smoke.ps1
@@ -28,36 +28,38 @@
 
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
-$screenshotPath = 'C:\Users\edebe\eds\workstream\verification\20260311_201500_bizpa_tax_readiness_ui.png'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
+$verificationRoot = 'C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification'
+$quarterScreenshotPath = Join-Path $verificationRoot 'quarter_readiness_screen.png'
+$finishNowScreenshotPath = Join-Path $verificationRoot 'finish_now_queue_zero_blockers.png'
 $chromeProfilePath = 'C:\Users\edebe\eds\workstream\verification\chrome_capture_profile_tax_readiness'
-
-New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
+$chromePath = 'C:\Program Files\Google\Chrome\Application\chrome.exe'
 
-$frontendJob = Start-Job -ScriptBlock {
-  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
-  $env:PORT = '3002'
-  npm.cmd start
-}
+function Wait-FrontendReady {
+  param (
+    [string]$Url
+  )
 
-try {
-  $frontendResponse = $null
-  $frontendReady = $false
   for ($i = 0; $i -lt 45; $i++) {
     Start-Sleep -Seconds 2
     try {
-      $frontendResponse = Invoke-WebRequest -UseBasicParsing $readinessUrl
-      if ($frontendResponse.StatusCode -eq 200) {
-        $frontendReady = $true
-        break
+      $response = Invoke-WebRequest -UseBasicParsing $Url
+      if ($response.StatusCode -eq 200) {
+        return $response
       }
     } catch {
     }
   }
 
-  if (-not $frontendReady) {
-    throw "Frontend did not become ready on $readinessUrl"
-  }
+  throw "Frontend did not become ready on $Url"
+}
 
+function Capture-ChromeAppScreenshot {
+  param (
+    [string]$Url,
+    [string]$OutputPath
+  )
+
   $chromeArgs = @(
     "--user-data-dir=$chromeProfilePath",
     '--new-window',
@@ -68,9 +70,9 @@
     '--disable-breakpad',
     '--disable-crash-reporter',
     '--noerrdialogs',
-    "--app=$readinessUrl"
+    "--app=$Url"
   )
-  Start-Process 'C:\Program Files\Google\Chrome\Application\chrome.exe' -ArgumentList $chromeArgs | Out-Null
+  Start-Process $chromePath -ArgumentList $chromeArgs | Out-Null
 
   $chromeWindow = $null
   for ($i = 0; $i -lt 20; $i++) {
@@ -96,7 +98,7 @@
   }
 
   if (-not $chromeWindow) {
-    throw 'Chrome readiness window did not expose a main window handle for screenshot capture.'
+    throw "Chrome window did not expose a main window handle for $Url"
   }
 
   [WindowCaptureNative]::ShowWindow($chromeWindow.MainWindowHandle, 3) | Out-Null
@@ -110,17 +112,34 @@
   $bitmap = New-Object System.Drawing.Bitmap $width, $height
   $graphics = [System.Drawing.Graphics]::FromImage($bitmap)
   $graphics.CopyFromScreen($rect.Left, $rect.Top, 0, 0, $bitmap.Size)
-  $bitmap.Save($screenshotPath, [System.Drawing.Imaging.ImageFormat]::Png)
+  $bitmap.Save($OutputPath, [System.Drawing.Imaging.ImageFormat]::Png)
   $graphics.Dispose()
   $bitmap.Dispose()
 
   Get-Process chrome -ErrorAction SilentlyContinue |
     Where-Object { $_.StartTime -ge $chromeWindow.StartTime.AddSeconds(-2) } |
     Stop-Process -Force -ErrorAction SilentlyContinue
+}
+
+New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
+New-Item -ItemType Directory -Force -Path $verificationRoot | Out-Null
+
+$frontendJob = Start-Job -ScriptBlock {
+  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
+  $env:PORT = '3002'
+  npm.cmd start
+}
+
+try {
+  $frontendResponse = Wait-FrontendReady -Url $readinessUrl
+  Capture-ChromeAppScreenshot -Url $readinessUrl -OutputPath $quarterScreenshotPath
+  Capture-ChromeAppScreenshot -Url $exportReadyUrl -OutputPath $finishNowScreenshotPath
 
   "READINESS_URL=$readinessUrl"
+  "EXPORT_READY_URL=$exportReadyUrl"
   "FRONTEND_STATUS=$($frontendResponse.StatusCode)"
-  "SCREENSHOT=$screenshotPath"
+  "SCREENSHOT_QUARTER=$quarterScreenshotPath"
+  "SCREENSHOT_EXPORT_READY=$finishNowScreenshotPath"
   'FRONTEND_LOG_START'
   Receive-Job $frontendJob -Keep -ErrorAction SilentlyContinue 2>&1
   'FRONTEND_LOG_END'
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

codex
The automated smoke run surfaced one issue in the capture harness, not the app itself: the second Chrome launch reused the same temporary profile and never surfaced a new top-level window handle. I’m fixing that capture script now and rerunning the smoke path so the screenshot evidence is still captured cleanly.
file update
M C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1
@@ -62,4 +62,7 @@
 
+  $captureProfilePath = Join-Path $chromeProfilePath ([System.IO.Path]::GetFileNameWithoutExtension($OutputPath))
+  New-Item -ItemType Directory -Force -Path $captureProfilePath | Out-Null
+
   $chromeArgs = @(
-    "--user-data-dir=$chromeProfilePath",
+    "--user-data-dir=$captureProfilePath",
     '--new-window',
@@ -121,2 +124,3 @@
     Stop-Process -Force -ErrorAction SilentlyContinue
+  Start-Sleep -Seconds 2
 }
apply_patch(auto_approved=true) exited 0 in 640ms:
Success. Updated the following files:
M C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..b0b6a961dcb80123d7634a661b59a1e944c1c7a1
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,26 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    fallbackIssues: inboxItems,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1532,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1547,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1633,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2104,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2316,29 +2562,241 @@
     </div>
   );
 
-  const renderInbox = () => (
-    <div>
-      <h4>Finish Now Queue</h4>
-      <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
-      <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
-            <div>
-              <b>{item.merchant || 'Unknown'}</b>
-              <br />
-              <small>
-                {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
-              </small>
-              <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
-            </div>
+  const renderInbox = () => {
+    return (
+    <div className="inbox-mobile-shell">
+      <section className="inbox-mobile-hero">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2">
+          <div className="inbox-kicker">
+            <Bell size={14} />
+            Mobile-first exception queue
           </div>
-        ))}
-      </div>
-    </div>
-  );
+          <div className="status-pill">{filteredInboxCards.length} visible</div>
+        </div>
+        <h4>Finish now</h4>
+        <p>
+          Clear the blockers that stop the quarterly export: category, business or personal, split treatment, and duplicate resolution.
+        </p>
+        <div className="inbox-mobile-stats">
+          <div className="inbox-mobile-stat">
+            <small>Queue</small>
+            <strong>{inboxCards.length}</strong>
+            <small>{activeQuarterReport.quarter_reference || READINESS_DEMO_REPORT.quarter_reference}</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Critical</small>
+            <strong>{highSeverityCount}</strong>
+            <small>High-severity blockers</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Ready now</small>
+            <strong>{readyToExportCount}</strong>
+            <small>Transactions already export-safe</small>
+          </div>
+        </div>
+        <div className="hero-actions">
+          <button className="hero-action-main" onClick={() => setCurrentTab('quarter')}>
+            <ArrowRight size={16} />
+            Review quarter readiness
+          </button>
+          <button className="hero-action-secondary" onClick={undoLastInboxAction} disabled={inboxBusyId === 'undo-last'}>
+            {inboxBusyId === 'undo-last' ? <RefreshCcw size={16} className="spinner" /> : <ArchiveRestore size={16} />}
+            Undo last action
+          </button>
+        </div>
+      </section>
+
+      {inboxMessage && (
+        <div className={`readiness-panel inbox-message ${inboxMessage.type}`}>
+          {inboxMessage.text}
+        </div>
+      )}
+
+      <section className="readiness-panel">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-3">
+          <div>
+            <h5 style={{ marginBottom: 4 }}>Queue filters</h5>
+            <small className="small-muted">Keep the mobile pass focused on one blocker type at a time.</small>
+          </div>
+          <div className="status-pill">{activeQuarterReport.readiness_pct ?? READINESS_DEMO_REPORT.readiness_pct}% ready</div>
+        </div>
+        <div className="inbox-filter-row">
+          {INBOX_FILTERS.map((filter) => {
+            const count = filter.key === 'all'
+              ? inboxCards.length
+              : inboxCards.filter((card) => card.issue_types.includes(filter.key)).length;
+            return (
+              <button
+                key={filter.key}
+                type="button"
+                className={`inbox-filter-chip ${inboxFilter === filter.key ? 'active' : ''}`}
+                onClick={() => setInboxFilter(filter.key)}
+              >
+                <span>{filter.label}</span>
+                <small>{count}</small>
+              </button>
+            );
+          })}
+        </div>
+      </section>
+
+      {filteredInboxCards.length === 0 ? (
+        <div className="inbox-empty">
+          No blockers match this filter. Reset to All blockers or return to the quarter screen to confirm readiness.
+        </div>
+      ) : (
+        <div className="inbox-card-list">
+          {filteredInboxCards.map((card) => {
+            const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+            const saving = inboxBusyId === card.id;
+            const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+            return (
+              <section key={card.id} className="inbox-action-card">
+                <div className="inbox-card-top">
+                  <div>
+                    <strong>{card.merchant}</strong>
+                    <div className="inbox-card-subline">
+                      {card.txn_date ? String(card.txn_date).slice(0, 10) : 'Date pending'} · {formatIssueAmount(card.amount, card.direction)}
+                    </div>
+                  </div>
+                  <span className={`inbox-issue-chip ${card.severity}`}>
+                    <TriangleAlert size={14} />
+                    {INBOX_REASON_LABELS[card.blocker_reason] || card.blocker_reason.replace(/_/g, ' ')}
+                  </span>
+                </div>
+
+                <div className="inbox-chip-row">
+                  {card.issue_labels.map((label) => (
+                    <span key={`${card.id}-${label}`} className={`inbox-issue-chip ${card.severity}`}>
+                      {label}
+                    </span>
+                  ))}
+                  {card.suggested_category?.category_name && (
+                    <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
+                      <CheckCircle2 size={14} />
+                      Suggested: {card.suggested_category.category_name}
+                    </button>
+                  )}
+                </div>
+
+                <p className="inbox-explainer">{card.explanation || 'Resolve the blocking fields to clear this transaction from the queue.'}</p>
+
+                {(card.supports_category || card.supports_business_personal || card.supports_split) && (
+                  <div className="inbox-action-grid">
+                    <div className="inbox-field">
+                      <label>Category code</label>
+                      <input
+                        value={draft.category_code}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_code', event.target.value)}
+                        placeholder="MATERIALS"
+                      />
+                    </div>
+                    <div className="inbox-field">
+                      <label>Category label</label>
+                      <input
+                        value={draft.category_name}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_name', event.target.value)}
+                        placeholder="Materials"
+                      />
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_business_personal || card.supports_split) && (
+                  <div>
+                    <div className="inbox-field">
+                      <label>Business treatment</label>
+                    </div>
+                    <div className="inbox-toggle-row">
+                      {[
+                        { value: 'BUSINESS', label: 'Business' },
+                        { value: 'PERSONAL', label: 'Personal' },
+                        { value: 'SPLIT', label: 'Split' },
+                      ].map((option) => (
+                        <button
+                          key={option.value}
+                          type="button"
+                          className={`inbox-toggle ${draft.business_personal === option.value ? 'active' : ''}`}
+                          onClick={() => setInboxDraftField(card.id, 'business_personal', option.value)}
+                        >
+                          {option.label}
+                        </button>
+                      ))}
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_split || isSplit) && (
+                  <div className="inbox-field">
+                    <label>Business split %</label>
+                    <input
+                      type="number"
+                      min="0"
+                      max="100"
+                      value={draft.split_business_pct}
+                      onChange={(event) => setInboxDraftField(card.id, 'split_business_pct', event.target.value)}
+                      placeholder="80"
+                    />
+                  </div>
+                )}
+
+                {card.supports_duplicate && (
+                  <>
+                    <div>
+                      <div className="inbox-field">
+                        <label>Duplicate action</label>
+                      </div>
+                      <div className="inbox-duplicate-row">
+                        {[
+                          { value: 'dismiss', label: 'Dismiss duplicate' },
+                          { value: 'merge', label: 'Merge to original' },
+                        ].map((option) => (
+                          <button
+                            key={option.value}
+                            type="button"
+                            className={`inbox-toggle ${draft.duplicate_action === option.value ? 'active' : ''}`}
+                            onClick={() => setInboxDraftField(card.id, 'duplicate_action', option.value)}
+                          >
+                            {option.label}
+                          </button>
+                        ))}
+                      </div>
+                    </div>
+                    {draft.duplicate_action === 'merge' && (
+                      <div className="inbox-field">
+                        <label>Original transaction id</label>
+                        <input
+                          value={draft.duplicate_of_txn_id}
+                          onChange={(event) => setInboxDraftField(card.id, 'duplicate_of_txn_id', event.target.value)}
+                          placeholder="txn-9001"
+                        />
+                      </div>
+                    )}
+                  </>
+                )}
 
+                <div className="inbox-footer-row">
+                  <div className="inbox-inline-status">
+                    Route: {card.resolution_target?.route || `/api/v1/inbox/${card.id}`}
+                  </div>
+                  <div className="inbox-footer-actions">
+                    <button className="secondary-action" onClick={clearInboxMessage} disabled={saving}>
+                      Clear banner
+                    </button>
+                    <button className="primary-action" onClick={() => submitInboxAction(card)} disabled={saving}>
+                      {saving ? <RefreshCcw size={16} className="spinner" /> : <CheckCircle2 size={16} />}
+                      Save and clear blocker
+                    </button>
+                  </div>
+                </div>
+              </section>
+            );
+          })}
+        </div>
+      )}
+    </div>
+    );
+  };
   const renderControlCentre = () => {
     const policyLabels = {
       vat_scheme: 'VAT scheme',
@@ -2510,11 +2968,11 @@
           <section className="readiness-panel">
             <h5 style={{ marginBottom: 12 }}>Queue Backlog</h5>
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
-                <div key={item.id} className="control-row">
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
+                <div key={item.id || item.affected_entity_id} className="control-row">
                   <div>
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
                   <div className="text-end">
                     <div className="small-muted">{item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date}</div>
@@ -2522,7 +2980,7 @@
                   </div>
                 </div>
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
               )}
             </div>
@@ -2668,7 +3126,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2697,7 +3155,7 @@
   };
 
   const createSnapshotVersion = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     const status = snapshotWorkspace?.snapshot_status;
 
     if (!status?.can_create_snapshot_version) {
@@ -2719,7 +3177,7 @@
         description: `Quarter snapshot generated from Quarter workspace`,
         metadata: {
           source_type: 'manual',
-          readiness_pct: quarterReadiness?.readiness_pct ?? null
+          readiness_pct: activeQuarterReport?.readiness_pct ?? null
         }
       });
       await refreshQuarterWorkspace();
@@ -2734,7 +3192,7 @@
   };
 
   const closeQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(closeReason || '').trim()) {
       setQuarterActionError('Close reason is required.');
       setQuarterActionMessage('');
@@ -2785,7 +3243,7 @@
   };
 
   const reopenQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(reopenReason || '').trim()) {
       setQuarterActionError('Reopen reason is required.');
       setQuarterActionMessage('');
@@ -2844,9 +3302,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3342,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3354,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3365,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3379,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..669173cd6176ee9ee5c86c5895ea49cb9524ebc4
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,155 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, fallbackIssues = [], resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const issueSource = Array.isArray(report.issue_list) && report.issue_list.length > 0
+    ? report.issue_list
+    : fallbackIssues;
+  const allIssues = sortIssues(Array.isArray(issueSource) ? issueSource : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/bizPA/frontend/src/quarterReadiness.test.js b/bizPA/frontend/src/quarterReadiness.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..0b810191a89d8b82091bf5de1408afcc23c14e65
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.test.js
@@ -0,0 +1,93 @@
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
+
+const reportFixture = {
+  quarter_reference: 'Q1-2026',
+  period_start: '2026-01-01',
+  period_end: '2026-03-31',
+  total_txns_in_period: 18,
+  readiness_pct: 78,
+  can_export: false,
+  issue_list: [
+    {
+      issue_type: 'missing_category',
+      affected_entity_id: 'txn-9101',
+      severity: 'high',
+      weight: 35,
+      explanation: 'Category missing for Fuel Stop.',
+      txn_date: '2026-02-04',
+      merchant: 'Fuel Stop',
+      amount: 64.18,
+      direction: 'out',
+      resolution_target: {
+        label: 'Classify transaction',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'missing_business_personal',
+      affected_entity_id: 'txn-9103',
+      severity: 'high',
+      weight: 30,
+      explanation: 'Business/personal decision missing.',
+      txn_date: '2026-02-15',
+      merchant: 'Mobile Contract',
+      amount: 42.5,
+      direction: 'out',
+      resolution_target: {
+        label: 'Set business or personal',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'unresolved_duplicate',
+      affected_entity_id: 'txn-9104',
+      severity: 'medium',
+      weight: 15,
+      explanation: 'Duplicate not resolved.',
+      txn_date: '2026-03-02',
+      merchant: 'Client Payment',
+      amount: 820,
+      direction: 'in',
+      resolution_target: {
+        label: 'Resolve duplicate',
+        workflow: '/inbox/finish-now',
+      },
+    },
+  ],
+};
+
+describe('quarter readiness helpers', () => {
+  test('keeps blockers ordered and updates headline while blockers remain', () => {
+    const state = buildQuarterUiState({ report: reportFixture, resolvedIssueIds: ['txn-9104'] });
+
+    expect(state.itemsLeft).toBe(2);
+    expect(state.exportEnabled).toBe(false);
+    expect(state.report.readiness_pct).toBeGreaterThan(78);
+    expect(state.headline).toContain('items left');
+    expect(state.activeIssues.map((issue) => issue.affected_entity_id)).toEqual(['txn-9101', 'txn-9103']);
+    expect(state.report.explanation_lines[0]).toContain('Readiness is');
+  });
+
+  test('unlocks export immediately when every blocker is resolved', () => {
+    const state = buildQuarterUiState({
+      report: reportFixture,
+      resolvedIssueIds: ['txn-9101', 'txn-9103', 'txn-9104'],
+    });
+
+    expect(state.itemsLeft).toBe(0);
+    expect(state.exportEnabled).toBe(true);
+    expect(state.report.readiness_pct).toBe(100);
+    expect(state.report.can_export).toBe(true);
+    expect(state.report.issue_summary).toEqual([]);
+  });
+
+  test('supports demo query params that resolve all blockers', () => {
+    const params = new URLSearchParams('resolvedDemoIssues=all');
+    const resolvedIds = deriveResolvedIssueIdsFromSearchParams(params, reportFixture.issue_list);
+
+    expect(resolvedIds).toEqual(['txn-9101', 'txn-9103', 'txn-9104']);
+  });
+});
diff --git a/bizPA/start_bizpa_tax_readiness_ui.ps1 b/bizPA/start_bizpa_tax_readiness_ui.ps1
index 1fc4bef8cd172c31fdc3db52d1acc9d4775c5485..7216e44eac386fe436a9af8e9b417cd2d61e37fe
--- a/bizPA/start_bizpa_tax_readiness_ui.ps1
+++ b/bizPA/start_bizpa_tax_readiness_ui.ps1
@@ -4,6 +4,7 @@
 $frontendPath = Join-Path $repoRoot 'frontend'
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
 
 Write-Host 'Starting bizPA tax readiness UI...' -ForegroundColor Cyan
 
@@ -14,7 +15,10 @@
 Write-Host 'Tax readiness dashboard URL:' -ForegroundColor Green
 Write-Host "  $readinessUrl" -ForegroundColor Yellow
 Write-Host ''
+Write-Host 'Export-ready verification URL:' -ForegroundColor Green
+Write-Host "  $exportReadyUrl" -ForegroundColor Yellow
+Write-Host ''
 Write-Host 'Base frontend URL:' -ForegroundColor Green
 Write-Host "  $frontendUrl" -ForegroundColor Yellow
 Write-Host ''
-Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI can be reviewed without backend setup.' -ForegroundColor Cyan
+Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI and zero-blocker export-ready state can be reviewed without backend setup.' -ForegroundColor Cyan
diff --git a/bizPA/tax_readiness_ui_smoke.ps1 b/bizPA/tax_readiness_ui_smoke.ps1
index 56d67892a51b22572503dec560d17123b370c2b7..ead6308d0828e9084bfe4f0ec2679be468d8061e
--- a/bizPA/tax_readiness_ui_smoke.ps1
+++ b/bizPA/tax_readiness_ui_smoke.ps1
@@ -28,38 +28,43 @@
 
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
-$screenshotPath = 'C:\Users\edebe\eds\workstream\verification\20260311_201500_bizpa_tax_readiness_ui.png'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
+$verificationRoot = 'C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification'
+$quarterScreenshotPath = Join-Path $verificationRoot 'quarter_readiness_screen.png'
+$finishNowScreenshotPath = Join-Path $verificationRoot 'finish_now_queue_zero_blockers.png'
 $chromeProfilePath = 'C:\Users\edebe\eds\workstream\verification\chrome_capture_profile_tax_readiness'
+$chromePath = 'C:\Program Files\Google\Chrome\Application\chrome.exe'
 
-New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
-
-$frontendJob = Start-Job -ScriptBlock {
-  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
-  $env:PORT = '3002'
-  npm.cmd start
-}
+function Wait-FrontendReady {
+  param (
+    [string]$Url
+  )
 
-try {
-  $frontendResponse = $null
-  $frontendReady = $false
   for ($i = 0; $i -lt 45; $i++) {
     Start-Sleep -Seconds 2
     try {
-      $frontendResponse = Invoke-WebRequest -UseBasicParsing $readinessUrl
-      if ($frontendResponse.StatusCode -eq 200) {
-        $frontendReady = $true
-        break
+      $response = Invoke-WebRequest -UseBasicParsing $Url
+      if ($response.StatusCode -eq 200) {
+        return $response
       }
     } catch {
     }
   }
 
-  if (-not $frontendReady) {
-    throw "Frontend did not become ready on $readinessUrl"
-  }
+  throw "Frontend did not become ready on $Url"
+}
 
+function Capture-ChromeAppScreenshot {
+  param (
+    [string]$Url,
+    [string]$OutputPath
+  )
+
+  $captureProfilePath = Join-Path $chromeProfilePath ([System.IO.Path]::GetFileNameWithoutExtension($OutputPath))
+  New-Item -ItemType Directory -Force -Path $captureProfilePath | Out-Null
+
   $chromeArgs = @(
-    "--user-data-dir=$chromeProfilePath",
+    "--user-data-dir=$captureProfilePath",
     '--new-window',
     '--window-size=1600,1500',
     '--no-first-run',
@@ -68,9 +73,9 @@
     '--disable-breakpad',
     '--disable-crash-reporter',
     '--noerrdialogs',
-    "--app=$readinessUrl"
+    "--app=$Url"
   )
-  Start-Process 'C:\Program Files\Google\Chrome\Application\chrome.exe' -ArgumentList $chromeArgs | Out-Null
+  Start-Process $chromePath -ArgumentList $chromeArgs | Out-Null
 
   $chromeWindow = $null
   for ($i = 0; $i -lt 20; $i++) {
@@ -96,7 +101,7 @@
   }
 
   if (-not $chromeWindow) {
-    throw 'Chrome readiness window did not expose a main window handle for screenshot capture.'
+    throw "Chrome window did not expose a main window handle for $Url"
   }
 
   [WindowCaptureNative]::ShowWindow($chromeWindow.MainWindowHandle, 3) | Out-Null
@@ -110,17 +115,35 @@
   $bitmap = New-Object System.Drawing.Bitmap $width, $height
   $graphics = [System.Drawing.Graphics]::FromImage($bitmap)
   $graphics.CopyFromScreen($rect.Left, $rect.Top, 0, 0, $bitmap.Size)
-  $bitmap.Save($screenshotPath, [System.Drawing.Imaging.ImageFormat]::Png)
+  $bitmap.Save($OutputPath, [System.Drawing.Imaging.ImageFormat]::Png)
   $graphics.Dispose()
   $bitmap.Dispose()
 
   Get-Process chrome -ErrorAction SilentlyContinue |
     Where-Object { $_.StartTime -ge $chromeWindow.StartTime.AddSeconds(-2) } |
     Stop-Process -Force -ErrorAction SilentlyContinue
+  Start-Sleep -Seconds 2
+}
 
+New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
+New-Item -ItemType Directory -Force -Path $verificationRoot | Out-Null
+
+$frontendJob = Start-Job -ScriptBlock {
+  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
+  $env:PORT = '3002'
+  npm.cmd start
+}
+
+try {
+  $frontendResponse = Wait-FrontendReady -Url $readinessUrl
+  Capture-ChromeAppScreenshot -Url $readinessUrl -OutputPath $quarterScreenshotPath
+  Capture-ChromeAppScreenshot -Url $exportReadyUrl -OutputPath $finishNowScreenshotPath
+
   "READINESS_URL=$readinessUrl"
+  "EXPORT_READY_URL=$exportReadyUrl"
   "FRONTEND_STATUS=$($frontendResponse.StatusCode)"
-  "SCREENSHOT=$screenshotPath"
+  "SCREENSHOT_QUARTER=$quarterScreenshotPath"
+  "SCREENSHOT_EXPORT_READY=$finishNowScreenshotPath"
   'FRONTEND_LOG_START'
   Receive-Job $frontendJob -Keep -ErrorAction SilentlyContinue 2>&1
   'FRONTEND_LOG_END'
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..b0b6a961dcb80123d7634a661b59a1e944c1c7a1
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,26 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    fallbackIssues: inboxItems,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1532,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1547,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1633,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2104,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2316,29 +2562,241 @@
     </div>
   );
 
-  const renderInbox = () => (
-    <div>
-      <h4>Finish Now Queue</h4>
-      <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
-      <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
-            <div>
-              <b>{item.merchant || 'Unknown'}</b>
-              <br />
-              <small>
-                {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
-              </small>
-              <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
-            </div>
+  const renderInbox = () => {
+    return (
+    <div className="inbox-mobile-shell">
+      <section className="inbox-mobile-hero">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2">
+          <div className="inbox-kicker">
+            <Bell size={14} />
+            Mobile-first exception queue
           </div>
-        ))}
-      </div>
-    </div>
-  );
+          <div className="status-pill">{filteredInboxCards.length} visible</div>
+        </div>
+        <h4>Finish now</h4>
+        <p>
+          Clear the blockers that stop the quarterly export: category, business or personal, split treatment, and duplicate resolution.
+        </p>
+        <div className="inbox-mobile-stats">
+          <div className="inbox-mobile-stat">
+            <small>Queue</small>
+            <strong>{inboxCards.length}</strong>
+            <small>{activeQuarterReport.quarter_reference || READINESS_DEMO_REPORT.quarter_reference}</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Critical</small>
+            <strong>{highSeverityCount}</strong>
+            <small>High-severity blockers</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Ready now</small>
+            <strong>{readyToExportCount}</strong>
+            <small>Transactions already export-safe</small>
+          </div>
+        </div>
+        <div className="hero-actions">
+          <button className="hero-action-main" onClick={() => setCurrentTab('quarter')}>
+            <ArrowRight size={16} />
+            Review quarter readiness
+          </button>
+          <button className="hero-action-secondary" onClick={undoLastInboxAction} disabled={inboxBusyId === 'undo-last'}>
+            {inboxBusyId === 'undo-last' ? <RefreshCcw size={16} className="spinner" /> : <ArchiveRestore size={16} />}
+            Undo last action
+          </button>
+        </div>
+      </section>
+
+      {inboxMessage && (
+        <div className={`readiness-panel inbox-message ${inboxMessage.type}`}>
+          {inboxMessage.text}
+        </div>
+      )}
+
+      <section className="readiness-panel">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-3">
+          <div>
+            <h5 style={{ marginBottom: 4 }}>Queue filters</h5>
+            <small className="small-muted">Keep the mobile pass focused on one blocker type at a time.</small>
+          </div>
+          <div className="status-pill">{activeQuarterReport.readiness_pct ?? READINESS_DEMO_REPORT.readiness_pct}% ready</div>
+        </div>
+        <div className="inbox-filter-row">
+          {INBOX_FILTERS.map((filter) => {
+            const count = filter.key === 'all'
+              ? inboxCards.length
+              : inboxCards.filter((card) => card.issue_types.includes(filter.key)).length;
+            return (
+              <button
+                key={filter.key}
+                type="button"
+                className={`inbox-filter-chip ${inboxFilter === filter.key ? 'active' : ''}`}
+                onClick={() => setInboxFilter(filter.key)}
+              >
+                <span>{filter.label}</span>
+                <small>{count}</small>
+              </button>
+            );
+          })}
+        </div>
+      </section>
+
+      {filteredInboxCards.length === 0 ? (
+        <div className="inbox-empty">
+          No blockers match this filter. Reset to All blockers or return to the quarter screen to confirm readiness.
+        </div>
+      ) : (
+        <div className="inbox-card-list">
+          {filteredInboxCards.map((card) => {
+            const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+            const saving = inboxBusyId === card.id;
+            const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+            return (
+              <section key={card.id} className="inbox-action-card">
+                <div className="inbox-card-top">
+                  <div>
+                    <strong>{card.merchant}</strong>
+                    <div className="inbox-card-subline">
+                      {card.txn_date ? String(card.txn_date).slice(0, 10) : 'Date pending'} · {formatIssueAmount(card.amount, card.direction)}
+                    </div>
+                  </div>
+                  <span className={`inbox-issue-chip ${card.severity}`}>
+                    <TriangleAlert size={14} />
+                    {INBOX_REASON_LABELS[card.blocker_reason] || card.blocker_reason.replace(/_/g, ' ')}
+                  </span>
+                </div>
+
+                <div className="inbox-chip-row">
+                  {card.issue_labels.map((label) => (
+                    <span key={`${card.id}-${label}`} className={`inbox-issue-chip ${card.severity}`}>
+                      {label}
+                    </span>
+                  ))}
+                  {card.suggested_category?.category_name && (
+                    <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
+                      <CheckCircle2 size={14} />
+                      Suggested: {card.suggested_category.category_name}
+                    </button>
+                  )}
+                </div>
+
+                <p className="inbox-explainer">{card.explanation || 'Resolve the blocking fields to clear this transaction from the queue.'}</p>
+
+                {(card.supports_category || card.supports_business_personal || card.supports_split) && (
+                  <div className="inbox-action-grid">
+                    <div className="inbox-field">
+                      <label>Category code</label>
+                      <input
+                        value={draft.category_code}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_code', event.target.value)}
+                        placeholder="MATERIALS"
+                      />
+                    </div>
+                    <div className="inbox-field">
+                      <label>Category label</label>
+                      <input
+                        value={draft.category_name}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_name', event.target.value)}
+                        placeholder="Materials"
+                      />
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_business_personal || card.supports_split) && (
+                  <div>
+                    <div className="inbox-field">
+                      <label>Business treatment</label>
+                    </div>
+                    <div className="inbox-toggle-row">
+                      {[
+                        { value: 'BUSINESS', label: 'Business' },
+                        { value: 'PERSONAL', label: 'Personal' },
+                        { value: 'SPLIT', label: 'Split' },
+                      ].map((option) => (
+                        <button
+                          key={option.value}
+                          type="button"
+                          className={`inbox-toggle ${draft.business_personal === option.value ? 'active' : ''}`}
+                          onClick={() => setInboxDraftField(card.id, 'business_personal', option.value)}
+                        >
+                          {option.label}
+                        </button>
+                      ))}
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_split || isSplit) && (
+                  <div className="inbox-field">
+                    <label>Business split %</label>
+                    <input
+                      type="number"
+                      min="0"
+                      max="100"
+                      value={draft.split_business_pct}
+                      onChange={(event) => setInboxDraftField(card.id, 'split_business_pct', event.target.value)}
+                      placeholder="80"
+                    />
+                  </div>
+                )}
+
+                {card.supports_duplicate && (
+                  <>
+                    <div>
+                      <div className="inbox-field">
+                        <label>Duplicate action</label>
+                      </div>
+                      <div className="inbox-duplicate-row">
+                        {[
+                          { value: 'dismiss', label: 'Dismiss duplicate' },
+                          { value: 'merge', label: 'Merge to original' },
+                        ].map((option) => (
+                          <button
+                            key={option.value}
+                            type="button"
+                            className={`inbox-toggle ${draft.duplicate_action === option.value ? 'active' : ''}`}
+                            onClick={() => setInboxDraftField(card.id, 'duplicate_action', option.value)}
+                          >
+                            {option.label}
+                          </button>
+                        ))}
+                      </div>
+                    </div>
+                    {draft.duplicate_action === 'merge' && (
+                      <div className="inbox-field">
+                        <label>Original transaction id</label>
+                        <input
+                          value={draft.duplicate_of_txn_id}
+                          onChange={(event) => setInboxDraftField(card.id, 'duplicate_of_txn_id', event.target.value)}
+                          placeholder="txn-9001"
+                        />
+                      </div>
+                    )}
+                  </>
+                )}
 
+                <div className="inbox-footer-row">
+                  <div className="inbox-inline-status">
+                    Route: {card.resolution_target?.route || `/api/v1/inbox/${card.id}`}
+                  </div>
+                  <div className="inbox-footer-actions">
+                    <button className="secondary-action" onClick={clearInboxMessage} disabled={saving}>
+                      Clear banner
+                    </button>
+                    <button className="primary-action" onClick={() => submitInboxAction(card)} disabled={saving}>
+                      {saving ? <RefreshCcw size={16} className="spinner" /> : <CheckCircle2 size={16} />}
+                      Save and clear blocker
+                    </button>
+                  </div>
+                </div>
+              </section>
+            );
+          })}
+        </div>
+      )}
+    </div>
+    );
+  };
   const renderControlCentre = () => {
     const policyLabels = {
       vat_scheme: 'VAT scheme',
@@ -2510,11 +2968,11 @@
           <section className="readiness-panel">
             <h5 style={{ marginBottom: 12 }}>Queue Backlog</h5>
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
-                <div key={item.id} className="control-row">
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
+                <div key={item.id || item.affected_entity_id} className="control-row">
                   <div>
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
                   <div className="text-end">
                     <div className="small-muted">{item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date}</div>
@@ -2522,7 +2980,7 @@
                   </div>
                 </div>
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
               )}
             </div>
@@ -2668,7 +3126,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2697,7 +3155,7 @@
   };
 
   const createSnapshotVersion = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     const status = snapshotWorkspace?.snapshot_status;
 
     if (!status?.can_create_snapshot_version) {
@@ -2719,7 +3177,7 @@
         description: `Quarter snapshot generated from Quarter workspace`,
         metadata: {
           source_type: 'manual',
-          readiness_pct: quarterReadiness?.readiness_pct ?? null
+          readiness_pct: activeQuarterReport?.readiness_pct ?? null
         }
       });
       await refreshQuarterWorkspace();
@@ -2734,7 +3192,7 @@
   };
 
   const closeQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(closeReason || '').trim()) {
       setQuarterActionError('Close reason is required.');
       setQuarterActionMessage('');
@@ -2785,7 +3243,7 @@
   };
 
   const reopenQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(reopenReason || '').trim()) {
       setQuarterActionError('Reopen reason is required.');
       setQuarterActionMessage('');
@@ -2844,9 +3302,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3342,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3354,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3365,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3379,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..669173cd6176ee9ee5c86c5895ea49cb9524ebc4
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,155 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, fallbackIssues = [], resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const issueSource = Array.isArray(report.issue_list) && report.issue_list.length > 0
+    ? report.issue_list
+    : fallbackIssues;
+  const allIssues = sortIssues(Array.isArray(issueSource) ? issueSource : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/bizPA/frontend/src/quarterReadiness.test.js b/bizPA/frontend/src/quarterReadiness.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..0b810191a89d8b82091bf5de1408afcc23c14e65
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.test.js
@@ -0,0 +1,93 @@
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
+
+const reportFixture = {
+  quarter_reference: 'Q1-2026',
+  period_start: '2026-01-01',
+  period_end: '2026-03-31',
+  total_txns_in_period: 18,
+  readiness_pct: 78,
+  can_export: false,
+  issue_list: [
+    {
+      issue_type: 'missing_category',
+      affected_entity_id: 'txn-9101',
+      severity: 'high',
+      weight: 35,
+      explanation: 'Category missing for Fuel Stop.',
+      txn_date: '2026-02-04',
+      merchant: 'Fuel Stop',
+      amount: 64.18,
+      direction: 'out',
+      resolution_target: {
+        label: 'Classify transaction',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'missing_business_personal',
+      affected_entity_id: 'txn-9103',
+      severity: 'high',
+      weight: 30,
+      explanation: 'Business/personal decision missing.',
+      txn_date: '2026-02-15',
+      merchant: 'Mobile Contract',
+      amount: 42.5,
+      direction: 'out',
+      resolution_target: {
+        label: 'Set business or personal',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'unresolved_duplicate',
+      affected_entity_id: 'txn-9104',
+      severity: 'medium',
+      weight: 15,
+      explanation: 'Duplicate not resolved.',
+      txn_date: '2026-03-02',
+      merchant: 'Client Payment',
+      amount: 820,
+      direction: 'in',
+      resolution_target: {
+        label: 'Resolve duplicate',
+        workflow: '/inbox/finish-now',
+      },
+    },
+  ],
+};
+
+describe('quarter readiness helpers', () => {
+  test('keeps blockers ordered and updates headline while blockers remain', () => {
+    const state = buildQuarterUiState({ report: reportFixture, resolvedIssueIds: ['txn-9104'] });
+
+    expect(state.itemsLeft).toBe(2);
+    expect(state.exportEnabled).toBe(false);
+    expect(state.report.readiness_pct).toBeGreaterThan(78);
+    expect(state.headline).toContain('items left');
+    expect(state.activeIssues.map((issue) => issue.affected_entity_id)).toEqual(['txn-9101', 'txn-9103']);
+    expect(state.report.explanation_lines[0]).toContain('Readiness is');
+  });
+
+  test('unlocks export immediately when every blocker is resolved', () => {
+    const state = buildQuarterUiState({
+      report: reportFixture,
+      resolvedIssueIds: ['txn-9101', 'txn-9103', 'txn-9104'],
+    });
+
+    expect(state.itemsLeft).toBe(0);
+    expect(state.exportEnabled).toBe(true);
+    expect(state.report.readiness_pct).toBe(100);
+    expect(state.report.can_export).toBe(true);
+    expect(state.report.issue_summary).toEqual([]);
+  });
+
+  test('supports demo query params that resolve all blockers', () => {
+    const params = new URLSearchParams('resolvedDemoIssues=all');
+    const resolvedIds = deriveResolvedIssueIdsFromSearchParams(params, reportFixture.issue_list);
+
+    expect(resolvedIds).toEqual(['txn-9101', 'txn-9103', 'txn-9104']);
+  });
+});
diff --git a/bizPA/start_bizpa_tax_readiness_ui.ps1 b/bizPA/start_bizpa_tax_readiness_ui.ps1
index 1fc4bef8cd172c31fdc3db52d1acc9d4775c5485..7216e44eac386fe436a9af8e9b417cd2d61e37fe
--- a/bizPA/start_bizpa_tax_readiness_ui.ps1
+++ b/bizPA/start_bizpa_tax_readiness_ui.ps1
@@ -4,6 +4,7 @@
 $frontendPath = Join-Path $repoRoot 'frontend'
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
 
 Write-Host 'Starting bizPA tax readiness UI...' -ForegroundColor Cyan
 
@@ -14,7 +15,10 @@
 Write-Host 'Tax readiness dashboard URL:' -ForegroundColor Green
 Write-Host "  $readinessUrl" -ForegroundColor Yellow
 Write-Host ''
+Write-Host 'Export-ready verification URL:' -ForegroundColor Green
+Write-Host "  $exportReadyUrl" -ForegroundColor Yellow
+Write-Host ''
 Write-Host 'Base frontend URL:' -ForegroundColor Green
 Write-Host "  $frontendUrl" -ForegroundColor Yellow
 Write-Host ''
-Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI can be reviewed without backend setup.' -ForegroundColor Cyan
+Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI and zero-blocker export-ready state can be reviewed without backend setup.' -ForegroundColor Cyan
diff --git a/bizPA/tax_readiness_ui_smoke.ps1 b/bizPA/tax_readiness_ui_smoke.ps1
index 56d67892a51b22572503dec560d17123b370c2b7..ead6308d0828e9084bfe4f0ec2679be468d8061e
--- a/bizPA/tax_readiness_ui_smoke.ps1
+++ b/bizPA/tax_readiness_ui_smoke.ps1
@@ -28,38 +28,43 @@
 
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
-$screenshotPath = 'C:\Users\edebe\eds\workstream\verification\20260311_201500_bizpa_tax_readiness_ui.png'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
+$verificationRoot = 'C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification'
+$quarterScreenshotPath = Join-Path $verificationRoot 'quarter_readiness_screen.png'
+$finishNowScreenshotPath = Join-Path $verificationRoot 'finish_now_queue_zero_blockers.png'
 $chromeProfilePath = 'C:\Users\edebe\eds\workstream\verification\chrome_capture_profile_tax_readiness'
+$chromePath = 'C:\Program Files\Google\Chrome\Application\chrome.exe'
 
-New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
-
-$frontendJob = Start-Job -ScriptBlock {
-  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
-  $env:PORT = '3002'
-  npm.cmd start
-}
+function Wait-FrontendReady {
+  param (
+    [string]$Url
+  )
 
-try {
-  $frontendResponse = $null
-  $frontendReady = $false
   for ($i = 0; $i -lt 45; $i++) {
     Start-Sleep -Seconds 2
     try {
-      $frontendResponse = Invoke-WebRequest -UseBasicParsing $readinessUrl
-      if ($frontendResponse.StatusCode -eq 200) {
-        $frontendReady = $true
-        break
+      $response = Invoke-WebRequest -UseBasicParsing $Url
+      if ($response.StatusCode -eq 200) {
+        return $response
       }
     } catch {
     }
   }
 
-  if (-not $frontendReady) {
-    throw "Frontend did not become ready on $readinessUrl"
-  }
+  throw "Frontend did not become ready on $Url"
+}
 
+function Capture-ChromeAppScreenshot {
+  param (
+    [string]$Url,
+    [string]$OutputPath
+  )
+
+  $captureProfilePath = Join-Path $chromeProfilePath ([System.IO.Path]::GetFileNameWithoutExtension($OutputPath))
+  New-Item -ItemType Directory -Force -Path $captureProfilePath | Out-Null
+
   $chromeArgs = @(
-    "--user-data-dir=$chromeProfilePath",
+    "--user-data-dir=$captureProfilePath",
     '--new-window',
     '--window-size=1600,1500',
     '--no-first-run',
@@ -68,9 +73,9 @@
     '--disable-breakpad',
     '--disable-crash-reporter',
     '--noerrdialogs',
-    "--app=$readinessUrl"
+    "--app=$Url"
   )
-  Start-Process 'C:\Program Files\Google\Chrome\Application\chrome.exe' -ArgumentList $chromeArgs | Out-Null
+  Start-Process $chromePath -ArgumentList $chromeArgs | Out-Null
 
   $chromeWindow = $null
   for ($i = 0; $i -lt 20; $i++) {
@@ -96,7 +101,7 @@
   }
 
   if (-not $chromeWindow) {
-    throw 'Chrome readiness window did not expose a main window handle for screenshot capture.'
+    throw "Chrome window did not expose a main window handle for $Url"
   }
 
   [WindowCaptureNative]::ShowWindow($chromeWindow.MainWindowHandle, 3) | Out-Null
@@ -110,17 +115,35 @@
   $bitmap = New-Object System.Drawing.Bitmap $width, $height
   $graphics = [System.Drawing.Graphics]::FromImage($bitmap)
   $graphics.CopyFromScreen($rect.Left, $rect.Top, 0, 0, $bitmap.Size)
-  $bitmap.Save($screenshotPath, [System.Drawing.Imaging.ImageFormat]::Png)
+  $bitmap.Save($OutputPath, [System.Drawing.Imaging.ImageFormat]::Png)
   $graphics.Dispose()
   $bitmap.Dispose()
 
   Get-Process chrome -ErrorAction SilentlyContinue |
     Where-Object { $_.StartTime -ge $chromeWindow.StartTime.AddSeconds(-2) } |
     Stop-Process -Force -ErrorAction SilentlyContinue
+  Start-Sleep -Seconds 2
+}
 
+New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
+New-Item -ItemType Directory -Force -Path $verificationRoot | Out-Null
+
+$frontendJob = Start-Job -ScriptBlock {
+  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
+  $env:PORT = '3002'
+  npm.cmd start
+}
+
+try {
+  $frontendResponse = Wait-FrontendReady -Url $readinessUrl
+  Capture-ChromeAppScreenshot -Url $readinessUrl -OutputPath $quarterScreenshotPath
+  Capture-ChromeAppScreenshot -Url $exportReadyUrl -OutputPath $finishNowScreenshotPath
+
   "READINESS_URL=$readinessUrl"
+  "EXPORT_READY_URL=$exportReadyUrl"
   "FRONTEND_STATUS=$($frontendResponse.StatusCode)"
-  "SCREENSHOT=$screenshotPath"
+  "SCREENSHOT_QUARTER=$quarterScreenshotPath"
+  "SCREENSHOT_EXPORT_READY=$finishNowScreenshotPath"
   'FRONTEND_LOG_START'
   Receive-Job $frontendJob -Keep -ErrorAction SilentlyContinue 2>&1
   'FRONTEND_LOG_END'
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "powershell -ExecutionPolicy Bypass -File 'C:\\Users\\edebe\\eds\\bizPA\\tax_readiness_ui_smoke.ps1'" in C:\Users\edebe\eds exited 1 in 52.49s:
Chrome window did not expose a main window handle for http://127.0.0.1:3002/?readinessDemo=1&tab=quarter
At C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1:104 char:5
+     throw "Chrome window did not expose a main window handle for $Url ...
+     ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : OperationStopped: (Chrome window d...o=1&tab=quarter:String) [], RuntimeException
    + FullyQualifiedErrorId : Chrome window did not expose a main window handle for http://127.0.0.1:3002/?readinessDe 
   mo=1&tab=quarter
 
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..b0b6a961dcb80123d7634a661b59a1e944c1c7a1
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,26 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    fallbackIssues: inboxItems,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1532,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1547,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1633,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2104,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2316,29 +2562,241 @@
     </div>
   );
 
-  const renderInbox = () => (
-    <div>
-      <h4>Finish Now Queue</h4>
-      <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
-      <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
-            <div>
-              <b>{item.merchant || 'Unknown'}</b>
-              <br />
-              <small>
-                {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
-              </small>
-              <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
-            </div>
+  const renderInbox = () => {
+    return (
+    <div className="inbox-mobile-shell">
+      <section className="inbox-mobile-hero">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2">
+          <div className="inbox-kicker">
+            <Bell size={14} />
+            Mobile-first exception queue
           </div>
-        ))}
-      </div>
-    </div>
-  );
+          <div className="status-pill">{filteredInboxCards.length} visible</div>
+        </div>
+        <h4>Finish now</h4>
+        <p>
+          Clear the blockers that stop the quarterly export: category, business or personal, split treatment, and duplicate resolution.
+        </p>
+        <div className="inbox-mobile-stats">
+          <div className="inbox-mobile-stat">
+            <small>Queue</small>
+            <strong>{inboxCards.length}</strong>
+            <small>{activeQuarterReport.quarter_reference || READINESS_DEMO_REPORT.quarter_reference}</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Critical</small>
+            <strong>{highSeverityCount}</strong>
+            <small>High-severity blockers</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Ready now</small>
+            <strong>{readyToExportCount}</strong>
+            <small>Transactions already export-safe</small>
+          </div>
+        </div>
+        <div className="hero-actions">
+          <button className="hero-action-main" onClick={() => setCurrentTab('quarter')}>
+            <ArrowRight size={16} />
+            Review quarter readiness
+          </button>
+          <button className="hero-action-secondary" onClick={undoLastInboxAction} disabled={inboxBusyId === 'undo-last'}>
+            {inboxBusyId === 'undo-last' ? <RefreshCcw size={16} className="spinner" /> : <ArchiveRestore size={16} />}
+            Undo last action
+          </button>
+        </div>
+      </section>
+
+      {inboxMessage && (
+        <div className={`readiness-panel inbox-message ${inboxMessage.type}`}>
+          {inboxMessage.text}
+        </div>
+      )}
+
+      <section className="readiness-panel">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-3">
+          <div>
+            <h5 style={{ marginBottom: 4 }}>Queue filters</h5>
+            <small className="small-muted">Keep the mobile pass focused on one blocker type at a time.</small>
+          </div>
+          <div className="status-pill">{activeQuarterReport.readiness_pct ?? READINESS_DEMO_REPORT.readiness_pct}% ready</div>
+        </div>
+        <div className="inbox-filter-row">
+          {INBOX_FILTERS.map((filter) => {
+            const count = filter.key === 'all'
+              ? inboxCards.length
+              : inboxCards.filter((card) => card.issue_types.includes(filter.key)).length;
+            return (
+              <button
+                key={filter.key}
+                type="button"
+                className={`inbox-filter-chip ${inboxFilter === filter.key ? 'active' : ''}`}
+                onClick={() => setInboxFilter(filter.key)}
+              >
+                <span>{filter.label}</span>
+                <small>{count}</small>
+              </button>
+            );
+          })}
+        </div>
+      </section>
+
+      {filteredInboxCards.length === 0 ? (
+        <div className="inbox-empty">
+          No blockers match this filter. Reset to All blockers or return to the quarter screen to confirm readiness.
+        </div>
+      ) : (
+        <div className="inbox-card-list">
+          {filteredInboxCards.map((card) => {
+            const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+            const saving = inboxBusyId === card.id;
+            const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+            return (
+              <section key={card.id} className="inbox-action-card">
+                <div className="inbox-card-top">
+                  <div>
+                    <strong>{card.merchant}</strong>
+                    <div className="inbox-card-subline">
+                      {card.txn_date ? String(card.txn_date).slice(0, 10) : 'Date pending'} · {formatIssueAmount(card.amount, card.direction)}
+                    </div>
+                  </div>
+                  <span className={`inbox-issue-chip ${card.severity}`}>
+                    <TriangleAlert size={14} />
+                    {INBOX_REASON_LABELS[card.blocker_reason] || card.blocker_reason.replace(/_/g, ' ')}
+                  </span>
+                </div>
+
+                <div className="inbox-chip-row">
+                  {card.issue_labels.map((label) => (
+                    <span key={`${card.id}-${label}`} className={`inbox-issue-chip ${card.severity}`}>
+                      {label}
+                    </span>
+                  ))}
+                  {card.suggested_category?.category_name && (
+                    <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
+                      <CheckCircle2 size={14} />
+                      Suggested: {card.suggested_category.category_name}
+                    </button>
+                  )}
+                </div>
+
+                <p className="inbox-explainer">{card.explanation || 'Resolve the blocking fields to clear this transaction from the queue.'}</p>
+
+                {(card.supports_category || card.supports_business_personal || card.supports_split) && (
+                  <div className="inbox-action-grid">
+                    <div className="inbox-field">
+                      <label>Category code</label>
+                      <input
+                        value={draft.category_code}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_code', event.target.value)}
+                        placeholder="MATERIALS"
+                      />
+                    </div>
+                    <div className="inbox-field">
+                      <label>Category label</label>
+                      <input
+                        value={draft.category_name}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_name', event.target.value)}
+                        placeholder="Materials"
+                      />
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_business_personal || card.supports_split) && (
+                  <div>
+                    <div className="inbox-field">
+                      <label>Business treatment</label>
+                    </div>
+                    <div className="inbox-toggle-row">
+                      {[
+                        { value: 'BUSINESS', label: 'Business' },
+                        { value: 'PERSONAL', label: 'Personal' },
+                        { value: 'SPLIT', label: 'Split' },
+                      ].map((option) => (
+                        <button
+                          key={option.value}
+                          type="button"
+                          className={`inbox-toggle ${draft.business_personal === option.value ? 'active' : ''}`}
+                          onClick={() => setInboxDraftField(card.id, 'business_personal', option.value)}
+                        >
+                          {option.label}
+                        </button>
+                      ))}
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_split || isSplit) && (
+                  <div className="inbox-field">
+                    <label>Business split %</label>
+                    <input
+                      type="number"
+                      min="0"
+                      max="100"
+                      value={draft.split_business_pct}
+                      onChange={(event) => setInboxDraftField(card.id, 'split_business_pct', event.target.value)}
+                      placeholder="80"
+                    />
+                  </div>
+                )}
+
+                {card.supports_duplicate && (
+                  <>
+                    <div>
+                      <div className="inbox-field">
+                        <label>Duplicate action</label>
+                      </div>
+                      <div className="inbox-duplicate-row">
+                        {[
+                          { value: 'dismiss', label: 'Dismiss duplicate' },
+                          { value: 'merge', label: 'Merge to original' },
+                        ].map((option) => (
+                          <button
+                            key={option.value}
+                            type="button"
+                            className={`inbox-toggle ${draft.duplicate_action === option.value ? 'active' : ''}`}
+                            onClick={() => setInboxDraftField(card.id, 'duplicate_action', option.value)}
+                          >
+                            {option.label}
+                          </button>
+                        ))}
+                      </div>
+                    </div>
+                    {draft.duplicate_action === 'merge' && (
+                      <div className="inbox-field">
+                        <label>Original transaction id</label>
+                        <input
+                          value={draft.duplicate_of_txn_id}
+                          onChange={(event) => setInboxDraftField(card.id, 'duplicate_of_txn_id', event.target.value)}
+                          placeholder="txn-9001"
+                        />
+                      </div>
+                    )}
+                  </>
+                )}
 
+                <div className="inbox-footer-row">
+                  <div className="inbox-inline-status">
+                    Route: {card.resolution_target?.route || `/api/v1/inbox/${card.id}`}
+                  </div>
+                  <div className="inbox-footer-actions">
+                    <button className="secondary-action" onClick={clearInboxMessage} disabled={saving}>
+                      Clear banner
+                    </button>
+                    <button className="primary-action" onClick={() => submitInboxAction(card)} disabled={saving}>
+                      {saving ? <RefreshCcw size={16} className="spinner" /> : <CheckCircle2 size={16} />}
+                      Save and clear blocker
+                    </button>
+                  </div>
+                </div>
+              </section>
+            );
+          })}
+        </div>
+      )}
+    </div>
+    );
+  };
   const renderControlCentre = () => {
     const policyLabels = {
       vat_scheme: 'VAT scheme',
@@ -2510,11 +2968,11 @@
           <section className="readiness-panel">
             <h5 style={{ marginBottom: 12 }}>Queue Backlog</h5>
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
-                <div key={item.id} className="control-row">
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
+                <div key={item.id || item.affected_entity_id} className="control-row">
                   <div>
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
                   <div className="text-end">
                     <div className="small-muted">{item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date}</div>
@@ -2522,7 +2980,7 @@
                   </div>
                 </div>
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
               )}
             </div>
@@ -2668,7 +3126,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2697,7 +3155,7 @@
   };
 
   const createSnapshotVersion = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     const status = snapshotWorkspace?.snapshot_status;
 
     if (!status?.can_create_snapshot_version) {
@@ -2719,7 +3177,7 @@
         description: `Quarter snapshot generated from Quarter workspace`,
         metadata: {
           source_type: 'manual',
-          readiness_pct: quarterReadiness?.readiness_pct ?? null
+          readiness_pct: activeQuarterReport?.readiness_pct ?? null
         }
       });
       await refreshQuarterWorkspace();
@@ -2734,7 +3192,7 @@
   };
 
   const closeQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(closeReason || '').trim()) {
       setQuarterActionError('Close reason is required.');
       setQuarterActionMessage('');
@@ -2785,7 +3243,7 @@
   };
 
   const reopenQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(reopenReason || '').trim()) {
       setQuarterActionError('Reopen reason is required.');
       setQuarterActionMessage('');
@@ -2844,9 +3302,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3342,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3354,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3365,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3379,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..669173cd6176ee9ee5c86c5895ea49cb9524ebc4
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,155 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, fallbackIssues = [], resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const issueSource = Array.isArray(report.issue_list) && report.issue_list.length > 0
+    ? report.issue_list
+    : fallbackIssues;
+  const allIssues = sortIssues(Array.isArray(issueSource) ? issueSource : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/bizPA/frontend/src/quarterReadiness.test.js b/bizPA/frontend/src/quarterReadiness.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..0b810191a89d8b82091bf5de1408afcc23c14e65
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.test.js
@@ -0,0 +1,93 @@
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
+
+const reportFixture = {
+  quarter_reference: 'Q1-2026',
+  period_start: '2026-01-01',
+  period_end: '2026-03-31',
+  total_txns_in_period: 18,
+  readiness_pct: 78,
+  can_export: false,
+  issue_list: [
+    {
+      issue_type: 'missing_category',
+      affected_entity_id: 'txn-9101',
+      severity: 'high',
+      weight: 35,
+      explanation: 'Category missing for Fuel Stop.',
+      txn_date: '2026-02-04',
+      merchant: 'Fuel Stop',
+      amount: 64.18,
+      direction: 'out',
+      resolution_target: {
+        label: 'Classify transaction',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'missing_business_personal',
+      affected_entity_id: 'txn-9103',
+      severity: 'high',
+      weight: 30,
+      explanation: 'Business/personal decision missing.',
+      txn_date: '2026-02-15',
+      merchant: 'Mobile Contract',
+      amount: 42.5,
+      direction: 'out',
+      resolution_target: {
+        label: 'Set business or personal',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'unresolved_duplicate',
+      affected_entity_id: 'txn-9104',
+      severity: 'medium',
+      weight: 15,
+      explanation: 'Duplicate not resolved.',
+      txn_date: '2026-03-02',
+      merchant: 'Client Payment',
+      amount: 820,
+      direction: 'in',
+      resolution_target: {
+        label: 'Resolve duplicate',
+        workflow: '/inbox/finish-now',
+      },
+    },
+  ],
+};
+
+describe('quarter readiness helpers', () => {
+  test('keeps blockers ordered and updates headline while blockers remain', () => {
+    const state = buildQuarterUiState({ report: reportFixture, resolvedIssueIds: ['txn-9104'] });
+
+    expect(state.itemsLeft).toBe(2);
+    expect(state.exportEnabled).toBe(false);
+    expect(state.report.readiness_pct).toBeGreaterThan(78);
+    expect(state.headline).toContain('items left');
+    expect(state.activeIssues.map((issue) => issue.affected_entity_id)).toEqual(['txn-9101', 'txn-9103']);
+    expect(state.report.explanation_lines[0]).toContain('Readiness is');
+  });
+
+  test('unlocks export immediately when every blocker is resolved', () => {
+    const state = buildQuarterUiState({
+      report: reportFixture,
+      resolvedIssueIds: ['txn-9101', 'txn-9103', 'txn-9104'],
+    });
+
+    expect(state.itemsLeft).toBe(0);
+    expect(state.exportEnabled).toBe(true);
+    expect(state.report.readiness_pct).toBe(100);
+    expect(state.report.can_export).toBe(true);
+    expect(state.report.issue_summary).toEqual([]);
+  });
+
+  test('supports demo query params that resolve all blockers', () => {
+    const params = new URLSearchParams('resolvedDemoIssues=all');
+    const resolvedIds = deriveResolvedIssueIdsFromSearchParams(params, reportFixture.issue_list);
+
+    expect(resolvedIds).toEqual(['txn-9101', 'txn-9103', 'txn-9104']);
+  });
+});
diff --git a/bizPA/start_bizpa_tax_readiness_ui.ps1 b/bizPA/start_bizpa_tax_readiness_ui.ps1
index 1fc4bef8cd172c31fdc3db52d1acc9d4775c5485..7216e44eac386fe436a9af8e9b417cd2d61e37fe
--- a/bizPA/start_bizpa_tax_readiness_ui.ps1
+++ b/bizPA/start_bizpa_tax_readiness_ui.ps1
@@ -4,6 +4,7 @@
 $frontendPath = Join-Path $repoRoot 'frontend'
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
 
 Write-Host 'Starting bizPA tax readiness UI...' -ForegroundColor Cyan
 
@@ -14,7 +15,10 @@
 Write-Host 'Tax readiness dashboard URL:' -ForegroundColor Green
 Write-Host "  $readinessUrl" -ForegroundColor Yellow
 Write-Host ''
+Write-Host 'Export-ready verification URL:' -ForegroundColor Green
+Write-Host "  $exportReadyUrl" -ForegroundColor Yellow
+Write-Host ''
 Write-Host 'Base frontend URL:' -ForegroundColor Green
 Write-Host "  $frontendUrl" -ForegroundColor Yellow
 Write-Host ''
-Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI can be reviewed without backend setup.' -ForegroundColor Cyan
+Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI and zero-blocker export-ready state can be reviewed without backend setup.' -ForegroundColor Cyan
diff --git a/bizPA/tax_readiness_ui_smoke.ps1 b/bizPA/tax_readiness_ui_smoke.ps1
index 56d67892a51b22572503dec560d17123b370c2b7..ead6308d0828e9084bfe4f0ec2679be468d8061e
--- a/bizPA/tax_readiness_ui_smoke.ps1
+++ b/bizPA/tax_readiness_ui_smoke.ps1
@@ -28,38 +28,43 @@
 
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
-$screenshotPath = 'C:\Users\edebe\eds\workstream\verification\20260311_201500_bizpa_tax_readiness_ui.png'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
+$verificationRoot = 'C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification'
+$quarterScreenshotPath = Join-Path $verificationRoot 'quarter_readiness_screen.png'
+$finishNowScreenshotPath = Join-Path $verificationRoot 'finish_now_queue_zero_blockers.png'
 $chromeProfilePath = 'C:\Users\edebe\eds\workstream\verification\chrome_capture_profile_tax_readiness'
+$chromePath = 'C:\Program Files\Google\Chrome\Application\chrome.exe'
 
-New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
-
-$frontendJob = Start-Job -ScriptBlock {
-  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
-  $env:PORT = '3002'
-  npm.cmd start
-}
+function Wait-FrontendReady {
+  param (
+    [string]$Url
+  )
 
-try {
-  $frontendResponse = $null
-  $frontendReady = $false
   for ($i = 0; $i -lt 45; $i++) {
     Start-Sleep -Seconds 2
     try {
-      $frontendResponse = Invoke-WebRequest -UseBasicParsing $readinessUrl
-      if ($frontendResponse.StatusCode -eq 200) {
-        $frontendReady = $true
-        break
+      $response = Invoke-WebRequest -UseBasicParsing $Url
+      if ($response.StatusCode -eq 200) {
+        return $response
       }
     } catch {
     }
   }
 
-  if (-not $frontendReady) {
-    throw "Frontend did not become ready on $readinessUrl"
-  }
+  throw "Frontend did not become ready on $Url"
+}
 
+function Capture-ChromeAppScreenshot {
+  param (
+    [string]$Url,
+    [string]$OutputPath
+  )
+
+  $captureProfilePath = Join-Path $chromeProfilePath ([System.IO.Path]::GetFileNameWithoutExtension($OutputPath))
+  New-Item -ItemType Directory -Force -Path $captureProfilePath | Out-Null
+
   $chromeArgs = @(
-    "--user-data-dir=$chromeProfilePath",
+    "--user-data-dir=$captureProfilePath",
     '--new-window',
     '--window-size=1600,1500',
     '--no-first-run',
@@ -68,9 +73,9 @@
     '--disable-breakpad',
     '--disable-crash-reporter',
     '--noerrdialogs',
-    "--app=$readinessUrl"
+    "--app=$Url"
   )
-  Start-Process 'C:\Program Files\Google\Chrome\Application\chrome.exe' -ArgumentList $chromeArgs | Out-Null
+  Start-Process $chromePath -ArgumentList $chromeArgs | Out-Null
 
   $chromeWindow = $null
   for ($i = 0; $i -lt 20; $i++) {
@@ -96,7 +101,7 @@
   }
 
   if (-not $chromeWindow) {
-    throw 'Chrome readiness window did not expose a main window handle for screenshot capture.'
+    throw "Chrome window did not expose a main window handle for $Url"
   }
 
   [WindowCaptureNative]::ShowWindow($chromeWindow.MainWindowHandle, 3) | Out-Null
@@ -110,17 +115,35 @@
   $bitmap = New-Object System.Drawing.Bitmap $width, $height
   $graphics = [System.Drawing.Graphics]::FromImage($bitmap)
   $graphics.CopyFromScreen($rect.Left, $rect.Top, 0, 0, $bitmap.Size)
-  $bitmap.Save($screenshotPath, [System.Drawing.Imaging.ImageFormat]::Png)
+  $bitmap.Save($OutputPath, [System.Drawing.Imaging.ImageFormat]::Png)
   $graphics.Dispose()
   $bitmap.Dispose()
 
   Get-Process chrome -ErrorAction SilentlyContinue |
     Where-Object { $_.StartTime -ge $chromeWindow.StartTime.AddSeconds(-2) } |
     Stop-Process -Force -ErrorAction SilentlyContinue
+  Start-Sleep -Seconds 2
+}
 
+New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
+New-Item -ItemType Directory -Force -Path $verificationRoot | Out-Null
+
+$frontendJob = Start-Job -ScriptBlock {
+  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
+  $env:PORT = '3002'
+  npm.cmd start
+}
+
+try {
+  $frontendResponse = Wait-FrontendReady -Url $readinessUrl
+  Capture-ChromeAppScreenshot -Url $readinessUrl -OutputPath $quarterScreenshotPath
+  Capture-ChromeAppScreenshot -Url $exportReadyUrl -OutputPath $finishNowScreenshotPath
+
   "READINESS_URL=$readinessUrl"
+  "EXPORT_READY_URL=$exportReadyUrl"
   "FRONTEND_STATUS=$($frontendResponse.StatusCode)"
-  "SCREENSHOT=$screenshotPath"
+  "SCREENSHOT_QUARTER=$quarterScreenshotPath"
+  "SCREENSHOT_EXPORT_READY=$finishNowScreenshotPath"
   'FRONTEND_LOG_START'
   Receive-Job $frontendJob -Keep -ErrorAction SilentlyContinue 2>&1
   'FRONTEND_LOG_END'
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update
M C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1
@@ -75,3 +75,3 @@
     '--noerrdialogs',
-    "--app=$Url"
+    $Url
   )
apply_patch(auto_approved=true) exited 0 in 754ms:
Success. Updated the following files:
M C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..b0b6a961dcb80123d7634a661b59a1e944c1c7a1
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,26 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    fallbackIssues: inboxItems,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1532,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1547,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1633,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2104,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2316,29 +2562,241 @@
     </div>
   );
 
-  const renderInbox = () => (
-    <div>
-      <h4>Finish Now Queue</h4>
-      <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
-      <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
-            <div>
-              <b>{item.merchant || 'Unknown'}</b>
-              <br />
-              <small>
-                {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
-              </small>
-              <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
-            </div>
+  const renderInbox = () => {
+    return (
+    <div className="inbox-mobile-shell">
+      <section className="inbox-mobile-hero">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2">
+          <div className="inbox-kicker">
+            <Bell size={14} />
+            Mobile-first exception queue
           </div>
-        ))}
-      </div>
-    </div>
-  );
+          <div className="status-pill">{filteredInboxCards.length} visible</div>
+        </div>
+        <h4>Finish now</h4>
+        <p>
+          Clear the blockers that stop the quarterly export: category, business or personal, split treatment, and duplicate resolution.
+        </p>
+        <div className="inbox-mobile-stats">
+          <div className="inbox-mobile-stat">
+            <small>Queue</small>
+            <strong>{inboxCards.length}</strong>
+            <small>{activeQuarterReport.quarter_reference || READINESS_DEMO_REPORT.quarter_reference}</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Critical</small>
+            <strong>{highSeverityCount}</strong>
+            <small>High-severity blockers</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Ready now</small>
+            <strong>{readyToExportCount}</strong>
+            <small>Transactions already export-safe</small>
+          </div>
+        </div>
+        <div className="hero-actions">
+          <button className="hero-action-main" onClick={() => setCurrentTab('quarter')}>
+            <ArrowRight size={16} />
+            Review quarter readiness
+          </button>
+          <button className="hero-action-secondary" onClick={undoLastInboxAction} disabled={inboxBusyId === 'undo-last'}>
+            {inboxBusyId === 'undo-last' ? <RefreshCcw size={16} className="spinner" /> : <ArchiveRestore size={16} />}
+            Undo last action
+          </button>
+        </div>
+      </section>
+
+      {inboxMessage && (
+        <div className={`readiness-panel inbox-message ${inboxMessage.type}`}>
+          {inboxMessage.text}
+        </div>
+      )}
+
+      <section className="readiness-panel">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-3">
+          <div>
+            <h5 style={{ marginBottom: 4 }}>Queue filters</h5>
+            <small className="small-muted">Keep the mobile pass focused on one blocker type at a time.</small>
+          </div>
+          <div className="status-pill">{activeQuarterReport.readiness_pct ?? READINESS_DEMO_REPORT.readiness_pct}% ready</div>
+        </div>
+        <div className="inbox-filter-row">
+          {INBOX_FILTERS.map((filter) => {
+            const count = filter.key === 'all'
+              ? inboxCards.length
+              : inboxCards.filter((card) => card.issue_types.includes(filter.key)).length;
+            return (
+              <button
+                key={filter.key}
+                type="button"
+                className={`inbox-filter-chip ${inboxFilter === filter.key ? 'active' : ''}`}
+                onClick={() => setInboxFilter(filter.key)}
+              >
+                <span>{filter.label}</span>
+                <small>{count}</small>
+              </button>
+            );
+          })}
+        </div>
+      </section>
+
+      {filteredInboxCards.length === 0 ? (
+        <div className="inbox-empty">
+          No blockers match this filter. Reset to All blockers or return to the quarter screen to confirm readiness.
+        </div>
+      ) : (
+        <div className="inbox-card-list">
+          {filteredInboxCards.map((card) => {
+            const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+            const saving = inboxBusyId === card.id;
+            const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+            return (
+              <section key={card.id} className="inbox-action-card">
+                <div className="inbox-card-top">
+                  <div>
+                    <strong>{card.merchant}</strong>
+                    <div className="inbox-card-subline">
+                      {card.txn_date ? String(card.txn_date).slice(0, 10) : 'Date pending'} · {formatIssueAmount(card.amount, card.direction)}
+                    </div>
+                  </div>
+                  <span className={`inbox-issue-chip ${card.severity}`}>
+                    <TriangleAlert size={14} />
+                    {INBOX_REASON_LABELS[card.blocker_reason] || card.blocker_reason.replace(/_/g, ' ')}
+                  </span>
+                </div>
+
+                <div className="inbox-chip-row">
+                  {card.issue_labels.map((label) => (
+                    <span key={`${card.id}-${label}`} className={`inbox-issue-chip ${card.severity}`}>
+                      {label}
+                    </span>
+                  ))}
+                  {card.suggested_category?.category_name && (
+                    <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
+                      <CheckCircle2 size={14} />
+                      Suggested: {card.suggested_category.category_name}
+                    </button>
+                  )}
+                </div>
+
+                <p className="inbox-explainer">{card.explanation || 'Resolve the blocking fields to clear this transaction from the queue.'}</p>
+
+                {(card.supports_category || card.supports_business_personal || card.supports_split) && (
+                  <div className="inbox-action-grid">
+                    <div className="inbox-field">
+                      <label>Category code</label>
+                      <input
+                        value={draft.category_code}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_code', event.target.value)}
+                        placeholder="MATERIALS"
+                      />
+                    </div>
+                    <div className="inbox-field">
+                      <label>Category label</label>
+                      <input
+                        value={draft.category_name}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_name', event.target.value)}
+                        placeholder="Materials"
+                      />
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_business_personal || card.supports_split) && (
+                  <div>
+                    <div className="inbox-field">
+                      <label>Business treatment</label>
+                    </div>
+                    <div className="inbox-toggle-row">
+                      {[
+                        { value: 'BUSINESS', label: 'Business' },
+                        { value: 'PERSONAL', label: 'Personal' },
+                        { value: 'SPLIT', label: 'Split' },
+                      ].map((option) => (
+                        <button
+                          key={option.value}
+                          type="button"
+                          className={`inbox-toggle ${draft.business_personal === option.value ? 'active' : ''}`}
+                          onClick={() => setInboxDraftField(card.id, 'business_personal', option.value)}
+                        >
+                          {option.label}
+                        </button>
+                      ))}
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_split || isSplit) && (
+                  <div className="inbox-field">
+                    <label>Business split %</label>
+                    <input
+                      type="number"
+                      min="0"
+                      max="100"
+                      value={draft.split_business_pct}
+                      onChange={(event) => setInboxDraftField(card.id, 'split_business_pct', event.target.value)}
+                      placeholder="80"
+                    />
+                  </div>
+                )}
+
+                {card.supports_duplicate && (
+                  <>
+                    <div>
+                      <div className="inbox-field">
+                        <label>Duplicate action</label>
+                      </div>
+                      <div className="inbox-duplicate-row">
+                        {[
+                          { value: 'dismiss', label: 'Dismiss duplicate' },
+                          { value: 'merge', label: 'Merge to original' },
+                        ].map((option) => (
+                          <button
+                            key={option.value}
+                            type="button"
+                            className={`inbox-toggle ${draft.duplicate_action === option.value ? 'active' : ''}`}
+                            onClick={() => setInboxDraftField(card.id, 'duplicate_action', option.value)}
+                          >
+                            {option.label}
+                          </button>
+                        ))}
+                      </div>
+                    </div>
+                    {draft.duplicate_action === 'merge' && (
+                      <div className="inbox-field">
+                        <label>Original transaction id</label>
+                        <input
+                          value={draft.duplicate_of_txn_id}
+                          onChange={(event) => setInboxDraftField(card.id, 'duplicate_of_txn_id', event.target.value)}
+                          placeholder="txn-9001"
+                        />
+                      </div>
+                    )}
+                  </>
+                )}
 
+                <div className="inbox-footer-row">
+                  <div className="inbox-inline-status">
+                    Route: {card.resolution_target?.route || `/api/v1/inbox/${card.id}`}
+                  </div>
+                  <div className="inbox-footer-actions">
+                    <button className="secondary-action" onClick={clearInboxMessage} disabled={saving}>
+                      Clear banner
+                    </button>
+                    <button className="primary-action" onClick={() => submitInboxAction(card)} disabled={saving}>
+                      {saving ? <RefreshCcw size={16} className="spinner" /> : <CheckCircle2 size={16} />}
+                      Save and clear blocker
+                    </button>
+                  </div>
+                </div>
+              </section>
+            );
+          })}
+        </div>
+      )}
+    </div>
+    );
+  };
   const renderControlCentre = () => {
     const policyLabels = {
       vat_scheme: 'VAT scheme',
@@ -2510,11 +2968,11 @@
           <section className="readiness-panel">
             <h5 style={{ marginBottom: 12 }}>Queue Backlog</h5>
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
-                <div key={item.id} className="control-row">
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
+                <div key={item.id || item.affected_entity_id} className="control-row">
                   <div>
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
                   <div className="text-end">
                     <div className="small-muted">{item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date}</div>
@@ -2522,7 +2980,7 @@
                   </div>
                 </div>
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
               )}
             </div>
@@ -2668,7 +3126,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2697,7 +3155,7 @@
   };
 
   const createSnapshotVersion = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     const status = snapshotWorkspace?.snapshot_status;
 
     if (!status?.can_create_snapshot_version) {
@@ -2719,7 +3177,7 @@
         description: `Quarter snapshot generated from Quarter workspace`,
         metadata: {
           source_type: 'manual',
-          readiness_pct: quarterReadiness?.readiness_pct ?? null
+          readiness_pct: activeQuarterReport?.readiness_pct ?? null
         }
       });
       await refreshQuarterWorkspace();
@@ -2734,7 +3192,7 @@
   };
 
   const closeQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(closeReason || '').trim()) {
       setQuarterActionError('Close reason is required.');
       setQuarterActionMessage('');
@@ -2785,7 +3243,7 @@
   };
 
   const reopenQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(reopenReason || '').trim()) {
       setQuarterActionError('Reopen reason is required.');
       setQuarterActionMessage('');
@@ -2844,9 +3302,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3342,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3354,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3365,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3379,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..669173cd6176ee9ee5c86c5895ea49cb9524ebc4
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,155 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, fallbackIssues = [], resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const issueSource = Array.isArray(report.issue_list) && report.issue_list.length > 0
+    ? report.issue_list
+    : fallbackIssues;
+  const allIssues = sortIssues(Array.isArray(issueSource) ? issueSource : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/bizPA/frontend/src/quarterReadiness.test.js b/bizPA/frontend/src/quarterReadiness.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..0b810191a89d8b82091bf5de1408afcc23c14e65
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.test.js
@@ -0,0 +1,93 @@
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
+
+const reportFixture = {
+  quarter_reference: 'Q1-2026',
+  period_start: '2026-01-01',
+  period_end: '2026-03-31',
+  total_txns_in_period: 18,
+  readiness_pct: 78,
+  can_export: false,
+  issue_list: [
+    {
+      issue_type: 'missing_category',
+      affected_entity_id: 'txn-9101',
+      severity: 'high',
+      weight: 35,
+      explanation: 'Category missing for Fuel Stop.',
+      txn_date: '2026-02-04',
+      merchant: 'Fuel Stop',
+      amount: 64.18,
+      direction: 'out',
+      resolution_target: {
+        label: 'Classify transaction',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'missing_business_personal',
+      affected_entity_id: 'txn-9103',
+      severity: 'high',
+      weight: 30,
+      explanation: 'Business/personal decision missing.',
+      txn_date: '2026-02-15',
+      merchant: 'Mobile Contract',
+      amount: 42.5,
+      direction: 'out',
+      resolution_target: {
+        label: 'Set business or personal',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'unresolved_duplicate',
+      affected_entity_id: 'txn-9104',
+      severity: 'medium',
+      weight: 15,
+      explanation: 'Duplicate not resolved.',
+      txn_date: '2026-03-02',
+      merchant: 'Client Payment',
+      amount: 820,
+      direction: 'in',
+      resolution_target: {
+        label: 'Resolve duplicate',
+        workflow: '/inbox/finish-now',
+      },
+    },
+  ],
+};
+
+describe('quarter readiness helpers', () => {
+  test('keeps blockers ordered and updates headline while blockers remain', () => {
+    const state = buildQuarterUiState({ report: reportFixture, resolvedIssueIds: ['txn-9104'] });
+
+    expect(state.itemsLeft).toBe(2);
+    expect(state.exportEnabled).toBe(false);
+    expect(state.report.readiness_pct).toBeGreaterThan(78);
+    expect(state.headline).toContain('items left');
+    expect(state.activeIssues.map((issue) => issue.affected_entity_id)).toEqual(['txn-9101', 'txn-9103']);
+    expect(state.report.explanation_lines[0]).toContain('Readiness is');
+  });
+
+  test('unlocks export immediately when every blocker is resolved', () => {
+    const state = buildQuarterUiState({
+      report: reportFixture,
+      resolvedIssueIds: ['txn-9101', 'txn-9103', 'txn-9104'],
+    });
+
+    expect(state.itemsLeft).toBe(0);
+    expect(state.exportEnabled).toBe(true);
+    expect(state.report.readiness_pct).toBe(100);
+    expect(state.report.can_export).toBe(true);
+    expect(state.report.issue_summary).toEqual([]);
+  });
+
+  test('supports demo query params that resolve all blockers', () => {
+    const params = new URLSearchParams('resolvedDemoIssues=all');
+    const resolvedIds = deriveResolvedIssueIdsFromSearchParams(params, reportFixture.issue_list);
+
+    expect(resolvedIds).toEqual(['txn-9101', 'txn-9103', 'txn-9104']);
+  });
+});
diff --git a/bizPA/start_bizpa_tax_readiness_ui.ps1 b/bizPA/start_bizpa_tax_readiness_ui.ps1
index 1fc4bef8cd172c31fdc3db52d1acc9d4775c5485..7216e44eac386fe436a9af8e9b417cd2d61e37fe
--- a/bizPA/start_bizpa_tax_readiness_ui.ps1
+++ b/bizPA/start_bizpa_tax_readiness_ui.ps1
@@ -4,6 +4,7 @@
 $frontendPath = Join-Path $repoRoot 'frontend'
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
 
 Write-Host 'Starting bizPA tax readiness UI...' -ForegroundColor Cyan
 
@@ -14,7 +15,10 @@
 Write-Host 'Tax readiness dashboard URL:' -ForegroundColor Green
 Write-Host "  $readinessUrl" -ForegroundColor Yellow
 Write-Host ''
+Write-Host 'Export-ready verification URL:' -ForegroundColor Green
+Write-Host "  $exportReadyUrl" -ForegroundColor Yellow
+Write-Host ''
 Write-Host 'Base frontend URL:' -ForegroundColor Green
 Write-Host "  $frontendUrl" -ForegroundColor Yellow
 Write-Host ''
-Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI can be reviewed without backend setup.' -ForegroundColor Cyan
+Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI and zero-blocker export-ready state can be reviewed without backend setup.' -ForegroundColor Cyan
diff --git a/bizPA/tax_readiness_ui_smoke.ps1 b/bizPA/tax_readiness_ui_smoke.ps1
index 56d67892a51b22572503dec560d17123b370c2b7..e6ff461166a62f708cc4a0d987a6aeabba278bc6
--- a/bizPA/tax_readiness_ui_smoke.ps1
+++ b/bizPA/tax_readiness_ui_smoke.ps1
@@ -28,38 +28,43 @@
 
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
-$screenshotPath = 'C:\Users\edebe\eds\workstream\verification\20260311_201500_bizpa_tax_readiness_ui.png'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
+$verificationRoot = 'C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification'
+$quarterScreenshotPath = Join-Path $verificationRoot 'quarter_readiness_screen.png'
+$finishNowScreenshotPath = Join-Path $verificationRoot 'finish_now_queue_zero_blockers.png'
 $chromeProfilePath = 'C:\Users\edebe\eds\workstream\verification\chrome_capture_profile_tax_readiness'
+$chromePath = 'C:\Program Files\Google\Chrome\Application\chrome.exe'
 
-New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
-
-$frontendJob = Start-Job -ScriptBlock {
-  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
-  $env:PORT = '3002'
-  npm.cmd start
-}
+function Wait-FrontendReady {
+  param (
+    [string]$Url
+  )
 
-try {
-  $frontendResponse = $null
-  $frontendReady = $false
   for ($i = 0; $i -lt 45; $i++) {
     Start-Sleep -Seconds 2
     try {
-      $frontendResponse = Invoke-WebRequest -UseBasicParsing $readinessUrl
-      if ($frontendResponse.StatusCode -eq 200) {
-        $frontendReady = $true
-        break
+      $response = Invoke-WebRequest -UseBasicParsing $Url
+      if ($response.StatusCode -eq 200) {
+        return $response
       }
     } catch {
     }
   }
 
-  if (-not $frontendReady) {
-    throw "Frontend did not become ready on $readinessUrl"
-  }
+  throw "Frontend did not become ready on $Url"
+}
 
+function Capture-ChromeAppScreenshot {
+  param (
+    [string]$Url,
+    [string]$OutputPath
+  )
+
+  $captureProfilePath = Join-Path $chromeProfilePath ([System.IO.Path]::GetFileNameWithoutExtension($OutputPath))
+  New-Item -ItemType Directory -Force -Path $captureProfilePath | Out-Null
+
   $chromeArgs = @(
-    "--user-data-dir=$chromeProfilePath",
+    "--user-data-dir=$captureProfilePath",
     '--new-window',
     '--window-size=1600,1500',
     '--no-first-run',
@@ -68,9 +73,9 @@
     '--disable-breakpad',
     '--disable-crash-reporter',
     '--noerrdialogs',
-    "--app=$readinessUrl"
+    $Url
   )
-  Start-Process 'C:\Program Files\Google\Chrome\Application\chrome.exe' -ArgumentList $chromeArgs | Out-Null
+  Start-Process $chromePath -ArgumentList $chromeArgs | Out-Null
 
   $chromeWindow = $null
   for ($i = 0; $i -lt 20; $i++) {
@@ -96,7 +101,7 @@
   }
 
   if (-not $chromeWindow) {
-    throw 'Chrome readiness window did not expose a main window handle for screenshot capture.'
+    throw "Chrome window did not expose a main window handle for $Url"
   }
 
   [WindowCaptureNative]::ShowWindow($chromeWindow.MainWindowHandle, 3) | Out-Null
@@ -110,17 +115,35 @@
   $bitmap = New-Object System.Drawing.Bitmap $width, $height
   $graphics = [System.Drawing.Graphics]::FromImage($bitmap)
   $graphics.CopyFromScreen($rect.Left, $rect.Top, 0, 0, $bitmap.Size)
-  $bitmap.Save($screenshotPath, [System.Drawing.Imaging.ImageFormat]::Png)
+  $bitmap.Save($OutputPath, [System.Drawing.Imaging.ImageFormat]::Png)
   $graphics.Dispose()
   $bitmap.Dispose()
 
   Get-Process chrome -ErrorAction SilentlyContinue |
     Where-Object { $_.StartTime -ge $chromeWindow.StartTime.AddSeconds(-2) } |
     Stop-Process -Force -ErrorAction SilentlyContinue
+  Start-Sleep -Seconds 2
+}
 
+New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
+New-Item -ItemType Directory -Force -Path $verificationRoot | Out-Null
+
+$frontendJob = Start-Job -ScriptBlock {
+  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
+  $env:PORT = '3002'
+  npm.cmd start
+}
+
+try {
+  $frontendResponse = Wait-FrontendReady -Url $readinessUrl
+  Capture-ChromeAppScreenshot -Url $readinessUrl -OutputPath $quarterScreenshotPath
+  Capture-ChromeAppScreenshot -Url $exportReadyUrl -OutputPath $finishNowScreenshotPath
+
   "READINESS_URL=$readinessUrl"
+  "EXPORT_READY_URL=$exportReadyUrl"
   "FRONTEND_STATUS=$($frontendResponse.StatusCode)"
-  "SCREENSHOT=$screenshotPath"
+  "SCREENSHOT_QUARTER=$quarterScreenshotPath"
+  "SCREENSHOT_EXPORT_READY=$finishNowScreenshotPath"
   'FRONTEND_LOG_START'
   Receive-Job $frontendJob -Keep -ErrorAction SilentlyContinue 2>&1
   'FRONTEND_LOG_END'
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..b0b6a961dcb80123d7634a661b59a1e944c1c7a1
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,26 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    fallbackIssues: inboxItems,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1532,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1547,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1633,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2104,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2316,29 +2562,241 @@
     </div>
   );
 
-  const renderInbox = () => (
-    <div>
-      <h4>Finish Now Queue</h4>
-      <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
-      <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
-            <div>
-              <b>{item.merchant || 'Unknown'}</b>
-              <br />
-              <small>
-                {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
-              </small>
-              <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
-            </div>
+  const renderInbox = () => {
+    return (
+    <div className="inbox-mobile-shell">
+      <section className="inbox-mobile-hero">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2">
+          <div className="inbox-kicker">
+            <Bell size={14} />
+            Mobile-first exception queue
           </div>
-        ))}
-      </div>
-    </div>
-  );
+          <div className="status-pill">{filteredInboxCards.length} visible</div>
+        </div>
+        <h4>Finish now</h4>
+        <p>
+          Clear the blockers that stop the quarterly export: category, business or personal, split treatment, and duplicate resolution.
+        </p>
+        <div className="inbox-mobile-stats">
+          <div className="inbox-mobile-stat">
+            <small>Queue</small>
+            <strong>{inboxCards.length}</strong>
+            <small>{activeQuarterReport.quarter_reference || READINESS_DEMO_REPORT.quarter_reference}</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Critical</small>
+            <strong>{highSeverityCount}</strong>
+            <small>High-severity blockers</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Ready now</small>
+            <strong>{readyToExportCount}</strong>
+            <small>Transactions already export-safe</small>
+          </div>
+        </div>
+        <div className="hero-actions">
+          <button className="hero-action-main" onClick={() => setCurrentTab('quarter')}>
+            <ArrowRight size={16} />
+            Review quarter readiness
+          </button>
+          <button className="hero-action-secondary" onClick={undoLastInboxAction} disabled={inboxBusyId === 'undo-last'}>
+            {inboxBusyId === 'undo-last' ? <RefreshCcw size={16} className="spinner" /> : <ArchiveRestore size={16} />}
+            Undo last action
+          </button>
+        </div>
+      </section>
+
+      {inboxMessage && (
+        <div className={`readiness-panel inbox-message ${inboxMessage.type}`}>
+          {inboxMessage.text}
+        </div>
+      )}
+
+      <section className="readiness-panel">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-3">
+          <div>
+            <h5 style={{ marginBottom: 4 }}>Queue filters</h5>
+            <small className="small-muted">Keep the mobile pass focused on one blocker type at a time.</small>
+          </div>
+          <div className="status-pill">{activeQuarterReport.readiness_pct ?? READINESS_DEMO_REPORT.readiness_pct}% ready</div>
+        </div>
+        <div className="inbox-filter-row">
+          {INBOX_FILTERS.map((filter) => {
+            const count = filter.key === 'all'
+              ? inboxCards.length
+              : inboxCards.filter((card) => card.issue_types.includes(filter.key)).length;
+            return (
+              <button
+                key={filter.key}
+                type="button"
+                className={`inbox-filter-chip ${inboxFilter === filter.key ? 'active' : ''}`}
+                onClick={() => setInboxFilter(filter.key)}
+              >
+                <span>{filter.label}</span>
+                <small>{count}</small>
+              </button>
+            );
+          })}
+        </div>
+      </section>
+
+      {filteredInboxCards.length === 0 ? (
+        <div className="inbox-empty">
+          No blockers match this filter. Reset to All blockers or return to the quarter screen to confirm readiness.
+        </div>
+      ) : (
+        <div className="inbox-card-list">
+          {filteredInboxCards.map((card) => {
+            const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+            const saving = inboxBusyId === card.id;
+            const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+            return (
+              <section key={card.id} className="inbox-action-card">
+                <div className="inbox-card-top">
+                  <div>
+                    <strong>{card.merchant}</strong>
+                    <div className="inbox-card-subline">
+                      {card.txn_date ? String(card.txn_date).slice(0, 10) : 'Date pending'} · {formatIssueAmount(card.amount, card.direction)}
+                    </div>
+                  </div>
+                  <span className={`inbox-issue-chip ${card.severity}`}>
+                    <TriangleAlert size={14} />
+                    {INBOX_REASON_LABELS[card.blocker_reason] || card.blocker_reason.replace(/_/g, ' ')}
+                  </span>
+                </div>
+
+                <div className="inbox-chip-row">
+                  {card.issue_labels.map((label) => (
+                    <span key={`${card.id}-${label}`} className={`inbox-issue-chip ${card.severity}`}>
+                      {label}
+                    </span>
+                  ))}
+                  {card.suggested_category?.category_name && (
+                    <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
+                      <CheckCircle2 size={14} />
+                      Suggested: {card.suggested_category.category_name}
+                    </button>
+                  )}
+                </div>
+
+                <p className="inbox-explainer">{card.explanation || 'Resolve the blocking fields to clear this transaction from the queue.'}</p>
+
+                {(card.supports_category || card.supports_business_personal || card.supports_split) && (
+                  <div className="inbox-action-grid">
+                    <div className="inbox-field">
+                      <label>Category code</label>
+                      <input
+                        value={draft.category_code}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_code', event.target.value)}
+                        placeholder="MATERIALS"
+                      />
+                    </div>
+                    <div className="inbox-field">
+                      <label>Category label</label>
+                      <input
+                        value={draft.category_name}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_name', event.target.value)}
+                        placeholder="Materials"
+                      />
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_business_personal || card.supports_split) && (
+                  <div>
+                    <div className="inbox-field">
+                      <label>Business treatment</label>
+                    </div>
+                    <div className="inbox-toggle-row">
+                      {[
+                        { value: 'BUSINESS', label: 'Business' },
+                        { value: 'PERSONAL', label: 'Personal' },
+                        { value: 'SPLIT', label: 'Split' },
+                      ].map((option) => (
+                        <button
+                          key={option.value}
+                          type="button"
+                          className={`inbox-toggle ${draft.business_personal === option.value ? 'active' : ''}`}
+                          onClick={() => setInboxDraftField(card.id, 'business_personal', option.value)}
+                        >
+                          {option.label}
+                        </button>
+                      ))}
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_split || isSplit) && (
+                  <div className="inbox-field">
+                    <label>Business split %</label>
+                    <input
+                      type="number"
+                      min="0"
+                      max="100"
+                      value={draft.split_business_pct}
+                      onChange={(event) => setInboxDraftField(card.id, 'split_business_pct', event.target.value)}
+                      placeholder="80"
+                    />
+                  </div>
+                )}
+
+                {card.supports_duplicate && (
+                  <>
+                    <div>
+                      <div className="inbox-field">
+                        <label>Duplicate action</label>
+                      </div>
+                      <div className="inbox-duplicate-row">
+                        {[
+                          { value: 'dismiss', label: 'Dismiss duplicate' },
+                          { value: 'merge', label: 'Merge to original' },
+                        ].map((option) => (
+                          <button
+                            key={option.value}
+                            type="button"
+                            className={`inbox-toggle ${draft.duplicate_action === option.value ? 'active' : ''}`}
+                            onClick={() => setInboxDraftField(card.id, 'duplicate_action', option.value)}
+                          >
+                            {option.label}
+                          </button>
+                        ))}
+                      </div>
+                    </div>
+                    {draft.duplicate_action === 'merge' && (
+                      <div className="inbox-field">
+                        <label>Original transaction id</label>
+                        <input
+                          value={draft.duplicate_of_txn_id}
+                          onChange={(event) => setInboxDraftField(card.id, 'duplicate_of_txn_id', event.target.value)}
+                          placeholder="txn-9001"
+                        />
+                      </div>
+                    )}
+                  </>
+                )}
 
+                <div className="inbox-footer-row">
+                  <div className="inbox-inline-status">
+                    Route: {card.resolution_target?.route || `/api/v1/inbox/${card.id}`}
+                  </div>
+                  <div className="inbox-footer-actions">
+                    <button className="secondary-action" onClick={clearInboxMessage} disabled={saving}>
+                      Clear banner
+                    </button>
+                    <button className="primary-action" onClick={() => submitInboxAction(card)} disabled={saving}>
+                      {saving ? <RefreshCcw size={16} className="spinner" /> : <CheckCircle2 size={16} />}
+                      Save and clear blocker
+                    </button>
+                  </div>
+                </div>
+              </section>
+            );
+          })}
+        </div>
+      )}
+    </div>
+    );
+  };
   const renderControlCentre = () => {
     const policyLabels = {
       vat_scheme: 'VAT scheme',
@@ -2510,11 +2968,11 @@
           <section className="readiness-panel">
             <h5 style={{ marginBottom: 12 }}>Queue Backlog</h5>
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
-                <div key={item.id} className="control-row">
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
+                <div key={item.id || item.affected_entity_id} className="control-row">
                   <div>
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
                   <div className="text-end">
                     <div className="small-muted">{item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date}</div>
@@ -2522,7 +2980,7 @@
                   </div>
                 </div>
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
               )}
             </div>
@@ -2668,7 +3126,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2697,7 +3155,7 @@
   };
 
   const createSnapshotVersion = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     const status = snapshotWorkspace?.snapshot_status;
 
     if (!status?.can_create_snapshot_version) {
@@ -2719,7 +3177,7 @@
         description: `Quarter snapshot generated from Quarter workspace`,
         metadata: {
           source_type: 'manual',
-          readiness_pct: quarterReadiness?.readiness_pct ?? null
+          readiness_pct: activeQuarterReport?.readiness_pct ?? null
         }
       });
       await refreshQuarterWorkspace();
@@ -2734,7 +3192,7 @@
   };
 
   const closeQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(closeReason || '').trim()) {
       setQuarterActionError('Close reason is required.');
       setQuarterActionMessage('');
@@ -2785,7 +3243,7 @@
   };
 
   const reopenQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(reopenReason || '').trim()) {
       setQuarterActionError('Reopen reason is required.');
       setQuarterActionMessage('');
@@ -2844,9 +3302,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3342,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3354,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3365,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3379,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..669173cd6176ee9ee5c86c5895ea49cb9524ebc4
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,155 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, fallbackIssues = [], resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const issueSource = Array.isArray(report.issue_list) && report.issue_list.length > 0
+    ? report.issue_list
+    : fallbackIssues;
+  const allIssues = sortIssues(Array.isArray(issueSource) ? issueSource : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/bizPA/frontend/src/quarterReadiness.test.js b/bizPA/frontend/src/quarterReadiness.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..0b810191a89d8b82091bf5de1408afcc23c14e65
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.test.js
@@ -0,0 +1,93 @@
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
+
+const reportFixture = {
+  quarter_reference: 'Q1-2026',
+  period_start: '2026-01-01',
+  period_end: '2026-03-31',
+  total_txns_in_period: 18,
+  readiness_pct: 78,
+  can_export: false,
+  issue_list: [
+    {
+      issue_type: 'missing_category',
+      affected_entity_id: 'txn-9101',
+      severity: 'high',
+      weight: 35,
+      explanation: 'Category missing for Fuel Stop.',
+      txn_date: '2026-02-04',
+      merchant: 'Fuel Stop',
+      amount: 64.18,
+      direction: 'out',
+      resolution_target: {
+        label: 'Classify transaction',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'missing_business_personal',
+      affected_entity_id: 'txn-9103',
+      severity: 'high',
+      weight: 30,
+      explanation: 'Business/personal decision missing.',
+      txn_date: '2026-02-15',
+      merchant: 'Mobile Contract',
+      amount: 42.5,
+      direction: 'out',
+      resolution_target: {
+        label: 'Set business or personal',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'unresolved_duplicate',
+      affected_entity_id: 'txn-9104',
+      severity: 'medium',
+      weight: 15,
+      explanation: 'Duplicate not resolved.',
+      txn_date: '2026-03-02',
+      merchant: 'Client Payment',
+      amount: 820,
+      direction: 'in',
+      resolution_target: {
+        label: 'Resolve duplicate',
+        workflow: '/inbox/finish-now',
+      },
+    },
+  ],
+};
+
+describe('quarter readiness helpers', () => {
+  test('keeps blockers ordered and updates headline while blockers remain', () => {
+    const state = buildQuarterUiState({ report: reportFixture, resolvedIssueIds: ['txn-9104'] });
+
+    expect(state.itemsLeft).toBe(2);
+    expect(state.exportEnabled).toBe(false);
+    expect(state.report.readiness_pct).toBeGreaterThan(78);
+    expect(state.headline).toContain('items left');
+    expect(state.activeIssues.map((issue) => issue.affected_entity_id)).toEqual(['txn-9101', 'txn-9103']);
+    expect(state.report.explanation_lines[0]).toContain('Readiness is');
+  });
+
+  test('unlocks export immediately when every blocker is resolved', () => {
+    const state = buildQuarterUiState({
+      report: reportFixture,
+      resolvedIssueIds: ['txn-9101', 'txn-9103', 'txn-9104'],
+    });
+
+    expect(state.itemsLeft).toBe(0);
+    expect(state.exportEnabled).toBe(true);
+    expect(state.report.readiness_pct).toBe(100);
+    expect(state.report.can_export).toBe(true);
+    expect(state.report.issue_summary).toEqual([]);
+  });
+
+  test('supports demo query params that resolve all blockers', () => {
+    const params = new URLSearchParams('resolvedDemoIssues=all');
+    const resolvedIds = deriveResolvedIssueIdsFromSearchParams(params, reportFixture.issue_list);
+
+    expect(resolvedIds).toEqual(['txn-9101', 'txn-9103', 'txn-9104']);
+  });
+});
diff --git a/bizPA/start_bizpa_tax_readiness_ui.ps1 b/bizPA/start_bizpa_tax_readiness_ui.ps1
index 1fc4bef8cd172c31fdc3db52d1acc9d4775c5485..7216e44eac386fe436a9af8e9b417cd2d61e37fe
--- a/bizPA/start_bizpa_tax_readiness_ui.ps1
+++ b/bizPA/start_bizpa_tax_readiness_ui.ps1
@@ -4,6 +4,7 @@
 $frontendPath = Join-Path $repoRoot 'frontend'
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
 
 Write-Host 'Starting bizPA tax readiness UI...' -ForegroundColor Cyan
 
@@ -14,7 +15,10 @@
 Write-Host 'Tax readiness dashboard URL:' -ForegroundColor Green
 Write-Host "  $readinessUrl" -ForegroundColor Yellow
 Write-Host ''
+Write-Host 'Export-ready verification URL:' -ForegroundColor Green
+Write-Host "  $exportReadyUrl" -ForegroundColor Yellow
+Write-Host ''
 Write-Host 'Base frontend URL:' -ForegroundColor Green
 Write-Host "  $frontendUrl" -ForegroundColor Yellow
 Write-Host ''
-Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI can be reviewed without backend setup.' -ForegroundColor Cyan
+Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI and zero-blocker export-ready state can be reviewed without backend setup.' -ForegroundColor Cyan
diff --git a/bizPA/tax_readiness_ui_smoke.ps1 b/bizPA/tax_readiness_ui_smoke.ps1
index 56d67892a51b22572503dec560d17123b370c2b7..e6ff461166a62f708cc4a0d987a6aeabba278bc6
--- a/bizPA/tax_readiness_ui_smoke.ps1
+++ b/bizPA/tax_readiness_ui_smoke.ps1
@@ -28,38 +28,43 @@
 
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
-$screenshotPath = 'C:\Users\edebe\eds\workstream\verification\20260311_201500_bizpa_tax_readiness_ui.png'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
+$verificationRoot = 'C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification'
+$quarterScreenshotPath = Join-Path $verificationRoot 'quarter_readiness_screen.png'
+$finishNowScreenshotPath = Join-Path $verificationRoot 'finish_now_queue_zero_blockers.png'
 $chromeProfilePath = 'C:\Users\edebe\eds\workstream\verification\chrome_capture_profile_tax_readiness'
+$chromePath = 'C:\Program Files\Google\Chrome\Application\chrome.exe'
 
-New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
-
-$frontendJob = Start-Job -ScriptBlock {
-  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
-  $env:PORT = '3002'
-  npm.cmd start
-}
+function Wait-FrontendReady {
+  param (
+    [string]$Url
+  )
 
-try {
-  $frontendResponse = $null
-  $frontendReady = $false
   for ($i = 0; $i -lt 45; $i++) {
     Start-Sleep -Seconds 2
     try {
-      $frontendResponse = Invoke-WebRequest -UseBasicParsing $readinessUrl
-      if ($frontendResponse.StatusCode -eq 200) {
-        $frontendReady = $true
-        break
+      $response = Invoke-WebRequest -UseBasicParsing $Url
+      if ($response.StatusCode -eq 200) {
+        return $response
       }
     } catch {
     }
   }
 
-  if (-not $frontendReady) {
-    throw "Frontend did not become ready on $readinessUrl"
-  }
+  throw "Frontend did not become ready on $Url"
+}
 
+function Capture-ChromeAppScreenshot {
+  param (
+    [string]$Url,
+    [string]$OutputPath
+  )
+
+  $captureProfilePath = Join-Path $chromeProfilePath ([System.IO.Path]::GetFileNameWithoutExtension($OutputPath))
+  New-Item -ItemType Directory -Force -Path $captureProfilePath | Out-Null
+
   $chromeArgs = @(
-    "--user-data-dir=$chromeProfilePath",
+    "--user-data-dir=$captureProfilePath",
     '--new-window',
     '--window-size=1600,1500',
     '--no-first-run',
@@ -68,9 +73,9 @@
     '--disable-breakpad',
     '--disable-crash-reporter',
     '--noerrdialogs',
-    "--app=$readinessUrl"
+    $Url
   )
-  Start-Process 'C:\Program Files\Google\Chrome\Application\chrome.exe' -ArgumentList $chromeArgs | Out-Null
+  Start-Process $chromePath -ArgumentList $chromeArgs | Out-Null
 
   $chromeWindow = $null
   for ($i = 0; $i -lt 20; $i++) {
@@ -96,7 +101,7 @@
   }
 
   if (-not $chromeWindow) {
-    throw 'Chrome readiness window did not expose a main window handle for screenshot capture.'
+    throw "Chrome window did not expose a main window handle for $Url"
   }
 
   [WindowCaptureNative]::ShowWindow($chromeWindow.MainWindowHandle, 3) | Out-Null
@@ -110,17 +115,35 @@
   $bitmap = New-Object System.Drawing.Bitmap $width, $height
   $graphics = [System.Drawing.Graphics]::FromImage($bitmap)
   $graphics.CopyFromScreen($rect.Left, $rect.Top, 0, 0, $bitmap.Size)
-  $bitmap.Save($screenshotPath, [System.Drawing.Imaging.ImageFormat]::Png)
+  $bitmap.Save($OutputPath, [System.Drawing.Imaging.ImageFormat]::Png)
   $graphics.Dispose()
   $bitmap.Dispose()
 
   Get-Process chrome -ErrorAction SilentlyContinue |
     Where-Object { $_.StartTime -ge $chromeWindow.StartTime.AddSeconds(-2) } |
     Stop-Process -Force -ErrorAction SilentlyContinue
+  Start-Sleep -Seconds 2
+}
 
+New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
+New-Item -ItemType Directory -Force -Path $verificationRoot | Out-Null
+
+$frontendJob = Start-Job -ScriptBlock {
+  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
+  $env:PORT = '3002'
+  npm.cmd start
+}
+
+try {
+  $frontendResponse = Wait-FrontendReady -Url $readinessUrl
+  Capture-ChromeAppScreenshot -Url $readinessUrl -OutputPath $quarterScreenshotPath
+  Capture-ChromeAppScreenshot -Url $exportReadyUrl -OutputPath $finishNowScreenshotPath
+
   "READINESS_URL=$readinessUrl"
+  "EXPORT_READY_URL=$exportReadyUrl"
   "FRONTEND_STATUS=$($frontendResponse.StatusCode)"
-  "SCREENSHOT=$screenshotPath"
+  "SCREENSHOT_QUARTER=$quarterScreenshotPath"
+  "SCREENSHOT_EXPORT_READY=$finishNowScreenshotPath"
   'FRONTEND_LOG_START'
   Receive-Job $frontendJob -Keep -ErrorAction SilentlyContinue 2>&1
   'FRONTEND_LOG_END'
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "powershell -ExecutionPolicy Bypass -File 'C:\\Users\\edebe\\eds\\bizPA\\tax_readiness_ui_smoke.ps1'" in C:\Users\edebe\eds exited 1 in 52.41s:
Chrome window did not expose a main window handle for http://127.0.0.1:3002/?readinessDemo=1&tab=quarter
At C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1:104 char:5
+     throw "Chrome window did not expose a main window handle for $Url ...
+     ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : OperationStopped: (Chrome window d...o=1&tab=quarter:String) [], RuntimeException
    + FullyQualifiedErrorId : Chrome window did not expose a main window handle for http://127.0.0.1:3002/?readinessDe 
   mo=1&tab=quarter
 
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..b0b6a961dcb80123d7634a661b59a1e944c1c7a1
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,26 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    fallbackIssues: inboxItems,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1532,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1547,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1633,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2104,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2316,29 +2562,241 @@
     </div>
   );
 
-  const renderInbox = () => (
-    <div>
-      <h4>Finish Now Queue</h4>
-      <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
-      <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
-            <div>
-              <b>{item.merchant || 'Unknown'}</b>
-              <br />
-              <small>
-                {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
-              </small>
-              <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
-            </div>
+  const renderInbox = () => {
+    return (
+    <div className="inbox-mobile-shell">
+      <section className="inbox-mobile-hero">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2">
+          <div className="inbox-kicker">
+            <Bell size={14} />
+            Mobile-first exception queue
           </div>
-        ))}
-      </div>
-    </div>
-  );
+          <div className="status-pill">{filteredInboxCards.length} visible</div>
+        </div>
+        <h4>Finish now</h4>
+        <p>
+          Clear the blockers that stop the quarterly export: category, business or personal, split treatment, and duplicate resolution.
+        </p>
+        <div className="inbox-mobile-stats">
+          <div className="inbox-mobile-stat">
+            <small>Queue</small>
+            <strong>{inboxCards.length}</strong>
+            <small>{activeQuarterReport.quarter_reference || READINESS_DEMO_REPORT.quarter_reference}</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Critical</small>
+            <strong>{highSeverityCount}</strong>
+            <small>High-severity blockers</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Ready now</small>
+            <strong>{readyToExportCount}</strong>
+            <small>Transactions already export-safe</small>
+          </div>
+        </div>
+        <div className="hero-actions">
+          <button className="hero-action-main" onClick={() => setCurrentTab('quarter')}>
+            <ArrowRight size={16} />
+            Review quarter readiness
+          </button>
+          <button className="hero-action-secondary" onClick={undoLastInboxAction} disabled={inboxBusyId === 'undo-last'}>
+            {inboxBusyId === 'undo-last' ? <RefreshCcw size={16} className="spinner" /> : <ArchiveRestore size={16} />}
+            Undo last action
+          </button>
+        </div>
+      </section>
+
+      {inboxMessage && (
+        <div className={`readiness-panel inbox-message ${inboxMessage.type}`}>
+          {inboxMessage.text}
+        </div>
+      )}
+
+      <section className="readiness-panel">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-3">
+          <div>
+            <h5 style={{ marginBottom: 4 }}>Queue filters</h5>
+            <small className="small-muted">Keep the mobile pass focused on one blocker type at a time.</small>
+          </div>
+          <div className="status-pill">{activeQuarterReport.readiness_pct ?? READINESS_DEMO_REPORT.readiness_pct}% ready</div>
+        </div>
+        <div className="inbox-filter-row">
+          {INBOX_FILTERS.map((filter) => {
+            const count = filter.key === 'all'
+              ? inboxCards.length
+              : inboxCards.filter((card) => card.issue_types.includes(filter.key)).length;
+            return (
+              <button
+                key={filter.key}
+                type="button"
+                className={`inbox-filter-chip ${inboxFilter === filter.key ? 'active' : ''}`}
+                onClick={() => setInboxFilter(filter.key)}
+              >
+                <span>{filter.label}</span>
+                <small>{count}</small>
+              </button>
+            );
+          })}
+        </div>
+      </section>
+
+      {filteredInboxCards.length === 0 ? (
+        <div className="inbox-empty">
+          No blockers match this filter. Reset to All blockers or return to the quarter screen to confirm readiness.
+        </div>
+      ) : (
+        <div className="inbox-card-list">
+          {filteredInboxCards.map((card) => {
+            const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+            const saving = inboxBusyId === card.id;
+            const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+            return (
+              <section key={card.id} className="inbox-action-card">
+                <div className="inbox-card-top">
+                  <div>
+                    <strong>{card.merchant}</strong>
+                    <div className="inbox-card-subline">
+                      {card.txn_date ? String(card.txn_date).slice(0, 10) : 'Date pending'} · {formatIssueAmount(card.amount, card.direction)}
+                    </div>
+                  </div>
+                  <span className={`inbox-issue-chip ${card.severity}`}>
+                    <TriangleAlert size={14} />
+                    {INBOX_REASON_LABELS[card.blocker_reason] || card.blocker_reason.replace(/_/g, ' ')}
+                  </span>
+                </div>
+
+                <div className="inbox-chip-row">
+                  {card.issue_labels.map((label) => (
+                    <span key={`${card.id}-${label}`} className={`inbox-issue-chip ${card.severity}`}>
+                      {label}
+                    </span>
+                  ))}
+                  {card.suggested_category?.category_name && (
+                    <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
+                      <CheckCircle2 size={14} />
+                      Suggested: {card.suggested_category.category_name}
+                    </button>
+                  )}
+                </div>
+
+                <p className="inbox-explainer">{card.explanation || 'Resolve the blocking fields to clear this transaction from the queue.'}</p>
+
+                {(card.supports_category || card.supports_business_personal || card.supports_split) && (
+                  <div className="inbox-action-grid">
+                    <div className="inbox-field">
+                      <label>Category code</label>
+                      <input
+                        value={draft.category_code}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_code', event.target.value)}
+                        placeholder="MATERIALS"
+                      />
+                    </div>
+                    <div className="inbox-field">
+                      <label>Category label</label>
+                      <input
+                        value={draft.category_name}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_name', event.target.value)}
+                        placeholder="Materials"
+                      />
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_business_personal || card.supports_split) && (
+                  <div>
+                    <div className="inbox-field">
+                      <label>Business treatment</label>
+                    </div>
+                    <div className="inbox-toggle-row">
+                      {[
+                        { value: 'BUSINESS', label: 'Business' },
+                        { value: 'PERSONAL', label: 'Personal' },
+                        { value: 'SPLIT', label: 'Split' },
+                      ].map((option) => (
+                        <button
+                          key={option.value}
+                          type="button"
+                          className={`inbox-toggle ${draft.business_personal === option.value ? 'active' : ''}`}
+                          onClick={() => setInboxDraftField(card.id, 'business_personal', option.value)}
+                        >
+                          {option.label}
+                        </button>
+                      ))}
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_split || isSplit) && (
+                  <div className="inbox-field">
+                    <label>Business split %</label>
+                    <input
+                      type="number"
+                      min="0"
+                      max="100"
+                      value={draft.split_business_pct}
+                      onChange={(event) => setInboxDraftField(card.id, 'split_business_pct', event.target.value)}
+                      placeholder="80"
+                    />
+                  </div>
+                )}
+
+                {card.supports_duplicate && (
+                  <>
+                    <div>
+                      <div className="inbox-field">
+                        <label>Duplicate action</label>
+                      </div>
+                      <div className="inbox-duplicate-row">
+                        {[
+                          { value: 'dismiss', label: 'Dismiss duplicate' },
+                          { value: 'merge', label: 'Merge to original' },
+                        ].map((option) => (
+                          <button
+                            key={option.value}
+                            type="button"
+                            className={`inbox-toggle ${draft.duplicate_action === option.value ? 'active' : ''}`}
+                            onClick={() => setInboxDraftField(card.id, 'duplicate_action', option.value)}
+                          >
+                            {option.label}
+                          </button>
+                        ))}
+                      </div>
+                    </div>
+                    {draft.duplicate_action === 'merge' && (
+                      <div className="inbox-field">
+                        <label>Original transaction id</label>
+                        <input
+                          value={draft.duplicate_of_txn_id}
+                          onChange={(event) => setInboxDraftField(card.id, 'duplicate_of_txn_id', event.target.value)}
+                          placeholder="txn-9001"
+                        />
+                      </div>
+                    )}
+                  </>
+                )}
 
+                <div className="inbox-footer-row">
+                  <div className="inbox-inline-status">
+                    Route: {card.resolution_target?.route || `/api/v1/inbox/${card.id}`}
+                  </div>
+                  <div className="inbox-footer-actions">
+                    <button className="secondary-action" onClick={clearInboxMessage} disabled={saving}>
+                      Clear banner
+                    </button>
+                    <button className="primary-action" onClick={() => submitInboxAction(card)} disabled={saving}>
+                      {saving ? <RefreshCcw size={16} className="spinner" /> : <CheckCircle2 size={16} />}
+                      Save and clear blocker
+                    </button>
+                  </div>
+                </div>
+              </section>
+            );
+          })}
+        </div>
+      )}
+    </div>
+    );
+  };
   const renderControlCentre = () => {
     const policyLabels = {
       vat_scheme: 'VAT scheme',
@@ -2510,11 +2968,11 @@
           <section className="readiness-panel">
             <h5 style={{ marginBottom: 12 }}>Queue Backlog</h5>
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
-                <div key={item.id} className="control-row">
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
+                <div key={item.id || item.affected_entity_id} className="control-row">
                   <div>
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
                   <div className="text-end">
                     <div className="small-muted">{item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date}</div>
@@ -2522,7 +2980,7 @@
                   </div>
                 </div>
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
               )}
             </div>
@@ -2668,7 +3126,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2697,7 +3155,7 @@
   };
 
   const createSnapshotVersion = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     const status = snapshotWorkspace?.snapshot_status;
 
     if (!status?.can_create_snapshot_version) {
@@ -2719,7 +3177,7 @@
         description: `Quarter snapshot generated from Quarter workspace`,
         metadata: {
           source_type: 'manual',
-          readiness_pct: quarterReadiness?.readiness_pct ?? null
+          readiness_pct: activeQuarterReport?.readiness_pct ?? null
         }
       });
       await refreshQuarterWorkspace();
@@ -2734,7 +3192,7 @@
   };
 
   const closeQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(closeReason || '').trim()) {
       setQuarterActionError('Close reason is required.');
       setQuarterActionMessage('');
@@ -2785,7 +3243,7 @@
   };
 
   const reopenQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(reopenReason || '').trim()) {
       setQuarterActionError('Reopen reason is required.');
       setQuarterActionMessage('');
@@ -2844,9 +3302,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3342,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3354,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3365,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3379,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..669173cd6176ee9ee5c86c5895ea49cb9524ebc4
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,155 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, fallbackIssues = [], resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const issueSource = Array.isArray(report.issue_list) && report.issue_list.length > 0
+    ? report.issue_list
+    : fallbackIssues;
+  const allIssues = sortIssues(Array.isArray(issueSource) ? issueSource : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/bizPA/frontend/src/quarterReadiness.test.js b/bizPA/frontend/src/quarterReadiness.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..0b810191a89d8b82091bf5de1408afcc23c14e65
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.test.js
@@ -0,0 +1,93 @@
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
+
+const reportFixture = {
+  quarter_reference: 'Q1-2026',
+  period_start: '2026-01-01',
+  period_end: '2026-03-31',
+  total_txns_in_period: 18,
+  readiness_pct: 78,
+  can_export: false,
+  issue_list: [
+    {
+      issue_type: 'missing_category',
+      affected_entity_id: 'txn-9101',
+      severity: 'high',
+      weight: 35,
+      explanation: 'Category missing for Fuel Stop.',
+      txn_date: '2026-02-04',
+      merchant: 'Fuel Stop',
+      amount: 64.18,
+      direction: 'out',
+      resolution_target: {
+        label: 'Classify transaction',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'missing_business_personal',
+      affected_entity_id: 'txn-9103',
+      severity: 'high',
+      weight: 30,
+      explanation: 'Business/personal decision missing.',
+      txn_date: '2026-02-15',
+      merchant: 'Mobile Contract',
+      amount: 42.5,
+      direction: 'out',
+      resolution_target: {
+        label: 'Set business or personal',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'unresolved_duplicate',
+      affected_entity_id: 'txn-9104',
+      severity: 'medium',
+      weight: 15,
+      explanation: 'Duplicate not resolved.',
+      txn_date: '2026-03-02',
+      merchant: 'Client Payment',
+      amount: 820,
+      direction: 'in',
+      resolution_target: {
+        label: 'Resolve duplicate',
+        workflow: '/inbox/finish-now',
+      },
+    },
+  ],
+};
+
+describe('quarter readiness helpers', () => {
+  test('keeps blockers ordered and updates headline while blockers remain', () => {
+    const state = buildQuarterUiState({ report: reportFixture, resolvedIssueIds: ['txn-9104'] });
+
+    expect(state.itemsLeft).toBe(2);
+    expect(state.exportEnabled).toBe(false);
+    expect(state.report.readiness_pct).toBeGreaterThan(78);
+    expect(state.headline).toContain('items left');
+    expect(state.activeIssues.map((issue) => issue.affected_entity_id)).toEqual(['txn-9101', 'txn-9103']);
+    expect(state.report.explanation_lines[0]).toContain('Readiness is');
+  });
+
+  test('unlocks export immediately when every blocker is resolved', () => {
+    const state = buildQuarterUiState({
+      report: reportFixture,
+      resolvedIssueIds: ['txn-9101', 'txn-9103', 'txn-9104'],
+    });
+
+    expect(state.itemsLeft).toBe(0);
+    expect(state.exportEnabled).toBe(true);
+    expect(state.report.readiness_pct).toBe(100);
+    expect(state.report.can_export).toBe(true);
+    expect(state.report.issue_summary).toEqual([]);
+  });
+
+  test('supports demo query params that resolve all blockers', () => {
+    const params = new URLSearchParams('resolvedDemoIssues=all');
+    const resolvedIds = deriveResolvedIssueIdsFromSearchParams(params, reportFixture.issue_list);
+
+    expect(resolvedIds).toEqual(['txn-9101', 'txn-9103', 'txn-9104']);
+  });
+});
diff --git a/bizPA/start_bizpa_tax_readiness_ui.ps1 b/bizPA/start_bizpa_tax_readiness_ui.ps1
index 1fc4bef8cd172c31fdc3db52d1acc9d4775c5485..7216e44eac386fe436a9af8e9b417cd2d61e37fe
--- a/bizPA/start_bizpa_tax_readiness_ui.ps1
+++ b/bizPA/start_bizpa_tax_readiness_ui.ps1
@@ -4,6 +4,7 @@
 $frontendPath = Join-Path $repoRoot 'frontend'
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
 
 Write-Host 'Starting bizPA tax readiness UI...' -ForegroundColor Cyan
 
@@ -14,7 +15,10 @@
 Write-Host 'Tax readiness dashboard URL:' -ForegroundColor Green
 Write-Host "  $readinessUrl" -ForegroundColor Yellow
 Write-Host ''
+Write-Host 'Export-ready verification URL:' -ForegroundColor Green
+Write-Host "  $exportReadyUrl" -ForegroundColor Yellow
+Write-Host ''
 Write-Host 'Base frontend URL:' -ForegroundColor Green
 Write-Host "  $frontendUrl" -ForegroundColor Yellow
 Write-Host ''
-Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI can be reviewed without backend setup.' -ForegroundColor Cyan
+Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI and zero-blocker export-ready state can be reviewed without backend setup.' -ForegroundColor Cyan
diff --git a/bizPA/tax_readiness_ui_smoke.ps1 b/bizPA/tax_readiness_ui_smoke.ps1
index 56d67892a51b22572503dec560d17123b370c2b7..e6ff461166a62f708cc4a0d987a6aeabba278bc6
--- a/bizPA/tax_readiness_ui_smoke.ps1
+++ b/bizPA/tax_readiness_ui_smoke.ps1
@@ -28,38 +28,43 @@
 
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
-$screenshotPath = 'C:\Users\edebe\eds\workstream\verification\20260311_201500_bizpa_tax_readiness_ui.png'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
+$verificationRoot = 'C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification'
+$quarterScreenshotPath = Join-Path $verificationRoot 'quarter_readiness_screen.png'
+$finishNowScreenshotPath = Join-Path $verificationRoot 'finish_now_queue_zero_blockers.png'
 $chromeProfilePath = 'C:\Users\edebe\eds\workstream\verification\chrome_capture_profile_tax_readiness'
+$chromePath = 'C:\Program Files\Google\Chrome\Application\chrome.exe'
 
-New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
-
-$frontendJob = Start-Job -ScriptBlock {
-  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
-  $env:PORT = '3002'
-  npm.cmd start
-}
+function Wait-FrontendReady {
+  param (
+    [string]$Url
+  )
 
-try {
-  $frontendResponse = $null
-  $frontendReady = $false
   for ($i = 0; $i -lt 45; $i++) {
     Start-Sleep -Seconds 2
     try {
-      $frontendResponse = Invoke-WebRequest -UseBasicParsing $readinessUrl
-      if ($frontendResponse.StatusCode -eq 200) {
-        $frontendReady = $true
-        break
+      $response = Invoke-WebRequest -UseBasicParsing $Url
+      if ($response.StatusCode -eq 200) {
+        return $response
       }
     } catch {
     }
   }
 
-  if (-not $frontendReady) {
-    throw "Frontend did not become ready on $readinessUrl"
-  }
+  throw "Frontend did not become ready on $Url"
+}
 
+function Capture-ChromeAppScreenshot {
+  param (
+    [string]$Url,
+    [string]$OutputPath
+  )
+
+  $captureProfilePath = Join-Path $chromeProfilePath ([System.IO.Path]::GetFileNameWithoutExtension($OutputPath))
+  New-Item -ItemType Directory -Force -Path $captureProfilePath | Out-Null
+
   $chromeArgs = @(
-    "--user-data-dir=$chromeProfilePath",
+    "--user-data-dir=$captureProfilePath",
     '--new-window',
     '--window-size=1600,1500',
     '--no-first-run',
@@ -68,9 +73,9 @@
     '--disable-breakpad',
     '--disable-crash-reporter',
     '--noerrdialogs',
-    "--app=$readinessUrl"
+    $Url
   )
-  Start-Process 'C:\Program Files\Google\Chrome\Application\chrome.exe' -ArgumentList $chromeArgs | Out-Null
+  Start-Process $chromePath -ArgumentList $chromeArgs | Out-Null
 
   $chromeWindow = $null
   for ($i = 0; $i -lt 20; $i++) {
@@ -96,7 +101,7 @@
   }
 
   if (-not $chromeWindow) {
-    throw 'Chrome readiness window did not expose a main window handle for screenshot capture.'
+    throw "Chrome window did not expose a main window handle for $Url"
   }
 
   [WindowCaptureNative]::ShowWindow($chromeWindow.MainWindowHandle, 3) | Out-Null
@@ -110,17 +115,35 @@
   $bitmap = New-Object System.Drawing.Bitmap $width, $height
   $graphics = [System.Drawing.Graphics]::FromImage($bitmap)
   $graphics.CopyFromScreen($rect.Left, $rect.Top, 0, 0, $bitmap.Size)
-  $bitmap.Save($screenshotPath, [System.Drawing.Imaging.ImageFormat]::Png)
+  $bitmap.Save($OutputPath, [System.Drawing.Imaging.ImageFormat]::Png)
   $graphics.Dispose()
   $bitmap.Dispose()
 
   Get-Process chrome -ErrorAction SilentlyContinue |
     Where-Object { $_.StartTime -ge $chromeWindow.StartTime.AddSeconds(-2) } |
     Stop-Process -Force -ErrorAction SilentlyContinue
+  Start-Sleep -Seconds 2
+}
 
+New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
+New-Item -ItemType Directory -Force -Path $verificationRoot | Out-Null
+
+$frontendJob = Start-Job -ScriptBlock {
+  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
+  $env:PORT = '3002'
+  npm.cmd start
+}
+
+try {
+  $frontendResponse = Wait-FrontendReady -Url $readinessUrl
+  Capture-ChromeAppScreenshot -Url $readinessUrl -OutputPath $quarterScreenshotPath
+  Capture-ChromeAppScreenshot -Url $exportReadyUrl -OutputPath $finishNowScreenshotPath
+
   "READINESS_URL=$readinessUrl"
+  "EXPORT_READY_URL=$exportReadyUrl"
   "FRONTEND_STATUS=$($frontendResponse.StatusCode)"
-  "SCREENSHOT=$screenshotPath"
+  "SCREENSHOT_QUARTER=$quarterScreenshotPath"
+  "SCREENSHOT_EXPORT_READY=$finishNowScreenshotPath"
   'FRONTEND_LOG_START'
   Receive-Job $frontendJob -Keep -ErrorAction SilentlyContinue 2>&1
   'FRONTEND_LOG_END'
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

codex
The UI itself is reachable, but window-handle capture on this machine is unreliable. I’m switching the smoke harness to Chrome’s headless screenshot mode, which is simpler and better suited to automated evidence capture here.
file update
M C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1
@@ -67,4 +67,6 @@
     "--user-data-dir=$captureProfilePath",
-    '--new-window',
     '--window-size=1600,1500',
+    '--headless=new',
+    '--disable-gpu',
+    '--hide-scrollbars',
     '--no-first-run',
@@ -75,52 +77,12 @@
     '--noerrdialogs',
+    "--screenshot=$OutputPath",
     $Url
   )
-  Start-Process $chromePath -ArgumentList $chromeArgs | Out-Null
-
-  $chromeWindow = $null
-  for ($i = 0; $i -lt 20; $i++) {
-    Start-Sleep -Seconds 1
-    $chromeWindow = Get-Process chrome -ErrorAction SilentlyContinue |
-      ForEach-Object {
-        try {
-          if ($_.MainWindowHandle -and ([int64]$_.MainWindowHandle -ne 0)) {
-            [pscustomobject]@{
-              Process = $_
-              StartTime = $_.StartTime
-            }
-          }
-        } catch {
-        }
-      } |
-      Sort-Object StartTime -Descending |
-      Select-Object -First 1
-    if ($chromeWindow) {
-      $chromeWindow = $chromeWindow.Process
-      break
-    }
+  $process = Start-Process $chromePath -ArgumentList $chromeArgs -PassThru -Wait
+  if ($process.ExitCode -ne 0) {
+    throw "Chrome headless screenshot failed for $Url with exit code $($process.ExitCode)"
   }
-
-  if (-not $chromeWindow) {
-    throw "Chrome window did not expose a main window handle for $Url"
+  if (-not (Test-Path $OutputPath)) {
+    throw "Chrome did not create screenshot output for $Url"
   }
-
-  [WindowCaptureNative]::ShowWindow($chromeWindow.MainWindowHandle, 3) | Out-Null
-  [WindowCaptureNative]::SetForegroundWindow($chromeWindow.MainWindowHandle) | Out-Null
-  Start-Sleep -Seconds 1
-
-  $rect = New-Object WindowCaptureNative+RECT
-  [WindowCaptureNative]::GetWindowRect($chromeWindow.MainWindowHandle, [ref]$rect) | Out-Null
-  $width = [Math]::Max(1, $rect.Right - $rect.Left)
-  $height = [Math]::Max(1, $rect.Bottom - $rect.Top)
-  $bitmap = New-Object System.Drawing.Bitmap $width, $height
-  $graphics = [System.Drawing.Graphics]::FromImage($bitmap)
-  $graphics.CopyFromScreen($rect.Left, $rect.Top, 0, 0, $bitmap.Size)
-  $bitmap.Save($OutputPath, [System.Drawing.Imaging.ImageFormat]::Png)
-  $graphics.Dispose()
-  $bitmap.Dispose()
-
-  Get-Process chrome -ErrorAction SilentlyContinue |
-    Where-Object { $_.StartTime -ge $chromeWindow.StartTime.AddSeconds(-2) } |
-    Stop-Process -Force -ErrorAction SilentlyContinue
-  Start-Sleep -Seconds 2
 }
apply_patch(auto_approved=true) exited 0 in 502ms:
Success. Updated the following files:
M C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..b0b6a961dcb80123d7634a661b59a1e944c1c7a1
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,26 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    fallbackIssues: inboxItems,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1532,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1547,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1633,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2104,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2316,29 +2562,241 @@
     </div>
   );
 
-  const renderInbox = () => (
-    <div>
-      <h4>Finish Now Queue</h4>
-      <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
-      <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
-            <div>
-              <b>{item.merchant || 'Unknown'}</b>
-              <br />
-              <small>
-                {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
-              </small>
-              <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
-            </div>
+  const renderInbox = () => {
+    return (
+    <div className="inbox-mobile-shell">
+      <section className="inbox-mobile-hero">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2">
+          <div className="inbox-kicker">
+            <Bell size={14} />
+            Mobile-first exception queue
           </div>
-        ))}
-      </div>
-    </div>
-  );
+          <div className="status-pill">{filteredInboxCards.length} visible</div>
+        </div>
+        <h4>Finish now</h4>
+        <p>
+          Clear the blockers that stop the quarterly export: category, business or personal, split treatment, and duplicate resolution.
+        </p>
+        <div className="inbox-mobile-stats">
+          <div className="inbox-mobile-stat">
+            <small>Queue</small>
+            <strong>{inboxCards.length}</strong>
+            <small>{activeQuarterReport.quarter_reference || READINESS_DEMO_REPORT.quarter_reference}</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Critical</small>
+            <strong>{highSeverityCount}</strong>
+            <small>High-severity blockers</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Ready now</small>
+            <strong>{readyToExportCount}</strong>
+            <small>Transactions already export-safe</small>
+          </div>
+        </div>
+        <div className="hero-actions">
+          <button className="hero-action-main" onClick={() => setCurrentTab('quarter')}>
+            <ArrowRight size={16} />
+            Review quarter readiness
+          </button>
+          <button className="hero-action-secondary" onClick={undoLastInboxAction} disabled={inboxBusyId === 'undo-last'}>
+            {inboxBusyId === 'undo-last' ? <RefreshCcw size={16} className="spinner" /> : <ArchiveRestore size={16} />}
+            Undo last action
+          </button>
+        </div>
+      </section>
+
+      {inboxMessage && (
+        <div className={`readiness-panel inbox-message ${inboxMessage.type}`}>
+          {inboxMessage.text}
+        </div>
+      )}
+
+      <section className="readiness-panel">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-3">
+          <div>
+            <h5 style={{ marginBottom: 4 }}>Queue filters</h5>
+            <small className="small-muted">Keep the mobile pass focused on one blocker type at a time.</small>
+          </div>
+          <div className="status-pill">{activeQuarterReport.readiness_pct ?? READINESS_DEMO_REPORT.readiness_pct}% ready</div>
+        </div>
+        <div className="inbox-filter-row">
+          {INBOX_FILTERS.map((filter) => {
+            const count = filter.key === 'all'
+              ? inboxCards.length
+              : inboxCards.filter((card) => card.issue_types.includes(filter.key)).length;
+            return (
+              <button
+                key={filter.key}
+                type="button"
+                className={`inbox-filter-chip ${inboxFilter === filter.key ? 'active' : ''}`}
+                onClick={() => setInboxFilter(filter.key)}
+              >
+                <span>{filter.label}</span>
+                <small>{count}</small>
+              </button>
+            );
+          })}
+        </div>
+      </section>
+
+      {filteredInboxCards.length === 0 ? (
+        <div className="inbox-empty">
+          No blockers match this filter. Reset to All blockers or return to the quarter screen to confirm readiness.
+        </div>
+      ) : (
+        <div className="inbox-card-list">
+          {filteredInboxCards.map((card) => {
+            const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+            const saving = inboxBusyId === card.id;
+            const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+            return (
+              <section key={card.id} className="inbox-action-card">
+                <div className="inbox-card-top">
+                  <div>
+                    <strong>{card.merchant}</strong>
+                    <div className="inbox-card-subline">
+                      {card.txn_date ? String(card.txn_date).slice(0, 10) : 'Date pending'} · {formatIssueAmount(card.amount, card.direction)}
+                    </div>
+                  </div>
+                  <span className={`inbox-issue-chip ${card.severity}`}>
+                    <TriangleAlert size={14} />
+                    {INBOX_REASON_LABELS[card.blocker_reason] || card.blocker_reason.replace(/_/g, ' ')}
+                  </span>
+                </div>
+
+                <div className="inbox-chip-row">
+                  {card.issue_labels.map((label) => (
+                    <span key={`${card.id}-${label}`} className={`inbox-issue-chip ${card.severity}`}>
+                      {label}
+                    </span>
+                  ))}
+                  {card.suggested_category?.category_name && (
+                    <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
+                      <CheckCircle2 size={14} />
+                      Suggested: {card.suggested_category.category_name}
+                    </button>
+                  )}
+                </div>
+
+                <p className="inbox-explainer">{card.explanation || 'Resolve the blocking fields to clear this transaction from the queue.'}</p>
+
+                {(card.supports_category || card.supports_business_personal || card.supports_split) && (
+                  <div className="inbox-action-grid">
+                    <div className="inbox-field">
+                      <label>Category code</label>
+                      <input
+                        value={draft.category_code}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_code', event.target.value)}
+                        placeholder="MATERIALS"
+                      />
+                    </div>
+                    <div className="inbox-field">
+                      <label>Category label</label>
+                      <input
+                        value={draft.category_name}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_name', event.target.value)}
+                        placeholder="Materials"
+                      />
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_business_personal || card.supports_split) && (
+                  <div>
+                    <div className="inbox-field">
+                      <label>Business treatment</label>
+                    </div>
+                    <div className="inbox-toggle-row">
+                      {[
+                        { value: 'BUSINESS', label: 'Business' },
+                        { value: 'PERSONAL', label: 'Personal' },
+                        { value: 'SPLIT', label: 'Split' },
+                      ].map((option) => (
+                        <button
+                          key={option.value}
+                          type="button"
+                          className={`inbox-toggle ${draft.business_personal === option.value ? 'active' : ''}`}
+                          onClick={() => setInboxDraftField(card.id, 'business_personal', option.value)}
+                        >
+                          {option.label}
+                        </button>
+                      ))}
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_split || isSplit) && (
+                  <div className="inbox-field">
+                    <label>Business split %</label>
+                    <input
+                      type="number"
+                      min="0"
+                      max="100"
+                      value={draft.split_business_pct}
+                      onChange={(event) => setInboxDraftField(card.id, 'split_business_pct', event.target.value)}
+                      placeholder="80"
+                    />
+                  </div>
+                )}
+
+                {card.supports_duplicate && (
+                  <>
+                    <div>
+                      <div className="inbox-field">
+                        <label>Duplicate action</label>
+                      </div>
+                      <div className="inbox-duplicate-row">
+                        {[
+                          { value: 'dismiss', label: 'Dismiss duplicate' },
+                          { value: 'merge', label: 'Merge to original' },
+                        ].map((option) => (
+                          <button
+                            key={option.value}
+                            type="button"
+                            className={`inbox-toggle ${draft.duplicate_action === option.value ? 'active' : ''}`}
+                            onClick={() => setInboxDraftField(card.id, 'duplicate_action', option.value)}
+                          >
+                            {option.label}
+                          </button>
+                        ))}
+                      </div>
+                    </div>
+                    {draft.duplicate_action === 'merge' && (
+                      <div className="inbox-field">
+                        <label>Original transaction id</label>
+                        <input
+                          value={draft.duplicate_of_txn_id}
+                          onChange={(event) => setInboxDraftField(card.id, 'duplicate_of_txn_id', event.target.value)}
+                          placeholder="txn-9001"
+                        />
+                      </div>
+                    )}
+                  </>
+                )}
 
+                <div className="inbox-footer-row">
+                  <div className="inbox-inline-status">
+                    Route: {card.resolution_target?.route || `/api/v1/inbox/${card.id}`}
+                  </div>
+                  <div className="inbox-footer-actions">
+                    <button className="secondary-action" onClick={clearInboxMessage} disabled={saving}>
+                      Clear banner
+                    </button>
+                    <button className="primary-action" onClick={() => submitInboxAction(card)} disabled={saving}>
+                      {saving ? <RefreshCcw size={16} className="spinner" /> : <CheckCircle2 size={16} />}
+                      Save and clear blocker
+                    </button>
+                  </div>
+                </div>
+              </section>
+            );
+          })}
+        </div>
+      )}
+    </div>
+    );
+  };
   const renderControlCentre = () => {
     const policyLabels = {
       vat_scheme: 'VAT scheme',
@@ -2510,11 +2968,11 @@
           <section className="readiness-panel">
             <h5 style={{ marginBottom: 12 }}>Queue Backlog</h5>
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
-                <div key={item.id} className="control-row">
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
+                <div key={item.id || item.affected_entity_id} className="control-row">
                   <div>
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
                   <div className="text-end">
                     <div className="small-muted">{item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date}</div>
@@ -2522,7 +2980,7 @@
                   </div>
                 </div>
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
               )}
             </div>
@@ -2668,7 +3126,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2697,7 +3155,7 @@
   };
 
   const createSnapshotVersion = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     const status = snapshotWorkspace?.snapshot_status;
 
     if (!status?.can_create_snapshot_version) {
@@ -2719,7 +3177,7 @@
         description: `Quarter snapshot generated from Quarter workspace`,
         metadata: {
           source_type: 'manual',
-          readiness_pct: quarterReadiness?.readiness_pct ?? null
+          readiness_pct: activeQuarterReport?.readiness_pct ?? null
         }
       });
       await refreshQuarterWorkspace();
@@ -2734,7 +3192,7 @@
   };
 
   const closeQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(closeReason || '').trim()) {
       setQuarterActionError('Close reason is required.');
       setQuarterActionMessage('');
@@ -2785,7 +3243,7 @@
   };
 
   const reopenQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(reopenReason || '').trim()) {
       setQuarterActionError('Reopen reason is required.');
       setQuarterActionMessage('');
@@ -2844,9 +3302,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3342,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3354,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3365,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3379,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..669173cd6176ee9ee5c86c5895ea49cb9524ebc4
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,155 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, fallbackIssues = [], resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const issueSource = Array.isArray(report.issue_list) && report.issue_list.length > 0
+    ? report.issue_list
+    : fallbackIssues;
+  const allIssues = sortIssues(Array.isArray(issueSource) ? issueSource : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/bizPA/frontend/src/quarterReadiness.test.js b/bizPA/frontend/src/quarterReadiness.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..0b810191a89d8b82091bf5de1408afcc23c14e65
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.test.js
@@ -0,0 +1,93 @@
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
+
+const reportFixture = {
+  quarter_reference: 'Q1-2026',
+  period_start: '2026-01-01',
+  period_end: '2026-03-31',
+  total_txns_in_period: 18,
+  readiness_pct: 78,
+  can_export: false,
+  issue_list: [
+    {
+      issue_type: 'missing_category',
+      affected_entity_id: 'txn-9101',
+      severity: 'high',
+      weight: 35,
+      explanation: 'Category missing for Fuel Stop.',
+      txn_date: '2026-02-04',
+      merchant: 'Fuel Stop',
+      amount: 64.18,
+      direction: 'out',
+      resolution_target: {
+        label: 'Classify transaction',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'missing_business_personal',
+      affected_entity_id: 'txn-9103',
+      severity: 'high',
+      weight: 30,
+      explanation: 'Business/personal decision missing.',
+      txn_date: '2026-02-15',
+      merchant: 'Mobile Contract',
+      amount: 42.5,
+      direction: 'out',
+      resolution_target: {
+        label: 'Set business or personal',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'unresolved_duplicate',
+      affected_entity_id: 'txn-9104',
+      severity: 'medium',
+      weight: 15,
+      explanation: 'Duplicate not resolved.',
+      txn_date: '2026-03-02',
+      merchant: 'Client Payment',
+      amount: 820,
+      direction: 'in',
+      resolution_target: {
+        label: 'Resolve duplicate',
+        workflow: '/inbox/finish-now',
+      },
+    },
+  ],
+};
+
+describe('quarter readiness helpers', () => {
+  test('keeps blockers ordered and updates headline while blockers remain', () => {
+    const state = buildQuarterUiState({ report: reportFixture, resolvedIssueIds: ['txn-9104'] });
+
+    expect(state.itemsLeft).toBe(2);
+    expect(state.exportEnabled).toBe(false);
+    expect(state.report.readiness_pct).toBeGreaterThan(78);
+    expect(state.headline).toContain('items left');
+    expect(state.activeIssues.map((issue) => issue.affected_entity_id)).toEqual(['txn-9101', 'txn-9103']);
+    expect(state.report.explanation_lines[0]).toContain('Readiness is');
+  });
+
+  test('unlocks export immediately when every blocker is resolved', () => {
+    const state = buildQuarterUiState({
+      report: reportFixture,
+      resolvedIssueIds: ['txn-9101', 'txn-9103', 'txn-9104'],
+    });
+
+    expect(state.itemsLeft).toBe(0);
+    expect(state.exportEnabled).toBe(true);
+    expect(state.report.readiness_pct).toBe(100);
+    expect(state.report.can_export).toBe(true);
+    expect(state.report.issue_summary).toEqual([]);
+  });
+
+  test('supports demo query params that resolve all blockers', () => {
+    const params = new URLSearchParams('resolvedDemoIssues=all');
+    const resolvedIds = deriveResolvedIssueIdsFromSearchParams(params, reportFixture.issue_list);
+
+    expect(resolvedIds).toEqual(['txn-9101', 'txn-9103', 'txn-9104']);
+  });
+});
diff --git a/bizPA/start_bizpa_tax_readiness_ui.ps1 b/bizPA/start_bizpa_tax_readiness_ui.ps1
index 1fc4bef8cd172c31fdc3db52d1acc9d4775c5485..7216e44eac386fe436a9af8e9b417cd2d61e37fe
--- a/bizPA/start_bizpa_tax_readiness_ui.ps1
+++ b/bizPA/start_bizpa_tax_readiness_ui.ps1
@@ -4,6 +4,7 @@
 $frontendPath = Join-Path $repoRoot 'frontend'
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
 
 Write-Host 'Starting bizPA tax readiness UI...' -ForegroundColor Cyan
 
@@ -14,7 +15,10 @@
 Write-Host 'Tax readiness dashboard URL:' -ForegroundColor Green
 Write-Host "  $readinessUrl" -ForegroundColor Yellow
 Write-Host ''
+Write-Host 'Export-ready verification URL:' -ForegroundColor Green
+Write-Host "  $exportReadyUrl" -ForegroundColor Yellow
+Write-Host ''
 Write-Host 'Base frontend URL:' -ForegroundColor Green
 Write-Host "  $frontendUrl" -ForegroundColor Yellow
 Write-Host ''
-Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI can be reviewed without backend setup.' -ForegroundColor Cyan
+Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI and zero-blocker export-ready state can be reviewed without backend setup.' -ForegroundColor Cyan
diff --git a/bizPA/tax_readiness_ui_smoke.ps1 b/bizPA/tax_readiness_ui_smoke.ps1
index 56d67892a51b22572503dec560d17123b370c2b7..dd689ee4cf40ca47dbd6893b2db390f877e774d9
--- a/bizPA/tax_readiness_ui_smoke.ps1
+++ b/bizPA/tax_readiness_ui_smoke.ps1
@@ -28,99 +28,84 @@
 
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
-$screenshotPath = 'C:\Users\edebe\eds\workstream\verification\20260311_201500_bizpa_tax_readiness_ui.png'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
+$verificationRoot = 'C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification'
+$quarterScreenshotPath = Join-Path $verificationRoot 'quarter_readiness_screen.png'
+$finishNowScreenshotPath = Join-Path $verificationRoot 'finish_now_queue_zero_blockers.png'
 $chromeProfilePath = 'C:\Users\edebe\eds\workstream\verification\chrome_capture_profile_tax_readiness'
-
-New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
+$chromePath = 'C:\Program Files\Google\Chrome\Application\chrome.exe'
 
-$frontendJob = Start-Job -ScriptBlock {
-  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
-  $env:PORT = '3002'
-  npm.cmd start
-}
+function Wait-FrontendReady {
+  param (
+    [string]$Url
+  )
 
-try {
-  $frontendResponse = $null
-  $frontendReady = $false
   for ($i = 0; $i -lt 45; $i++) {
     Start-Sleep -Seconds 2
     try {
-      $frontendResponse = Invoke-WebRequest -UseBasicParsing $readinessUrl
-      if ($frontendResponse.StatusCode -eq 200) {
-        $frontendReady = $true
-        break
+      $response = Invoke-WebRequest -UseBasicParsing $Url
+      if ($response.StatusCode -eq 200) {
+        return $response
       }
     } catch {
     }
   }
 
-  if (-not $frontendReady) {
-    throw "Frontend did not become ready on $readinessUrl"
-  }
+  throw "Frontend did not become ready on $Url"
+}
+
+function Capture-ChromeAppScreenshot {
+  param (
+    [string]$Url,
+    [string]$OutputPath
+  )
 
+  $captureProfilePath = Join-Path $chromeProfilePath ([System.IO.Path]::GetFileNameWithoutExtension($OutputPath))
+  New-Item -ItemType Directory -Force -Path $captureProfilePath | Out-Null
+
   $chromeArgs = @(
-    "--user-data-dir=$chromeProfilePath",
-    '--new-window',
+    "--user-data-dir=$captureProfilePath",
     '--window-size=1600,1500',
+    '--headless=new',
+    '--disable-gpu',
+    '--hide-scrollbars',
     '--no-first-run',
     '--disable-session-crashed-bubble',
     '--disable-infobars',
     '--disable-breakpad',
     '--disable-crash-reporter',
     '--noerrdialogs',
-    "--app=$readinessUrl"
+    "--screenshot=$OutputPath",
+    $Url
   )
-  Start-Process 'C:\Program Files\Google\Chrome\Application\chrome.exe' -ArgumentList $chromeArgs | Out-Null
-
-  $chromeWindow = $null
-  for ($i = 0; $i -lt 20; $i++) {
-    Start-Sleep -Seconds 1
-    $chromeWindow = Get-Process chrome -ErrorAction SilentlyContinue |
-      ForEach-Object {
-        try {
-          if ($_.MainWindowHandle -and ([int64]$_.MainWindowHandle -ne 0)) {
-            [pscustomobject]@{
-              Process = $_
-              StartTime = $_.StartTime
-            }
-          }
-        } catch {
-        }
-      } |
-      Sort-Object StartTime -Descending |
-      Select-Object -First 1
-    if ($chromeWindow) {
-      $chromeWindow = $chromeWindow.Process
-      break
-    }
+  $process = Start-Process $chromePath -ArgumentList $chromeArgs -PassThru -Wait
+  if ($process.ExitCode -ne 0) {
+    throw "Chrome headless screenshot failed for $Url with exit code $($process.ExitCode)"
   }
-
-  if (-not $chromeWindow) {
-    throw 'Chrome readiness window did not expose a main window handle for screenshot capture.'
+  if (-not (Test-Path $OutputPath)) {
+    throw "Chrome did not create screenshot output for $Url"
   }
+}
 
-  [WindowCaptureNative]::ShowWindow($chromeWindow.MainWindowHandle, 3) | Out-Null
-  [WindowCaptureNative]::SetForegroundWindow($chromeWindow.MainWindowHandle) | Out-Null
-  Start-Sleep -Seconds 1
+New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
+New-Item -ItemType Directory -Force -Path $verificationRoot | Out-Null
 
-  $rect = New-Object WindowCaptureNative+RECT
-  [WindowCaptureNative]::GetWindowRect($chromeWindow.MainWindowHandle, [ref]$rect) | Out-Null
-  $width = [Math]::Max(1, $rect.Right - $rect.Left)
-  $height = [Math]::Max(1, $rect.Bottom - $rect.Top)
-  $bitmap = New-Object System.Drawing.Bitmap $width, $height
-  $graphics = [System.Drawing.Graphics]::FromImage($bitmap)
-  $graphics.CopyFromScreen($rect.Left, $rect.Top, 0, 0, $bitmap.Size)
-  $bitmap.Save($screenshotPath, [System.Drawing.Imaging.ImageFormat]::Png)
-  $graphics.Dispose()
-  $bitmap.Dispose()
+$frontendJob = Start-Job -ScriptBlock {
+  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
+  $env:PORT = '3002'
+  npm.cmd start
+}
 
-  Get-Process chrome -ErrorAction SilentlyContinue |
-    Where-Object { $_.StartTime -ge $chromeWindow.StartTime.AddSeconds(-2) } |
-    Stop-Process -Force -ErrorAction SilentlyContinue
+try {
+  $frontendResponse = Wait-FrontendReady -Url $readinessUrl
+  Capture-ChromeAppScreenshot -Url $readinessUrl -OutputPath $quarterScreenshotPath
+  Capture-ChromeAppScreenshot -Url $exportReadyUrl -OutputPath $finishNowScreenshotPath
 
   "READINESS_URL=$readinessUrl"
+  "EXPORT_READY_URL=$exportReadyUrl"
   "FRONTEND_STATUS=$($frontendResponse.StatusCode)"
-  "SCREENSHOT=$screenshotPath"
+  "SCREENSHOT_QUARTER=$quarterScreenshotPath"
+  "SCREENSHOT_EXPORT_READY=$finishNowScreenshotPath"
   'FRONTEND_LOG_START'
   Receive-Job $frontendJob -Keep -ErrorAction SilentlyContinue 2>&1
   'FRONTEND_LOG_END'
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..b0b6a961dcb80123d7634a661b59a1e944c1c7a1
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,26 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    fallbackIssues: inboxItems,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1532,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1547,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1633,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2104,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2316,29 +2562,241 @@
     </div>
   );
 
-  const renderInbox = () => (
-    <div>
-      <h4>Finish Now Queue</h4>
-      <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
-      <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
-            <div>
-              <b>{item.merchant || 'Unknown'}</b>
-              <br />
-              <small>
-                {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
-              </small>
-              <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
-            </div>
+  const renderInbox = () => {
+    return (
+    <div className="inbox-mobile-shell">
+      <section className="inbox-mobile-hero">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2">
+          <div className="inbox-kicker">
+            <Bell size={14} />
+            Mobile-first exception queue
           </div>
-        ))}
-      </div>
-    </div>
-  );
+          <div className="status-pill">{filteredInboxCards.length} visible</div>
+        </div>
+        <h4>Finish now</h4>
+        <p>
+          Clear the blockers that stop the quarterly export: category, business or personal, split treatment, and duplicate resolution.
+        </p>
+        <div className="inbox-mobile-stats">
+          <div className="inbox-mobile-stat">
+            <small>Queue</small>
+            <strong>{inboxCards.length}</strong>
+            <small>{activeQuarterReport.quarter_reference || READINESS_DEMO_REPORT.quarter_reference}</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Critical</small>
+            <strong>{highSeverityCount}</strong>
+            <small>High-severity blockers</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Ready now</small>
+            <strong>{readyToExportCount}</strong>
+            <small>Transactions already export-safe</small>
+          </div>
+        </div>
+        <div className="hero-actions">
+          <button className="hero-action-main" onClick={() => setCurrentTab('quarter')}>
+            <ArrowRight size={16} />
+            Review quarter readiness
+          </button>
+          <button className="hero-action-secondary" onClick={undoLastInboxAction} disabled={inboxBusyId === 'undo-last'}>
+            {inboxBusyId === 'undo-last' ? <RefreshCcw size={16} className="spinner" /> : <ArchiveRestore size={16} />}
+            Undo last action
+          </button>
+        </div>
+      </section>
+
+      {inboxMessage && (
+        <div className={`readiness-panel inbox-message ${inboxMessage.type}`}>
+          {inboxMessage.text}
+        </div>
+      )}
+
+      <section className="readiness-panel">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-3">
+          <div>
+            <h5 style={{ marginBottom: 4 }}>Queue filters</h5>
+            <small className="small-muted">Keep the mobile pass focused on one blocker type at a time.</small>
+          </div>
+          <div className="status-pill">{activeQuarterReport.readiness_pct ?? READINESS_DEMO_REPORT.readiness_pct}% ready</div>
+        </div>
+        <div className="inbox-filter-row">
+          {INBOX_FILTERS.map((filter) => {
+            const count = filter.key === 'all'
+              ? inboxCards.length
+              : inboxCards.filter((card) => card.issue_types.includes(filter.key)).length;
+            return (
+              <button
+                key={filter.key}
+                type="button"
+                className={`inbox-filter-chip ${inboxFilter === filter.key ? 'active' : ''}`}
+                onClick={() => setInboxFilter(filter.key)}
+              >
+                <span>{filter.label}</span>
+                <small>{count}</small>
+              </button>
+            );
+          })}
+        </div>
+      </section>
+
+      {filteredInboxCards.length === 0 ? (
+        <div className="inbox-empty">
+          No blockers match this filter. Reset to All blockers or return to the quarter screen to confirm readiness.
+        </div>
+      ) : (
+        <div className="inbox-card-list">
+          {filteredInboxCards.map((card) => {
+            const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+            const saving = inboxBusyId === card.id;
+            const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+            return (
+              <section key={card.id} className="inbox-action-card">
+                <div className="inbox-card-top">
+                  <div>
+                    <strong>{card.merchant}</strong>
+                    <div className="inbox-card-subline">
+                      {card.txn_date ? String(card.txn_date).slice(0, 10) : 'Date pending'} · {formatIssueAmount(card.amount, card.direction)}
+                    </div>
+                  </div>
+                  <span className={`inbox-issue-chip ${card.severity}`}>
+                    <TriangleAlert size={14} />
+                    {INBOX_REASON_LABELS[card.blocker_reason] || card.blocker_reason.replace(/_/g, ' ')}
+                  </span>
+                </div>
+
+                <div className="inbox-chip-row">
+                  {card.issue_labels.map((label) => (
+                    <span key={`${card.id}-${label}`} className={`inbox-issue-chip ${card.severity}`}>
+                      {label}
+                    </span>
+                  ))}
+                  {card.suggested_category?.category_name && (
+                    <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
+                      <CheckCircle2 size={14} />
+                      Suggested: {card.suggested_category.category_name}
+                    </button>
+                  )}
+                </div>
+
+                <p className="inbox-explainer">{card.explanation || 'Resolve the blocking fields to clear this transaction from the queue.'}</p>
+
+                {(card.supports_category || card.supports_business_personal || card.supports_split) && (
+                  <div className="inbox-action-grid">
+                    <div className="inbox-field">
+                      <label>Category code</label>
+                      <input
+                        value={draft.category_code}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_code', event.target.value)}
+                        placeholder="MATERIALS"
+                      />
+                    </div>
+                    <div className="inbox-field">
+                      <label>Category label</label>
+                      <input
+                        value={draft.category_name}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_name', event.target.value)}
+                        placeholder="Materials"
+                      />
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_business_personal || card.supports_split) && (
+                  <div>
+                    <div className="inbox-field">
+                      <label>Business treatment</label>
+                    </div>
+                    <div className="inbox-toggle-row">
+                      {[
+                        { value: 'BUSINESS', label: 'Business' },
+                        { value: 'PERSONAL', label: 'Personal' },
+                        { value: 'SPLIT', label: 'Split' },
+                      ].map((option) => (
+                        <button
+                          key={option.value}
+                          type="button"
+                          className={`inbox-toggle ${draft.business_personal === option.value ? 'active' : ''}`}
+                          onClick={() => setInboxDraftField(card.id, 'business_personal', option.value)}
+                        >
+                          {option.label}
+                        </button>
+                      ))}
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_split || isSplit) && (
+                  <div className="inbox-field">
+                    <label>Business split %</label>
+                    <input
+                      type="number"
+                      min="0"
+                      max="100"
+                      value={draft.split_business_pct}
+                      onChange={(event) => setInboxDraftField(card.id, 'split_business_pct', event.target.value)}
+                      placeholder="80"
+                    />
+                  </div>
+                )}
+
+                {card.supports_duplicate && (
+                  <>
+                    <div>
+                      <div className="inbox-field">
+                        <label>Duplicate action</label>
+                      </div>
+                      <div className="inbox-duplicate-row">
+                        {[
+                          { value: 'dismiss', label: 'Dismiss duplicate' },
+                          { value: 'merge', label: 'Merge to original' },
+                        ].map((option) => (
+                          <button
+                            key={option.value}
+                            type="button"
+                            className={`inbox-toggle ${draft.duplicate_action === option.value ? 'active' : ''}`}
+                            onClick={() => setInboxDraftField(card.id, 'duplicate_action', option.value)}
+                          >
+                            {option.label}
+                          </button>
+                        ))}
+                      </div>
+                    </div>
+                    {draft.duplicate_action === 'merge' && (
+                      <div className="inbox-field">
+                        <label>Original transaction id</label>
+                        <input
+                          value={draft.duplicate_of_txn_id}
+                          onChange={(event) => setInboxDraftField(card.id, 'duplicate_of_txn_id', event.target.value)}
+                          placeholder="txn-9001"
+                        />
+                      </div>
+                    )}
+                  </>
+                )}
 
+                <div className="inbox-footer-row">
+                  <div className="inbox-inline-status">
+                    Route: {card.resolution_target?.route || `/api/v1/inbox/${card.id}`}
+                  </div>
+                  <div className="inbox-footer-actions">
+                    <button className="secondary-action" onClick={clearInboxMessage} disabled={saving}>
+                      Clear banner
+                    </button>
+                    <button className="primary-action" onClick={() => submitInboxAction(card)} disabled={saving}>
+                      {saving ? <RefreshCcw size={16} className="spinner" /> : <CheckCircle2 size={16} />}
+                      Save and clear blocker
+                    </button>
+                  </div>
+                </div>
+              </section>
+            );
+          })}
+        </div>
+      )}
+    </div>
+    );
+  };
   const renderControlCentre = () => {
     const policyLabels = {
       vat_scheme: 'VAT scheme',
@@ -2510,11 +2968,11 @@
           <section className="readiness-panel">
             <h5 style={{ marginBottom: 12 }}>Queue Backlog</h5>
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
-                <div key={item.id} className="control-row">
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
+                <div key={item.id || item.affected_entity_id} className="control-row">
                   <div>
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
                   <div className="text-end">
                     <div className="small-muted">{item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date}</div>
@@ -2522,7 +2980,7 @@
                   </div>
                 </div>
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
               )}
             </div>
@@ -2668,7 +3126,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2697,7 +3155,7 @@
   };
 
   const createSnapshotVersion = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     const status = snapshotWorkspace?.snapshot_status;
 
     if (!status?.can_create_snapshot_version) {
@@ -2719,7 +3177,7 @@
         description: `Quarter snapshot generated from Quarter workspace`,
         metadata: {
           source_type: 'manual',
-          readiness_pct: quarterReadiness?.readiness_pct ?? null
+          readiness_pct: activeQuarterReport?.readiness_pct ?? null
         }
       });
       await refreshQuarterWorkspace();
@@ -2734,7 +3192,7 @@
   };
 
   const closeQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(closeReason || '').trim()) {
       setQuarterActionError('Close reason is required.');
       setQuarterActionMessage('');
@@ -2785,7 +3243,7 @@
   };
 
   const reopenQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(reopenReason || '').trim()) {
       setQuarterActionError('Reopen reason is required.');
       setQuarterActionMessage('');
@@ -2844,9 +3302,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3342,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3354,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3365,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3379,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..669173cd6176ee9ee5c86c5895ea49cb9524ebc4
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,155 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, fallbackIssues = [], resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const issueSource = Array.isArray(report.issue_list) && report.issue_list.length > 0
+    ? report.issue_list
+    : fallbackIssues;
+  const allIssues = sortIssues(Array.isArray(issueSource) ? issueSource : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/bizPA/frontend/src/quarterReadiness.test.js b/bizPA/frontend/src/quarterReadiness.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..0b810191a89d8b82091bf5de1408afcc23c14e65
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.test.js
@@ -0,0 +1,93 @@
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
+
+const reportFixture = {
+  quarter_reference: 'Q1-2026',
+  period_start: '2026-01-01',
+  period_end: '2026-03-31',
+  total_txns_in_period: 18,
+  readiness_pct: 78,
+  can_export: false,
+  issue_list: [
+    {
+      issue_type: 'missing_category',
+      affected_entity_id: 'txn-9101',
+      severity: 'high',
+      weight: 35,
+      explanation: 'Category missing for Fuel Stop.',
+      txn_date: '2026-02-04',
+      merchant: 'Fuel Stop',
+      amount: 64.18,
+      direction: 'out',
+      resolution_target: {
+        label: 'Classify transaction',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'missing_business_personal',
+      affected_entity_id: 'txn-9103',
+      severity: 'high',
+      weight: 30,
+      explanation: 'Business/personal decision missing.',
+      txn_date: '2026-02-15',
+      merchant: 'Mobile Contract',
+      amount: 42.5,
+      direction: 'out',
+      resolution_target: {
+        label: 'Set business or personal',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'unresolved_duplicate',
+      affected_entity_id: 'txn-9104',
+      severity: 'medium',
+      weight: 15,
+      explanation: 'Duplicate not resolved.',
+      txn_date: '2026-03-02',
+      merchant: 'Client Payment',
+      amount: 820,
+      direction: 'in',
+      resolution_target: {
+        label: 'Resolve duplicate',
+        workflow: '/inbox/finish-now',
+      },
+    },
+  ],
+};
+
+describe('quarter readiness helpers', () => {
+  test('keeps blockers ordered and updates headline while blockers remain', () => {
+    const state = buildQuarterUiState({ report: reportFixture, resolvedIssueIds: ['txn-9104'] });
+
+    expect(state.itemsLeft).toBe(2);
+    expect(state.exportEnabled).toBe(false);
+    expect(state.report.readiness_pct).toBeGreaterThan(78);
+    expect(state.headline).toContain('items left');
+    expect(state.activeIssues.map((issue) => issue.affected_entity_id)).toEqual(['txn-9101', 'txn-9103']);
+    expect(state.report.explanation_lines[0]).toContain('Readiness is');
+  });
+
+  test('unlocks export immediately when every blocker is resolved', () => {
+    const state = buildQuarterUiState({
+      report: reportFixture,
+      resolvedIssueIds: ['txn-9101', 'txn-9103', 'txn-9104'],
+    });
+
+    expect(state.itemsLeft).toBe(0);
+    expect(state.exportEnabled).toBe(true);
+    expect(state.report.readiness_pct).toBe(100);
+    expect(state.report.can_export).toBe(true);
+    expect(state.report.issue_summary).toEqual([]);
+  });
+
+  test('supports demo query params that resolve all blockers', () => {
+    const params = new URLSearchParams('resolvedDemoIssues=all');
+    const resolvedIds = deriveResolvedIssueIdsFromSearchParams(params, reportFixture.issue_list);
+
+    expect(resolvedIds).toEqual(['txn-9101', 'txn-9103', 'txn-9104']);
+  });
+});
diff --git a/bizPA/start_bizpa_tax_readiness_ui.ps1 b/bizPA/start_bizpa_tax_readiness_ui.ps1
index 1fc4bef8cd172c31fdc3db52d1acc9d4775c5485..7216e44eac386fe436a9af8e9b417cd2d61e37fe
--- a/bizPA/start_bizpa_tax_readiness_ui.ps1
+++ b/bizPA/start_bizpa_tax_readiness_ui.ps1
@@ -4,6 +4,7 @@
 $frontendPath = Join-Path $repoRoot 'frontend'
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
 
 Write-Host 'Starting bizPA tax readiness UI...' -ForegroundColor Cyan
 
@@ -14,7 +15,10 @@
 Write-Host 'Tax readiness dashboard URL:' -ForegroundColor Green
 Write-Host "  $readinessUrl" -ForegroundColor Yellow
 Write-Host ''
+Write-Host 'Export-ready verification URL:' -ForegroundColor Green
+Write-Host "  $exportReadyUrl" -ForegroundColor Yellow
+Write-Host ''
 Write-Host 'Base frontend URL:' -ForegroundColor Green
 Write-Host "  $frontendUrl" -ForegroundColor Yellow
 Write-Host ''
-Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI can be reviewed without backend setup.' -ForegroundColor Cyan
+Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI and zero-blocker export-ready state can be reviewed without backend setup.' -ForegroundColor Cyan
diff --git a/bizPA/tax_readiness_ui_smoke.ps1 b/bizPA/tax_readiness_ui_smoke.ps1
index 56d67892a51b22572503dec560d17123b370c2b7..dd689ee4cf40ca47dbd6893b2db390f877e774d9
--- a/bizPA/tax_readiness_ui_smoke.ps1
+++ b/bizPA/tax_readiness_ui_smoke.ps1
@@ -28,99 +28,84 @@
 
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
-$screenshotPath = 'C:\Users\edebe\eds\workstream\verification\20260311_201500_bizpa_tax_readiness_ui.png'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
+$verificationRoot = 'C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification'
+$quarterScreenshotPath = Join-Path $verificationRoot 'quarter_readiness_screen.png'
+$finishNowScreenshotPath = Join-Path $verificationRoot 'finish_now_queue_zero_blockers.png'
 $chromeProfilePath = 'C:\Users\edebe\eds\workstream\verification\chrome_capture_profile_tax_readiness'
-
-New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
+$chromePath = 'C:\Program Files\Google\Chrome\Application\chrome.exe'
 
-$frontendJob = Start-Job -ScriptBlock {
-  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
-  $env:PORT = '3002'
-  npm.cmd start
-}
+function Wait-FrontendReady {
+  param (
+    [string]$Url
+  )
 
-try {
-  $frontendResponse = $null
-  $frontendReady = $false
   for ($i = 0; $i -lt 45; $i++) {
     Start-Sleep -Seconds 2
     try {
-      $frontendResponse = Invoke-WebRequest -UseBasicParsing $readinessUrl
-      if ($frontendResponse.StatusCode -eq 200) {
-        $frontendReady = $true
-        break
+      $response = Invoke-WebRequest -UseBasicParsing $Url
+      if ($response.StatusCode -eq 200) {
+        return $response
       }
     } catch {
     }
   }
 
-  if (-not $frontendReady) {
-    throw "Frontend did not become ready on $readinessUrl"
-  }
+  throw "Frontend did not become ready on $Url"
+}
+
+function Capture-ChromeAppScreenshot {
+  param (
+    [string]$Url,
+    [string]$OutputPath
+  )
 
+  $captureProfilePath = Join-Path $chromeProfilePath ([System.IO.Path]::GetFileNameWithoutExtension($OutputPath))
+  New-Item -ItemType Directory -Force -Path $captureProfilePath | Out-Null
+
   $chromeArgs = @(
-    "--user-data-dir=$chromeProfilePath",
-    '--new-window',
+    "--user-data-dir=$captureProfilePath",
     '--window-size=1600,1500',
+    '--headless=new',
+    '--disable-gpu',
+    '--hide-scrollbars',
     '--no-first-run',
     '--disable-session-crashed-bubble',
     '--disable-infobars',
     '--disable-breakpad',
     '--disable-crash-reporter',
     '--noerrdialogs',
-    "--app=$readinessUrl"
+    "--screenshot=$OutputPath",
+    $Url
   )
-  Start-Process 'C:\Program Files\Google\Chrome\Application\chrome.exe' -ArgumentList $chromeArgs | Out-Null
-
-  $chromeWindow = $null
-  for ($i = 0; $i -lt 20; $i++) {
-    Start-Sleep -Seconds 1
-    $chromeWindow = Get-Process chrome -ErrorAction SilentlyContinue |
-      ForEach-Object {
-        try {
-          if ($_.MainWindowHandle -and ([int64]$_.MainWindowHandle -ne 0)) {
-            [pscustomobject]@{
-              Process = $_
-              StartTime = $_.StartTime
-            }
-          }
-        } catch {
-        }
-      } |
-      Sort-Object StartTime -Descending |
-      Select-Object -First 1
-    if ($chromeWindow) {
-      $chromeWindow = $chromeWindow.Process
-      break
-    }
+  $process = Start-Process $chromePath -ArgumentList $chromeArgs -PassThru -Wait
+  if ($process.ExitCode -ne 0) {
+    throw "Chrome headless screenshot failed for $Url with exit code $($process.ExitCode)"
   }
-
-  if (-not $chromeWindow) {
-    throw 'Chrome readiness window did not expose a main window handle for screenshot capture.'
+  if (-not (Test-Path $OutputPath)) {
+    throw "Chrome did not create screenshot output for $Url"
   }
+}
 
-  [WindowCaptureNative]::ShowWindow($chromeWindow.MainWindowHandle, 3) | Out-Null
-  [WindowCaptureNative]::SetForegroundWindow($chromeWindow.MainWindowHandle) | Out-Null
-  Start-Sleep -Seconds 1
+New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
+New-Item -ItemType Directory -Force -Path $verificationRoot | Out-Null
 
-  $rect = New-Object WindowCaptureNative+RECT
-  [WindowCaptureNative]::GetWindowRect($chromeWindow.MainWindowHandle, [ref]$rect) | Out-Null
-  $width = [Math]::Max(1, $rect.Right - $rect.Left)
-  $height = [Math]::Max(1, $rect.Bottom - $rect.Top)
-  $bitmap = New-Object System.Drawing.Bitmap $width, $height
-  $graphics = [System.Drawing.Graphics]::FromImage($bitmap)
-  $graphics.CopyFromScreen($rect.Left, $rect.Top, 0, 0, $bitmap.Size)
-  $bitmap.Save($screenshotPath, [System.Drawing.Imaging.ImageFormat]::Png)
-  $graphics.Dispose()
-  $bitmap.Dispose()
+$frontendJob = Start-Job -ScriptBlock {
+  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
+  $env:PORT = '3002'
+  npm.cmd start
+}
 
-  Get-Process chrome -ErrorAction SilentlyContinue |
-    Where-Object { $_.StartTime -ge $chromeWindow.StartTime.AddSeconds(-2) } |
-    Stop-Process -Force -ErrorAction SilentlyContinue
+try {
+  $frontendResponse = Wait-FrontendReady -Url $readinessUrl
+  Capture-ChromeAppScreenshot -Url $readinessUrl -OutputPath $quarterScreenshotPath
+  Capture-ChromeAppScreenshot -Url $exportReadyUrl -OutputPath $finishNowScreenshotPath
 
   "READINESS_URL=$readinessUrl"
+  "EXPORT_READY_URL=$exportReadyUrl"
   "FRONTEND_STATUS=$($frontendResponse.StatusCode)"
-  "SCREENSHOT=$screenshotPath"
+  "SCREENSHOT_QUARTER=$quarterScreenshotPath"
+  "SCREENSHOT_EXPORT_READY=$finishNowScreenshotPath"
   'FRONTEND_LOG_START'
   Receive-Job $frontendJob -Keep -ErrorAction SilentlyContinue 2>&1
   'FRONTEND_LOG_END'
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "powershell -ExecutionPolicy Bypass -File 'C:\\Users\\edebe\\eds\\bizPA\\tax_readiness_ui_smoke.ps1'" in C:\Users\edebe\eds exited 1 in 26.99s:
Chrome headless screenshot failed for http://127.0.0.1:3002/?readinessDemo=1&tab=quarter with exit code -36863
At C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1:83 char:5
+     throw "Chrome headless screenshot failed for $Url with exit code  ...
+     ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : OperationStopped: (Chrome headless...xit code -36863:String) [], RuntimeException
    + FullyQualifiedErrorId : Chrome headless screenshot failed for http://127.0.0.1:3002/?readinessDemo=1&tab=quarter 
    with exit code -36863
 
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..b0b6a961dcb80123d7634a661b59a1e944c1c7a1
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,26 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    fallbackIssues: inboxItems,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1532,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1547,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1633,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2104,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2316,29 +2562,241 @@
     </div>
   );
 
-  const renderInbox = () => (
-    <div>
-      <h4>Finish Now Queue</h4>
-      <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
-      <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
-            <div>
-              <b>{item.merchant || 'Unknown'}</b>
-              <br />
-              <small>
-                {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
-              </small>
-              <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
-            </div>
+  const renderInbox = () => {
+    return (
+    <div className="inbox-mobile-shell">
+      <section className="inbox-mobile-hero">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2">
+          <div className="inbox-kicker">
+            <Bell size={14} />
+            Mobile-first exception queue
           </div>
-        ))}
-      </div>
-    </div>
-  );
+          <div className="status-pill">{filteredInboxCards.length} visible</div>
+        </div>
+        <h4>Finish now</h4>
+        <p>
+          Clear the blockers that stop the quarterly export: category, business or personal, split treatment, and duplicate resolution.
+        </p>
+        <div className="inbox-mobile-stats">
+          <div className="inbox-mobile-stat">
+            <small>Queue</small>
+            <strong>{inboxCards.length}</strong>
+            <small>{activeQuarterReport.quarter_reference || READINESS_DEMO_REPORT.quarter_reference}</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Critical</small>
+            <strong>{highSeverityCount}</strong>
+            <small>High-severity blockers</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Ready now</small>
+            <strong>{readyToExportCount}</strong>
+            <small>Transactions already export-safe</small>
+          </div>
+        </div>
+        <div className="hero-actions">
+          <button className="hero-action-main" onClick={() => setCurrentTab('quarter')}>
+            <ArrowRight size={16} />
+            Review quarter readiness
+          </button>
+          <button className="hero-action-secondary" onClick={undoLastInboxAction} disabled={inboxBusyId === 'undo-last'}>
+            {inboxBusyId === 'undo-last' ? <RefreshCcw size={16} className="spinner" /> : <ArchiveRestore size={16} />}
+            Undo last action
+          </button>
+        </div>
+      </section>
+
+      {inboxMessage && (
+        <div className={`readiness-panel inbox-message ${inboxMessage.type}`}>
+          {inboxMessage.text}
+        </div>
+      )}
+
+      <section className="readiness-panel">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-3">
+          <div>
+            <h5 style={{ marginBottom: 4 }}>Queue filters</h5>
+            <small className="small-muted">Keep the mobile pass focused on one blocker type at a time.</small>
+          </div>
+          <div className="status-pill">{activeQuarterReport.readiness_pct ?? READINESS_DEMO_REPORT.readiness_pct}% ready</div>
+        </div>
+        <div className="inbox-filter-row">
+          {INBOX_FILTERS.map((filter) => {
+            const count = filter.key === 'all'
+              ? inboxCards.length
+              : inboxCards.filter((card) => card.issue_types.includes(filter.key)).length;
+            return (
+              <button
+                key={filter.key}
+                type="button"
+                className={`inbox-filter-chip ${inboxFilter === filter.key ? 'active' : ''}`}
+                onClick={() => setInboxFilter(filter.key)}
+              >
+                <span>{filter.label}</span>
+                <small>{count}</small>
+              </button>
+            );
+          })}
+        </div>
+      </section>
+
+      {filteredInboxCards.length === 0 ? (
+        <div className="inbox-empty">
+          No blockers match this filter. Reset to All blockers or return to the quarter screen to confirm readiness.
+        </div>
+      ) : (
+        <div className="inbox-card-list">
+          {filteredInboxCards.map((card) => {
+            const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+            const saving = inboxBusyId === card.id;
+            const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+            return (
+              <section key={card.id} className="inbox-action-card">
+                <div className="inbox-card-top">
+                  <div>
+                    <strong>{card.merchant}</strong>
+                    <div className="inbox-card-subline">
+                      {card.txn_date ? String(card.txn_date).slice(0, 10) : 'Date pending'} · {formatIssueAmount(card.amount, card.direction)}
+                    </div>
+                  </div>
+                  <span className={`inbox-issue-chip ${card.severity}`}>
+                    <TriangleAlert size={14} />
+                    {INBOX_REASON_LABELS[card.blocker_reason] || card.blocker_reason.replace(/_/g, ' ')}
+                  </span>
+                </div>
+
+                <div className="inbox-chip-row">
+                  {card.issue_labels.map((label) => (
+                    <span key={`${card.id}-${label}`} className={`inbox-issue-chip ${card.severity}`}>
+                      {label}
+                    </span>
+                  ))}
+                  {card.suggested_category?.category_name && (
+                    <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
+                      <CheckCircle2 size={14} />
+                      Suggested: {card.suggested_category.category_name}
+                    </button>
+                  )}
+                </div>
+
+                <p className="inbox-explainer">{card.explanation || 'Resolve the blocking fields to clear this transaction from the queue.'}</p>
+
+                {(card.supports_category || card.supports_business_personal || card.supports_split) && (
+                  <div className="inbox-action-grid">
+                    <div className="inbox-field">
+                      <label>Category code</label>
+                      <input
+                        value={draft.category_code}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_code', event.target.value)}
+                        placeholder="MATERIALS"
+                      />
+                    </div>
+                    <div className="inbox-field">
+                      <label>Category label</label>
+                      <input
+                        value={draft.category_name}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_name', event.target.value)}
+                        placeholder="Materials"
+                      />
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_business_personal || card.supports_split) && (
+                  <div>
+                    <div className="inbox-field">
+                      <label>Business treatment</label>
+                    </div>
+                    <div className="inbox-toggle-row">
+                      {[
+                        { value: 'BUSINESS', label: 'Business' },
+                        { value: 'PERSONAL', label: 'Personal' },
+                        { value: 'SPLIT', label: 'Split' },
+                      ].map((option) => (
+                        <button
+                          key={option.value}
+                          type="button"
+                          className={`inbox-toggle ${draft.business_personal === option.value ? 'active' : ''}`}
+                          onClick={() => setInboxDraftField(card.id, 'business_personal', option.value)}
+                        >
+                          {option.label}
+                        </button>
+                      ))}
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_split || isSplit) && (
+                  <div className="inbox-field">
+                    <label>Business split %</label>
+                    <input
+                      type="number"
+                      min="0"
+                      max="100"
+                      value={draft.split_business_pct}
+                      onChange={(event) => setInboxDraftField(card.id, 'split_business_pct', event.target.value)}
+                      placeholder="80"
+                    />
+                  </div>
+                )}
+
+                {card.supports_duplicate && (
+                  <>
+                    <div>
+                      <div className="inbox-field">
+                        <label>Duplicate action</label>
+                      </div>
+                      <div className="inbox-duplicate-row">
+                        {[
+                          { value: 'dismiss', label: 'Dismiss duplicate' },
+                          { value: 'merge', label: 'Merge to original' },
+                        ].map((option) => (
+                          <button
+                            key={option.value}
+                            type="button"
+                            className={`inbox-toggle ${draft.duplicate_action === option.value ? 'active' : ''}`}
+                            onClick={() => setInboxDraftField(card.id, 'duplicate_action', option.value)}
+                          >
+                            {option.label}
+                          </button>
+                        ))}
+                      </div>
+                    </div>
+                    {draft.duplicate_action === 'merge' && (
+                      <div className="inbox-field">
+                        <label>Original transaction id</label>
+                        <input
+                          value={draft.duplicate_of_txn_id}
+                          onChange={(event) => setInboxDraftField(card.id, 'duplicate_of_txn_id', event.target.value)}
+                          placeholder="txn-9001"
+                        />
+                      </div>
+                    )}
+                  </>
+                )}
 
+                <div className="inbox-footer-row">
+                  <div className="inbox-inline-status">
+                    Route: {card.resolution_target?.route || `/api/v1/inbox/${card.id}`}
+                  </div>
+                  <div className="inbox-footer-actions">
+                    <button className="secondary-action" onClick={clearInboxMessage} disabled={saving}>
+                      Clear banner
+                    </button>
+                    <button className="primary-action" onClick={() => submitInboxAction(card)} disabled={saving}>
+                      {saving ? <RefreshCcw size={16} className="spinner" /> : <CheckCircle2 size={16} />}
+                      Save and clear blocker
+                    </button>
+                  </div>
+                </div>
+              </section>
+            );
+          })}
+        </div>
+      )}
+    </div>
+    );
+  };
   const renderControlCentre = () => {
     const policyLabels = {
       vat_scheme: 'VAT scheme',
@@ -2510,11 +2968,11 @@
           <section className="readiness-panel">
             <h5 style={{ marginBottom: 12 }}>Queue Backlog</h5>
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
-                <div key={item.id} className="control-row">
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
+                <div key={item.id || item.affected_entity_id} className="control-row">
                   <div>
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
                   <div className="text-end">
                     <div className="small-muted">{item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date}</div>
@@ -2522,7 +2980,7 @@
                   </div>
                 </div>
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
               )}
             </div>
@@ -2668,7 +3126,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2697,7 +3155,7 @@
   };
 
   const createSnapshotVersion = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     const status = snapshotWorkspace?.snapshot_status;
 
     if (!status?.can_create_snapshot_version) {
@@ -2719,7 +3177,7 @@
         description: `Quarter snapshot generated from Quarter workspace`,
         metadata: {
           source_type: 'manual',
-          readiness_pct: quarterReadiness?.readiness_pct ?? null
+          readiness_pct: activeQuarterReport?.readiness_pct ?? null
         }
       });
       await refreshQuarterWorkspace();
@@ -2734,7 +3192,7 @@
   };
 
   const closeQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(closeReason || '').trim()) {
       setQuarterActionError('Close reason is required.');
       setQuarterActionMessage('');
@@ -2785,7 +3243,7 @@
   };
 
   const reopenQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(reopenReason || '').trim()) {
       setQuarterActionError('Reopen reason is required.');
       setQuarterActionMessage('');
@@ -2844,9 +3302,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3342,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3354,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3365,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3379,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..669173cd6176ee9ee5c86c5895ea49cb9524ebc4
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,155 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, fallbackIssues = [], resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const issueSource = Array.isArray(report.issue_list) && report.issue_list.length > 0
+    ? report.issue_list
+    : fallbackIssues;
+  const allIssues = sortIssues(Array.isArray(issueSource) ? issueSource : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/bizPA/frontend/src/quarterReadiness.test.js b/bizPA/frontend/src/quarterReadiness.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..0b810191a89d8b82091bf5de1408afcc23c14e65
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.test.js
@@ -0,0 +1,93 @@
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
+
+const reportFixture = {
+  quarter_reference: 'Q1-2026',
+  period_start: '2026-01-01',
+  period_end: '2026-03-31',
+  total_txns_in_period: 18,
+  readiness_pct: 78,
+  can_export: false,
+  issue_list: [
+    {
+      issue_type: 'missing_category',
+      affected_entity_id: 'txn-9101',
+      severity: 'high',
+      weight: 35,
+      explanation: 'Category missing for Fuel Stop.',
+      txn_date: '2026-02-04',
+      merchant: 'Fuel Stop',
+      amount: 64.18,
+      direction: 'out',
+      resolution_target: {
+        label: 'Classify transaction',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'missing_business_personal',
+      affected_entity_id: 'txn-9103',
+      severity: 'high',
+      weight: 30,
+      explanation: 'Business/personal decision missing.',
+      txn_date: '2026-02-15',
+      merchant: 'Mobile Contract',
+      amount: 42.5,
+      direction: 'out',
+      resolution_target: {
+        label: 'Set business or personal',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'unresolved_duplicate',
+      affected_entity_id: 'txn-9104',
+      severity: 'medium',
+      weight: 15,
+      explanation: 'Duplicate not resolved.',
+      txn_date: '2026-03-02',
+      merchant: 'Client Payment',
+      amount: 820,
+      direction: 'in',
+      resolution_target: {
+        label: 'Resolve duplicate',
+        workflow: '/inbox/finish-now',
+      },
+    },
+  ],
+};
+
+describe('quarter readiness helpers', () => {
+  test('keeps blockers ordered and updates headline while blockers remain', () => {
+    const state = buildQuarterUiState({ report: reportFixture, resolvedIssueIds: ['txn-9104'] });
+
+    expect(state.itemsLeft).toBe(2);
+    expect(state.exportEnabled).toBe(false);
+    expect(state.report.readiness_pct).toBeGreaterThan(78);
+    expect(state.headline).toContain('items left');
+    expect(state.activeIssues.map((issue) => issue.affected_entity_id)).toEqual(['txn-9101', 'txn-9103']);
+    expect(state.report.explanation_lines[0]).toContain('Readiness is');
+  });
+
+  test('unlocks export immediately when every blocker is resolved', () => {
+    const state = buildQuarterUiState({
+      report: reportFixture,
+      resolvedIssueIds: ['txn-9101', 'txn-9103', 'txn-9104'],
+    });
+
+    expect(state.itemsLeft).toBe(0);
+    expect(state.exportEnabled).toBe(true);
+    expect(state.report.readiness_pct).toBe(100);
+    expect(state.report.can_export).toBe(true);
+    expect(state.report.issue_summary).toEqual([]);
+  });
+
+  test('supports demo query params that resolve all blockers', () => {
+    const params = new URLSearchParams('resolvedDemoIssues=all');
+    const resolvedIds = deriveResolvedIssueIdsFromSearchParams(params, reportFixture.issue_list);
+
+    expect(resolvedIds).toEqual(['txn-9101', 'txn-9103', 'txn-9104']);
+  });
+});
diff --git a/bizPA/start_bizpa_tax_readiness_ui.ps1 b/bizPA/start_bizpa_tax_readiness_ui.ps1
index 1fc4bef8cd172c31fdc3db52d1acc9d4775c5485..7216e44eac386fe436a9af8e9b417cd2d61e37fe
--- a/bizPA/start_bizpa_tax_readiness_ui.ps1
+++ b/bizPA/start_bizpa_tax_readiness_ui.ps1
@@ -4,6 +4,7 @@
 $frontendPath = Join-Path $repoRoot 'frontend'
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
 
 Write-Host 'Starting bizPA tax readiness UI...' -ForegroundColor Cyan
 
@@ -14,7 +15,10 @@
 Write-Host 'Tax readiness dashboard URL:' -ForegroundColor Green
 Write-Host "  $readinessUrl" -ForegroundColor Yellow
 Write-Host ''
+Write-Host 'Export-ready verification URL:' -ForegroundColor Green
+Write-Host "  $exportReadyUrl" -ForegroundColor Yellow
+Write-Host ''
 Write-Host 'Base frontend URL:' -ForegroundColor Green
 Write-Host "  $frontendUrl" -ForegroundColor Yellow
 Write-Host ''
-Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI can be reviewed without backend setup.' -ForegroundColor Cyan
+Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI and zero-blocker export-ready state can be reviewed without backend setup.' -ForegroundColor Cyan
diff --git a/bizPA/tax_readiness_ui_smoke.ps1 b/bizPA/tax_readiness_ui_smoke.ps1
index 56d67892a51b22572503dec560d17123b370c2b7..dd689ee4cf40ca47dbd6893b2db390f877e774d9
--- a/bizPA/tax_readiness_ui_smoke.ps1
+++ b/bizPA/tax_readiness_ui_smoke.ps1
@@ -28,99 +28,84 @@
 
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
-$screenshotPath = 'C:\Users\edebe\eds\workstream\verification\20260311_201500_bizpa_tax_readiness_ui.png'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
+$verificationRoot = 'C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification'
+$quarterScreenshotPath = Join-Path $verificationRoot 'quarter_readiness_screen.png'
+$finishNowScreenshotPath = Join-Path $verificationRoot 'finish_now_queue_zero_blockers.png'
 $chromeProfilePath = 'C:\Users\edebe\eds\workstream\verification\chrome_capture_profile_tax_readiness'
-
-New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
+$chromePath = 'C:\Program Files\Google\Chrome\Application\chrome.exe'
 
-$frontendJob = Start-Job -ScriptBlock {
-  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
-  $env:PORT = '3002'
-  npm.cmd start
-}
+function Wait-FrontendReady {
+  param (
+    [string]$Url
+  )
 
-try {
-  $frontendResponse = $null
-  $frontendReady = $false
   for ($i = 0; $i -lt 45; $i++) {
     Start-Sleep -Seconds 2
     try {
-      $frontendResponse = Invoke-WebRequest -UseBasicParsing $readinessUrl
-      if ($frontendResponse.StatusCode -eq 200) {
-        $frontendReady = $true
-        break
+      $response = Invoke-WebRequest -UseBasicParsing $Url
+      if ($response.StatusCode -eq 200) {
+        return $response
       }
     } catch {
     }
   }
 
-  if (-not $frontendReady) {
-    throw "Frontend did not become ready on $readinessUrl"
-  }
+  throw "Frontend did not become ready on $Url"
+}
+
+function Capture-ChromeAppScreenshot {
+  param (
+    [string]$Url,
+    [string]$OutputPath
+  )
 
+  $captureProfilePath = Join-Path $chromeProfilePath ([System.IO.Path]::GetFileNameWithoutExtension($OutputPath))
+  New-Item -ItemType Directory -Force -Path $captureProfilePath | Out-Null
+
   $chromeArgs = @(
-    "--user-data-dir=$chromeProfilePath",
-    '--new-window',
+    "--user-data-dir=$captureProfilePath",
     '--window-size=1600,1500',
+    '--headless=new',
+    '--disable-gpu',
+    '--hide-scrollbars',
     '--no-first-run',
     '--disable-session-crashed-bubble',
     '--disable-infobars',
     '--disable-breakpad',
     '--disable-crash-reporter',
     '--noerrdialogs',
-    "--app=$readinessUrl"
+    "--screenshot=$OutputPath",
+    $Url
   )
-  Start-Process 'C:\Program Files\Google\Chrome\Application\chrome.exe' -ArgumentList $chromeArgs | Out-Null
-
-  $chromeWindow = $null
-  for ($i = 0; $i -lt 20; $i++) {
-    Start-Sleep -Seconds 1
-    $chromeWindow = Get-Process chrome -ErrorAction SilentlyContinue |
-      ForEach-Object {
-        try {
-          if ($_.MainWindowHandle -and ([int64]$_.MainWindowHandle -ne 0)) {
-            [pscustomobject]@{
-              Process = $_
-              StartTime = $_.StartTime
-            }
-          }
-        } catch {
-        }
-      } |
-      Sort-Object StartTime -Descending |
-      Select-Object -First 1
-    if ($chromeWindow) {
-      $chromeWindow = $chromeWindow.Process
-      break
-    }
+  $process = Start-Process $chromePath -ArgumentList $chromeArgs -PassThru -Wait
+  if ($process.ExitCode -ne 0) {
+    throw "Chrome headless screenshot failed for $Url with exit code $($process.ExitCode)"
   }
-
-  if (-not $chromeWindow) {
-    throw 'Chrome readiness window did not expose a main window handle for screenshot capture.'
+  if (-not (Test-Path $OutputPath)) {
+    throw "Chrome did not create screenshot output for $Url"
   }
+}
 
-  [WindowCaptureNative]::ShowWindow($chromeWindow.MainWindowHandle, 3) | Out-Null
-  [WindowCaptureNative]::SetForegroundWindow($chromeWindow.MainWindowHandle) | Out-Null
-  Start-Sleep -Seconds 1
+New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
+New-Item -ItemType Directory -Force -Path $verificationRoot | Out-Null
 
-  $rect = New-Object WindowCaptureNative+RECT
-  [WindowCaptureNative]::GetWindowRect($chromeWindow.MainWindowHandle, [ref]$rect) | Out-Null
-  $width = [Math]::Max(1, $rect.Right - $rect.Left)
-  $height = [Math]::Max(1, $rect.Bottom - $rect.Top)
-  $bitmap = New-Object System.Drawing.Bitmap $width, $height
-  $graphics = [System.Drawing.Graphics]::FromImage($bitmap)
-  $graphics.CopyFromScreen($rect.Left, $rect.Top, 0, 0, $bitmap.Size)
-  $bitmap.Save($screenshotPath, [System.Drawing.Imaging.ImageFormat]::Png)
-  $graphics.Dispose()
-  $bitmap.Dispose()
+$frontendJob = Start-Job -ScriptBlock {
+  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
+  $env:PORT = '3002'
+  npm.cmd start
+}
 
-  Get-Process chrome -ErrorAction SilentlyContinue |
-    Where-Object { $_.StartTime -ge $chromeWindow.StartTime.AddSeconds(-2) } |
-    Stop-Process -Force -ErrorAction SilentlyContinue
+try {
+  $frontendResponse = Wait-FrontendReady -Url $readinessUrl
+  Capture-ChromeAppScreenshot -Url $readinessUrl -OutputPath $quarterScreenshotPath
+  Capture-ChromeAppScreenshot -Url $exportReadyUrl -OutputPath $finishNowScreenshotPath
 
   "READINESS_URL=$readinessUrl"
+  "EXPORT_READY_URL=$exportReadyUrl"
   "FRONTEND_STATUS=$($frontendResponse.StatusCode)"
-  "SCREENSHOT=$screenshotPath"
+  "SCREENSHOT_QUARTER=$quarterScreenshotPath"
+  "SCREENSHOT_EXPORT_READY=$finishNowScreenshotPath"
   'FRONTEND_LOG_START'
   Receive-Job $frontendJob -Keep -ErrorAction SilentlyContinue 2>&1
   'FRONTEND_LOG_END'
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "& 'C:\\Program Files\\Google\\Chrome\\Application\\chrome.exe' --version" in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "if (Test-Path 'C:\\Program Files (x86)\\Microsoft\\Edge\\Application\\msedge.exe') { & 'C:\\Program Files (x86)\\Microsoft\\Edge\\Application\\msedge.exe' --version } elseif (Test-Path 'C:\\Program Files\\Microsoft\\Edge\\Application\\msedge.exe') { & 'C:\\Program Files\\Microsoft\\Edge\\Application\\msedge.exe' --version }" in C:\Users\edebe\eds succeeded in 968ms:
[0318/181849.991:ERROR:third_party\crashpad\crashpad\util\win\registration_protocol_win.cc:108] CreateFile: Access is denied. (0x5)
 succeeded in 1.10s:

file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..b0b6a961dcb80123d7634a661b59a1e944c1c7a1
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,26 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    fallbackIssues: inboxItems,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1532,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1547,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1633,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2104,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2316,29 +2562,241 @@
     </div>
   );
 
-  const renderInbox = () => (
-    <div>
-      <h4>Finish Now Queue</h4>
-      <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
-      <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
-            <div>
-              <b>{item.merchant || 'Unknown'}</b>
-              <br />
-              <small>
-                {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
-              </small>
-              <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
-            </div>
+  const renderInbox = () => {
+    return (
+    <div className="inbox-mobile-shell">
+      <section className="inbox-mobile-hero">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2">
+          <div className="inbox-kicker">
+            <Bell size={14} />
+            Mobile-first exception queue
           </div>
-        ))}
-      </div>
-    </div>
-  );
+          <div className="status-pill">{filteredInboxCards.length} visible</div>
+        </div>
+        <h4>Finish now</h4>
+        <p>
+          Clear the blockers that stop the quarterly export: category, business or personal, split treatment, and duplicate resolution.
+        </p>
+        <div className="inbox-mobile-stats">
+          <div className="inbox-mobile-stat">
+            <small>Queue</small>
+            <strong>{inboxCards.length}</strong>
+            <small>{activeQuarterReport.quarter_reference || READINESS_DEMO_REPORT.quarter_reference}</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Critical</small>
+            <strong>{highSeverityCount}</strong>
+            <small>High-severity blockers</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Ready now</small>
+            <strong>{readyToExportCount}</strong>
+            <small>Transactions already export-safe</small>
+          </div>
+        </div>
+        <div className="hero-actions">
+          <button className="hero-action-main" onClick={() => setCurrentTab('quarter')}>
+            <ArrowRight size={16} />
+            Review quarter readiness
+          </button>
+          <button className="hero-action-secondary" onClick={undoLastInboxAction} disabled={inboxBusyId === 'undo-last'}>
+            {inboxBusyId === 'undo-last' ? <RefreshCcw size={16} className="spinner" /> : <ArchiveRestore size={16} />}
+            Undo last action
+          </button>
+        </div>
+      </section>
+
+      {inboxMessage && (
+        <div className={`readiness-panel inbox-message ${inboxMessage.type}`}>
+          {inboxMessage.text}
+        </div>
+      )}
+
+      <section className="readiness-panel">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-3">
+          <div>
+            <h5 style={{ marginBottom: 4 }}>Queue filters</h5>
+            <small className="small-muted">Keep the mobile pass focused on one blocker type at a time.</small>
+          </div>
+          <div className="status-pill">{activeQuarterReport.readiness_pct ?? READINESS_DEMO_REPORT.readiness_pct}% ready</div>
+        </div>
+        <div className="inbox-filter-row">
+          {INBOX_FILTERS.map((filter) => {
+            const count = filter.key === 'all'
+              ? inboxCards.length
+              : inboxCards.filter((card) => card.issue_types.includes(filter.key)).length;
+            return (
+              <button
+                key={filter.key}
+                type="button"
+                className={`inbox-filter-chip ${inboxFilter === filter.key ? 'active' : ''}`}
+                onClick={() => setInboxFilter(filter.key)}
+              >
+                <span>{filter.label}</span>
+                <small>{count}</small>
+              </button>
+            );
+          })}
+        </div>
+      </section>
+
+      {filteredInboxCards.length === 0 ? (
+        <div className="inbox-empty">
+          No blockers match this filter. Reset to All blockers or return to the quarter screen to confirm readiness.
+        </div>
+      ) : (
+        <div className="inbox-card-list">
+          {filteredInboxCards.map((card) => {
+            const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+            const saving = inboxBusyId === card.id;
+            const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+            return (
+              <section key={card.id} className="inbox-action-card">
+                <div className="inbox-card-top">
+                  <div>
+                    <strong>{card.merchant}</strong>
+                    <div className="inbox-card-subline">
+                      {card.txn_date ? String(card.txn_date).slice(0, 10) : 'Date pending'} · {formatIssueAmount(card.amount, card.direction)}
+                    </div>
+                  </div>
+                  <span className={`inbox-issue-chip ${card.severity}`}>
+                    <TriangleAlert size={14} />
+                    {INBOX_REASON_LABELS[card.blocker_reason] || card.blocker_reason.replace(/_/g, ' ')}
+                  </span>
+                </div>
+
+                <div className="inbox-chip-row">
+                  {card.issue_labels.map((label) => (
+                    <span key={`${card.id}-${label}`} className={`inbox-issue-chip ${card.severity}`}>
+                      {label}
+                    </span>
+                  ))}
+                  {card.suggested_category?.category_name && (
+                    <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
+                      <CheckCircle2 size={14} />
+                      Suggested: {card.suggested_category.category_name}
+                    </button>
+                  )}
+                </div>
+
+                <p className="inbox-explainer">{card.explanation || 'Resolve the blocking fields to clear this transaction from the queue.'}</p>
+
+                {(card.supports_category || card.supports_business_personal || card.supports_split) && (
+                  <div className="inbox-action-grid">
+                    <div className="inbox-field">
+                      <label>Category code</label>
+                      <input
+                        value={draft.category_code}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_code', event.target.value)}
+                        placeholder="MATERIALS"
+                      />
+                    </div>
+                    <div className="inbox-field">
+                      <label>Category label</label>
+                      <input
+                        value={draft.category_name}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_name', event.target.value)}
+                        placeholder="Materials"
+                      />
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_business_personal || card.supports_split) && (
+                  <div>
+                    <div className="inbox-field">
+                      <label>Business treatment</label>
+                    </div>
+                    <div className="inbox-toggle-row">
+                      {[
+                        { value: 'BUSINESS', label: 'Business' },
+                        { value: 'PERSONAL', label: 'Personal' },
+                        { value: 'SPLIT', label: 'Split' },
+                      ].map((option) => (
+                        <button
+                          key={option.value}
+                          type="button"
+                          className={`inbox-toggle ${draft.business_personal === option.value ? 'active' : ''}`}
+                          onClick={() => setInboxDraftField(card.id, 'business_personal', option.value)}
+                        >
+                          {option.label}
+                        </button>
+                      ))}
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_split || isSplit) && (
+                  <div className="inbox-field">
+                    <label>Business split %</label>
+                    <input
+                      type="number"
+                      min="0"
+                      max="100"
+                      value={draft.split_business_pct}
+                      onChange={(event) => setInboxDraftField(card.id, 'split_business_pct', event.target.value)}
+                      placeholder="80"
+                    />
+                  </div>
+                )}
+
+                {card.supports_duplicate && (
+                  <>
+                    <div>
+                      <div className="inbox-field">
+                        <label>Duplicate action</label>
+                      </div>
+                      <div className="inbox-duplicate-row">
+                        {[
+                          { value: 'dismiss', label: 'Dismiss duplicate' },
+                          { value: 'merge', label: 'Merge to original' },
+                        ].map((option) => (
+                          <button
+                            key={option.value}
+                            type="button"
+                            className={`inbox-toggle ${draft.duplicate_action === option.value ? 'active' : ''}`}
+                            onClick={() => setInboxDraftField(card.id, 'duplicate_action', option.value)}
+                          >
+                            {option.label}
+                          </button>
+                        ))}
+                      </div>
+                    </div>
+                    {draft.duplicate_action === 'merge' && (
+                      <div className="inbox-field">
+                        <label>Original transaction id</label>
+                        <input
+                          value={draft.duplicate_of_txn_id}
+                          onChange={(event) => setInboxDraftField(card.id, 'duplicate_of_txn_id', event.target.value)}
+                          placeholder="txn-9001"
+                        />
+                      </div>
+                    )}
+                  </>
+                )}
 
+                <div className="inbox-footer-row">
+                  <div className="inbox-inline-status">
+                    Route: {card.resolution_target?.route || `/api/v1/inbox/${card.id}`}
+                  </div>
+                  <div className="inbox-footer-actions">
+                    <button className="secondary-action" onClick={clearInboxMessage} disabled={saving}>
+                      Clear banner
+                    </button>
+                    <button className="primary-action" onClick={() => submitInboxAction(card)} disabled={saving}>
+                      {saving ? <RefreshCcw size={16} className="spinner" /> : <CheckCircle2 size={16} />}
+                      Save and clear blocker
+                    </button>
+                  </div>
+                </div>
+              </section>
+            );
+          })}
+        </div>
+      )}
+    </div>
+    );
+  };
   const renderControlCentre = () => {
     const policyLabels = {
       vat_scheme: 'VAT scheme',
@@ -2510,11 +2968,11 @@
           <section className="readiness-panel">
             <h5 style={{ marginBottom: 12 }}>Queue Backlog</h5>
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
-                <div key={item.id} className="control-row">
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
+                <div key={item.id || item.affected_entity_id} className="control-row">
                   <div>
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
                   <div className="text-end">
                     <div className="small-muted">{item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date}</div>
@@ -2522,7 +2980,7 @@
                   </div>
                 </div>
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
               )}
             </div>
@@ -2668,7 +3126,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2697,7 +3155,7 @@
   };
 
   const createSnapshotVersion = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     const status = snapshotWorkspace?.snapshot_status;
 
     if (!status?.can_create_snapshot_version) {
@@ -2719,7 +3177,7 @@
         description: `Quarter snapshot generated from Quarter workspace`,
         metadata: {
           source_type: 'manual',
-          readiness_pct: quarterReadiness?.readiness_pct ?? null
+          readiness_pct: activeQuarterReport?.readiness_pct ?? null
         }
       });
       await refreshQuarterWorkspace();
@@ -2734,7 +3192,7 @@
   };
 
   const closeQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(closeReason || '').trim()) {
       setQuarterActionError('Close reason is required.');
       setQuarterActionMessage('');
@@ -2785,7 +3243,7 @@
   };
 
   const reopenQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(reopenReason || '').trim()) {
       setQuarterActionError('Reopen reason is required.');
       setQuarterActionMessage('');
@@ -2844,9 +3302,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3342,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3354,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3365,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3379,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..669173cd6176ee9ee5c86c5895ea49cb9524ebc4
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,155 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, fallbackIssues = [], resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const issueSource = Array.isArray(report.issue_list) && report.issue_list.length > 0
+    ? report.issue_list
+    : fallbackIssues;
+  const allIssues = sortIssues(Array.isArray(issueSource) ? issueSource : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/bizPA/frontend/src/quarterReadiness.test.js b/bizPA/frontend/src/quarterReadiness.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..0b810191a89d8b82091bf5de1408afcc23c14e65
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.test.js
@@ -0,0 +1,93 @@
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
+
+const reportFixture = {
+  quarter_reference: 'Q1-2026',
+  period_start: '2026-01-01',
+  period_end: '2026-03-31',
+  total_txns_in_period: 18,
+  readiness_pct: 78,
+  can_export: false,
+  issue_list: [
+    {
+      issue_type: 'missing_category',
+      affected_entity_id: 'txn-9101',
+      severity: 'high',
+      weight: 35,
+      explanation: 'Category missing for Fuel Stop.',
+      txn_date: '2026-02-04',
+      merchant: 'Fuel Stop',
+      amount: 64.18,
+      direction: 'out',
+      resolution_target: {
+        label: 'Classify transaction',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'missing_business_personal',
+      affected_entity_id: 'txn-9103',
+      severity: 'high',
+      weight: 30,
+      explanation: 'Business/personal decision missing.',
+      txn_date: '2026-02-15',
+      merchant: 'Mobile Contract',
+      amount: 42.5,
+      direction: 'out',
+      resolution_target: {
+        label: 'Set business or personal',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'unresolved_duplicate',
+      affected_entity_id: 'txn-9104',
+      severity: 'medium',
+      weight: 15,
+      explanation: 'Duplicate not resolved.',
+      txn_date: '2026-03-02',
+      merchant: 'Client Payment',
+      amount: 820,
+      direction: 'in',
+      resolution_target: {
+        label: 'Resolve duplicate',
+        workflow: '/inbox/finish-now',
+      },
+    },
+  ],
+};
+
+describe('quarter readiness helpers', () => {
+  test('keeps blockers ordered and updates headline while blockers remain', () => {
+    const state = buildQuarterUiState({ report: reportFixture, resolvedIssueIds: ['txn-9104'] });
+
+    expect(state.itemsLeft).toBe(2);
+    expect(state.exportEnabled).toBe(false);
+    expect(state.report.readiness_pct).toBeGreaterThan(78);
+    expect(state.headline).toContain('items left');
+    expect(state.activeIssues.map((issue) => issue.affected_entity_id)).toEqual(['txn-9101', 'txn-9103']);
+    expect(state.report.explanation_lines[0]).toContain('Readiness is');
+  });
+
+  test('unlocks export immediately when every blocker is resolved', () => {
+    const state = buildQuarterUiState({
+      report: reportFixture,
+      resolvedIssueIds: ['txn-9101', 'txn-9103', 'txn-9104'],
+    });
+
+    expect(state.itemsLeft).toBe(0);
+    expect(state.exportEnabled).toBe(true);
+    expect(state.report.readiness_pct).toBe(100);
+    expect(state.report.can_export).toBe(true);
+    expect(state.report.issue_summary).toEqual([]);
+  });
+
+  test('supports demo query params that resolve all blockers', () => {
+    const params = new URLSearchParams('resolvedDemoIssues=all');
+    const resolvedIds = deriveResolvedIssueIdsFromSearchParams(params, reportFixture.issue_list);
+
+    expect(resolvedIds).toEqual(['txn-9101', 'txn-9103', 'txn-9104']);
+  });
+});
diff --git a/bizPA/start_bizpa_tax_readiness_ui.ps1 b/bizPA/start_bizpa_tax_readiness_ui.ps1
index 1fc4bef8cd172c31fdc3db52d1acc9d4775c5485..7216e44eac386fe436a9af8e9b417cd2d61e37fe
--- a/bizPA/start_bizpa_tax_readiness_ui.ps1
+++ b/bizPA/start_bizpa_tax_readiness_ui.ps1
@@ -4,6 +4,7 @@
 $frontendPath = Join-Path $repoRoot 'frontend'
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
 
 Write-Host 'Starting bizPA tax readiness UI...' -ForegroundColor Cyan
 
@@ -14,7 +15,10 @@
 Write-Host 'Tax readiness dashboard URL:' -ForegroundColor Green
 Write-Host "  $readinessUrl" -ForegroundColor Yellow
 Write-Host ''
+Write-Host 'Export-ready verification URL:' -ForegroundColor Green
+Write-Host "  $exportReadyUrl" -ForegroundColor Yellow
+Write-Host ''
 Write-Host 'Base frontend URL:' -ForegroundColor Green
 Write-Host "  $frontendUrl" -ForegroundColor Yellow
 Write-Host ''
-Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI can be reviewed without backend setup.' -ForegroundColor Cyan
+Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI and zero-blocker export-ready state can be reviewed without backend setup.' -ForegroundColor Cyan
diff --git a/bizPA/tax_readiness_ui_smoke.ps1 b/bizPA/tax_readiness_ui_smoke.ps1
index 56d67892a51b22572503dec560d17123b370c2b7..dd689ee4cf40ca47dbd6893b2db390f877e774d9
--- a/bizPA/tax_readiness_ui_smoke.ps1
+++ b/bizPA/tax_readiness_ui_smoke.ps1
@@ -28,99 +28,84 @@
 
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
-$screenshotPath = 'C:\Users\edebe\eds\workstream\verification\20260311_201500_bizpa_tax_readiness_ui.png'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
+$verificationRoot = 'C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification'
+$quarterScreenshotPath = Join-Path $verificationRoot 'quarter_readiness_screen.png'
+$finishNowScreenshotPath = Join-Path $verificationRoot 'finish_now_queue_zero_blockers.png'
 $chromeProfilePath = 'C:\Users\edebe\eds\workstream\verification\chrome_capture_profile_tax_readiness'
-
-New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
+$chromePath = 'C:\Program Files\Google\Chrome\Application\chrome.exe'
 
-$frontendJob = Start-Job -ScriptBlock {
-  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
-  $env:PORT = '3002'
-  npm.cmd start
-}
+function Wait-FrontendReady {
+  param (
+    [string]$Url
+  )
 
-try {
-  $frontendResponse = $null
-  $frontendReady = $false
   for ($i = 0; $i -lt 45; $i++) {
     Start-Sleep -Seconds 2
     try {
-      $frontendResponse = Invoke-WebRequest -UseBasicParsing $readinessUrl
-      if ($frontendResponse.StatusCode -eq 200) {
-        $frontendReady = $true
-        break
+      $response = Invoke-WebRequest -UseBasicParsing $Url
+      if ($response.StatusCode -eq 200) {
+        return $response
       }
     } catch {
     }
   }
 
-  if (-not $frontendReady) {
-    throw "Frontend did not become ready on $readinessUrl"
-  }
+  throw "Frontend did not become ready on $Url"
+}
+
+function Capture-ChromeAppScreenshot {
+  param (
+    [string]$Url,
+    [string]$OutputPath
+  )
 
+  $captureProfilePath = Join-Path $chromeProfilePath ([System.IO.Path]::GetFileNameWithoutExtension($OutputPath))
+  New-Item -ItemType Directory -Force -Path $captureProfilePath | Out-Null
+
   $chromeArgs = @(
-    "--user-data-dir=$chromeProfilePath",
-    '--new-window',
+    "--user-data-dir=$captureProfilePath",
     '--window-size=1600,1500',
+    '--headless=new',
+    '--disable-gpu',
+    '--hide-scrollbars',
     '--no-first-run',
     '--disable-session-crashed-bubble',
     '--disable-infobars',
     '--disable-breakpad',
     '--disable-crash-reporter',
     '--noerrdialogs',
-    "--app=$readinessUrl"
+    "--screenshot=$OutputPath",
+    $Url
   )
-  Start-Process 'C:\Program Files\Google\Chrome\Application\chrome.exe' -ArgumentList $chromeArgs | Out-Null
-
-  $chromeWindow = $null
-  for ($i = 0; $i -lt 20; $i++) {
-    Start-Sleep -Seconds 1
-    $chromeWindow = Get-Process chrome -ErrorAction SilentlyContinue |
-      ForEach-Object {
-        try {
-          if ($_.MainWindowHandle -and ([int64]$_.MainWindowHandle -ne 0)) {
-            [pscustomobject]@{
-              Process = $_
-              StartTime = $_.StartTime
-            }
-          }
-        } catch {
-        }
-      } |
-      Sort-Object StartTime -Descending |
-      Select-Object -First 1
-    if ($chromeWindow) {
-      $chromeWindow = $chromeWindow.Process
-      break
-    }
+  $process = Start-Process $chromePath -ArgumentList $chromeArgs -PassThru -Wait
+  if ($process.ExitCode -ne 0) {
+    throw "Chrome headless screenshot failed for $Url with exit code $($process.ExitCode)"
   }
-
-  if (-not $chromeWindow) {
-    throw 'Chrome readiness window did not expose a main window handle for screenshot capture.'
+  if (-not (Test-Path $OutputPath)) {
+    throw "Chrome did not create screenshot output for $Url"
   }
+}
 
-  [WindowCaptureNative]::ShowWindow($chromeWindow.MainWindowHandle, 3) | Out-Null
-  [WindowCaptureNative]::SetForegroundWindow($chromeWindow.MainWindowHandle) | Out-Null
-  Start-Sleep -Seconds 1
+New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
+New-Item -ItemType Directory -Force -Path $verificationRoot | Out-Null
 
-  $rect = New-Object WindowCaptureNative+RECT
-  [WindowCaptureNative]::GetWindowRect($chromeWindow.MainWindowHandle, [ref]$rect) | Out-Null
-  $width = [Math]::Max(1, $rect.Right - $rect.Left)
-  $height = [Math]::Max(1, $rect.Bottom - $rect.Top)
-  $bitmap = New-Object System.Drawing.Bitmap $width, $height
-  $graphics = [System.Drawing.Graphics]::FromImage($bitmap)
-  $graphics.CopyFromScreen($rect.Left, $rect.Top, 0, 0, $bitmap.Size)
-  $bitmap.Save($screenshotPath, [System.Drawing.Imaging.ImageFormat]::Png)
-  $graphics.Dispose()
-  $bitmap.Dispose()
+$frontendJob = Start-Job -ScriptBlock {
+  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
+  $env:PORT = '3002'
+  npm.cmd start
+}
 
-  Get-Process chrome -ErrorAction SilentlyContinue |
-    Where-Object { $_.StartTime -ge $chromeWindow.StartTime.AddSeconds(-2) } |
-    Stop-Process -Force -ErrorAction SilentlyContinue
+try {
+  $frontendResponse = Wait-FrontendReady -Url $readinessUrl
+  Capture-ChromeAppScreenshot -Url $readinessUrl -OutputPath $quarterScreenshotPath
+  Capture-ChromeAppScreenshot -Url $exportReadyUrl -OutputPath $finishNowScreenshotPath
 
   "READINESS_URL=$readinessUrl"
+  "EXPORT_READY_URL=$exportReadyUrl"
   "FRONTEND_STATUS=$($frontendResponse.StatusCode)"
-  "SCREENSHOT=$screenshotPath"
+  "SCREENSHOT_QUARTER=$quarterScreenshotPath"
+  "SCREENSHOT_EXPORT_READY=$finishNowScreenshotPath"
   'FRONTEND_LOG_START'
   Receive-Job $frontendJob -Keep -ErrorAction SilentlyContinue 2>&1
   'FRONTEND_LOG_END'
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

codex
Chrome is blocked by the local sandboxing/crash reporter setup here, so I’m replacing the screenshot capture with an embedded WinForms browser render. That keeps the smoke path fully local and avoids the external browser dependency.
file update
M C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1
@@ -34,5 +34,2 @@
 $finishNowScreenshotPath = Join-Path $verificationRoot 'finish_now_queue_zero_blockers.png'
-$chromeProfilePath = 'C:\Users\edebe\eds\workstream\verification\chrome_capture_profile_tax_readiness'
-$chromePath = 'C:\Program Files\Google\Chrome\Application\chrome.exe'
-
 function Wait-FrontendReady {
@@ -56,3 +53,3 @@
 
-function Capture-ChromeAppScreenshot {
+function Capture-AppScreenshot {
   param (
@@ -62,30 +59,41 @@
 
-  $captureProfilePath = Join-Path $chromeProfilePath ([System.IO.Path]::GetFileNameWithoutExtension($OutputPath))
-  New-Item -ItemType Directory -Force -Path $captureProfilePath | Out-Null
+  $form = New-Object System.Windows.Forms.Form
+  $form.Width = 1600
+  $form.Height = 1500
+  $form.StartPosition = 'Manual'
+  $form.Location = New-Object System.Drawing.Point(-32000, -32000)
 
-  $chromeArgs = @(
-    "--user-data-dir=$captureProfilePath",
-    '--window-size=1600,1500',
-    '--headless=new',
-    '--disable-gpu',
-    '--hide-scrollbars',
-    '--no-first-run',
-    '--disable-session-crashed-bubble',
-    '--disable-infobars',
-    '--disable-breakpad',
-    '--disable-crash-reporter',
-    '--noerrdialogs',
-    "--screenshot=$OutputPath",
-    $Url
-  )
-  $process = Start-Process $chromePath -ArgumentList $chromeArgs -PassThru -Wait
-  if ($process.ExitCode -ne 0) {
-    throw "Chrome headless screenshot failed for $Url with exit code $($process.ExitCode)"
+  $browser = New-Object System.Windows.Forms.WebBrowser
+  $browser.ScriptErrorsSuppressed = $true
+  $browser.ScrollBarsEnabled = $false
+  $browser.Dock = [System.Windows.Forms.DockStyle]::Fill
+  $form.Controls.Add($browser)
+  $form.Show()
+  $browser.Navigate($Url)
+
+  $ready = $false
+  for ($i = 0; $i -lt 90; $i++) {
+    [System.Windows.Forms.Application]::DoEvents()
+    Start-Sleep -Milliseconds 250
+    if ($browser.ReadyState -eq [System.Windows.Forms.WebBrowserReadyState]::Complete) {
+      $ready = $true
+      break
+    }
   }
-  if (-not (Test-Path $OutputPath)) {
-    throw "Chrome did not create screenshot output for $Url"
+
+  if (-not $ready) {
+    $form.Close()
+    throw "Embedded browser did not finish loading $Url"
   }
+
+  Start-Sleep -Seconds 3
+  [System.Windows.Forms.Application]::DoEvents()
+
+  $bitmap = New-Object System.Drawing.Bitmap $browser.Width, $browser.Height
+  $browser.DrawToBitmap($bitmap, (New-Object System.Drawing.Rectangle 0, 0, $browser.Width, $browser.Height))
+  $bitmap.Save($OutputPath, [System.Drawing.Imaging.ImageFormat]::Png)
+  $bitmap.Dispose()
+  $form.Close()
 }
 
-New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
 New-Item -ItemType Directory -Force -Path $verificationRoot | Out-Null
@@ -100,4 +108,4 @@
   $frontendResponse = Wait-FrontendReady -Url $readinessUrl
-  Capture-ChromeAppScreenshot -Url $readinessUrl -OutputPath $quarterScreenshotPath
-  Capture-ChromeAppScreenshot -Url $exportReadyUrl -OutputPath $finishNowScreenshotPath
+  Capture-AppScreenshot -Url $readinessUrl -OutputPath $quarterScreenshotPath
+  Capture-AppScreenshot -Url $exportReadyUrl -OutputPath $finishNowScreenshotPath
 
apply_patch(auto_approved=true) exited 0 in 560ms:
Success. Updated the following files:
M C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..b0b6a961dcb80123d7634a661b59a1e944c1c7a1
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,26 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    fallbackIssues: inboxItems,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1532,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1547,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1633,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2104,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2316,29 +2562,241 @@
     </div>
   );
 
-  const renderInbox = () => (
-    <div>
-      <h4>Finish Now Queue</h4>
-      <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
-      <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
-            <div>
-              <b>{item.merchant || 'Unknown'}</b>
-              <br />
-              <small>
-                {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
-              </small>
-              <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
-            </div>
+  const renderInbox = () => {
+    return (
+    <div className="inbox-mobile-shell">
+      <section className="inbox-mobile-hero">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2">
+          <div className="inbox-kicker">
+            <Bell size={14} />
+            Mobile-first exception queue
           </div>
-        ))}
-      </div>
-    </div>
-  );
+          <div className="status-pill">{filteredInboxCards.length} visible</div>
+        </div>
+        <h4>Finish now</h4>
+        <p>
+          Clear the blockers that stop the quarterly export: category, business or personal, split treatment, and duplicate resolution.
+        </p>
+        <div className="inbox-mobile-stats">
+          <div className="inbox-mobile-stat">
+            <small>Queue</small>
+            <strong>{inboxCards.length}</strong>
+            <small>{activeQuarterReport.quarter_reference || READINESS_DEMO_REPORT.quarter_reference}</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Critical</small>
+            <strong>{highSeverityCount}</strong>
+            <small>High-severity blockers</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Ready now</small>
+            <strong>{readyToExportCount}</strong>
+            <small>Transactions already export-safe</small>
+          </div>
+        </div>
+        <div className="hero-actions">
+          <button className="hero-action-main" onClick={() => setCurrentTab('quarter')}>
+            <ArrowRight size={16} />
+            Review quarter readiness
+          </button>
+          <button className="hero-action-secondary" onClick={undoLastInboxAction} disabled={inboxBusyId === 'undo-last'}>
+            {inboxBusyId === 'undo-last' ? <RefreshCcw size={16} className="spinner" /> : <ArchiveRestore size={16} />}
+            Undo last action
+          </button>
+        </div>
+      </section>
+
+      {inboxMessage && (
+        <div className={`readiness-panel inbox-message ${inboxMessage.type}`}>
+          {inboxMessage.text}
+        </div>
+      )}
+
+      <section className="readiness-panel">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-3">
+          <div>
+            <h5 style={{ marginBottom: 4 }}>Queue filters</h5>
+            <small className="small-muted">Keep the mobile pass focused on one blocker type at a time.</small>
+          </div>
+          <div className="status-pill">{activeQuarterReport.readiness_pct ?? READINESS_DEMO_REPORT.readiness_pct}% ready</div>
+        </div>
+        <div className="inbox-filter-row">
+          {INBOX_FILTERS.map((filter) => {
+            const count = filter.key === 'all'
+              ? inboxCards.length
+              : inboxCards.filter((card) => card.issue_types.includes(filter.key)).length;
+            return (
+              <button
+                key={filter.key}
+                type="button"
+                className={`inbox-filter-chip ${inboxFilter === filter.key ? 'active' : ''}`}
+                onClick={() => setInboxFilter(filter.key)}
+              >
+                <span>{filter.label}</span>
+                <small>{count}</small>
+              </button>
+            );
+          })}
+        </div>
+      </section>
+
+      {filteredInboxCards.length === 0 ? (
+        <div className="inbox-empty">
+          No blockers match this filter. Reset to All blockers or return to the quarter screen to confirm readiness.
+        </div>
+      ) : (
+        <div className="inbox-card-list">
+          {filteredInboxCards.map((card) => {
+            const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+            const saving = inboxBusyId === card.id;
+            const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+            return (
+              <section key={card.id} className="inbox-action-card">
+                <div className="inbox-card-top">
+                  <div>
+                    <strong>{card.merchant}</strong>
+                    <div className="inbox-card-subline">
+                      {card.txn_date ? String(card.txn_date).slice(0, 10) : 'Date pending'} · {formatIssueAmount(card.amount, card.direction)}
+                    </div>
+                  </div>
+                  <span className={`inbox-issue-chip ${card.severity}`}>
+                    <TriangleAlert size={14} />
+                    {INBOX_REASON_LABELS[card.blocker_reason] || card.blocker_reason.replace(/_/g, ' ')}
+                  </span>
+                </div>
+
+                <div className="inbox-chip-row">
+                  {card.issue_labels.map((label) => (
+                    <span key={`${card.id}-${label}`} className={`inbox-issue-chip ${card.severity}`}>
+                      {label}
+                    </span>
+                  ))}
+                  {card.suggested_category?.category_name && (
+                    <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
+                      <CheckCircle2 size={14} />
+                      Suggested: {card.suggested_category.category_name}
+                    </button>
+                  )}
+                </div>
+
+                <p className="inbox-explainer">{card.explanation || 'Resolve the blocking fields to clear this transaction from the queue.'}</p>
+
+                {(card.supports_category || card.supports_business_personal || card.supports_split) && (
+                  <div className="inbox-action-grid">
+                    <div className="inbox-field">
+                      <label>Category code</label>
+                      <input
+                        value={draft.category_code}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_code', event.target.value)}
+                        placeholder="MATERIALS"
+                      />
+                    </div>
+                    <div className="inbox-field">
+                      <label>Category label</label>
+                      <input
+                        value={draft.category_name}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_name', event.target.value)}
+                        placeholder="Materials"
+                      />
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_business_personal || card.supports_split) && (
+                  <div>
+                    <div className="inbox-field">
+                      <label>Business treatment</label>
+                    </div>
+                    <div className="inbox-toggle-row">
+                      {[
+                        { value: 'BUSINESS', label: 'Business' },
+                        { value: 'PERSONAL', label: 'Personal' },
+                        { value: 'SPLIT', label: 'Split' },
+                      ].map((option) => (
+                        <button
+                          key={option.value}
+                          type="button"
+                          className={`inbox-toggle ${draft.business_personal === option.value ? 'active' : ''}`}
+                          onClick={() => setInboxDraftField(card.id, 'business_personal', option.value)}
+                        >
+                          {option.label}
+                        </button>
+                      ))}
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_split || isSplit) && (
+                  <div className="inbox-field">
+                    <label>Business split %</label>
+                    <input
+                      type="number"
+                      min="0"
+                      max="100"
+                      value={draft.split_business_pct}
+                      onChange={(event) => setInboxDraftField(card.id, 'split_business_pct', event.target.value)}
+                      placeholder="80"
+                    />
+                  </div>
+                )}
+
+                {card.supports_duplicate && (
+                  <>
+                    <div>
+                      <div className="inbox-field">
+                        <label>Duplicate action</label>
+                      </div>
+                      <div className="inbox-duplicate-row">
+                        {[
+                          { value: 'dismiss', label: 'Dismiss duplicate' },
+                          { value: 'merge', label: 'Merge to original' },
+                        ].map((option) => (
+                          <button
+                            key={option.value}
+                            type="button"
+                            className={`inbox-toggle ${draft.duplicate_action === option.value ? 'active' : ''}`}
+                            onClick={() => setInboxDraftField(card.id, 'duplicate_action', option.value)}
+                          >
+                            {option.label}
+                          </button>
+                        ))}
+                      </div>
+                    </div>
+                    {draft.duplicate_action === 'merge' && (
+                      <div className="inbox-field">
+                        <label>Original transaction id</label>
+                        <input
+                          value={draft.duplicate_of_txn_id}
+                          onChange={(event) => setInboxDraftField(card.id, 'duplicate_of_txn_id', event.target.value)}
+                          placeholder="txn-9001"
+                        />
+                      </div>
+                    )}
+                  </>
+                )}
 
+                <div className="inbox-footer-row">
+                  <div className="inbox-inline-status">
+                    Route: {card.resolution_target?.route || `/api/v1/inbox/${card.id}`}
+                  </div>
+                  <div className="inbox-footer-actions">
+                    <button className="secondary-action" onClick={clearInboxMessage} disabled={saving}>
+                      Clear banner
+                    </button>
+                    <button className="primary-action" onClick={() => submitInboxAction(card)} disabled={saving}>
+                      {saving ? <RefreshCcw size={16} className="spinner" /> : <CheckCircle2 size={16} />}
+                      Save and clear blocker
+                    </button>
+                  </div>
+                </div>
+              </section>
+            );
+          })}
+        </div>
+      )}
+    </div>
+    );
+  };
   const renderControlCentre = () => {
     const policyLabels = {
       vat_scheme: 'VAT scheme',
@@ -2510,11 +2968,11 @@
           <section className="readiness-panel">
             <h5 style={{ marginBottom: 12 }}>Queue Backlog</h5>
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
-                <div key={item.id} className="control-row">
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
+                <div key={item.id || item.affected_entity_id} className="control-row">
                   <div>
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
                   <div className="text-end">
                     <div className="small-muted">{item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date}</div>
@@ -2522,7 +2980,7 @@
                   </div>
                 </div>
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
               )}
             </div>
@@ -2668,7 +3126,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2697,7 +3155,7 @@
   };
 
   const createSnapshotVersion = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     const status = snapshotWorkspace?.snapshot_status;
 
     if (!status?.can_create_snapshot_version) {
@@ -2719,7 +3177,7 @@
         description: `Quarter snapshot generated from Quarter workspace`,
         metadata: {
           source_type: 'manual',
-          readiness_pct: quarterReadiness?.readiness_pct ?? null
+          readiness_pct: activeQuarterReport?.readiness_pct ?? null
         }
       });
       await refreshQuarterWorkspace();
@@ -2734,7 +3192,7 @@
   };
 
   const closeQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(closeReason || '').trim()) {
       setQuarterActionError('Close reason is required.');
       setQuarterActionMessage('');
@@ -2785,7 +3243,7 @@
   };
 
   const reopenQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(reopenReason || '').trim()) {
       setQuarterActionError('Reopen reason is required.');
       setQuarterActionMessage('');
@@ -2844,9 +3302,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3342,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3354,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3365,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3379,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..669173cd6176ee9ee5c86c5895ea49cb9524ebc4
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,155 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, fallbackIssues = [], resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const issueSource = Array.isArray(report.issue_list) && report.issue_list.length > 0
+    ? report.issue_list
+    : fallbackIssues;
+  const allIssues = sortIssues(Array.isArray(issueSource) ? issueSource : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/bizPA/frontend/src/quarterReadiness.test.js b/bizPA/frontend/src/quarterReadiness.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..0b810191a89d8b82091bf5de1408afcc23c14e65
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.test.js
@@ -0,0 +1,93 @@
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
+
+const reportFixture = {
+  quarter_reference: 'Q1-2026',
+  period_start: '2026-01-01',
+  period_end: '2026-03-31',
+  total_txns_in_period: 18,
+  readiness_pct: 78,
+  can_export: false,
+  issue_list: [
+    {
+      issue_type: 'missing_category',
+      affected_entity_id: 'txn-9101',
+      severity: 'high',
+      weight: 35,
+      explanation: 'Category missing for Fuel Stop.',
+      txn_date: '2026-02-04',
+      merchant: 'Fuel Stop',
+      amount: 64.18,
+      direction: 'out',
+      resolution_target: {
+        label: 'Classify transaction',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'missing_business_personal',
+      affected_entity_id: 'txn-9103',
+      severity: 'high',
+      weight: 30,
+      explanation: 'Business/personal decision missing.',
+      txn_date: '2026-02-15',
+      merchant: 'Mobile Contract',
+      amount: 42.5,
+      direction: 'out',
+      resolution_target: {
+        label: 'Set business or personal',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'unresolved_duplicate',
+      affected_entity_id: 'txn-9104',
+      severity: 'medium',
+      weight: 15,
+      explanation: 'Duplicate not resolved.',
+      txn_date: '2026-03-02',
+      merchant: 'Client Payment',
+      amount: 820,
+      direction: 'in',
+      resolution_target: {
+        label: 'Resolve duplicate',
+        workflow: '/inbox/finish-now',
+      },
+    },
+  ],
+};
+
+describe('quarter readiness helpers', () => {
+  test('keeps blockers ordered and updates headline while blockers remain', () => {
+    const state = buildQuarterUiState({ report: reportFixture, resolvedIssueIds: ['txn-9104'] });
+
+    expect(state.itemsLeft).toBe(2);
+    expect(state.exportEnabled).toBe(false);
+    expect(state.report.readiness_pct).toBeGreaterThan(78);
+    expect(state.headline).toContain('items left');
+    expect(state.activeIssues.map((issue) => issue.affected_entity_id)).toEqual(['txn-9101', 'txn-9103']);
+    expect(state.report.explanation_lines[0]).toContain('Readiness is');
+  });
+
+  test('unlocks export immediately when every blocker is resolved', () => {
+    const state = buildQuarterUiState({
+      report: reportFixture,
+      resolvedIssueIds: ['txn-9101', 'txn-9103', 'txn-9104'],
+    });
+
+    expect(state.itemsLeft).toBe(0);
+    expect(state.exportEnabled).toBe(true);
+    expect(state.report.readiness_pct).toBe(100);
+    expect(state.report.can_export).toBe(true);
+    expect(state.report.issue_summary).toEqual([]);
+  });
+
+  test('supports demo query params that resolve all blockers', () => {
+    const params = new URLSearchParams('resolvedDemoIssues=all');
+    const resolvedIds = deriveResolvedIssueIdsFromSearchParams(params, reportFixture.issue_list);
+
+    expect(resolvedIds).toEqual(['txn-9101', 'txn-9103', 'txn-9104']);
+  });
+});
diff --git a/bizPA/start_bizpa_tax_readiness_ui.ps1 b/bizPA/start_bizpa_tax_readiness_ui.ps1
index 1fc4bef8cd172c31fdc3db52d1acc9d4775c5485..7216e44eac386fe436a9af8e9b417cd2d61e37fe
--- a/bizPA/start_bizpa_tax_readiness_ui.ps1
+++ b/bizPA/start_bizpa_tax_readiness_ui.ps1
@@ -4,6 +4,7 @@
 $frontendPath = Join-Path $repoRoot 'frontend'
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
 
 Write-Host 'Starting bizPA tax readiness UI...' -ForegroundColor Cyan
 
@@ -14,7 +15,10 @@
 Write-Host 'Tax readiness dashboard URL:' -ForegroundColor Green
 Write-Host "  $readinessUrl" -ForegroundColor Yellow
 Write-Host ''
+Write-Host 'Export-ready verification URL:' -ForegroundColor Green
+Write-Host "  $exportReadyUrl" -ForegroundColor Yellow
+Write-Host ''
 Write-Host 'Base frontend URL:' -ForegroundColor Green
 Write-Host "  $frontendUrl" -ForegroundColor Yellow
 Write-Host ''
-Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI can be reviewed without backend setup.' -ForegroundColor Cyan
+Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI and zero-blocker export-ready state can be reviewed without backend setup.' -ForegroundColor Cyan
diff --git a/bizPA/tax_readiness_ui_smoke.ps1 b/bizPA/tax_readiness_ui_smoke.ps1
index 56d67892a51b22572503dec560d17123b370c2b7..47a50c8d3ac8ff8c933943a7c6a966cdce89328f
--- a/bizPA/tax_readiness_ui_smoke.ps1
+++ b/bizPA/tax_readiness_ui_smoke.ps1
@@ -28,99 +28,92 @@
 
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
-$screenshotPath = 'C:\Users\edebe\eds\workstream\verification\20260311_201500_bizpa_tax_readiness_ui.png'
-$chromeProfilePath = 'C:\Users\edebe\eds\workstream\verification\chrome_capture_profile_tax_readiness'
-
-New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
-
-$frontendJob = Start-Job -ScriptBlock {
-  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
-  $env:PORT = '3002'
-  npm.cmd start
-}
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
+$verificationRoot = 'C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification'
+$quarterScreenshotPath = Join-Path $verificationRoot 'quarter_readiness_screen.png'
+$finishNowScreenshotPath = Join-Path $verificationRoot 'finish_now_queue_zero_blockers.png'
+function Wait-FrontendReady {
+  param (
+    [string]$Url
+  )
 
-try {
-  $frontendResponse = $null
-  $frontendReady = $false
   for ($i = 0; $i -lt 45; $i++) {
     Start-Sleep -Seconds 2
     try {
-      $frontendResponse = Invoke-WebRequest -UseBasicParsing $readinessUrl
-      if ($frontendResponse.StatusCode -eq 200) {
-        $frontendReady = $true
-        break
+      $response = Invoke-WebRequest -UseBasicParsing $Url
+      if ($response.StatusCode -eq 200) {
+        return $response
       }
     } catch {
     }
   }
 
-  if (-not $frontendReady) {
-    throw "Frontend did not become ready on $readinessUrl"
-  }
+  throw "Frontend did not become ready on $Url"
+}
 
-  $chromeArgs = @(
-    "--user-data-dir=$chromeProfilePath",
-    '--new-window',
-    '--window-size=1600,1500',
-    '--no-first-run',
-    '--disable-session-crashed-bubble',
-    '--disable-infobars',
-    '--disable-breakpad',
-    '--disable-crash-reporter',
-    '--noerrdialogs',
-    "--app=$readinessUrl"
+function Capture-AppScreenshot {
+  param (
+    [string]$Url,
+    [string]$OutputPath
   )
-  Start-Process 'C:\Program Files\Google\Chrome\Application\chrome.exe' -ArgumentList $chromeArgs | Out-Null
 
-  $chromeWindow = $null
-  for ($i = 0; $i -lt 20; $i++) {
-    Start-Sleep -Seconds 1
-    $chromeWindow = Get-Process chrome -ErrorAction SilentlyContinue |
-      ForEach-Object {
-        try {
-          if ($_.MainWindowHandle -and ([int64]$_.MainWindowHandle -ne 0)) {
-            [pscustomobject]@{
-              Process = $_
-              StartTime = $_.StartTime
-            }
-          }
-        } catch {
-        }
-      } |
-      Sort-Object StartTime -Descending |
-      Select-Object -First 1
-    if ($chromeWindow) {
-      $chromeWindow = $chromeWindow.Process
+  $form = New-Object System.Windows.Forms.Form
+  $form.Width = 1600
+  $form.Height = 1500
+  $form.StartPosition = 'Manual'
+  $form.Location = New-Object System.Drawing.Point(-32000, -32000)
+
+  $browser = New-Object System.Windows.Forms.WebBrowser
+  $browser.ScriptErrorsSuppressed = $true
+  $browser.ScrollBarsEnabled = $false
+  $browser.Dock = [System.Windows.Forms.DockStyle]::Fill
+  $form.Controls.Add($browser)
+  $form.Show()
+  $browser.Navigate($Url)
+
+  $ready = $false
+  for ($i = 0; $i -lt 90; $i++) {
+    [System.Windows.Forms.Application]::DoEvents()
+    Start-Sleep -Milliseconds 250
+    if ($browser.ReadyState -eq [System.Windows.Forms.WebBrowserReadyState]::Complete) {
+      $ready = $true
       break
     }
   }
 
-  if (-not $chromeWindow) {
-    throw 'Chrome readiness window did not expose a main window handle for screenshot capture.'
+  if (-not $ready) {
+    $form.Close()
+    throw "Embedded browser did not finish loading $Url"
   }
 
-  [WindowCaptureNative]::ShowWindow($chromeWindow.MainWindowHandle, 3) | Out-Null
-  [WindowCaptureNative]::SetForegroundWindow($chromeWindow.MainWindowHandle) | Out-Null
-  Start-Sleep -Seconds 1
+  Start-Sleep -Seconds 3
+  [System.Windows.Forms.Application]::DoEvents()
 
-  $rect = New-Object WindowCaptureNative+RECT
-  [WindowCaptureNative]::GetWindowRect($chromeWindow.MainWindowHandle, [ref]$rect) | Out-Null
-  $width = [Math]::Max(1, $rect.Right - $rect.Left)
-  $height = [Math]::Max(1, $rect.Bottom - $rect.Top)
-  $bitmap = New-Object System.Drawing.Bitmap $width, $height
-  $graphics = [System.Drawing.Graphics]::FromImage($bitmap)
-  $graphics.CopyFromScreen($rect.Left, $rect.Top, 0, 0, $bitmap.Size)
-  $bitmap.Save($screenshotPath, [System.Drawing.Imaging.ImageFormat]::Png)
-  $graphics.Dispose()
+  $bitmap = New-Object System.Drawing.Bitmap $browser.Width, $browser.Height
+  $browser.DrawToBitmap($bitmap, (New-Object System.Drawing.Rectangle 0, 0, $browser.Width, $browser.Height))
+  $bitmap.Save($OutputPath, [System.Drawing.Imaging.ImageFormat]::Png)
   $bitmap.Dispose()
+  $form.Close()
+}
+
+New-Item -ItemType Directory -Force -Path $verificationRoot | Out-Null
+
+$frontendJob = Start-Job -ScriptBlock {
+  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
+  $env:PORT = '3002'
+  npm.cmd start
+}
 
-  Get-Process chrome -ErrorAction SilentlyContinue |
-    Where-Object { $_.StartTime -ge $chromeWindow.StartTime.AddSeconds(-2) } |
-    Stop-Process -Force -ErrorAction SilentlyContinue
+try {
+  $frontendResponse = Wait-FrontendReady -Url $readinessUrl
+  Capture-AppScreenshot -Url $readinessUrl -OutputPath $quarterScreenshotPath
+  Capture-AppScreenshot -Url $exportReadyUrl -OutputPath $finishNowScreenshotPath
 
   "READINESS_URL=$readinessUrl"
+  "EXPORT_READY_URL=$exportReadyUrl"
   "FRONTEND_STATUS=$($frontendResponse.StatusCode)"
-  "SCREENSHOT=$screenshotPath"
+  "SCREENSHOT_QUARTER=$quarterScreenshotPath"
+  "SCREENSHOT_EXPORT_READY=$finishNowScreenshotPath"
   'FRONTEND_LOG_START'
   Receive-Job $frontendJob -Keep -ErrorAction SilentlyContinue 2>&1
   'FRONTEND_LOG_END'
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..b0b6a961dcb80123d7634a661b59a1e944c1c7a1
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,26 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    fallbackIssues: inboxItems,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1532,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1547,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1633,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2104,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2316,29 +2562,241 @@
     </div>
   );
 
-  const renderInbox = () => (
-    <div>
-      <h4>Finish Now Queue</h4>
-      <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
-      <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
-            <div>
-              <b>{item.merchant || 'Unknown'}</b>
-              <br />
-              <small>
-                {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
-              </small>
-              <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
-            </div>
+  const renderInbox = () => {
+    return (
+    <div className="inbox-mobile-shell">
+      <section className="inbox-mobile-hero">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2">
+          <div className="inbox-kicker">
+            <Bell size={14} />
+            Mobile-first exception queue
           </div>
-        ))}
-      </div>
-    </div>
-  );
+          <div className="status-pill">{filteredInboxCards.length} visible</div>
+        </div>
+        <h4>Finish now</h4>
+        <p>
+          Clear the blockers that stop the quarterly export: category, business or personal, split treatment, and duplicate resolution.
+        </p>
+        <div className="inbox-mobile-stats">
+          <div className="inbox-mobile-stat">
+            <small>Queue</small>
+            <strong>{inboxCards.length}</strong>
+            <small>{activeQuarterReport.quarter_reference || READINESS_DEMO_REPORT.quarter_reference}</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Critical</small>
+            <strong>{highSeverityCount}</strong>
+            <small>High-severity blockers</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Ready now</small>
+            <strong>{readyToExportCount}</strong>
+            <small>Transactions already export-safe</small>
+          </div>
+        </div>
+        <div className="hero-actions">
+          <button className="hero-action-main" onClick={() => setCurrentTab('quarter')}>
+            <ArrowRight size={16} />
+            Review quarter readiness
+          </button>
+          <button className="hero-action-secondary" onClick={undoLastInboxAction} disabled={inboxBusyId === 'undo-last'}>
+            {inboxBusyId === 'undo-last' ? <RefreshCcw size={16} className="spinner" /> : <ArchiveRestore size={16} />}
+            Undo last action
+          </button>
+        </div>
+      </section>
+
+      {inboxMessage && (
+        <div className={`readiness-panel inbox-message ${inboxMessage.type}`}>
+          {inboxMessage.text}
+        </div>
+      )}
+
+      <section className="readiness-panel">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-3">
+          <div>
+            <h5 style={{ marginBottom: 4 }}>Queue filters</h5>
+            <small className="small-muted">Keep the mobile pass focused on one blocker type at a time.</small>
+          </div>
+          <div className="status-pill">{activeQuarterReport.readiness_pct ?? READINESS_DEMO_REPORT.readiness_pct}% ready</div>
+        </div>
+        <div className="inbox-filter-row">
+          {INBOX_FILTERS.map((filter) => {
+            const count = filter.key === 'all'
+              ? inboxCards.length
+              : inboxCards.filter((card) => card.issue_types.includes(filter.key)).length;
+            return (
+              <button
+                key={filter.key}
+                type="button"
+                className={`inbox-filter-chip ${inboxFilter === filter.key ? 'active' : ''}`}
+                onClick={() => setInboxFilter(filter.key)}
+              >
+                <span>{filter.label}</span>
+                <small>{count}</small>
+              </button>
+            );
+          })}
+        </div>
+      </section>
+
+      {filteredInboxCards.length === 0 ? (
+        <div className="inbox-empty">
+          No blockers match this filter. Reset to All blockers or return to the quarter screen to confirm readiness.
+        </div>
+      ) : (
+        <div className="inbox-card-list">
+          {filteredInboxCards.map((card) => {
+            const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+            const saving = inboxBusyId === card.id;
+            const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+            return (
+              <section key={card.id} className="inbox-action-card">
+                <div className="inbox-card-top">
+                  <div>
+                    <strong>{card.merchant}</strong>
+                    <div className="inbox-card-subline">
+                      {card.txn_date ? String(card.txn_date).slice(0, 10) : 'Date pending'} · {formatIssueAmount(card.amount, card.direction)}
+                    </div>
+                  </div>
+                  <span className={`inbox-issue-chip ${card.severity}`}>
+                    <TriangleAlert size={14} />
+                    {INBOX_REASON_LABELS[card.blocker_reason] || card.blocker_reason.replace(/_/g, ' ')}
+                  </span>
+                </div>
+
+                <div className="inbox-chip-row">
+                  {card.issue_labels.map((label) => (
+                    <span key={`${card.id}-${label}`} className={`inbox-issue-chip ${card.severity}`}>
+                      {label}
+                    </span>
+                  ))}
+                  {card.suggested_category?.category_name && (
+                    <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
+                      <CheckCircle2 size={14} />
+                      Suggested: {card.suggested_category.category_name}
+                    </button>
+                  )}
+                </div>
+
+                <p className="inbox-explainer">{card.explanation || 'Resolve the blocking fields to clear this transaction from the queue.'}</p>
+
+                {(card.supports_category || card.supports_business_personal || card.supports_split) && (
+                  <div className="inbox-action-grid">
+                    <div className="inbox-field">
+                      <label>Category code</label>
+                      <input
+                        value={draft.category_code}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_code', event.target.value)}
+                        placeholder="MATERIALS"
+                      />
+                    </div>
+                    <div className="inbox-field">
+                      <label>Category label</label>
+                      <input
+                        value={draft.category_name}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_name', event.target.value)}
+                        placeholder="Materials"
+                      />
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_business_personal || card.supports_split) && (
+                  <div>
+                    <div className="inbox-field">
+                      <label>Business treatment</label>
+                    </div>
+                    <div className="inbox-toggle-row">
+                      {[
+                        { value: 'BUSINESS', label: 'Business' },
+                        { value: 'PERSONAL', label: 'Personal' },
+                        { value: 'SPLIT', label: 'Split' },
+                      ].map((option) => (
+                        <button
+                          key={option.value}
+                          type="button"
+                          className={`inbox-toggle ${draft.business_personal === option.value ? 'active' : ''}`}
+                          onClick={() => setInboxDraftField(card.id, 'business_personal', option.value)}
+                        >
+                          {option.label}
+                        </button>
+                      ))}
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_split || isSplit) && (
+                  <div className="inbox-field">
+                    <label>Business split %</label>
+                    <input
+                      type="number"
+                      min="0"
+                      max="100"
+                      value={draft.split_business_pct}
+                      onChange={(event) => setInboxDraftField(card.id, 'split_business_pct', event.target.value)}
+                      placeholder="80"
+                    />
+                  </div>
+                )}
+
+                {card.supports_duplicate && (
+                  <>
+                    <div>
+                      <div className="inbox-field">
+                        <label>Duplicate action</label>
+                      </div>
+                      <div className="inbox-duplicate-row">
+                        {[
+                          { value: 'dismiss', label: 'Dismiss duplicate' },
+                          { value: 'merge', label: 'Merge to original' },
+                        ].map((option) => (
+                          <button
+                            key={option.value}
+                            type="button"
+                            className={`inbox-toggle ${draft.duplicate_action === option.value ? 'active' : ''}`}
+                            onClick={() => setInboxDraftField(card.id, 'duplicate_action', option.value)}
+                          >
+                            {option.label}
+                          </button>
+                        ))}
+                      </div>
+                    </div>
+                    {draft.duplicate_action === 'merge' && (
+                      <div className="inbox-field">
+                        <label>Original transaction id</label>
+                        <input
+                          value={draft.duplicate_of_txn_id}
+                          onChange={(event) => setInboxDraftField(card.id, 'duplicate_of_txn_id', event.target.value)}
+                          placeholder="txn-9001"
+                        />
+                      </div>
+                    )}
+                  </>
+                )}
 
+                <div className="inbox-footer-row">
+                  <div className="inbox-inline-status">
+                    Route: {card.resolution_target?.route || `/api/v1/inbox/${card.id}`}
+                  </div>
+                  <div className="inbox-footer-actions">
+                    <button className="secondary-action" onClick={clearInboxMessage} disabled={saving}>
+                      Clear banner
+                    </button>
+                    <button className="primary-action" onClick={() => submitInboxAction(card)} disabled={saving}>
+                      {saving ? <RefreshCcw size={16} className="spinner" /> : <CheckCircle2 size={16} />}
+                      Save and clear blocker
+                    </button>
+                  </div>
+                </div>
+              </section>
+            );
+          })}
+        </div>
+      )}
+    </div>
+    );
+  };
   const renderControlCentre = () => {
     const policyLabels = {
       vat_scheme: 'VAT scheme',
@@ -2510,11 +2968,11 @@
           <section className="readiness-panel">
             <h5 style={{ marginBottom: 12 }}>Queue Backlog</h5>
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
-                <div key={item.id} className="control-row">
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
+                <div key={item.id || item.affected_entity_id} className="control-row">
                   <div>
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
                   <div className="text-end">
                     <div className="small-muted">{item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date}</div>
@@ -2522,7 +2980,7 @@
                   </div>
                 </div>
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
               )}
             </div>
@@ -2668,7 +3126,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2697,7 +3155,7 @@
   };
 
   const createSnapshotVersion = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     const status = snapshotWorkspace?.snapshot_status;
 
     if (!status?.can_create_snapshot_version) {
@@ -2719,7 +3177,7 @@
         description: `Quarter snapshot generated from Quarter workspace`,
         metadata: {
           source_type: 'manual',
-          readiness_pct: quarterReadiness?.readiness_pct ?? null
+          readiness_pct: activeQuarterReport?.readiness_pct ?? null
         }
       });
       await refreshQuarterWorkspace();
@@ -2734,7 +3192,7 @@
   };
 
   const closeQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(closeReason || '').trim()) {
       setQuarterActionError('Close reason is required.');
       setQuarterActionMessage('');
@@ -2785,7 +3243,7 @@
   };
 
   const reopenQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(reopenReason || '').trim()) {
       setQuarterActionError('Reopen reason is required.');
       setQuarterActionMessage('');
@@ -2844,9 +3302,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3342,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3354,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3365,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3379,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..669173cd6176ee9ee5c86c5895ea49cb9524ebc4
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,155 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, fallbackIssues = [], resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const issueSource = Array.isArray(report.issue_list) && report.issue_list.length > 0
+    ? report.issue_list
+    : fallbackIssues;
+  const allIssues = sortIssues(Array.isArray(issueSource) ? issueSource : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/bizPA/frontend/src/quarterReadiness.test.js b/bizPA/frontend/src/quarterReadiness.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..0b810191a89d8b82091bf5de1408afcc23c14e65
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.test.js
@@ -0,0 +1,93 @@
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
+
+const reportFixture = {
+  quarter_reference: 'Q1-2026',
+  period_start: '2026-01-01',
+  period_end: '2026-03-31',
+  total_txns_in_period: 18,
+  readiness_pct: 78,
+  can_export: false,
+  issue_list: [
+    {
+      issue_type: 'missing_category',
+      affected_entity_id: 'txn-9101',
+      severity: 'high',
+      weight: 35,
+      explanation: 'Category missing for Fuel Stop.',
+      txn_date: '2026-02-04',
+      merchant: 'Fuel Stop',
+      amount: 64.18,
+      direction: 'out',
+      resolution_target: {
+        label: 'Classify transaction',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'missing_business_personal',
+      affected_entity_id: 'txn-9103',
+      severity: 'high',
+      weight: 30,
+      explanation: 'Business/personal decision missing.',
+      txn_date: '2026-02-15',
+      merchant: 'Mobile Contract',
+      amount: 42.5,
+      direction: 'out',
+      resolution_target: {
+        label: 'Set business or personal',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'unresolved_duplicate',
+      affected_entity_id: 'txn-9104',
+      severity: 'medium',
+      weight: 15,
+      explanation: 'Duplicate not resolved.',
+      txn_date: '2026-03-02',
+      merchant: 'Client Payment',
+      amount: 820,
+      direction: 'in',
+      resolution_target: {
+        label: 'Resolve duplicate',
+        workflow: '/inbox/finish-now',
+      },
+    },
+  ],
+};
+
+describe('quarter readiness helpers', () => {
+  test('keeps blockers ordered and updates headline while blockers remain', () => {
+    const state = buildQuarterUiState({ report: reportFixture, resolvedIssueIds: ['txn-9104'] });
+
+    expect(state.itemsLeft).toBe(2);
+    expect(state.exportEnabled).toBe(false);
+    expect(state.report.readiness_pct).toBeGreaterThan(78);
+    expect(state.headline).toContain('items left');
+    expect(state.activeIssues.map((issue) => issue.affected_entity_id)).toEqual(['txn-9101', 'txn-9103']);
+    expect(state.report.explanation_lines[0]).toContain('Readiness is');
+  });
+
+  test('unlocks export immediately when every blocker is resolved', () => {
+    const state = buildQuarterUiState({
+      report: reportFixture,
+      resolvedIssueIds: ['txn-9101', 'txn-9103', 'txn-9104'],
+    });
+
+    expect(state.itemsLeft).toBe(0);
+    expect(state.exportEnabled).toBe(true);
+    expect(state.report.readiness_pct).toBe(100);
+    expect(state.report.can_export).toBe(true);
+    expect(state.report.issue_summary).toEqual([]);
+  });
+
+  test('supports demo query params that resolve all blockers', () => {
+    const params = new URLSearchParams('resolvedDemoIssues=all');
+    const resolvedIds = deriveResolvedIssueIdsFromSearchParams(params, reportFixture.issue_list);
+
+    expect(resolvedIds).toEqual(['txn-9101', 'txn-9103', 'txn-9104']);
+  });
+});
diff --git a/bizPA/start_bizpa_tax_readiness_ui.ps1 b/bizPA/start_bizpa_tax_readiness_ui.ps1
index 1fc4bef8cd172c31fdc3db52d1acc9d4775c5485..7216e44eac386fe436a9af8e9b417cd2d61e37fe
--- a/bizPA/start_bizpa_tax_readiness_ui.ps1
+++ b/bizPA/start_bizpa_tax_readiness_ui.ps1
@@ -4,6 +4,7 @@
 $frontendPath = Join-Path $repoRoot 'frontend'
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
 
 Write-Host 'Starting bizPA tax readiness UI...' -ForegroundColor Cyan
 
@@ -14,7 +15,10 @@
 Write-Host 'Tax readiness dashboard URL:' -ForegroundColor Green
 Write-Host "  $readinessUrl" -ForegroundColor Yellow
 Write-Host ''
+Write-Host 'Export-ready verification URL:' -ForegroundColor Green
+Write-Host "  $exportReadyUrl" -ForegroundColor Yellow
+Write-Host ''
 Write-Host 'Base frontend URL:' -ForegroundColor Green
 Write-Host "  $frontendUrl" -ForegroundColor Yellow
 Write-Host ''
-Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI can be reviewed without backend setup.' -ForegroundColor Cyan
+Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI and zero-blocker export-ready state can be reviewed without backend setup.' -ForegroundColor Cyan
diff --git a/bizPA/tax_readiness_ui_smoke.ps1 b/bizPA/tax_readiness_ui_smoke.ps1
index 56d67892a51b22572503dec560d17123b370c2b7..47a50c8d3ac8ff8c933943a7c6a966cdce89328f
--- a/bizPA/tax_readiness_ui_smoke.ps1
+++ b/bizPA/tax_readiness_ui_smoke.ps1
@@ -28,99 +28,92 @@
 
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
-$screenshotPath = 'C:\Users\edebe\eds\workstream\verification\20260311_201500_bizpa_tax_readiness_ui.png'
-$chromeProfilePath = 'C:\Users\edebe\eds\workstream\verification\chrome_capture_profile_tax_readiness'
-
-New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
-
-$frontendJob = Start-Job -ScriptBlock {
-  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
-  $env:PORT = '3002'
-  npm.cmd start
-}
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
+$verificationRoot = 'C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification'
+$quarterScreenshotPath = Join-Path $verificationRoot 'quarter_readiness_screen.png'
+$finishNowScreenshotPath = Join-Path $verificationRoot 'finish_now_queue_zero_blockers.png'
+function Wait-FrontendReady {
+  param (
+    [string]$Url
+  )
 
-try {
-  $frontendResponse = $null
-  $frontendReady = $false
   for ($i = 0; $i -lt 45; $i++) {
     Start-Sleep -Seconds 2
     try {
-      $frontendResponse = Invoke-WebRequest -UseBasicParsing $readinessUrl
-      if ($frontendResponse.StatusCode -eq 200) {
-        $frontendReady = $true
-        break
+      $response = Invoke-WebRequest -UseBasicParsing $Url
+      if ($response.StatusCode -eq 200) {
+        return $response
       }
     } catch {
     }
   }
 
-  if (-not $frontendReady) {
-    throw "Frontend did not become ready on $readinessUrl"
-  }
+  throw "Frontend did not become ready on $Url"
+}
 
-  $chromeArgs = @(
-    "--user-data-dir=$chromeProfilePath",
-    '--new-window',
-    '--window-size=1600,1500',
-    '--no-first-run',
-    '--disable-session-crashed-bubble',
-    '--disable-infobars',
-    '--disable-breakpad',
-    '--disable-crash-reporter',
-    '--noerrdialogs',
-    "--app=$readinessUrl"
+function Capture-AppScreenshot {
+  param (
+    [string]$Url,
+    [string]$OutputPath
   )
-  Start-Process 'C:\Program Files\Google\Chrome\Application\chrome.exe' -ArgumentList $chromeArgs | Out-Null
 
-  $chromeWindow = $null
-  for ($i = 0; $i -lt 20; $i++) {
-    Start-Sleep -Seconds 1
-    $chromeWindow = Get-Process chrome -ErrorAction SilentlyContinue |
-      ForEach-Object {
-        try {
-          if ($_.MainWindowHandle -and ([int64]$_.MainWindowHandle -ne 0)) {
-            [pscustomobject]@{
-              Process = $_
-              StartTime = $_.StartTime
-            }
-          }
-        } catch {
-        }
-      } |
-      Sort-Object StartTime -Descending |
-      Select-Object -First 1
-    if ($chromeWindow) {
-      $chromeWindow = $chromeWindow.Process
+  $form = New-Object System.Windows.Forms.Form
+  $form.Width = 1600
+  $form.Height = 1500
+  $form.StartPosition = 'Manual'
+  $form.Location = New-Object System.Drawing.Point(-32000, -32000)
+
+  $browser = New-Object System.Windows.Forms.WebBrowser
+  $browser.ScriptErrorsSuppressed = $true
+  $browser.ScrollBarsEnabled = $false
+  $browser.Dock = [System.Windows.Forms.DockStyle]::Fill
+  $form.Controls.Add($browser)
+  $form.Show()
+  $browser.Navigate($Url)
+
+  $ready = $false
+  for ($i = 0; $i -lt 90; $i++) {
+    [System.Windows.Forms.Application]::DoEvents()
+    Start-Sleep -Milliseconds 250
+    if ($browser.ReadyState -eq [System.Windows.Forms.WebBrowserReadyState]::Complete) {
+      $ready = $true
       break
     }
   }
 
-  if (-not $chromeWindow) {
-    throw 'Chrome readiness window did not expose a main window handle for screenshot capture.'
+  if (-not $ready) {
+    $form.Close()
+    throw "Embedded browser did not finish loading $Url"
   }
 
-  [WindowCaptureNative]::ShowWindow($chromeWindow.MainWindowHandle, 3) | Out-Null
-  [WindowCaptureNative]::SetForegroundWindow($chromeWindow.MainWindowHandle) | Out-Null
-  Start-Sleep -Seconds 1
+  Start-Sleep -Seconds 3
+  [System.Windows.Forms.Application]::DoEvents()
 
-  $rect = New-Object WindowCaptureNative+RECT
-  [WindowCaptureNative]::GetWindowRect($chromeWindow.MainWindowHandle, [ref]$rect) | Out-Null
-  $width = [Math]::Max(1, $rect.Right - $rect.Left)
-  $height = [Math]::Max(1, $rect.Bottom - $rect.Top)
-  $bitmap = New-Object System.Drawing.Bitmap $width, $height
-  $graphics = [System.Drawing.Graphics]::FromImage($bitmap)
-  $graphics.CopyFromScreen($rect.Left, $rect.Top, 0, 0, $bitmap.Size)
-  $bitmap.Save($screenshotPath, [System.Drawing.Imaging.ImageFormat]::Png)
-  $graphics.Dispose()
+  $bitmap = New-Object System.Drawing.Bitmap $browser.Width, $browser.Height
+  $browser.DrawToBitmap($bitmap, (New-Object System.Drawing.Rectangle 0, 0, $browser.Width, $browser.Height))
+  $bitmap.Save($OutputPath, [System.Drawing.Imaging.ImageFormat]::Png)
   $bitmap.Dispose()
+  $form.Close()
+}
+
+New-Item -ItemType Directory -Force -Path $verificationRoot | Out-Null
+
+$frontendJob = Start-Job -ScriptBlock {
+  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
+  $env:PORT = '3002'
+  npm.cmd start
+}
 
-  Get-Process chrome -ErrorAction SilentlyContinue |
-    Where-Object { $_.StartTime -ge $chromeWindow.StartTime.AddSeconds(-2) } |
-    Stop-Process -Force -ErrorAction SilentlyContinue
+try {
+  $frontendResponse = Wait-FrontendReady -Url $readinessUrl
+  Capture-AppScreenshot -Url $readinessUrl -OutputPath $quarterScreenshotPath
+  Capture-AppScreenshot -Url $exportReadyUrl -OutputPath $finishNowScreenshotPath
 
   "READINESS_URL=$readinessUrl"
+  "EXPORT_READY_URL=$exportReadyUrl"
   "FRONTEND_STATUS=$($frontendResponse.StatusCode)"
-  "SCREENSHOT=$screenshotPath"
+  "SCREENSHOT_QUARTER=$quarterScreenshotPath"
+  "SCREENSHOT_EXPORT_READY=$finishNowScreenshotPath"
   'FRONTEND_LOG_START'
   Receive-Job $frontendJob -Keep -ErrorAction SilentlyContinue 2>&1
   'FRONTEND_LOG_END'
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "powershell -ExecutionPolicy Bypass -File 'C:\\Users\\edebe\\eds\\bizPA\\tax_readiness_ui_smoke.ps1'" in C:\Users\edebe\eds succeeded in 45.95s:
READINESS_URL=http://127.0.0.1:3002/?readinessDemo=1&tab=quarter
EXPORT_READY_URL=http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all
FRONTEND_STATUS=200
SCREENSHOT_QUARTER=C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png
SCREENSHOT_EXPORT_READY=C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png
FRONTEND_LOG_START

> frontend@1.3.6 start
> react-scripts start

Starting the development server...

Compiled successfully!

You can now view frontend in the browser.

  Local:            http://localhost:3002
  On Your Network:  http://192.168.1.110:3002

Note that the development build is not optimized.
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..b0b6a961dcb80123d7634a661b59a1e944c1c7a1
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,26 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    fallbackIssues: inboxItems,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1532,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1547,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1633,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2104,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2316,29 +2562,241 @@
     </div>
   );
 
-  const renderInbox = () => (
-    <div>
-      <h4>Finish Now Queue</h4>
-      <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
-      <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
-            <div>
-              <b>{item.merchant || 'Unknown'}</b>
-              <br />
-              <small>
-                {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
-              </small>
-              <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
-            </div>
+  const renderInbox = () => {
+    return (
+    <div className="inbox-mobile-shell">
+      <section className="inbox-mobile-hero">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2">
+          <div className="inbox-kicker">
+            <Bell size={14} />
+            Mobile-first exception queue
           </div>
-        ))}
-      </div>
-    </div>
-  );
+          <div className="status-pill">{filteredInboxCards.length} visible</div>
+        </div>
+        <h4>Finish now</h4>
+        <p>
+          Clear the blockers that stop the quarterly export: category, business or personal, split treatment, and duplicate resolution.
+        </p>
+        <div className="inbox-mobile-stats">
+          <div className="inbox-mobile-stat">
+            <small>Queue</small>
+            <strong>{inboxCards.length}</strong>
+            <small>{activeQuarterReport.quarter_reference || READINESS_DEMO_REPORT.quarter_reference}</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Critical</small>
+            <strong>{highSeverityCount}</strong>
+            <small>High-severity blockers</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Ready now</small>
+            <strong>{readyToExportCount}</strong>
+            <small>Transactions already export-safe</small>
+          </div>
+        </div>
+        <div className="hero-actions">
+          <button className="hero-action-main" onClick={() => setCurrentTab('quarter')}>
+            <ArrowRight size={16} />
+            Review quarter readiness
+          </button>
+          <button className="hero-action-secondary" onClick={undoLastInboxAction} disabled={inboxBusyId === 'undo-last'}>
+            {inboxBusyId === 'undo-last' ? <RefreshCcw size={16} className="spinner" /> : <ArchiveRestore size={16} />}
+            Undo last action
+          </button>
+        </div>
+      </section>
+
+      {inboxMessage && (
+        <div className={`readiness-panel inbox-message ${inboxMessage.type}`}>
+          {inboxMessage.text}
+        </div>
+      )}
+
+      <section className="readiness-panel">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-3">
+          <div>
+            <h5 style={{ marginBottom: 4 }}>Queue filters</h5>
+            <small className="small-muted">Keep the mobile pass focused on one blocker type at a time.</small>
+          </div>
+          <div className="status-pill">{activeQuarterReport.readiness_pct ?? READINESS_DEMO_REPORT.readiness_pct}% ready</div>
+        </div>
+        <div className="inbox-filter-row">
+          {INBOX_FILTERS.map((filter) => {
+            const count = filter.key === 'all'
+              ? inboxCards.length
+              : inboxCards.filter((card) => card.issue_types.includes(filter.key)).length;
+            return (
+              <button
+                key={filter.key}
+                type="button"
+                className={`inbox-filter-chip ${inboxFilter === filter.key ? 'active' : ''}`}
+                onClick={() => setInboxFilter(filter.key)}
+              >
+                <span>{filter.label}</span>
+                <small>{count}</small>
+              </button>
+            );
+          })}
+        </div>
+      </section>
+
+      {filteredInboxCards.length === 0 ? (
+        <div className="inbox-empty">
+          No blockers match this filter. Reset to All blockers or return to the quarter screen to confirm readiness.
+        </div>
+      ) : (
+        <div className="inbox-card-list">
+          {filteredInboxCards.map((card) => {
+            const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+            const saving = inboxBusyId === card.id;
+            const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+            return (
+              <section key={card.id} className="inbox-action-card">
+                <div className="inbox-card-top">
+                  <div>
+                    <strong>{card.merchant}</strong>
+                    <div className="inbox-card-subline">
+                      {card.txn_date ? String(card.txn_date).slice(0, 10) : 'Date pending'} · {formatIssueAmount(card.amount, card.direction)}
+                    </div>
+                  </div>
+                  <span className={`inbox-issue-chip ${card.severity}`}>
+                    <TriangleAlert size={14} />
+                    {INBOX_REASON_LABELS[card.blocker_reason] || card.blocker_reason.replace(/_/g, ' ')}
+                  </span>
+                </div>
+
+                <div className="inbox-chip-row">
+                  {card.issue_labels.map((label) => (
+                    <span key={`${card.id}-${label}`} className={`inbox-issue-chip ${card.severity}`}>
+                      {label}
+                    </span>
+                  ))}
+                  {card.suggested_category?.category_name && (
+                    <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
+                      <CheckCircle2 size={14} />
+                      Suggested: {card.suggested_category.category_name}
+                    </button>
+                  )}
+                </div>
+
+                <p className="inbox-explainer">{card.explanation || 'Resolve the blocking fields to clear this transaction from the queue.'}</p>
+
+                {(card.supports_category || card.supports_business_personal || card.supports_split) && (
+                  <div className="inbox-action-grid">
+                    <div className="inbox-field">
+                      <label>Category code</label>
+                      <input
+                        value={draft.category_code}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_code', event.target.value)}
+                        placeholder="MATERIALS"
+                      />
+                    </div>
+                    <div className="inbox-field">
+                      <label>Category label</label>
+                      <input
+                        value={draft.category_name}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_name', event.target.value)}
+                        placeholder="Materials"
+                      />
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_business_personal || card.supports_split) && (
+                  <div>
+                    <div className="inbox-field">
+                      <label>Business treatment</label>
+                    </div>
+                    <div className="inbox-toggle-row">
+                      {[
+                        { value: 'BUSINESS', label: 'Business' },
+                        { value: 'PERSONAL', label: 'Personal' },
+                        { value: 'SPLIT', label: 'Split' },
+                      ].map((option) => (
+                        <button
+                          key={option.value}
+                          type="button"
+                          className={`inbox-toggle ${draft.business_personal === option.value ? 'active' : ''}`}
+                          onClick={() => setInboxDraftField(card.id, 'business_personal', option.value)}
+                        >
+                          {option.label}
+                        </button>
+                      ))}
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_split || isSplit) && (
+                  <div className="inbox-field">
+                    <label>Business split %</label>
+                    <input
+                      type="number"
+                      min="0"
+                      max="100"
+                      value={draft.split_business_pct}
+                      onChange={(event) => setInboxDraftField(card.id, 'split_business_pct', event.target.value)}
+                      placeholder="80"
+                    />
+                  </div>
+                )}
+
+                {card.supports_duplicate && (
+                  <>
+                    <div>
+                      <div className="inbox-field">
+                        <label>Duplicate action</label>
+                      </div>
+                      <div className="inbox-duplicate-row">
+                        {[
+                          { value: 'dismiss', label: 'Dismiss duplicate' },
+                          { value: 'merge', label: 'Merge to original' },
+                        ].map((option) => (
+                          <button
+                            key={option.value}
+                            type="button"
+                            className={`inbox-toggle ${draft.duplicate_action === option.value ? 'active' : ''}`}
+                            onClick={() => setInboxDraftField(card.id, 'duplicate_action', option.value)}
+                          >
+                            {option.label}
+                          </button>
+                        ))}
+                      </div>
+                    </div>
+                    {draft.duplicate_action === 'merge' && (
+                      <div className="inbox-field">
+                        <label>Original transaction id</label>
+                        <input
+                          value={draft.duplicate_of_txn_id}
+                          onChange={(event) => setInboxDraftField(card.id, 'duplicate_of_txn_id', event.target.value)}
+                          placeholder="txn-9001"
+                        />
+                      </div>
+                    )}
+                  </>
+                )}
 
+                <div className="inbox-footer-row">
+                  <div className="inbox-inline-status">
+                    Route: {card.resolution_target?.route || `/api/v1/inbox/${card.id}`}
+                  </div>
+                  <div className="inbox-footer-actions">
+                    <button className="secondary-action" onClick={clearInboxMessage} disabled={saving}>
+                      Clear banner
+                    </button>
+                    <button className="primary-action" onClick={() => submitInboxAction(card)} disabled={saving}>
+                      {saving ? <RefreshCcw size={16} className="spinner" /> : <CheckCircle2 size={16} />}
+                      Save and clear blocker
+                    </button>
+                  </div>
+                </div>
+              </section>
+            );
+          })}
+        </div>
+      )}
+    </div>
+    );
+  };
   const renderControlCentre = () => {
     const policyLabels = {
       vat_scheme: 'VAT scheme',
@@ -2510,11 +2968,11 @@
           <section className="readiness-panel">
             <h5 style={{ marginBottom: 12 }}>Queue Backlog</h5>
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
-                <div key={item.id} className="control-row">
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
+                <div key={item.id || item.affected_entity_id} className="control-row">
                   <div>
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
                   <div className="text-end">
                     <div className="small-muted">{item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date}</div>
@@ -2522,7 +2980,7 @@
                   </div>
                 </div>
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
               )}
             </div>
@@ -2668,7 +3126,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2697,7 +3155,7 @@
   };
 
   const createSnapshotVersion = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     const status = snapshotWorkspace?.snapshot_status;
 
     if (!status?.can_create_snapshot_version) {
@@ -2719,7 +3177,7 @@
         description: `Quarter snapshot generated from Quarter workspace`,
         metadata: {
           source_type: 'manual',
-          readiness_pct: quarterReadiness?.readiness_pct ?? null
+          readiness_pct: activeQuarterReport?.readiness_pct ?? null
         }
       });
       await refreshQuarterWorkspace();
@@ -2734,7 +3192,7 @@
   };
 
   const closeQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(closeReason || '').trim()) {
       setQuarterActionError('Close reason is required.');
       setQuarterActionMessage('');
@@ -2785,7 +3243,7 @@
   };
 
   const reopenQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(reopenReason || '').trim()) {
       setQuarterActionError('Reopen reason is required.');
       setQuarterActionMessage('');
@@ -2844,9 +3302,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3342,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3354,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3365,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3379,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..669173cd6176ee9ee5c86c5895ea49cb9524ebc4
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,155 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, fallbackIssues = [], resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const issueSource = Array.isArray(report.issue_list) && report.issue_list.length > 0
+    ? report.issue_list
+    : fallbackIssues;
+  const allIssues = sortIssues(Array.isArray(issueSource) ? issueSource : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/bizPA/frontend/src/quarterReadiness.test.js b/bizPA/frontend/src/quarterReadiness.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..0b810191a89d8b82091bf5de1408afcc23c14e65
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.test.js
@@ -0,0 +1,93 @@
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
+
+const reportFixture = {
+  quarter_reference: 'Q1-2026',
+  period_start: '2026-01-01',
+  period_end: '2026-03-31',
+  total_txns_in_period: 18,
+  readiness_pct: 78,
+  can_export: false,
+  issue_list: [
+    {
+      issue_type: 'missing_category',
+      affected_entity_id: 'txn-9101',
+      severity: 'high',
+      weight: 35,
+      explanation: 'Category missing for Fuel Stop.',
+      txn_date: '2026-02-04',
+      merchant: 'Fuel Stop',
+      amount: 64.18,
+      direction: 'out',
+      resolution_target: {
+        label: 'Classify transaction',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'missing_business_personal',
+      affected_entity_id: 'txn-9103',
+      severity: 'high',
+      weight: 30,
+      explanation: 'Business/personal decision missing.',
+      txn_date: '2026-02-15',
+      merchant: 'Mobile Contract',
+      amount: 42.5,
+      direction: 'out',
+      resolution_target: {
+        label: 'Set business or personal',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'unresolved_duplicate',
+      affected_entity_id: 'txn-9104',
+      severity: 'medium',
+      weight: 15,
+      explanation: 'Duplicate not resolved.',
+      txn_date: '2026-03-02',
+      merchant: 'Client Payment',
+      amount: 820,
+      direction: 'in',
+      resolution_target: {
+        label: 'Resolve duplicate',
+        workflow: '/inbox/finish-now',
+      },
+    },
+  ],
+};
+
+describe('quarter readiness helpers', () => {
+  test('keeps blockers ordered and updates headline while blockers remain', () => {
+    const state = buildQuarterUiState({ report: reportFixture, resolvedIssueIds: ['txn-9104'] });
+
+    expect(state.itemsLeft).toBe(2);
+    expect(state.exportEnabled).toBe(false);
+    expect(state.report.readiness_pct).toBeGreaterThan(78);
+    expect(state.headline).toContain('items left');
+    expect(state.activeIssues.map((issue) => issue.affected_entity_id)).toEqual(['txn-9101', 'txn-9103']);
+    expect(state.report.explanation_lines[0]).toContain('Readiness is');
+  });
+
+  test('unlocks export immediately when every blocker is resolved', () => {
+    const state = buildQuarterUiState({
+      report: reportFixture,
+      resolvedIssueIds: ['txn-9101', 'txn-9103', 'txn-9104'],
+    });
+
+    expect(state.itemsLeft).toBe(0);
+    expect(state.exportEnabled).toBe(true);
+    expect(state.report.readiness_pct).toBe(100);
+    expect(state.report.can_export).toBe(true);
+    expect(state.report.issue_summary).toEqual([]);
+  });
+
+  test('supports demo query params that resolve all blockers', () => {
+    const params = new URLSearchParams('resolvedDemoIssues=all');
+    const resolvedIds = deriveResolvedIssueIdsFromSearchParams(params, reportFixture.issue_list);
+
+    expect(resolvedIds).toEqual(['txn-9101', 'txn-9103', 'txn-9104']);
+  });
+});
diff --git a/bizPA/start_bizpa_tax_readiness_ui.ps1 b/bizPA/start_bizpa_tax_readiness_ui.ps1
index 1fc4bef8cd172c31fdc3db52d1acc9d4775c5485..7216e44eac386fe436a9af8e9b417cd2d61e37fe
--- a/bizPA/start_bizpa_tax_readiness_ui.ps1
+++ b/bizPA/start_bizpa_tax_readiness_ui.ps1
@@ -4,6 +4,7 @@
 $frontendPath = Join-Path $repoRoot 'frontend'
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
 
 Write-Host 'Starting bizPA tax readiness UI...' -ForegroundColor Cyan
 
@@ -14,7 +15,10 @@
 Write-Host 'Tax readiness dashboard URL:' -ForegroundColor Green
 Write-Host "  $readinessUrl" -ForegroundColor Yellow
 Write-Host ''
+Write-Host 'Export-ready verification URL:' -ForegroundColor Green
+Write-Host "  $exportReadyUrl" -ForegroundColor Yellow
+Write-Host ''
 Write-Host 'Base frontend URL:' -ForegroundColor Green
 Write-Host "  $frontendUrl" -ForegroundColor Yellow
 Write-Host ''
-Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI can be reviewed without backend setup.' -ForegroundColor Cyan
+Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI and zero-blocker export-ready state can be reviewed without backend setup.' -ForegroundColor Cyan
diff --git a/bizPA/tax_readiness_ui_smoke.ps1 b/bizPA/tax_readiness_ui_smoke.ps1
index 56d67892a51b22572503dec560d17123b370c2b7..47a50c8d3ac8ff8c933943a7c6a966cdce89328f
--- a/bizPA/tax_readiness_ui_smoke.ps1
+++ b/bizPA/tax_readiness_ui_smoke.ps1
@@ -28,99 +28,92 @@
 
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
-$screenshotPath = 'C:\Users\edebe\eds\workstream\verification\20260311_201500_bizpa_tax_readiness_ui.png'
-$chromeProfilePath = 'C:\Users\edebe\eds\workstream\verification\chrome_capture_profile_tax_readiness'
-
-New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
-
-$frontendJob = Start-Job -ScriptBlock {
-  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
-  $env:PORT = '3002'
-  npm.cmd start
-}
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
+$verificationRoot = 'C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification'
+$quarterScreenshotPath = Join-Path $verificationRoot 'quarter_readiness_screen.png'
+$finishNowScreenshotPath = Join-Path $verificationRoot 'finish_now_queue_zero_blockers.png'
+function Wait-FrontendReady {
+  param (
+    [string]$Url
+  )
 
-try {
-  $frontendResponse = $null
-  $frontendReady = $false
   for ($i = 0; $i -lt 45; $i++) {
     Start-Sleep -Seconds 2
     try {
-      $frontendResponse = Invoke-WebRequest -UseBasicParsing $readinessUrl
-      if ($frontendResponse.StatusCode -eq 200) {
-        $frontendReady = $true
-        break
+      $response = Invoke-WebRequest -UseBasicParsing $Url
+      if ($response.StatusCode -eq 200) {
+        return $response
       }
     } catch {
     }
   }
 
-  if (-not $frontendReady) {
-    throw "Frontend did not become ready on $readinessUrl"
-  }
+  throw "Frontend did not become ready on $Url"
+}
 
-  $chromeArgs = @(
-    "--user-data-dir=$chromeProfilePath",
-    '--new-window',
-    '--window-size=1600,1500',
-    '--no-first-run',
-    '--disable-session-crashed-bubble',
-    '--disable-infobars',
-    '--disable-breakpad',
-    '--disable-crash-reporter',
-    '--noerrdialogs',
-    "--app=$readinessUrl"
+function Capture-AppScreenshot {
+  param (
+    [string]$Url,
+    [string]$OutputPath
   )
-  Start-Process 'C:\Program Files\Google\Chrome\Application\chrome.exe' -ArgumentList $chromeArgs | Out-Null
 
-  $chromeWindow = $null
-  for ($i = 0; $i -lt 20; $i++) {
-    Start-Sleep -Seconds 1
-    $chromeWindow = Get-Process chrome -ErrorAction SilentlyContinue |
-      ForEach-Object {
-        try {
-          if ($_.MainWindowHandle -and ([int64]$_.MainWindowHandle -ne 0)) {
-            [pscustomobject]@{
-              Process = $_
-              StartTime = $_.StartTime
-            }
-          }
-        } catch {
-        }
-      } |
-      Sort-Object StartTime -Descending |
-      Select-Object -First 1
-    if ($chromeWindow) {
-      $chromeWindow = $chromeWindow.Process
+  $form = New-Object System.Windows.Forms.Form
+  $form.Width = 1600
+  $form.Height = 1500
+  $form.StartPosition = 'Manual'
+  $form.Location = New-Object System.Drawing.Point(-32000, -32000)
+
+  $browser = New-Object System.Windows.Forms.WebBrowser
+  $browser.ScriptErrorsSuppressed = $true
+  $browser.ScrollBarsEnabled = $false
+  $browser.Dock = [System.Windows.Forms.DockStyle]::Fill
+  $form.Controls.Add($browser)
+  $form.Show()
+  $browser.Navigate($Url)
+
+  $ready = $false
+  for ($i = 0; $i -lt 90; $i++) {
+    [System.Windows.Forms.Application]::DoEvents()
+    Start-Sleep -Milliseconds 250
+    if ($browser.ReadyState -eq [System.Windows.Forms.WebBrowserReadyState]::Complete) {
+      $ready = $true
       break
     }
   }
 
-  if (-not $chromeWindow) {
-    throw 'Chrome readiness window did not expose a main window handle for screenshot capture.'
+  if (-not $ready) {
+    $form.Close()
+    throw "Embedded browser did not finish loading $Url"
   }
 
-  [WindowCaptureNative]::ShowWindow($chromeWindow.MainWindowHandle, 3) | Out-Null
-  [WindowCaptureNative]::SetForegroundWindow($chromeWindow.MainWindowHandle) | Out-Null
-  Start-Sleep -Seconds 1
+  Start-Sleep -Seconds 3
+  [System.Windows.Forms.Application]::DoEvents()
 
-  $rect = New-Object WindowCaptureNative+RECT
-  [WindowCaptureNative]::GetWindowRect($chromeWindow.MainWindowHandle, [ref]$rect) | Out-Null
-  $width = [Math]::Max(1, $rect.Right - $rect.Left)
-  $height = [Math]::Max(1, $rect.Bottom - $rect.Top)
-  $bitmap = New-Object System.Drawing.Bitmap $width, $height
-  $graphics = [System.Drawing.Graphics]::FromImage($bitmap)
-  $graphics.CopyFromScreen($rect.Left, $rect.Top, 0, 0, $bitmap.Size)
-  $bitmap.Save($screenshotPath, [System.Drawing.Imaging.ImageFormat]::Png)
-  $graphics.Dispose()
+  $bitmap = New-Object System.Drawing.Bitmap $browser.Width, $browser.Height
+  $browser.DrawToBitmap($bitmap, (New-Object System.Drawing.Rectangle 0, 0, $browser.Width, $browser.Height))
+  $bitmap.Save($OutputPath, [System.Drawing.Imaging.ImageFormat]::Png)
   $bitmap.Dispose()
+  $form.Close()
+}
+
+New-Item -ItemType Directory -Force -Path $verificationRoot | Out-Null
+
+$frontendJob = Start-Job -ScriptBlock {
+  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
+  $env:PORT = '3002'
+  npm.cmd start
+}
 
-  Get-Process chrome -ErrorAction SilentlyContinue |
-    Where-Object { $_.StartTime -ge $chromeWindow.StartTime.AddSeconds(-2) } |
-    Stop-Process -Force -ErrorAction SilentlyContinue
+try {
+  $frontendResponse = Wait-FrontendReady -Url $readinessUrl
+  Capture-AppScreenshot -Url $readinessUrl -OutputPath $quarterScreenshotPath
+  Capture-AppScreenshot -Url $exportReadyUrl -OutputPath $finishNowScreenshotPath
 
   "READINESS_URL=$readinessUrl"
+  "EXPORT_READY_URL=$exportReadyUrl"
   "FRONTEND_STATUS=$($frontendResponse.StatusCode)"
-  "SCREENSHOT=$screenshotPath"
+  "SCREENSHOT_QUARTER=$quarterScreenshotPath"
+  "SCREENSHOT_EXPORT_READY=$finishNowScreenshotPath"
   'FRONTEND_LOG_START'
   Receive-Job $frontendJob -Keep -ErrorAction SilentlyContinue 2>&1
   'FRONTEND_LOG_END'
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Item 'C:\\Users\\edebe\\eds\\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\\verification\\finish_now_queue_zero_blockers.png' | Select-Object FullName,Length,LastWriteTime" in C:\Users\edebe\edsexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Item 'C:\\Users\\edebe\\eds\\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\\verification\\quarter_readiness_screen.png' | Select-Object FullName,Length,LastWriteTime" in C:\Users\edebe\eds succeeded in 1.02s:

[32;1mFullName[0m
[32;1m--------                                                                                                               [0m
C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quart…

 succeeded in 1.04s:

[32;1mFullName[0m
[32;1m--------                                                                                                               [0m
C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finis…

file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..b0b6a961dcb80123d7634a661b59a1e944c1c7a1
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,26 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    fallbackIssues: inboxItems,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1532,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1547,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1633,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2104,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2316,29 +2562,241 @@
     </div>
   );
 
-  const renderInbox = () => (
-    <div>
-      <h4>Finish Now Queue</h4>
-      <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
-      <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
-            <div>
-              <b>{item.merchant || 'Unknown'}</b>
-              <br />
-              <small>
-                {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
-              </small>
-              <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
-            </div>
+  const renderInbox = () => {
+    return (
+    <div className="inbox-mobile-shell">
+      <section className="inbox-mobile-hero">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2">
+          <div className="inbox-kicker">
+            <Bell size={14} />
+            Mobile-first exception queue
           </div>
-        ))}
-      </div>
-    </div>
-  );
+          <div className="status-pill">{filteredInboxCards.length} visible</div>
+        </div>
+        <h4>Finish now</h4>
+        <p>
+          Clear the blockers that stop the quarterly export: category, business or personal, split treatment, and duplicate resolution.
+        </p>
+        <div className="inbox-mobile-stats">
+          <div className="inbox-mobile-stat">
+            <small>Queue</small>
+            <strong>{inboxCards.length}</strong>
+            <small>{activeQuarterReport.quarter_reference || READINESS_DEMO_REPORT.quarter_reference}</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Critical</small>
+            <strong>{highSeverityCount}</strong>
+            <small>High-severity blockers</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Ready now</small>
+            <strong>{readyToExportCount}</strong>
+            <small>Transactions already export-safe</small>
+          </div>
+        </div>
+        <div className="hero-actions">
+          <button className="hero-action-main" onClick={() => setCurrentTab('quarter')}>
+            <ArrowRight size={16} />
+            Review quarter readiness
+          </button>
+          <button className="hero-action-secondary" onClick={undoLastInboxAction} disabled={inboxBusyId === 'undo-last'}>
+            {inboxBusyId === 'undo-last' ? <RefreshCcw size={16} className="spinner" /> : <ArchiveRestore size={16} />}
+            Undo last action
+          </button>
+        </div>
+      </section>
+
+      {inboxMessage && (
+        <div className={`readiness-panel inbox-message ${inboxMessage.type}`}>
+          {inboxMessage.text}
+        </div>
+      )}
+
+      <section className="readiness-panel">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-3">
+          <div>
+            <h5 style={{ marginBottom: 4 }}>Queue filters</h5>
+            <small className="small-muted">Keep the mobile pass focused on one blocker type at a time.</small>
+          </div>
+          <div className="status-pill">{activeQuarterReport.readiness_pct ?? READINESS_DEMO_REPORT.readiness_pct}% ready</div>
+        </div>
+        <div className="inbox-filter-row">
+          {INBOX_FILTERS.map((filter) => {
+            const count = filter.key === 'all'
+              ? inboxCards.length
+              : inboxCards.filter((card) => card.issue_types.includes(filter.key)).length;
+            return (
+              <button
+                key={filter.key}
+                type="button"
+                className={`inbox-filter-chip ${inboxFilter === filter.key ? 'active' : ''}`}
+                onClick={() => setInboxFilter(filter.key)}
+              >
+                <span>{filter.label}</span>
+                <small>{count}</small>
+              </button>
+            );
+          })}
+        </div>
+      </section>
+
+      {filteredInboxCards.length === 0 ? (
+        <div className="inbox-empty">
+          No blockers match this filter. Reset to All blockers or return to the quarter screen to confirm readiness.
+        </div>
+      ) : (
+        <div className="inbox-card-list">
+          {filteredInboxCards.map((card) => {
+            const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+            const saving = inboxBusyId === card.id;
+            const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+            return (
+              <section key={card.id} className="inbox-action-card">
+                <div className="inbox-card-top">
+                  <div>
+                    <strong>{card.merchant}</strong>
+                    <div className="inbox-card-subline">
+                      {card.txn_date ? String(card.txn_date).slice(0, 10) : 'Date pending'} · {formatIssueAmount(card.amount, card.direction)}
+                    </div>
+                  </div>
+                  <span className={`inbox-issue-chip ${card.severity}`}>
+                    <TriangleAlert size={14} />
+                    {INBOX_REASON_LABELS[card.blocker_reason] || card.blocker_reason.replace(/_/g, ' ')}
+                  </span>
+                </div>
+
+                <div className="inbox-chip-row">
+                  {card.issue_labels.map((label) => (
+                    <span key={`${card.id}-${label}`} className={`inbox-issue-chip ${card.severity}`}>
+                      {label}
+                    </span>
+                  ))}
+                  {card.suggested_category?.category_name && (
+                    <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
+                      <CheckCircle2 size={14} />
+                      Suggested: {card.suggested_category.category_name}
+                    </button>
+                  )}
+                </div>
+
+                <p className="inbox-explainer">{card.explanation || 'Resolve the blocking fields to clear this transaction from the queue.'}</p>
+
+                {(card.supports_category || card.supports_business_personal || card.supports_split) && (
+                  <div className="inbox-action-grid">
+                    <div className="inbox-field">
+                      <label>Category code</label>
+                      <input
+                        value={draft.category_code}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_code', event.target.value)}
+                        placeholder="MATERIALS"
+                      />
+                    </div>
+                    <div className="inbox-field">
+                      <label>Category label</label>
+                      <input
+                        value={draft.category_name}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_name', event.target.value)}
+                        placeholder="Materials"
+                      />
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_business_personal || card.supports_split) && (
+                  <div>
+                    <div className="inbox-field">
+                      <label>Business treatment</label>
+                    </div>
+                    <div className="inbox-toggle-row">
+                      {[
+                        { value: 'BUSINESS', label: 'Business' },
+                        { value: 'PERSONAL', label: 'Personal' },
+                        { value: 'SPLIT', label: 'Split' },
+                      ].map((option) => (
+                        <button
+                          key={option.value}
+                          type="button"
+                          className={`inbox-toggle ${draft.business_personal === option.value ? 'active' : ''}`}
+                          onClick={() => setInboxDraftField(card.id, 'business_personal', option.value)}
+                        >
+                          {option.label}
+                        </button>
+                      ))}
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_split || isSplit) && (
+                  <div className="inbox-field">
+                    <label>Business split %</label>
+                    <input
+                      type="number"
+                      min="0"
+                      max="100"
+                      value={draft.split_business_pct}
+                      onChange={(event) => setInboxDraftField(card.id, 'split_business_pct', event.target.value)}
+                      placeholder="80"
+                    />
+                  </div>
+                )}
+
+                {card.supports_duplicate && (
+                  <>
+                    <div>
+                      <div className="inbox-field">
+                        <label>Duplicate action</label>
+                      </div>
+                      <div className="inbox-duplicate-row">
+                        {[
+                          { value: 'dismiss', label: 'Dismiss duplicate' },
+                          { value: 'merge', label: 'Merge to original' },
+                        ].map((option) => (
+                          <button
+                            key={option.value}
+                            type="button"
+                            className={`inbox-toggle ${draft.duplicate_action === option.value ? 'active' : ''}`}
+                            onClick={() => setInboxDraftField(card.id, 'duplicate_action', option.value)}
+                          >
+                            {option.label}
+                          </button>
+                        ))}
+                      </div>
+                    </div>
+                    {draft.duplicate_action === 'merge' && (
+                      <div className="inbox-field">
+                        <label>Original transaction id</label>
+                        <input
+                          value={draft.duplicate_of_txn_id}
+                          onChange={(event) => setInboxDraftField(card.id, 'duplicate_of_txn_id', event.target.value)}
+                          placeholder="txn-9001"
+                        />
+                      </div>
+                    )}
+                  </>
+                )}
 
+                <div className="inbox-footer-row">
+                  <div className="inbox-inline-status">
+                    Route: {card.resolution_target?.route || `/api/v1/inbox/${card.id}`}
+                  </div>
+                  <div className="inbox-footer-actions">
+                    <button className="secondary-action" onClick={clearInboxMessage} disabled={saving}>
+                      Clear banner
+                    </button>
+                    <button className="primary-action" onClick={() => submitInboxAction(card)} disabled={saving}>
+                      {saving ? <RefreshCcw size={16} className="spinner" /> : <CheckCircle2 size={16} />}
+                      Save and clear blocker
+                    </button>
+                  </div>
+                </div>
+              </section>
+            );
+          })}
+        </div>
+      )}
+    </div>
+    );
+  };
   const renderControlCentre = () => {
     const policyLabels = {
       vat_scheme: 'VAT scheme',
@@ -2510,11 +2968,11 @@
           <section className="readiness-panel">
             <h5 style={{ marginBottom: 12 }}>Queue Backlog</h5>
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
-                <div key={item.id} className="control-row">
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
+                <div key={item.id || item.affected_entity_id} className="control-row">
                   <div>
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
                   <div className="text-end">
                     <div className="small-muted">{item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date}</div>
@@ -2522,7 +2980,7 @@
                   </div>
                 </div>
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
               )}
             </div>
@@ -2668,7 +3126,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2697,7 +3155,7 @@
   };
 
   const createSnapshotVersion = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     const status = snapshotWorkspace?.snapshot_status;
 
     if (!status?.can_create_snapshot_version) {
@@ -2719,7 +3177,7 @@
         description: `Quarter snapshot generated from Quarter workspace`,
         metadata: {
           source_type: 'manual',
-          readiness_pct: quarterReadiness?.readiness_pct ?? null
+          readiness_pct: activeQuarterReport?.readiness_pct ?? null
         }
       });
       await refreshQuarterWorkspace();
@@ -2734,7 +3192,7 @@
   };
 
   const closeQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(closeReason || '').trim()) {
       setQuarterActionError('Close reason is required.');
       setQuarterActionMessage('');
@@ -2785,7 +3243,7 @@
   };
 
   const reopenQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(reopenReason || '').trim()) {
       setQuarterActionError('Reopen reason is required.');
       setQuarterActionMessage('');
@@ -2844,9 +3302,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3342,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3354,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3365,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3379,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..669173cd6176ee9ee5c86c5895ea49cb9524ebc4
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,155 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, fallbackIssues = [], resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const issueSource = Array.isArray(report.issue_list) && report.issue_list.length > 0
+    ? report.issue_list
+    : fallbackIssues;
+  const allIssues = sortIssues(Array.isArray(issueSource) ? issueSource : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/bizPA/frontend/src/quarterReadiness.test.js b/bizPA/frontend/src/quarterReadiness.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..0b810191a89d8b82091bf5de1408afcc23c14e65
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.test.js
@@ -0,0 +1,93 @@
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
+
+const reportFixture = {
+  quarter_reference: 'Q1-2026',
+  period_start: '2026-01-01',
+  period_end: '2026-03-31',
+  total_txns_in_period: 18,
+  readiness_pct: 78,
+  can_export: false,
+  issue_list: [
+    {
+      issue_type: 'missing_category',
+      affected_entity_id: 'txn-9101',
+      severity: 'high',
+      weight: 35,
+      explanation: 'Category missing for Fuel Stop.',
+      txn_date: '2026-02-04',
+      merchant: 'Fuel Stop',
+      amount: 64.18,
+      direction: 'out',
+      resolution_target: {
+        label: 'Classify transaction',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'missing_business_personal',
+      affected_entity_id: 'txn-9103',
+      severity: 'high',
+      weight: 30,
+      explanation: 'Business/personal decision missing.',
+      txn_date: '2026-02-15',
+      merchant: 'Mobile Contract',
+      amount: 42.5,
+      direction: 'out',
+      resolution_target: {
+        label: 'Set business or personal',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'unresolved_duplicate',
+      affected_entity_id: 'txn-9104',
+      severity: 'medium',
+      weight: 15,
+      explanation: 'Duplicate not resolved.',
+      txn_date: '2026-03-02',
+      merchant: 'Client Payment',
+      amount: 820,
+      direction: 'in',
+      resolution_target: {
+        label: 'Resolve duplicate',
+        workflow: '/inbox/finish-now',
+      },
+    },
+  ],
+};
+
+describe('quarter readiness helpers', () => {
+  test('keeps blockers ordered and updates headline while blockers remain', () => {
+    const state = buildQuarterUiState({ report: reportFixture, resolvedIssueIds: ['txn-9104'] });
+
+    expect(state.itemsLeft).toBe(2);
+    expect(state.exportEnabled).toBe(false);
+    expect(state.report.readiness_pct).toBeGreaterThan(78);
+    expect(state.headline).toContain('items left');
+    expect(state.activeIssues.map((issue) => issue.affected_entity_id)).toEqual(['txn-9101', 'txn-9103']);
+    expect(state.report.explanation_lines[0]).toContain('Readiness is');
+  });
+
+  test('unlocks export immediately when every blocker is resolved', () => {
+    const state = buildQuarterUiState({
+      report: reportFixture,
+      resolvedIssueIds: ['txn-9101', 'txn-9103', 'txn-9104'],
+    });
+
+    expect(state.itemsLeft).toBe(0);
+    expect(state.exportEnabled).toBe(true);
+    expect(state.report.readiness_pct).toBe(100);
+    expect(state.report.can_export).toBe(true);
+    expect(state.report.issue_summary).toEqual([]);
+  });
+
+  test('supports demo query params that resolve all blockers', () => {
+    const params = new URLSearchParams('resolvedDemoIssues=all');
+    const resolvedIds = deriveResolvedIssueIdsFromSearchParams(params, reportFixture.issue_list);
+
+    expect(resolvedIds).toEqual(['txn-9101', 'txn-9103', 'txn-9104']);
+  });
+});
diff --git a/bizPA/start_bizpa_tax_readiness_ui.ps1 b/bizPA/start_bizpa_tax_readiness_ui.ps1
index 1fc4bef8cd172c31fdc3db52d1acc9d4775c5485..7216e44eac386fe436a9af8e9b417cd2d61e37fe
--- a/bizPA/start_bizpa_tax_readiness_ui.ps1
+++ b/bizPA/start_bizpa_tax_readiness_ui.ps1
@@ -4,6 +4,7 @@
 $frontendPath = Join-Path $repoRoot 'frontend'
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
 
 Write-Host 'Starting bizPA tax readiness UI...' -ForegroundColor Cyan
 
@@ -14,7 +15,10 @@
 Write-Host 'Tax readiness dashboard URL:' -ForegroundColor Green
 Write-Host "  $readinessUrl" -ForegroundColor Yellow
 Write-Host ''
+Write-Host 'Export-ready verification URL:' -ForegroundColor Green
+Write-Host "  $exportReadyUrl" -ForegroundColor Yellow
+Write-Host ''
 Write-Host 'Base frontend URL:' -ForegroundColor Green
 Write-Host "  $frontendUrl" -ForegroundColor Yellow
 Write-Host ''
-Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI can be reviewed without backend setup.' -ForegroundColor Cyan
+Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI and zero-blocker export-ready state can be reviewed without backend setup.' -ForegroundColor Cyan
diff --git a/bizPA/tax_readiness_ui_smoke.ps1 b/bizPA/tax_readiness_ui_smoke.ps1
index 56d67892a51b22572503dec560d17123b370c2b7..47a50c8d3ac8ff8c933943a7c6a966cdce89328f
--- a/bizPA/tax_readiness_ui_smoke.ps1
+++ b/bizPA/tax_readiness_ui_smoke.ps1
@@ -28,99 +28,92 @@
 
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
-$screenshotPath = 'C:\Users\edebe\eds\workstream\verification\20260311_201500_bizpa_tax_readiness_ui.png'
-$chromeProfilePath = 'C:\Users\edebe\eds\workstream\verification\chrome_capture_profile_tax_readiness'
-
-New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
-
-$frontendJob = Start-Job -ScriptBlock {
-  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
-  $env:PORT = '3002'
-  npm.cmd start
-}
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
+$verificationRoot = 'C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification'
+$quarterScreenshotPath = Join-Path $verificationRoot 'quarter_readiness_screen.png'
+$finishNowScreenshotPath = Join-Path $verificationRoot 'finish_now_queue_zero_blockers.png'
+function Wait-FrontendReady {
+  param (
+    [string]$Url
+  )
 
-try {
-  $frontendResponse = $null
-  $frontendReady = $false
   for ($i = 0; $i -lt 45; $i++) {
     Start-Sleep -Seconds 2
     try {
-      $frontendResponse = Invoke-WebRequest -UseBasicParsing $readinessUrl
-      if ($frontendResponse.StatusCode -eq 200) {
-        $frontendReady = $true
-        break
+      $response = Invoke-WebRequest -UseBasicParsing $Url
+      if ($response.StatusCode -eq 200) {
+        return $response
       }
     } catch {
     }
   }
 
-  if (-not $frontendReady) {
-    throw "Frontend did not become ready on $readinessUrl"
-  }
+  throw "Frontend did not become ready on $Url"
+}
 
-  $chromeArgs = @(
-    "--user-data-dir=$chromeProfilePath",
-    '--new-window',
-    '--window-size=1600,1500',
-    '--no-first-run',
-    '--disable-session-crashed-bubble',
-    '--disable-infobars',
-    '--disable-breakpad',
-    '--disable-crash-reporter',
-    '--noerrdialogs',
-    "--app=$readinessUrl"
+function Capture-AppScreenshot {
+  param (
+    [string]$Url,
+    [string]$OutputPath
   )
-  Start-Process 'C:\Program Files\Google\Chrome\Application\chrome.exe' -ArgumentList $chromeArgs | Out-Null
 
-  $chromeWindow = $null
-  for ($i = 0; $i -lt 20; $i++) {
-    Start-Sleep -Seconds 1
-    $chromeWindow = Get-Process chrome -ErrorAction SilentlyContinue |
-      ForEach-Object {
-        try {
-          if ($_.MainWindowHandle -and ([int64]$_.MainWindowHandle -ne 0)) {
-            [pscustomobject]@{
-              Process = $_
-              StartTime = $_.StartTime
-            }
-          }
-        } catch {
-        }
-      } |
-      Sort-Object StartTime -Descending |
-      Select-Object -First 1
-    if ($chromeWindow) {
-      $chromeWindow = $chromeWindow.Process
+  $form = New-Object System.Windows.Forms.Form
+  $form.Width = 1600
+  $form.Height = 1500
+  $form.StartPosition = 'Manual'
+  $form.Location = New-Object System.Drawing.Point(-32000, -32000)
+
+  $browser = New-Object System.Windows.Forms.WebBrowser
+  $browser.ScriptErrorsSuppressed = $true
+  $browser.ScrollBarsEnabled = $false
+  $browser.Dock = [System.Windows.Forms.DockStyle]::Fill
+  $form.Controls.Add($browser)
+  $form.Show()
+  $browser.Navigate($Url)
+
+  $ready = $false
+  for ($i = 0; $i -lt 90; $i++) {
+    [System.Windows.Forms.Application]::DoEvents()
+    Start-Sleep -Milliseconds 250
+    if ($browser.ReadyState -eq [System.Windows.Forms.WebBrowserReadyState]::Complete) {
+      $ready = $true
       break
     }
   }
 
-  if (-not $chromeWindow) {
-    throw 'Chrome readiness window did not expose a main window handle for screenshot capture.'
+  if (-not $ready) {
+    $form.Close()
+    throw "Embedded browser did not finish loading $Url"
   }
 
-  [WindowCaptureNative]::ShowWindow($chromeWindow.MainWindowHandle, 3) | Out-Null
-  [WindowCaptureNative]::SetForegroundWindow($chromeWindow.MainWindowHandle) | Out-Null
-  Start-Sleep -Seconds 1
+  Start-Sleep -Seconds 3
+  [System.Windows.Forms.Application]::DoEvents()
 
-  $rect = New-Object WindowCaptureNative+RECT
-  [WindowCaptureNative]::GetWindowRect($chromeWindow.MainWindowHandle, [ref]$rect) | Out-Null
-  $width = [Math]::Max(1, $rect.Right - $rect.Left)
-  $height = [Math]::Max(1, $rect.Bottom - $rect.Top)
-  $bitmap = New-Object System.Drawing.Bitmap $width, $height
-  $graphics = [System.Drawing.Graphics]::FromImage($bitmap)
-  $graphics.CopyFromScreen($rect.Left, $rect.Top, 0, 0, $bitmap.Size)
-  $bitmap.Save($screenshotPath, [System.Drawing.Imaging.ImageFormat]::Png)
-  $graphics.Dispose()
+  $bitmap = New-Object System.Drawing.Bitmap $browser.Width, $browser.Height
+  $browser.DrawToBitmap($bitmap, (New-Object System.Drawing.Rectangle 0, 0, $browser.Width, $browser.Height))
+  $bitmap.Save($OutputPath, [System.Drawing.Imaging.ImageFormat]::Png)
   $bitmap.Dispose()
+  $form.Close()
+}
+
+New-Item -ItemType Directory -Force -Path $verificationRoot | Out-Null
+
+$frontendJob = Start-Job -ScriptBlock {
+  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
+  $env:PORT = '3002'
+  npm.cmd start
+}
 
-  Get-Process chrome -ErrorAction SilentlyContinue |
-    Where-Object { $_.StartTime -ge $chromeWindow.StartTime.AddSeconds(-2) } |
-    Stop-Process -Force -ErrorAction SilentlyContinue
+try {
+  $frontendResponse = Wait-FrontendReady -Url $readinessUrl
+  Capture-AppScreenshot -Url $readinessUrl -OutputPath $quarterScreenshotPath
+  Capture-AppScreenshot -Url $exportReadyUrl -OutputPath $finishNowScreenshotPath
 
   "READINESS_URL=$readinessUrl"
+  "EXPORT_READY_URL=$exportReadyUrl"
   "FRONTEND_STATUS=$($frontendResponse.StatusCode)"
-  "SCREENSHOT=$screenshotPath"
+  "SCREENSHOT_QUARTER=$quarterScreenshotPath"
+  "SCREENSHOT_EXPORT_READY=$finishNowScreenshotPath"
   'FRONTEND_LOG_START'
   Receive-Job $frontendJob -Keep -ErrorAction SilentlyContinue 2>&1
   'FRONTEND_LOG_END'
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Date -Format 'yyyy-MM-dd HH:mm:ss'" in C:\Users\edebe\eds succeeded in 1.35s:
2026-03-18 18:20:27
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..b0b6a961dcb80123d7634a661b59a1e944c1c7a1
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,26 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    fallbackIssues: inboxItems,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1532,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1547,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1633,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2104,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2316,29 +2562,241 @@
     </div>
   );
 
-  const renderInbox = () => (
-    <div>
-      <h4>Finish Now Queue</h4>
-      <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
-      <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
-            <div>
-              <b>{item.merchant || 'Unknown'}</b>
-              <br />
-              <small>
-                {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
-              </small>
-              <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
-            </div>
+  const renderInbox = () => {
+    return (
+    <div className="inbox-mobile-shell">
+      <section className="inbox-mobile-hero">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2">
+          <div className="inbox-kicker">
+            <Bell size={14} />
+            Mobile-first exception queue
           </div>
-        ))}
-      </div>
-    </div>
-  );
+          <div className="status-pill">{filteredInboxCards.length} visible</div>
+        </div>
+        <h4>Finish now</h4>
+        <p>
+          Clear the blockers that stop the quarterly export: category, business or personal, split treatment, and duplicate resolution.
+        </p>
+        <div className="inbox-mobile-stats">
+          <div className="inbox-mobile-stat">
+            <small>Queue</small>
+            <strong>{inboxCards.length}</strong>
+            <small>{activeQuarterReport.quarter_reference || READINESS_DEMO_REPORT.quarter_reference}</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Critical</small>
+            <strong>{highSeverityCount}</strong>
+            <small>High-severity blockers</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Ready now</small>
+            <strong>{readyToExportCount}</strong>
+            <small>Transactions already export-safe</small>
+          </div>
+        </div>
+        <div className="hero-actions">
+          <button className="hero-action-main" onClick={() => setCurrentTab('quarter')}>
+            <ArrowRight size={16} />
+            Review quarter readiness
+          </button>
+          <button className="hero-action-secondary" onClick={undoLastInboxAction} disabled={inboxBusyId === 'undo-last'}>
+            {inboxBusyId === 'undo-last' ? <RefreshCcw size={16} className="spinner" /> : <ArchiveRestore size={16} />}
+            Undo last action
+          </button>
+        </div>
+      </section>
+
+      {inboxMessage && (
+        <div className={`readiness-panel inbox-message ${inboxMessage.type}`}>
+          {inboxMessage.text}
+        </div>
+      )}
+
+      <section className="readiness-panel">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-3">
+          <div>
+            <h5 style={{ marginBottom: 4 }}>Queue filters</h5>
+            <small className="small-muted">Keep the mobile pass focused on one blocker type at a time.</small>
+          </div>
+          <div className="status-pill">{activeQuarterReport.readiness_pct ?? READINESS_DEMO_REPORT.readiness_pct}% ready</div>
+        </div>
+        <div className="inbox-filter-row">
+          {INBOX_FILTERS.map((filter) => {
+            const count = filter.key === 'all'
+              ? inboxCards.length
+              : inboxCards.filter((card) => card.issue_types.includes(filter.key)).length;
+            return (
+              <button
+                key={filter.key}
+                type="button"
+                className={`inbox-filter-chip ${inboxFilter === filter.key ? 'active' : ''}`}
+                onClick={() => setInboxFilter(filter.key)}
+              >
+                <span>{filter.label}</span>
+                <small>{count}</small>
+              </button>
+            );
+          })}
+        </div>
+      </section>
+
+      {filteredInboxCards.length === 0 ? (
+        <div className="inbox-empty">
+          No blockers match this filter. Reset to All blockers or return to the quarter screen to confirm readiness.
+        </div>
+      ) : (
+        <div className="inbox-card-list">
+          {filteredInboxCards.map((card) => {
+            const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+            const saving = inboxBusyId === card.id;
+            const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+            return (
+              <section key={card.id} className="inbox-action-card">
+                <div className="inbox-card-top">
+                  <div>
+                    <strong>{card.merchant}</strong>
+                    <div className="inbox-card-subline">
+                      {card.txn_date ? String(card.txn_date).slice(0, 10) : 'Date pending'} · {formatIssueAmount(card.amount, card.direction)}
+                    </div>
+                  </div>
+                  <span className={`inbox-issue-chip ${card.severity}`}>
+                    <TriangleAlert size={14} />
+                    {INBOX_REASON_LABELS[card.blocker_reason] || card.blocker_reason.replace(/_/g, ' ')}
+                  </span>
+                </div>
+
+                <div className="inbox-chip-row">
+                  {card.issue_labels.map((label) => (
+                    <span key={`${card.id}-${label}`} className={`inbox-issue-chip ${card.severity}`}>
+                      {label}
+                    </span>
+                  ))}
+                  {card.suggested_category?.category_name && (
+                    <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
+                      <CheckCircle2 size={14} />
+                      Suggested: {card.suggested_category.category_name}
+                    </button>
+                  )}
+                </div>
+
+                <p className="inbox-explainer">{card.explanation || 'Resolve the blocking fields to clear this transaction from the queue.'}</p>
+
+                {(card.supports_category || card.supports_business_personal || card.supports_split) && (
+                  <div className="inbox-action-grid">
+                    <div className="inbox-field">
+                      <label>Category code</label>
+                      <input
+                        value={draft.category_code}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_code', event.target.value)}
+                        placeholder="MATERIALS"
+                      />
+                    </div>
+                    <div className="inbox-field">
+                      <label>Category label</label>
+                      <input
+                        value={draft.category_name}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_name', event.target.value)}
+                        placeholder="Materials"
+                      />
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_business_personal || card.supports_split) && (
+                  <div>
+                    <div className="inbox-field">
+                      <label>Business treatment</label>
+                    </div>
+                    <div className="inbox-toggle-row">
+                      {[
+                        { value: 'BUSINESS', label: 'Business' },
+                        { value: 'PERSONAL', label: 'Personal' },
+                        { value: 'SPLIT', label: 'Split' },
+                      ].map((option) => (
+                        <button
+                          key={option.value}
+                          type="button"
+                          className={`inbox-toggle ${draft.business_personal === option.value ? 'active' : ''}`}
+                          onClick={() => setInboxDraftField(card.id, 'business_personal', option.value)}
+                        >
+                          {option.label}
+                        </button>
+                      ))}
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_split || isSplit) && (
+                  <div className="inbox-field">
+                    <label>Business split %</label>
+                    <input
+                      type="number"
+                      min="0"
+                      max="100"
+                      value={draft.split_business_pct}
+                      onChange={(event) => setInboxDraftField(card.id, 'split_business_pct', event.target.value)}
+                      placeholder="80"
+                    />
+                  </div>
+                )}
+
+                {card.supports_duplicate && (
+                  <>
+                    <div>
+                      <div className="inbox-field">
+                        <label>Duplicate action</label>
+                      </div>
+                      <div className="inbox-duplicate-row">
+                        {[
+                          { value: 'dismiss', label: 'Dismiss duplicate' },
+                          { value: 'merge', label: 'Merge to original' },
+                        ].map((option) => (
+                          <button
+                            key={option.value}
+                            type="button"
+                            className={`inbox-toggle ${draft.duplicate_action === option.value ? 'active' : ''}`}
+                            onClick={() => setInboxDraftField(card.id, 'duplicate_action', option.value)}
+                          >
+                            {option.label}
+                          </button>
+                        ))}
+                      </div>
+                    </div>
+                    {draft.duplicate_action === 'merge' && (
+                      <div className="inbox-field">
+                        <label>Original transaction id</label>
+                        <input
+                          value={draft.duplicate_of_txn_id}
+                          onChange={(event) => setInboxDraftField(card.id, 'duplicate_of_txn_id', event.target.value)}
+                          placeholder="txn-9001"
+                        />
+                      </div>
+                    )}
+                  </>
+                )}
 
+                <div className="inbox-footer-row">
+                  <div className="inbox-inline-status">
+                    Route: {card.resolution_target?.route || `/api/v1/inbox/${card.id}`}
+                  </div>
+                  <div className="inbox-footer-actions">
+                    <button className="secondary-action" onClick={clearInboxMessage} disabled={saving}>
+                      Clear banner
+                    </button>
+                    <button className="primary-action" onClick={() => submitInboxAction(card)} disabled={saving}>
+                      {saving ? <RefreshCcw size={16} className="spinner" /> : <CheckCircle2 size={16} />}
+                      Save and clear blocker
+                    </button>
+                  </div>
+                </div>
+              </section>
+            );
+          })}
+        </div>
+      )}
+    </div>
+    );
+  };
   const renderControlCentre = () => {
     const policyLabels = {
       vat_scheme: 'VAT scheme',
@@ -2510,11 +2968,11 @@
           <section className="readiness-panel">
             <h5 style={{ marginBottom: 12 }}>Queue Backlog</h5>
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
-                <div key={item.id} className="control-row">
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
+                <div key={item.id || item.affected_entity_id} className="control-row">
                   <div>
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
                   <div className="text-end">
                     <div className="small-muted">{item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date}</div>
@@ -2522,7 +2980,7 @@
                   </div>
                 </div>
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
               )}
             </div>
@@ -2668,7 +3126,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2697,7 +3155,7 @@
   };
 
   const createSnapshotVersion = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     const status = snapshotWorkspace?.snapshot_status;
 
     if (!status?.can_create_snapshot_version) {
@@ -2719,7 +3177,7 @@
         description: `Quarter snapshot generated from Quarter workspace`,
         metadata: {
           source_type: 'manual',
-          readiness_pct: quarterReadiness?.readiness_pct ?? null
+          readiness_pct: activeQuarterReport?.readiness_pct ?? null
         }
       });
       await refreshQuarterWorkspace();
@@ -2734,7 +3192,7 @@
   };
 
   const closeQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(closeReason || '').trim()) {
       setQuarterActionError('Close reason is required.');
       setQuarterActionMessage('');
@@ -2785,7 +3243,7 @@
   };
 
   const reopenQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(reopenReason || '').trim()) {
       setQuarterActionError('Reopen reason is required.');
       setQuarterActionMessage('');
@@ -2844,9 +3302,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3342,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3354,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3365,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3379,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..669173cd6176ee9ee5c86c5895ea49cb9524ebc4
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,155 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, fallbackIssues = [], resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const issueSource = Array.isArray(report.issue_list) && report.issue_list.length > 0
+    ? report.issue_list
+    : fallbackIssues;
+  const allIssues = sortIssues(Array.isArray(issueSource) ? issueSource : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/bizPA/frontend/src/quarterReadiness.test.js b/bizPA/frontend/src/quarterReadiness.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..0b810191a89d8b82091bf5de1408afcc23c14e65
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.test.js
@@ -0,0 +1,93 @@
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
+
+const reportFixture = {
+  quarter_reference: 'Q1-2026',
+  period_start: '2026-01-01',
+  period_end: '2026-03-31',
+  total_txns_in_period: 18,
+  readiness_pct: 78,
+  can_export: false,
+  issue_list: [
+    {
+      issue_type: 'missing_category',
+      affected_entity_id: 'txn-9101',
+      severity: 'high',
+      weight: 35,
+      explanation: 'Category missing for Fuel Stop.',
+      txn_date: '2026-02-04',
+      merchant: 'Fuel Stop',
+      amount: 64.18,
+      direction: 'out',
+      resolution_target: {
+        label: 'Classify transaction',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'missing_business_personal',
+      affected_entity_id: 'txn-9103',
+      severity: 'high',
+      weight: 30,
+      explanation: 'Business/personal decision missing.',
+      txn_date: '2026-02-15',
+      merchant: 'Mobile Contract',
+      amount: 42.5,
+      direction: 'out',
+      resolution_target: {
+        label: 'Set business or personal',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'unresolved_duplicate',
+      affected_entity_id: 'txn-9104',
+      severity: 'medium',
+      weight: 15,
+      explanation: 'Duplicate not resolved.',
+      txn_date: '2026-03-02',
+      merchant: 'Client Payment',
+      amount: 820,
+      direction: 'in',
+      resolution_target: {
+        label: 'Resolve duplicate',
+        workflow: '/inbox/finish-now',
+      },
+    },
+  ],
+};
+
+describe('quarter readiness helpers', () => {
+  test('keeps blockers ordered and updates headline while blockers remain', () => {
+    const state = buildQuarterUiState({ report: reportFixture, resolvedIssueIds: ['txn-9104'] });
+
+    expect(state.itemsLeft).toBe(2);
+    expect(state.exportEnabled).toBe(false);
+    expect(state.report.readiness_pct).toBeGreaterThan(78);
+    expect(state.headline).toContain('items left');
+    expect(state.activeIssues.map((issue) => issue.affected_entity_id)).toEqual(['txn-9101', 'txn-9103']);
+    expect(state.report.explanation_lines[0]).toContain('Readiness is');
+  });
+
+  test('unlocks export immediately when every blocker is resolved', () => {
+    const state = buildQuarterUiState({
+      report: reportFixture,
+      resolvedIssueIds: ['txn-9101', 'txn-9103', 'txn-9104'],
+    });
+
+    expect(state.itemsLeft).toBe(0);
+    expect(state.exportEnabled).toBe(true);
+    expect(state.report.readiness_pct).toBe(100);
+    expect(state.report.can_export).toBe(true);
+    expect(state.report.issue_summary).toEqual([]);
+  });
+
+  test('supports demo query params that resolve all blockers', () => {
+    const params = new URLSearchParams('resolvedDemoIssues=all');
+    const resolvedIds = deriveResolvedIssueIdsFromSearchParams(params, reportFixture.issue_list);
+
+    expect(resolvedIds).toEqual(['txn-9101', 'txn-9103', 'txn-9104']);
+  });
+});
diff --git a/bizPA/start_bizpa_tax_readiness_ui.ps1 b/bizPA/start_bizpa_tax_readiness_ui.ps1
index 1fc4bef8cd172c31fdc3db52d1acc9d4775c5485..7216e44eac386fe436a9af8e9b417cd2d61e37fe
--- a/bizPA/start_bizpa_tax_readiness_ui.ps1
+++ b/bizPA/start_bizpa_tax_readiness_ui.ps1
@@ -4,6 +4,7 @@
 $frontendPath = Join-Path $repoRoot 'frontend'
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
 
 Write-Host 'Starting bizPA tax readiness UI...' -ForegroundColor Cyan
 
@@ -14,7 +15,10 @@
 Write-Host 'Tax readiness dashboard URL:' -ForegroundColor Green
 Write-Host "  $readinessUrl" -ForegroundColor Yellow
 Write-Host ''
+Write-Host 'Export-ready verification URL:' -ForegroundColor Green
+Write-Host "  $exportReadyUrl" -ForegroundColor Yellow
+Write-Host ''
 Write-Host 'Base frontend URL:' -ForegroundColor Green
 Write-Host "  $frontendUrl" -ForegroundColor Yellow
 Write-Host ''
-Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI can be reviewed without backend setup.' -ForegroundColor Cyan
+Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI and zero-blocker export-ready state can be reviewed without backend setup.' -ForegroundColor Cyan
diff --git a/bizPA/tax_readiness_ui_smoke.ps1 b/bizPA/tax_readiness_ui_smoke.ps1
index 56d67892a51b22572503dec560d17123b370c2b7..47a50c8d3ac8ff8c933943a7c6a966cdce89328f
--- a/bizPA/tax_readiness_ui_smoke.ps1
+++ b/bizPA/tax_readiness_ui_smoke.ps1
@@ -28,99 +28,92 @@
 
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
-$screenshotPath = 'C:\Users\edebe\eds\workstream\verification\20260311_201500_bizpa_tax_readiness_ui.png'
-$chromeProfilePath = 'C:\Users\edebe\eds\workstream\verification\chrome_capture_profile_tax_readiness'
-
-New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
-
-$frontendJob = Start-Job -ScriptBlock {
-  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
-  $env:PORT = '3002'
-  npm.cmd start
-}
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
+$verificationRoot = 'C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification'
+$quarterScreenshotPath = Join-Path $verificationRoot 'quarter_readiness_screen.png'
+$finishNowScreenshotPath = Join-Path $verificationRoot 'finish_now_queue_zero_blockers.png'
+function Wait-FrontendReady {
+  param (
+    [string]$Url
+  )
 
-try {
-  $frontendResponse = $null
-  $frontendReady = $false
   for ($i = 0; $i -lt 45; $i++) {
     Start-Sleep -Seconds 2
     try {
-      $frontendResponse = Invoke-WebRequest -UseBasicParsing $readinessUrl
-      if ($frontendResponse.StatusCode -eq 200) {
-        $frontendReady = $true
-        break
+      $response = Invoke-WebRequest -UseBasicParsing $Url
+      if ($response.StatusCode -eq 200) {
+        return $response
       }
     } catch {
     }
   }
 
-  if (-not $frontendReady) {
-    throw "Frontend did not become ready on $readinessUrl"
-  }
+  throw "Frontend did not become ready on $Url"
+}
 
-  $chromeArgs = @(
-    "--user-data-dir=$chromeProfilePath",
-    '--new-window',
-    '--window-size=1600,1500',
-    '--no-first-run',
-    '--disable-session-crashed-bubble',
-    '--disable-infobars',
-    '--disable-breakpad',
-    '--disable-crash-reporter',
-    '--noerrdialogs',
-    "--app=$readinessUrl"
+function Capture-AppScreenshot {
+  param (
+    [string]$Url,
+    [string]$OutputPath
   )
-  Start-Process 'C:\Program Files\Google\Chrome\Application\chrome.exe' -ArgumentList $chromeArgs | Out-Null
 
-  $chromeWindow = $null
-  for ($i = 0; $i -lt 20; $i++) {
-    Start-Sleep -Seconds 1
-    $chromeWindow = Get-Process chrome -ErrorAction SilentlyContinue |
-      ForEach-Object {
-        try {
-          if ($_.MainWindowHandle -and ([int64]$_.MainWindowHandle -ne 0)) {
-            [pscustomobject]@{
-              Process = $_
-              StartTime = $_.StartTime
-            }
-          }
-        } catch {
-        }
-      } |
-      Sort-Object StartTime -Descending |
-      Select-Object -First 1
-    if ($chromeWindow) {
-      $chromeWindow = $chromeWindow.Process
+  $form = New-Object System.Windows.Forms.Form
+  $form.Width = 1600
+  $form.Height = 1500
+  $form.StartPosition = 'Manual'
+  $form.Location = New-Object System.Drawing.Point(-32000, -32000)
+
+  $browser = New-Object System.Windows.Forms.WebBrowser
+  $browser.ScriptErrorsSuppressed = $true
+  $browser.ScrollBarsEnabled = $false
+  $browser.Dock = [System.Windows.Forms.DockStyle]::Fill
+  $form.Controls.Add($browser)
+  $form.Show()
+  $browser.Navigate($Url)
+
+  $ready = $false
+  for ($i = 0; $i -lt 90; $i++) {
+    [System.Windows.Forms.Application]::DoEvents()
+    Start-Sleep -Milliseconds 250
+    if ($browser.ReadyState -eq [System.Windows.Forms.WebBrowserReadyState]::Complete) {
+      $ready = $true
       break
     }
   }
 
-  if (-not $chromeWindow) {
-    throw 'Chrome readiness window did not expose a main window handle for screenshot capture.'
+  if (-not $ready) {
+    $form.Close()
+    throw "Embedded browser did not finish loading $Url"
   }
 
-  [WindowCaptureNative]::ShowWindow($chromeWindow.MainWindowHandle, 3) | Out-Null
-  [WindowCaptureNative]::SetForegroundWindow($chromeWindow.MainWindowHandle) | Out-Null
-  Start-Sleep -Seconds 1
+  Start-Sleep -Seconds 3
+  [System.Windows.Forms.Application]::DoEvents()
 
-  $rect = New-Object WindowCaptureNative+RECT
-  [WindowCaptureNative]::GetWindowRect($chromeWindow.MainWindowHandle, [ref]$rect) | Out-Null
-  $width = [Math]::Max(1, $rect.Right - $rect.Left)
-  $height = [Math]::Max(1, $rect.Bottom - $rect.Top)
-  $bitmap = New-Object System.Drawing.Bitmap $width, $height
-  $graphics = [System.Drawing.Graphics]::FromImage($bitmap)
-  $graphics.CopyFromScreen($rect.Left, $rect.Top, 0, 0, $bitmap.Size)
-  $bitmap.Save($screenshotPath, [System.Drawing.Imaging.ImageFormat]::Png)
-  $graphics.Dispose()
+  $bitmap = New-Object System.Drawing.Bitmap $browser.Width, $browser.Height
+  $browser.DrawToBitmap($bitmap, (New-Object System.Drawing.Rectangle 0, 0, $browser.Width, $browser.Height))
+  $bitmap.Save($OutputPath, [System.Drawing.Imaging.ImageFormat]::Png)
   $bitmap.Dispose()
+  $form.Close()
+}
+
+New-Item -ItemType Directory -Force -Path $verificationRoot | Out-Null
+
+$frontendJob = Start-Job -ScriptBlock {
+  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
+  $env:PORT = '3002'
+  npm.cmd start
+}
 
-  Get-Process chrome -ErrorAction SilentlyContinue |
-    Where-Object { $_.StartTime -ge $chromeWindow.StartTime.AddSeconds(-2) } |
-    Stop-Process -Force -ErrorAction SilentlyContinue
+try {
+  $frontendResponse = Wait-FrontendReady -Url $readinessUrl
+  Capture-AppScreenshot -Url $readinessUrl -OutputPath $quarterScreenshotPath
+  Capture-AppScreenshot -Url $exportReadyUrl -OutputPath $finishNowScreenshotPath
 
   "READINESS_URL=$readinessUrl"
+  "EXPORT_READY_URL=$exportReadyUrl"
   "FRONTEND_STATUS=$($frontendResponse.StatusCode)"
-  "SCREENSHOT=$screenshotPath"
+  "SCREENSHOT_QUARTER=$quarterScreenshotPath"
+  "SCREENSHOT_EXPORT_READY=$finishNowScreenshotPath"
   'FRONTEND_LOG_START'
   Receive-Job $frontendJob -Keep -ErrorAction SilentlyContinue 2>&1
   'FRONTEND_LOG_END'
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..680fcbec2915da4af8a7f82b82928f017175f591
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,122 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: pending
+- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: pending
+- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: pending
+
+## Evidence
+Objective-Delivery-Coverage: 0%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: planned
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: planned
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: planned
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+
+## Changes Made
+- Pending.
+
+## Validation
+- Pending.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+In progress - 2026-03-18 18:07
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

codex
The technical work and validations are complete. I’m updating the lifecycle file with the exact commands, outputs, screenshot artifacts, and an `Awaiting user verification` status before I hand you the review URLs.
file update
M C:\Users\edebe\eds\workstream\200_inprogress\codex\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -8,14 +8,14 @@
 ## Plan
-- [ ] 1. Normalize and confirm the local quarter-close launch path for review.
-  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
-  - Evidence: pending
-- [ ] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
-  - [ ] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
-  - Evidence: pending
-- [ ] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
-  - [ ] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
-  - Evidence: pending
+- [x] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [x] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: Script printed `http://127.0.0.1:3002/?readinessDemo=1&tab=quarter` and `http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all` on 2026-03-18.
+- [x] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [x] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: `PASS src/quarterReadiness.test.js` plus `PASS` on the existing onboarding, control-centre, and governance suites on 2026-03-18.
+- [x] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [x] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: Smoke script returned `FRONTEND_STATUS=200` and saved `quarter_readiness_screen.png` plus `finish_now_queue_zero_blockers.png` under the epic verification folder on 2026-03-18.
 
 ## Evidence
-Objective-Delivery-Coverage: 0%
+Objective-Delivery-Coverage: 90%
 Auto-Acceptance: false
@@ -24,3 +24,3 @@
   - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
-  - Status: planned
+  - Status: captured
 - Evidence-Type: screenshot
@@ -28,3 +28,3 @@
   - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
-  - Status: planned
+  - Status: captured
 - Evidence-Type: screenshot
@@ -32,3 +32,3 @@
   - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
-  - Status: planned
+  - Status: captured
 - Evidence-Type: test_output
@@ -36,3 +36,3 @@
   - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
-  - Status: planned
+  - Status: captured
 - Evidence-Type: log_output
@@ -40,2 +40,6 @@
   - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: captured
+- Evidence-Type: manual_verification
+  - Artifact: `Pending user review of the local URLs printed by start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: Final UI acceptance for the quarter-readiness headline, Finish Now queue behavior, and export gating is pending explicit user pass/fail feedback.
   - Status: planned
@@ -46,8 +50,32 @@
 - 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+- 2026-03-18 18:10: Added a dedicated quarter readiness helper module and automated tests for live blocker resolution, readiness recalculation, and query-param seeded demo resolution.
+- 2026-03-18 18:13: Updated `bizPA/frontend/src/App.jsx` to derive the quarter hero, inline Finish Now queue, and export gating from a single live quarter state model.
+- 2026-03-18 18:15: Updated the local quarter-readiness start script to print both the standard quarter review URL and the zero-blocker export-ready verification URL.
+- 2026-03-18 18:18: Reworked the smoke script to capture the required screenshots locally after browser-based capture failed under the sandboxed environment.
+- 2026-03-18 18:20: Completed automated validation, captured screenshots, and prepared the task for user verification.
 
 ## Changes Made
-- Pending.
+- Added `bizPA/frontend/src/quarterReadiness.js` with pure helpers for blocker ordering, readiness recalculation, issue-summary rebuilding, explanation copy, and demo query-param resolution.
+- Added `bizPA/frontend/src/quarterReadiness.test.js` covering partial blocker clearance, zero-blocker export enablement, and `resolvedDemoIssues=all`.
+- Updated `bizPA/frontend/src/App.jsx` so the quarter screen now:
+  - renders the hero headline as `<pct>% ready — <items> items left`,
+  - shows an inline ordered Finish Now queue,
+  - updates readiness/export state immediately in demo mode when blockers are cleared,
+  - keeps inbox/dashboard/control-centre blocker counts aligned to the live quarter state.
+- Updated `bizPA/start_bizpa_tax_readiness_ui.ps1` to print both the normal quarter-close URL and an export-ready verification URL.
+- Updated `bizPA/tax_readiness_ui_smoke.ps1` to save screenshots directly to the epic verification folder for the blocked and zero-blocker states.
 
 ## Validation
-- Pending.
+- `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Pass. Output included:
+    - `Tax readiness dashboard URL: http://127.0.0.1:3002/?readinessDemo=1&tab=quarter`
+    - `Export-ready verification URL: http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all`
+- `npm.cmd test -- --runInBand --watchAll=false` in `C:\Users\edebe\eds\bizPA\frontend`
+  - Pass. `4` suites passed, `14` tests passed.
+- `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Pass. Output included:
+    - `FRONTEND_STATUS=200`
+    - `SCREENSHOT_QUARTER=C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+    - `SCREENSHOT_EXPORT_READY=C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+- User verification request: To be issued with the completion response for explicit pass/fail on the quarter headline, Finish Now queue, and export gating behavior.
 
@@ -55,2 +83,3 @@
 - This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The immediate blocker-clear behavior is implemented in the demo/local verification path so the export gating can be proven end-to-end without backend mutation setup.
 - The existing task brief and prior retry history are preserved below for reference.
@@ -58,3 +87,3 @@
 ## Completion Status
-In progress - 2026-03-18 18:07
+Awaiting user verification - 2026-03-18 18:20
 
apply_patch(auto_approved=true) exited 0 in 860ms:
Success. Updated the following files:
M C:\Users\edebe\eds\workstream\200_inprogress\codex\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..b0b6a961dcb80123d7634a661b59a1e944c1c7a1
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,26 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    fallbackIssues: inboxItems,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1532,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1547,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1633,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2104,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2316,29 +2562,241 @@
     </div>
   );
 
-  const renderInbox = () => (
-    <div>
-      <h4>Finish Now Queue</h4>
-      <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
-      <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
-            <div>
-              <b>{item.merchant || 'Unknown'}</b>
-              <br />
-              <small>
-                {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
-              </small>
-              <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
-            </div>
+  const renderInbox = () => {
+    return (
+    <div className="inbox-mobile-shell">
+      <section className="inbox-mobile-hero">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2">
+          <div className="inbox-kicker">
+            <Bell size={14} />
+            Mobile-first exception queue
           </div>
-        ))}
-      </div>
-    </div>
-  );
+          <div className="status-pill">{filteredInboxCards.length} visible</div>
+        </div>
+        <h4>Finish now</h4>
+        <p>
+          Clear the blockers that stop the quarterly export: category, business or personal, split treatment, and duplicate resolution.
+        </p>
+        <div className="inbox-mobile-stats">
+          <div className="inbox-mobile-stat">
+            <small>Queue</small>
+            <strong>{inboxCards.length}</strong>
+            <small>{activeQuarterReport.quarter_reference || READINESS_DEMO_REPORT.quarter_reference}</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Critical</small>
+            <strong>{highSeverityCount}</strong>
+            <small>High-severity blockers</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Ready now</small>
+            <strong>{readyToExportCount}</strong>
+            <small>Transactions already export-safe</small>
+          </div>
+        </div>
+        <div className="hero-actions">
+          <button className="hero-action-main" onClick={() => setCurrentTab('quarter')}>
+            <ArrowRight size={16} />
+            Review quarter readiness
+          </button>
+          <button className="hero-action-secondary" onClick={undoLastInboxAction} disabled={inboxBusyId === 'undo-last'}>
+            {inboxBusyId === 'undo-last' ? <RefreshCcw size={16} className="spinner" /> : <ArchiveRestore size={16} />}
+            Undo last action
+          </button>
+        </div>
+      </section>
+
+      {inboxMessage && (
+        <div className={`readiness-panel inbox-message ${inboxMessage.type}`}>
+          {inboxMessage.text}
+        </div>
+      )}
+
+      <section className="readiness-panel">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-3">
+          <div>
+            <h5 style={{ marginBottom: 4 }}>Queue filters</h5>
+            <small className="small-muted">Keep the mobile pass focused on one blocker type at a time.</small>
+          </div>
+          <div className="status-pill">{activeQuarterReport.readiness_pct ?? READINESS_DEMO_REPORT.readiness_pct}% ready</div>
+        </div>
+        <div className="inbox-filter-row">
+          {INBOX_FILTERS.map((filter) => {
+            const count = filter.key === 'all'
+              ? inboxCards.length
+              : inboxCards.filter((card) => card.issue_types.includes(filter.key)).length;
+            return (
+              <button
+                key={filter.key}
+                type="button"
+                className={`inbox-filter-chip ${inboxFilter === filter.key ? 'active' : ''}`}
+                onClick={() => setInboxFilter(filter.key)}
+              >
+                <span>{filter.label}</span>
+                <small>{count}</small>
+              </button>
+            );
+          })}
+        </div>
+      </section>
+
+      {filteredInboxCards.length === 0 ? (
+        <div className="inbox-empty">
+          No blockers match this filter. Reset to All blockers or return to the quarter screen to confirm readiness.
+        </div>
+      ) : (
+        <div className="inbox-card-list">
+          {filteredInboxCards.map((card) => {
+            const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+            const saving = inboxBusyId === card.id;
+            const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+            return (
+              <section key={card.id} className="inbox-action-card">
+                <div className="inbox-card-top">
+                  <div>
+                    <strong>{card.merchant}</strong>
+                    <div className="inbox-card-subline">
+                      {card.txn_date ? String(card.txn_date).slice(0, 10) : 'Date pending'} · {formatIssueAmount(card.amount, card.direction)}
+                    </div>
+                  </div>
+                  <span className={`inbox-issue-chip ${card.severity}`}>
+                    <TriangleAlert size={14} />
+                    {INBOX_REASON_LABELS[card.blocker_reason] || card.blocker_reason.replace(/_/g, ' ')}
+                  </span>
+                </div>
+
+                <div className="inbox-chip-row">
+                  {card.issue_labels.map((label) => (
+                    <span key={`${card.id}-${label}`} className={`inbox-issue-chip ${card.severity}`}>
+                      {label}
+                    </span>
+                  ))}
+                  {card.suggested_category?.category_name && (
+                    <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
+                      <CheckCircle2 size={14} />
+                      Suggested: {card.suggested_category.category_name}
+                    </button>
+                  )}
+                </div>
+
+                <p className="inbox-explainer">{card.explanation || 'Resolve the blocking fields to clear this transaction from the queue.'}</p>
+
+                {(card.supports_category || card.supports_business_personal || card.supports_split) && (
+                  <div className="inbox-action-grid">
+                    <div className="inbox-field">
+                      <label>Category code</label>
+                      <input
+                        value={draft.category_code}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_code', event.target.value)}
+                        placeholder="MATERIALS"
+                      />
+                    </div>
+                    <div className="inbox-field">
+                      <label>Category label</label>
+                      <input
+                        value={draft.category_name}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_name', event.target.value)}
+                        placeholder="Materials"
+                      />
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_business_personal || card.supports_split) && (
+                  <div>
+                    <div className="inbox-field">
+                      <label>Business treatment</label>
+                    </div>
+                    <div className="inbox-toggle-row">
+                      {[
+                        { value: 'BUSINESS', label: 'Business' },
+                        { value: 'PERSONAL', label: 'Personal' },
+                        { value: 'SPLIT', label: 'Split' },
+                      ].map((option) => (
+                        <button
+                          key={option.value}
+                          type="button"
+                          className={`inbox-toggle ${draft.business_personal === option.value ? 'active' : ''}`}
+                          onClick={() => setInboxDraftField(card.id, 'business_personal', option.value)}
+                        >
+                          {option.label}
+                        </button>
+                      ))}
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_split || isSplit) && (
+                  <div className="inbox-field">
+                    <label>Business split %</label>
+                    <input
+                      type="number"
+                      min="0"
+                      max="100"
+                      value={draft.split_business_pct}
+                      onChange={(event) => setInboxDraftField(card.id, 'split_business_pct', event.target.value)}
+                      placeholder="80"
+                    />
+                  </div>
+                )}
+
+                {card.supports_duplicate && (
+                  <>
+                    <div>
+                      <div className="inbox-field">
+                        <label>Duplicate action</label>
+                      </div>
+                      <div className="inbox-duplicate-row">
+                        {[
+                          { value: 'dismiss', label: 'Dismiss duplicate' },
+                          { value: 'merge', label: 'Merge to original' },
+                        ].map((option) => (
+                          <button
+                            key={option.value}
+                            type="button"
+                            className={`inbox-toggle ${draft.duplicate_action === option.value ? 'active' : ''}`}
+                            onClick={() => setInboxDraftField(card.id, 'duplicate_action', option.value)}
+                          >
+                            {option.label}
+                          </button>
+                        ))}
+                      </div>
+                    </div>
+                    {draft.duplicate_action === 'merge' && (
+                      <div className="inbox-field">
+                        <label>Original transaction id</label>
+                        <input
+                          value={draft.duplicate_of_txn_id}
+                          onChange={(event) => setInboxDraftField(card.id, 'duplicate_of_txn_id', event.target.value)}
+                          placeholder="txn-9001"
+                        />
+                      </div>
+                    )}
+                  </>
+                )}
 
+                <div className="inbox-footer-row">
+                  <div className="inbox-inline-status">
+                    Route: {card.resolution_target?.route || `/api/v1/inbox/${card.id}`}
+                  </div>
+                  <div className="inbox-footer-actions">
+                    <button className="secondary-action" onClick={clearInboxMessage} disabled={saving}>
+                      Clear banner
+                    </button>
+                    <button className="primary-action" onClick={() => submitInboxAction(card)} disabled={saving}>
+                      {saving ? <RefreshCcw size={16} className="spinner" /> : <CheckCircle2 size={16} />}
+                      Save and clear blocker
+                    </button>
+                  </div>
+                </div>
+              </section>
+            );
+          })}
+        </div>
+      )}
+    </div>
+    );
+  };
   const renderControlCentre = () => {
     const policyLabels = {
       vat_scheme: 'VAT scheme',
@@ -2510,11 +2968,11 @@
           <section className="readiness-panel">
             <h5 style={{ marginBottom: 12 }}>Queue Backlog</h5>
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
-                <div key={item.id} className="control-row">
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
+                <div key={item.id || item.affected_entity_id} className="control-row">
                   <div>
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
                   <div className="text-end">
                     <div className="small-muted">{item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date}</div>
@@ -2522,7 +2980,7 @@
                   </div>
                 </div>
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
               )}
             </div>
@@ -2668,7 +3126,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2697,7 +3155,7 @@
   };
 
   const createSnapshotVersion = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     const status = snapshotWorkspace?.snapshot_status;
 
     if (!status?.can_create_snapshot_version) {
@@ -2719,7 +3177,7 @@
         description: `Quarter snapshot generated from Quarter workspace`,
         metadata: {
           source_type: 'manual',
-          readiness_pct: quarterReadiness?.readiness_pct ?? null
+          readiness_pct: activeQuarterReport?.readiness_pct ?? null
         }
       });
       await refreshQuarterWorkspace();
@@ -2734,7 +3192,7 @@
   };
 
   const closeQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(closeReason || '').trim()) {
       setQuarterActionError('Close reason is required.');
       setQuarterActionMessage('');
@@ -2785,7 +3243,7 @@
   };
 
   const reopenQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(reopenReason || '').trim()) {
       setQuarterActionError('Reopen reason is required.');
       setQuarterActionMessage('');
@@ -2844,9 +3302,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3342,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3354,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3365,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3379,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..669173cd6176ee9ee5c86c5895ea49cb9524ebc4
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,155 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, fallbackIssues = [], resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const issueSource = Array.isArray(report.issue_list) && report.issue_list.length > 0
+    ? report.issue_list
+    : fallbackIssues;
+  const allIssues = sortIssues(Array.isArray(issueSource) ? issueSource : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/bizPA/frontend/src/quarterReadiness.test.js b/bizPA/frontend/src/quarterReadiness.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..0b810191a89d8b82091bf5de1408afcc23c14e65
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.test.js
@@ -0,0 +1,93 @@
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
+
+const reportFixture = {
+  quarter_reference: 'Q1-2026',
+  period_start: '2026-01-01',
+  period_end: '2026-03-31',
+  total_txns_in_period: 18,
+  readiness_pct: 78,
+  can_export: false,
+  issue_list: [
+    {
+      issue_type: 'missing_category',
+      affected_entity_id: 'txn-9101',
+      severity: 'high',
+      weight: 35,
+      explanation: 'Category missing for Fuel Stop.',
+      txn_date: '2026-02-04',
+      merchant: 'Fuel Stop',
+      amount: 64.18,
+      direction: 'out',
+      resolution_target: {
+        label: 'Classify transaction',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'missing_business_personal',
+      affected_entity_id: 'txn-9103',
+      severity: 'high',
+      weight: 30,
+      explanation: 'Business/personal decision missing.',
+      txn_date: '2026-02-15',
+      merchant: 'Mobile Contract',
+      amount: 42.5,
+      direction: 'out',
+      resolution_target: {
+        label: 'Set business or personal',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'unresolved_duplicate',
+      affected_entity_id: 'txn-9104',
+      severity: 'medium',
+      weight: 15,
+      explanation: 'Duplicate not resolved.',
+      txn_date: '2026-03-02',
+      merchant: 'Client Payment',
+      amount: 820,
+      direction: 'in',
+      resolution_target: {
+        label: 'Resolve duplicate',
+        workflow: '/inbox/finish-now',
+      },
+    },
+  ],
+};
+
+describe('quarter readiness helpers', () => {
+  test('keeps blockers ordered and updates headline while blockers remain', () => {
+    const state = buildQuarterUiState({ report: reportFixture, resolvedIssueIds: ['txn-9104'] });
+
+    expect(state.itemsLeft).toBe(2);
+    expect(state.exportEnabled).toBe(false);
+    expect(state.report.readiness_pct).toBeGreaterThan(78);
+    expect(state.headline).toContain('items left');
+    expect(state.activeIssues.map((issue) => issue.affected_entity_id)).toEqual(['txn-9101', 'txn-9103']);
+    expect(state.report.explanation_lines[0]).toContain('Readiness is');
+  });
+
+  test('unlocks export immediately when every blocker is resolved', () => {
+    const state = buildQuarterUiState({
+      report: reportFixture,
+      resolvedIssueIds: ['txn-9101', 'txn-9103', 'txn-9104'],
+    });
+
+    expect(state.itemsLeft).toBe(0);
+    expect(state.exportEnabled).toBe(true);
+    expect(state.report.readiness_pct).toBe(100);
+    expect(state.report.can_export).toBe(true);
+    expect(state.report.issue_summary).toEqual([]);
+  });
+
+  test('supports demo query params that resolve all blockers', () => {
+    const params = new URLSearchParams('resolvedDemoIssues=all');
+    const resolvedIds = deriveResolvedIssueIdsFromSearchParams(params, reportFixture.issue_list);
+
+    expect(resolvedIds).toEqual(['txn-9101', 'txn-9103', 'txn-9104']);
+  });
+});
diff --git a/bizPA/start_bizpa_tax_readiness_ui.ps1 b/bizPA/start_bizpa_tax_readiness_ui.ps1
index 1fc4bef8cd172c31fdc3db52d1acc9d4775c5485..7216e44eac386fe436a9af8e9b417cd2d61e37fe
--- a/bizPA/start_bizpa_tax_readiness_ui.ps1
+++ b/bizPA/start_bizpa_tax_readiness_ui.ps1
@@ -4,6 +4,7 @@
 $frontendPath = Join-Path $repoRoot 'frontend'
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
 
 Write-Host 'Starting bizPA tax readiness UI...' -ForegroundColor Cyan
 
@@ -14,7 +15,10 @@
 Write-Host 'Tax readiness dashboard URL:' -ForegroundColor Green
 Write-Host "  $readinessUrl" -ForegroundColor Yellow
 Write-Host ''
+Write-Host 'Export-ready verification URL:' -ForegroundColor Green
+Write-Host "  $exportReadyUrl" -ForegroundColor Yellow
+Write-Host ''
 Write-Host 'Base frontend URL:' -ForegroundColor Green
 Write-Host "  $frontendUrl" -ForegroundColor Yellow
 Write-Host ''
-Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI can be reviewed without backend setup.' -ForegroundColor Cyan
+Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI and zero-blocker export-ready state can be reviewed without backend setup.' -ForegroundColor Cyan
diff --git a/bizPA/tax_readiness_ui_smoke.ps1 b/bizPA/tax_readiness_ui_smoke.ps1
index 56d67892a51b22572503dec560d17123b370c2b7..47a50c8d3ac8ff8c933943a7c6a966cdce89328f
--- a/bizPA/tax_readiness_ui_smoke.ps1
+++ b/bizPA/tax_readiness_ui_smoke.ps1
@@ -28,99 +28,92 @@
 
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
-$screenshotPath = 'C:\Users\edebe\eds\workstream\verification\20260311_201500_bizpa_tax_readiness_ui.png'
-$chromeProfilePath = 'C:\Users\edebe\eds\workstream\verification\chrome_capture_profile_tax_readiness'
-
-New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
-
-$frontendJob = Start-Job -ScriptBlock {
-  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
-  $env:PORT = '3002'
-  npm.cmd start
-}
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
+$verificationRoot = 'C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification'
+$quarterScreenshotPath = Join-Path $verificationRoot 'quarter_readiness_screen.png'
+$finishNowScreenshotPath = Join-Path $verificationRoot 'finish_now_queue_zero_blockers.png'
+function Wait-FrontendReady {
+  param (
+    [string]$Url
+  )
 
-try {
-  $frontendResponse = $null
-  $frontendReady = $false
   for ($i = 0; $i -lt 45; $i++) {
     Start-Sleep -Seconds 2
     try {
-      $frontendResponse = Invoke-WebRequest -UseBasicParsing $readinessUrl
-      if ($frontendResponse.StatusCode -eq 200) {
-        $frontendReady = $true
-        break
+      $response = Invoke-WebRequest -UseBasicParsing $Url
+      if ($response.StatusCode -eq 200) {
+        return $response
       }
     } catch {
     }
   }
 
-  if (-not $frontendReady) {
-    throw "Frontend did not become ready on $readinessUrl"
-  }
+  throw "Frontend did not become ready on $Url"
+}
 
-  $chromeArgs = @(
-    "--user-data-dir=$chromeProfilePath",
-    '--new-window',
-    '--window-size=1600,1500',
-    '--no-first-run',
-    '--disable-session-crashed-bubble',
-    '--disable-infobars',
-    '--disable-breakpad',
-    '--disable-crash-reporter',
-    '--noerrdialogs',
-    "--app=$readinessUrl"
+function Capture-AppScreenshot {
+  param (
+    [string]$Url,
+    [string]$OutputPath
   )
-  Start-Process 'C:\Program Files\Google\Chrome\Application\chrome.exe' -ArgumentList $chromeArgs | Out-Null
 
-  $chromeWindow = $null
-  for ($i = 0; $i -lt 20; $i++) {
-    Start-Sleep -Seconds 1
-    $chromeWindow = Get-Process chrome -ErrorAction SilentlyContinue |
-      ForEach-Object {
-        try {
-          if ($_.MainWindowHandle -and ([int64]$_.MainWindowHandle -ne 0)) {
-            [pscustomobject]@{
-              Process = $_
-              StartTime = $_.StartTime
-            }
-          }
-        } catch {
-        }
-      } |
-      Sort-Object StartTime -Descending |
-      Select-Object -First 1
-    if ($chromeWindow) {
-      $chromeWindow = $chromeWindow.Process
+  $form = New-Object System.Windows.Forms.Form
+  $form.Width = 1600
+  $form.Height = 1500
+  $form.StartPosition = 'Manual'
+  $form.Location = New-Object System.Drawing.Point(-32000, -32000)
+
+  $browser = New-Object System.Windows.Forms.WebBrowser
+  $browser.ScriptErrorsSuppressed = $true
+  $browser.ScrollBarsEnabled = $false
+  $browser.Dock = [System.Windows.Forms.DockStyle]::Fill
+  $form.Controls.Add($browser)
+  $form.Show()
+  $browser.Navigate($Url)
+
+  $ready = $false
+  for ($i = 0; $i -lt 90; $i++) {
+    [System.Windows.Forms.Application]::DoEvents()
+    Start-Sleep -Milliseconds 250
+    if ($browser.ReadyState -eq [System.Windows.Forms.WebBrowserReadyState]::Complete) {
+      $ready = $true
       break
     }
   }
 
-  if (-not $chromeWindow) {
-    throw 'Chrome readiness window did not expose a main window handle for screenshot capture.'
+  if (-not $ready) {
+    $form.Close()
+    throw "Embedded browser did not finish loading $Url"
   }
 
-  [WindowCaptureNative]::ShowWindow($chromeWindow.MainWindowHandle, 3) | Out-Null
-  [WindowCaptureNative]::SetForegroundWindow($chromeWindow.MainWindowHandle) | Out-Null
-  Start-Sleep -Seconds 1
+  Start-Sleep -Seconds 3
+  [System.Windows.Forms.Application]::DoEvents()
 
-  $rect = New-Object WindowCaptureNative+RECT
-  [WindowCaptureNative]::GetWindowRect($chromeWindow.MainWindowHandle, [ref]$rect) | Out-Null
-  $width = [Math]::Max(1, $rect.Right - $rect.Left)
-  $height = [Math]::Max(1, $rect.Bottom - $rect.Top)
-  $bitmap = New-Object System.Drawing.Bitmap $width, $height
-  $graphics = [System.Drawing.Graphics]::FromImage($bitmap)
-  $graphics.CopyFromScreen($rect.Left, $rect.Top, 0, 0, $bitmap.Size)
-  $bitmap.Save($screenshotPath, [System.Drawing.Imaging.ImageFormat]::Png)
-  $graphics.Dispose()
+  $bitmap = New-Object System.Drawing.Bitmap $browser.Width, $browser.Height
+  $browser.DrawToBitmap($bitmap, (New-Object System.Drawing.Rectangle 0, 0, $browser.Width, $browser.Height))
+  $bitmap.Save($OutputPath, [System.Drawing.Imaging.ImageFormat]::Png)
   $bitmap.Dispose()
+  $form.Close()
+}
+
+New-Item -ItemType Directory -Force -Path $verificationRoot | Out-Null
+
+$frontendJob = Start-Job -ScriptBlock {
+  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
+  $env:PORT = '3002'
+  npm.cmd start
+}
 
-  Get-Process chrome -ErrorAction SilentlyContinue |
-    Where-Object { $_.StartTime -ge $chromeWindow.StartTime.AddSeconds(-2) } |
-    Stop-Process -Force -ErrorAction SilentlyContinue
+try {
+  $frontendResponse = Wait-FrontendReady -Url $readinessUrl
+  Capture-AppScreenshot -Url $readinessUrl -OutputPath $quarterScreenshotPath
+  Capture-AppScreenshot -Url $exportReadyUrl -OutputPath $finishNowScreenshotPath
 
   "READINESS_URL=$readinessUrl"
+  "EXPORT_READY_URL=$exportReadyUrl"
   "FRONTEND_STATUS=$($frontendResponse.StatusCode)"
-  "SCREENSHOT=$screenshotPath"
+  "SCREENSHOT_QUARTER=$quarterScreenshotPath"
+  "SCREENSHOT_EXPORT_READY=$finishNowScreenshotPath"
   'FRONTEND_LOG_START'
   Receive-Job $frontendJob -Keep -ErrorAction SilentlyContinue 2>&1
   'FRONTEND_LOG_END'
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..067ce847896d101d973aab77be01c41e12976e3c
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,151 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [x] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [x] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: Script printed `http://127.0.0.1:3002/?readinessDemo=1&tab=quarter` and `http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all` on 2026-03-18.
+- [x] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [x] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: `PASS src/quarterReadiness.test.js` plus `PASS` on the existing onboarding, control-centre, and governance suites on 2026-03-18.
+- [x] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [x] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: Smoke script returned `FRONTEND_STATUS=200` and saved `quarter_readiness_screen.png` plus `finish_now_queue_zero_blockers.png` under the epic verification folder on 2026-03-18.
+
+## Evidence
+Objective-Delivery-Coverage: 90%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: captured
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: captured
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: captured
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: captured
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: captured
+- Evidence-Type: manual_verification
+  - Artifact: `Pending user review of the local URLs printed by start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: Final UI acceptance for the quarter-readiness headline, Finish Now queue behavior, and export gating is pending explicit user pass/fail feedback.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+- 2026-03-18 18:10: Added a dedicated quarter readiness helper module and automated tests for live blocker resolution, readiness recalculation, and query-param seeded demo resolution.
+- 2026-03-18 18:13: Updated `bizPA/frontend/src/App.jsx` to derive the quarter hero, inline Finish Now queue, and export gating from a single live quarter state model.
+- 2026-03-18 18:15: Updated the local quarter-readiness start script to print both the standard quarter review URL and the zero-blocker export-ready verification URL.
+- 2026-03-18 18:18: Reworked the smoke script to capture the required screenshots locally after browser-based capture failed under the sandboxed environment.
+- 2026-03-18 18:20: Completed automated validation, captured screenshots, and prepared the task for user verification.
+
+## Changes Made
+- Added `bizPA/frontend/src/quarterReadiness.js` with pure helpers for blocker ordering, readiness recalculation, issue-summary rebuilding, explanation copy, and demo query-param resolution.
+- Added `bizPA/frontend/src/quarterReadiness.test.js` covering partial blocker clearance, zero-blocker export enablement, and `resolvedDemoIssues=all`.
+- Updated `bizPA/frontend/src/App.jsx` so the quarter screen now:
+  - renders the hero headline as `<pct>% ready — <items> items left`,
+  - shows an inline ordered Finish Now queue,
+  - updates readiness/export state immediately in demo mode when blockers are cleared,
+  - keeps inbox/dashboard/control-centre blocker counts aligned to the live quarter state.
+- Updated `bizPA/start_bizpa_tax_readiness_ui.ps1` to print both the normal quarter-close URL and an export-ready verification URL.
+- Updated `bizPA/tax_readiness_ui_smoke.ps1` to save screenshots directly to the epic verification folder for the blocked and zero-blocker states.
+
+## Validation
+- `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Pass. Output included:
+    - `Tax readiness dashboard URL: http://127.0.0.1:3002/?readinessDemo=1&tab=quarter`
+    - `Export-ready verification URL: http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all`
+- `npm.cmd test -- --runInBand --watchAll=false` in `C:\Users\edebe\eds\bizPA\frontend`
+  - Pass. `4` suites passed, `14` tests passed.
+- `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Pass. Output included:
+    - `FRONTEND_STATUS=200`
+    - `SCREENSHOT_QUARTER=C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+    - `SCREENSHOT_EXPORT_READY=C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+- User verification request: To be issued with the completion response for explicit pass/fail on the quarter headline, Finish Now queue, and export gating behavior.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The immediate blocker-clear behavior is implemented in the demo/local verification path so the export gating can be proven end-to-end without backend mutation setup.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+Awaiting user verification - 2026-03-18 18:20
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..b0b6a961dcb80123d7634a661b59a1e944c1c7a1
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,26 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    fallbackIssues: inboxItems,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1532,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1547,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1633,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2104,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2316,29 +2562,241 @@
     </div>
   );
 
-  const renderInbox = () => (
-    <div>
-      <h4>Finish Now Queue</h4>
-      <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
-      <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
-            <div>
-              <b>{item.merchant || 'Unknown'}</b>
-              <br />
-              <small>
-                {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
-              </small>
-              <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
-            </div>
+  const renderInbox = () => {
+    return (
+    <div className="inbox-mobile-shell">
+      <section className="inbox-mobile-hero">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2">
+          <div className="inbox-kicker">
+            <Bell size={14} />
+            Mobile-first exception queue
           </div>
-        ))}
-      </div>
-    </div>
-  );
+          <div className="status-pill">{filteredInboxCards.length} visible</div>
+        </div>
+        <h4>Finish now</h4>
+        <p>
+          Clear the blockers that stop the quarterly export: category, business or personal, split treatment, and duplicate resolution.
+        </p>
+        <div className="inbox-mobile-stats">
+          <div className="inbox-mobile-stat">
+            <small>Queue</small>
+            <strong>{inboxCards.length}</strong>
+            <small>{activeQuarterReport.quarter_reference || READINESS_DEMO_REPORT.quarter_reference}</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Critical</small>
+            <strong>{highSeverityCount}</strong>
+            <small>High-severity blockers</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Ready now</small>
+            <strong>{readyToExportCount}</strong>
+            <small>Transactions already export-safe</small>
+          </div>
+        </div>
+        <div className="hero-actions">
+          <button className="hero-action-main" onClick={() => setCurrentTab('quarter')}>
+            <ArrowRight size={16} />
+            Review quarter readiness
+          </button>
+          <button className="hero-action-secondary" onClick={undoLastInboxAction} disabled={inboxBusyId === 'undo-last'}>
+            {inboxBusyId === 'undo-last' ? <RefreshCcw size={16} className="spinner" /> : <ArchiveRestore size={16} />}
+            Undo last action
+          </button>
+        </div>
+      </section>
+
+      {inboxMessage && (
+        <div className={`readiness-panel inbox-message ${inboxMessage.type}`}>
+          {inboxMessage.text}
+        </div>
+      )}
+
+      <section className="readiness-panel">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-3">
+          <div>
+            <h5 style={{ marginBottom: 4 }}>Queue filters</h5>
+            <small className="small-muted">Keep the mobile pass focused on one blocker type at a time.</small>
+          </div>
+          <div className="status-pill">{activeQuarterReport.readiness_pct ?? READINESS_DEMO_REPORT.readiness_pct}% ready</div>
+        </div>
+        <div className="inbox-filter-row">
+          {INBOX_FILTERS.map((filter) => {
+            const count = filter.key === 'all'
+              ? inboxCards.length
+              : inboxCards.filter((card) => card.issue_types.includes(filter.key)).length;
+            return (
+              <button
+                key={filter.key}
+                type="button"
+                className={`inbox-filter-chip ${inboxFilter === filter.key ? 'active' : ''}`}
+                onClick={() => setInboxFilter(filter.key)}
+              >
+                <span>{filter.label}</span>
+                <small>{count}</small>
+              </button>
+            );
+          })}
+        </div>
+      </section>
+
+      {filteredInboxCards.length === 0 ? (
+        <div className="inbox-empty">
+          No blockers match this filter. Reset to All blockers or return to the quarter screen to confirm readiness.
+        </div>
+      ) : (
+        <div className="inbox-card-list">
+          {filteredInboxCards.map((card) => {
+            const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+            const saving = inboxBusyId === card.id;
+            const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+            return (
+              <section key={card.id} className="inbox-action-card">
+                <div className="inbox-card-top">
+                  <div>
+                    <strong>{card.merchant}</strong>
+                    <div className="inbox-card-subline">
+                      {card.txn_date ? String(card.txn_date).slice(0, 10) : 'Date pending'} · {formatIssueAmount(card.amount, card.direction)}
+                    </div>
+                  </div>
+                  <span className={`inbox-issue-chip ${card.severity}`}>
+                    <TriangleAlert size={14} />
+                    {INBOX_REASON_LABELS[card.blocker_reason] || card.blocker_reason.replace(/_/g, ' ')}
+                  </span>
+                </div>
+
+                <div className="inbox-chip-row">
+                  {card.issue_labels.map((label) => (
+                    <span key={`${card.id}-${label}`} className={`inbox-issue-chip ${card.severity}`}>
+                      {label}
+                    </span>
+                  ))}
+                  {card.suggested_category?.category_name && (
+                    <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
+                      <CheckCircle2 size={14} />
+                      Suggested: {card.suggested_category.category_name}
+                    </button>
+                  )}
+                </div>
+
+                <p className="inbox-explainer">{card.explanation || 'Resolve the blocking fields to clear this transaction from the queue.'}</p>
+
+                {(card.supports_category || card.supports_business_personal || card.supports_split) && (
+                  <div className="inbox-action-grid">
+                    <div className="inbox-field">
+                      <label>Category code</label>
+                      <input
+                        value={draft.category_code}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_code', event.target.value)}
+                        placeholder="MATERIALS"
+                      />
+                    </div>
+                    <div className="inbox-field">
+                      <label>Category label</label>
+                      <input
+                        value={draft.category_name}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_name', event.target.value)}
+                        placeholder="Materials"
+                      />
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_business_personal || card.supports_split) && (
+                  <div>
+                    <div className="inbox-field">
+                      <label>Business treatment</label>
+                    </div>
+                    <div className="inbox-toggle-row">
+                      {[
+                        { value: 'BUSINESS', label: 'Business' },
+                        { value: 'PERSONAL', label: 'Personal' },
+                        { value: 'SPLIT', label: 'Split' },
+                      ].map((option) => (
+                        <button
+                          key={option.value}
+                          type="button"
+                          className={`inbox-toggle ${draft.business_personal === option.value ? 'active' : ''}`}
+                          onClick={() => setInboxDraftField(card.id, 'business_personal', option.value)}
+                        >
+                          {option.label}
+                        </button>
+                      ))}
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_split || isSplit) && (
+                  <div className="inbox-field">
+                    <label>Business split %</label>
+                    <input
+                      type="number"
+                      min="0"
+                      max="100"
+                      value={draft.split_business_pct}
+                      onChange={(event) => setInboxDraftField(card.id, 'split_business_pct', event.target.value)}
+                      placeholder="80"
+                    />
+                  </div>
+                )}
+
+                {card.supports_duplicate && (
+                  <>
+                    <div>
+                      <div className="inbox-field">
+                        <label>Duplicate action</label>
+                      </div>
+                      <div className="inbox-duplicate-row">
+                        {[
+                          { value: 'dismiss', label: 'Dismiss duplicate' },
+                          { value: 'merge', label: 'Merge to original' },
+                        ].map((option) => (
+                          <button
+                            key={option.value}
+                            type="button"
+                            className={`inbox-toggle ${draft.duplicate_action === option.value ? 'active' : ''}`}
+                            onClick={() => setInboxDraftField(card.id, 'duplicate_action', option.value)}
+                          >
+                            {option.label}
+                          </button>
+                        ))}
+                      </div>
+                    </div>
+                    {draft.duplicate_action === 'merge' && (
+                      <div className="inbox-field">
+                        <label>Original transaction id</label>
+                        <input
+                          value={draft.duplicate_of_txn_id}
+                          onChange={(event) => setInboxDraftField(card.id, 'duplicate_of_txn_id', event.target.value)}
+                          placeholder="txn-9001"
+                        />
+                      </div>
+                    )}
+                  </>
+                )}
 
+                <div className="inbox-footer-row">
+                  <div className="inbox-inline-status">
+                    Route: {card.resolution_target?.route || `/api/v1/inbox/${card.id}`}
+                  </div>
+                  <div className="inbox-footer-actions">
+                    <button className="secondary-action" onClick={clearInboxMessage} disabled={saving}>
+                      Clear banner
+                    </button>
+                    <button className="primary-action" onClick={() => submitInboxAction(card)} disabled={saving}>
+                      {saving ? <RefreshCcw size={16} className="spinner" /> : <CheckCircle2 size={16} />}
+                      Save and clear blocker
+                    </button>
+                  </div>
+                </div>
+              </section>
+            );
+          })}
+        </div>
+      )}
+    </div>
+    );
+  };
   const renderControlCentre = () => {
     const policyLabels = {
       vat_scheme: 'VAT scheme',
@@ -2510,11 +2968,11 @@
           <section className="readiness-panel">
             <h5 style={{ marginBottom: 12 }}>Queue Backlog</h5>
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
-                <div key={item.id} className="control-row">
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
+                <div key={item.id || item.affected_entity_id} className="control-row">
                   <div>
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
                   <div className="text-end">
                     <div className="small-muted">{item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date}</div>
@@ -2522,7 +2980,7 @@
                   </div>
                 </div>
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
               )}
             </div>
@@ -2668,7 +3126,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2697,7 +3155,7 @@
   };
 
   const createSnapshotVersion = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     const status = snapshotWorkspace?.snapshot_status;
 
     if (!status?.can_create_snapshot_version) {
@@ -2719,7 +3177,7 @@
         description: `Quarter snapshot generated from Quarter workspace`,
         metadata: {
           source_type: 'manual',
-          readiness_pct: quarterReadiness?.readiness_pct ?? null
+          readiness_pct: activeQuarterReport?.readiness_pct ?? null
         }
       });
       await refreshQuarterWorkspace();
@@ -2734,7 +3192,7 @@
   };
 
   const closeQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(closeReason || '').trim()) {
       setQuarterActionError('Close reason is required.');
       setQuarterActionMessage('');
@@ -2785,7 +3243,7 @@
   };
 
   const reopenQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(reopenReason || '').trim()) {
       setQuarterActionError('Reopen reason is required.');
       setQuarterActionMessage('');
@@ -2844,9 +3302,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3342,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3354,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3365,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3379,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..669173cd6176ee9ee5c86c5895ea49cb9524ebc4
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,155 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, fallbackIssues = [], resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const issueSource = Array.isArray(report.issue_list) && report.issue_list.length > 0
+    ? report.issue_list
+    : fallbackIssues;
+  const allIssues = sortIssues(Array.isArray(issueSource) ? issueSource : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/bizPA/frontend/src/quarterReadiness.test.js b/bizPA/frontend/src/quarterReadiness.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..0b810191a89d8b82091bf5de1408afcc23c14e65
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.test.js
@@ -0,0 +1,93 @@
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
+
+const reportFixture = {
+  quarter_reference: 'Q1-2026',
+  period_start: '2026-01-01',
+  period_end: '2026-03-31',
+  total_txns_in_period: 18,
+  readiness_pct: 78,
+  can_export: false,
+  issue_list: [
+    {
+      issue_type: 'missing_category',
+      affected_entity_id: 'txn-9101',
+      severity: 'high',
+      weight: 35,
+      explanation: 'Category missing for Fuel Stop.',
+      txn_date: '2026-02-04',
+      merchant: 'Fuel Stop',
+      amount: 64.18,
+      direction: 'out',
+      resolution_target: {
+        label: 'Classify transaction',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'missing_business_personal',
+      affected_entity_id: 'txn-9103',
+      severity: 'high',
+      weight: 30,
+      explanation: 'Business/personal decision missing.',
+      txn_date: '2026-02-15',
+      merchant: 'Mobile Contract',
+      amount: 42.5,
+      direction: 'out',
+      resolution_target: {
+        label: 'Set business or personal',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'unresolved_duplicate',
+      affected_entity_id: 'txn-9104',
+      severity: 'medium',
+      weight: 15,
+      explanation: 'Duplicate not resolved.',
+      txn_date: '2026-03-02',
+      merchant: 'Client Payment',
+      amount: 820,
+      direction: 'in',
+      resolution_target: {
+        label: 'Resolve duplicate',
+        workflow: '/inbox/finish-now',
+      },
+    },
+  ],
+};
+
+describe('quarter readiness helpers', () => {
+  test('keeps blockers ordered and updates headline while blockers remain', () => {
+    const state = buildQuarterUiState({ report: reportFixture, resolvedIssueIds: ['txn-9104'] });
+
+    expect(state.itemsLeft).toBe(2);
+    expect(state.exportEnabled).toBe(false);
+    expect(state.report.readiness_pct).toBeGreaterThan(78);
+    expect(state.headline).toContain('items left');
+    expect(state.activeIssues.map((issue) => issue.affected_entity_id)).toEqual(['txn-9101', 'txn-9103']);
+    expect(state.report.explanation_lines[0]).toContain('Readiness is');
+  });
+
+  test('unlocks export immediately when every blocker is resolved', () => {
+    const state = buildQuarterUiState({
+      report: reportFixture,
+      resolvedIssueIds: ['txn-9101', 'txn-9103', 'txn-9104'],
+    });
+
+    expect(state.itemsLeft).toBe(0);
+    expect(state.exportEnabled).toBe(true);
+    expect(state.report.readiness_pct).toBe(100);
+    expect(state.report.can_export).toBe(true);
+    expect(state.report.issue_summary).toEqual([]);
+  });
+
+  test('supports demo query params that resolve all blockers', () => {
+    const params = new URLSearchParams('resolvedDemoIssues=all');
+    const resolvedIds = deriveResolvedIssueIdsFromSearchParams(params, reportFixture.issue_list);
+
+    expect(resolvedIds).toEqual(['txn-9101', 'txn-9103', 'txn-9104']);
+  });
+});
diff --git a/bizPA/start_bizpa_tax_readiness_ui.ps1 b/bizPA/start_bizpa_tax_readiness_ui.ps1
index 1fc4bef8cd172c31fdc3db52d1acc9d4775c5485..7216e44eac386fe436a9af8e9b417cd2d61e37fe
--- a/bizPA/start_bizpa_tax_readiness_ui.ps1
+++ b/bizPA/start_bizpa_tax_readiness_ui.ps1
@@ -4,6 +4,7 @@
 $frontendPath = Join-Path $repoRoot 'frontend'
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
 
 Write-Host 'Starting bizPA tax readiness UI...' -ForegroundColor Cyan
 
@@ -14,7 +15,10 @@
 Write-Host 'Tax readiness dashboard URL:' -ForegroundColor Green
 Write-Host "  $readinessUrl" -ForegroundColor Yellow
 Write-Host ''
+Write-Host 'Export-ready verification URL:' -ForegroundColor Green
+Write-Host "  $exportReadyUrl" -ForegroundColor Yellow
+Write-Host ''
 Write-Host 'Base frontend URL:' -ForegroundColor Green
 Write-Host "  $frontendUrl" -ForegroundColor Yellow
 Write-Host ''
-Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI can be reviewed without backend setup.' -ForegroundColor Cyan
+Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI and zero-blocker export-ready state can be reviewed without backend setup.' -ForegroundColor Cyan
diff --git a/bizPA/tax_readiness_ui_smoke.ps1 b/bizPA/tax_readiness_ui_smoke.ps1
index 56d67892a51b22572503dec560d17123b370c2b7..47a50c8d3ac8ff8c933943a7c6a966cdce89328f
--- a/bizPA/tax_readiness_ui_smoke.ps1
+++ b/bizPA/tax_readiness_ui_smoke.ps1
@@ -28,99 +28,92 @@
 
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
-$screenshotPath = 'C:\Users\edebe\eds\workstream\verification\20260311_201500_bizpa_tax_readiness_ui.png'
-$chromeProfilePath = 'C:\Users\edebe\eds\workstream\verification\chrome_capture_profile_tax_readiness'
-
-New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
-
-$frontendJob = Start-Job -ScriptBlock {
-  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
-  $env:PORT = '3002'
-  npm.cmd start
-}
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
+$verificationRoot = 'C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification'
+$quarterScreenshotPath = Join-Path $verificationRoot 'quarter_readiness_screen.png'
+$finishNowScreenshotPath = Join-Path $verificationRoot 'finish_now_queue_zero_blockers.png'
+function Wait-FrontendReady {
+  param (
+    [string]$Url
+  )
 
-try {
-  $frontendResponse = $null
-  $frontendReady = $false
   for ($i = 0; $i -lt 45; $i++) {
     Start-Sleep -Seconds 2
     try {
-      $frontendResponse = Invoke-WebRequest -UseBasicParsing $readinessUrl
-      if ($frontendResponse.StatusCode -eq 200) {
-        $frontendReady = $true
-        break
+      $response = Invoke-WebRequest -UseBasicParsing $Url
+      if ($response.StatusCode -eq 200) {
+        return $response
       }
     } catch {
     }
   }
 
-  if (-not $frontendReady) {
-    throw "Frontend did not become ready on $readinessUrl"
-  }
+  throw "Frontend did not become ready on $Url"
+}
 
-  $chromeArgs = @(
-    "--user-data-dir=$chromeProfilePath",
-    '--new-window',
-    '--window-size=1600,1500',
-    '--no-first-run',
-    '--disable-session-crashed-bubble',
-    '--disable-infobars',
-    '--disable-breakpad',
-    '--disable-crash-reporter',
-    '--noerrdialogs',
-    "--app=$readinessUrl"
+function Capture-AppScreenshot {
+  param (
+    [string]$Url,
+    [string]$OutputPath
   )
-  Start-Process 'C:\Program Files\Google\Chrome\Application\chrome.exe' -ArgumentList $chromeArgs | Out-Null
 
-  $chromeWindow = $null
-  for ($i = 0; $i -lt 20; $i++) {
-    Start-Sleep -Seconds 1
-    $chromeWindow = Get-Process chrome -ErrorAction SilentlyContinue |
-      ForEach-Object {
-        try {
-          if ($_.MainWindowHandle -and ([int64]$_.MainWindowHandle -ne 0)) {
-            [pscustomobject]@{
-              Process = $_
-              StartTime = $_.StartTime
-            }
-          }
-        } catch {
-        }
-      } |
-      Sort-Object StartTime -Descending |
-      Select-Object -First 1
-    if ($chromeWindow) {
-      $chromeWindow = $chromeWindow.Process
+  $form = New-Object System.Windows.Forms.Form
+  $form.Width = 1600
+  $form.Height = 1500
+  $form.StartPosition = 'Manual'
+  $form.Location = New-Object System.Drawing.Point(-32000, -32000)
+
+  $browser = New-Object System.Windows.Forms.WebBrowser
+  $browser.ScriptErrorsSuppressed = $true
+  $browser.ScrollBarsEnabled = $false
+  $browser.Dock = [System.Windows.Forms.DockStyle]::Fill
+  $form.Controls.Add($browser)
+  $form.Show()
+  $browser.Navigate($Url)
+
+  $ready = $false
+  for ($i = 0; $i -lt 90; $i++) {
+    [System.Windows.Forms.Application]::DoEvents()
+    Start-Sleep -Milliseconds 250
+    if ($browser.ReadyState -eq [System.Windows.Forms.WebBrowserReadyState]::Complete) {
+      $ready = $true
       break
     }
   }
 
-  if (-not $chromeWindow) {
-    throw 'Chrome readiness window did not expose a main window handle for screenshot capture.'
+  if (-not $ready) {
+    $form.Close()
+    throw "Embedded browser did not finish loading $Url"
   }
 
-  [WindowCaptureNative]::ShowWindow($chromeWindow.MainWindowHandle, 3) | Out-Null
-  [WindowCaptureNative]::SetForegroundWindow($chromeWindow.MainWindowHandle) | Out-Null
-  Start-Sleep -Seconds 1
+  Start-Sleep -Seconds 3
+  [System.Windows.Forms.Application]::DoEvents()
 
-  $rect = New-Object WindowCaptureNative+RECT
-  [WindowCaptureNative]::GetWindowRect($chromeWindow.MainWindowHandle, [ref]$rect) | Out-Null
-  $width = [Math]::Max(1, $rect.Right - $rect.Left)
-  $height = [Math]::Max(1, $rect.Bottom - $rect.Top)
-  $bitmap = New-Object System.Drawing.Bitmap $width, $height
-  $graphics = [System.Drawing.Graphics]::FromImage($bitmap)
-  $graphics.CopyFromScreen($rect.Left, $rect.Top, 0, 0, $bitmap.Size)
-  $bitmap.Save($screenshotPath, [System.Drawing.Imaging.ImageFormat]::Png)
-  $graphics.Dispose()
+  $bitmap = New-Object System.Drawing.Bitmap $browser.Width, $browser.Height
+  $browser.DrawToBitmap($bitmap, (New-Object System.Drawing.Rectangle 0, 0, $browser.Width, $browser.Height))
+  $bitmap.Save($OutputPath, [System.Drawing.Imaging.ImageFormat]::Png)
   $bitmap.Dispose()
+  $form.Close()
+}
+
+New-Item -ItemType Directory -Force -Path $verificationRoot | Out-Null
+
+$frontendJob = Start-Job -ScriptBlock {
+  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
+  $env:PORT = '3002'
+  npm.cmd start
+}
 
-  Get-Process chrome -ErrorAction SilentlyContinue |
-    Where-Object { $_.StartTime -ge $chromeWindow.StartTime.AddSeconds(-2) } |
-    Stop-Process -Force -ErrorAction SilentlyContinue
+try {
+  $frontendResponse = Wait-FrontendReady -Url $readinessUrl
+  Capture-AppScreenshot -Url $readinessUrl -OutputPath $quarterScreenshotPath
+  Capture-AppScreenshot -Url $exportReadyUrl -OutputPath $finishNowScreenshotPath
 
   "READINESS_URL=$readinessUrl"
+  "EXPORT_READY_URL=$exportReadyUrl"
   "FRONTEND_STATUS=$($frontendResponse.StatusCode)"
-  "SCREENSHOT=$screenshotPath"
+  "SCREENSHOT_QUARTER=$quarterScreenshotPath"
+  "SCREENSHOT_EXPORT_READY=$finishNowScreenshotPath"
   'FRONTEND_LOG_START'
   Receive-Job $frontendJob -Keep -ErrorAction SilentlyContinue 2>&1
   'FRONTEND_LOG_END'
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..067ce847896d101d973aab77be01c41e12976e3c
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,151 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [x] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [x] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: Script printed `http://127.0.0.1:3002/?readinessDemo=1&tab=quarter` and `http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all` on 2026-03-18.
+- [x] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [x] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: `PASS src/quarterReadiness.test.js` plus `PASS` on the existing onboarding, control-centre, and governance suites on 2026-03-18.
+- [x] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [x] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: Smoke script returned `FRONTEND_STATUS=200` and saved `quarter_readiness_screen.png` plus `finish_now_queue_zero_blockers.png` under the epic verification folder on 2026-03-18.
+
+## Evidence
+Objective-Delivery-Coverage: 90%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: captured
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: captured
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: captured
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: captured
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: captured
+- Evidence-Type: manual_verification
+  - Artifact: `Pending user review of the local URLs printed by start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: Final UI acceptance for the quarter-readiness headline, Finish Now queue behavior, and export gating is pending explicit user pass/fail feedback.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+- 2026-03-18 18:10: Added a dedicated quarter readiness helper module and automated tests for live blocker resolution, readiness recalculation, and query-param seeded demo resolution.
+- 2026-03-18 18:13: Updated `bizPA/frontend/src/App.jsx` to derive the quarter hero, inline Finish Now queue, and export gating from a single live quarter state model.
+- 2026-03-18 18:15: Updated the local quarter-readiness start script to print both the standard quarter review URL and the zero-blocker export-ready verification URL.
+- 2026-03-18 18:18: Reworked the smoke script to capture the required screenshots locally after browser-based capture failed under the sandboxed environment.
+- 2026-03-18 18:20: Completed automated validation, captured screenshots, and prepared the task for user verification.
+
+## Changes Made
+- Added `bizPA/frontend/src/quarterReadiness.js` with pure helpers for blocker ordering, readiness recalculation, issue-summary rebuilding, explanation copy, and demo query-param resolution.
+- Added `bizPA/frontend/src/quarterReadiness.test.js` covering partial blocker clearance, zero-blocker export enablement, and `resolvedDemoIssues=all`.
+- Updated `bizPA/frontend/src/App.jsx` so the quarter screen now:
+  - renders the hero headline as `<pct>% ready — <items> items left`,
+  - shows an inline ordered Finish Now queue,
+  - updates readiness/export state immediately in demo mode when blockers are cleared,
+  - keeps inbox/dashboard/control-centre blocker counts aligned to the live quarter state.
+- Updated `bizPA/start_bizpa_tax_readiness_ui.ps1` to print both the normal quarter-close URL and an export-ready verification URL.
+- Updated `bizPA/tax_readiness_ui_smoke.ps1` to save screenshots directly to the epic verification folder for the blocked and zero-blocker states.
+
+## Validation
+- `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Pass. Output included:
+    - `Tax readiness dashboard URL: http://127.0.0.1:3002/?readinessDemo=1&tab=quarter`
+    - `Export-ready verification URL: http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all`
+- `npm.cmd test -- --runInBand --watchAll=false` in `C:\Users\edebe\eds\bizPA\frontend`
+  - Pass. `4` suites passed, `14` tests passed.
+- `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Pass. Output included:
+    - `FRONTEND_STATUS=200`
+    - `SCREENSHOT_QUARTER=C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+    - `SCREENSHOT_EXPORT_READY=C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+- User verification request: To be issued with the completion response for explicit pass/fail on the quarter headline, Finish Now queue, and export gating behavior.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The immediate blocker-clear behavior is implemented in the demo/local verification path so the export gating can be proven end-to-end without backend mutation setup.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+Awaiting user verification - 2026-03-18 18:20
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

Plan update
Implementation and technical validation are complete. The task is now documented as awaiting the required user verification for the UI behavior.
  ✓ Normalize the in-progress lifecycle file into the required template and record the ordered implementation plan for this task.
  ✓ Implement the quarter readiness screen updates, including inline Finish Now queue behavior, live readiness/export state updates, and any needed start/smoke script adjustments.
  ✓ Run targeted validations, capture screenshot/evidence artifacts, update the lifecycle checklist/evidence/results, and determine final completion state.
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..b0b6a961dcb80123d7634a661b59a1e944c1c7a1
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,26 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    fallbackIssues: inboxItems,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1532,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1547,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1633,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2104,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2316,29 +2562,241 @@
     </div>
   );
 
-  const renderInbox = () => (
-    <div>
-      <h4>Finish Now Queue</h4>
-      <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
-      <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
-            <div>
-              <b>{item.merchant || 'Unknown'}</b>
-              <br />
-              <small>
-                {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
-              </small>
-              <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
-            </div>
+  const renderInbox = () => {
+    return (
+    <div className="inbox-mobile-shell">
+      <section className="inbox-mobile-hero">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2">
+          <div className="inbox-kicker">
+            <Bell size={14} />
+            Mobile-first exception queue
           </div>
-        ))}
-      </div>
-    </div>
-  );
+          <div className="status-pill">{filteredInboxCards.length} visible</div>
+        </div>
+        <h4>Finish now</h4>
+        <p>
+          Clear the blockers that stop the quarterly export: category, business or personal, split treatment, and duplicate resolution.
+        </p>
+        <div className="inbox-mobile-stats">
+          <div className="inbox-mobile-stat">
+            <small>Queue</small>
+            <strong>{inboxCards.length}</strong>
+            <small>{activeQuarterReport.quarter_reference || READINESS_DEMO_REPORT.quarter_reference}</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Critical</small>
+            <strong>{highSeverityCount}</strong>
+            <small>High-severity blockers</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Ready now</small>
+            <strong>{readyToExportCount}</strong>
+            <small>Transactions already export-safe</small>
+          </div>
+        </div>
+        <div className="hero-actions">
+          <button className="hero-action-main" onClick={() => setCurrentTab('quarter')}>
+            <ArrowRight size={16} />
+            Review quarter readiness
+          </button>
+          <button className="hero-action-secondary" onClick={undoLastInboxAction} disabled={inboxBusyId === 'undo-last'}>
+            {inboxBusyId === 'undo-last' ? <RefreshCcw size={16} className="spinner" /> : <ArchiveRestore size={16} />}
+            Undo last action
+          </button>
+        </div>
+      </section>
+
+      {inboxMessage && (
+        <div className={`readiness-panel inbox-message ${inboxMessage.type}`}>
+          {inboxMessage.text}
+        </div>
+      )}
+
+      <section className="readiness-panel">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-3">
+          <div>
+            <h5 style={{ marginBottom: 4 }}>Queue filters</h5>
+            <small className="small-muted">Keep the mobile pass focused on one blocker type at a time.</small>
+          </div>
+          <div className="status-pill">{activeQuarterReport.readiness_pct ?? READINESS_DEMO_REPORT.readiness_pct}% ready</div>
+        </div>
+        <div className="inbox-filter-row">
+          {INBOX_FILTERS.map((filter) => {
+            const count = filter.key === 'all'
+              ? inboxCards.length
+              : inboxCards.filter((card) => card.issue_types.includes(filter.key)).length;
+            return (
+              <button
+                key={filter.key}
+                type="button"
+                className={`inbox-filter-chip ${inboxFilter === filter.key ? 'active' : ''}`}
+                onClick={() => setInboxFilter(filter.key)}
+              >
+                <span>{filter.label}</span>
+                <small>{count}</small>
+              </button>
+            );
+          })}
+        </div>
+      </section>
+
+      {filteredInboxCards.length === 0 ? (
+        <div className="inbox-empty">
+          No blockers match this filter. Reset to All blockers or return to the quarter screen to confirm readiness.
+        </div>
+      ) : (
+        <div className="inbox-card-list">
+          {filteredInboxCards.map((card) => {
+            const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+            const saving = inboxBusyId === card.id;
+            const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+            return (
+              <section key={card.id} className="inbox-action-card">
+                <div className="inbox-card-top">
+                  <div>
+                    <strong>{card.merchant}</strong>
+                    <div className="inbox-card-subline">
+                      {card.txn_date ? String(card.txn_date).slice(0, 10) : 'Date pending'} · {formatIssueAmount(card.amount, card.direction)}
+                    </div>
+                  </div>
+                  <span className={`inbox-issue-chip ${card.severity}`}>
+                    <TriangleAlert size={14} />
+                    {INBOX_REASON_LABELS[card.blocker_reason] || card.blocker_reason.replace(/_/g, ' ')}
+                  </span>
+                </div>
+
+                <div className="inbox-chip-row">
+                  {card.issue_labels.map((label) => (
+                    <span key={`${card.id}-${label}`} className={`inbox-issue-chip ${card.severity}`}>
+                      {label}
+                    </span>
+                  ))}
+                  {card.suggested_category?.category_name && (
+                    <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
+                      <CheckCircle2 size={14} />
+                      Suggested: {card.suggested_category.category_name}
+                    </button>
+                  )}
+                </div>
+
+                <p className="inbox-explainer">{card.explanation || 'Resolve the blocking fields to clear this transaction from the queue.'}</p>
+
+                {(card.supports_category || card.supports_business_personal || card.supports_split) && (
+                  <div className="inbox-action-grid">
+                    <div className="inbox-field">
+                      <label>Category code</label>
+                      <input
+                        value={draft.category_code}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_code', event.target.value)}
+                        placeholder="MATERIALS"
+                      />
+                    </div>
+                    <div className="inbox-field">
+                      <label>Category label</label>
+                      <input
+                        value={draft.category_name}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_name', event.target.value)}
+                        placeholder="Materials"
+                      />
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_business_personal || card.supports_split) && (
+                  <div>
+                    <div className="inbox-field">
+                      <label>Business treatment</label>
+                    </div>
+                    <div className="inbox-toggle-row">
+                      {[
+                        { value: 'BUSINESS', label: 'Business' },
+                        { value: 'PERSONAL', label: 'Personal' },
+                        { value: 'SPLIT', label: 'Split' },
+                      ].map((option) => (
+                        <button
+                          key={option.value}
+                          type="button"
+                          className={`inbox-toggle ${draft.business_personal === option.value ? 'active' : ''}`}
+                          onClick={() => setInboxDraftField(card.id, 'business_personal', option.value)}
+                        >
+                          {option.label}
+                        </button>
+                      ))}
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_split || isSplit) && (
+                  <div className="inbox-field">
+                    <label>Business split %</label>
+                    <input
+                      type="number"
+                      min="0"
+                      max="100"
+                      value={draft.split_business_pct}
+                      onChange={(event) => setInboxDraftField(card.id, 'split_business_pct', event.target.value)}
+                      placeholder="80"
+                    />
+                  </div>
+                )}
+
+                {card.supports_duplicate && (
+                  <>
+                    <div>
+                      <div className="inbox-field">
+                        <label>Duplicate action</label>
+                      </div>
+                      <div className="inbox-duplicate-row">
+                        {[
+                          { value: 'dismiss', label: 'Dismiss duplicate' },
+                          { value: 'merge', label: 'Merge to original' },
+                        ].map((option) => (
+                          <button
+                            key={option.value}
+                            type="button"
+                            className={`inbox-toggle ${draft.duplicate_action === option.value ? 'active' : ''}`}
+                            onClick={() => setInboxDraftField(card.id, 'duplicate_action', option.value)}
+                          >
+                            {option.label}
+                          </button>
+                        ))}
+                      </div>
+                    </div>
+                    {draft.duplicate_action === 'merge' && (
+                      <div className="inbox-field">
+                        <label>Original transaction id</label>
+                        <input
+                          value={draft.duplicate_of_txn_id}
+                          onChange={(event) => setInboxDraftField(card.id, 'duplicate_of_txn_id', event.target.value)}
+                          placeholder="txn-9001"
+                        />
+                      </div>
+                    )}
+                  </>
+                )}
 
+                <div className="inbox-footer-row">
+                  <div className="inbox-inline-status">
+                    Route: {card.resolution_target?.route || `/api/v1/inbox/${card.id}`}
+                  </div>
+                  <div className="inbox-footer-actions">
+                    <button className="secondary-action" onClick={clearInboxMessage} disabled={saving}>
+                      Clear banner
+                    </button>
+                    <button className="primary-action" onClick={() => submitInboxAction(card)} disabled={saving}>
+                      {saving ? <RefreshCcw size={16} className="spinner" /> : <CheckCircle2 size={16} />}
+                      Save and clear blocker
+                    </button>
+                  </div>
+                </div>
+              </section>
+            );
+          })}
+        </div>
+      )}
+    </div>
+    );
+  };
   const renderControlCentre = () => {
     const policyLabels = {
       vat_scheme: 'VAT scheme',
@@ -2510,11 +2968,11 @@
           <section className="readiness-panel">
             <h5 style={{ marginBottom: 12 }}>Queue Backlog</h5>
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
-                <div key={item.id} className="control-row">
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
+                <div key={item.id || item.affected_entity_id} className="control-row">
                   <div>
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
                   <div className="text-end">
                     <div className="small-muted">{item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date}</div>
@@ -2522,7 +2980,7 @@
                   </div>
                 </div>
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
               )}
             </div>
@@ -2668,7 +3126,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2697,7 +3155,7 @@
   };
 
   const createSnapshotVersion = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     const status = snapshotWorkspace?.snapshot_status;
 
     if (!status?.can_create_snapshot_version) {
@@ -2719,7 +3177,7 @@
         description: `Quarter snapshot generated from Quarter workspace`,
         metadata: {
           source_type: 'manual',
-          readiness_pct: quarterReadiness?.readiness_pct ?? null
+          readiness_pct: activeQuarterReport?.readiness_pct ?? null
         }
       });
       await refreshQuarterWorkspace();
@@ -2734,7 +3192,7 @@
   };
 
   const closeQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(closeReason || '').trim()) {
       setQuarterActionError('Close reason is required.');
       setQuarterActionMessage('');
@@ -2785,7 +3243,7 @@
   };
 
   const reopenQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(reopenReason || '').trim()) {
       setQuarterActionError('Reopen reason is required.');
       setQuarterActionMessage('');
@@ -2844,9 +3302,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3342,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3354,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3365,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3379,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..669173cd6176ee9ee5c86c5895ea49cb9524ebc4
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,155 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, fallbackIssues = [], resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const issueSource = Array.isArray(report.issue_list) && report.issue_list.length > 0
+    ? report.issue_list
+    : fallbackIssues;
+  const allIssues = sortIssues(Array.isArray(issueSource) ? issueSource : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/bizPA/frontend/src/quarterReadiness.test.js b/bizPA/frontend/src/quarterReadiness.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..0b810191a89d8b82091bf5de1408afcc23c14e65
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.test.js
@@ -0,0 +1,93 @@
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
+
+const reportFixture = {
+  quarter_reference: 'Q1-2026',
+  period_start: '2026-01-01',
+  period_end: '2026-03-31',
+  total_txns_in_period: 18,
+  readiness_pct: 78,
+  can_export: false,
+  issue_list: [
+    {
+      issue_type: 'missing_category',
+      affected_entity_id: 'txn-9101',
+      severity: 'high',
+      weight: 35,
+      explanation: 'Category missing for Fuel Stop.',
+      txn_date: '2026-02-04',
+      merchant: 'Fuel Stop',
+      amount: 64.18,
+      direction: 'out',
+      resolution_target: {
+        label: 'Classify transaction',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'missing_business_personal',
+      affected_entity_id: 'txn-9103',
+      severity: 'high',
+      weight: 30,
+      explanation: 'Business/personal decision missing.',
+      txn_date: '2026-02-15',
+      merchant: 'Mobile Contract',
+      amount: 42.5,
+      direction: 'out',
+      resolution_target: {
+        label: 'Set business or personal',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'unresolved_duplicate',
+      affected_entity_id: 'txn-9104',
+      severity: 'medium',
+      weight: 15,
+      explanation: 'Duplicate not resolved.',
+      txn_date: '2026-03-02',
+      merchant: 'Client Payment',
+      amount: 820,
+      direction: 'in',
+      resolution_target: {
+        label: 'Resolve duplicate',
+        workflow: '/inbox/finish-now',
+      },
+    },
+  ],
+};
+
+describe('quarter readiness helpers', () => {
+  test('keeps blockers ordered and updates headline while blockers remain', () => {
+    const state = buildQuarterUiState({ report: reportFixture, resolvedIssueIds: ['txn-9104'] });
+
+    expect(state.itemsLeft).toBe(2);
+    expect(state.exportEnabled).toBe(false);
+    expect(state.report.readiness_pct).toBeGreaterThan(78);
+    expect(state.headline).toContain('items left');
+    expect(state.activeIssues.map((issue) => issue.affected_entity_id)).toEqual(['txn-9101', 'txn-9103']);
+    expect(state.report.explanation_lines[0]).toContain('Readiness is');
+  });
+
+  test('unlocks export immediately when every blocker is resolved', () => {
+    const state = buildQuarterUiState({
+      report: reportFixture,
+      resolvedIssueIds: ['txn-9101', 'txn-9103', 'txn-9104'],
+    });
+
+    expect(state.itemsLeft).toBe(0);
+    expect(state.exportEnabled).toBe(true);
+    expect(state.report.readiness_pct).toBe(100);
+    expect(state.report.can_export).toBe(true);
+    expect(state.report.issue_summary).toEqual([]);
+  });
+
+  test('supports demo query params that resolve all blockers', () => {
+    const params = new URLSearchParams('resolvedDemoIssues=all');
+    const resolvedIds = deriveResolvedIssueIdsFromSearchParams(params, reportFixture.issue_list);
+
+    expect(resolvedIds).toEqual(['txn-9101', 'txn-9103', 'txn-9104']);
+  });
+});
diff --git a/bizPA/start_bizpa_tax_readiness_ui.ps1 b/bizPA/start_bizpa_tax_readiness_ui.ps1
index 1fc4bef8cd172c31fdc3db52d1acc9d4775c5485..7216e44eac386fe436a9af8e9b417cd2d61e37fe
--- a/bizPA/start_bizpa_tax_readiness_ui.ps1
+++ b/bizPA/start_bizpa_tax_readiness_ui.ps1
@@ -4,6 +4,7 @@
 $frontendPath = Join-Path $repoRoot 'frontend'
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
 
 Write-Host 'Starting bizPA tax readiness UI...' -ForegroundColor Cyan
 
@@ -14,7 +15,10 @@
 Write-Host 'Tax readiness dashboard URL:' -ForegroundColor Green
 Write-Host "  $readinessUrl" -ForegroundColor Yellow
 Write-Host ''
+Write-Host 'Export-ready verification URL:' -ForegroundColor Green
+Write-Host "  $exportReadyUrl" -ForegroundColor Yellow
+Write-Host ''
 Write-Host 'Base frontend URL:' -ForegroundColor Green
 Write-Host "  $frontendUrl" -ForegroundColor Yellow
 Write-Host ''
-Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI can be reviewed without backend setup.' -ForegroundColor Cyan
+Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI and zero-blocker export-ready state can be reviewed without backend setup.' -ForegroundColor Cyan
diff --git a/bizPA/tax_readiness_ui_smoke.ps1 b/bizPA/tax_readiness_ui_smoke.ps1
index 56d67892a51b22572503dec560d17123b370c2b7..47a50c8d3ac8ff8c933943a7c6a966cdce89328f
--- a/bizPA/tax_readiness_ui_smoke.ps1
+++ b/bizPA/tax_readiness_ui_smoke.ps1
@@ -28,99 +28,92 @@
 
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
-$screenshotPath = 'C:\Users\edebe\eds\workstream\verification\20260311_201500_bizpa_tax_readiness_ui.png'
-$chromeProfilePath = 'C:\Users\edebe\eds\workstream\verification\chrome_capture_profile_tax_readiness'
-
-New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
-
-$frontendJob = Start-Job -ScriptBlock {
-  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
-  $env:PORT = '3002'
-  npm.cmd start
-}
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
+$verificationRoot = 'C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification'
+$quarterScreenshotPath = Join-Path $verificationRoot 'quarter_readiness_screen.png'
+$finishNowScreenshotPath = Join-Path $verificationRoot 'finish_now_queue_zero_blockers.png'
+function Wait-FrontendReady {
+  param (
+    [string]$Url
+  )
 
-try {
-  $frontendResponse = $null
-  $frontendReady = $false
   for ($i = 0; $i -lt 45; $i++) {
     Start-Sleep -Seconds 2
     try {
-      $frontendResponse = Invoke-WebRequest -UseBasicParsing $readinessUrl
-      if ($frontendResponse.StatusCode -eq 200) {
-        $frontendReady = $true
-        break
+      $response = Invoke-WebRequest -UseBasicParsing $Url
+      if ($response.StatusCode -eq 200) {
+        return $response
       }
     } catch {
     }
   }
 
-  if (-not $frontendReady) {
-    throw "Frontend did not become ready on $readinessUrl"
-  }
+  throw "Frontend did not become ready on $Url"
+}
 
-  $chromeArgs = @(
-    "--user-data-dir=$chromeProfilePath",
-    '--new-window',
-    '--window-size=1600,1500',
-    '--no-first-run',
-    '--disable-session-crashed-bubble',
-    '--disable-infobars',
-    '--disable-breakpad',
-    '--disable-crash-reporter',
-    '--noerrdialogs',
-    "--app=$readinessUrl"
+function Capture-AppScreenshot {
+  param (
+    [string]$Url,
+    [string]$OutputPath
   )
-  Start-Process 'C:\Program Files\Google\Chrome\Application\chrome.exe' -ArgumentList $chromeArgs | Out-Null
 
-  $chromeWindow = $null
-  for ($i = 0; $i -lt 20; $i++) {
-    Start-Sleep -Seconds 1
-    $chromeWindow = Get-Process chrome -ErrorAction SilentlyContinue |
-      ForEach-Object {
-        try {
-          if ($_.MainWindowHandle -and ([int64]$_.MainWindowHandle -ne 0)) {
-            [pscustomobject]@{
-              Process = $_
-              StartTime = $_.StartTime
-            }
-          }
-        } catch {
-        }
-      } |
-      Sort-Object StartTime -Descending |
-      Select-Object -First 1
-    if ($chromeWindow) {
-      $chromeWindow = $chromeWindow.Process
+  $form = New-Object System.Windows.Forms.Form
+  $form.Width = 1600
+  $form.Height = 1500
+  $form.StartPosition = 'Manual'
+  $form.Location = New-Object System.Drawing.Point(-32000, -32000)
+
+  $browser = New-Object System.Windows.Forms.WebBrowser
+  $browser.ScriptErrorsSuppressed = $true
+  $browser.ScrollBarsEnabled = $false
+  $browser.Dock = [System.Windows.Forms.DockStyle]::Fill
+  $form.Controls.Add($browser)
+  $form.Show()
+  $browser.Navigate($Url)
+
+  $ready = $false
+  for ($i = 0; $i -lt 90; $i++) {
+    [System.Windows.Forms.Application]::DoEvents()
+    Start-Sleep -Milliseconds 250
+    if ($browser.ReadyState -eq [System.Windows.Forms.WebBrowserReadyState]::Complete) {
+      $ready = $true
       break
     }
   }
 
-  if (-not $chromeWindow) {
-    throw 'Chrome readiness window did not expose a main window handle for screenshot capture.'
+  if (-not $ready) {
+    $form.Close()
+    throw "Embedded browser did not finish loading $Url"
   }
 
-  [WindowCaptureNative]::ShowWindow($chromeWindow.MainWindowHandle, 3) | Out-Null
-  [WindowCaptureNative]::SetForegroundWindow($chromeWindow.MainWindowHandle) | Out-Null
-  Start-Sleep -Seconds 1
+  Start-Sleep -Seconds 3
+  [System.Windows.Forms.Application]::DoEvents()
 
-  $rect = New-Object WindowCaptureNative+RECT
-  [WindowCaptureNative]::GetWindowRect($chromeWindow.MainWindowHandle, [ref]$rect) | Out-Null
-  $width = [Math]::Max(1, $rect.Right - $rect.Left)
-  $height = [Math]::Max(1, $rect.Bottom - $rect.Top)
-  $bitmap = New-Object System.Drawing.Bitmap $width, $height
-  $graphics = [System.Drawing.Graphics]::FromImage($bitmap)
-  $graphics.CopyFromScreen($rect.Left, $rect.Top, 0, 0, $bitmap.Size)
-  $bitmap.Save($screenshotPath, [System.Drawing.Imaging.ImageFormat]::Png)
-  $graphics.Dispose()
+  $bitmap = New-Object System.Drawing.Bitmap $browser.Width, $browser.Height
+  $browser.DrawToBitmap($bitmap, (New-Object System.Drawing.Rectangle 0, 0, $browser.Width, $browser.Height))
+  $bitmap.Save($OutputPath, [System.Drawing.Imaging.ImageFormat]::Png)
   $bitmap.Dispose()
+  $form.Close()
+}
+
+New-Item -ItemType Directory -Force -Path $verificationRoot | Out-Null
+
+$frontendJob = Start-Job -ScriptBlock {
+  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
+  $env:PORT = '3002'
+  npm.cmd start
+}
 
-  Get-Process chrome -ErrorAction SilentlyContinue |
-    Where-Object { $_.StartTime -ge $chromeWindow.StartTime.AddSeconds(-2) } |
-    Stop-Process -Force -ErrorAction SilentlyContinue
+try {
+  $frontendResponse = Wait-FrontendReady -Url $readinessUrl
+  Capture-AppScreenshot -Url $readinessUrl -OutputPath $quarterScreenshotPath
+  Capture-AppScreenshot -Url $exportReadyUrl -OutputPath $finishNowScreenshotPath
 
   "READINESS_URL=$readinessUrl"
+  "EXPORT_READY_URL=$exportReadyUrl"
   "FRONTEND_STATUS=$($frontendResponse.StatusCode)"
-  "SCREENSHOT=$screenshotPath"
+  "SCREENSHOT_QUARTER=$quarterScreenshotPath"
+  "SCREENSHOT_EXPORT_READY=$finishNowScreenshotPath"
   'FRONTEND_LOG_START'
   Receive-Job $frontendJob -Keep -ErrorAction SilentlyContinue 2>&1
   'FRONTEND_LOG_END'
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..067ce847896d101d973aab77be01c41e12976e3c
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,151 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [x] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [x] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: Script printed `http://127.0.0.1:3002/?readinessDemo=1&tab=quarter` and `http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all` on 2026-03-18.
+- [x] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [x] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: `PASS src/quarterReadiness.test.js` plus `PASS` on the existing onboarding, control-centre, and governance suites on 2026-03-18.
+- [x] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [x] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: Smoke script returned `FRONTEND_STATUS=200` and saved `quarter_readiness_screen.png` plus `finish_now_queue_zero_blockers.png` under the epic verification folder on 2026-03-18.
+
+## Evidence
+Objective-Delivery-Coverage: 90%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: captured
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: captured
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: captured
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: captured
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: captured
+- Evidence-Type: manual_verification
+  - Artifact: `Pending user review of the local URLs printed by start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: Final UI acceptance for the quarter-readiness headline, Finish Now queue behavior, and export gating is pending explicit user pass/fail feedback.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+- 2026-03-18 18:10: Added a dedicated quarter readiness helper module and automated tests for live blocker resolution, readiness recalculation, and query-param seeded demo resolution.
+- 2026-03-18 18:13: Updated `bizPA/frontend/src/App.jsx` to derive the quarter hero, inline Finish Now queue, and export gating from a single live quarter state model.
+- 2026-03-18 18:15: Updated the local quarter-readiness start script to print both the standard quarter review URL and the zero-blocker export-ready verification URL.
+- 2026-03-18 18:18: Reworked the smoke script to capture the required screenshots locally after browser-based capture failed under the sandboxed environment.
+- 2026-03-18 18:20: Completed automated validation, captured screenshots, and prepared the task for user verification.
+
+## Changes Made
+- Added `bizPA/frontend/src/quarterReadiness.js` with pure helpers for blocker ordering, readiness recalculation, issue-summary rebuilding, explanation copy, and demo query-param resolution.
+- Added `bizPA/frontend/src/quarterReadiness.test.js` covering partial blocker clearance, zero-blocker export enablement, and `resolvedDemoIssues=all`.
+- Updated `bizPA/frontend/src/App.jsx` so the quarter screen now:
+  - renders the hero headline as `<pct>% ready — <items> items left`,
+  - shows an inline ordered Finish Now queue,
+  - updates readiness/export state immediately in demo mode when blockers are cleared,
+  - keeps inbox/dashboard/control-centre blocker counts aligned to the live quarter state.
+- Updated `bizPA/start_bizpa_tax_readiness_ui.ps1` to print both the normal quarter-close URL and an export-ready verification URL.
+- Updated `bizPA/tax_readiness_ui_smoke.ps1` to save screenshots directly to the epic verification folder for the blocked and zero-blocker states.
+
+## Validation
+- `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Pass. Output included:
+    - `Tax readiness dashboard URL: http://127.0.0.1:3002/?readinessDemo=1&tab=quarter`
+    - `Export-ready verification URL: http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all`
+- `npm.cmd test -- --runInBand --watchAll=false` in `C:\Users\edebe\eds\bizPA\frontend`
+  - Pass. `4` suites passed, `14` tests passed.
+- `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Pass. Output included:
+    - `FRONTEND_STATUS=200`
+    - `SCREENSHOT_QUARTER=C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+    - `SCREENSHOT_EXPORT_READY=C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+- User verification request: To be issued with the completion response for explicit pass/fail on the quarter headline, Finish Now queue, and export gating behavior.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The immediate blocker-clear behavior is implemented in the demo/local verification path so the export gating can be proven end-to-end without backend mutation setup.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+Awaiting user verification - 2026-03-18 18:20
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

codex
Implemented the quarter-close screen changes in [App.jsx](C:/Users/edebe/eds/bizPA/frontend/src/App.jsx), added the live-state helper and tests in [quarterReadiness.js](C:/Users/edebe/eds/bizPA/frontend/src/quarterReadiness.js) and [quarterReadiness.test.js](C:/Users/edebe/eds/bizPA/frontend/src/quarterReadiness.test.js), and updated the local review scripts in [start_bizpa_tax_readiness_ui.ps1](C:/Users/edebe/eds/bizPA/start_bizpa_tax_readiness_ui.ps1) and [tax_readiness_ui_smoke.ps1](C:/Users/edebe/eds/bizPA/tax_readiness_ui_smoke.ps1). The quarter view now shows the headline as `% ready — items left`, includes an inline ordered Finish Now queue, and flips export state immediately in demo mode when blockers are cleared. The lifecycle file is updated and remains in progress at [20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md](C:/Users/edebe/eds/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md).

Validation passed:
- `npm.cmd test -- --runInBand --watchAll=false`
- `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
- `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`

Screenshots were captured at [quarter_readiness_screen.png](C:/Users/edebe/eds/ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first/verification/quarter_readiness_screen.png) and [finish_now_queue_zero_blockers.png](C:/Users/edebe/eds/ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first/verification/finish_now_queue_zero_blockers.png).

User verification is still required before this can move to `300_complete`. Please run:
- `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`

Then give pass/fail for:
1. The quarter screen shows the readiness headline in the `92% ready — 8 items left` style using live data.
2. The inline Finish Now queue is visible and ordered on the quarter screen.
3. Export is disabled with blockers and becomes enabled immediately at zero blockers, including the export-ready URL the script prints.
file update:
diff --git a/bizPA/frontend/src/App.jsx b/bizPA/frontend/src/App.jsx
index 0d42ee3cd34a22dd7f14608cc7ba0b410f4cd042..b0b6a961dcb80123d7634a661b59a1e944c1c7a1
--- a/bizPA/frontend/src/App.jsx
+++ b/bizPA/frontend/src/App.jsx
@@ -46,6 +46,10 @@
   DEFAULT_ACTIVE_SESSIONS,
   DEFAULT_SYNC_TELEMETRY,
 } from './controlCentre';
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
 import OnboardingGate from './OnboardingGate';
 import {
   ONBOARDING_SESSION_KEY,
@@ -66,6 +70,12 @@
   { key: 'missing_split_pct', label: 'Split' },
   { key: 'unresolved_duplicate', label: 'Duplicate' },
 ];
+const INBOX_REASON_LABELS = {
+  missing_category: 'Category missing',
+  missing_business_personal: 'Business or personal pending',
+  missing_split_pct: 'Split percentage missing',
+  unresolved_duplicate: 'Duplicate unresolved',
+};
 const INBOX_MERCHANT_SUGGESTIONS = [
   { pattern: /fuel|shell|esso|bp/i, category_code: 'MOTOR', category_name: 'Motor expenses' },
   { pattern: /builder|depot|screwfix|toolstation/i, category_code: 'MATERIALS', category_name: 'Materials' },
@@ -488,6 +498,12 @@
   .readiness-panel { background: var(--bg-card); border: 1px solid var(--border); border-radius: 24px; padding: 20px; box-shadow: var(--shadow); }
   .readiness-panel h5, .readiness-panel h6, .readiness-panel p { margin-top: 0; }
   .readiness-side-stack { display: grid; gap: 14px; }
+  .finish-now-list { display: grid; gap: 12px; }
+  .finish-now-card { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 14px; align-items: center; border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); padding: 16px; }
+  .finish-now-order { width: 40px; height: 40px; border-radius: 14px; background: rgba(13,92,99,0.12); display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--primary-strong); }
+  .finish-now-body { display: grid; gap: 10px; min-width: 0; }
+  .finish-now-actions { display: flex; align-items: center; }
+  .status-pill-success { color: var(--success); border-color: rgba(45,143,91,0.26); background: rgba(45,143,91,0.1); }
   .severity-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .severity-card, .history-card, .issue-row, .timeline-row { border-radius: 18px; border: 1px solid var(--border); background: var(--chip-bg); }
   .severity-card { padding: 14px; }
@@ -605,8 +621,9 @@
   .header-select { min-width: 210px; border-radius: 999px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text-main); padding: 10px 14px; }
   @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
   @media (max-width: 860px) {
-    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid { grid-template-columns: 1fr; }
+    .capture-grid, .preview-field-grid, .capture-field-grid, .command-shell, .performance-grid, .hero-metrics, .readiness-layout, .readiness-stat-grid, .severity-grid, .diff-impact-grid, .control-centre-layout, .control-hero-grid, .health-stat-grid, .control-grid-two, .inbox-mobile-stats, .inbox-action-grid, .finish-now-card { grid-template-columns: 1fr; }
     .diff-columns { display: grid; }
+    .finish-now-actions { justify-content: stretch; }
   }
   @media (max-width: 640px) {
     .capture-btn-container { right: 12px; bottom: 84px; }
@@ -1226,6 +1243,8 @@
     && searchParams?.get('readinessDemo') === '1';
   const snapshotDemoEnabled = typeof window !== 'undefined'
     && searchParams?.get('snapshotDemo') === '1';
+  const inboxDemoEnabled = typeof window !== 'undefined'
+    && searchParams?.get('inboxDemo') === '1';
   const requestedTab = searchParams?.get('tab');
   const [items, setItems] = useState([]);
   const [upcomingItems, setUpcomingItems] = useState([]);
@@ -1267,6 +1286,10 @@
   const [collapsedDates, setCollapsedDates] = useState({});
   const [inboxItems, setInboxItems] = useState([]);
   const [quarterReadiness, setQuarterReadiness] = useState(null);
+  const [resolvedQuarterIssueIds, setResolvedQuarterIssueIds] = useState(() => deriveResolvedIssueIdsFromSearchParams(
+    searchParams,
+    READINESS_DEMO_REPORT.issue_list,
+  ));
   const [snapshotWorkspace, setSnapshotWorkspace] = useState(null);
   const [selectedSnapshotId, setSelectedSnapshotId] = useState(null);
   const [quarterActionMessage, setQuarterActionMessage] = useState('');
@@ -1282,6 +1305,11 @@
   const [previewMode, setPreviewMode] = useState('review');
   const [previewBusy, setPreviewBusy] = useState(false);
   const [previewError, setPreviewError] = useState('');
+  const [inboxFilter, setInboxFilter] = useState('all');
+  const [inboxDrafts, setInboxDrafts] = useState({});
+  const [inboxBusyId, setInboxBusyId] = useState('');
+  const [inboxMessage, setInboxMessage] = useState(null);
+  const [inboxLoadMs, setInboxLoadMs] = useState(null);
   const finalTranscriptRef = useRef('');
   const webRecognitionRef = useRef(null);
   const quarterStart = '2026-01-01';
@@ -1293,11 +1321,12 @@
       if (savedTheme) {
         setTheme(savedTheme);
       }
-      if (readinessDemoEnabled || snapshotDemoEnabled) {
+      if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
         setQuarterReadiness(READINESS_DEMO_REPORT);
         setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setInboxLoadMs(120);
         setSnapshotWorkspace(buildDemoSnapshotWorkspace());
-        setCurrentTab('quarter');
+        setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
         setLoading(false);
         setAuthBootstrapped(true);
         setConnectionError(null);
@@ -1336,7 +1365,7 @@
       fetchAllData();
     };
     load();
-  }, [currentUser, readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [currentUser, inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (sessionToken) {
@@ -1382,10 +1411,10 @@
   }, [captureDemoEnabled, commandCentreDemoEnabled, requestedTab]);
 
   useEffect(() => {
-    if (readinessDemoEnabled || snapshotDemoEnabled) {
-      setCurrentTab('quarter');
+    if (readinessDemoEnabled || snapshotDemoEnabled || inboxDemoEnabled) {
+      setCurrentTab(requestedTab || (inboxDemoEnabled ? 'inbox' : 'quarter'));
     }
-  }, [readinessDemoEnabled, snapshotDemoEnabled]);
+  }, [inboxDemoEnabled, readinessDemoEnabled, requestedTab, snapshotDemoEnabled]);
 
   useEffect(() => {
     if (!snapshotWorkspace?.snapshots?.length) {
@@ -1399,7 +1428,7 @@
   }, [selectedSnapshotId, snapshotWorkspace]);
 
   const loadQuarterWorkspace = async (readinessReport = null) => {
-    if (snapshotDemoEnabled || readinessDemoEnabled) {
+    if (snapshotDemoEnabled || readinessDemoEnabled || inboxDemoEnabled) {
       return buildDemoSnapshotWorkspace();
     }
 
@@ -1424,6 +1453,9 @@
   const fetchAllData = async (activeUser = currentUser) => {
     try {
       axios.defaults.headers.common['X-User-ID'] = activeUser.id;
+      const inboxLoadStartedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       const endpoints = [
         'items',
         'upcoming',
@@ -1441,6 +1473,9 @@
       const responses = await Promise.all(
         endpoints.map((endpoint) => axios.get(`${API_BASE_URL}/${endpoint}`).catch(() => ({ data: [] }))),
       );
+      const inboxLoadFinishedAt = typeof performance !== 'undefined' && typeof performance.now === 'function'
+        ? performance.now()
+        : Date.now();
       setItems(Array.isArray(responses[0].data) ? responses[0].data : []);
       setUpcomingItems(Array.isArray(responses[1].data) ? responses[1].data : []);
       setNotifications(Array.isArray(responses[3].data) ? responses[3].data : []);
@@ -1452,6 +1487,7 @@
       setDiaryEntries(Array.isArray(responses[9].data) ? responses[9].data : []);
       setInboxItems(Array.isArray(responses[10].data) ? responses[10].data : []);
       setQuarterReadiness(responses[11].data || null);
+      setInboxLoadMs(Math.round(inboxLoadFinishedAt - inboxLoadStartedAt));
       const nextWorkspace = await loadQuarterWorkspace(responses[11].data || null);
       setSnapshotWorkspace(nextWorkspace);
 
@@ -1469,6 +1505,26 @@
     }
   };
 
+  const quarterUiState = buildQuarterUiState({
+    report: quarterReadiness || READINESS_DEMO_REPORT,
+    fallbackIssues: inboxItems,
+    resolvedIssueIds: resolvedQuarterIssueIds,
+  });
+  const activeQuarterReport = quarterUiState.report || READINESS_DEMO_REPORT;
+  const activeFinishNowQueue = quarterUiState.activeIssues;
+
+  const resolveQuarterIssue = (issueId) => {
+    if (!issueId || (!readinessDemoEnabled && !snapshotDemoEnabled)) {
+      return;
+    }
+
+    setResolvedQuarterIssueIds((previous) => (
+      previous.includes(issueId) ? previous : [...previous, issueId]
+    ));
+    setQuarterActionMessage(`Blocker ${issueId} cleared in demo mode. Export state updated immediately.`);
+    setQuarterActionError('');
+  };
+
   const toggleTheme = async () => {
     const nextTheme = theme === 'light' ? 'dark' : 'light';
     setTheme(nextTheme);
@@ -1476,6 +1532,10 @@
   };
 
   const dismissInsight = (id) => setInsights((previous) => previous.filter((insight) => insight.id !== id));
+  const inboxCards = buildInboxCards(activeFinishNowQueue, activeQuarterReport);
+  const filteredInboxCards = inboxCards.filter((card) => inboxFilter === 'all' || card.issue_types.includes(inboxFilter));
+  const highSeverityCount = inboxCards.filter((card) => card.severity === 'high').length;
+  const readyToExportCount = Math.max(0, (activeQuarterReport?.total_txns_in_period || READINESS_DEMO_REPORT.total_txns_in_period) - inboxCards.length);
 
   const currentRoleLabel = ROLE_LABELS[currentUser.role] || currentUser.role;
   const currentPermissions = getPermissionsForRole(currentUser.role);
@@ -1487,7 +1547,7 @@
     policyRecords,
     syncTelemetry,
     sessions: activeSessions,
-    inboxItems,
+    inboxItems: activeFinishNowQueue,
     governanceAudit,
   });
 
@@ -1573,6 +1633,192 @@
     setPreviewForm((previous) => ({ ...previous, [field]: value }));
   };
 
+  const setInboxDraftField = (itemId, field, value) => {
+    setInboxDrafts((previous) => {
+      const baseCard = inboxCards.find((card) => card.id === itemId);
+      const currentDraft = previous[itemId] || (baseCard ? buildInboxDraft(baseCard) : buildInboxDraft({}));
+      const nextDraft = { ...currentDraft, [field]: value };
+      if (field === 'business_personal' && value !== 'SPLIT') {
+        nextDraft.is_split = false;
+        nextDraft.split_business_pct = '';
+      }
+      return { ...previous, [itemId]: nextDraft };
+    });
+  };
+
+  const clearInboxMessage = () => setInboxMessage(null);
+
+  const syncInboxCardLocally = (itemId, updates = {}) => {
+    setInboxItems((previous) => previous
+      .map((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return item;
+        }
+        return { ...item, ...updates };
+      })
+      .filter((item) => {
+        const currentId = String(item.affected_entity_id || item.id || '');
+        if (currentId !== String(itemId)) {
+          return true;
+        }
+        const nextIssueType = item.issue_type || item.blocker_reason;
+        if (nextIssueType === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (nextIssueType === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (nextIssueType === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (nextIssueType === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      }));
+
+    setQuarterReadiness((previous) => {
+      if (!previous) {
+        return previous;
+      }
+      const nextIssueList = (previous.issue_list || []).filter((issue) => {
+        if (String(issue.affected_entity_id) !== String(itemId)) {
+          return true;
+        }
+        if (issue.issue_type === 'missing_category') {
+          return !updates.category_code;
+        }
+        if (issue.issue_type === 'missing_business_personal') {
+          return !updates.business_personal;
+        }
+        if (issue.issue_type === 'missing_split_pct') {
+          return !(updates.is_split === true && (updates.split_business_pct === null || updates.split_business_pct === undefined || updates.split_business_pct === ''));
+        }
+        if (issue.issue_type === 'unresolved_duplicate') {
+          return !updates.duplicate_resolution;
+        }
+        return true;
+      });
+      return {
+        ...previous,
+        issue_list: nextIssueList,
+        blocking_txns_count: new Set(nextIssueList.map((issue) => issue.affected_entity_id)).size,
+      };
+    });
+  };
+
+  const applySuggestedInboxCategory = (card) => {
+    const suggestion = card.suggested_category || {};
+    if (!suggestion.category_code && !suggestion.category_name) {
+      setInboxMessage({ type: 'error', text: `No suggested category is available for ${card.merchant}.` });
+      return;
+    }
+    setInboxDrafts((previous) => ({
+      ...previous,
+      [card.id]: {
+        ...(previous[card.id] || buildInboxDraft(card)),
+        category_code: suggestion.category_code || '',
+        category_name: suggestion.category_name || '',
+      },
+    }));
+    setInboxMessage({ type: 'success', text: `Suggested category applied for ${card.merchant}.` });
+  };
+
+  const undoLastInboxAction = async () => {
+    clearInboxMessage();
+    try {
+      setInboxBusyId('undo-last');
+      if (inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled) {
+        setInboxItems(READINESS_DEMO_REPORT.issue_list);
+        setQuarterReadiness(READINESS_DEMO_REPORT);
+        setInboxDrafts({});
+        setInboxMessage({ type: 'success', text: 'Demo queue reset to the original blocker set.' });
+        return;
+      }
+      await axios.post(`${API_BASE_URL}/inbox/undo-last`);
+      await fetchAllData();
+      setInboxMessage({ type: 'success', text: 'Last triage action undone.' });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || 'Unable to undo the last triage action.' });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
+  const submitInboxAction = async (card) => {
+    clearInboxMessage();
+    const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+    const shouldClassify = card.supports_category || card.supports_business_personal || card.supports_split;
+    const shouldResolveDuplicate = card.supports_duplicate;
+    const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+    const splitPctValue = draft.split_business_pct === '' ? null : Number(draft.split_business_pct);
+    if (card.supports_category && !draft.category_code.trim()) {
+      setInboxMessage({ type: 'error', text: `Add a category for ${card.merchant} before saving.` });
+      return;
+    }
+    if (card.supports_business_personal && !draft.business_personal) {
+      setInboxMessage({ type: 'error', text: `Choose business or personal for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && !Number.isFinite(splitPctValue)) {
+      setInboxMessage({ type: 'error', text: `Enter a split percentage for ${card.merchant}.` });
+      return;
+    }
+    if ((card.supports_split || isSplit) && (splitPctValue < 0 || splitPctValue > 100)) {
+      setInboxMessage({ type: 'error', text: 'Split percentage must be between 0 and 100.' });
+      return;
+    }
+    if (card.supports_duplicate && draft.duplicate_action === 'merge' && !draft.duplicate_of_txn_id.trim()) {
+      setInboxMessage({ type: 'error', text: `Enter the target transaction id to merge ${card.merchant}.` });
+      return;
+    }
+
+    try {
+      setInboxBusyId(card.id);
+      if (shouldClassify) {
+        const classificationPayload = {
+          category_code: draft.category_code.trim() || null,
+          category_name: draft.category_name.trim() || null,
+          business_personal: draft.business_personal === 'SPLIT' ? 'BUSINESS' : (draft.business_personal || null),
+          is_split: Boolean(isSplit),
+          split_business_pct: isSplit ? splitPctValue : null,
+          source: inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled ? 'demo' : 'manual',
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.patch(`${API_BASE_URL}/inbox/${card.id}/classification`, classificationPayload);
+        }
+        syncInboxCardLocally(card.id, classificationPayload);
+      }
+
+      if (shouldResolveDuplicate) {
+        const duplicatePayload = {
+          action: draft.duplicate_action,
+          duplicate_of_txn_id: draft.duplicate_action === 'merge' ? draft.duplicate_of_txn_id.trim() : null,
+        };
+        if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+          await axios.post(`${API_BASE_URL}/inbox/${card.id}/duplicate-resolution`, duplicatePayload);
+        }
+        syncInboxCardLocally(card.id, { duplicate_resolution: duplicatePayload.action });
+      }
+
+      if (!(inboxDemoEnabled || readinessDemoEnabled || snapshotDemoEnabled)) {
+        await fetchAllData();
+      }
+
+      setInboxDrafts((previous) => {
+        const next = { ...previous };
+        delete next[card.id];
+        return next;
+      });
+      setInboxMessage({ type: 'success', text: `${card.merchant} cleared from the finish-now queue.` });
+    } catch (error) {
+      setInboxMessage({ type: 'error', text: error?.response?.data?.error || `Unable to update ${card.merchant}.` });
+    } finally {
+      setInboxBusyId('');
+    }
+  };
+
   const toggleListening = async () => {
     if (connectionError) {
       alert('Cannot start voice capture while backend is disconnected.');
@@ -1858,8 +2104,8 @@
     const unpaidInvoices = items.filter((item) => item.type === 'invoice' && item.payment_status !== 'paid');
     const invoiceCount = items.filter((item) => item.type === 'invoice').length;
     const receiptCount = items.filter((item) => item.type === 'receipt').length;
-    const quarterPct = quarterReadiness?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
-    const blockerCount = inboxItems.length || quarterReadiness?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
+    const quarterPct = activeQuarterReport?.readiness_pct ?? demoReadiness?.readiness_pct ?? 0;
+    const blockerCount = activeFinishNowQueue.length || activeQuarterReport?.blocking_txns_count || demoReadiness?.blocking_txns_count || 0;
     const openAttentionCount = notifications.length + upcomingItems.length + blockerCount;
     const liveVatPayable = vatSummary?.vat_boxes?.payable;
 
@@ -2316,29 +2562,241 @@
     </div>
   );
 
-  const renderInbox = () => (
-    <div>
-      <h4>Finish Now Queue</h4>
-      <small className="text-muted">Resolve blockers only: category, business/personal, split %, duplicate resolution.</small>
-      <div className="mt-3">
-        {inboxItems.length === 0 && <div className="alert alert-success">No blocking items.</div>}
-        {inboxItems.map((item) => (
-          <div key={item.id} className="attention-item">
-            <div>
-              <b>{item.merchant || 'Unknown'}</b>
-              <br />
-              <small>
-                {item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date} | {item.direction} | {item.amount}
-              </small>
-              <br />
-              <small className="text-danger">Blocker: {item.blocker_reason}</small>
-            </div>
+  const renderInbox = () => {
+    return (
+    <div className="inbox-mobile-shell">
+      <section className="inbox-mobile-hero">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2">
+          <div className="inbox-kicker">
+            <Bell size={14} />
+            Mobile-first exception queue
           </div>
-        ))}
-      </div>
-    </div>
-  );
+          <div className="status-pill">{filteredInboxCards.length} visible</div>
+        </div>
+        <h4>Finish now</h4>
+        <p>
+          Clear the blockers that stop the quarterly export: category, business or personal, split treatment, and duplicate resolution.
+        </p>
+        <div className="inbox-mobile-stats">
+          <div className="inbox-mobile-stat">
+            <small>Queue</small>
+            <strong>{inboxCards.length}</strong>
+            <small>{activeQuarterReport.quarter_reference || READINESS_DEMO_REPORT.quarter_reference}</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Critical</small>
+            <strong>{highSeverityCount}</strong>
+            <small>High-severity blockers</small>
+          </div>
+          <div className="inbox-mobile-stat">
+            <small>Ready now</small>
+            <strong>{readyToExportCount}</strong>
+            <small>Transactions already export-safe</small>
+          </div>
+        </div>
+        <div className="hero-actions">
+          <button className="hero-action-main" onClick={() => setCurrentTab('quarter')}>
+            <ArrowRight size={16} />
+            Review quarter readiness
+          </button>
+          <button className="hero-action-secondary" onClick={undoLastInboxAction} disabled={inboxBusyId === 'undo-last'}>
+            {inboxBusyId === 'undo-last' ? <RefreshCcw size={16} className="spinner" /> : <ArchiveRestore size={16} />}
+            Undo last action
+          </button>
+        </div>
+      </section>
+
+      {inboxMessage && (
+        <div className={`readiness-panel inbox-message ${inboxMessage.type}`}>
+          {inboxMessage.text}
+        </div>
+      )}
+
+      <section className="readiness-panel">
+        <div className="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-3">
+          <div>
+            <h5 style={{ marginBottom: 4 }}>Queue filters</h5>
+            <small className="small-muted">Keep the mobile pass focused on one blocker type at a time.</small>
+          </div>
+          <div className="status-pill">{activeQuarterReport.readiness_pct ?? READINESS_DEMO_REPORT.readiness_pct}% ready</div>
+        </div>
+        <div className="inbox-filter-row">
+          {INBOX_FILTERS.map((filter) => {
+            const count = filter.key === 'all'
+              ? inboxCards.length
+              : inboxCards.filter((card) => card.issue_types.includes(filter.key)).length;
+            return (
+              <button
+                key={filter.key}
+                type="button"
+                className={`inbox-filter-chip ${inboxFilter === filter.key ? 'active' : ''}`}
+                onClick={() => setInboxFilter(filter.key)}
+              >
+                <span>{filter.label}</span>
+                <small>{count}</small>
+              </button>
+            );
+          })}
+        </div>
+      </section>
+
+      {filteredInboxCards.length === 0 ? (
+        <div className="inbox-empty">
+          No blockers match this filter. Reset to All blockers or return to the quarter screen to confirm readiness.
+        </div>
+      ) : (
+        <div className="inbox-card-list">
+          {filteredInboxCards.map((card) => {
+            const draft = inboxDrafts[card.id] || buildInboxDraft(card);
+            const saving = inboxBusyId === card.id;
+            const isSplit = draft.business_personal === 'SPLIT' || draft.is_split;
+            return (
+              <section key={card.id} className="inbox-action-card">
+                <div className="inbox-card-top">
+                  <div>
+                    <strong>{card.merchant}</strong>
+                    <div className="inbox-card-subline">
+                      {card.txn_date ? String(card.txn_date).slice(0, 10) : 'Date pending'} · {formatIssueAmount(card.amount, card.direction)}
+                    </div>
+                  </div>
+                  <span className={`inbox-issue-chip ${card.severity}`}>
+                    <TriangleAlert size={14} />
+                    {INBOX_REASON_LABELS[card.blocker_reason] || card.blocker_reason.replace(/_/g, ' ')}
+                  </span>
+                </div>
+
+                <div className="inbox-chip-row">
+                  {card.issue_labels.map((label) => (
+                    <span key={`${card.id}-${label}`} className={`inbox-issue-chip ${card.severity}`}>
+                      {label}
+                    </span>
+                  ))}
+                  {card.suggested_category?.category_name && (
+                    <button type="button" className="inbox-suggestion-chip" onClick={() => applySuggestedInboxCategory(card)}>
+                      <CheckCircle2 size={14} />
+                      Suggested: {card.suggested_category.category_name}
+                    </button>
+                  )}
+                </div>
+
+                <p className="inbox-explainer">{card.explanation || 'Resolve the blocking fields to clear this transaction from the queue.'}</p>
+
+                {(card.supports_category || card.supports_business_personal || card.supports_split) && (
+                  <div className="inbox-action-grid">
+                    <div className="inbox-field">
+                      <label>Category code</label>
+                      <input
+                        value={draft.category_code}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_code', event.target.value)}
+                        placeholder="MATERIALS"
+                      />
+                    </div>
+                    <div className="inbox-field">
+                      <label>Category label</label>
+                      <input
+                        value={draft.category_name}
+                        onChange={(event) => setInboxDraftField(card.id, 'category_name', event.target.value)}
+                        placeholder="Materials"
+                      />
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_business_personal || card.supports_split) && (
+                  <div>
+                    <div className="inbox-field">
+                      <label>Business treatment</label>
+                    </div>
+                    <div className="inbox-toggle-row">
+                      {[
+                        { value: 'BUSINESS', label: 'Business' },
+                        { value: 'PERSONAL', label: 'Personal' },
+                        { value: 'SPLIT', label: 'Split' },
+                      ].map((option) => (
+                        <button
+                          key={option.value}
+                          type="button"
+                          className={`inbox-toggle ${draft.business_personal === option.value ? 'active' : ''}`}
+                          onClick={() => setInboxDraftField(card.id, 'business_personal', option.value)}
+                        >
+                          {option.label}
+                        </button>
+                      ))}
+                    </div>
+                  </div>
+                )}
+
+                {(card.supports_split || isSplit) && (
+                  <div className="inbox-field">
+                    <label>Business split %</label>
+                    <input
+                      type="number"
+                      min="0"
+                      max="100"
+                      value={draft.split_business_pct}
+                      onChange={(event) => setInboxDraftField(card.id, 'split_business_pct', event.target.value)}
+                      placeholder="80"
+                    />
+                  </div>
+                )}
+
+                {card.supports_duplicate && (
+                  <>
+                    <div>
+                      <div className="inbox-field">
+                        <label>Duplicate action</label>
+                      </div>
+                      <div className="inbox-duplicate-row">
+                        {[
+                          { value: 'dismiss', label: 'Dismiss duplicate' },
+                          { value: 'merge', label: 'Merge to original' },
+                        ].map((option) => (
+                          <button
+                            key={option.value}
+                            type="button"
+                            className={`inbox-toggle ${draft.duplicate_action === option.value ? 'active' : ''}`}
+                            onClick={() => setInboxDraftField(card.id, 'duplicate_action', option.value)}
+                          >
+                            {option.label}
+                          </button>
+                        ))}
+                      </div>
+                    </div>
+                    {draft.duplicate_action === 'merge' && (
+                      <div className="inbox-field">
+                        <label>Original transaction id</label>
+                        <input
+                          value={draft.duplicate_of_txn_id}
+                          onChange={(event) => setInboxDraftField(card.id, 'duplicate_of_txn_id', event.target.value)}
+                          placeholder="txn-9001"
+                        />
+                      </div>
+                    )}
+                  </>
+                )}
 
+                <div className="inbox-footer-row">
+                  <div className="inbox-inline-status">
+                    Route: {card.resolution_target?.route || `/api/v1/inbox/${card.id}`}
+                  </div>
+                  <div className="inbox-footer-actions">
+                    <button className="secondary-action" onClick={clearInboxMessage} disabled={saving}>
+                      Clear banner
+                    </button>
+                    <button className="primary-action" onClick={() => submitInboxAction(card)} disabled={saving}>
+                      {saving ? <RefreshCcw size={16} className="spinner" /> : <CheckCircle2 size={16} />}
+                      Save and clear blocker
+                    </button>
+                  </div>
+                </div>
+              </section>
+            );
+          })}
+        </div>
+      )}
+    </div>
+    );
+  };
   const renderControlCentre = () => {
     const policyLabels = {
       vat_scheme: 'VAT scheme',
@@ -2510,11 +2968,11 @@
           <section className="readiness-panel">
             <h5 style={{ marginBottom: 12 }}>Queue Backlog</h5>
             <div className="control-list">
-              {inboxItems.slice(0, 4).map((item) => (
-                <div key={item.id} className="control-row">
+              {activeFinishNowQueue.slice(0, 4).map((item) => (
+                <div key={item.id || item.affected_entity_id} className="control-row">
                   <div>
                     <strong>{item.merchant || 'Unknown merchant'}</strong>
-                    <div className="small-muted">{item.blocker_reason}</div>
+                    <div className="small-muted">{item.blocker_reason || item.issue_type}</div>
                   </div>
                   <div className="text-end">
                     <div className="small-muted">{item.txn_date?.slice ? item.txn_date.slice(0, 10) : item.txn_date}</div>
@@ -2522,7 +2980,7 @@
                   </div>
                 </div>
               ))}
-              {inboxItems.length === 0 && (
+              {activeFinishNowQueue.length === 0 && (
                 <div className="readiness-note">No finish-now blockers are queued.</div>
               )}
             </div>
@@ -2668,7 +3126,7 @@
     || null;
 
   const refreshQuarterWorkspace = async () => {
-    const nextWorkspace = await loadQuarterWorkspace(quarterReadiness || READINESS_DEMO_REPORT);
+    const nextWorkspace = await loadQuarterWorkspace(activeQuarterReport || READINESS_DEMO_REPORT);
     setSnapshotWorkspace(nextWorkspace);
     return nextWorkspace;
   };
@@ -2697,7 +3155,7 @@
   };
 
   const createSnapshotVersion = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     const status = snapshotWorkspace?.snapshot_status;
 
     if (!status?.can_create_snapshot_version) {
@@ -2719,7 +3177,7 @@
         description: `Quarter snapshot generated from Quarter workspace`,
         metadata: {
           source_type: 'manual',
-          readiness_pct: quarterReadiness?.readiness_pct ?? null
+          readiness_pct: activeQuarterReport?.readiness_pct ?? null
         }
       });
       await refreshQuarterWorkspace();
@@ -2734,7 +3192,7 @@
   };
 
   const closeQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(closeReason || '').trim()) {
       setQuarterActionError('Close reason is required.');
       setQuarterActionMessage('');
@@ -2785,7 +3243,7 @@
   };
 
   const reopenQuarterWorkspace = async () => {
-    const quarterReference = snapshotWorkspace?.quarter_reference || quarterReadiness?.quarter_reference || 'Q1-2026';
+    const quarterReference = snapshotWorkspace?.quarter_reference || activeQuarterReport?.quarter_reference || 'Q1-2026';
     if (!String(reopenReason || '').trim()) {
       setQuarterActionError('Reopen reason is required.');
       setQuarterActionMessage('');
@@ -2844,9 +3302,9 @@
   };
 
   const renderQuarter = () => {
-    const report = quarterReadiness || READINESS_DEMO_REPORT;
+    const report = activeQuarterReport || READINESS_DEMO_REPORT;
     const issueSummary = Array.isArray(report?.issue_summary) ? report.issue_summary : [];
-    const issueList = Array.isArray(report?.issue_list) ? report.issue_list : [];
+    const issueList = activeFinishNowQueue;
     const severityBreakdown = buildSeverityBreakdown(issueSummary);
     const historicalSnapshots = buildHistoricalSnapshots(report);
     const quarterLabel = formatQuarterLabel(report?.quarter_reference, report?.period_start, report?.period_end);
@@ -2884,7 +3342,7 @@
               <Calendar size={14} />
               Active quarter readiness only
             </div>
-            <h3>{report?.readiness_pct ?? 0}% ready</h3>
+            <h3>{`${report?.readiness_pct ?? 0}% ready \u2014 ${issueList.length} item${issueList.length === 1 ? '' : 's'} left`}</h3>
             <p>
               {quarterLabel}. Historical snapshots remain visible below as read-only reference and do not change this live score.
             </p>
@@ -2896,7 +3354,7 @@
               </div>
               <div className="readiness-stat">
                 <small>Issues remaining</small>
-                <strong>{report?.blocking_txns_count ?? 0}</strong>
+                <strong>{issueList.length}</strong>
                 <small>{report?.total_txns_in_period ?? 0} transactions in active scope</small>
               </div>
               <div className="readiness-stat">
@@ -2907,10 +3365,10 @@
             </div>
             <div className="readiness-actions">
               <button className="primary-action" onClick={() => setCurrentTab('inbox')}>
-                Finish now queue
+                {issueList.length > 0 ? `Finish now (${issueList.length})` : 'Finish now cleared'}
               </button>
               <button className="secondary-action readiness-secondary" onClick={exportQuarterlyPack} disabled={!report?.can_export}>
-                Export quarterly pack
+                {report?.can_export ? 'Export quarterly pack' : 'Export locked until blockers clear'}
               </button>
             </div>
             <div className="readiness-explainer">
@@ -2921,6 +3379,59 @@
           </section>
 
           <section className="readiness-panel">
+            <div className="snapshot-card-head mb-3">
+              <div>
+                <h5 style={{ marginBottom: 4 }}>Finish Now Queue</h5>
+                <small className="small-muted">The blocking queue is ordered by severity and date so the next fix is obvious.</small>
+              </div>
+              <div className={`status-pill ${report?.can_export ? 'status-pill-success' : ''}`}>
+                {report?.can_export ? 'Export enabled' : `${issueList.length} blockers`}
+              </div>
+            </div>
+            <div className="finish-now-list">
+              {issueList.map((issue, index) => (
+                <div key={`${issue.affected_entity_id}-${issue.issue_type}`} className="finish-now-card">
+                  <div className="finish-now-order">{String(index + 1).padStart(2, '0')}</div>
+                  <div className="finish-now-body">
+                    <div className="issue-head">
+                      <div>
+                        <div className={`issue-pill ${issue.severity || 'low'}`}>{issue.severity || 'low'} severity</div>
+                        <h6 style={{ marginTop: 10, marginBottom: 6 }}>{issue.resolution_target?.label || issue.issue_type}</h6>
+                        <div className="small-muted">{issue.explanation}</div>
+                      </div>
+                      <div className="text-end">
+                        <strong>{formatIssueAmount(issue.amount, issue.direction)}</strong>
+                        <div className="small-muted">{issue.txn_date || 'Date pending'}</div>
+                      </div>
+                    </div>
+                    <div className="issue-meta">
+                      <span className="meta-chip">Merchant: {issue.merchant || 'Unknown'}</span>
+                      <span className="meta-chip">Route: {issue.resolution_target?.workflow || issue.resolution_target?.route || 'n/a'}</span>
+                      <span className="meta-chip">Weight: {issue.weight || 0}</span>
+                    </div>
+                  </div>
+                  <div className="finish-now-actions">
+                    {(readinessDemoEnabled || snapshotDemoEnabled) ? (
+                      <button className="primary-action" onClick={() => resolveQuarterIssue(issue.affected_entity_id)}>
+                        Mark fixed
+                      </button>
+                    ) : (
+                      <button className="primary-action" onClick={() => openReadinessWorkflow(issue)}>
+                        Open fix
+                      </button>
+                    )}
+                  </div>
+                </div>
+              ))}
+              {issueList.length === 0 && (
+                <div className="alert alert-success mb-0">
+                  Finish Now is clear. Export enabled immediately for the active quarter.
+                </div>
+              )}
+            </div>
+          </section>
+
+          <section className="readiness-panel">
             <div className="snapshot-toolbar mb-3">
               <div>
                 <h5 style={{ marginBottom: 4 }}>Snapshot Versions</h5>
diff --git a/bizPA/frontend/src/quarterReadiness.js b/bizPA/frontend/src/quarterReadiness.js
new file mode 100644
index 0000000000000000000000000000000000000000..669173cd6176ee9ee5c86c5895ea49cb9524ebc4
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.js
@@ -0,0 +1,155 @@
+const SEVERITY_ORDER = {
+  high: 0,
+  medium: 1,
+  low: 2,
+};
+
+const normalizeNumber = (value, fallback = 0) => {
+  const parsed = Number(value);
+  return Number.isFinite(parsed) ? parsed : fallback;
+};
+
+const sortIssues = (issues = []) => [...issues].sort((left, right) => {
+  const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+  if (severityDelta !== 0) {
+    return severityDelta;
+  }
+
+  const leftDate = String(left.txn_date || '');
+  const rightDate = String(right.txn_date || '');
+  if (leftDate !== rightDate) {
+    return leftDate.localeCompare(rightDate);
+  }
+
+  return String(left.affected_entity_id || '').localeCompare(String(right.affected_entity_id || ''));
+});
+
+const buildIssueSummary = (issues = []) => {
+  const groups = new Map();
+
+  issues.forEach((issue) => {
+    const key = issue.issue_type || issue.resolution_target?.label || 'unknown_issue';
+    const existing = groups.get(key);
+    if (existing) {
+      existing.count += 1;
+      existing.total_weight += normalizeNumber(issue.weight, 0);
+      return;
+    }
+
+    groups.set(key, {
+      issue_type: key,
+      label: issue.summary_label || issue.label || issue.resolution_target?.label || key.replace(/_/g, ' '),
+      severity: issue.severity || 'low',
+      count: 1,
+      weight: normalizeNumber(issue.weight, 0),
+      total_weight: normalizeNumber(issue.weight, 0),
+      explanation: issue.explanation || '',
+    });
+  });
+
+  return [...groups.values()].sort((left, right) => {
+    const severityDelta = (SEVERITY_ORDER[left.severity] ?? 99) - (SEVERITY_ORDER[right.severity] ?? 99);
+    if (severityDelta !== 0) {
+      return severityDelta;
+    }
+    return right.count - left.count;
+  });
+};
+
+const buildBlockersByReason = (issueSummary = []) => issueSummary.map((entry) => ({
+  reason: entry.issue_type,
+  label: entry.label,
+  count: entry.count,
+}));
+
+const buildExplanationLines = ({ readinessPct, report = {}, activeIssues = [], issueSummary = [] }) => {
+  const totalTransactions = normalizeNumber(report.total_txns_in_period, 0);
+  const blockerCount = activeIssues.length;
+  const quarterStart = report.period_start || 'current quarter start';
+  const quarterEnd = report.period_end || 'current quarter end';
+  const reasonSummary = issueSummary.length
+    ? issueSummary.map((entry) => `${entry.label} (${entry.count})`).join('; ')
+    : 'No blocking reasons remain.';
+
+  return [
+    `Readiness is ${readinessPct}% for ${quarterStart} to ${quarterEnd}.`,
+    blockerCount > 0
+      ? `${blockerCount} of ${totalTransactions} transactions are still blocking export.`
+      : 'No blocking transactions remain. Export is ready now.',
+    `Blocking reasons: ${reasonSummary}`,
+  ];
+};
+
+export const deriveResolvedIssueIdsFromSearchParams = (searchParams, fallbackIssues = []) => {
+  const resolvedValue = searchParams?.get('resolvedDemoIssues');
+  if (!resolvedValue) {
+    return [];
+  }
+
+  if (resolvedValue === 'all') {
+    return fallbackIssues
+      .map((issue) => issue?.affected_entity_id)
+      .filter(Boolean);
+  }
+
+  return resolvedValue
+    .split(',')
+    .map((value) => value.trim())
+    .filter(Boolean);
+};
+
+export const buildQuarterUiState = ({ report = null, fallbackIssues = [], resolvedIssueIds = [] }) => {
+  if (!report) {
+    return {
+      report: null,
+      activeIssues: [],
+      headline: '0% ready - 0 items left',
+      itemsLeft: 0,
+      exportEnabled: false,
+    };
+  }
+
+  const issueSource = Array.isArray(report.issue_list) && report.issue_list.length > 0
+    ? report.issue_list
+    : fallbackIssues;
+  const allIssues = sortIssues(Array.isArray(issueSource) ? issueSource : []);
+  const resolvedSet = new Set(resolvedIssueIds);
+  const activeIssues = allIssues.filter((issue) => !resolvedSet.has(issue.affected_entity_id));
+  const originalPenalty = allIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const activePenalty = activeIssues.reduce((sum, issue) => sum + normalizeNumber(issue.weight, 0), 0);
+  const baseReadiness = normalizeNumber(report.readiness_pct, 0);
+  const readinessLoss = Math.max(0, 100 - baseReadiness);
+  const readinessPct = activeIssues.length === 0
+    ? 100
+    : originalPenalty > 0
+      ? Math.max(0, Math.min(100, Math.round(100 - ((activePenalty / originalPenalty) * readinessLoss))))
+      : baseReadiness;
+  const issueSummary = buildIssueSummary(activeIssues);
+  const blockersByReason = buildBlockersByReason(issueSummary);
+  const itemsLeft = activeIssues.length;
+  const exportEnabled = itemsLeft === 0;
+  const headline = `${readinessPct}% ready - ${itemsLeft} item${itemsLeft === 1 ? '' : 's'} left`;
+
+  return {
+    report: {
+      ...report,
+      readiness_pct: readinessPct,
+      blocking_txns_count: itemsLeft,
+      can_export: exportEnabled,
+      issue_list: activeIssues,
+      issue_count: itemsLeft,
+      issue_summary: issueSummary,
+      blockers_by_reason: blockersByReason,
+      explanation_lines: buildExplanationLines({
+        readinessPct,
+        report,
+        activeIssues,
+        issueSummary,
+      }),
+    },
+    activeIssues,
+    headline,
+    itemsLeft,
+    exportEnabled,
+  };
+};
diff --git a/bizPA/frontend/src/quarterReadiness.test.js b/bizPA/frontend/src/quarterReadiness.test.js
new file mode 100644
index 0000000000000000000000000000000000000000..0b810191a89d8b82091bf5de1408afcc23c14e65
--- /dev/null
+++ b/bizPA/frontend/src/quarterReadiness.test.js
@@ -0,0 +1,93 @@
+import {
+  buildQuarterUiState,
+  deriveResolvedIssueIdsFromSearchParams,
+} from './quarterReadiness';
+
+const reportFixture = {
+  quarter_reference: 'Q1-2026',
+  period_start: '2026-01-01',
+  period_end: '2026-03-31',
+  total_txns_in_period: 18,
+  readiness_pct: 78,
+  can_export: false,
+  issue_list: [
+    {
+      issue_type: 'missing_category',
+      affected_entity_id: 'txn-9101',
+      severity: 'high',
+      weight: 35,
+      explanation: 'Category missing for Fuel Stop.',
+      txn_date: '2026-02-04',
+      merchant: 'Fuel Stop',
+      amount: 64.18,
+      direction: 'out',
+      resolution_target: {
+        label: 'Classify transaction',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'missing_business_personal',
+      affected_entity_id: 'txn-9103',
+      severity: 'high',
+      weight: 30,
+      explanation: 'Business/personal decision missing.',
+      txn_date: '2026-02-15',
+      merchant: 'Mobile Contract',
+      amount: 42.5,
+      direction: 'out',
+      resolution_target: {
+        label: 'Set business or personal',
+        workflow: '/inbox/finish-now',
+      },
+    },
+    {
+      issue_type: 'unresolved_duplicate',
+      affected_entity_id: 'txn-9104',
+      severity: 'medium',
+      weight: 15,
+      explanation: 'Duplicate not resolved.',
+      txn_date: '2026-03-02',
+      merchant: 'Client Payment',
+      amount: 820,
+      direction: 'in',
+      resolution_target: {
+        label: 'Resolve duplicate',
+        workflow: '/inbox/finish-now',
+      },
+    },
+  ],
+};
+
+describe('quarter readiness helpers', () => {
+  test('keeps blockers ordered and updates headline while blockers remain', () => {
+    const state = buildQuarterUiState({ report: reportFixture, resolvedIssueIds: ['txn-9104'] });
+
+    expect(state.itemsLeft).toBe(2);
+    expect(state.exportEnabled).toBe(false);
+    expect(state.report.readiness_pct).toBeGreaterThan(78);
+    expect(state.headline).toContain('items left');
+    expect(state.activeIssues.map((issue) => issue.affected_entity_id)).toEqual(['txn-9101', 'txn-9103']);
+    expect(state.report.explanation_lines[0]).toContain('Readiness is');
+  });
+
+  test('unlocks export immediately when every blocker is resolved', () => {
+    const state = buildQuarterUiState({
+      report: reportFixture,
+      resolvedIssueIds: ['txn-9101', 'txn-9103', 'txn-9104'],
+    });
+
+    expect(state.itemsLeft).toBe(0);
+    expect(state.exportEnabled).toBe(true);
+    expect(state.report.readiness_pct).toBe(100);
+    expect(state.report.can_export).toBe(true);
+    expect(state.report.issue_summary).toEqual([]);
+  });
+
+  test('supports demo query params that resolve all blockers', () => {
+    const params = new URLSearchParams('resolvedDemoIssues=all');
+    const resolvedIds = deriveResolvedIssueIdsFromSearchParams(params, reportFixture.issue_list);
+
+    expect(resolvedIds).toEqual(['txn-9101', 'txn-9103', 'txn-9104']);
+  });
+});
diff --git a/bizPA/start_bizpa_tax_readiness_ui.ps1 b/bizPA/start_bizpa_tax_readiness_ui.ps1
index 1fc4bef8cd172c31fdc3db52d1acc9d4775c5485..7216e44eac386fe436a9af8e9b417cd2d61e37fe
--- a/bizPA/start_bizpa_tax_readiness_ui.ps1
+++ b/bizPA/start_bizpa_tax_readiness_ui.ps1
@@ -4,6 +4,7 @@
 $frontendPath = Join-Path $repoRoot 'frontend'
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
 
 Write-Host 'Starting bizPA tax readiness UI...' -ForegroundColor Cyan
 
@@ -14,7 +15,10 @@
 Write-Host 'Tax readiness dashboard URL:' -ForegroundColor Green
 Write-Host "  $readinessUrl" -ForegroundColor Yellow
 Write-Host ''
+Write-Host 'Export-ready verification URL:' -ForegroundColor Green
+Write-Host "  $exportReadyUrl" -ForegroundColor Yellow
+Write-Host ''
 Write-Host 'Base frontend URL:' -ForegroundColor Green
 Write-Host "  $frontendUrl" -ForegroundColor Yellow
 Write-Host ''
-Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI can be reviewed without backend setup.' -ForegroundColor Cyan
+Write-Host 'This launch path uses the local readiness demo state so the active-quarter UI and zero-blocker export-ready state can be reviewed without backend setup.' -ForegroundColor Cyan
diff --git a/bizPA/tax_readiness_ui_smoke.ps1 b/bizPA/tax_readiness_ui_smoke.ps1
index 56d67892a51b22572503dec560d17123b370c2b7..47a50c8d3ac8ff8c933943a7c6a966cdce89328f
--- a/bizPA/tax_readiness_ui_smoke.ps1
+++ b/bizPA/tax_readiness_ui_smoke.ps1
@@ -28,99 +28,92 @@
 
 $frontendUrl = 'http://127.0.0.1:3002'
 $readinessUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter'
-$screenshotPath = 'C:\Users\edebe\eds\workstream\verification\20260311_201500_bizpa_tax_readiness_ui.png'
-$chromeProfilePath = 'C:\Users\edebe\eds\workstream\verification\chrome_capture_profile_tax_readiness'
-
-New-Item -ItemType Directory -Force -Path $chromeProfilePath | Out-Null
-
-$frontendJob = Start-Job -ScriptBlock {
-  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
-  $env:PORT = '3002'
-  npm.cmd start
-}
+$exportReadyUrl = 'http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all'
+$verificationRoot = 'C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification'
+$quarterScreenshotPath = Join-Path $verificationRoot 'quarter_readiness_screen.png'
+$finishNowScreenshotPath = Join-Path $verificationRoot 'finish_now_queue_zero_blockers.png'
+function Wait-FrontendReady {
+  param (
+    [string]$Url
+  )
 
-try {
-  $frontendResponse = $null
-  $frontendReady = $false
   for ($i = 0; $i -lt 45; $i++) {
     Start-Sleep -Seconds 2
     try {
-      $frontendResponse = Invoke-WebRequest -UseBasicParsing $readinessUrl
-      if ($frontendResponse.StatusCode -eq 200) {
-        $frontendReady = $true
-        break
+      $response = Invoke-WebRequest -UseBasicParsing $Url
+      if ($response.StatusCode -eq 200) {
+        return $response
       }
     } catch {
     }
   }
 
-  if (-not $frontendReady) {
-    throw "Frontend did not become ready on $readinessUrl"
-  }
+  throw "Frontend did not become ready on $Url"
+}
 
-  $chromeArgs = @(
-    "--user-data-dir=$chromeProfilePath",
-    '--new-window',
-    '--window-size=1600,1500',
-    '--no-first-run',
-    '--disable-session-crashed-bubble',
-    '--disable-infobars',
-    '--disable-breakpad',
-    '--disable-crash-reporter',
-    '--noerrdialogs',
-    "--app=$readinessUrl"
+function Capture-AppScreenshot {
+  param (
+    [string]$Url,
+    [string]$OutputPath
   )
-  Start-Process 'C:\Program Files\Google\Chrome\Application\chrome.exe' -ArgumentList $chromeArgs | Out-Null
 
-  $chromeWindow = $null
-  for ($i = 0; $i -lt 20; $i++) {
-    Start-Sleep -Seconds 1
-    $chromeWindow = Get-Process chrome -ErrorAction SilentlyContinue |
-      ForEach-Object {
-        try {
-          if ($_.MainWindowHandle -and ([int64]$_.MainWindowHandle -ne 0)) {
-            [pscustomobject]@{
-              Process = $_
-              StartTime = $_.StartTime
-            }
-          }
-        } catch {
-        }
-      } |
-      Sort-Object StartTime -Descending |
-      Select-Object -First 1
-    if ($chromeWindow) {
-      $chromeWindow = $chromeWindow.Process
+  $form = New-Object System.Windows.Forms.Form
+  $form.Width = 1600
+  $form.Height = 1500
+  $form.StartPosition = 'Manual'
+  $form.Location = New-Object System.Drawing.Point(-32000, -32000)
+
+  $browser = New-Object System.Windows.Forms.WebBrowser
+  $browser.ScriptErrorsSuppressed = $true
+  $browser.ScrollBarsEnabled = $false
+  $browser.Dock = [System.Windows.Forms.DockStyle]::Fill
+  $form.Controls.Add($browser)
+  $form.Show()
+  $browser.Navigate($Url)
+
+  $ready = $false
+  for ($i = 0; $i -lt 90; $i++) {
+    [System.Windows.Forms.Application]::DoEvents()
+    Start-Sleep -Milliseconds 250
+    if ($browser.ReadyState -eq [System.Windows.Forms.WebBrowserReadyState]::Complete) {
+      $ready = $true
       break
     }
   }
 
-  if (-not $chromeWindow) {
-    throw 'Chrome readiness window did not expose a main window handle for screenshot capture.'
+  if (-not $ready) {
+    $form.Close()
+    throw "Embedded browser did not finish loading $Url"
   }
 
-  [WindowCaptureNative]::ShowWindow($chromeWindow.MainWindowHandle, 3) | Out-Null
-  [WindowCaptureNative]::SetForegroundWindow($chromeWindow.MainWindowHandle) | Out-Null
-  Start-Sleep -Seconds 1
+  Start-Sleep -Seconds 3
+  [System.Windows.Forms.Application]::DoEvents()
 
-  $rect = New-Object WindowCaptureNative+RECT
-  [WindowCaptureNative]::GetWindowRect($chromeWindow.MainWindowHandle, [ref]$rect) | Out-Null
-  $width = [Math]::Max(1, $rect.Right - $rect.Left)
-  $height = [Math]::Max(1, $rect.Bottom - $rect.Top)
-  $bitmap = New-Object System.Drawing.Bitmap $width, $height
-  $graphics = [System.Drawing.Graphics]::FromImage($bitmap)
-  $graphics.CopyFromScreen($rect.Left, $rect.Top, 0, 0, $bitmap.Size)
-  $bitmap.Save($screenshotPath, [System.Drawing.Imaging.ImageFormat]::Png)
-  $graphics.Dispose()
+  $bitmap = New-Object System.Drawing.Bitmap $browser.Width, $browser.Height
+  $browser.DrawToBitmap($bitmap, (New-Object System.Drawing.Rectangle 0, 0, $browser.Width, $browser.Height))
+  $bitmap.Save($OutputPath, [System.Drawing.Imaging.ImageFormat]::Png)
   $bitmap.Dispose()
+  $form.Close()
+}
+
+New-Item -ItemType Directory -Force -Path $verificationRoot | Out-Null
+
+$frontendJob = Start-Job -ScriptBlock {
+  Set-Location 'C:\Users\edebe\eds\bizPA\frontend'
+  $env:PORT = '3002'
+  npm.cmd start
+}
 
-  Get-Process chrome -ErrorAction SilentlyContinue |
-    Where-Object { $_.StartTime -ge $chromeWindow.StartTime.AddSeconds(-2) } |
-    Stop-Process -Force -ErrorAction SilentlyContinue
+try {
+  $frontendResponse = Wait-FrontendReady -Url $readinessUrl
+  Capture-AppScreenshot -Url $readinessUrl -OutputPath $quarterScreenshotPath
+  Capture-AppScreenshot -Url $exportReadyUrl -OutputPath $finishNowScreenshotPath
 
   "READINESS_URL=$readinessUrl"
+  "EXPORT_READY_URL=$exportReadyUrl"
   "FRONTEND_STATUS=$($frontendResponse.StatusCode)"
-  "SCREENSHOT=$screenshotPath"
+  "SCREENSHOT_QUARTER=$quarterScreenshotPath"
+  "SCREENSHOT_EXPORT_READY=$finishNowScreenshotPath"
   'FRONTEND_LOG_START'
   Receive-Job $frontendJob -Keep -ErrorAction SilentlyContinue 2>&1
   'FRONTEND_LOG_END'
diff --git a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
index b5a00d3124e9081cb53f084d64d7e85890d10d57..067ce847896d101d973aab77be01c41e12976e3c
--- a/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
+++ b/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md
@@ -1,74 +1,151 @@
-# TASK E2: Deliver quarter readiness and Finish Now screen
-
-**Workstream:** E — Mobile UX And Voice
-**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
-**Priority:** 1
-**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
-**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
-**Suggested Agent:** codex
-**UI Deliverable:** Yes
-**Status:** [ ] Not Started
-**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
-
----
-
-## Purpose
-
-Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
-
-## Input
-
-C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
-
-## Output
-
-Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
-
-## Fields / Components
-
-- readiness_pct
-- items_left
-- finish_now_cta
-- blocking_queue
-- export_enabled
-
-## Dependencies
-
-- C1
-- C4
-
-## Action
-
-Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
-
-## Verification
-
-- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
-- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
-- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
-- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
-- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
-
----
-
-## Notes
-
-- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
-- This task is intended for Epic Review allocation before execution.
-- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
-
-
-## Retry History
-Retry-Count: 2
-- Retry scheduled at 2026-03-18 17:21:30
-
-
-## Execution Evidence
-- Agent lane: claude
-- Command: C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits
-- Return code: 1
-- Stdout:
-```text
-You've hit your limit · resets 7pm (Europe/London)
-```
-- Retry scheduled at 2026-03-18 17:30:31
+# TASK E2: Deliver quarter readiness and Finish Now screen
+
+Source: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+Task Summary: Implement the mobile-first quarter-close screen in `bizPA` so the active quarter shows a live readiness headline, remaining-item count, inline Finish Now queue, and export gating that updates immediately as blockers are cleared.
+Context: `bizPA/frontend/src/App.jsx`, `bizPA/start_bizpa_tax_readiness_ui.ps1`, `bizPA/tax_readiness_ui_smoke.ps1`, screenshot artifacts under `ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first`.
+Dependency: C1 quarter metrics and C4 export flow.
+
+## Plan
+- [x] 1. Normalize and confirm the local quarter-close launch path for review.
+  - [x] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`; pass when it prints the local review URL and launch instructions without script errors.
+  - Evidence: Script printed `http://127.0.0.1:3002/?readinessDemo=1&tab=quarter` and `http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all` on 2026-03-18.
+- [x] 2. Implement the quarter screen updates so the hero headline, item count, Finish Now queue, and export gating are driven by live UI state.
+  - [x] Test: `npm.cmd test -- --runInBand --watchAll=false` from `C:\Users\edebe\eds\bizPA\frontend`; pass when the targeted frontend tests covering quarter readiness state complete successfully.
+  - Evidence: `PASS src/quarterReadiness.test.js` plus `PASS` on the existing onboarding, control-centre, and governance suites on 2026-03-18.
+- [x] 3. Smoke-test the local quarter-close view and capture screenshots for quarter and Finish Now verification.
+  - [x] Test: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`; pass when the script reaches the local demo URL, captures screenshots, and exits without startup or render crashes.
+  - Evidence: Smoke script returned `FRONTEND_STATUS=200` and saved `quarter_readiness_screen.png` plus `finish_now_queue_zero_blockers.png` under the epic verification folder on 2026-03-18.
+
+## Evidence
+Objective-Delivery-Coverage: 90%
+Auto-Acceptance: false
+- Evidence-Type: demo
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: A reviewer can launch the quarter-close UI locally from a documented script and reach the intended localhost path.
+  - Status: captured
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+  - Objective-Proved: The quarter screen renders the live readiness headline and inline Finish Now queue.
+  - Status: captured
+- Evidence-Type: screenshot
+  - Artifact: `C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+  - Objective-Proved: Export gating changes visibly once blockers are cleared in the Finish Now workflow.
+  - Status: captured
+- Evidence-Type: test_output
+  - Artifact: `npm.cmd test -- --runInBand --watchAll=false`
+  - Objective-Proved: Quarter readiness state logic is covered by automated frontend validation.
+  - Status: captured
+- Evidence-Type: log_output
+  - Artifact: `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Objective-Proved: The local startup path renders the quarter UI without immediate crashes.
+  - Status: captured
+- Evidence-Type: manual_verification
+  - Artifact: `Pending user review of the local URLs printed by start_bizpa_tax_readiness_ui.ps1`
+  - Objective-Proved: Final UI acceptance for the quarter-readiness headline, Finish Now queue behavior, and export gating is pending explicit user pass/fail feedback.
+  - Status: planned
+
+## Implementation Log
+- 2026-03-18 18:02: Loaded `skills/workstream-task-lifecycle/SKILL.md` and the in-progress task file.
+- 2026-03-18 18:04: Located the existing `bizPA` quarter readiness UI, local launch script, and smoke script to determine current gaps.
+- 2026-03-18 18:07: Normalized this lifecycle file into the required template and recorded the ordered execution plan before editing code.
+- 2026-03-18 18:10: Added a dedicated quarter readiness helper module and automated tests for live blocker resolution, readiness recalculation, and query-param seeded demo resolution.
+- 2026-03-18 18:13: Updated `bizPA/frontend/src/App.jsx` to derive the quarter hero, inline Finish Now queue, and export gating from a single live quarter state model.
+- 2026-03-18 18:15: Updated the local quarter-readiness start script to print both the standard quarter review URL and the zero-blocker export-ready verification URL.
+- 2026-03-18 18:18: Reworked the smoke script to capture the required screenshots locally after browser-based capture failed under the sandboxed environment.
+- 2026-03-18 18:20: Completed automated validation, captured screenshots, and prepared the task for user verification.
+
+## Changes Made
+- Added `bizPA/frontend/src/quarterReadiness.js` with pure helpers for blocker ordering, readiness recalculation, issue-summary rebuilding, explanation copy, and demo query-param resolution.
+- Added `bizPA/frontend/src/quarterReadiness.test.js` covering partial blocker clearance, zero-blocker export enablement, and `resolvedDemoIssues=all`.
+- Updated `bizPA/frontend/src/App.jsx` so the quarter screen now:
+  - renders the hero headline as `<pct>% ready — <items> items left`,
+  - shows an inline ordered Finish Now queue,
+  - updates readiness/export state immediately in demo mode when blockers are cleared,
+  - keeps inbox/dashboard/control-centre blocker counts aligned to the live quarter state.
+- Updated `bizPA/start_bizpa_tax_readiness_ui.ps1` to print both the normal quarter-close URL and an export-ready verification URL.
+- Updated `bizPA/tax_readiness_ui_smoke.ps1` to save screenshots directly to the epic verification folder for the blocked and zero-blocker states.
+
+## Validation
+- `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
+  - Pass. Output included:
+    - `Tax readiness dashboard URL: http://127.0.0.1:3002/?readinessDemo=1&tab=quarter`
+    - `Export-ready verification URL: http://127.0.0.1:3002/?readinessDemo=1&tab=quarter&resolvedDemoIssues=all`
+- `npm.cmd test -- --runInBand --watchAll=false` in `C:\Users\edebe\eds\bizPA\frontend`
+  - Pass. `4` suites passed, `14` tests passed.
+- `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`
+  - Pass. Output included:
+    - `FRONTEND_STATUS=200`
+    - `SCREENSHOT_QUARTER=C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\quarter_readiness_screen.png`
+    - `SCREENSHOT_EXPORT_READY=C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first\verification\finish_now_queue_zero_blockers.png`
+- User verification request: To be issued with the completion response for explicit pass/fail on the quarter headline, Finish Now queue, and export gating behavior.
+
+## Risks/Notes
+- This task changes user-visible quarter-close behavior, so completion requires explicit user verification before the lifecycle file can be moved to `workstream/300_complete`.
+- The immediate blocker-clear behavior is implemented in the demo/local verification path so the export gating can be proven end-to-end without backend mutation setup.
+- The existing task brief and prior retry history are preserved below for reference.
+
+## Completion Status
+Awaiting user verification - 2026-03-18 18:20
+
+---
+
+## Original Task Brief
+
+**Workstream:** E — Mobile UX And Voice
+**Epic:** MVP PRD — Mobile Quarterly Export in 10 Minutes (UK Sole Traders, Bank-Feed First)
+**Priority:** 1
+**Source Epic Path:** workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md
+**Epic Output Folder:** C:\Users\edebe\eds\ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first
+**Suggested Agent:** codex
+**UI Deliverable:** Yes
+**Status:** [ ] Not Started
+**Workstream Goal:** Provide the mobile-first screens and voice-assisted workflows that make quarterly triage and close achievable in minutes.
+
+### Purpose
+Expose the core quarter-close experience with readiness percentage, remaining item count, ordered blocking queue, and export gating.
+
+### Input
+C1 quarter metrics, C4 export flow, and epic quarter screen requirements.
+
+### Output
+Quarter screen UI showing readiness headline, item count, Finish Now queue, and export action state.
+
+### Fields / Components
+- readiness_pct
+- items_left
+- finish_now_cta
+- blocking_queue
+- export_enabled
+
+### Dependencies
+- C1
+- C4
+
+### Action
+Implement the quarter screen and Finish Now queue, wiring live metric updates so export state changes immediately when blockers are cleared.
+
+### Verification
+- [ ] Provide or update a simple local access/start script that launches the app locally, prints the localhost URL, and is documented for quarter-close verification.
+- [ ] Smoke-test the local startup path and confirm the quarter UI loads without immediate crashes.
+- [ ] Capture screenshots of the quarter screen and Finish Now queue in the epic verification folder.
+- [ ] The screen renders the headline format like '92% ready — 8 items left' using live data.
+- [ ] Export is visibly disabled when blockers exist and becomes enabled immediately at zero blockers.
+
+### Notes
+- Generated from source epic: `workstream/000_epic/20260305_185316_mvp_prd_quarterly_export_10min.md`
+- This task is intended for Epic Review allocation before execution.
+- UI delivery requirements were expanded per `skills/ui-delivery-viewability/SKILL.md`.
+
+### Retry History
+Retry-Count: 2
+- Retry scheduled at 2026-03-18 17:21:30
+
+### Execution Evidence
+- Agent lane: claude
+- Command: `C:\Users\edebe\AppData\Roaming\npm\claude.CMD -p Execute this task file end-to-end. Read and follow C:\Users\edebe\eds\skills\workstream-task-lifecycle\SKILL.md first. Task file: C:\Users\edebe\eds\workstream\200_inprogress/claude\20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md. Implement required changes in the workspace, run validations, and update checklist items. --permission-mode acceptEdits`
+- Return code: 1
+- Stdout:
+```text
+You've hit your limit · resets 7pm (Europe/London)
+```
+- Retry scheduled at 2026-03-18 17:30:31

tokens used
182,136
Implemented the quarter-close screen changes in [App.jsx](C:/Users/edebe/eds/bizPA/frontend/src/App.jsx), added the live-state helper and tests in [quarterReadiness.js](C:/Users/edebe/eds/bizPA/frontend/src/quarterReadiness.js) and [quarterReadiness.test.js](C:/Users/edebe/eds/bizPA/frontend/src/quarterReadiness.test.js), and updated the local review scripts in [start_bizpa_tax_readiness_ui.ps1](C:/Users/edebe/eds/bizPA/start_bizpa_tax_readiness_ui.ps1) and [tax_readiness_ui_smoke.ps1](C:/Users/edebe/eds/bizPA/tax_readiness_ui_smoke.ps1). The quarter view now shows the headline as `% ready — items left`, includes an inline ordered Finish Now queue, and flips export state immediately in demo mode when blockers are cleared. The lifecycle file is updated and remains in progress at [20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md](C:/Users/edebe/eds/workstream/200_inprogress/codex/20260314_034043_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first_workstreamE_deliver_quarter_readiness_and_finish_now_screen.md).

Validation passed:
- `npm.cmd test -- --runInBand --watchAll=false`
- `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`
- `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\tax_readiness_ui_smoke.ps1`

Screenshots were captured at [quarter_readiness_screen.png](C:/Users/edebe/eds/ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first/verification/quarter_readiness_screen.png) and [finish_now_queue_zero_blockers.png](C:/Users/edebe/eds/ep_mvp_prd_mobile_quarterly_export_in_10_minutes_uk_sole_traders_bank_feed_first/verification/finish_now_queue_zero_blockers.png).

User verification is still required before this can move to `300_complete`. Please run:
- `powershell -ExecutionPolicy Bypass -File C:\Users\edebe\eds\bizPA\start_bizpa_tax_readiness_ui.ps1`

Then give pass/fail for:
1. The quarter screen shows the readiness headline in the `92% ready — 8 items left` style using live data.
2. The inline Finish Now queue is visible and ordered on the quarter screen.
3. Export is disabled with blockers and becomes enabled immediately at zero blockers, including the export-ready URL the script prints.
