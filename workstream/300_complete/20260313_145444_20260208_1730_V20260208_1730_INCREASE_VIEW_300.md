# Plan: Increase Default Performance View to 300 [V20260208_1730]

The user wants to replace the Top/Bottom 100 options with Top/Bottom 300 in the Strategy Performance dashboard.

## 1. Requirements
*   Update the `viewLimitSelect` in `strategy_performance.html` to offer "Top 300" and "Bottom 300" as the primary options.
*   Remove the legacy 100-row options and the redundant 300-row specific options.
*   Update `loadStats` to use 300 as the default limit for both `top` and `bottom` modes.

## 2. Plan of Approach
1.  **`strategy_performance.html`**:
    *   [ ] Modify the `<select id="viewLimitSelect">` to only have two options: `top` (Top 300) and `bottom` (Bottom 300).
    *   [ ] Update `loadStats()` logic:
        *   Set `limit = 300` by default.
        *   Simplify the `if (viewLimitVal === 'bottom')` block to handle sorting direction while keeping the 300-row limit.
2.  **`constants.py`**:
    *   [ ] Update version to `V20260208_1730`.

## 3. List of Changes
- **`breakout/fs/strategy_performance.html`**:
    - Update HTML dropdown options.
    - Simplify limit/sort logic in `loadStats`.
- **`breakout/fs/constants.py`**:
    - Increment version.

## 4. Confirmation
Please confirm this plan.
