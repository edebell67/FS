# Plan: Dynamic Source Attribution (V20260204_1650)

## 1. Requirements Understanding
- **Objective**: Refine the `source` field in `grid_live.json` to accurately reflect the origin of strategy promotions.
- **Rules**:
    - Build Source from Strategy: `frequency_{strategy name}`.
    - Buckets: Use the bucket name directly as the source.
    - DNA Net: `frequency_dna_net_{strategy name}` if it starts with `DNA_` and uses net metrics.
    - DNA Alt: `frequency_dna_alt_{strategy name}` if it starts with `DNA_` and uses alt metrics.
- **Bug Fix (2025-02-04 19:42)**: Prevented standard product cards (e.g., 'GBPEUR_C') from being incorrectly tagged as bucket names.

## 2. Checklist of Changes

- [x] **`live_monitoring_logic.js`**:
    - [x] Implement `_calculateSource(leader, groupName)` to derive the source string based on strategy name, metric, and group.
    - [x] Integrate `_calculateSource` into `activateGroup` and `update` logic to pass the enriched source to the API.
    - [x] Update `_setLiveMonitoring` to utilize the provided source payload.
- [x] **`trade_viewer_api.py`**:
    - [x] Update `_sync_bucket_to_grid_live` to set `source_tag` to the literal bucket name (removing `trade_bucket:` prefix).
- [x] **Versioning**:
    - [x] Update `fs/constants.py` to `V20260204_1650`.
    - [x] Update `db/constants.py` to `V20260204_1650`.

## 3. List of Changes

- **`C:\Users\edebe\eds\TradeApps\breakout\fs\live_monitoring_logic.js`**:
    - Added `_calculateSource` method.
    - Updated promotion calls to include calculated source.

## 4. Verification Plan
- [ ] Toggle a standard breakout strategy in the Multi-Chart UI. Verify `grid_live.json` shows `"source": "frequency_breakout_..."`.
- [ ] Toggle a DNA strategy with Alt metric. Verify `grid_live.json` shows `"source": "frequency_dna_alt_DNA_..."`.
- [ ] Activate a trade bucket. Verify `grid_live.json` shows `"source": "{bucketName}"`.
