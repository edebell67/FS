# Plan: Remove "Create New Trade Bucket" Section

## 1. Understanding of Requirements
The user wants to remove the "Create New Trade Bucket" panel/modal from the bottom of the Trade Buckets screen (`trade_bucket.html`) as it is no longer needed.

## 2. Plan of Approach
1.  **Modify `trade_bucket.html`**:
    *   Remove the `<div id="bucketModal" class="modal">...</div>` block which contains the bucket creation form.
    *   Remove the "New Bucket" button (`<button onclick="openModal()">...New Bucket...</button>`) from the controls bar.
    *   Remove the associated JavaScript functions: `createBucket()`, `openModal()`, `closeModal()`, and the `window.onclick` handler that references `bucketModal`.
    *   Update the version tag in the HTML header to `V20260222_1347 FS`.
2.  **Update `constants.py`**:
    *   Update the `VERSION` variable to `V20260222_1347`.

## 3. Checklist
*   [ ] Remove `bucketModal` HTML block from `trade_bucket.html`.
*   [ ] Remove "New Bucket" button from `trade_bucket.html`.
*   [ ] Remove `createBucket()`, `openModal()`, `closeModal()` and `window.onclick` logic from `trade_bucket.html`.
*   [ ] Update version tag in `trade_bucket.html`.
*   [ ] Update `VERSION` in `constants.py`.
