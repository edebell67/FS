# Plan: Fix Misplaced Daily Target Logic in Trade Engine

## Goal Description
The logic for checking if the daily profit target has been reached is currently located inside the `_update_product_trade_restriction` method. This means the daily target is only checked when that specific method is called (rarely), rather than on every data refresh cycle. This plan involves moving that logic into the dedicated `_check_daily_target` method.

## User Review Required
> [!IMPORTANT]
> This is a refactoring fix. The logic for calculating the daily P&L and triggering the 'DAILY TARGET ACHIEVED' status remains the same, but it will now execute at the correct time (every refresh cycle).

## Proposed Changes

### Trade Panel
#### [MODIFY] [trade_engine.py](file:///C:/Users/edebe/eds/algo_viewer/tradepanel/trade_engine.py)
*   **Move Code**: Cut the logic from lines 1998-2039 (inside `_update_product_trade_restriction`) and paste it into the body of `_check_daily_target` (around line 1928).
    *   The moved logic calculates today's P&L from active and closed trades and compares it against `self.daily_target`.

### Configuration
#### [MODIFY] [constants.py](file:///C:/Users/edebe/eds/algo_viewer/tradepanel/constants.py)
*   Update version number (e.g., `V20260115_1710`).

## Verification Plan

### Manual Verification
*   Since this is a backend logic fix, verification relies on code review to ensure the indentation and placement are correct.
*   Ideally, we would run the app and verifying that `DAILY TARGET ACHIEVED` triggers correctly when the P&L threshold is hit, but we will rely on static analysis for now.
