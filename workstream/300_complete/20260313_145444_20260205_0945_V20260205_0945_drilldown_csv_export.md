[V20260205_0945] Add CSV Export to Trade Drilldown Modal

## 1. Requirement
The user wants to export the trade list shown in the Trade Drilldown modal (launched from multi-chart cards) to a CSV file.

## 2. Plan of Approach
1.  **UI Update (`multi_chart.html`)**: Add an "Export CSV" button to the `tradeDrilldownModal` header.
2.  **JS Logic (`multi_chart.js`)**:
    *   Implement `exportDrilldownToCSV()` function.
    *   This function will iterate through `currentDrilldownTrades`, format the data, and trigger a browser download.
    *   Ensure all columns including the newly added `Alt Net` are included.
3.  **Refinement**: Provide a clear filename like `trades_{groupName}_{date}.csv`.

## 3. Checklist of Changes

*   **`multi_chart.html`**:
    *   [x] Insert `<button id="exportDrilldownCsvBtn" class="btn btn-outline btn-icon" title="Export CSV"><i class="fas fa-file-csv"></i></button>` into the `modal-header` of `tradeDrilldownModal`.
*   **`multi_chart.js`**:
    *   [x] In `initEventHandlers`: Add event listener for `exportDrilldownCsvBtn`.
    *   [x] Implement `exportDrilldownToCSV()` logic.
    *   [x] Ensure `currentDrilldownTrades` handles null values and proper formatting for CSV (e.g., quotes for string fields with spaces).
*   **`Constants.py`**:
    *   [x] Update version to `V20260205_0945`.

## 4. Verification
1.  Open the Multi-Chart P&L Grid.
2.  Open a Drilldown Modal for any card.
3.  Click the CSV icon in the modal header.
4.  Verify the downloaded file contains the correct trade list.
