# Plan: Implementation 1.4 - Core Capture Logic [V20260219_1600]

**Project**: bizPA (Voice-First Small Trader Capture App)  
**Objective**: Implement the CRUD endpoints for `capture_items` to support structured data storage, retrieval, and linking.  
**Version**: V20260219_1600

## Checklist
- [x] 1. Create Item Route and Controller - 100%
- [x] 2. Implement Create Item (`POST /api/v1/items`) with validation - 100%
- [x] 3. Implement List Items (`GET /api/v1/items`) with filtering - 100%
- [x] 4. Implement Update Item (`PATCH /api/v1/items/:id`) for linking/labels - 100%
- [x] 5. Implement Archive Item (`DELETE /api/v1/items/:id`) - 100%
- [x] 6. Verify CRUD operations with Test Payloads - 100%

## Approach
1.  **Endpoint Strategy**: Adhere to the API Endpoint Inventory defined in WS2.
2.  **Creation**: Support bulk and single item creation to accommodate offline-first sync pushes.
3.  **Linking**: Ensure that when an item is linked to a `client_id` or `job_id`, the corresponding IDs are validated against their respective tables.
4.  **Soft-Delete**: Implement deletion as a status update to `archived` to maintain sync journal integrity.
5.  **Filtering**: Provide basic query parameters for `type`, `status`, and `date_range`.

## Verification
- Verify `POST` creates a valid record and triggers an audit entry.
- Verify `GET` returns the expected items with correct filtering.
- Verify `PATCH` updates only the specified fields (e.g., adding a label).
- Verify `DELETE` sets the status to `archived`.
