# Plan: Fix Trade Bucket Creation and Persistence

**Date**: 2026-01-20 00:55
**Version**: V20260120_0055
**Description**: Consolidate duplicate frontend logic and ensure trade buckets are persisted to the database when DATA_SOURCE is set to 'database'.

## Proposed Changes

### 1. Frontend Consolidation (`pnl_graph.js`)
- [x] Remove the duplicate `createTradeBucket` function at line 1034.
- [x] Refine the `createTradeBucket` function at line 1746:
    - [x] Ensure it uses the latest `activeKeys` and `overlayDeltas`.
    - [x] Keep the auto-save view functionality as it's a premium feature.
    - [x] Improve error handling and user feedback.

### 2. Backend Database Persistence (`trade_viewer_api.py`)
- [x] Implement `_save_daily_summary(run_mode, date_str, summary_type, data)`:
    - [x] This function will `UPSERT` (or delete then insert) the JSON data into the `daily_summary` table.
- [x] Update `_save_trade_buckets` to call `_save_daily_summary` if `DATA_SOURCE == 'database'`.
- [x] This ensures that newly created buckets are immediately visible even when the system is prioritized to read from the database.

### 3. Versioning
- [x] Update `VERSION` in `constants.py` to `V20260120_0055`.
- [x] Update `pnl_graph.html` versioning.

## Verification Plan
1. [ ] Add several overlays to the P&L Graph.
2. [ ] Click "Create Bucket" and provide a name.
3. [ ] Verify the success alert appears and says a view was also saved.
4. [ ] Navigate to the "Trade Buckets" page and verify the new bucket is present.
5. [ ] Navigate back to "P&L Graph" and verify "TB_{BucketName}" appears in the Layout Presets.
6. [ ] Confirm the database `daily_summary` table has been updated for `_trade_bucket`.
