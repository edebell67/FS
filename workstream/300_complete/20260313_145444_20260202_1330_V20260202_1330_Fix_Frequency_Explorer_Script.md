# Plan: Move Frequency Explorer Script

## 1. Understanding of Requirements
The user pointed out text appearing at the bottom of the `frequency_explorer.html` page.
This text is actually the source code of `toggleGridLive` and `isLeaderMonitored` functions, which I inadvertently placed *after* the `</html>` tag in the previous step.
I need to move these functions from outside the HTML structure to inside the `<script>` tag.

## 2. Plan of Approach
1.  **Identify the misplaced block**:
    *   It starts from line 1876 (`async function toggleGridLive...`) and goes to the end of the file.
2.  **Identify the target location**:
    *   It should be placed inside the `<script>` tag, *before* `initExplorer();` (or anywhere valid inside the script).
    *   Currently, the script tag closes at line 1872 (`</script>`).
    *   Line 1870 is `bindNotificationControls();`.
    *   Line 1871 is `initExplorer();`.
3.  **Action**:
    *   I will cut the code from lines 1876-1938.
    *   I will paste it *before* `bindNotificationControls();` (around line 1870).
    *   I will delete the code from outside `</html>`.

## 3. List of Changes

*   **`TradeApps/breakout/fs/frequency_explorer.html`**:
    *   [x] Move `toggleGridLive` and `isLeaderMonitored` from the file tail (outside HTML) to inside the `<script>` block.

## 4. Validation
*   Verify no text appears below the footer/timeline.
*   Verify functions are available in the console scope.
