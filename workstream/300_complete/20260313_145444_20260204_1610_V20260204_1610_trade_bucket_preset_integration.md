# Plan: Trade Bucket Preset Integration (V20260204_1610)

## 1. Requirements Understanding
- **Objective**: enable saved trade buckets to be viewable via the preset selection list in the Multi-Chart UI.
- **Functionality**:
    - The "Presets" dropdown should dynamically fetch trade buckets for the currently selected date and mode.
    - Selecting a trade bucket from the list should load all its constituent strategies into the grid, grouped under the bucket's name.

## 2. Checklist of Changes

- [x] **`multi_chart.js`**:
    - [x] Add global `loadedBuckets` array to cache fetched buckets.
    - [x] Refactor `populateSavedViews` to be `async` and fetch data from `/api/trade_buckets`.
    - [x] Implement bucket inclusion in the dropdown with a "Bucket: " prefix and box emoji.
    - [x] Modify `loadView` to detect `BUCKET:` prefix, find the bucket in cache, and load its strategies into `activeOverlays`.
    - [x] Update `runMode` and `tradeDate` listeners to trigger `populateSavedViews`.
- [x] **Versioning**:
    - [x] Update `fs/constants.py` to `V20260204_1610`.
    - [x] Update `db/constants.py` to `V20260204_1610`.

## 3. List of Changes

- **`C:\Users\edebe\eds\TradeApps\breakout\fs\multi_chart.js`**:
    - Added `let loadedBuckets = []`.
    - Updated `populateSavedViews()` to fetch buckets.
    - Updated `loadView(name)` to handle `BUCKET:` prefix.
    - Updated event handlers for date/mode changes.

## 4. Verification Plan
- [ ] Open Multi-Chart UI.
- [ ] Select a date with saved trade buckets.
- [ ] Check "Presets" dropdown; verified "── TRADE BUCKETS ──" separator and bucket list appear.
- [ ] Select a bucket; verified all strategies load into a single card with the bucket name.
