# Plan: Fix Over-trading and UI Restoration (V20260129_0130)
**Date:** 2026-01-29 01:30

## 1. Requirements Understanding
- User reports "too many trades generated".
- User reports "product not in grid_live also generated".
- User reports "live screen shows nothing" (Closed executions missing).

## 2. Approach
- **Stop Frequent Re-entry (Cooldown)**: 
    - Implement a 30-minute cooldown in `common.py` after a trade closes. This prevents immediate re-entry if the price is still past the trigger level.
- **Process Cleanup**: 
    - Stop all existing breakout/monitor processes to ensure code updates (like cooldown) take effect and to stop redundant "ghost" scripts.
- **UI Restoration**: 
    - Fix the Live Orders page (`live_trades.html`) or API to ensure closed trades with `is_live_trade: true` are correctly displayed.
- **Strict Monitoring**: 
    - Ensure `grid_live_monitor.py` handles the latest JSON keys correctly and only processes active models.

## 3. Implementation Checklist
- [ ] **Cleanup**: Kill legacy python processes.
- [ ] **`common.py`**:
    - [ ] Add `cooldown_minutes` (default 30) to `BaseBreakoutStrategy`.
    - [ ] Update `close_trade` to record `last_exit_time`.
    - [ ] Update `enter_trade` to respect the cooldown.
- [ ] **`grid_live_monitor.py`**:
    - [ ] Ensure robust loading and logging.
- [ ] **`live_trades.html` / API**:
    - [ ] Verify why closed trades are not showing and fix.
- [ ] **Restart**: Relaunch services.
- [ ] **Version Update**: `V20260129_0130`.

## 4. Verification
- Verify `NZDAUD_C` doesn't re-enter immediately after closing.
- Verify `EURAUD_C` (not in grid) stops generating new orders.
- Verify "Closed Executions" shows today's completed trades.
