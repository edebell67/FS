# Plan: Enable Real-Time Time Slider Interaction

Date: 2026-01-26
Version: V20260126_1200

## 1. Objective
Ensure that dragging the time slider manually (not just clicking "Play") causes the charts and legend values to update in real-time based on the selected playback timestamp.

## 2. Plan of Approach
1.  **Modify `multi_chart_v2.js`**:
    *   Update `axisMaxMs` calculation in `updateCharts()` to use `playbackTime` whenever it exists, rather than only when `isPlaying` is true.
2.  **Modify `multi_chart_v3.js`**:
    *   Update `axisMaxMs` calculation in `updateCharts()` to use `playbackTime` whenever it exists, rather than only when `isPlaying` is true.
3.  **Update Versioning**:
    *   Increment version to `V20260126_1200` in `constants.py`.
    *   Update script version query parameters in `multi_chart_v2.html` and `multi_chart_v3.html`.

## 3. List of Changes
- [x] **`c:\Users\edebe\eds\TradeApps\breakout\fs\multi_chart_v2.js`**:
    *   Update `axisMaxMs` in `updateCharts()`.
- [x] **`c:\Users\edebe\eds\TradeApps\breakout\fs\multi_chart_v3.js`**:
    *   Update `axisMaxMs` in `updateCharts()`.
- [x] **`c:\Users\edebe\eds\TradeApps\breakout\fs\constants.py`**:
    *   Update `VERSION` to `V20260126_1200`.
- [x] **`c:\Users\edebe\eds\TradeApps\breakout\fs\multi_chart_v2.html`**:
    *   Update script version query parameter.
- [x] **`c:\Users\edebe\eds\TradeApps\breakout\fs\multi_chart_v3.html`**:
    *   Update script version query parameter.

## 4. Verification
- Open Multi-Chart V3.
- Drag the time slider to the middle of the day.
- Verify that the chart lines "clip" at the slider position and legend values (P&L totals) update to reflect the totals *at that time*.
