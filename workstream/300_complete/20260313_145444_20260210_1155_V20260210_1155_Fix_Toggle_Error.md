[V20260210_1155] Fix Hierarchy Toggle Error
**Current Status**: COMPLETED by Claude Code (2026-02-10 11:55)
**Objective**: Resolve the `Uncaught ReferenceError: stableId is not defined` error occurring when users click on hierarchy rows to expand/collapse them.

## 1. Issue Analysis
The user reported seeing errors in the console. Inspection revealed that `stableId` was undefined inside the `window.toggleHierRow` function. This was caused by an accidental deletion of the line `const stableId = el.dataset.stableId;` during a previous "Optimize DOM Updates" refactoring step (Step 431).

## 2. Solution Implemented
1.  Restored the missing variable declaration `const stableId = el.dataset.stableId;` at the beginning of the `window.toggleHierRow` function.

## 3. List of Changes
*   **`TradeApps/breakout/fs/strategy_performance.html`**:
    *   [x] Restored `stableId` variable in `toggleHierRow`.

## 4. Verification
*   Clicking on any hierarchy row (Category, Window, Ratio) should now correctly toggle its expansion state without throwing console errors.
*   The persistence logic (saving expanded state) depends on this ID, so persistence should now work correctly again.
