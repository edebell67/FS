# Plan: Multi Chart Rank #1 Loader

## 1. Understanding of Requirements
The user wants a function in `multi_chart.html` to easily view product/strategy combos that have achieved Rank #1.
I will add a button "Rank #1s" to the control panel. When clicked, it will load all strategies that have ever been Rank #1 (based on `firstRankOneTimes`) into the chart grid as individual cards.

## 2. Plan of Approach
1.  **Modify `multi_chart.html`**:
    *   Find the "Smart Actions" section (lines 510-550).
    *   Add a new button `<button id="btnShowRankOnes">` in the button row (`nav-buttons` or logic equivalent).
    *   I'll place it alongside "Auto", "Bucket", "Clear All". It might need a new row or squeeze in.
    *   Current row has 3 buttons. I can make it a 2x2 grid or just 4 buttons side-by-side if they fit.

2.  **Modify `multi_chart.js`**:
    *   Add event listener for `btnShowRankOnes`.
    *   Implement `loadRankOneStrategies()`:
        *   Iterate `Object.keys(firstRankOneTimes)`.
        *   Keys are formatted as `Strategy|Product`.
        *   For each key, check if it's already in `activeOverlays`.
        *   If not, add it.
        *   Use `key.replace('|', ' | ')` for overlay key format if needed (MultiChart uses "Strat | Prod" style usually, need to confirm).
        *   Update charts.

3.  **Refinement on Keys**:
    *   `firstRankOneTimes` keys from `processFrequencyData` step 204 are `l.strategy + '|' + l.product` (e.g., `StrategyA|ProductB`).
    *   `activeOverlays` keys: In `addOverlayHandler` (line 451), it uses `${strat} | ${prod}`.
    *   I need to be consistent. I will construct the overlay key as `StrategyA | ProductB`.

## 3. List of Changes

*   **`TradeApps/breakout/fs/multi_chart.html`**:
    *   [x] Add "Rank 1s" button to the control panel.

*   **`TradeApps/breakout/fs/multi_chart.js`**:
    *   [x] Add event listener.
    *   [x] Implement `loadRankOneStrategies`.

*   **`TradeApps/breakout/fs/constants.py`**:
    *   [x] Update version to `V20260202_1345`.

## 4. Execution
I will perform the edits.
