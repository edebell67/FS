# Plan: live_trades.html Filter Refinement

## 1. Understanding of Requirements
The user provided feedback that the "Live Trades" view is displaying incorrect information because it is showing non-live trades.
The user explicitly stated: "live trade should be ones where is_live_trade".
And: "is_live_trade:true".

This indicates I must re-introduce a client-side filter to only show trades where `is_live_trade` is true.

## 2. Plan of Approach
1.  **Modify `fs/live_trades.html`**:
    *   Locate the `loadLiveTrades` function where I previously removed the filter.
    *   Re-introduce the `filter()` method on `data.trades`.
    *   The condition will be: `t => t.is_live_trade === true`.
    *   This will ensure only actual live trades are displayed, aligning with the screen's purpose.

2.  **Versioning**:
    *   Update `c:/Users/edebe/eds/TradeApps/breakout/fs/constants.py` to `V20260202_1230`.

## 3. List of Changes

*   **`TradeApps/breakout/fs/live_trades.html`**:
    *   [x] In `loadLiveTrades()`: Restore the filter with the specific condition requested.
        ```javascript
        // [V20260202_1230] Restore strict filtering per user request (is_live_trade: true)
        const liveTrades = data.trades.filter(t => t.is_live_trade === true);
        ```

*   **`TradeApps/breakout/fs/constants.py`**:
    *   [x] Update `VERSION` to `V20260202_1230`.

## 4. Validation
*   Verify the code strictly filters for `is_live_trade === true`.
