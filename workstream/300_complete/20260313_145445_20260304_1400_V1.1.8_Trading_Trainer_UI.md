# Gemini Coder - Plan for Trading Trainer UI & Lifecycle Implementation

This document outlines the plan to implement the "Endur-style" Trading Trainer platform as requested on 2026-03-04.

## 1. Understanding of Requirements
The goal is to create a high-density, grid-heavy trading system (ETRM) with a rigid state machine for deal lifecycles, specifically implementing the "Auto-Confirmation" workflow (2A) where MO approval automatically confirms the deal. The UI must feel professional, dense, and "teachability-focused" (showing calculation components).

## 2. Plan of Approach
1.  **Project Scaffolding**: Create the `bizETRM` directory and initialize a React-based frontend using Vite.
2.  **Core CSS (Endur Vibe)**: Establish a global CSS variable system for high-density layouts (small fonts, tight spacing, slate/gray color palette).
3.  **State Machine Logic**: Implement a central `lifecycleEngine.js` that handles status transitions and role-based permissions.
4.  **UI Components**:
    *   **TradingShell**: Layout with sticky top bar and left navigation.
    *   **DataGrid**: A custom-styled high-density grid with quick-filters and multi-sort.
    *   **DetailsDrawer**: Right-side tabbed panel for deal details and "Drivers" (MTM calcs).
5.  **Mock Data & APIs**: Create a simulation layer for deals, curves, and positions.

## 3. List of Changes
- [x] **Scaffold**: Initialize `C:\Users\edebe\eds\bizETRM` with Vite/React.
- [x] **Constants**: Create `src/constants/tradeStatus.js` (Status 100-700).
- [x] **Engine**: Implement `src/logic/lifecycleEngine.js`.
- [x] **Styles**: Create `src/styles/endur.css` (Integrated into `index.css`).
- [x] **Layout**: Implement `src/components/layout/TradingShell.jsx` (Integrated into `App.jsx`).
- [x] **Blotter**: Implement `src/components/trading/DealBlotter.jsx` (Integrated into `App.jsx`).
- [x] **Drawer**: Implement `src/components/trading/DealDrawer.jsx` (Integrated into `App.jsx`).
- [x] **Verification**: Ensure role-switching (FO/MO) updates available actions correctly.


## 4. Verification Strategy
- **Lifecycle Test**: Create a "Draft" deal, "Submit" it, and verify it appears in the MO queue.
- **Permission Test**: Verify FO cannot "Confirm" a deal.
- **UI Density**: Ensure 50+ rows are visible on a standard 1080p viewport without excessive scrolling.

---
Plan Created: 2026-03-04 14:00
Version: V1.1.8
Target Directory: C:\Users\edebe\eds\bizETRM
