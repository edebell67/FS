# Plan: Fix Time Playback and AI Picker Stats (V20260209_2220)

## 1. Requirements
*   Fix the **Time Playback** feature in `trade_viewer.html`: The user reported the "Play" button doesn't work.
*   Resolve "stale" data in **AI Picker** (`ai_picker.html`): The screen reflects data from 16:36, but current time is 22:10.

## 2. Analysis & Findings
*   **Time Playback**: 
    *   Missing global variable declarations (`playbackInterval`, `playbackSpeed`, `minTime`, etc.) caused reference errors.
    *   A logic mismatch existed between `setTimeout` (used to start playback loop) and `clearInterval` (used to stop it). `setTimeout` should be cleared using `clearTimeout`.
*   **AI Picker**: 
    *   `automated_strategy_picker.py` had a condition that only updated the `_targeted_strategies.json` file if the bias direction (BUY/SELL) changed. This caused the UI stats (trade counts, net P&L, last entry times) to freeze when the bias was stable.
    *   The background process for `automated_strategy_picker.py` appears to have stopped or was never running continuously.

## 3. Action Plan
*   **`trade_viewer.html`**:
    *   [x] Add explicit declarations for all time playback global variables.
    *   [x] Replace `clearInterval` with `clearTimeout` for the `playbackInterval` variable.
    *   [x] Sync version tag to `V20260209_2220`.
*   **`automated_strategy_picker.py`**:
    *   [x] Modify the file-writing logic to always update `_targeted_strategies.json` regardless of bias change.
    *   [x] Retain console logging for bias flips to maintain visibility.
*   **System Integration**:
    *   [x] Update `constants.py` version to `V20260209_2220`.
    *   [ ] (Recommendation) User to restart `automated_strategy_picker.py` for continuous live updates.

## 4. Verification
*   Playback button logic now correctly clears timeouts and manages state.
*   AI Picker script will refresh data file every cycle, ensuring the dashboard shows current counts.
