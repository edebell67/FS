# Plan: Activation Explorer Source Visibility (V20260204_1940)

## 1. Requirements Understanding
- **Objective**: Display the `"source"` information for each strategy in the Activation Explorer.
- **Goal**: High visibility for strategy origin (UI, Bucket, Frequency).

## 2. Checklist of Changes

- [x] **`trade_viewer_api.py`**:
    - [x] Modify `_sync_grid_to_activations` to propagate `source` from `grid_live.json` to `activations.json`.
    - [x] Update `update_activations` (POST) to handle the `source` field.
- [x] **`activations_explorer.html`**:
    - [x] Update card template to display the `source` field.
    - [x] Add `source` to the search filter logic.
- [x] **Versioning**:
    - [x] Update `fs/constants.py` to `V20260204_1940`.
    - [x] Update `db/constants.py` to `V20260204_1940`.

## 3. List of Changes

- **`C:\Users\edebe\eds\TradeApps\breakout\fs\trade_viewer_api.py`**:
    - Propagated `source` during sync and manual updates.
- **`C:\Users\edebe\eds\TradeApps\breakout\fs\activations_explorer.html`**:
    - Added Source tag below the strategy key in each card.

## 4. Verification Plan
- [ ] Open Activation Explorer.
- [ ] Verify each card shows a `SOURCE` tag (e.g., `ui`, `frequency_...`, or bucket name).
- [ ] Search for a specific source (e.g., "bucket") to verify the filter works.
