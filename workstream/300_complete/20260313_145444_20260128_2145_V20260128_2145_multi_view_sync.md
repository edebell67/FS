# Plan: Card Management - Multi-View Synchronization (V20260128_2145)
**Date:** 2026-01-28 21:45

## 1. Requirements Understanding
- Propagate the reordering (`<`/`>`) and transfer (`exchange-alt`) features to `multi_chart_v2` and `multi_chart_v3` pages.
- Ensure state handling correctly references the respective `localStorage` keys for each version.

## 2. Approach
- **HTML Updates:** Added the `transferCardModal` to both `v2` and `v3` HTML files.
- **JS Logic:** 
    - Updated `updateCharts` to use `grid.appendChild(card)` for DOM ordering.
    - Added `moveGroup`, `openTransferModal`, `submitTransfer`, and `closeTransferModal` to both script files.
    - Verified that `v2` uses `pnl_graph_views_multi_v2` and `v3` uses `pnl_graph_views_multi_v3`.

## 3. Implementation Checklist
- [x] Apply HTML/JS changes to `multi_chart_v2`.
- [x] Apply HTML/JS changes to `multi_chart_v3`.
- [x] Fix card grid DOM reordering in both versions.
- [x] Update project version to `V20260128_2145`.

## 4. Verification
- Verify that SQL-based views (V2/V3) now support card shifting.
- Confirm that "Transfer to Preset" correctly moves cards between saved layouts in both versions.
