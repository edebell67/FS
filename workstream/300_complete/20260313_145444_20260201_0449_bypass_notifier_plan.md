# Bypass & Notifier Plan (2026-02-01 04:49)

## Requirements
- Treat bypass_criteria_check as a global activation guard and add an ultra mode that only lets strategies listed in grid_live.json bypass everything except the always-on max live trade cap.
- Ensure the notifier/rank alert UI persists the user's preferred notification threshold (rank #1 / #2 / #3+) and respects the suspend but act state so that automatic grid updates still occur when the request has been remembered.
- Surface the new ultra bypass option via the configuration UI so the viewer API can persist it in config.json.

## Proposed Steps
1. Capture these requirements in the plan file so the upcoming changes are traceable.
2. Update fs/common.py to centralize the bypass flag, enforce grid membership + summary net checks per mode, and clean up block reason tracking.
3. Extend the configuration form (trade_viewer.html) and defaults to list the new ultra option.
4. Verify the notifier UI logic persists the chosen threshold and suspend toggle behavior, and adjust if the remembered action is skipped when notifications are hidden.
