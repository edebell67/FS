[V20260204_2130] Trade Bucket Source Tag Prefix (TB_)

## 1. Requirement
The user wants Trade Bucket strategies in `grid_live.json` to have their `source` field prefixed with `TB_` (e.g., `TB_0204_1333_GBPNZD_C...`). This makes it easier to identify the origin of the activation in the UI.

## 2. Plan of Approach
I will update the two functions responsible for promoting bucket leaders to `grid_live.json` to include the `TB_` prefix. I will also ensure the removal logic handles both the prefixed and non-prefixed versions to ensure a clean transition and prevent duplicates.

## 3. Checklist of Changes

*   **`trade_viewer_api.py`**:
    *   [x] Modify `_sync_bucket_to_grid_live`:
        *   Prefix `source_tag` with `TB_`.
        *   Update cleanup logic to remove both `TB_` prefixed and raw bucket name tags.
    *   [x] Modify `_reconcile_active_buckets`:
        *   Prefix `source_tag` with `TB_`.
        *   Update `existing_for_bucket` check to look for both formats.
        *   Update cleanup/promotion logic to handle both formats.
    *   [x] Update version to `V20260204_2130` in `Constants.py`.

## 4. Verification
1.  Refresh the Dashboard.
2.  Check `grid_live.json` to see if `source` is now prefixed with `TB_`.
3.  Check Activation Explorer to confirm the source is correctly displayed as `TB_...`.
