# Plan: Add Positive Net Filter to Top X Multi-Chart Loader Workflow

## 1. Requirement
Add an additional constraint to the `Top X Multi-Chart Loader Workflow` so that only strategies with a positive `total_net` (or the selected metric) are processed.

## 2. Approach
1. Modify `TradeApps/breakout/fs/trade_viewer_api.py`:
   - Locate `_run_top_x_multi_chart_workflow`.
   - In the filtering loop (around line 1470), add a check to skip entries where the sort metric is `<= 0`.
2. Update `.agent/workflows/top-x-multi-chart-loader.md`:
   - Add a step clarifying the "Positive Net only" constraint.
3. Update `TradeApps/breakout/fs/workflows.json`:
   - Update the description and steps for the `top_x_multi_chart_workflow`.
4. Update `TradeApps/breakout/fs/constants.py`:
   - Update version to `V20260312_1410`.

## 3. Checklist
- [ ] Modify `trade_viewer_api.py` (Add `total_net > 0` filter).
- [ ] Update `top-x-multi-chart-loader.md` (Update documentation).
- [ ] Update `workflows.json` (Update workflow metadata).
- [ ] Update `constants.py` (Version `V20260312_1410`).
- [ ] Verify logic correctness.
