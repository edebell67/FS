# Plan: Apply V3 Drilldown and Performance to Multi-Chart (Base) [V20260126_1245]

This plan migrates the advanced trade investigation and performance optimizations from the V2/V3 dashboards back to the base Multi-Chart implementation.

## 1. Requirements
*   **Performance**: Use the `vwCombined_trades_output_top200` SQL view for data fetching instead of raw JSON files.
*   **Investigation**: Add the "Double-Click to Drilldown" modal functionality.
*   **Columns**: Show Time, Model, Type, Signal, Result, Total, Exit, Status, and L-Trade in the drilldown.
*   **Sorting**: Enable client-side sorting for all drilldown columns.

## 2. Plan of Approach

### A. HTML Update (`multi_chart.html`)
*   Add the **Trade Drilldown Modal** HTML structure.
*   Sync CSS styles for the modal, table, and header interactions (sticky headers, hover effects).
*   Add the loading overlay structure.

### B. JavaScript Update (`multi_chart.js`)
*   **`fetchData()`**: 
    *   Update URL to `http://127.0.0.1:8001/api/vwCombined_trades_output_top200`.
    *   Update mapping to include `signal`, `net_return`, `exit_time`, `status`, and `is_live`.
*   **`showTradeDrilldown(groupName)`**: Implement the same logic as V2/V3 to filter and prepare drilldown data.
*   **`renderDrilldownTable()`**: Implement the 9-column row generator.
*   **`sortDrilldownTable()`**: Implement the 9-column sorter.
*   **`createGroupCard(groupName)`**: Add `ondblclick="showTradeDrilldown('${groupName.replace(/'/g, "\\'")}')"` to the card.
*   **Versioning**: Update reference to `V20260126_1245`.

## 3. Checklist
*   [ ] `multi_chart.html` Modal and CSS added.
*   [ ] `multi_chart.js`: `fetchData` URL and mapping updated.
*   [ ] `multi_chart.js`: `showTradeDrilldown`, `renderDrilldownTable`, `sortDrilldownTable` added.
*   [ ] `multi_chart.js`: `createGroupCard` double-click added.
*   [ ] `constants.py`: Version updated to `V20260126_1245`.

## 4. Verification
*   Confirm `multi_chart.html` loads faster.
*   Confirm double-click opens drilldown.
*   Confirm all 9 columns are visible and sorting works.
