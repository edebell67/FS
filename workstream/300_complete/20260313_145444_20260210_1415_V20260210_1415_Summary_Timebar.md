# Performance Summary Timeline Playback [V20260210_1445]

This document outlines the implementation of the Performance Timeline feature for the Summary Breakdown modal.

## 1. Requirements
*   Integrate a high-fidelity playback controller into the Performance Summary modal.
*   Enable scrubbing through historical data using a time slider.
*   Display a session-wide trade distribution (00:00 - 23:59).
*   Correctly project tree statistics and leaderboards based on the selected playback time.
*   Match the "Performance Timeline" aesthetic (amber clock, speed control, indigo buttons).

## 2. Implementation History
*   **V20260210_1415**: Initial Global/Local timebar implementation.
*   **V20260210_1430**: Basic playback engine and slider integration.
*   **V20260210_1445**: High-fidelity UI restoration and robust historical projection fix.

## 3. Key Changes
### Backend (`trade_viewer_api.py`)
*   [x] Calculate hourly trade density (`time_dist`) per strategy.
*   [x] Aggregate session-wide `global_time_dist`.

### Frontend (`strategy_performance.html`)
*   [x] **CSS**: Added "PERFORMANCE TIMELINE" styles (glassmorphism cards, amber typography, gradient buttons).
*   [x] **HTML**: Structured the playback control panel with Play/Pause, Step navigation, and Speed badges.
*   [x] **JS**: Implemented `updateSummaryPlayback` to map `_summary_net.json` snapshots to the current hierarchical view.
*   [x] **JS**: Added recursive aggregation logic to re-calculate tree totals (`bn`, `sn`, `sc`, `bc`) during scrubbing.
*   [x] **JS**: Implemented a variable-speed playback engine (1x to 3600x).

## 4. Verification Checklist
- [x] Global timebar shows 24h distribution.
- [x] Slider scrubs correctly through the session.
- [x] Clock displays historical time accurately.
- [x] Leaderboard updates in real-time while scrubbing.
- [x] Play/Pause functionality works at multiple speeds.
- [x] "Skip to Start/End" buttons behave as expected.

**Status**: COMPLETED
