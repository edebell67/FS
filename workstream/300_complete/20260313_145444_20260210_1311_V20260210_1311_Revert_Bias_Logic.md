[V20260210_1311] Revert Bias Logic to Count-Based (Aligned with UI)
**Current Status**: PLANNED (2026-02-10 13:11)
**Objective**: Revert the bias calculation and UI highlighting to use trade counts, matching the original "approach" of the strategy performance screen.

## 1. Problem Statement
The switch to P&L-driven bias in V20260210_1305 caused a misalignment with the user's expectations of the "screen bias approach". The user explicitly requested to use the screen's approach to eliminate future misalignment. The screen's approach identifies the dominant side based on total trade volume (counts) rather than net profit.

## 2. Plan of Approach
1.  **`automated_strategy_picker.py`**:
    *   Revert `day_bias` to use `total_buy_count >= total_sell_count`.
    *   Revert `recent_bias` and `ui_bias` to use `recent_buy_count_all >= recent_sell_count_all`.
    *   Revert "Stable Bias" logic to use the 5-trade difference threshold.
    *   Update debug print statements to reflect count-based logic.
2.  **`strategy_performance.html`**:
    *   Revert `buyIsDominant` calculation to use `totalBuyCount >= totalSellCount` (which is functionally the same as the original `avgBuy >= avgSell`).
3.  **`Constants.py`**:
    *   Update version to `V20260210_1311`.

## 3. List of Changes
*   **`TradeApps/breakout/fs/automated_strategy_picker.py`**:
    *   [ ] Revert `day_bias` logic to trade counts.
    *   [ ] Revert `recent_bias` / `ui_bias` logic to trade counts.
    *   [ ] Restore 5-trade count difference threshold for stable bias.
*   **`TradeApps/breakout/fs/strategy_performance.html`**:
    *   [ ] Revert `buyIsDominant` to trade counts.
*   **`DataInsights/src/Constants.py`**:
    *   [ ] Update VERSION to `V20260210_1311`.

## 4. Verification
*   Run `automated_strategy_picker.py` and verify it now reports BUY (since 8059 > 7884).
*   Verify `grid_live.json` is updated with BUY bias if a flip occurs.
*   Refresh `strategy_performance.html` and verify HIGHLIGHTING matches the reported bias.
