# Plan: Final Polishes and ID Synchronization

**Date**: 2026-01-20 03:25
**Version**: V20260120_0325
**Description**: Fix ID mismatches between JS and HTML, and refine Chart.js visualization for a more premium look.

## Proposed Changes

### 1. JavaScript ID Synchronization (`pnl_graph.js`)
- [x] Update `updatePlaybackUI` to use `playbackClock` instead of `currentPlaybackTime`.
- [x] Update `slowerPlayback` and `fasterPlayback` to use `playbackSpeedBadge` instead of `playbackSpeedLabel`.
- [x] Ensure `playbackSpeedBadge` format matches "Xx SPEED".

### 2. Chart.js Visual Refinement (`pnl_graph.js`)
- [x] Enable smooth animations for chart updates.
- [x] Update font family for axis labels and tooltips to 'Outfit'.
- [x] Refine grid lines and border colors for better integration with the glassmorphism theme.
- [x] Adjust tooltip styling for a more modern look (border radius, padding, background).

### 3. CSS Refinements (`pnl_graph.css`)
- [x] Add explicit styles for Chart.js tooltips if needed.
- [x] Ensure the "Simulation" clock has a nice monospaced look as intended.

### 4. Versioning
- [x] Update `VERSION` in `constants.py` to `V20260120_0325`.
- [x] Update cache busters in `pnl_graph.html`.

## Verification Plan
1. [ ] Testing playback controls (Play, Pause, Speed, Slider) and verify the clock and speed badge update correctly.
2. [ ] Verify the chart looks polished and integrates well with the theme.
3. [ ] Confirm no console errors related to missing IDs.
