# Plan: Apply V3 Drilldown to Multi-Chart V2 [V20260126_1225]

This plan synchronizes the trade drilldown functionality from Multi-Chart V3 to Multi-Chart V2, including new columns (Exit Time, Status, L-Trade) and improved data binding.

## 1. Requirements
*   **Sync UI**: Update the drilldown table in `multi_chart_v2.html` to match the enhanced V3 version.
*   **Sync Logic**: Update `multi_chart_v2.js` to correctly fetch, store, and render the extra trade details.
*   **Sorting**: Ensure sorting works for all columns.

## 2. Plan of Approach

### A. HTML Update (`multi_chart_v2.html`)
*   Add the new headers: **Exit**, **Status**, **L-Trade**.
*   Update header `onclick` indices to match the new 9-column layout.

### B. Logic Update (`multi_chart_v2.js`)
*   **`fetchData()`**: Update the record push to include `signal`, `net_return`, `exit_time`, `status`, and `is_live`.
*   **`showTradeDrilldown()`**:
    *   Initialize `pt.net_return` handling (using `indivReturn`).
    *   Push new fields to `currentDrilldownTrades`.
*   **`renderDrilldownTable()`**: Update the table row generation to include the 3 new cells with proper styling.
*   **`sortDrilldownTable()`**: Update the sort key array to include the 3 new keys.

## 3. Checklist
*   [x] `multi_chart_v2.html` headers updated.
*   [x] `multi_chart_v2.js`: `fetchData` updated to store full detail.
*   [x] `multi_chart_v2.js`: `showTradeDrilldown` mapping updated.
*   [x] `multi_chart_v2.js`: `renderDrilldownTable` updated for new columns.
*   [x] `multi_chart_v2.js`: `sortDrilldownTable` keys updated.
*   [x] Version updated to `V20260126_1225`.

## 4. Verification
*   Open Multi-Chart V2.
*   Double-click a chart card.
*   Confirm the drilldown table shows "Exit", "Status", and "L-Trade" columns.
*   Confirm data is populated (Exit Time HH:MM:SS or '-', Status Open/Closed, L-Trade Yes/No).
*   Verify sorting works on all headers.
