# DNA Frequency Extraction Update

## 1. Understanding of Requirements
The user wants to update the JSON key extraction logic in `dna_frequency_net.py` and `dna_frequency_alt.py`.
- **Strategy Key**: Combine `{strategy name}` (model) and `{strategy parm}` (params) from the underlying datasource.
- **Product Key**: Combine `{model}` and `{product}`.

## 2. Plan of Approach
1.  **Modify SQL Query**: Update `fetch_rows` in both scripts to include the `params` column from `vw_113_Combined_trades_all`.
2.  **Update Key Construction**: 
    - Retrieve `model`, `params`, and `product`.
    - Construct `strategy` field as `{model}_{params}`.
    - Construct `product` field as `{model}_{product}`.
3.  **Update Constants**: Bump version to `V20260201_0230`.

## 3. List of Changes

*   **`fs/dna_frequency_net.py`**:
    *   [ ] Add `strategy_param` to SELECT statement in `fetch_rows`.
    *   [ ] Update loop in `main` to fetch `strategy_param` from row.
    *   [ ] Construct `strategy = f"{model}_{strategy_param}"`.
    *   [ ] Construct `product = f"{model}_{raw_product}"`.

*   **`fs/dna_frequency_alt.py`**:
    *   [ ] Add `strategy_param` to SELECT statement in `fetch_rows`.
    *   [ ] Update loop in `main` to fetch `strategy_param` from row.
    *   [ ] Construct `strategy = f"{model}_{strategy_param}"`.
    *   [ ] Construct `product = f"{model}_{raw_product}"`.

*   **`fs/constants.py`**:
    *   [ ] Update version to `V20260201_0230`.
