# Plan: Grid Live Source Persistence (V20260204_1640)

## 1. Requirements Understanding
- **Objective**: Ensure the `"source"` field is present and persisted for all entries in `grid_live.json`.
- **Reason**: Correct tracking and filtering of strategy promotions.

## 2. Checklist of Changes

- [x] **`grid_live.json`**:
    - [x] Manually add `"source": "ui"` to existing missing entry.
- [x] **`live_monitoring_logic.js`**:
    - [x] Update `_setLiveMonitoring` to explicitly include `source: 'ui'` in the payload sent to the API.
- [x] **Versioning**:
    - [x] Update `fs/constants.py` to `V20260204_1640`.
    - [x] Update `db/constants.py` to `V20260204_1640`.

## 3. List of Changes

- **`C:\Users\edebe\eds\TradeApps\breakout\fs\grid_live.json`**:
    - Added `"source": "ui"` to the GBPEUR_C entry.
- **`C:\Users\edebe\eds\TradeApps\breakout\fs\live_monitoring_logic.js`**:
    - Modified `_setLiveMonitoring` to include `payload.source = 'ui'`.

## 4. Verification Plan
- [ ] Observe `grid_live.json` after the fix; verified `"source": "ui"` is present.
- [ ] Toggle a leader in the Multi-Chart UI.
- [ ] Verify that the resulting entry in `grid_live.json` continues to have the `"source"` field.
