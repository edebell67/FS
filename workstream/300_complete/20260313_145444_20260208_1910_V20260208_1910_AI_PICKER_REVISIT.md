# Plan: Revisit AI Picker Logic for Consistency & Robustness [V20260208_1910]

The user wants to revisit `automated_strategy_picker.py`. I will address the consistency issues between the Picker and the Performance table while refining the selection thresholds.

## 1. Requirements
*   Align "Net Return" values with the main Performance dashboard for easier cross-referencing.
*   Make market condition thresholds configurable via `config.json`.
*   Support dual-side selection (Buy & Sell) when market is caught in a "CHOPPY" state.
*   Ensure the "View / Push" functionality remains robust with the improved search blobs.

## 2. Plan of Approach
1.  **Configurable Thresholds**: 
    *   Load `picker_pnl_spread_threshold` (default 2000) and `picker_count_diff_threshold` (default 0.05) from `config.json`.
2.  **Refined Selection Logic**:
    *   If `is_choppy` is true, the `bias` check should be relaxed to allow both BUY and SELL candidates if they are scalpers.
3.  **Expose "Dashboard Net"**:
    *   In the `results` loop, calculate the "Dashboard Net" (Total P&L for all trades of that strategy today).
    *   Store this as `total_net` in the JSON, while keeping the sequential simulation as `sequential_net`.
4.  **UI Alignment**:
    *   Ensure the JSON keys sent to `_targeted_strategies.json` provide enough context for the UI to explain why a strategy was picked (e.g., `market_limit` logic).
5.  **Version Update**:
    *   Update `constants.py` to `V20260208_1910`.

## 3. List of Changes
- **`config.json`**:
    - [ ] Add `picker_pnl_spread_threshold`: 2000
    - [ ] Add `picker_count_diff_threshold`: 0.05
- **`automated_strategy_picker.py`**:
    - [ ] Update `is_choppy` to use config values.
    - [ ] Modify `eligible_apps` loop to be more inclusive if `is_choppy`.
    - [ ] Calculate both `sequential_net` (realistic) and `total_net` (dashboard equivalent).
- **`constants.py`**:
    - [ ] Update version.

## 4. Confirmation
Does this plan to align the metrics and formalize the thresholds address your concerns?
