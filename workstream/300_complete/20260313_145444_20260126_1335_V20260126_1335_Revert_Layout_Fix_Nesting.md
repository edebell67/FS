# Plan: Revert Multi-Chart Layout to Top-Horizontal [V20260126_1335]

This plan restores the horizontal layout of the standard Multi-Chart dashboard while keeping all new synchronized features (V3 sync, Rank By, Drilldown).

## 1. Requirements
*   **Layout Revert**: Move controls from the "vertical column" back to a horizontal grid at the top.
*   **HTML Structure Fix**: Fix nested `<div>` tags that were causing displacement.
*   **Legibility**: Ensure the playback controls and chart grid take up the full available width.

## 2. Plan of Approach

### A. Structure Update (`multi_chart.html`)
*   Re-group the control cards into a single `control-grid` section.
*   Ensure the `container` div wraps the entire page content.
*   Place the playback bar prominently below the configuration grid.

### B. CSS Refinement
*   Ensure `.control-grid` uses `repeat(auto-fit, minmax(320px, 1fr))` for flexible rows.

### C. Versioning
*   Update to `V20260126_1335`.

## 3. Checklist
*   [ ] `multi_chart.html`: HTML structure restored to horizontal top-grid.
*   [ ] `multi_chart.html`: Nesting errors fixed.
*   [ ] `constants.py`: Version updated.

## 4. Verification
*   Confirm the dashboard looks "normal" (controls at top, charts below).
*   Verify all new features (Live Mode, Rank By) still function in the restored layout.
