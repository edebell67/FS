# Plan: Add Total Net Column to Strategy Performance Stats
Date: 2025-12-30 00:30
Version: V20251230_0045

## 1. Understanding of Requirements
*   Add a new column "Total Net" to the strategy performance table.
*   "Total Net" = buy net + sell net (sum of all closed trades' net P&L for that configuration).
*   Column position: between "Parm" and "#buy".
*   Target file: `TradeApps/breakout/strategy_performance.html`.

## 2. Plan of Approach
1.  **Modify `strategy_performance.html`**:
    *   Update the table header (`<thead>`) to include "Total Net" and adjust `sortTab` indices.
    *   Update `processTrades` logic to calculate the total net return for each strategy/product/parameter combination.
    *   Update `renderTable` to include the "Total Net" cell value.
    *   Update `doSort` key list to include 'total_net'.
2.  **Update Version**: Update `constants.py` to `V20251230_0045`.
3.  **Verification**: Inform the user to refresh the page.

## 3. List of Changes
*   **`TradeApps/breakout/strategy_performance.html`**:
    *   [x] Insert "Total Net" `<th>` in `<thead>` (line 486). [V20251230_0045]
    *   [x] Update `sortTab` indices for subsequent columns in `<thead>`. [V20251230_0045]
    *   [x] Add `total_net: 0` to stats object initialization in `processTrades` (line 621 approx). [V20251230_0045]
    *   [x] Accumulate `total_net` in `processTrades` loop. [V20251230_0045]
    *   [x] Insert `total_net` cell in `renderTable` (line 668 approx). [V20251230_0045]
    *   [x] Update `keys` array and numeric index check in `doSort` (line 702/652). [V20251230_0045]
    *   [x] Add "Min Total Net" input and filter functionality. [V20251230_0055]
*   **`TradeApps/breakout/constants.py`**:
    *   [x] Update `VERSION = "V20251230_0055"`. [V20251230_0055]
