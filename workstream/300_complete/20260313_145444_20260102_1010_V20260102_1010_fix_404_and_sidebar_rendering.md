# Plan: Fix 404 and Sidebar Rendering

This document outlines the fix for the 404 error on `activations_explorer.html` and the rendering issues with the sidebar navigation.

## 1. Understanding of Requirements
*   The user is experiencing a 404 error when accessing `activations_explorer.html` via the Flask API (port 5000).
*   The sidebar menu appears "squashed" (likely because `sidebar.css` is also 404ing).
*   The user wants the menu arranged vertically (our sidebar design is vertical, so fixing the CSS will solve this).
*   The user wants confirmation that L-trades are **blocked**, not queued, when `max_live_trades` is hit.

## 2. Approach
1.  **Fix Flask Routes**: Update `trade_viewer_api.py` to explicitly serve `activations_explorer.html` and `sidebar.css`.
2.  **Verify L-Trade Logic**: Audit `common.py` to confirm "Blocked" behavior.
3.  **Update Version**: Bump version to `V20260102_1010`.

## 3. List of Changes
*   **`trade_viewer_api.py`**:
    *   [x] Added route for `/activations_explorer.html`.
    *   [x] Added route for `/sidebar.css`.
*   **`constants.py`**:
    *   [ ] Update `VERSION` to `V20260102_1010`.
*   **Logic Confirmation**:
    *   [x] Confirmed `_handle_live_orders` in `common.py` prints a failure and exits if json creation fails (no queuing).

## 4. Verification
*   Check that `activations_explorer.html` loads on port 5000.
*   Check that sidebar renders vertically with glassmorphic styles.
