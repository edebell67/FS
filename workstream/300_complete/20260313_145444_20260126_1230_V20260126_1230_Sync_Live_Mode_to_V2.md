# Plan: Fix Missing Result Column and Final Consistency [V20260126_1230]

This plan fixes a layout oversight where the "Result" column was accidentally removed from the drilldown table in both Multi-Chart V2 and V3. It also ensures final consistency of the headers and sorting indices.

## 1. Requirements
*   **Restore Header**: Add the "Result" header back to Multi-Chart V2 and V3 drilldown modals.
*   **Fix Indices**: Update the `onclick="sortDrilldownTable(n)"` values to match the 9-column structure.

## 2. Plan of Approach

### A. HTML Updates (`v2.html` and `v3.html`)
*   Restore `<th onclick="sortDrilldownTable(4)">Result <i class="fas fa-sort"></i></th>`.
*   Ensure the sequential order: 0, 1, 2, 3, 4, 5, 6, 7, 8.

### B. Versioning
*   Update version to `V20260126_1230`.

## 3. Checklist
*   [x] `multi_chart_v2.html` header structure restored.
*   [x] `multi_chart_v3.html` header structure restored.
*   [x] Version updated to `V20260126_1230`.

## 4. Verification
*   Open V2 and V3 dashboards.
*   Confirm "Result" column is visible in drilldown.
*   Confirm sorting works for ALL columns.
