# Plan - V20260123_0955 - Fix P&L Graph Auto-Refresh Selection

## 1. Understanding of Requirements
- The user likes the P&L graph but noted an issue with auto-refresh.
- Currently, auto-refresh re-executes the "Top 5" ranking, which can shuffle the strategies shown on screen if their rankings change.
- The user wants the refresh to maintain whatever is currently on the screen.

## 2. Plan of Approach
1.  **Selection Persistence**: Modify `pnl_graph.js` to ensure that manual selection changes (adding/removing overlays, loading presets) disable the "Persistent Auto-Ranking" mode.
2.  **Refresh Logic Fix**:
    - Update `displayCurrentSelection` (triggered by the manual 'Refresh' button) to disable auto-ranking so it refreshes the *user's current choice*.
    - Ensure `fetchData` only calls `applyRankSelection` if the user is explicitly in "Auto" mode.
3.  **Verification**:
    - Verify that adding a manual overlay stops the "Top 5" from overriding it on the next 5-second tick.
    - Verify that the 'Refresh' button updates data points for the *current* keys only.

## 3. List of Changes
- `fs/pnl_graph.js`:
    - [x] Set `isAutoRankingActive = false` in `addOverlay()`. (2026-01-23 09:50)
    - [x] Set `isAutoRankingActive = false` in `removeOverlay()`. (2026-01-23 09:50)
    - [x] Set `isAutoRankingActive = false` in `loadView()`. (2026-01-23 09:58)
    - [x] Update `displayCurrentSelection()` to reset auto-ranking and remove redundant `applyRankSelection()` call. (2026-01-23 09:58)
- `fs/constants.py`:
    - [x] Update version to `V20260123_0955`. (2026-01-23 09:55)

## 4. Verification Results
- [x] Verified code logic in `pnl_graph.js`.
- [x] Checked `fetchData` logic to confirm it preserves `activeLinks` when `isAutoRankingActive` is false.
