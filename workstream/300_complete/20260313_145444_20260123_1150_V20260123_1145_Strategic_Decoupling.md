# Plan - V20260123_1145 - Strategic Decoupling: Always Track, Selective Trade

## 1. Understanding of Requirements
- The user wants all strategies to execute their basic entry criteria regardless of "gates" (activations/guards).
- Gates should only apply when determining if a trade is promoted to a Live Order (L-trade).
- This ensures full virtual P&L tracking for all strategies on the dashboard.

## 2. Plan of Approach
1.  **Modify `fs/common.py`**:
    - Remove the early-exit `_is_active` and `max_live_trades` checks from `BaseBreakoutStrategy.enter_trade()`.
    - This allows strategy instances to open trades in memory and save JSON records immediately upon meeting entry criteria.
2.  **Verify Order Handler**:
    - Ensure `_handle_live_orders()` still gates live order generation using `_is_active()` and `_is_profitability_guard_passed()`.
    - Corrected a duplicate line error in `enter_trade()` during implementation.
3.  **Audit Logs**:
    - Removed the noisy `[SKIPPING]` log from the entry point.
4.  **BUMP VERSION** to `V20260123_1145`.

## 3. List of Changes
- `fs/common.py`:
    - [x] Refactored `enter_trade()` to remove activation gating. (2026-01-23 11:45)
    - [x] Fixed duplicate code line in `enter_trade()`. (2026-01-23 11:47)
- `fs/constants.py`:
    - [x] Update version to `V20260123_1145`. (2026-01-23 11:48)

## 4. Verification Results
- [x] Code verified to allow trade object instantiation before checking live readiness.
- [x] `_handle_live_orders()` confirmed as the single authority for broker orders.
