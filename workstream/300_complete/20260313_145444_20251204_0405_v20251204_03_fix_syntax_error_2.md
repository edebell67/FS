# Fix Syntax Error in Trade Engine - Part 2 (2025-12-04)

## 1. Understanding of Requirements
The user reported another `SyntaxError: expected 'except' or 'finally' block` in `algo_viewer/tradepanel/trade_engine.py` at line 11755.
This is caused by a `try` block starting at line 11731 that is missing its `except` block. The code inside the `try` block also seems to have a logic error where it overwrites the variable it just set, suggesting the second line was meant to be in the `except` block.

## 2. Plan of Approach
1.  **Correct Try-Except Block**: Modify `_get_csv_rule_lookback` method in `trade_engine.py`. Change the structure so that `lookback = getattr(...)` is inside an `except (ValueError, TypeError):` block, handling cases where `int(raw_value)` fails.
2.  **Verify**: Run `py_compile` to ensure no further syntax errors exist.

## 3. List of Changes
*   **`algo_viewer/tradepanel/trade_engine.py`**:
    *   [ ] Refactor lines 11731-11755 to properly implement try-except logic for `lookback` calculation.
*   **`algo_viewer/tradepanel/Constants.py`**:
    *   [ ] Update version to `v20251204_03`.
