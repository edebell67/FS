# Plan: Trade Bucket Workflow Fix & Restituiton (V20260204_1440)

## 1. Requirements Understanding
- **Symptom**: "TypeError: Failed to fetch" in Multi-Chart UI when saving/interacting.
- **Root Cause**: `IndentationError` in `trade_viewer_api.py` at line 1292 caused by a truncated `try` block in `_sync_bucket_to_grid_live`.
- **Goal**: Restore the missing logic in `_sync_bucket_to_grid_live`, fix the syntax error, and verify the server health.

## 2. Checklist of Changes

- [ ] **`trade_viewer_api.py` (FS version)**:
    - [ ] Restore the missing lines in `_sync_bucket_to_grid_live` starting from line 1290.
    - [ ] Ensure all `try/except` blocks are properly closed.
    - [ ] Increment version in `fs/constants.py` and `db/constants.py`.
- [ ] **Verification**:
    - [ ] Run `python -m py_compile` to ensure no syntax errors.
    - [ ] Inform the user to refresh the page once the server is back up.

## 3. List of Changes

- **`trade_viewer_api.py`**:
    - Complete the `_sync_bucket_to_grid_live` function.
- **`fs/constants.py` & `db/constants.py`**:
    - Update `VERSION = "V20260204_1440"`.

## 4. Verification Plan
- [ ] Compile check: `python -m py_compile TradeApps/breakout/fs/trade_viewer_api.py`.
- [ ] Verify `grid_live.json` is updated correctly when a bucket is activated.
