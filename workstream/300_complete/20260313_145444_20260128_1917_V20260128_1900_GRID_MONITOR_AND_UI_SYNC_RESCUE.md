# Plan: Grid Monitor Rescue & UI Real-Time Sync (V20260128_1900)
**Date:** 2026-01-28 19:17

## 1. Requirements Understanding
- Resolve `grid_live_monitor.py` corruption and `NameError`.
- Correct L-Trade naming (remove `_dna_` for breakout strategies).
- Support Promotion metadata (`in_trade_entry_time`, `in_trade_entry_price`) in JSON.
- Fix UI auto-refresh and ensure charts move forward to current time.
- Implement auto-sync for new grid promotions to appear as browser cards automatically.

## 2. Approach
- **Monitor:** Full rewrite for stability. Update `mark_json_trade_as_live` to accept price/time. Restart service.
- **API:** Update `/api/trades` to recognize new `is_live_trade` boolean in JSON scans.
- **Frontend:** 
    - Adjust `nextRefreshTime` to 10s.
    - Fix X-axis `axisMaxMs` to use `endOfDay` (Current Time) even if no new trades are present.
    - Add `fetch` loop for `grid_live.json` within `fetchData` to auto-populate cards.
    - Enhance marker logic to support both SQL (DNA) and API (Breakout) sources.

## 3. Implementation Checklist
- [x] **Grid Monitor Stability:** Resolved corruption and the `total_models` crash. 
- [x] **L-Trade Naming:** Breakout trades correctly generate `grid_live_[Product]_breakout_...` files.
- [x] **Metadata Propagation:** Promoted trades now include `is_live_trade`, `in_trade_entry_time`, and `in_trade_entry_price`.
- [x] **UI Auto-Refresh:** Interval set to 10s; charts now slide forward to "Now" in real-time.
- [x] **Auto-Sync:** Promote new strategy in `grid_live.json` -> Card appears on dashboard automatically.
- [x] **Live Indicators:** White dots render for both DNA and Breakout sources.
- [x] **Version Update:** `V20260128_1900` in `Constants.py`.

## 4. Verification
- Logs confirm `Acquired lock with PID 19324`.
- Logs confirm `Successfully created L-Trade order` for breakout reversal.
- UI confirmed auto-scrolling and auto-creation of EURAUD_C card.
