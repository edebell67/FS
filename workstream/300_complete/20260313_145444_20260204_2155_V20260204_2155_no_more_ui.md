[V20260204_2155] Enforcing Bucket Priority & Purging Manual UI Entries

## 1. Requirement
The user wants to eliminate `source: "ui"` (manual) entries from the live grid when Trade Buckets are active to avoids duplicates and confusion. "no more - source:UI".

## 2. Plan of Approach
I will update the background reconciliation logic in `trade_viewer_api.py`. When any Trade Bucket is live, the system will now automatically purge all `source: "ui"` entries from `grid_live.json`. This ensures that once you start using Buckets, they have absolute control and manual overrides are cleared out to prevent double-trading.

## 3. Checklist of Changes

*   **`trade_viewer_api.py`**:
    *   [x] Update `_reconcile_active_buckets` to include `ui` in the list of source tags to purge when buckets are active.
    *   [x] Update the log message to reflect that UI entries are also being removed.
*   **`grid_live.json`**:
    *   [x] Perform a manual cleanup to remove existing `ui` entries.
*   **`Constants.py`**:
    *   [x] Update version to `V20260204_2155`.

## 4. Verification
1.  Refresh the Dashboard.
2.  Verify that all `source: "ui"` entries are automatically removed from `grid_live.json` while buckets are live.
3.  Check that only `TB_...` entries remain.
