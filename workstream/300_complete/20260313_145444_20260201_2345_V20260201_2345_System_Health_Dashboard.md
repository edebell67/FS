# System Health Dashboard - Dual View

## 1. Requirement
Replace the single-mode JSON response for system health with a 2-column visual dashboard showing both Live and Sim health status simultaneously. Problematic items must flash red.

## 2. Plan
1. Refactor `trade_viewer_api.py` to support `?mode=all` and return nested health status for both 'live' and 'sim'.
2. Create `fs/system_health.html` with a dark-themed, 2-column layout.
3. Implement JS to fetch data and apply styling (Green for OK, Flashing Red for issues).
4. Update `sidebar.html` to link to the new HTML page.
5. Update `Constants.py` version.

## 3. Changes
* [x] **trade_viewer_api.py**: Refactor `health_check` and add `_get_health_status`.
* [x] **fs/system_health.html**: Create new dashboard file.
* [x] **fs/sidebar.html**: Update link.
* [x] **Constants.py**: Update version.
