# Plan: Optimize Live Trades Refresh

## 1. Understanding of Requirements
The user wants to improve the refresh mechanism of the "Live Trades" page.
The goal is to "refresh only data not the page".
Currently, `loadLiveTrades` clears the container (`container.innerHTML = ...`) and shows a loading spinner *before* fetching data, which causes the UI to flicker or reset completely on every refresh. This is disruptive, especially with auto-refresh enabled.

## 2. Plan of Approach
1.  **Modify `loadLiveTrades` in `fs/live_trades.html`**:
    *   Change the logic so that it **does not** clear the container initially if it's an auto-refresh or a silent update.
    *   The `isBackground` flag (or `isSilent`) is already present in the signature but might need better utilization.
    *   I will ensure that the "Loading..." spinner only appears on the *initial* load or explicit user-triggered refresh where a full clear is desired (optional). For auto-refresh, it should be silent.
    *   Crucially, when data arrives, update the DOM intelligently or just swap the innerHTML *after* the data is ready, rather than clearing it first.
    *   Actually, the current code clears *before* fetch: `container.innerHTML = '<div class="loading-wrap">...</div>';`. This is the cause of the full page refresh feel.
    *   I will modify this to only show the loading spinner if `!isBackground`.
    *   The user's request "refresh only data" implies that the table structure should ideally remain or update seamlessly. The current `renderTrades()` function rebuilds the entire table HTML string. While not a Virtual DOM, replacing the innerHTML *once data is ready* is much better than clearing -> waiting -> rendering.

2.  **Refine `toggleAutoRefresh`**:
    *   Ensure the interval calls `loadLiveTrades(true)` so that `isBackground` is true.
    *   In the existing code (lines 357 and 481), there are two `toggleAutoRefresh` functions? I need to check line 353 and 477. Yes, there seems to be duplicate code or a merge artifact in `live_trades.html`. I see `function toggleAutoRefresh` twice in the file content I viewed (lines 353 and 477). I should clean this up.
    *   Wait, looking at the file content in Step 111:
        *   Line 305: `function sortTrades...`
        *   Line 315: `function renderTrades...`
        *   Line 353: `function toggleAutoRefresh...` (First definition)
        *   Line 367: `function createTableSection...`
        *   Line 438: `function exportToCSV...`
        *   Line 464: `function loadConfig...`
        *   Line 477: `function toggleAutoRefresh...` (Second definition!)
    *   This duplication is a bug. I will remove the second definition and ensure the first one calls `loadLiveTrades(true)`.

3.  **Versioning**:
    *   Update version to `V20260202_1235`.

## 3. List of Changes

*   **`TradeApps/breakout/fs/live_trades.html`**:
    *   [x] Remove duplicate `toggleAutoRefresh` function at the end of the script.
    *   [x] In `loadLiveTrades(isBackground)`:
        *   Only set `container.innerHTML = Loading...` if `!isBackground`.
        *   Ensures that for background refreshes, the old data remains visible until the new data is ready to be rendered.
    *   [x] In `toggleAutoRefresh`:
        *   Ensure `setInterval` calls `loadLiveTrades(true)`. The first definition (line 357) uses `() => loadLiveTrades(true)`, which is correct. The second (line 481) uses `loadLiveTrades`, which would pass the (event?) or undefined, treating it as false (show spinner). I will keep the correct one.

*   **`TradeApps/breakout/fs/constants.py`**:
    *   [x] Update `VERSION` to `V20260202_1235`.

## 4. Validation
*   Verify that `autoRefreshCheck` triggers a silent update without showing the loading spinner.
