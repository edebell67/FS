# Plan: Disable Trading in Grid Live Monitor (V20260129_1200)

This plan restricts `grid_live_monitor.py` to a passive role, disabling all trade execution and signal detection.

## 1. Requirements
- Disable DNA and Breakout model monitoring (Functions 2 & 3).
- Disable L-Trade JSON generation and TWS order creation (Function 4).
- Disable metric mapping, safety guards, and trade lifecycle tracking (Functions 5, 6, 7).
- Disable dynamic sizing calculations (Function 9).
- Ensure all disabled code is commented out and preserved, not deleted.

## 2. Plan of Approach

### `grid_live_monitor.py`
- [x] Comment out the `connect_to_db` and SQL execution logic.
- [x] Comment out the scanning of JSON `live` folders for breakout trades.
- [x] Comment out the logic inside `monitor_loop` that triggers `create_l_trade_json`.
- [x] Comment out the profitability guard and open trade verification logic.
- [x] Comment out the trade closure detection logic.

### Versioning
- [x] Update `Constants.py` to `V20260129_1200`.

## 3. Impact Analysis
- No live trades will be executed by this script.
- The script will remain active as a system process but will take no action other than reading `grid_live.json`.
- UI leader updates will continue to be written to `grid_live.json` by the browser, but will not result in orders.
