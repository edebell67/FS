[V20260204_2158] Total Hard Block on Source:UI during Bucket Control

## 1. Requirement
The user has repeated "NO MORE - SOURCE:UI", indicating that manual UI activations should be strictly prohibited in `grid_live.json` when any Trade Bucket is active. My previous fix only purged them during GET reconciliation; I must also block them during POST updates.

## 2. Plan of Approach
I will update the `update_grid_live` (POST) route in `trade_viewer_api.py`. It currently blocks `rank_alert` and `frequency` sources if a bucket is live. I will extend this check to also block `ui` source updates. This ensures that even if a user clicks a button, the request is rejected by the server if a bucket is live.

## 3. Checklist of Changes

*   **`trade_viewer_api.py`**:
    *   [x] Modify `update_grid_live` to block `source == 'ui'` updates if `_any_trade_bucket_is_live(mode)` is True.
    *   [x] Add a specific print message for blocked UI updates.
*   **`Constants.py`**:
    *   [x] Update version to `V20260204_2200`.

## 4. Verification
1.  Try to manually toggle a chart while a Trade Bucket is Live.
2.  Verify the update is rejected and "NO MORE - SOURCE:UI" remains true in `grid_live.json`.
