# Plan: Show All Live Trades in Dashboard (FS Version Correction)

## 1. Understanding of Requirements
The user wants to remove the strict filtering of live trades in the dashboard.
ERROR IN PREVIOUS STEP: I modified the `db` version (`c:/Users/edebe/eds/TradeApps/breakout/db/live_trades.html`).
CORRECTION: The user explicitly requested to modify `c:/Users/edebe/eds/TradeApps/breakout/fs/live_trades.html`.

This plan outlines the steps to apply the same fix to the correct file in the `fs` directory.

## 2. Plan of Approach
1.  **Modify Frontend (`fs/live_trades.html`)**:
    *   Locate the `loadLiveTrades` function.
    *   Remove `.filter(...)` logic that restricts trades to `is_live_trade`, `order_sent_net`, or `order_sent_alt`.
    *   Note: The `fs` version of `live_trades.html` (viewed in step 28) doesn't seem to have the `live_only=true` parameter in the fetch call (line 260 calls `/api/trades?mode=${mode}&date=${date}`). It *does* have client-side filtering (lines 265-269).
    *   I will remove this client-side filtering.

2.  **Versioning**:
    *   Update `c:/Users/edebe/eds/TradeApps/breakout/fs/constants.py` to `V20260202_1215`.

## 3. List of Changes

*   **`TradeApps/breakout/fs/live_trades.html`**:
    *   [x] In `loadLiveTrades()`: Remove the filter:
        ```javascript
        const liveTrades = data.trades.filter(t =>
            t.is_live_trade === true ||
            t.order_sent_net === true ||
            t.order_sent_alt === true
        );
        ```
        And replace it with:
        ```javascript
        const liveTrades = data.trades;
        ```

*   **`TradeApps/breakout/fs/constants.py`**:
    *   [x] Update `VERSION` to `V20260202_1215`.

## 4. Validation
*   Verify that `fs/live_trades.html` uses all trades returned by the API.
