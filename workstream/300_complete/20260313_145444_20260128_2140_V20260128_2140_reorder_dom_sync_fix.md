# Plan: Card Reordering DOM Sync Fix (V20260128_2140)
**Date:** 2026-01-28 21:40

## 1. Requirements Understanding
- User reported that `<` and `>` buttons are not working (no visual change).
- Identified that while `activeOverlays` state was being swapped correctly, the DOM was not being rearranged because `updateCharts` only appended *new* cards.

## 2. Approach
- Modify `updateCharts` in `multi_chart.js`.
- Always call `grid.appendChild(card)` for every card in the `orderedGroupNames` loop.
- This utilizes the standard DOM behavior where appending an existing node moves it to the target position.
- Update version to `V20260128_2140`.

## 3. Implementation Checklist
- [x] Modify `updateCharts` to use `grid.appendChild` for all existing cards in the correct order.
- [x] Update version number.

## 4. Verification
- Verify that clicking `<` or `>` immediately swaps the card positions on the screen.
- Verify that the nested charts and legend values remain intact after the move.
