# Plan: Fix L-Trade Metadata Leakage and Version Update

## 1. Understanding of Requirements
Archiving fixed the L-trade promotion block by clearing the `max_live_trades` count. However, even when a trade is blocked, metadata like `sent_at` and `source_screen` is being written to the trade JSON prematurely. The goal is to ensure these fields only exist if the promotion actually succeeded.

## 2. Plan of Approach
1. **Modify `common.py`**:
    - Update `_trigger_grid_live_activation` to roll back all live-related metadata if `_create_tradeable_json` fails.
    - Ensure `is_live_trade` and `sent_at` are handled atomically relative to the order file creation.
2. **Update Version**:
    - Increment `VERSION` in `constants.py` to `V20260204_1245`.
3. **Verify**:
    - Check code for logical consistency.

## 3. List of Changes
- [ ] **`TradeApps/breakout/fs/common.py`**: Fix `_trigger_grid_live_activation` rollback logic (Line ~808).
- [ ] **`TradeApps/breakout/fs/constants.py`**: Update `VERSION` (Line 1).

## 4. Confirmation Needed
- [ ] Agreement on the fix for the `sent_at` leakage.

---
**Date**: 2026-02-04 12:45
**Version**: V20260204_1245
