# Plan: Remove Query Refresh Status Bar from All Multi-Charts [V20260126_1345]

This plan removes the "Query Refresh Status Bar" (loading overlay) from V2 and V3 Multi-Chart dashboards to ensure a seamless background refresh experience across all versions.

## 1. Requirements
*   **Remove UI Overlay**: Delete the `loadingOverlay` div and its styles from `multi_chart_v2.html` and `multi_chart_v3.html`.
*   **Remove JS Logic (if any)**: Ensure no orphaned loader calls remain in `multi_chart_v2.js` and `multi_chart_v3.js`.

## 2. Plan of Approach

### A. V2 Cleanup
*   **`multi_chart_v2.html`**:
    *   Remove `.loading-overlay`, `.loader-bar-container`, `.loader-bar-progress`, and `#loaderStatusText` CSS.
    *   Remove `<div id="loadingOverlay">` HTML.
*   **`multi_chart_v2.js`**:
    *   Confirm no `showLoader`/`hideLoader` calls exist (pre-checked, none found).

### B. V3 Cleanup
*   **`multi_chart_v3.html`**:
    *   Remove `.loading-overlay` CSS (if present, will check).
    *   Remove `<div id="loadingOverlay">` HTML.
*   **`multi_chart_v3.js`**:
    *   Confirm no `showLoader`/`hideLoader` calls exist.

### C. Standard Cleanup (Verification)
*   Already completed in V20260126_1340.

### D. Versioning
*   Update version to `V20260126_1345`.

## 3. Checklist
*   [ ] `multi_chart_v2.html`: CSS and HTML for loader removed.
*   [ ] `multi_chart_v3.html`: HTML for loader removed (Verify CSS location).
*   [ ] `constants.py`: Version updated.

## 4. Verification
*   Open V2 and V3 dashboards.
*   Click "Refresh" or wait for auto-refresh.
*   Confirm no loading bar appears.
