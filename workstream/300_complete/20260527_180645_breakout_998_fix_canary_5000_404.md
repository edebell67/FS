# Fix Canary 5000 404

Source: Direct user report with browser screenshot showing `localhost:5000/canary_tripwire_dashboard.html` returning 404.
Task Type: standard

Task Attributes:
- recurring_task: false
- looping_task: false
- splittable_task: false
- workflow_task: false

Task Summary: Restore access to the canary tripwire dashboard at the port 5000 URL used by the browser and runner.
Context: `TradeApps/breakout/fs/trade_viewer_api.py`, `TradeApps/breakout/fs/run_canary_dashboard.bat`, running local Python processes on port 5000.
Destination Folder: None
Dependency: None

Plan:
- [x] 1. Confirm the live failure and current listener state.
  - [x] Test: Request `http://127.0.0.1:5000/canary_tripwire_dashboard.html` and inspect listeners on port 5000.
  - Evidence: Initial request returned 404; `netstat -ano` showed multiple Python PIDs listening on port 5000.
- [x] 2. Determine whether code or runtime restart is required.
  - [x] Test: Validate the updated `fs/trade_viewer_api.py` serves the route in-process.
  - Evidence: Flask test client against checked-in `fs/trade_viewer_api.py` returned `200` with `Canary & Tripwire Simulator=True`.
- [x] 3. Apply the runtime/code fix and validate browser URL.
  - [x] Test: The port 5000 URL returns the canary dashboard HTML.
  - Evidence: Stopped stale Python listeners on port 5000, started `python trade_viewer_api.py` from `TradeApps/breakout/fs`, and `Invoke-WebRequest` returned `status=200 title=Canary & Tripwire Simulator has_canary=True`.

Evidence:
Objective-Delivery-Coverage: 100%
Auto-Acceptance: true
- Evidence-Type: url
  - Artifact: http://127.0.0.1:5000/canary_tripwire_dashboard.html
  - Objective-Proved: The requested URL now serves the canary tripwire dashboard.
  - Status: captured
- Evidence-Type: test_output
  - Artifact: `status=200 title=Canary & Tripwire Simulator has_canary=True`
  - Objective-Proved: Port 5000 now returns the expected canary dashboard HTML.
  - Status: captured
- Evidence-Type: user_feedback
  - Artifact: User message: "ok verified"
  - Objective-Proved: User confirmed the browser URL is working after the stale port-5000 server restart.
  - Status: captured

Implementation Log:
- 2026-05-27 18:06: Confirmed browser screenshot shows 404 and shell request to port 5000 returns 404.
- 2026-05-27 18:08: Confirmed checked-in `fs/trade_viewer_api.py` serves the dashboard route correctly in-process, isolating the issue to stale runtime listeners.
- 2026-05-27 18:09: Stopped stale Python listeners on port 5000 and restarted `trade_viewer_api.py` from `TradeApps/breakout/fs`.
- 2026-05-27 18:10: User verified the browser page is working.

Changes Made:
- Runtime-only fix: restarted the port 5000 dashboard server from `TradeApps/breakout/fs`.

Validation:
- Initial `Invoke-WebRequest http://127.0.0.1:5000/canary_tripwire_dashboard.html` returned 404.
- Flask test client against local `fs/trade_viewer_api.py` returned `200` with `Canary & Tripwire Simulator=True`.
- After restart, `Invoke-WebRequest http://localhost:5000/canary_tripwire_dashboard.html` returned `status=200 title=Canary & Tripwire Simulator has_canary=True`.
- `netstat -ano | Select-String ':5000'` shows a single listener PID `40292` on `0.0.0.0:5000`.
- User confirmed verification in the in-app browser with message: `ok verified`.

Risks/Notes:
- Stale Python listeners were stopped to free port 5000 and restart the updated `fs` server.

Completion Status: Complete at 2026-05-27 18:09.
