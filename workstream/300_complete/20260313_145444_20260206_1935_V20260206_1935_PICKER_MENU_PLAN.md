# Plan: Add Sidebar Menu to AI Picker [V20260206_1935]

The goal is to integrate the standard sidebar menu into the `ai_picker.html` page to provide consistent navigation across the application.

## 1. Requirements

- Add the sidebar container to `ai_picker.html`.
- Use `sidebar-loader.js` to dynamically load the menu.
- Ensure the layout is compatible with the fixed sidebar (using `with-sidebar` class).
- Maintain the premium aesthetics of the AI Picker while adding navigation.

## 2. Plan of Approach

1.  **Modify `ai_picker.html`**:
    - Add `<div id="sidebar-container"></div>` as the first child of `<body>`.
    - Add the `with-sidebar` class to the `<body>` element. This class is already defined in `sidebar.css` and handles the necessary left padding.
    - Include `<script src="sidebar-loader.js"></script>` at the end of the `<body>`.
    - Review the `.container` style in `ai_picker.html`. It currently has `max-width: 1400px` and `margin: 0 auto`. With a sidebar, `margin: 0 auto` might center it relative to the whole screen or the remaining space. I'll change it to `margin: 20px` to better align with other dashboard pages.

## 3. List of Changes

- **`TradeApps/breakout/fs/ai_picker.html`**:
    - [ ] Add `with-sidebar` class to `<body>`.
    - [ ] Inject `<div id="sidebar-container"></div>`.
    - [ ] Inject `<script src="sidebar-loader.js"></script>`.
    - [ ] Adjust `.container` margin for better alignment with sidebar.

## 4. Verification Plan

- [ ] Open `ai_picker.html` in the browser.
- [ ] Verify the sidebar is visible on the left.
- [ ] Verify that the "AI Picker" link in the sidebar is highlighted as active.
- [ ] Verify that the main content does not overlap with the sidebar.
- [ ] Verify that navigation links in the sidebar work.
