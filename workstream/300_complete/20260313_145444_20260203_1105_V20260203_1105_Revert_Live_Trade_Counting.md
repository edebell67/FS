# Revert Live Trade Counting Logic

## 1. Requirement
The user requested to restrict the `max_live_trades` check to only consider the `is_live_trade` flag, confirming that checks for `order_sent_*` or `is_live_scheduled` are superfluous.

## 2. Plan of Approach
1.  **Modify `common.py`**:
    *   Revert the logic in `_get_total_open_live_trades`.
    *   Condition will be: `if data.get('status') == 'OPEN' and bool(data.get('is_live_trade')):`
2.  **Update Version**:
    *   Update `constants.py` to `V20260203_1105`.

## 3. Checklist
- [x] Modify `_get_total_open_live_trades` in `common.py` to check only `is_live_trade`.
- [x] Update `constants.py` version.
