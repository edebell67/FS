[V20260210_1305] Fix Bias Alignment (P&L Driven)
**Current Status**: PLANNED (2026-02-10 13:05)
**Objective**: Align the AI calculated bias and UI heat-mapping with P&L performance rather than trade counts.

## 1. Problem Statement
The user reported a misalignment between the calculated bias and the UI. 
- `automated_strategy_picker.py` has a comment indicating it should use P&L for bias, but the implementation is still count-based.
- `strategy_performance.html` heat-mapping (green bars) is based on average trade counts, which can conflict with P&L performance.
- Global stats show Buy is losing ~$79k while Sell is losing ~$18k today, yet bias was reported as BUY due to slightly higher trade counts.

## 2. Plan of Approach
1.  **`automated_strategy_picker.py`**:
    *   Change `day_bias` calculation to use `total_buy_net >= total_sell_net`.
    *   Update "Stable Bias" jitter logic to use `pnl_threshold` (from config) instead of a 5-trade count difference.
    *   Change `recent_bias` to use `recent_buy_pnl >= recent_sell_pnl` (matching `pnl_bias`).
2.  **`strategy_performance.html`**:
    *   Update `renderTable` to calculate `buyIsDominant` based on `totalBuyNet >= totalSellNet` across the displayed dataset.
    *   This ensures the visual highlighting matches the most profitable (or least losing) side.
3.  **`Constants.py`**:
    *   Update version to `V20260210_1305`.

## 3. List of Changes
*   **`TradeApps/breakout/fs/automated_strategy_picker.py`**:
    *   [ ] Fix `day_bias` logic to use P&L net return.
    *   [ ] Fix `recent_bias` logic to use P&L net return.
    *   [ ] Update stable jitter check to use P&L spread.
*   **`TradeApps/breakout/fs/strategy_performance.html`**:
    *   [ ] Change `buyIsDominant` to use P&L instead of averages.
*   **`DataInsights/src/Constants.py`**:
    *   [ ] Update VERSION to `V20260210_1305`.

## 4. Verification
*   Run `automated_strategy_picker.py` and verify it now reports SELL (since -18k > -79k).
*   Refresh `strategy_performance.html` and verify the background bars highlight Sell columns more brightly.
*   Check that "Current Bias" at the top shows SELL.
