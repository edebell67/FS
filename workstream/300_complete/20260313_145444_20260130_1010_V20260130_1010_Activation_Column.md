# Plan: Add Activation Column to Frequency Explorer

**Version:** V20260130_1010
**Date:** 2026-01-30 10:10

## 1. Understanding of Requirements
The user wants to add an "Activate" column to the Frequency Explorer (Leaderboard and Consistency tables). This column will contain a button for each row. Clicking the button should add the row's Product and Strategy to `C:\Users\edebe\eds\TradeApps\breakout\fs\grid_live.json` in the same format as existing entries.

## 2. Plan of Approach
1.  **Backend Support**: Use existing `/api/grid_live` endpoints in `trade_viewer_api.py`.
2.  **UI Updates**:
    *   Add "Actions" or "Activate" header to both tables in `frequency_explorer.html`.
    *   Add the "Activate" button to each row.
    *   Implement `toggleActivation(product, strategy)` JavaScript function.
3.  **Activation Logic**:
    *   Fetch current `grid_live` state from `/api/grid_live`.
    *   Check if the combination is already active.
    *   If not active, add it with `metric: "net"` and `group: "[Product]"` (to match requested format examples).
    *   If active, remove it (to provide a toggle functionality, or just keep it as "Add if missing"). User asked to "add the row", so I'll focus on adding/enabling.
    *   Send POST to `/api/grid_live` to update the backend.
4.  **Feedback**: Show a visual cue (e.g., button color change or icon change) to indicate the combination is active.

## 3. List of Changes

### [x] 1. Update Version
- **File**: `C:\Users\edebe\eds\TradeApps\breakout\fs\constants.py`
- Change version to `V20260130_1010`.

### [x] 2. Update Frequency Explorer UI
- **File**: `C:\Users\edebe\eds\TradeApps\breakout\fs\frequency_explorer.html`
- Add "Actions" column header to table templates.
- Update `renderTable` and `renderConsistencyList` to include the action button.
- Add `activeGrid` state variable to track what's currently in `grid_live.json`.

### [x] 3. Implement Activation Logic
- **File**: `C:\Users\edebe\eds\TradeApps\breakout\fs\frequency_explorer.html`
- Add `fetchGridLive()` to load current monitoring state.
- Add `toggleGridLive(product, strategy)` to handle adding/removing from `grid_live.json`.
- Integrate `fetchGridLive` into `fetchData`.

## 4. Verification Plan
1. Open Frequency Explorer.
2. Observe new "Actions" column with "Activate" buttons.
3. Click "Activate" on a row.
4. Verify `grid_live.json` is updated with the new entry (Strategy + Product + metric:net + group:product).
5. Verify button state updates (e.g. turns green or says "Monitoring").
