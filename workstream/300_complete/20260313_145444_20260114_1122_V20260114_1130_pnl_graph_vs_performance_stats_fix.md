# Fix P&L Graph vs Strategy Performance Stats Discrepancy

## Goal Description

The Multi-Overlay P&L Graph and Strategy Performance Stats show different values because they use different data sources and filtering logic:

- **P&L Graph**: Uses `_summary_net.json` generated by `summary_net_generator.py`, which **excludes virtual trades** (`vt_*.json` files)
- **Strategy Performance Stats**: Appears to load all trade files directly, likely **including virtual trades**

This causes the charts to show different cumulative P&L values.

## Root Cause Analysis

### P&L Graph Data Source (`summary_net_generator.py`)
```python
# Line 63: Explicitly excludes virtual trades
if fname.startswith("_") or fname.startswith("vt_"): 
    continue
```

### Strategy Performance Stats Data Source
Need to verify what data source it uses, but based on the discrepancy, it likely includes virtual trades or uses a different aggregation method.

## User Review Required

> [!IMPORTANT]
> **Decision Required:** Should virtual trades be included or excluded from both views?
> 
> **Option A:** Exclude virtual trades from both (recommended for consistency)
> - P&L Graph: Already excludes them ✓
> - Strategy Performance: Need to add filter
> 
> **Option B:** Include virtual trades in both
> - P&L Graph: Remove `vt_` filter from summary_net_generator.py
> - Strategy Performance: Already includes them ✓

> [!WARNING]
> There may also be a difference in how OPEN vs CLOSED trades are handled. Need to investigate Strategy Performance Stats data loading logic.

## Investigation Steps

### 1. Identify Strategy Performance Stats Data Source

**Files to check:**
- [strategy_performance.html](file:///C:/Users/edebe/eds/TradeApps/breakout/strategy_performance.html)
- Look for JavaScript that loads trade data
- Check if it uses an API endpoint or loads files directly

### 2. Compare Filtering Logic

**Current P&L Graph filters:**
- ✓ Excludes files starting with `_` (summary files)
- ✓ Excludes files starting with `vt_` (virtual trades)
- ✓ Filters by date (only trades from folder date)
- ? Status filter (OPEN vs CLOSED) - need to verify

**Strategy Performance filters:**
- ? Virtual trade handling - unknown
- ? Date filtering - unknown
- ? Status filtering - unknown

### 3. Compare Aggregation Logic

**P&L Graph:**
- Groups by `(strategy, product)` tuple
- Cumulative sum over time
- Separate buy/sell tracking

**Strategy Performance:**
- Need to verify grouping logic
- Need to verify if it's cumulative or snapshot

## Proposed Changes

### Option A: Exclude Virtual Trades from Both (Recommended)

#### [INVESTIGATE] [strategy_performance.html](file:///C:/Users/edebe/eds/TradeApps/breakout/strategy_performance.html)
- Identify data loading mechanism
- Check current filtering logic
- Determine if virtual trades are included

#### [MODIFY] Strategy Performance Data Loading
- Add filter to exclude `vt_*.json` files
- Ensure date filtering matches `summary_net_generator.py`
- Ensure status filtering is consistent

---

### Option B: Include Virtual Trades in Both

#### [MODIFY] [summary_net_generator.py](file:///C:/Users/edebe/eds/TradeApps/breakout/summary_net_generator.py)
- Remove `vt_` exclusion from line 63
- Update logic to handle virtual trade structure
- Ensure virtual trades are properly aggregated

---

## Verification Plan

### Manual Verification

1. **Compare Totals:**
   ```
   1. Open P&L Graph for date 2026-01-14 (live mode)
   2. Note the final cumulative value for a specific strategy/product
   3. Open Strategy Performance Stats for same date
   4. Find the same strategy/product combination
   5. Compare Total Net values
   6. They should match exactly
   ```

2. **Check Virtual Trade Count:**
   ```
   1. Count virtual trade files: 
      ls C:\Users\edebe\eds\TradeApps\breakout\json\live\2026-01-14\virtual\vt_*.json | wc -l
   2. If excluding virtual trades:
      - Verify P&L Graph total = Strategy Performance total
   3. If including virtual trades:
      - Verify totals include virtual trade PnL
   ```

3. **Verify Consistency Across Dates:**
   ```
   1. Test with multiple dates (2026-01-12, 2026-01-13, 2026-01-14)
   2. Ensure both views show same values for each date
   ```

### Automated Tests

No existing automated tests found for these views. Manual verification is required.

## Next Steps

1. **Investigate Strategy Performance Stats data source** (currently unknown)
2. **Get user decision** on whether to include or exclude virtual trades
3. **Implement chosen option**
4. **Verify consistency** using manual verification steps above

## Files to Investigate

- [ ] [strategy_performance.html](file:///C:/Users/edebe/eds/TradeApps/breakout/strategy_performance.html) - Find data loading logic
- [ ] Check if there's a `strategy_performance.js` file
- [ ] Check for API endpoints that serve strategy performance data
