# Plan: Fix Dropdown Visibility in P&L Graph

**Date**: 2026-01-20 00:43
**Version**: V20260120_0045
**Description**: Fix the issue where dropdown options have a white background and white font, making them unreadable.

## Proposed Changes

### 1. CSS Fix
- [x] Update `pnl_graph.css` to explicitly set the background color and text color of `option` elements within `select` menus.
- [x] Use a dark background (`#0f1225`) that matches the theme to ensure white text is visible.

### 2. Versioning & Cache Busting
- [x] Update `VERSION` in `constants.py` to `V20260120_0045`.
- [x] Update the `v` parameter in `pnl_graph.html` for the CSS link to force a browser refresh.

### 3. Documentation
- [x] Update the main redesign plan to reflect this fix.

## Verification Plan
1. [ ] Open all dropdowns on the P&L Graph page.
2. [ ] Verify that the options have a dark background and clearly visible white text.
3. [ ] Confirm the new version number is visible in the sidebar.
