# Plan: Frequency Explorer Enhancements (V20260128_2120)
**Date:** 2026-01-28 21:20

## 1. Requirements Understanding
- Reduce leaderboard rows to improve focus.
- Add "Recent Consistency" section to view top performers over a sliding window (5, 30, 60 mins).
- Show metrics: Net Change in window, Appearance count, and Average Weight (rank-based).
- Goal: Identify consistent performers in the most recent trading period.

## 2. Approach
- Modify `frequency_explorer.html`.
- Set `TABLE_MAX_ROWS = 8`.
- Insert a new `data-panel` for "Recent Consistency".
- Add a dropdown to select the window duration.
- Implement `renderConsistencyList()`:
    - Determine sliding window relative to the *last* available snapshot.
    - Aggregate appearances, net delta, and ranking weights for all strategies in that window.
    - Rank primarily by Appearance Count to highlight consistency.
    - Display top 5.
- Integrate with `fetchData()` for automatic updates.

## 3. Implementation Checklist
- [x] Reduce `TABLE_MAX_ROWS` to 8.
- [x] Add "Recent Consistency" UI section with dropdown and table.
- [x] Implement windowed aggregation logic in `renderConsistencyList()`.
- [x] Ensure `renderConsistencyList()` is called after every data fetch.
- [x] Update version number to `V20260128_2120`.

## 4. Verification
- Verify the new panel appears below the summary cards.
- Verify the dropdown correctly filters data for 5, 30, and 60 minutes.
- Confirm the main Leaderboard is now capped at 8 rows.
