# Strategy Selection Optimization (2026-02-06)

This plan outlines the enhancement of the Automated Strategy Selector to prioritize profitability percentage (win rate) while maintaining high profitability standards.

## 1. Understanding of Requirements
- **Core Requirement**: Change the selection logic to prioritize the "best profitability percentage" (average of buy and sell win rates).
- **LLM Clarification**: The agent is a **deterministic Python script** (not an LLM), ensuring consistent, rule-based selection. However, I (the LLM) wrote the code for it.
- **Criteria**:
    1.  Max Win Rate (average of `buyPercent` and `sellPercent`).
    2.  Minimum `trade_count` (e.g., >= 3) to ensure statistical significance.
    3.  Positive `total_net`.
    4.  Tie-breaker: Highest `total_net`.

## 2. Plan of Approach
1.  **Refine Logic**: Update `automated_strategy_selector.py` to calculate a `win_rate_avg` for each strategy.
2.  **Sorting Priority**:
    - Primary: `win_rate_avg` (Descending).
    - Secondary: `total_net` (Descending).
3.  **Minimum Activity Guard**: Ensure selected strategies have at least 3 trades to prevent 100% win rate "flukes" from single trades.
4.  **Versioning**: Update system version to `V20260206_1100`.

## 3. List of Changes
*   **`automated_strategy_selector.py`**:
    *   [ ] Update selection logic to calculate and sort by average win rate.
    *   [ ] Increase minimum trade count to 3.
*   **`constants.py`**:
    *   [ ] Update version to `V20260206_1100`.

## 4. Verification Plan
*   [ ] Run the script and verify that it picks a strategy with the highest win rate among those with >= 3 trades.
*   [ ] Verify the selection in `grid_live.json`.
