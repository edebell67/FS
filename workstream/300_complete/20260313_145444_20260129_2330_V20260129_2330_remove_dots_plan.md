# Plan: Remove Dots from Multi-Chart Grids (V20260129_2330)

This plan outlines the removal of data points (dots) and live markers from the Multi-Chart P&L Grid to match the clean visual style of the master P&L Graph.

## 1. Understanding of Requirements
The user finds the dots in the Multi-Chart P&L Grid confusing and prefers the clean line style used in `pnl_graph.html`. I will remove the `pointRadius` and any dynamic marker logic that highlights specific trades or live orders.

## 2. Plan of Approach
1.  **Modify `multi_chart.js`**:
    *   Set global `pointRadius: 0` for all datasets.
    *   Disable the "Live Marker" fetch and style override logic.
2.  **Modify `multi_chart_v2.js`**:
    *   Remove the `pointRadii` and `pointStyles` array logic.
    *   Set `pointRadius: 0` for all datasets.
3.  **Update Version**:
    *   Update `Constants.py` to `V20260129_2330`.
4.  **Verification**:
    *   Ensure the charts still render correctly with `stepped: true` but without visible dots.

## 3. List of Changes

### `multi_chart.js`
- [x] In `updateCharts()`:
    - [x] Set `pointRadius: 0`, `pointHoverRadius: 0`, `pointHitRadius: 5` (for tooltip accuracy).
    - [x] Comment out or remove the `fetchProms` logic that overlays `pointStyle` and `pointRadius` based on live trade detection.

### `multi_chart_v2.js`
- [x] In `updateCharts()`:
    - [x] Remove `pointStyles`, `pointRadii`, `pointColors` array population.
    - [x] Set `pointRadius: 0` in the dataset object.

### `multi_chart_v3.js`
- [x] Applied identical clean-up to V3 as done for V2.

### `Constants.py`
- [x] Update VERSION to `V20260129_2330`.

## 4. Confirmation
Please confirm if you would like me to proceed with this visual cleanup.
