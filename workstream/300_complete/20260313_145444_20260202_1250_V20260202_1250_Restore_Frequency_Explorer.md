# Plan: Restore Frequency Explorer Corrupted Functions

## 1. Understanding of Requirements
The `frequency_explorer.html` file was severely corrupted by a previous tool usage.
A large chunk of code (~300 lines) was deleted, merging `applyDateFilter` directly into `showDrilldown` (where `showDrilldown`'s signature was lost and its body pasted into `applyDateFilter`).
This caused:
1.  `applyDateFilter` to be incomplete and malformed.
2.  `clearDateFilter`, `renderTable`, `renderConsistencyList`, `renderTimeline`, `buildTimelineOptions`, `handleTimelineRangeChange`, `setupAutoRefresh`, and `showDrilldown` (signature) to be missing.
3.  The file end is also corrupted (`isLeaderMonitored` is broken).

I must restore the missing functions and fix the file structure.

## 2. Plan of Approach
1.  **Repair `applyDateFilter`**:
    *   It currently ends at line 983 with `timelineRange = 'recent';` and then immediately jumps to `const title = ...` which belongs to `showDrilldown`.
    *   I need to close `applyDateFilter` properly, including the new historical check logic I *intended* to add.

2.  **Restore Missing Functions**:
    *   I will insert the full block of missing functions:
        *   `clearDateFilter` (with historical check)
        *   `isHistoricalView` & `checkHistoricalMode` (new helpers)
        *   `renderTable`
        *   `renderConsistencyList`
        *   `renderTimeline`
        *   `buildTimelineOptions`
        *   `handleTimelineRangeChange`
        *   `setupAutoRefresh` (with historical check)
        *   `showDrilldown` (the beginning of it)

3.  **Fix `showDrilldown`**:
    *   The code currently jumps from line 983 to the *body* of `showDrilldown` (starting at `const title = ...`).
    *   I need to ensure the `showDrilldown` function signature is compliant.

4.  **Fix File End / `isLeaderMonitored`**:
    *   The file seems truncated at the end. I need to check if `toggleGridLive` and `isLeaderMonitored` and the rest of the bottom logic is intact or needs restoring.
    *   Based on Step 162, the file ends with a broken `isLeaderMonitored`. I will overwrite the end of the file with the correct logic.

## 3. List of Changes

*   **`TradeApps/breakout/fs/frequency_explorer.html`**:
    *   [x] **Major Restore**: Replace the text from line 980 (`function applyDateFilter() {`) down to line 1041 (`modal.style.display = 'flex'; }`) with the CORRECT sequence of functions.
        *   This serves to disconnect the fused `applyDateFilter`-`showDrilldown` hybrid and insert all the missing middle functions.
        *   I will verify I am not duplicating `showDrilldown`'s body. The fused code contains the body of `showDrilldown`. My replacement should end with the *start* of `showDrilldown` and then I'll let the existing body (lines 984-1041) remain? No, line 984 IS the start of the body.
        *   Better approach: Replace the entire fused block (980-1041) with the [Correct `applyDateFilter`] + [All Missing Functions] + [Correct `showDrilldown` (full function)]. This is safer.

    *   [x] **Fix File Tail**:
        *   Replace the broken `isLeaderMonitored` at the end (roughly line 1561) with the correct `toggleGridLive`, `shouldShowRankAlert`, `checkRankChanges`, etc., if they are missing?
        *   Wait, the `checkRankChanges` and others *were* present in Step 162 (lines 1210+).
        *   The file end was just `isLeaderMonitored`.
        *   I will check if `toggleGridLive` is missing. Step 137 showed `toggleGridLive` before `checkRankChanges`?
        *   Actually, `toggleGridLive` is called by the UI.
        *   In Step 162, `toggleGridLive` is NOT visible. It should be before `checkRankChanges` or after `fetchGridLive`.
        *   I will re-add `toggleGridLive` and `isLeaderMonitored` properly.

## 4. Execution details
I'll use `multi_replace_file_content` or `replace_file_content` to essentially rewrite the middle section.

**Middle Section Payload:**
```javascript
        function applyDateFilter() {
            const input = document.getElementById('dateInput');
            selectedDate = input && input.value ? input.value : null;
            timelineRange = 'recent';
            const rangeSelect = document.getElementById('timelineRangeSelect');
            if (rangeSelect) rangeSelect.value = 'recent';
            
            checkHistoricalMode(); // [V20260202_1245]
            
            fetchData();
        }

        function clearDateFilter() {
            const input = document.getElementById('dateInput');
            if (input) input.value = '';
            selectedDate = null;
            timelineRange = 'recent';
            const rangeSelect = document.getElementById('timelineRangeSelect');
            if (rangeSelect) rangeSelect.value = 'recent';
            
            checkHistoricalMode(); // [V20260202_1245]
            
            fetchData();
        }

        // [V20260202_1245] Helper to detect historical view
        function isHistoricalView() {
            if (!selectedDate) return false;
            const today = new Date().toISOString().split('T')[0];
            return selectedDate !== today;
        }

        function checkHistoricalMode() {
            const isHist = isHistoricalView();
            const chk = document.getElementById('autoRefresh');
            if (!chk) return;

            if (isHist) {
                if (refreshTimer) {
                    clearInterval(refreshTimer);
                    refreshTimer = null;
                }
                chk.checked = false;
                chk.disabled = true;
                chk.parentElement.title = "Auto-refresh disabled for historical data";
                chk.parentElement.style.opacity = "0.5";
            } else {
                chk.disabled = false;
                chk.parentElement.title = "";
                chk.parentElement.style.opacity = "1";
            }
        }

        // ... [Insert renderTable, renderConsistencyList, renderTimeline, buildTimelineOptions, handleTimelineRangeChange, setupAutoRefresh] ...

        function showDrilldown(product, strategy) {
            const modal = document.getElementById('drilldownModal');
            // ... (rest of start of showDrilldown)
```
I need to be careful to stitch it exactly to where `const title = document.getElementById('modalTitle');` begins if I don't replace the whole thing.

Given the mess, replacing the whole block `980` to `1041` is safest.

**Tail Section:**
Append or replace the broken tail with the definitions of `fetchGridLive`, `toggleGridLive`, etc.

**Versioning**:
Update `VERSION` to `V20260202_1250`.
