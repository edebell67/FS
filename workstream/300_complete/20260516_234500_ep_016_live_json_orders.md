# Task: EP_016 Live JSON Order Execution & Daily Profit Target
# Date: 2026-05-16 23:45
# Version: V20260516_2345

## Task Type
Implementation / Execution

## Destination Folder
epics/ep_016_turning_point_pattern_engine/logic

## Dependency
20260516_233000_ep_016_live_analyzer_persistence.md

## Plan
1. **Implement CLI Parameters**:
    - [ ] Add `--live` (boolean) to enable JSON order generation.
    - [ ] Add `--target_pips` (float) to set the daily profit ceiling.
    - Evidence: CLI help output shows new flags.

2. **Implement Order Generation Logic**:
    - [ ] Create `generate_order_json(action, price, guid)` function.
    - [ ] Format: `breakout_{GUID}_{SYMBOL}_V6_{ACTION}_{TIMESTAMP}_{STATUS}_tradeable.json`.
    - [ ] Destination: `Z:\trades_rt\ORDERS\`.
    - [ ] Content: Match the schema found in `trades_rt3/orders/`.
    - Evidence: Sample JSON generated in Z drive.

3. **Implement Daily Target Guard**:
    - [ ] Update `run_analyzer` to check `cumulative_net_pips >= target_pips`.
    - [ ] If target reached, stop generating JSON orders (but keep logging/capturing).
    - Evidence: Terminal log shows "Daily Target Reached. Live execution suspended."

4. **Integration with Persistence**:
    - [ ] Ensure `target_pips` and `live` mode status are saved in the state file.
    - Evidence: State file contains execution flags.

5. **Validation**:
    - [ ] Dry run with `--live` (using a dummy path) to verify file naming and content.
    - [ ] Verify that reaching 10 pips (test target) stops order generation.

## Evidence Inventory
- Objective-Delivery-Coverage: 0%
- Auto-Acceptance: false

## Implementation Log
- 2026-05-16 23:45: Task created to add real-world trade execution and profit protection.

## Completion Status
**IN PROGRESS**
