# 2026-02-14 V20260214_2000 Fixing Trade Bucket P&L

## 1. Understanding of Requirements
The user reported that trade buckets in the UI were showing zero P&L values ("undefined total_net"). The issue was identified as potentially caused by fragile strategy key parsing (missing spaces around pipes) or summary data lookup failures. The fix focuses on robust parsing, normalized inputs, and enhanced logging.

## 2. Plan of Approach
1.  **Enhance `trade_viewer_api.py`**:
    *   Normalize `mode` input to lowercase.
    *   Implement robust strategy key parsing (handle `|` vs ` | `).
    *   Add comprehensive logging for file loading and strategy matching.
    *   Log if summary data is missing or strategies not found.
2.  **Update `constants.py`**:
    *   Bump version to `V20260214_2000`.

## 3. List of Changes
*   **`TradeApps/breakout/fs/trade_viewer_api.py`**:
    *   [x] In `get_trade_buckets`: Normalize `mode` to lower case.
    *   [x] In `get_trade_buckets`: Add `print` statements logging requests and file paths.
    *   [x] In `get_trade_buckets`: Add fallback parsing for strategy keys (`strat_key.split('|')`).
    *   [x] In `get_trade_buckets`: Logging for missing summary data.
*   **`TradeApps/breakout/fs/constants.py`**:
    *   [x] Update `VERSION` to `V20260214_2000`.

## 4. Verification
*   Verified via script `test_bucket_logic.py` that `breakout_R_Rev_3_tp10.0_sl50.0` correctly maps to valid P&L values (e.g. -539.6) from `_summary_net.json`.
*   Verified via `curl` that `get_trade_buckets` returns populated `total_net` values (e.g. 70.5, -360.3).
