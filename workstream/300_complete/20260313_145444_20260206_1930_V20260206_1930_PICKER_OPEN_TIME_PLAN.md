# Plan: Update OPEN Trades Time in AI Picker [V20260206_1930]

The goal is to ensure that trades marked as `OPEN` in the Targeted Strategies (AI Picker) view display the "current time" of the scan instead of their entry time. This makes it clear that the trade is still active at the time the report was generated.

## 1. Requirements

- Modify `automated_strategy_picker.py` to set the `last_entry` field for `OPEN` trades to the latest timestamp found in the data (`latest_time`).
- Ensure the `last_update` field in the exported JSON reflects the data's current time (`latest_time`) when performing historical scans, or the system time when scanning live.
- Update `Constants.py` version.

## 2. Plan of Approach

1.  **Modify `TradeApps/breakout/fs/automated_strategy_picker.py`**:
    - Identify the part where the `results` list is populated.
    - Check the `status` of the `last_trade`.
    - If `status == 'OPEN'`, set `last_entry` to `latest_time.strftime('%H:%M:%S')`.
    - Otherwise, keep the original `entry_time` extraction.
2.  **Update `TradeApps/breakout/fs/constants.py`**:
    - Increment version to `V20260206_1930`.

## 3. List of Changes

- **`TradeApps/breakout/fs/automated_strategy_picker.py`**:
    - [ ] Modify `last_entry` logic to use `latest_time` for `OPEN` trades.
- **`TradeApps/breakout/fs/constants.py`**:
    - [ ] Update version to `V20260206_1930`.

## 4. Verification Plan

- [ ] Run `python automated_strategy_picker.py <date>` for a date with open trades.
- [ ] Inspect the generated `_targeted_strategies.json` (or the console output).
- [ ] Verify that trades with `last_status: "OPEN"` have `last_entry` matching the report's "Now" time.
- [ ] Verify that `CLOSED` trades still show their actual entry time.
