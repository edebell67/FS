# 20251219_1028_V20251219_1028_Breakout_Test_Execution

## 1. Understanding of Requirements
The user wants to run a test on `breakout.py` to verify that all recent updates (multi-product support, state persistence, profitability guard refactor, and guide price logic) are working correctly together.

## 2. Plan of Approach
1. **Prepare Execution**: I will run `python breakout.py` in the terminal.
2. **Observe Initialization**:
    - Verify that `DEFAULT_TRADE_PRODUCTS` are correctly loaded from `config.json`.
    - Verify that `State Persistence` scans the disk and potentially resumes trades.
    - Verify that the `Profitability Guard` initialized its scan.
3. **Observe Loop**:
    - Verify that it fetches quotes for all 4 products (GBP, EUR, AUD, NZD) in parallel.
    - Verify that it processes ticks without errors.
4. **Conclusion**: Stop the script after a few successful cycles to confirm stability.

## 3. Checklist of Verification
- [ ] Load `constants.py` version correctly.
- [ ] Initialize strategies for GBP, EUR, AUD, NZD.
- [ ] Perform `State Persistence` scan on startup.
- [ ] Successfully fetch latest quotes via ThreadPoolExecutor.
- [ ] Verify console logging for transparency (e.g., `[INFO]`, `[RESUME]`, `[GUARD]`).

## 4. Execution
Command: `python breakout.py`
Expected Duration: ~60 seconds to observe a few cycles.
