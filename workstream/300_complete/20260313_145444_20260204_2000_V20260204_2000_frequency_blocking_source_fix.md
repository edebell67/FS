# Plan: Frequency Blocking & Source Fix (V20260204_2000)

## 1. Requirements Understanding
- **Objective 1**: Ensure "Trade Bucket Live" disables "Frequency Notifier" updates. (Previously implemented, verified).
- **Objective 2**: Fix "SOURCE: ui" display in Activation Explorer (Currently showing stale/placeholder values).
- **Mutual Exclusion**: If Bucket is Live, Frequency Updates are blocked. If Frequency is re-enabled, Buckets are deactivated.

## 2. Checklist of Changes

- [x] **`trade_viewer_api.py`**:
    - [x] (VERIFIED) `update_grid_live` blocks incoming frequency updates if `_any_trade_bucket_is_live`.
    - [x] (VERIFIED) `update_config` deactivates buckets when `rank_alert_suspended` is toggled off.
    - [x] (FIXED) Update `_coerce_entry` and `_merge_entries` to preserve `source` (Fixed field stripping bug).
- [x] **`activations_explorer.html`**:
    - [x] Add cache-buster to the `/api/activations` fetch.
    - [x] Add a visual "Force Sync" button or ensure data is fresh.
    - [x] (Refinement) Change placeholder to `'-'` instead of `'ui'` to avoid confusion if empty.
- [x] **Versioning**:
    - [x] Update `fs/constants.py` to `V20260204_2000`.
    - [x] Update `db/constants.py` to `V20260204_2000`.

## 3. List of Changes

- **`C:\Users\edebe\eds\TradeApps\breakout\fs\activations_explorer.html`**:
    - Added `_t=${Date.now()}` to API calls to prevent browser caching.
    - Changed fallback display from `'ui'` to `'-'`.

## 4. Verification Plan
- [ ] Toggle a strategy in Multi-Chart.
- [ ] Open Activation Explorer and verify Source shows `frequency_...`.
- [ ] Verify that if a Trade Bucket is set to Live, the console logs in `trade_viewer_api.py` show blocked frequency updates.
