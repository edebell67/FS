# Plan: Implementation 1.2 - API Schema & Models [V20260219_1500]

**Project**: bizPA (Voice-First Small Trader Capture App)  
**Objective**: Implement the PostgreSQL database schema based on the Workstream 2 entity dictionary and create the initial data models.  
**Version**: V20260219_1500

## Checklist
- [x] 1. Translate WS2 Dictionary to PostgreSQL DDL - 100%
- [x] 2. Create Core Tables (capture_items, clients, jobs, etc.) - 100%
- [x] 3. Create Plugin-ready Tables (calendar, diary, revenue, etc.) - 100%
- [x] 4. Implement Audit & Sync Triggers - 100%
- [x] 5. Verify Schema Integrity (FK constraints & Indexes) - 100%

## Approach
1.  **PostgreSQL Translation**: Adapt the SQLite DDL from WS2 to PostgreSQL syntax (e.g., using `UUID` type, `TIMESTAMPTZ`, and `JSONB` for flexible metadata).
2.  **Schema Script**: Create a master `schema.sql` file in `src/models/`.
3.  **Audit Logic**: Implement a generic audit trigger to automatically populate the `audit_events` table on every INSERT/UPDATE/DELETE across core entities.
4.  **Security**: Ensure sensitive fields are identified for future application-level encryption if required.
5.  **Execution**: Use `psql` or a Node.js migration script to apply the schema to the target database.

## Verification
- Verify all 8 core tables exist in the database.
- Verify FK relationships correctly prevent orphaned records.
- Verify that an insert into `capture_items` successfully triggers an `audit_events` entry.
