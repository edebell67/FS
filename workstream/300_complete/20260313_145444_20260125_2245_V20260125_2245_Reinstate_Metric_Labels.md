# Plan: Reinstate Metric Labels ('A'/'N') in Multi-Chart V3

Date: 2026-01-25
Version: V20260125_2245

## 1. Objective
Reinstate the short labels ('A' for Alt or 'N' for Net) in the Multi-Chart Grid card headers to clearly indicate which metric is being plotted for each model.

## 2. Plan of Approach
1.  **Modify Frontend (`multi_chart_v3.js`)**:
    *   Update the `HTML` generation for the `group-value-item` to include the prefix `(A)` or `(N)` based on the metric type.
    *   Ensure this logic respects the `globalMetric` if no specific metric is set for the overlay.
2.  **Versioning**: Keep the current version V20260125_2245 as this is a refinement of the same day's work.

## 3. List of Changes
- [x] **`c:\Users\edebe\eds\TradeApps\breakout\fs\multi_chart_v3.js`**:
    *   Modify the `listEl.innerHTML` template inside `updateCharts()`.
    *   Add logical check for metric type and append `(A)` or `(N)` to the model name span.

## 4. Verification
- Open Multi-Chart V3.
- Observe the Legend list on each card.
- Verify `(A)` appears for Alt Net and `(N)` appears for Net Return.
