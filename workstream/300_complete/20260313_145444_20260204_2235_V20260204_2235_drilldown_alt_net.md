[V20260204_2235] Add Alt_net column to Trade Drilldown Modal

## 1. Requirement
The user wants to see the `Alt_net` value for each trade in the Drilldown Table. This column should be positioned between the `Result` and `Total` columns.

## 2. Plan of Approach
I will modify `multi_chart.html` to add the column header and `multi_chart.js` to populate, render, and allow sorting for this new column.

## 3. Checklist of Changes

*   **`multi_chart.html`**:
    *   [x] Add `<th onclick="sortDrilldownTable(5)">Alt Net <i class="fas fa-sort"></i></th>` and update subsequent column indices.
*   **`multi_chart.js`**:
    *   [x] In `showTradeDrilldown`: Capture `altNet: t.alt_net || 0` in the `currentDrilldownTrades` objects.
    *   [x] In `renderDrilldownTable`:
        *   [x] Calculate `altResCol` color based on `altNet` value.
        *   [x] Add the `<td>` for `altNet` between the result and total columns.
    *   [x] In `sortDrilldownTable`: Update the `keys` array to include `altNet` at index 5.
    *   [x] Update help text/colspan if necessary (it seems `colspan="9"` is used for empty states, should be `10`).
*   **`Constants.py`**:
    *   [x] Update version to `V20260204_2235`.

## 4. Verification
1.  Open the Multi-Chart P&L Grid.
2.  Click on a card to open the Trade Drilldown.
3.  Verify the new `Alt Net` column is present and correctly populated.
4.  Test sorting on the new column.
