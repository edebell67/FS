# WF-202 Sample Chart Contract

- Period return bars distinguish complete and incomplete periods.
- Rolling-return lines display window size and observation count.
- Missing periods are explicit zero/no-trade calendar observations only when the selected calendar policy requires them; missing data is never silently zero-filled.
- Tooltips include period timezone, start/end, trades, result basis, completeness and methodology version.

