# WF-401 UI/API Evidence Contract

States: `collecting data`, `insufficient evidence`, `sufficient evidence`, `stale`, and `error`. Sparse regimes display no performance claim. Sufficient responses include raw metrics, sample count, as-of time, methodology/definition versions, comparative baseline and limitation text.

