"use client";
import Link from "next/link";
import { useState } from "react";

type Props={role:string};
const workflow=[['1. Import businesses','/directoryadmin/import'],['2. Run field validation','/directoryadmin/validation'],['3. Select verification batch','/directoryadmin/verification-batches'],['4. Review claims','/directoryadmin/claims'],['5. Generate sites','/directoryadmin/pipeline'],['6. Send site preview links','/directoryadmin/site-previews']] as const;
export function AdminMenuModal({role}:Props){const [open,setOpen]=useState(false);const admin: Array<[string,string]>=[['Dashboard','/directoryadmin/dashboard'],['Pipeline','/directoryadmin/pipeline'],['Businesses','/directoryadmin/businesses'],...(role==='super_admin'?[['Visibility','/directoryadmin/visibility'] as [string,string],['News desk','/directoryadmin/news'] as [string,string]]:[]),...(['super_admin','admin','operations'].includes(role)?[['Claims','/directoryadmin/claims'] as [string,string],['Validation','/directoryadmin/validation'] as [string,string]]:[])];return <><button type="button" onClick={()=>setOpen(true)} className="rounded bg-brand-600 px-3 py-1.5 text-white hover:bg-brand-700">Admin menu</button>{open&&<div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/50 p-4" onMouseDown={()=>setOpen(false)}><section role="dialog" aria-modal="true" aria-label="Admin menu" className="max-h-[90vh] w-full max-w-md overflow-y-auto rounded-xl bg-white p-6 shadow-2xl" onMouseDown={e=>e.stopPropagation()}><div className="flex justify-between"><h2 className="text-xl font-semibold">Admin menu</h2><button onClick={()=>setOpen(false)} aria-label="Close admin menu">×</button></div><h3 className="mt-5 text-xs font-semibold uppercase text-slate-500">Admin menus</h3><div className="mt-2 grid grid-cols-2 gap-2">{admin.map(([label,href])=><Link key={href} href={href} onClick={()=>setOpen(false)} className="rounded border p-3 text-sm font-medium hover:bg-brand-50">{label}</Link>)}</div><h3 className="mt-6 text-xs font-semibold uppercase text-slate-500">Workflow menus</h3><div className="mt-2 space-y-2">{workflow.map(([label,href])=><Link key={href} href={href} onClick={()=>setOpen(false)} className="block rounded border p-3 text-sm font-medium hover:bg-brand-50">{label}</Link>)}</div></section></div>}</>}
